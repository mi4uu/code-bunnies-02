# Crate Documentation

**Version:** 0.36.7

**Format Version:** 43

# Module `object`

# `object`

The `object` crate provides a unified interface to working with object files
across platforms. It supports reading relocatable object files and executable files,
and writing relocatable object files and some executable files.

## Raw struct definitions

Raw structs are defined for: [ELF](elf), [Mach-O](macho), [PE/COFF](pe),
[XCOFF](xcoff), [archive].
Types and traits for zerocopy support are defined in the [`pod`] and [`endian`] modules.

## Unified read API

The [`read`] module provides a unified read API using the [`read::Object`] trait.
There is an implementation of this trait for [`read::File`], which allows reading any
file format, as well as implementations for each file format.

## Low level read API

The [`read#modules`] submodules define helpers that operate on the raw structs.
These can be used instead of the unified API, or in conjunction with it to access
details that are not available via the unified API.

## Unified write API

The [`mod@write`] module provides a unified write API for relocatable object files
using [`write::Object`]. This does not support writing executable files.

## Low level write API

The [`mod@write#modules`] submodules define helpers for writing the raw structs.

## Build API

The [`mod@build`] submodules define helpers for building object files, either from
scratch or by modifying existing files.

## Shared definitions

The crate provides a number of definitions that are used by both the read and write
APIs. These are defined at the top level module, but none of these are the main entry
points of the crate.

## Modules

## Module `endian`

**Attributes:**

- `#[macro_use]`

Types for compile-time and run-time endianness.

```rust
pub mod endian { /* ... */ }
```

### Types

#### Enum `Endianness`

An endianness that is selectable at run-time.

```rust
pub enum Endianness {
    Little,
    Big,
}
```

##### Variants

###### `Little`

Little endian byte order.

###### `Big`

Big endian byte order.

##### Implementations

###### Trait Implementations

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Copy**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> Endianness { /* ... */ }
    ```

- **Endian**
  - ```rust
    fn from_big_endian(big_endian: bool) -> Option<Self> { /* ... */ }
    ```

  - ```rust
    fn is_big_endian(self: Self) -> bool { /* ... */ }
    ```

- **Eq**
- **RefUnwindSafe**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Freeze**
- **UnwindSafe**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Hash**
  - ```rust
    fn hash<__H: $crate::hash::Hasher>(self: &Self, state: &mut __H) { /* ... */ }
    ```

- **Default**
  - ```rust
    fn default() -> Endianness { /* ... */ }
    ```

- **Sync**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Send**
- **StructuralPartialEq**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Unpin**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **PartialEq**
  - ```rust
    fn eq(self: &Self, other: &Endianness) -> bool { /* ... */ }
    ```

#### Struct `LittleEndian`

Compile-time little endian byte order.

```rust
pub struct LittleEndian;
```

##### Implementations

###### Trait Implementations

- **Freeze**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Eq**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Send**
- **UnwindSafe**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Default**
  - ```rust
    fn default() -> LittleEndian { /* ... */ }
    ```

- **Unpin**
- **Sync**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> LittleEndian { /* ... */ }
    ```

- **RefUnwindSafe**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **PartialEq**
  - ```rust
    fn eq(self: &Self, other: &LittleEndian) -> bool { /* ... */ }
    ```

- **Hash**
  - ```rust
    fn hash<__H: $crate::hash::Hasher>(self: &Self, state: &mut __H) { /* ... */ }
    ```

- **Endian**
  - ```rust
    fn from_big_endian(big_endian: bool) -> Option<Self> { /* ... */ }
    ```

  - ```rust
    fn is_big_endian(self: Self) -> bool { /* ... */ }
    ```

- **StructuralPartialEq**
- **Copy**
#### Struct `BigEndian`

Compile-time big endian byte order.

```rust
pub struct BigEndian;
```

##### Implementations

###### Trait Implementations

- **Send**
- **Hash**
  - ```rust
    fn hash<__H: $crate::hash::Hasher>(self: &Self, state: &mut __H) { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **StructuralPartialEq**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Copy**
- **PartialEq**
  - ```rust
    fn eq(self: &Self, other: &BigEndian) -> bool { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> BigEndian { /* ... */ }
    ```

- **Endian**
  - ```rust
    fn from_big_endian(big_endian: bool) -> Option<Self> { /* ... */ }
    ```

  - ```rust
    fn is_big_endian(self: Self) -> bool { /* ... */ }
    ```

- **UnwindSafe**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **RefUnwindSafe**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Freeze**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Unpin**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Sync**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Default**
  - ```rust
    fn default() -> BigEndian { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Eq**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

#### Type Alias `NativeEndian`

**Attributes:**

- `#[<cfg>(target_endian = "little")]`

The native endianness for the target platform.

```rust
pub type NativeEndian = LittleEndian;
```

#### Type Alias `U16`

**Attributes:**

- `#[<cfg>(feature = "unaligned")]`

A `u16` value with an externally specified endianness of type `E`.

```rust
pub type U16<E> = U16Bytes<E>;
```

#### Type Alias `U32`

**Attributes:**

- `#[<cfg>(feature = "unaligned")]`

A `u32` value with an externally specified endianness of type `E`.

```rust
pub type U32<E> = U32Bytes<E>;
```

#### Type Alias `U64`

**Attributes:**

- `#[<cfg>(feature = "unaligned")]`

A `u64` value with an externally specified endianness of type `E`.

```rust
pub type U64<E> = U64Bytes<E>;
```

#### Type Alias `I16`

**Attributes:**

- `#[<cfg>(feature = "unaligned")]`

An `i16` value with an externally specified endianness of type `E`.

```rust
pub type I16<E> = I16Bytes<E>;
```

#### Type Alias `I32`

**Attributes:**

- `#[<cfg>(feature = "unaligned")]`

An `i32` value with an externally specified endianness of type `E`.

```rust
pub type I32<E> = I32Bytes<E>;
```

#### Type Alias `I64`

**Attributes:**

- `#[<cfg>(feature = "unaligned")]`

An `i64` value with an externally specified endianness of type `E`.

```rust
pub type I64<E> = I64Bytes<E>;
```

#### Struct `U16Bytes`

An unaligned `u16` value with an externally specified endianness of type `E`.

```rust
pub struct U16Bytes<E: Endian>(/* private field */, /* private field */);
```

##### Fields

| Index | Type | Documentation |
|-------|------|---------------|
| 0 | `private` | *Private field* |
| 1 | `private` | *Private field* |

##### Implementations

###### Methods

- ```rust
  pub const fn from_bytes(n: [u8; 2]) -> Self { /* ... */ }
  ```
  Construct a new value given bytes that already have the required endianness.

- ```rust
  pub fn new(e: E, n: u16) -> Self { /* ... */ }
  ```
  Construct a new value given a native endian value.

- ```rust
  pub fn get(self: Self, e: E) -> u16 { /* ... */ }
  ```
  Return the value as a native endian value.

- ```rust
  pub fn set(self: &mut Self, e: E, n: u16) { /* ... */ }
  ```
  Set the value given a native endian value.

###### Trait Implementations

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Send**
- **PartialEq**
  - ```rust
    fn eq(self: &Self, other: &U16Bytes<E>) -> bool { /* ... */ }
    ```

- **StructuralPartialEq**
- **Eq**
- **PartialOrd**
  - ```rust
    fn partial_cmp(self: &Self, other: &U16Bytes<E>) -> $crate::option::Option<$crate::cmp::Ordering> { /* ... */ }
    ```

- **Ord**
  - ```rust
    fn cmp(self: &Self, other: &U16Bytes<E>) -> $crate::cmp::Ordering { /* ... */ }
    ```

- **Hash**
  - ```rust
    fn hash<__H: $crate::hash::Hasher>(self: &Self, state: &mut __H) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut fmt::Formatter<''_>) -> fmt::Result { /* ... */ }
    ```

- **Pod**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Copy**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> U16Bytes<E> { /* ... */ }
    ```

- **RefUnwindSafe**
- **Unpin**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Freeze**
- **Default**
  - ```rust
    fn default() -> U16Bytes<E> { /* ... */ }
    ```

- **Sync**
- **UnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

#### Struct `U32Bytes`

An unaligned `u32` value with an externally specified endianness of type `E`.

```rust
pub struct U32Bytes<E: Endian>(/* private field */, /* private field */);
```

##### Fields

| Index | Type | Documentation |
|-------|------|---------------|
| 0 | `private` | *Private field* |
| 1 | `private` | *Private field* |

##### Implementations

###### Methods

- ```rust
  pub const fn from_bytes(n: [u8; 4]) -> Self { /* ... */ }
  ```
  Construct a new value given bytes that already have the required endianness.

- ```rust
  pub fn new(e: E, n: u32) -> Self { /* ... */ }
  ```
  Construct a new value given a native endian value.

- ```rust
  pub fn get(self: Self, e: E) -> u32 { /* ... */ }
  ```
  Return the value as a native endian value.

- ```rust
  pub fn set(self: &mut Self, e: E, n: u32) { /* ... */ }
  ```
  Set the value given a native endian value.

###### Trait Implementations

- **Eq**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Ord**
  - ```rust
    fn cmp(self: &Self, other: &U32Bytes<E>) -> $crate::cmp::Ordering { /* ... */ }
    ```

- **Sync**
- **RefUnwindSafe**
- **Unpin**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Send**
- **UnwindSafe**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Copy**
- **StructuralPartialEq**
- **Hash**
  - ```rust
    fn hash<__H: $crate::hash::Hasher>(self: &Self, state: &mut __H) { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut fmt::Formatter<''_>) -> fmt::Result { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Default**
  - ```rust
    fn default() -> U32Bytes<E> { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> U32Bytes<E> { /* ... */ }
    ```

- **PartialOrd**
  - ```rust
    fn partial_cmp(self: &Self, other: &U32Bytes<E>) -> $crate::option::Option<$crate::cmp::Ordering> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Pod**
- **Freeze**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **PartialEq**
  - ```rust
    fn eq(self: &Self, other: &U32Bytes<E>) -> bool { /* ... */ }
    ```

#### Struct `U64Bytes`

An unaligned `u64` value with an externally specified endianness of type `E`.

```rust
pub struct U64Bytes<E: Endian>(/* private field */, /* private field */);
```

##### Fields

| Index | Type | Documentation |
|-------|------|---------------|
| 0 | `private` | *Private field* |
| 1 | `private` | *Private field* |

##### Implementations

###### Methods

- ```rust
  pub const fn from_bytes(n: [u8; 8]) -> Self { /* ... */ }
  ```
  Construct a new value given bytes that already have the required endianness.

- ```rust
  pub fn new(e: E, n: u64) -> Self { /* ... */ }
  ```
  Construct a new value given a native endian value.

- ```rust
  pub fn get(self: Self, e: E) -> u64 { /* ... */ }
  ```
  Return the value as a native endian value.

- ```rust
  pub fn set(self: &mut Self, e: E, n: u64) { /* ... */ }
  ```
  Set the value given a native endian value.

###### Trait Implementations

- **Pod**
- **Send**
- **Ord**
  - ```rust
    fn cmp(self: &Self, other: &U64Bytes<E>) -> $crate::cmp::Ordering { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Default**
  - ```rust
    fn default() -> U64Bytes<E> { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Unpin**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Eq**
- **PartialOrd**
  - ```rust
    fn partial_cmp(self: &Self, other: &U64Bytes<E>) -> $crate::option::Option<$crate::cmp::Ordering> { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut fmt::Formatter<''_>) -> fmt::Result { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Hash**
  - ```rust
    fn hash<__H: $crate::hash::Hasher>(self: &Self, state: &mut __H) { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **StructuralPartialEq**
- **RefUnwindSafe**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Freeze**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> U64Bytes<E> { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Sync**
- **Copy**
- **PartialEq**
  - ```rust
    fn eq(self: &Self, other: &U64Bytes<E>) -> bool { /* ... */ }
    ```

- **UnwindSafe**
#### Struct `I16Bytes`

An unaligned `i16` value with an externally specified endianness of type `E`.

```rust
pub struct I16Bytes<E: Endian>(/* private field */, /* private field */);
```

##### Fields

| Index | Type | Documentation |
|-------|------|---------------|
| 0 | `private` | *Private field* |
| 1 | `private` | *Private field* |

##### Implementations

###### Methods

- ```rust
  pub const fn from_bytes(n: [u8; 2]) -> Self { /* ... */ }
  ```
  Construct a new value given bytes that already have the required endianness.

- ```rust
  pub fn new(e: E, n: i16) -> Self { /* ... */ }
  ```
  Construct a new value given a native endian value.

- ```rust
  pub fn get(self: Self, e: E) -> i16 { /* ... */ }
  ```
  Return the value as a native endian value.

- ```rust
  pub fn set(self: &mut Self, e: E, n: i16) { /* ... */ }
  ```
  Set the value given a native endian value.

###### Trait Implementations

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **PartialEq**
  - ```rust
    fn eq(self: &Self, other: &I16Bytes<E>) -> bool { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Unpin**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut fmt::Formatter<''_>) -> fmt::Result { /* ... */ }
    ```

- **Freeze**
- **StructuralPartialEq**
- **RefUnwindSafe**
- **Ord**
  - ```rust
    fn cmp(self: &Self, other: &I16Bytes<E>) -> $crate::cmp::Ordering { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Send**
- **UnwindSafe**
- **Pod**
- **Eq**
- **Default**
  - ```rust
    fn default() -> I16Bytes<E> { /* ... */ }
    ```

- **Sync**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Copy**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> I16Bytes<E> { /* ... */ }
    ```

- **Hash**
  - ```rust
    fn hash<__H: $crate::hash::Hasher>(self: &Self, state: &mut __H) { /* ... */ }
    ```

- **PartialOrd**
  - ```rust
    fn partial_cmp(self: &Self, other: &I16Bytes<E>) -> $crate::option::Option<$crate::cmp::Ordering> { /* ... */ }
    ```

#### Struct `I32Bytes`

An unaligned `i32` value with an externally specified endianness of type `E`.

```rust
pub struct I32Bytes<E: Endian>(/* private field */, /* private field */);
```

##### Fields

| Index | Type | Documentation |
|-------|------|---------------|
| 0 | `private` | *Private field* |
| 1 | `private` | *Private field* |

##### Implementations

###### Methods

- ```rust
  pub const fn from_bytes(n: [u8; 4]) -> Self { /* ... */ }
  ```
  Construct a new value given bytes that already have the required endianness.

- ```rust
  pub fn new(e: E, n: i32) -> Self { /* ... */ }
  ```
  Construct a new value given a native endian value.

- ```rust
  pub fn get(self: Self, e: E) -> i32 { /* ... */ }
  ```
  Return the value as a native endian value.

- ```rust
  pub fn set(self: &mut Self, e: E, n: i32) { /* ... */ }
  ```
  Set the value given a native endian value.

###### Trait Implementations

- **Default**
  - ```rust
    fn default() -> I32Bytes<E> { /* ... */ }
    ```

- **StructuralPartialEq**
- **Freeze**
- **Eq**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **PartialOrd**
  - ```rust
    fn partial_cmp(self: &Self, other: &I32Bytes<E>) -> $crate::option::Option<$crate::cmp::Ordering> { /* ... */ }
    ```

- **Hash**
  - ```rust
    fn hash<__H: $crate::hash::Hasher>(self: &Self, state: &mut __H) { /* ... */ }
    ```

- **Ord**
  - ```rust
    fn cmp(self: &Self, other: &I32Bytes<E>) -> $crate::cmp::Ordering { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> I32Bytes<E> { /* ... */ }
    ```

- **PartialEq**
  - ```rust
    fn eq(self: &Self, other: &I32Bytes<E>) -> bool { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut fmt::Formatter<''_>) -> fmt::Result { /* ... */ }
    ```

- **Pod**
- **Sync**
- **RefUnwindSafe**
- **Unpin**
- **Send**
- **Copy**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **UnwindSafe**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

#### Struct `I64Bytes`

An unaligned `i64` value with an externally specified endianness of type `E`.

```rust
pub struct I64Bytes<E: Endian>(/* private field */, /* private field */);
```

##### Fields

| Index | Type | Documentation |
|-------|------|---------------|
| 0 | `private` | *Private field* |
| 1 | `private` | *Private field* |

##### Implementations

###### Methods

- ```rust
  pub const fn from_bytes(n: [u8; 8]) -> Self { /* ... */ }
  ```
  Construct a new value given bytes that already have the required endianness.

- ```rust
  pub fn new(e: E, n: i64) -> Self { /* ... */ }
  ```
  Construct a new value given a native endian value.

- ```rust
  pub fn get(self: Self, e: E) -> i64 { /* ... */ }
  ```
  Return the value as a native endian value.

- ```rust
  pub fn set(self: &mut Self, e: E, n: i64) { /* ... */ }
  ```
  Set the value given a native endian value.

###### Trait Implementations

- **StructuralPartialEq**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Unpin**
- **PartialOrd**
  - ```rust
    fn partial_cmp(self: &Self, other: &I64Bytes<E>) -> $crate::option::Option<$crate::cmp::Ordering> { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Copy**
- **RefUnwindSafe**
- **Default**
  - ```rust
    fn default() -> I64Bytes<E> { /* ... */ }
    ```

- **Hash**
  - ```rust
    fn hash<__H: $crate::hash::Hasher>(self: &Self, state: &mut __H) { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Freeze**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> I64Bytes<E> { /* ... */ }
    ```

- **Sync**
- **Send**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **PartialEq**
  - ```rust
    fn eq(self: &Self, other: &I64Bytes<E>) -> bool { /* ... */ }
    ```

- **UnwindSafe**
- **Eq**
- **Ord**
  - ```rust
    fn cmp(self: &Self, other: &I64Bytes<E>) -> $crate::cmp::Ordering { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut fmt::Formatter<''_>) -> fmt::Result { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Pod**
### Traits

#### Trait `Endian`

A trait for using an endianness specification.

Provides methods for converting between the specified endianness and
the native endianness of the target machine.

This trait does not require that the endianness is known at compile time.

```rust
pub trait Endian: Debug + Default + Clone + Copy + PartialEq + Eq + ''static {
    /* Associated items */
}
```

> This trait is not object-safe and cannot be used in dynamic trait objects.

##### Required Items

###### Required Methods

- `from_big_endian`: Construct a specification for the endianness of some values.
- `is_big_endian`: Return true for big endian byte order.

##### Provided Methods

- ```rust
  fn from_little_endian(little_endian: bool) -> Option<Self> { /* ... */ }
  ```
  Construct a specification for the endianness of some values.

- ```rust
  fn is_little_endian(self: Self) -> bool { /* ... */ }
  ```
  Return true for little endian byte order.

- ```rust
  fn read_u16(self: Self, n: u16) -> u16 { /* ... */ }
  ```
  Converts an unsigned 16 bit integer to native endian.

- ```rust
  fn read_u32(self: Self, n: u32) -> u32 { /* ... */ }
  ```
  Converts an unsigned 32 bit integer to native endian.

- ```rust
  fn read_u64(self: Self, n: u64) -> u64 { /* ... */ }
  ```
  Converts an unsigned 64 bit integer to native endian.

- ```rust
  fn read_i16(self: Self, n: i16) -> i16 { /* ... */ }
  ```
  Converts a signed 16 bit integer to native endian.

- ```rust
  fn read_i32(self: Self, n: i32) -> i32 { /* ... */ }
  ```
  Converts a signed 32 bit integer to native endian.

- ```rust
  fn read_i64(self: Self, n: i64) -> i64 { /* ... */ }
  ```
  Converts a signed 64 bit integer to native endian.

- ```rust
  fn read_u16_bytes(self: Self, n: [u8; 2]) -> u16 { /* ... */ }
  ```
  Converts an unaligned unsigned 16 bit integer to native endian.

- ```rust
  fn read_u32_bytes(self: Self, n: [u8; 4]) -> u32 { /* ... */ }
  ```
  Converts an unaligned unsigned 32 bit integer to native endian.

- ```rust
  fn read_u64_bytes(self: Self, n: [u8; 8]) -> u64 { /* ... */ }
  ```
  Converts an unaligned unsigned 64 bit integer to native endian.

- ```rust
  fn read_i16_bytes(self: Self, n: [u8; 2]) -> i16 { /* ... */ }
  ```
  Converts an unaligned signed 16 bit integer to native endian.

- ```rust
  fn read_i32_bytes(self: Self, n: [u8; 4]) -> i32 { /* ... */ }
  ```
  Converts an unaligned signed 32 bit integer to native endian.

- ```rust
  fn read_i64_bytes(self: Self, n: [u8; 8]) -> i64 { /* ... */ }
  ```
  Converts an unaligned signed 64 bit integer to native endian.

- ```rust
  fn write_u16(self: Self, n: u16) -> u16 { /* ... */ }
  ```
  Converts an unsigned 16 bit integer from native endian.

- ```rust
  fn write_u32(self: Self, n: u32) -> u32 { /* ... */ }
  ```
  Converts an unsigned 32 bit integer from native endian.

- ```rust
  fn write_u64(self: Self, n: u64) -> u64 { /* ... */ }
  ```
  Converts an unsigned 64 bit integer from native endian.

- ```rust
  fn write_i16(self: Self, n: i16) -> i16 { /* ... */ }
  ```
  Converts a signed 16 bit integer from native endian.

- ```rust
  fn write_i32(self: Self, n: i32) -> i32 { /* ... */ }
  ```
  Converts a signed 32 bit integer from native endian.

- ```rust
  fn write_i64(self: Self, n: i64) -> i64 { /* ... */ }
  ```
  Converts a signed 64 bit integer from native endian.

- ```rust
  fn write_u16_bytes(self: Self, n: u16) -> [u8; 2] { /* ... */ }
  ```
  Converts an unaligned unsigned 16 bit integer from native endian.

- ```rust
  fn write_u32_bytes(self: Self, n: u32) -> [u8; 4] { /* ... */ }
  ```
  Converts an unaligned unsigned 32 bit integer from native endian.

- ```rust
  fn write_u64_bytes(self: Self, n: u64) -> [u8; 8] { /* ... */ }
  ```
  Converts an unaligned unsigned 64 bit integer from native endian.

- ```rust
  fn write_i16_bytes(self: Self, n: i16) -> [u8; 2] { /* ... */ }
  ```
  Converts an unaligned signed 16 bit integer from native endian.

- ```rust
  fn write_i32_bytes(self: Self, n: i32) -> [u8; 4] { /* ... */ }
  ```
  Converts an unaligned signed 32 bit integer from native endian.

- ```rust
  fn write_i64_bytes(self: Self, n: i64) -> [u8; 8] { /* ... */ }
  ```
  Converts an unaligned signed 64 bit integer from native endian.

##### Implementations

This trait is implemented for the following types:

- `Endianness`
- `LittleEndian`
- `BigEndian`

## Module `pod`

**Attributes:**

- `#[macro_use]`
- `#[<cfg_attr>(not(all(feature = "read_core", feature = "write_core")),
allow(dead_code))]`
- `#[allow(dead_code)]`

Tools for converting file format structures to and from bytes.

This module should be replaced once rust provides safe transmutes.

```rust
pub mod pod { /* ... */ }
```

### Traits

#### Trait `Pod`

A trait for types that can safely be converted from and to byte slices.

# Safety
A type that is `Pod` must:
- be `#[repr(C)]` or `#[repr(transparent)]`
- have no invalid byte values
- have no padding

```rust
pub unsafe trait Pod: Copy + ''static {
    /* Associated items */
}
```

> This trait is unsafe to implement.

> This trait is not object-safe and cannot be used in dynamic trait objects.

##### Implementations

This trait is implemented for the following types:

- `U16Bytes<E>` with <E: Endian>
- `U32Bytes<E>` with <E: Endian>
- `U64Bytes<E>` with <E: Endian>
- `I16Bytes<E>` with <E: Endian>
- `I32Bytes<E>` with <E: Endian>
- `I64Bytes<E>` with <E: Endian>
- `u8`
- `u16`
- `u32`
- `u64`
- `[T; N]` with <const N: usize, T: Pod>
- `Header`
- `AixHeader`
- `AixFileHeader`
- `AixMemberOffset`
- `FileHeader32<E>` with <E: Endian>
- `FileHeader64<E>` with <E: Endian>
- `SectionHeader32<E>` with <E: Endian>
- `SectionHeader64<E>` with <E: Endian>
- `CompressionHeader32<E>` with <E: Endian>
- `CompressionHeader64<E>` with <E: Endian>
- `Sym32<E>` with <E: Endian>
- `Sym64<E>` with <E: Endian>
- `Syminfo32<E>` with <E: Endian>
- `Syminfo64<E>` with <E: Endian>
- `Rel32<E>` with <E: Endian>
- `Rel64<E>` with <E: Endian>
- `Rela32<E>` with <E: Endian>
- `Rela64<E>` with <E: Endian>
- `Relr32<E>` with <E: Endian>
- `Relr64<E>` with <E: Endian>
- `ProgramHeader32<E>` with <E: Endian>
- `ProgramHeader64<E>` with <E: Endian>
- `Dyn32<E>` with <E: Endian>
- `Dyn64<E>` with <E: Endian>
- `Versym<E>` with <E: Endian>
- `Verdef<E>` with <E: Endian>
- `Verdaux<E>` with <E: Endian>
- `Verneed<E>` with <E: Endian>
- `Vernaux<E>` with <E: Endian>
- `NoteHeader32<E>` with <E: Endian>
- `NoteHeader64<E>` with <E: Endian>
- `HashHeader<E>` with <E: Endian>
- `GnuHashHeader<E>` with <E: Endian>
- `FatHeader`
- `FatArch32`
- `FatArch64`
- `DyldCacheHeader<E>` with <E: Endian>
- `DyldCacheMappingInfo<E>` with <E: Endian>
- `DyldCacheImageInfo<E>` with <E: Endian>
- `DyldSubCacheEntryV1<E>` with <E: Endian>
- `DyldSubCacheEntryV2<E>` with <E: Endian>
- `MachHeader32<E>` with <E: Endian>
- `MachHeader64<E>` with <E: Endian>
- `LoadCommand<E>` with <E: Endian>
- `LcStr<E>` with <E: Endian>
- `SegmentCommand32<E>` with <E: Endian>
- `SegmentCommand64<E>` with <E: Endian>
- `Section32<E>` with <E: Endian>
- `Section64<E>` with <E: Endian>
- `Fvmlib<E>` with <E: Endian>
- `FvmlibCommand<E>` with <E: Endian>
- `Dylib<E>` with <E: Endian>
- `DylibCommand<E>` with <E: Endian>
- `SubFrameworkCommand<E>` with <E: Endian>
- `SubClientCommand<E>` with <E: Endian>
- `SubUmbrellaCommand<E>` with <E: Endian>
- `SubLibraryCommand<E>` with <E: Endian>
- `PreboundDylibCommand<E>` with <E: Endian>
- `DylinkerCommand<E>` with <E: Endian>
- `ThreadCommand<E>` with <E: Endian>
- `RoutinesCommand32<E>` with <E: Endian>
- `RoutinesCommand64<E>` with <E: Endian>
- `SymtabCommand<E>` with <E: Endian>
- `DysymtabCommand<E>` with <E: Endian>
- `DylibTableOfContents<E>` with <E: Endian>
- `DylibModule32<E>` with <E: Endian>
- `DylibModule64<E>` with <E: Endian>
- `DylibReference<E>` with <E: Endian>
- `TwolevelHintsCommand<E>` with <E: Endian>
- `TwolevelHint<E>` with <E: Endian>
- `PrebindCksumCommand<E>` with <E: Endian>
- `UuidCommand<E>` with <E: Endian>
- `RpathCommand<E>` with <E: Endian>
- `LinkeditDataCommand<E>` with <E: Endian>
- `FilesetEntryCommand<E>` with <E: Endian>
- `EncryptionInfoCommand32<E>` with <E: Endian>
- `EncryptionInfoCommand64<E>` with <E: Endian>
- `VersionMinCommand<E>` with <E: Endian>
- `BuildVersionCommand<E>` with <E: Endian>
- `BuildToolVersion<E>` with <E: Endian>
- `DyldInfoCommand<E>` with <E: Endian>
- `LinkerOptionCommand<E>` with <E: Endian>
- `SymsegCommand<E>` with <E: Endian>
- `IdentCommand<E>` with <E: Endian>
- `FvmfileCommand<E>` with <E: Endian>
- `EntryPointCommand<E>` with <E: Endian>
- `SourceVersionCommand<E>` with <E: Endian>
- `DataInCodeEntry<E>` with <E: Endian>
- `NoteCommand<E>` with <E: Endian>
- `Nlist32<E>` with <E: Endian>
- `Nlist64<E>` with <E: Endian>
- `Relocation<E>` with <E: Endian>
- `ImageDosHeader`
- `ImageOs2Header`
- `ImageVxdHeader`
- `ImageFileHeader`
- `ImageDataDirectory`
- `ImageOptionalHeader32`
- `ImageRomOptionalHeader`
- `ImageOptionalHeader64`
- `ImageNtHeaders64`
- `ImageNtHeaders32`
- `ImageRomHeaders`
- `Guid`
- `AnonObjectHeader`
- `AnonObjectHeaderV2`
- `AnonObjectHeaderBigobj`
- `ImageSectionHeader`
- `ImageSymbol`
- `ImageSymbolBytes`
- `ImageSymbolEx`
- `ImageSymbolExBytes`
- `ImageAuxSymbolTokenDef`
- `ImageAuxSymbolFunction`
- `ImageAuxSymbolFunctionBeginEnd`
- `ImageAuxSymbolWeak`
- `ImageAuxSymbolSection`
- `ImageAuxSymbolCrc`
- `ImageRelocation`
- `ImageLinenumber`
- `ImageBaseRelocation`
- `ImageArchiveMemberHeader`
- `ImageExportDirectory`
- `ImageImportByName`
- `ImageThunkData64`
- `ImageThunkData32`
- `ImageTlsDirectory64`
- `ImageTlsDirectory32`
- `ImageImportDescriptor`
- `ImageBoundImportDescriptor`
- `ImageBoundForwarderRef`
- `ImageDelayloadDescriptor`
- `ImageResourceDirectory`
- `ImageResourceDirectoryEntry`
- `ImageResourceDirectoryString`
- `ImageResourceDirStringU`
- `ImageResourceDataEntry`
- `ImageLoadConfigCodeIntegrity`
- `ImageDynamicRelocationTable`
- `ImageDynamicRelocation32`
- `ImageDynamicRelocation64`
- `ImageDynamicRelocation32V2`
- `ImageDynamicRelocation64V2`
- `ImagePrologueDynamicRelocationHeader`
- `ImageEpilogueDynamicRelocationHeader`
- `ImageLoadConfigDirectory32`
- `ImageLoadConfigDirectory64`
- `ImageHotPatchInfo`
- `ImageHotPatchBase`
- `ImageHotPatchHashes`
- `ImageArmRuntimeFunctionEntry`
- `ImageArm64RuntimeFunctionEntry`
- `ImageAlpha64RuntimeFunctionEntry`
- `ImageAlphaRuntimeFunctionEntry`
- `ImageRuntimeFunctionEntry`
- `ImageEnclaveConfig32`
- `ImageEnclaveConfig64`
- `ImageEnclaveImport`
- `ImageDebugDirectory`
- `ImageCoffSymbolsHeader`
- `ImageDebugMisc`
- `ImageFunctionEntry`
- `ImageFunctionEntry64`
- `ImageSeparateDebugHeader`
- `NonPagedDebugInfo`
- `ImageArchitectureEntry`
- `ImportObjectHeader`
- `ImageCor20Header`
- `MaskedRichHeaderEntry`
- `FileHeader32`
- `FileHeader64`
- `AuxHeader32`
- `AuxHeader64`
- `SectionHeader32`
- `SectionHeader64`
- `SymbolBytes`
- `Symbol32`
- `Symbol64`
- `FileAux32`
- `FileAux64`
- `CsectAux32`
- `CsectAux64`
- `FunAux32`
- `FunAux64`
- `ExpAux`
- `BlockAux32`
- `BlockAux64`
- `StatAux`
- `DwarfAux32`
- `DwarfAux64`
- `Rel32`
- `Rel64`

### Functions

#### Function `from_bytes`

**Attributes:**

- `#[inline]`

Cast the head of a byte slice to a `Pod` type.

Returns the type and the tail of the byte slice.

Returns an error if the byte slice is too short or the alignment is invalid.

```rust
pub fn from_bytes<T: Pod>(data: &[u8]) -> result::Result<(&T, &[u8]), ()> { /* ... */ }
```

#### Function `from_bytes_mut`

**Attributes:**

- `#[inline]`

Cast the head of a mutable byte slice to a `Pod` type.

Returns the type and the tail of the byte slice.

Returns an error if the byte slice is too short or the alignment is invalid.

```rust
pub fn from_bytes_mut<T: Pod>(data: &mut [u8]) -> result::Result<(&mut T, &mut [u8]), ()> { /* ... */ }
```

#### Function `slice_from_bytes`

**Attributes:**

- `#[inline]`

Cast the head of a byte slice to a slice of a `Pod` type.

Returns the type slice and the tail of the byte slice.

Returns an error if the byte slice is too short or the alignment is invalid.

```rust
pub fn slice_from_bytes<T: Pod>(data: &[u8], count: usize) -> result::Result<(&[T], &[u8]), ()> { /* ... */ }
```

#### Function `slice_from_bytes_mut`

**Attributes:**

- `#[inline]`

Cast the head of a mutable byte slice to a slice of a `Pod` type.

Returns the type slice and the tail of the byte slice.

Returns an error if the byte slice is too short or the alignment is invalid.

```rust
pub fn slice_from_bytes_mut<T: Pod>(data: &mut [u8], count: usize) -> result::Result<(&mut [T], &mut [u8]), ()> { /* ... */ }
```

#### Function `slice_from_all_bytes`

**Attributes:**

- `#[inline]`

Cast all of a byte slice to a slice of a `Pod` type.

Returns the type slice.

Returns an error if the size of the byte slice is not an exact multiple
of the type size, or the alignment is invalid.

```rust
pub fn slice_from_all_bytes<T: Pod>(data: &[u8]) -> result::Result<&[T], ()> { /* ... */ }
```

#### Function `slice_from_all_bytes_mut`

**Attributes:**

- `#[inline]`

Cast all of a byte slice to a slice of a `Pod` type.

Returns the type slice.

Returns an error if the size of the byte slice is not an exact multiple
of the type size, or the alignment is invalid.

```rust
pub fn slice_from_all_bytes_mut<T: Pod>(data: &mut [u8]) -> result::Result<&mut [T], ()> { /* ... */ }
```

#### Function `bytes_of`

**Attributes:**

- `#[inline]`

Cast a `Pod` type to a byte slice.

```rust
pub fn bytes_of<T: Pod>(val: &T) -> &[u8] { /* ... */ }
```

#### Function `bytes_of_mut`

**Attributes:**

- `#[inline]`

Cast a `Pod` type to a mutable byte slice.

```rust
pub fn bytes_of_mut<T: Pod>(val: &mut T) -> &mut [u8] { /* ... */ }
```

#### Function `bytes_of_slice`

**Attributes:**

- `#[inline]`

Cast a slice of a `Pod` type to a byte slice.

```rust
pub fn bytes_of_slice<T: Pod>(val: &[T]) -> &[u8] { /* ... */ }
```

#### Function `bytes_of_slice_mut`

**Attributes:**

- `#[inline]`

Cast a slice of a `Pod` type to a mutable byte slice.

```rust
pub fn bytes_of_slice_mut<T: Pod>(val: &mut [T]) -> &mut [u8] { /* ... */ }
```

## Module `read`

**Attributes:**

- `#[<cfg>(feature = "read_core")]`

Interface for reading object files.

## Unified read API

The [`Object`] trait provides a unified read API for accessing common features of
object files, such as sections and symbols. There is an implementation of this
trait for [`File`], which allows reading any file format, as well as implementations
for each file format:
[`ElfFile`](elf::ElfFile), [`MachOFile`](macho::MachOFile), [`CoffFile`](coff::CoffFile),
[`PeFile`](pe::PeFile), [`WasmFile`](wasm::WasmFile), [`XcoffFile`](xcoff::XcoffFile).

## Low level read API

The submodules for each file format define helpers that operate on the raw structs.
These can be used instead of the unified API, or in conjunction with it to access
details that are not available via the unified API.

See the [submodules](#modules) for examples of the low level read API.

## Naming Convention

Types that form part of the unified API for a file format are prefixed with the
name of the file format.

## Example for unified read API
 ```no_run
use object::{Object, ObjectSection};
use std::error::Error;
use std::fs;

/// Reads a file and displays the name of each section.
fn main() -> Result<(), Box<dyn Error>> {
#   #[cfg(all(feature = "read", feature = "std"))] {
    let data = fs::read("path/to/binary")?;
    let file = object::File::parse(&*data)?;
    for section in file.sections() {
        println!("{}", section.name()?);
    }
#   }
    Ok(())
}
```

```rust
pub mod read { /* ... */ }
```

### Modules

## Module `archive`

**Attributes:**

- `#[<cfg>(feature = "archive")]`

Support for archive files.

## Example
 ```no_run
use object::{Object, ObjectSection};
use std::error::Error;
use std::fs;

/// Reads an archive and displays the name of each member.
fn main() -> Result<(), Box<dyn Error>> {
#   #[cfg(feature = "std")] {
    let data = fs::read("path/to/binary")?;
    let file = object::read::archive::ArchiveFile::parse(&*data)?;
    for member in file.members() {
        let member = member?;
        println!("{}", String::from_utf8_lossy(member.name()));
    }
#   }
    Ok(())
}
```

```rust
pub mod archive { /* ... */ }
```

### Types

#### Enum `ArchiveKind`

**Attributes:**

- `#[non_exhaustive]`

The kind of archive format.

```rust
pub enum ArchiveKind {
    Unknown,
    Gnu,
    Gnu64,
    Bsd,
    Bsd64,
    Coff,
    AixBig,
}
```

##### Variants

###### `Unknown`

There are no special files that indicate the archive format.

###### `Gnu`

The GNU (or System V) archive format.

###### `Gnu64`

The GNU (or System V) archive format with 64-bit symbol table.

###### `Bsd`

The BSD archive format.

###### `Bsd64`

The BSD archive format with 64-bit symbol table.

This is used for Darwin.

###### `Coff`

The Windows COFF archive format.

###### `AixBig`

The AIX big archive format.

##### Implementations

###### Trait Implementations

- **Freeze**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Unpin**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Sync**
- **UnwindSafe**
- **RefUnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ArchiveKind { /* ... */ }
    ```

- **PartialEq**
  - ```rust
    fn eq(self: &Self, other: &ArchiveKind) -> bool { /* ... */ }
    ```

- **Eq**
- **Send**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Hash**
  - ```rust
    fn hash<__H: $crate::hash::Hasher>(self: &Self, state: &mut __H) { /* ... */ }
    ```

- **Copy**
- **StructuralPartialEq**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

#### Struct `ArchiveFile`

A partially parsed archive file.

```rust
pub struct ArchiveFile<''data, R: ReadRef<''data> = &''data [u8]> {
    // Some fields omitted
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| *private fields* | ... | *Some fields have been omitted* |

##### Implementations

###### Methods

- ```rust
  pub fn parse(data: R) -> read::Result<Self> { /* ... */ }
  ```
  Parse the archive header and special members.

- ```rust
  pub fn kind(self: &Self) -> ArchiveKind { /* ... */ }
  ```
  Return the archive format.

- ```rust
  pub fn is_thin(self: &Self) -> bool { /* ... */ }
  ```
  Return true if the archive is a thin archive.

- ```rust
  pub fn members(self: &Self) -> ArchiveMemberIterator<''data, R> { /* ... */ }
  ```
  Iterate over the members of the archive.

- ```rust
  pub fn member(self: &Self, member: ArchiveOffset) -> read::Result<ArchiveMember<''data>> { /* ... */ }
  ```
  Return the member at the given offset.

- ```rust
  pub fn symbols(self: &Self) -> read::Result<Option<ArchiveSymbolIterator<''data>>> { /* ... */ }
  ```
  Iterate over the symbols in the archive.

###### Trait Implementations

- **Unpin**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Sync**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ArchiveFile<''data, R> { /* ... */ }
    ```

- **Freeze**
- **Send**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Copy**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **UnwindSafe**
- **RefUnwindSafe**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

#### Struct `ArchiveMemberIterator`

An iterator over the members of an archive.

```rust
pub struct ArchiveMemberIterator<''data, R: ReadRef<''data> = &''data [u8]> {
    // Some fields omitted
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| *private fields* | ... | *Some fields have been omitted* |

##### Implementations

###### Trait Implementations

- **Unpin**
- **Send**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **IntoIterator**
  - ```rust
    fn into_iter(self: Self) -> I { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **RefUnwindSafe**
- **Iterator**
  - ```rust
    fn next(self: &mut Self) -> Option<<Self as >::Item> { /* ... */ }
    ```

- **UnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Sync**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Freeze**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

#### Struct `ArchiveMember`

A partially parsed archive member.

```rust
pub struct ArchiveMember<''data> {
    // Some fields omitted
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| *private fields* | ... | *Some fields have been omitted* |

##### Implementations

###### Methods

- ```rust
  pub fn header(self: &Self) -> Option<&''data archive::Header> { /* ... */ }
  ```
  Return the raw header that is common to many archive formats.

- ```rust
  pub fn aix_header(self: &Self) -> Option<&''data archive::AixHeader> { /* ... */ }
  ```
  Return the raw header for AIX big archives.

- ```rust
  pub fn name(self: &Self) -> &''data [u8] { /* ... */ }
  ```
  Return the parsed file name.

- ```rust
  pub fn date(self: &Self) -> Option<u64> { /* ... */ }
  ```
  Parse the file modification timestamp from the header.

- ```rust
  pub fn uid(self: &Self) -> Option<u64> { /* ... */ }
  ```
  Parse the user ID from the header.

- ```rust
  pub fn gid(self: &Self) -> Option<u64> { /* ... */ }
  ```
  Parse the group ID from the header.

- ```rust
  pub fn mode(self: &Self) -> Option<u64> { /* ... */ }
  ```
  Parse the file mode from the header.

- ```rust
  pub fn size(self: &Self) -> u64 { /* ... */ }
  ```
  Return the size of the file data.

- ```rust
  pub fn file_range(self: &Self) -> (u64, u64) { /* ... */ }
  ```
  Return the offset and size of the file data.

- ```rust
  pub fn is_thin(self: &Self) -> bool { /* ... */ }
  ```
  Return true if the member is a thin member.

- ```rust
  pub fn data<R: ReadRef<''data>>(self: &Self, data: R) -> read::Result<&''data [u8]> { /* ... */ }
  ```
  Return the file data.

###### Trait Implementations

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Freeze**
- **RefUnwindSafe**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Unpin**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Send**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Sync**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **UnwindSafe**
#### Struct `ArchiveOffset`

An offset of a member in an archive.

```rust
pub struct ArchiveOffset(pub u64);
```

##### Fields

| Index | Type | Documentation |
|-------|------|---------------|
| 0 | `u64` |  |

##### Implementations

###### Trait Implementations

- **Freeze**
- **Sync**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Send**
- **RefUnwindSafe**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **UnwindSafe**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> ArchiveOffset { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Unpin**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Copy**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

#### Struct `ArchiveSymbolIterator`

An iterator over the symbols in the archive symbol table.

```rust
pub struct ArchiveSymbolIterator<''data>(/* private field */);
```

##### Fields

| Index | Type | Documentation |
|-------|------|---------------|
| 0 | `private` | *Private field* |

##### Implementations

###### Trait Implementations

- **Freeze**
- **Unpin**
- **UnwindSafe**
- **Send**
- **RefUnwindSafe**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ArchiveSymbolIterator<''data> { /* ... */ }
    ```

- **Iterator**
  - ```rust
    fn next(self: &mut Self) -> Option<<Self as >::Item> { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Sync**
- **IntoIterator**
  - ```rust
    fn into_iter(self: Self) -> I { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

#### Struct `ArchiveSymbol`

A symbol in the archive symbol table.

This is used to find the member containing the symbol.

```rust
pub struct ArchiveSymbol<''data> {
    // Some fields omitted
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| *private fields* | ... | *Some fields have been omitted* |

##### Implementations

###### Methods

- ```rust
  pub fn name(self: &Self) -> &''data [u8] { /* ... */ }
  ```
  Return the symbol name.

- ```rust
  pub fn offset(self: &Self) -> ArchiveOffset { /* ... */ }
  ```
  Return the offset of the header for the member containing the symbol.

###### Trait Implementations

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Freeze**
- **RefUnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Copy**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Unpin**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ArchiveSymbol<''data> { /* ... */ }
    ```

- **Sync**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Send**
- **UnwindSafe**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

## Module `coff`

**Attributes:**

- `#[<cfg>(feature = "coff")]`

Support for reading Windows COFF files.

Traits are used to abstract over the difference between COFF object files
and COFF bigobj files. The primary trait for this is [`CoffHeader`].

## High level API

[`CoffFile`] implements the [`Object`](crate::read::Object) trait for
COFF files. [`CoffFile`] is parameterised by [`CoffHeader`].
The default parameter allows reading regular COFF object files,
while the type alias [`CoffBigFile`] allows reading COFF bigobj files.

[`ImportFile`] allows reading COFF short imports that are used in import
libraries. Currently these are not integrated with the unified read API.

## Low level API

The [`CoffHeader`] trait can be directly used to parse both COFF
object files (which start with [`pe::ImageFileHeader`]) and COFF bigobj
files (which start with [`pe::AnonObjectHeaderBigobj`]).

### Example for low level API
 ```no_run
use object::pe;
use object::read::coff::{CoffHeader, ImageSymbol as _};
use std::error::Error;
use std::fs;

/// Reads a file and displays the name of each section and symbol.
fn main() -> Result<(), Box<dyn Error>> {
#   #[cfg(feature = "std")] {
    let data = fs::read("path/to/binary")?;
    let mut offset = 0;
    let header = pe::ImageFileHeader::parse(&*data, &mut offset)?;
    let sections = header.sections(&*data, offset)?;
    let symbols = header.symbols(&*data)?;
    for section in sections.iter() {
        println!("{}", String::from_utf8_lossy(section.name(symbols.strings())?));
    }
    for (_index, symbol) in symbols.iter() {
        println!("{}", String::from_utf8_lossy(symbol.name(symbols.strings())?));
    }
#   }
    Ok(())
}
```

```rust
pub mod coff { /* ... */ }
```

### Re-exports

#### Re-export `file::*`

```rust
pub use file::*;
```

#### Re-export `section::*`

```rust
pub use section::*;
```

#### Re-export `symbol::*`

```rust
pub use symbol::*;
```

#### Re-export `relocation::*`

```rust
pub use relocation::*;
```

#### Re-export `comdat::*`

```rust
pub use comdat::*;
```

#### Re-export `import::*`

```rust
pub use import::*;
```

## Module `elf`

**Attributes:**

- `#[<cfg>(feature = "elf")]`

Support for reading ELF files.

Traits are used to abstract over the difference between 32-bit and 64-bit ELF.
The primary trait for this is [`FileHeader`].

## High level API

[`ElfFile`] implements the [`Object`](crate::read::Object) trait for ELF files.
[`ElfFile`] is parameterised by [`FileHeader`] to allow reading both 32-bit and
64-bit ELF. There are type aliases for these parameters ([`ElfFile32`] and
[`ElfFile64`]).

## Low level API

The [`FileHeader`] trait can be directly used to parse both [`elf::FileHeader32`]
and [`elf::FileHeader64`].

### Example for low level API
 ```no_run
use object::elf;
use object::read::elf::{FileHeader, Sym};
use std::error::Error;
use std::fs;

/// Reads a file and displays the name of each symbol.
fn main() -> Result<(), Box<dyn Error>> {
#   #[cfg(feature = "std")] {
    let data = fs::read("path/to/binary")?;
    let elf = elf::FileHeader64::<object::Endianness>::parse(&*data)?;
    let endian = elf.endian()?;
    let sections = elf.sections(endian, &*data)?;
    let symbols = sections.symbols(endian, &*data, elf::SHT_SYMTAB)?;
    for symbol in symbols.iter() {
        let name = symbol.name(endian, symbols.strings())?;
        println!("{}", String::from_utf8_lossy(name));
    }
#   }
    Ok(())
}
```

```rust
pub mod elf { /* ... */ }
```

### Re-exports

#### Re-export `file::*`

```rust
pub use file::*;
```

#### Re-export `segment::*`

```rust
pub use segment::*;
```

#### Re-export `section::*`

```rust
pub use section::*;
```

#### Re-export `symbol::*`

```rust
pub use symbol::*;
```

#### Re-export `relocation::*`

```rust
pub use relocation::*;
```

#### Re-export `comdat::*`

```rust
pub use comdat::*;
```

#### Re-export `dynamic::*`

```rust
pub use dynamic::*;
```

#### Re-export `compression::*`

```rust
pub use compression::*;
```

#### Re-export `note::*`

```rust
pub use note::*;
```

#### Re-export `hash::*`

```rust
pub use hash::*;
```

#### Re-export `version::*`

```rust
pub use version::*;
```

#### Re-export `attributes::*`

```rust
pub use attributes::*;
```

## Module `macho`

**Attributes:**

- `#[<cfg>(feature = "macho")]`

Support for reading Mach-O files.

Traits are used to abstract over the difference between 32-bit and 64-bit Mach-O
files. The primary trait for this is [`MachHeader`].

## High level API

[`MachOFile`] implements the [`Object`](crate::read::Object) trait for Mach-O files.
[`MachOFile`] is parameterised by [`MachHeader`] to allow reading both 32-bit and
64-bit Mach-O files. There are type aliases for these parameters ([`MachOFile32`] and
[`MachOFile64`]).

## Low level API

The [`MachHeader`] trait can be directly used to parse both [`macho::MachHeader32`]
and [`macho::MachHeader64`]. Additionally, [`FatHeader`] and the [`FatArch`] trait
can be used to iterate images in multi-architecture binaries, and [`DyldCache`] can
be used to locate images in a dyld shared cache.

### Example for low level API
 ```no_run
use object::macho;
use object::read::macho::{MachHeader, Nlist};
use std::error::Error;
use std::fs;

/// Reads a file and displays the name of each symbol.
fn main() -> Result<(), Box<dyn Error>> {
#   #[cfg(feature = "std")] {
    let data = fs::read("path/to/binary")?;
    let header = macho::MachHeader64::<object::Endianness>::parse(&*data, 0)?;
    let endian = header.endian()?;
    let mut commands = header.load_commands(endian, &*data, 0)?;
    while let Some(command) = commands.next()? {
        if let Some(symtab_command) = command.symtab()? {
            let symbols = symtab_command.symbols::<macho::MachHeader64<_>, _>(endian, &*data)?;
            for symbol in symbols.iter() {
                let name = symbol.name(endian, symbols.strings())?;
                println!("{}", String::from_utf8_lossy(name));
            }
        }
    }
#   }
    Ok(())
}
```

```rust
pub mod macho { /* ... */ }
```

### Re-exports

#### Re-export `dyld_cache::*`

```rust
pub use dyld_cache::*;
```

#### Re-export `fat::*`

```rust
pub use fat::*;
```

#### Re-export `file::*`

```rust
pub use file::*;
```

#### Re-export `load_command::*`

```rust
pub use load_command::*;
```

#### Re-export `segment::*`

```rust
pub use segment::*;
```

#### Re-export `section::*`

```rust
pub use section::*;
```

#### Re-export `symbol::*`

```rust
pub use symbol::*;
```

#### Re-export `relocation::*`

```rust
pub use relocation::*;
```

## Module `pe`

**Attributes:**

- `#[<cfg>(feature = "pe")]`

Support for reading PE files.

Traits are used to abstract over the difference between PE32 and PE32+.
The primary trait for this is [`ImageNtHeaders`].

## High level API

[`PeFile`] implements the [`Object`](crate::read::Object) trait for
PE files. [`PeFile`] is parameterised by [`ImageNtHeaders`] to allow
reading both PE32 and PE32+. There are type aliases for these parameters
([`PeFile32`] and [`PeFile64`]).

## Low level API

The [`ImageNtHeaders`] trait can be directly used to parse both
[`pe::ImageNtHeaders32`] and [`pe::ImageNtHeaders64`].

### Example for low level API
 ```no_run
use object::pe;
use object::read::pe::ImageNtHeaders;
use std::error::Error;
use std::fs;

/// Reads a file and displays the name of each section.
fn main() -> Result<(), Box<dyn Error>> {
#   #[cfg(feature = "std")] {
    let data = fs::read("path/to/binary")?;
    let dos_header = pe::ImageDosHeader::parse(&*data)?;
    let mut offset = dos_header.nt_headers_offset().into();
    let (nt_headers, data_directories) = pe::ImageNtHeaders64::parse(&*data, &mut offset)?;
    let sections = nt_headers.sections(&*data, offset)?;
    let symbols = nt_headers.symbols(&*data)?;
    for section in sections.iter() {
        println!("{}", String::from_utf8_lossy(section.name(symbols.strings())?));
    }
#   }
    Ok(())
}
```

```rust
pub mod pe { /* ... */ }
```

### Re-exports

#### Re-export `SectionTable`

```rust
pub use super::coff::SectionTable;
```

#### Re-export `SymbolTable`

```rust
pub use super::coff::SymbolTable;
```

#### Re-export `file::*`

```rust
pub use file::*;
```

#### Re-export `section::*`

```rust
pub use section::*;
```

#### Re-export `data_directory::*`

```rust
pub use data_directory::*;
```

#### Re-export `export::*`

```rust
pub use export::*;
```

#### Re-export `import::*`

```rust
pub use import::*;
```

#### Re-export `relocation::*`

```rust
pub use relocation::*;
```

#### Re-export `resource::*`

```rust
pub use resource::*;
```

#### Re-export `rich::*`

```rust
pub use rich::*;
```

## Module `xcoff`

**Attributes:**

- `#[<cfg>(feature = "xcoff")]`

Support for reading AIX XCOFF files.

Traits are used to abstract over the difference between 32-bit and 64-bit XCOFF.
The primary trait for this is [`FileHeader`].

## High level API

[`XcoffFile`] implements the [`Object`](crate::read::Object) trait for XCOFF files.
[`XcoffFile`] is parameterised by [`FileHeader`] to allow reading both 32-bit and
64-bit XCOFF. There are type aliases for these parameters ([`XcoffFile32`] and
[`XcoffFile64`]).

## Low level API

The [`FileHeader`] trait can be directly used to parse both [`xcoff::FileHeader32`]
and [`xcoff::FileHeader64`].

### Example for low level API
 ```no_run
use object::xcoff;
use object::read::xcoff::{FileHeader, SectionHeader, Symbol};
use std::error::Error;
use std::fs;

/// Reads a file and displays the name of each section and symbol.
fn main() -> Result<(), Box<dyn Error>> {
#   #[cfg(feature = "std")] {
    let data = fs::read("path/to/binary")?;
    let mut offset = 0;
    let header = xcoff::FileHeader64::parse(&*data, &mut offset)?;
    let aux_header = header.aux_header(&*data, &mut offset)?;
    let sections = header.sections(&*data, &mut offset)?;
    let symbols = header.symbols(&*data)?;
    for section in sections.iter() {
        println!("{}", String::from_utf8_lossy(section.name()));
    }
    for (_index, symbol) in symbols.iter() {
        println!("{}", String::from_utf8_lossy(symbol.name(symbols.strings())?));
    }
#   }
    Ok(())
}
```

```rust
pub mod xcoff { /* ... */ }
```

### Re-exports

#### Re-export `file::*`

```rust
pub use file::*;
```

#### Re-export `section::*`

```rust
pub use section::*;
```

#### Re-export `symbol::*`

```rust
pub use symbol::*;
```

#### Re-export `relocation::*`

```rust
pub use relocation::*;
```

#### Re-export `comdat::*`

```rust
pub use comdat::*;
```

#### Re-export `segment::*`

```rust
pub use segment::*;
```

### Types

#### Struct `Error`

The error type used within the read module.

```rust
pub struct Error(/* private field */);
```

##### Fields

| Index | Type | Documentation |
|-------|------|---------------|
| 0 | `private` | *Private field* |

##### Implementations

###### Trait Implementations

- **Sync**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Send**
- **UnwindSafe**
- **Eq**
- **Error**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **ToString**
  - ```rust
    fn to_string(self: &Self) -> String { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **StructuralPartialEq**
- **Freeze**
- **RefUnwindSafe**
- **PartialEq**
  - ```rust
    fn eq(self: &Self, other: &Error) -> bool { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Display**
  - ```rust
    fn fmt(self: &Self, f: &mut fmt::Formatter<''_>) -> fmt::Result { /* ... */ }
    ```

- **Unpin**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> Error { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Copy**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

#### Type Alias `Result`

The result type used within the read module.

```rust
pub type Result<T> = result::Result<T, Error>;
```

#### Type Alias `NativeFile`

**Attributes:**

- `#[<cfg>(all(unix, not(target_os = "macos"), target_pointer_width = "64",
feature = "elf"))]`

The native executable file for the target platform.

```rust
pub type NativeFile<''data, R = &''data [u8]> = elf::ElfFile64<''data, crate::endian::Endianness, R>;
```

#### Enum `FileKind`

**Attributes:**

- `#[non_exhaustive]`

A file format kind.

```rust
pub enum FileKind {
    Archive,
    Coff,
    CoffBig,
    CoffImport,
    DyldCache,
    Elf32,
    Elf64,
    MachO32,
    MachO64,
    MachOFat32,
    MachOFat64,
    Pe32,
    Pe64,
    Xcoff32,
    Xcoff64,
}
```

##### Variants

###### `Archive`

A Unix archive.

See [`archive::ArchiveFile`].

###### `Coff`

A COFF object file.

See [`coff::CoffFile`].

###### `CoffBig`

A COFF bigobj object file.

This supports a larger number of sections.

See [`coff::CoffBigFile`].

###### `CoffImport`

A Windows short import file.

See [`coff::ImportFile`].

###### `DyldCache`

A dyld cache file containing Mach-O images.

See [`macho::DyldCache`]

###### `Elf32`

A 32-bit ELF file.

See [`elf::ElfFile32`].

###### `Elf64`

A 64-bit ELF file.

See [`elf::ElfFile64`].

###### `MachO32`

A 32-bit Mach-O file.

See [`macho::MachOFile32`].

###### `MachO64`

A 64-bit Mach-O file.

See [`macho::MachOFile64`].

###### `MachOFat32`

A 32-bit Mach-O fat binary.

See [`macho::MachOFatFile32`].

###### `MachOFat64`

A 64-bit Mach-O fat binary.

See [`macho::MachOFatFile64`].

###### `Pe32`

A 32-bit PE file.

See [`pe::PeFile32`].

###### `Pe64`

A 64-bit PE file.

See [`pe::PeFile64`].

###### `Xcoff32`

A 32-bit XCOFF file.

See [`xcoff::XcoffFile32`].

###### `Xcoff64`

A 64-bit XCOFF file.

See [`xcoff::XcoffFile64`].

##### Implementations

###### Methods

- ```rust
  pub fn parse<''data, R: ReadRef<''data>>(data: R) -> Result<FileKind> { /* ... */ }
  ```
  Determine a file kind by parsing the start of the file.

- ```rust
  pub fn parse_at<''data, R: ReadRef<''data>>(data: R, offset: u64) -> Result<FileKind> { /* ... */ }
  ```
  Determine a file kind by parsing at the given offset.

###### Trait Implementations

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Sync**
- **Copy**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **StructuralPartialEq**
- **UnwindSafe**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Hash**
  - ```rust
    fn hash<__H: $crate::hash::Hasher>(self: &Self, state: &mut __H) { /* ... */ }
    ```

- **PartialEq**
  - ```rust
    fn eq(self: &Self, other: &FileKind) -> bool { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **RefUnwindSafe**
- **Eq**
- **Freeze**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Unpin**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> FileKind { /* ... */ }
    ```

- **Send**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

#### Enum `ObjectKind`

**Attributes:**

- `#[non_exhaustive]`

An object kind.

Returned by [`Object::kind`].

```rust
pub enum ObjectKind {
    Unknown,
    Relocatable,
    Executable,
    Dynamic,
    Core,
}
```

##### Variants

###### `Unknown`

The object kind is unknown.

###### `Relocatable`

Relocatable object.

###### `Executable`

Executable.

###### `Dynamic`

Dynamic shared object.

###### `Core`

Core.

##### Implementations

###### Trait Implementations

- **Freeze**
- **RefUnwindSafe**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Unpin**
- **UnwindSafe**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> ObjectKind { /* ... */ }
    ```

- **PartialEq**
  - ```rust
    fn eq(self: &Self, other: &ObjectKind) -> bool { /* ... */ }
    ```

- **Send**
- **StructuralPartialEq**
- **Eq**
- **Hash**
  - ```rust
    fn hash<__H: $crate::hash::Hasher>(self: &Self, state: &mut __H) { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Sync**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Copy**
#### Struct `SectionIndex`

The index used to identify a section in a file.

```rust
pub struct SectionIndex(pub usize);
```

##### Fields

| Index | Type | Documentation |
|-------|------|---------------|
| 0 | `usize` |  |

##### Implementations

###### Trait Implementations

- **Unpin**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> SectionIndex { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Sync**
- **Freeze**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **StructuralPartialEq**
- **PartialEq**
  - ```rust
    fn eq(self: &Self, other: &SectionIndex) -> bool { /* ... */ }
    ```

- **UnwindSafe**
- **Display**
  - ```rust
    fn fmt(self: &Self, f: &mut fmt::Formatter<''_>) -> fmt::Result { /* ... */ }
    ```

- **Send**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **ToString**
  - ```rust
    fn to_string(self: &Self) -> String { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **RefUnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Copy**
- **Eq**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Hash**
  - ```rust
    fn hash<__H: $crate::hash::Hasher>(self: &Self, state: &mut __H) { /* ... */ }
    ```

#### Struct `SymbolIndex`

The index used to identify a symbol in a symbol table.

```rust
pub struct SymbolIndex(pub usize);
```

##### Fields

| Index | Type | Documentation |
|-------|------|---------------|
| 0 | `usize` |  |

##### Implementations

###### Trait Implementations

- **Clone**
  - ```rust
    fn clone(self: &Self) -> SymbolIndex { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **StructuralPartialEq**
- **Eq**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Copy**
- **Hash**
  - ```rust
    fn hash<__H: $crate::hash::Hasher>(self: &Self, state: &mut __H) { /* ... */ }
    ```

- **UnwindSafe**
- **Display**
  - ```rust
    fn fmt(self: &Self, f: &mut fmt::Formatter<''_>) -> fmt::Result { /* ... */ }
    ```

- **Freeze**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **ToString**
  - ```rust
    fn to_string(self: &Self) -> String { /* ... */ }
    ```

- **Unpin**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **RefUnwindSafe**
- **Send**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **PartialEq**
  - ```rust
    fn eq(self: &Self, other: &SymbolIndex) -> bool { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Sync**
#### Enum `SymbolSection`

**Attributes:**

- `#[non_exhaustive]`

The section where an [`ObjectSymbol`] is defined.

```rust
pub enum SymbolSection {
    Unknown,
    None,
    Undefined,
    Absolute,
    Common,
    Section(SectionIndex),
}
```

##### Variants

###### `Unknown`

The section is unknown.

###### `None`

The section is not applicable for this symbol (such as file symbols).

###### `Undefined`

The symbol is undefined.

###### `Absolute`

The symbol has an absolute value.

###### `Common`

The symbol is a zero-initialized symbol that will be combined with duplicate definitions.

###### `Section`

The symbol is defined in the given section.

Fields:

| Index | Type | Documentation |
|-------|------|---------------|
| 0 | `SectionIndex` |  |

##### Implementations

###### Methods

- ```rust
  pub fn index(self: Self) -> Option<SectionIndex> { /* ... */ }
  ```
  Returns the section index for the section where the symbol is defined.

###### Trait Implementations

- **Freeze**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> SymbolSection { /* ... */ }
    ```

- **Unpin**
- **Eq**
- **RefUnwindSafe**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **PartialEq**
  - ```rust
    fn eq(self: &Self, other: &SymbolSection) -> bool { /* ... */ }
    ```

- **Hash**
  - ```rust
    fn hash<__H: $crate::hash::Hasher>(self: &Self, state: &mut __H) { /* ... */ }
    ```

- **StructuralPartialEq**
- **UnwindSafe**
- **Copy**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Sync**
- **Send**
#### Struct `SymbolMap`

A map from addresses to symbol information.

The symbol information depends on the chosen entry type, such as [`SymbolMapName`].

Returned by [`Object::symbol_map`].

```rust
pub struct SymbolMap<T: SymbolMapEntry> {
    // Some fields omitted
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| *private fields* | ... | *Some fields have been omitted* |

##### Implementations

###### Methods

- ```rust
  pub fn new(symbols: Vec<T>) -> Self { /* ... */ }
  ```
  Construct a new symbol map.

- ```rust
  pub fn get(self: &Self, address: u64) -> Option<&T> { /* ... */ }
  ```
  Get the symbol before the given address.

- ```rust
  pub fn symbols(self: &Self) -> &[T] { /* ... */ }
  ```
  Get all symbols in the map.

###### Trait Implementations

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Default**
  - ```rust
    fn default() -> SymbolMap<T> { /* ... */ }
    ```

- **UnwindSafe**
- **Sync**
- **Freeze**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Unpin**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> SymbolMap<T> { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **RefUnwindSafe**
- **Send**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

#### Struct `SymbolMapName`

The type used for entries in a [`SymbolMap`] that maps from addresses to names.

```rust
pub struct SymbolMapName<''data> {
    // Some fields omitted
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| *private fields* | ... | *Some fields have been omitted* |

##### Implementations

###### Methods

- ```rust
  pub fn new(address: u64, name: &''data str) -> Self { /* ... */ }
  ```
  Construct a `SymbolMapName`.

- ```rust
  pub fn address(self: &Self) -> u64 { /* ... */ }
  ```
  The symbol address.

- ```rust
  pub fn name(self: &Self) -> &''data str { /* ... */ }
  ```
  The symbol name.

###### Trait Implementations

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **SymbolMapEntry**
  - ```rust
    fn address(self: &Self) -> u64 { /* ... */ }
    ```

- **Sync**
- **Unpin**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Eq**
- **Send**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> SymbolMapName<''data> { /* ... */ }
    ```

- **Hash**
  - ```rust
    fn hash<__H: $crate::hash::Hasher>(self: &Self, state: &mut __H) { /* ... */ }
    ```

- **RefUnwindSafe**
- **Copy**
- **Freeze**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **UnwindSafe**
- **StructuralPartialEq**
- **PartialEq**
  - ```rust
    fn eq(self: &Self, other: &SymbolMapName<''data>) -> bool { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

#### Struct `ObjectMap`

A map from addresses to symbol names and object files.

This is derived from STAB entries in Mach-O files.

Returned by [`Object::object_map`].

```rust
pub struct ObjectMap<''data> {
    // Some fields omitted
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| *private fields* | ... | *Some fields have been omitted* |

##### Implementations

###### Methods

- ```rust
  pub fn get(self: &Self, address: u64) -> Option<&ObjectMapEntry<''data>> { /* ... */ }
  ```
  Get the entry containing the given address.

- ```rust
  pub fn symbols(self: &Self) -> &[ObjectMapEntry<''data>] { /* ... */ }
  ```
  Get all symbols in the map.

- ```rust
  pub fn objects(self: &Self) -> &[ObjectMapFile<''data>] { /* ... */ }
  ```
  Get all objects in the map.

###### Trait Implementations

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Freeze**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Unpin**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> ObjectMap<''data> { /* ... */ }
    ```

- **RefUnwindSafe**
- **UnwindSafe**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Default**
  - ```rust
    fn default() -> ObjectMap<''data> { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Sync**
- **Send**
#### Struct `ObjectMapEntry`

A symbol in an [`ObjectMap`].

```rust
pub struct ObjectMapEntry<''data> {
    // Some fields omitted
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| *private fields* | ... | *Some fields have been omitted* |

##### Implementations

###### Methods

- ```rust
  pub fn address(self: &Self) -> u64 { /* ... */ }
  ```
  Get the symbol address.

- ```rust
  pub fn size(self: &Self) -> u64 { /* ... */ }
  ```
  Get the symbol size.

- ```rust
  pub fn name(self: &Self) -> &''data [u8] { /* ... */ }
  ```
  Get the symbol name.

- ```rust
  pub fn object_index(self: &Self) -> usize { /* ... */ }
  ```
  Get the index of the object file name.

- ```rust
  pub fn object<''a>(self: &Self, map: &''a ObjectMap<''data>) -> &''a ObjectMapFile<''data> { /* ... */ }
  ```
  Get the object file name.

###### Trait Implementations

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **PartialEq**
  - ```rust
    fn eq(self: &Self, other: &ObjectMapEntry<''data>) -> bool { /* ... */ }
    ```

- **Eq**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> ObjectMapEntry<''data> { /* ... */ }
    ```

- **Copy**
- **Freeze**
- **Sync**
- **RefUnwindSafe**
- **Unpin**
- **Send**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **UnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Default**
  - ```rust
    fn default() -> ObjectMapEntry<''data> { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **StructuralPartialEq**
- **Hash**
  - ```rust
    fn hash<__H: $crate::hash::Hasher>(self: &Self, state: &mut __H) { /* ... */ }
    ```

- **SymbolMapEntry**
  - ```rust
    fn address(self: &Self) -> u64 { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

#### Struct `ObjectMapFile`

An object file name in an [`ObjectMap`].

```rust
pub struct ObjectMapFile<''data> {
    // Some fields omitted
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| *private fields* | ... | *Some fields have been omitted* |

##### Implementations

###### Methods

- ```rust
  pub fn path(self: &Self) -> &''data [u8] { /* ... */ }
  ```
  Get the path to the file containing the object.

- ```rust
  pub fn member(self: &Self) -> Option<&''data [u8]> { /* ... */ }
  ```
  If the file is an archive, get the name of the member containing the object.

###### Trait Implementations

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ObjectMapFile<''data> { /* ... */ }
    ```

- **Copy**
- **PartialEq**
  - ```rust
    fn eq(self: &Self, other: &ObjectMapFile<''data>) -> bool { /* ... */ }
    ```

- **Hash**
  - ```rust
    fn hash<__H: $crate::hash::Hasher>(self: &Self, state: &mut __H) { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Sync**
- **Freeze**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Send**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Eq**
- **UnwindSafe**
- **Unpin**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **StructuralPartialEq**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **RefUnwindSafe**
#### Struct `Import`

An imported symbol.

Returned by [`Object::imports`].

```rust
pub struct Import<''data> {
    // Some fields omitted
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| *private fields* | ... | *Some fields have been omitted* |

##### Implementations

###### Methods

- ```rust
  pub fn name(self: &Self) -> &''data [u8] { /* ... */ }
  ```
  The symbol name.

- ```rust
  pub fn library(self: &Self) -> &''data [u8] { /* ... */ }
  ```
  The name of the library to import the symbol from.

###### Trait Implementations

- **Eq**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Sync**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **PartialEq**
  - ```rust
    fn eq(self: &Self, other: &Import<''data>) -> bool { /* ... */ }
    ```

- **Freeze**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **UnwindSafe**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Copy**
- **StructuralPartialEq**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **RefUnwindSafe**
- **Send**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> Import<''data> { /* ... */ }
    ```

- **Unpin**
#### Struct `Export`

An exported symbol.

Returned by [`Object::exports`].

```rust
pub struct Export<''data> {
    // Some fields omitted
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| *private fields* | ... | *Some fields have been omitted* |

##### Implementations

###### Methods

- ```rust
  pub fn name(self: &Self) -> &''data [u8] { /* ... */ }
  ```
  The symbol name.

- ```rust
  pub fn address(self: &Self) -> u64 { /* ... */ }
  ```
  The virtual address of the symbol.

###### Trait Implementations

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Copy**
- **Send**
- **PartialEq**
  - ```rust
    fn eq(self: &Self, other: &Export<''data>) -> bool { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Eq**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **UnwindSafe**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Unpin**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **StructuralPartialEq**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> Export<''data> { /* ... */ }
    ```

- **Sync**
- **Freeze**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **RefUnwindSafe**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

#### Struct `CodeView`

PDB information from the debug directory in a PE file.

```rust
pub struct CodeView<''data> {
    // Some fields omitted
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| *private fields* | ... | *Some fields have been omitted* |

##### Implementations

###### Methods

- ```rust
  pub fn path(self: &Self) -> &''data [u8] { /* ... */ }
  ```
  The path to the PDB as stored in CodeView.

- ```rust
  pub fn age(self: &Self) -> u32 { /* ... */ }
  ```
  The age of the PDB.

- ```rust
  pub fn guid(self: &Self) -> [u8; 16] { /* ... */ }
  ```
  The GUID of the PDB.

###### Trait Implementations

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **RefUnwindSafe**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Sync**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> CodeView<''data> { /* ... */ }
    ```

- **UnwindSafe**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Send**
- **Freeze**
- **Copy**
- **StructuralPartialEq**
- **PartialEq**
  - ```rust
    fn eq(self: &Self, other: &CodeView<''data>) -> bool { /* ... */ }
    ```

- **Unpin**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Eq**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

#### Enum `RelocationTarget`

**Attributes:**

- `#[non_exhaustive]`

The target referenced by a [`Relocation`].

```rust
pub enum RelocationTarget {
    Symbol(SymbolIndex),
    Section(SectionIndex),
    Absolute,
}
```

##### Variants

###### `Symbol`

The target is a symbol.

Fields:

| Index | Type | Documentation |
|-------|------|---------------|
| 0 | `SymbolIndex` |  |

###### `Section`

The target is a section.

Fields:

| Index | Type | Documentation |
|-------|------|---------------|
| 0 | `SectionIndex` |  |

###### `Absolute`

The offset is an absolute address.

##### Implementations

###### Trait Implementations

- **Eq**
- **Send**
- **StructuralPartialEq**
- **Freeze**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> RelocationTarget { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Sync**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **PartialEq**
  - ```rust
    fn eq(self: &Self, other: &RelocationTarget) -> bool { /* ... */ }
    ```

- **UnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **RefUnwindSafe**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Copy**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Hash**
  - ```rust
    fn hash<__H: $crate::hash::Hasher>(self: &Self, state: &mut __H) { /* ... */ }
    ```

- **Unpin**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

#### Struct `Relocation`

A relocation entry.

Returned by [`Object::dynamic_relocations`] or [`ObjectSection::relocations`].

```rust
pub struct Relocation {
    // Some fields omitted
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| *private fields* | ... | *Some fields have been omitted* |

##### Implementations

###### Methods

- ```rust
  pub fn kind(self: &Self) -> RelocationKind { /* ... */ }
  ```
  The operation used to calculate the result of the relocation.

- ```rust
  pub fn encoding(self: &Self) -> RelocationEncoding { /* ... */ }
  ```
  Information about how the result of the relocation operation is encoded in the place.

- ```rust
  pub fn size(self: &Self) -> u8 { /* ... */ }
  ```
  The size in bits of the place of the relocation.

- ```rust
  pub fn target(self: &Self) -> RelocationTarget { /* ... */ }
  ```
  The target of the relocation.

- ```rust
  pub fn addend(self: &Self) -> i64 { /* ... */ }
  ```
  The addend to use in the relocation calculation.

- ```rust
  pub fn set_addend(self: &mut Self, addend: i64) { /* ... */ }
  ```
  Set the addend to use in the relocation calculation.

- ```rust
  pub fn has_implicit_addend(self: &Self) -> bool { /* ... */ }
  ```
  Returns true if there is an implicit addend stored in the data at the offset

- ```rust
  pub fn flags(self: &Self) -> RelocationFlags { /* ... */ }
  ```
  Relocation flags that are specific to each file format.

###### Trait Implementations

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Send**
- **UnwindSafe**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Freeze**
- **Sync**
- **Unpin**
- **RefUnwindSafe**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

#### Struct `RelocationMap`

A map from section offsets to relocation information.

This can be used to apply relocations to a value at a given section offset.
This is intended for use with DWARF in relocatable object files, and only
supports relocations that are used in DWARF.

Returned by [`ObjectSection::relocation_map`].

```rust
pub struct RelocationMap(/* private field */);
```

##### Fields

| Index | Type | Documentation |
|-------|------|---------------|
| 0 | `private` | *Private field* |

##### Implementations

###### Methods

- ```rust
  pub fn new<''data, ''file, T>(file: &''file T, section: &<T as >::Section<''file>) -> Result<Self>
where
    T: Object<''data> { /* ... */ }
  ```
  Construct a new relocation map for a section.

- ```rust
  pub fn add<''data: ''file, ''file, T>(self: &mut Self, file: &''file T, offset: u64, relocation: Relocation) -> Result<()>
where
    T: Object<''data> { /* ... */ }
  ```
  Add a single relocation to the map.

- ```rust
  pub fn relocate(self: &Self, offset: u64, value: u64) -> u64 { /* ... */ }
  ```
  Relocate a value that was read from the section at the given offset.

###### Trait Implementations

- **Freeze**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Default**
  - ```rust
    fn default() -> RelocationMap { /* ... */ }
    ```

- **Sync**
- **RefUnwindSafe**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Send**
- **UnwindSafe**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Unpin**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

#### Enum `CompressionFormat`

**Attributes:**

- `#[non_exhaustive]`

A data compression format.

```rust
pub enum CompressionFormat {
    None,
    Unknown,
    Zlib,
    Zstandard,
}
```

##### Variants

###### `None`

The data is uncompressed.

###### `Unknown`

The data is compressed, but the compression format is unknown.

###### `Zlib`

ZLIB/DEFLATE.

Used for ELF compression and GNU compressed debug information.

###### `Zstandard`

Zstandard.

Used for ELF compression.

##### Implementations

###### Trait Implementations

- **Freeze**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Hash**
  - ```rust
    fn hash<__H: $crate::hash::Hasher>(self: &Self, state: &mut __H) { /* ... */ }
    ```

- **Send**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> CompressionFormat { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Copy**
- **Unpin**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **UnwindSafe**
- **Sync**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **PartialEq**
  - ```rust
    fn eq(self: &Self, other: &CompressionFormat) -> bool { /* ... */ }
    ```

- **Eq**
- **StructuralPartialEq**
- **RefUnwindSafe**
#### Struct `CompressedFileRange`

A range in a file that may be compressed.

Returned by [`ObjectSection::compressed_file_range`].

```rust
pub struct CompressedFileRange {
    pub format: CompressionFormat,
    pub offset: u64,
    pub compressed_size: u64,
    pub uncompressed_size: u64,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `format` | `CompressionFormat` | The data compression format. |
| `offset` | `u64` | The file offset of the compressed data. |
| `compressed_size` | `u64` | The compressed data size. |
| `uncompressed_size` | `u64` | The uncompressed data size. |

##### Implementations

###### Methods

- ```rust
  pub fn none(range: Option<(u64, u64)>) -> Self { /* ... */ }
  ```
  Data that is uncompressed.

- ```rust
  pub fn data<''data, R: ReadRef<''data>>(self: Self, file: R) -> Result<CompressedData<''data>> { /* ... */ }
  ```
  Convert to [`CompressedData`] by reading from the file.

###### Trait Implementations

- **Copy**
- **Unpin**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **RefUnwindSafe**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Sync**
- **UnwindSafe**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **StructuralPartialEq**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **PartialEq**
  - ```rust
    fn eq(self: &Self, other: &CompressedFileRange) -> bool { /* ... */ }
    ```

- **Hash**
  - ```rust
    fn hash<__H: $crate::hash::Hasher>(self: &Self, state: &mut __H) { /* ... */ }
    ```

- **Freeze**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> CompressedFileRange { /* ... */ }
    ```

- **Eq**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Send**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

#### Struct `CompressedData`

Data that may be compressed.

Returned by [`ObjectSection::compressed_data`].

```rust
pub struct CompressedData<''data> {
    pub format: CompressionFormat,
    pub data: &''data [u8],
    pub uncompressed_size: u64,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `format` | `CompressionFormat` | The data compression format. |
| `data` | `&''data [u8]` | The compressed data. |
| `uncompressed_size` | `u64` | The uncompressed data size. |

##### Implementations

###### Methods

- ```rust
  pub fn none(data: &''data [u8]) -> Self { /* ... */ }
  ```
  Data that is uncompressed.

- ```rust
  pub fn decompress(self: Self) -> Result<Cow<''data, [u8]>> { /* ... */ }
  ```
  Return the uncompressed data.

###### Trait Implementations

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **RefUnwindSafe**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Send**
- **Sync**
- **Copy**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> CompressedData<''data> { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **PartialEq**
  - ```rust
    fn eq(self: &Self, other: &CompressedData<''data>) -> bool { /* ... */ }
    ```

- **Freeze**
- **StructuralPartialEq**
- **Eq**
- **Hash**
  - ```rust
    fn hash<__H: $crate::hash::Hasher>(self: &Self, state: &mut __H) { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Unpin**
- **UnwindSafe**
### Traits

#### Trait `SymbolMapEntry`

An entry in a [`SymbolMap`].

```rust
pub trait SymbolMapEntry {
    /* Associated items */
}
```

##### Required Items

###### Required Methods

- `address`: The symbol address.

##### Implementations

This trait is implemented for the following types:

- `SymbolMapName<''data>` with <''data>
- `ObjectMapEntry<''data>` with <''data>

### Re-exports

#### Re-export `crate::common::*`

```rust
pub use crate::common::*;
```

#### Re-export `read_ref::*`

```rust
pub use read_ref::*;
```

#### Re-export `read_cache::*`

```rust
pub use read_cache::*;
```

#### Re-export `util::*`

```rust
pub use util::*;
```

#### Re-export `any::*`

**Attributes:**

- `#[<cfg>(any(feature = "coff", feature = "elf", feature = "macho", feature =
"pe", feature = "wasm", feature = "xcoff"))]`

```rust
pub use any::*;
```

#### Re-export `traits::*`

```rust
pub use traits::*;
```

## Module `archive`

**Attributes:**

- `#[<cfg>(feature = "archive")]`

Archive definitions.

These definitions are independent of read/write support, although we do implement
some traits useful for those.

```rust
pub mod archive { /* ... */ }
```

### Types

#### Struct `Header`

**Attributes:**

- `#[repr(C)]`

The header at the start of an archive member.

```rust
pub struct Header {
    pub name: [u8; 16],
    pub date: [u8; 12],
    pub uid: [u8; 6],
    pub gid: [u8; 6],
    pub mode: [u8; 8],
    pub size: [u8; 10],
    pub terminator: [u8; 2],
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `name` | `[u8; 16]` | The file name. |
| `date` | `[u8; 12]` | File modification timestamp in decimal. |
| `uid` | `[u8; 6]` | User ID in decimal. |
| `gid` | `[u8; 6]` | Group ID in decimal. |
| `mode` | `[u8; 8]` | File mode in octal. |
| `size` | `[u8; 10]` | File size in decimal. |
| `terminator` | `[u8; 2]` | Must be equal to `TERMINATOR`. |

##### Implementations

###### Trait Implementations

- **UnwindSafe**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Unpin**
- **Sync**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Copy**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Pod**
- **Freeze**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Send**
- **RefUnwindSafe**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> Header { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

#### Struct `AixHeader`

**Attributes:**

- `#[repr(C)]`

The header at the start of an AIX big archive member, without name.

```rust
pub struct AixHeader {
    pub size: [u8; 20],
    pub nxtmem: [u8; 20],
    pub prvmem: [u8; 20],
    pub date: [u8; 12],
    pub uid: [u8; 12],
    pub gid: [u8; 12],
    pub mode: [u8; 12],
    pub namlen: [u8; 4],
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `size` | `[u8; 20]` | File member size in decimal. |
| `nxtmem` | `[u8; 20]` | Next member offset in decimal. |
| `prvmem` | `[u8; 20]` | Previous member offset in decimal. |
| `date` | `[u8; 12]` | File member date in decimal. |
| `uid` | `[u8; 12]` | File member user id in decimal. |
| `gid` | `[u8; 12]` | File member group id in decimal. |
| `mode` | `[u8; 12]` | File member mode in octal. |
| `namlen` | `[u8; 4]` | File member name length in decimal. |

##### Implementations

###### Trait Implementations

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Freeze**
- **Pod**
- **Send**
- **UnwindSafe**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Unpin**
- **Sync**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **RefUnwindSafe**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> AixHeader { /* ... */ }
    ```

- **Copy**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

#### Struct `AixFileHeader`

**Attributes:**

- `#[repr(C)]`

The AIX big archive's fixed length header at file beginning.

```rust
pub struct AixFileHeader {
    pub magic: [u8; 8],
    pub memoff: [u8; 20],
    pub gstoff: [u8; 20],
    pub gst64off: [u8; 20],
    pub fstmoff: [u8; 20],
    pub lstmoff: [u8; 20],
    pub freeoff: [u8; 20],
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `magic` | `[u8; 8]` | Archive magic string. |
| `memoff` | `[u8; 20]` | Offset of member table. |
| `gstoff` | `[u8; 20]` | Offset of global symbol table. |
| `gst64off` | `[u8; 20]` | Offset of global symbol table for 64-bit objects. |
| `fstmoff` | `[u8; 20]` | Offset of first member. |
| `lstmoff` | `[u8; 20]` | Offset of last member. |
| `freeoff` | `[u8; 20]` | Offset of first member on free list. |

##### Implementations

###### Trait Implementations

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Unpin**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Sync**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **UnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> AixFileHeader { /* ... */ }
    ```

- **Copy**
- **Pod**
- **Freeze**
- **Send**
- **RefUnwindSafe**
#### Struct `AixMemberOffset`

**Attributes:**

- `#[repr(C)]`

Offset of a member in an AIX big archive.

This is used in the member index.

```rust
pub struct AixMemberOffset(pub [u8; 20]);
```

##### Fields

| Index | Type | Documentation |
|-------|------|---------------|
| 0 | `[u8; 20]` |  |

##### Implementations

###### Trait Implementations

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Copy**
- **Send**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **RefUnwindSafe**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> AixMemberOffset { /* ... */ }
    ```

- **Freeze**
- **Sync**
- **Pod**
- **UnwindSafe**
- **Unpin**
### Constants and Statics

#### Constant `MAGIC`

File identification bytes stored at the beginning of the file.

```rust
pub const MAGIC: [u8; 8] = _;
```

#### Constant `AIX_BIG_MAGIC`

File identification bytes at the beginning of AIX big archive.

```rust
pub const AIX_BIG_MAGIC: [u8; 8] = _;
```

#### Constant `THIN_MAGIC`

File identification bytes stored at the beginning of a thin archive.

A thin archive only contains a symbol table and file names.

```rust
pub const THIN_MAGIC: [u8; 8] = _;
```

#### Constant `TERMINATOR`

The terminator for each archive member header.

```rust
pub const TERMINATOR: [u8; 2] = _;
```

## Module `elf`

**Attributes:**

- `#[<cfg>(feature = "elf")]`
- `#[allow(missing_docs)]`
- `#[allow(clippy::identity_op)]`

ELF definitions.

These definitions are independent of read/write support, although we do implement
some traits useful for those.

This module is the equivalent of /usr/include/elf.h, and is based heavily on it.

```rust
pub mod elf { /* ... */ }
```

### Types

#### Struct `FileHeader32`

**Attributes:**

- `#[repr(C)]`

The header at the start of every 32-bit ELF file.

```rust
pub struct FileHeader32<E: Endian> {
    pub e_ident: Ident,
    pub e_type: crate::endian::U16<E>,
    pub e_machine: crate::endian::U16<E>,
    pub e_version: crate::endian::U32<E>,
    pub e_entry: crate::endian::U32<E>,
    pub e_phoff: crate::endian::U32<E>,
    pub e_shoff: crate::endian::U32<E>,
    pub e_flags: crate::endian::U32<E>,
    pub e_ehsize: crate::endian::U16<E>,
    pub e_phentsize: crate::endian::U16<E>,
    pub e_phnum: crate::endian::U16<E>,
    pub e_shentsize: crate::endian::U16<E>,
    pub e_shnum: crate::endian::U16<E>,
    pub e_shstrndx: crate::endian::U16<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `e_ident` | `Ident` | Magic number and other information. |
| `e_type` | `crate::endian::U16<E>` | Object file type. One of the `ET_*` constants. |
| `e_machine` | `crate::endian::U16<E>` | Architecture. One of the `EM_*` constants. |
| `e_version` | `crate::endian::U32<E>` | Object file version. Must be `EV_CURRENT`. |
| `e_entry` | `crate::endian::U32<E>` | Entry point virtual address. |
| `e_phoff` | `crate::endian::U32<E>` | Program header table file offset. |
| `e_shoff` | `crate::endian::U32<E>` | Section header table file offset. |
| `e_flags` | `crate::endian::U32<E>` | Processor-specific flags.<br><br>A combination of the `EF_*` constants. |
| `e_ehsize` | `crate::endian::U16<E>` | Size in bytes of this header. |
| `e_phentsize` | `crate::endian::U16<E>` | Program header table entry size. |
| `e_phnum` | `crate::endian::U16<E>` | Program header table entry count.<br><br>If the count is greater than or equal to `PN_XNUM` then this field is set to<br>`PN_XNUM` and the count is stored in the `sh_info` field of section 0. |
| `e_shentsize` | `crate::endian::U16<E>` | Section header table entry size. |
| `e_shnum` | `crate::endian::U16<E>` | Section header table entry count.<br><br>If the count is greater than or equal to `SHN_LORESERVE` then this field is set to<br>`0` and the count is stored in the `sh_size` field of section 0.<br>first section header. |
| `e_shstrndx` | `crate::endian::U16<E>` | Section header string table index.<br><br>If the index is greater than or equal to `SHN_LORESERVE` then this field is set to<br>`SHN_XINDEX` and the index is stored in the `sh_link` field of section 0. |

##### Implementations

###### Trait Implementations

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Send**
- **Unpin**
- **RefUnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Clone**
  - ```rust
    fn clone(self: &Self) -> FileHeader32<E> { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Pod**
- **Freeze**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **FileHeader**
  - ```rust
    fn is_type_64(self: &Self) -> bool { /* ... */ }
    ```

  - ```rust
    fn is_type_64_sized() -> bool
where
    Self: Sized { /* ... */ }
    ```

  - ```rust
    fn e_ident(self: &Self) -> &elf::Ident { /* ... */ }
    ```

  - ```rust
    fn e_type(self: &Self, endian: <Self as >::Endian) -> u16 { /* ... */ }
    ```

  - ```rust
    fn e_machine(self: &Self, endian: <Self as >::Endian) -> u16 { /* ... */ }
    ```

  - ```rust
    fn e_version(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn e_entry(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn e_phoff(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn e_shoff(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn e_flags(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn e_ehsize(self: &Self, endian: <Self as >::Endian) -> u16 { /* ... */ }
    ```

  - ```rust
    fn e_phentsize(self: &Self, endian: <Self as >::Endian) -> u16 { /* ... */ }
    ```

  - ```rust
    fn e_phnum(self: &Self, endian: <Self as >::Endian) -> u16 { /* ... */ }
    ```

  - ```rust
    fn e_shentsize(self: &Self, endian: <Self as >::Endian) -> u16 { /* ... */ }
    ```

  - ```rust
    fn e_shnum(self: &Self, endian: <Self as >::Endian) -> u16 { /* ... */ }
    ```

  - ```rust
    fn e_shstrndx(self: &Self, endian: <Self as >::Endian) -> u16 { /* ... */ }
    ```

- **UnwindSafe**
- **Sync**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Copy**
#### Struct `FileHeader64`

**Attributes:**

- `#[repr(C)]`

The header at the start of every 64-bit ELF file.

```rust
pub struct FileHeader64<E: Endian> {
    pub e_ident: Ident,
    pub e_type: crate::endian::U16<E>,
    pub e_machine: crate::endian::U16<E>,
    pub e_version: crate::endian::U32<E>,
    pub e_entry: crate::endian::U64<E>,
    pub e_phoff: crate::endian::U64<E>,
    pub e_shoff: crate::endian::U64<E>,
    pub e_flags: crate::endian::U32<E>,
    pub e_ehsize: crate::endian::U16<E>,
    pub e_phentsize: crate::endian::U16<E>,
    pub e_phnum: crate::endian::U16<E>,
    pub e_shentsize: crate::endian::U16<E>,
    pub e_shnum: crate::endian::U16<E>,
    pub e_shstrndx: crate::endian::U16<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `e_ident` | `Ident` | Magic number and other information. |
| `e_type` | `crate::endian::U16<E>` | Object file type. One of the `ET_*` constants. |
| `e_machine` | `crate::endian::U16<E>` | Architecture. One of the `EM_*` constants. |
| `e_version` | `crate::endian::U32<E>` | Object file version. Must be `EV_CURRENT`. |
| `e_entry` | `crate::endian::U64<E>` | Entry point virtual address. |
| `e_phoff` | `crate::endian::U64<E>` | Program header table file offset. |
| `e_shoff` | `crate::endian::U64<E>` | Section header table file offset. |
| `e_flags` | `crate::endian::U32<E>` | Processor-specific flags.<br><br>A combination of the `EF_*` constants. |
| `e_ehsize` | `crate::endian::U16<E>` | Size in bytes of this header. |
| `e_phentsize` | `crate::endian::U16<E>` | Program header table entry size. |
| `e_phnum` | `crate::endian::U16<E>` | Program header table entry count.<br><br>If the count is greater than or equal to `PN_XNUM` then this field is set to<br>`PN_XNUM` and the count is stored in the `sh_info` field of section 0. |
| `e_shentsize` | `crate::endian::U16<E>` | Section header table entry size. |
| `e_shnum` | `crate::endian::U16<E>` | Section header table entry count.<br><br>If the count is greater than or equal to `SHN_LORESERVE` then this field is set to<br>`0` and the count is stored in the `sh_size` field of section 0.<br>first section header. |
| `e_shstrndx` | `crate::endian::U16<E>` | Section header string table index.<br><br>If the index is greater than or equal to `SHN_LORESERVE` then this field is set to<br>`SHN_XINDEX` and the index is stored in the `sh_link` field of section 0. |

##### Implementations

###### Trait Implementations

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Sync**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Copy**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **RefUnwindSafe**
- **Send**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> FileHeader64<E> { /* ... */ }
    ```

- **UnwindSafe**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Unpin**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Freeze**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Pod**
- **FileHeader**
  - ```rust
    fn is_type_64(self: &Self) -> bool { /* ... */ }
    ```

  - ```rust
    fn is_type_64_sized() -> bool
where
    Self: Sized { /* ... */ }
    ```

  - ```rust
    fn e_ident(self: &Self) -> &elf::Ident { /* ... */ }
    ```

  - ```rust
    fn e_type(self: &Self, endian: <Self as >::Endian) -> u16 { /* ... */ }
    ```

  - ```rust
    fn e_machine(self: &Self, endian: <Self as >::Endian) -> u16 { /* ... */ }
    ```

  - ```rust
    fn e_version(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn e_entry(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn e_phoff(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn e_shoff(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn e_flags(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn e_ehsize(self: &Self, endian: <Self as >::Endian) -> u16 { /* ... */ }
    ```

  - ```rust
    fn e_phentsize(self: &Self, endian: <Self as >::Endian) -> u16 { /* ... */ }
    ```

  - ```rust
    fn e_phnum(self: &Self, endian: <Self as >::Endian) -> u16 { /* ... */ }
    ```

  - ```rust
    fn e_shentsize(self: &Self, endian: <Self as >::Endian) -> u16 { /* ... */ }
    ```

  - ```rust
    fn e_shnum(self: &Self, endian: <Self as >::Endian) -> u16 { /* ... */ }
    ```

  - ```rust
    fn e_shstrndx(self: &Self, endian: <Self as >::Endian) -> u16 { /* ... */ }
    ```

#### Struct `Ident`

**Attributes:**

- `#[repr(C)]`

Magic number and other information.

Contained in the file header.

```rust
pub struct Ident {
    pub magic: [u8; 4],
    pub class: u8,
    pub data: u8,
    pub version: u8,
    pub os_abi: u8,
    pub abi_version: u8,
    pub padding: [u8; 7],
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `magic` | `[u8; 4]` | Magic number. Must be `ELFMAG`. |
| `class` | `u8` | File class. One of the `ELFCLASS*` constants. |
| `data` | `u8` | Data encoding. One of the `ELFDATA*` constants. |
| `version` | `u8` | ELF version. Must be `EV_CURRENT`. |
| `os_abi` | `u8` | OS ABI identification. One of the `ELFOSABI*` constants. |
| `abi_version` | `u8` | ABI version.<br><br>The meaning of this field depends on the `os_abi` value. |
| `padding` | `[u8; 7]` | Padding bytes. |

##### Implementations

###### Trait Implementations

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Freeze**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> Ident { /* ... */ }
    ```

- **Copy**
- **UnwindSafe**
- **Sync**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Send**
- **RefUnwindSafe**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Unpin**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

#### Struct `SectionHeader32`

**Attributes:**

- `#[repr(C)]`

Section header.

```rust
pub struct SectionHeader32<E: Endian> {
    pub sh_name: crate::endian::U32<E>,
    pub sh_type: crate::endian::U32<E>,
    pub sh_flags: crate::endian::U32<E>,
    pub sh_addr: crate::endian::U32<E>,
    pub sh_offset: crate::endian::U32<E>,
    pub sh_size: crate::endian::U32<E>,
    pub sh_link: crate::endian::U32<E>,
    pub sh_info: crate::endian::U32<E>,
    pub sh_addralign: crate::endian::U32<E>,
    pub sh_entsize: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `sh_name` | `crate::endian::U32<E>` | Section name.<br><br>This is an offset into the section header string table. |
| `sh_type` | `crate::endian::U32<E>` | Section type. One of the `SHT_*` constants. |
| `sh_flags` | `crate::endian::U32<E>` | Section flags. A combination of the `SHF_*` constants. |
| `sh_addr` | `crate::endian::U32<E>` | Section virtual address at execution. |
| `sh_offset` | `crate::endian::U32<E>` | Section file offset. |
| `sh_size` | `crate::endian::U32<E>` | Section size in bytes. |
| `sh_link` | `crate::endian::U32<E>` | Link to another section.<br><br>The section relationship depends on the `sh_type` value. |
| `sh_info` | `crate::endian::U32<E>` | Additional section information.<br><br>The meaning of this field depends on the `sh_type` value. |
| `sh_addralign` | `crate::endian::U32<E>` | Section alignment. |
| `sh_entsize` | `crate::endian::U32<E>` | Entry size if the section holds a table. |

##### Implementations

###### Trait Implementations

- **Copy**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Unpin**
- **UnwindSafe**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **SectionHeader**
  - ```rust
    fn sh_name(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn sh_type(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn sh_flags(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn sh_addr(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn sh_offset(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn sh_size(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn sh_link(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn sh_info(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn sh_addralign(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn sh_entsize(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> SectionHeader32<E> { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Freeze**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Send**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **RefUnwindSafe**
- **Sync**
- **Pod**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

#### Struct `SectionHeader64`

**Attributes:**

- `#[repr(C)]`

Section header.

```rust
pub struct SectionHeader64<E: Endian> {
    pub sh_name: crate::endian::U32<E>,
    pub sh_type: crate::endian::U32<E>,
    pub sh_flags: crate::endian::U64<E>,
    pub sh_addr: crate::endian::U64<E>,
    pub sh_offset: crate::endian::U64<E>,
    pub sh_size: crate::endian::U64<E>,
    pub sh_link: crate::endian::U32<E>,
    pub sh_info: crate::endian::U32<E>,
    pub sh_addralign: crate::endian::U64<E>,
    pub sh_entsize: crate::endian::U64<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `sh_name` | `crate::endian::U32<E>` | Section name.<br><br>This is an offset into the section header string table. |
| `sh_type` | `crate::endian::U32<E>` | Section type. One of the `SHT_*` constants. |
| `sh_flags` | `crate::endian::U64<E>` | Section flags. A combination of the `SHF_*` constants. |
| `sh_addr` | `crate::endian::U64<E>` | Section virtual address at execution. |
| `sh_offset` | `crate::endian::U64<E>` | Section file offset. |
| `sh_size` | `crate::endian::U64<E>` | Section size in bytes. |
| `sh_link` | `crate::endian::U32<E>` | Link to another section.<br><br>The section relationship depends on the `sh_type` value. |
| `sh_info` | `crate::endian::U32<E>` | Additional section information.<br><br>The meaning of this field depends on the `sh_type` value. |
| `sh_addralign` | `crate::endian::U64<E>` | Section alignment. |
| `sh_entsize` | `crate::endian::U64<E>` | Entry size if the section holds a table. |

##### Implementations

###### Trait Implementations

- **Unpin**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> SectionHeader64<E> { /* ... */ }
    ```

- **Copy**
- **Pod**
- **Freeze**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Send**
- **Sync**
- **UnwindSafe**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **RefUnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **SectionHeader**
  - ```rust
    fn sh_name(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn sh_type(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn sh_flags(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn sh_addr(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn sh_offset(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn sh_size(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn sh_link(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn sh_info(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn sh_addralign(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn sh_entsize(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

#### Struct `CompressionHeader32`

**Attributes:**

- `#[repr(C)]`

Section compression header.

Used when `SHF_COMPRESSED` is set.

Note: this type currently allows for misaligned headers, but that may be
changed in a future version.

```rust
pub struct CompressionHeader32<E: Endian> {
    pub ch_type: crate::endian::U32Bytes<E>,
    pub ch_size: crate::endian::U32Bytes<E>,
    pub ch_addralign: crate::endian::U32Bytes<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `ch_type` | `crate::endian::U32Bytes<E>` | Compression format. One of the `ELFCOMPRESS_*` values. |
| `ch_size` | `crate::endian::U32Bytes<E>` | Uncompressed data size. |
| `ch_addralign` | `crate::endian::U32Bytes<E>` | Uncompressed data alignment. |

##### Implementations

###### Trait Implementations

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Default**
  - ```rust
    fn default() -> CompressionHeader32<E> { /* ... */ }
    ```

- **Copy**
- **Unpin**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Send**
- **CompressionHeader**
  - ```rust
    fn ch_type(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn ch_size(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn ch_addralign(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> CompressionHeader32<E> { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Pod**
- **Sync**
- **RefUnwindSafe**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Freeze**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **UnwindSafe**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

#### Struct `CompressionHeader64`

**Attributes:**

- `#[repr(C)]`

Section compression header.

Used when `SHF_COMPRESSED` is set.

Note: this type currently allows for misaligned headers, but that may be
changed in a future version.

```rust
pub struct CompressionHeader64<E: Endian> {
    pub ch_type: crate::endian::U32Bytes<E>,
    pub ch_reserved: crate::endian::U32Bytes<E>,
    pub ch_size: crate::endian::U64Bytes<E>,
    pub ch_addralign: crate::endian::U64Bytes<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `ch_type` | `crate::endian::U32Bytes<E>` | Compression format. One of the `ELFCOMPRESS_*` values. |
| `ch_reserved` | `crate::endian::U32Bytes<E>` | Reserved. |
| `ch_size` | `crate::endian::U64Bytes<E>` | Uncompressed data size. |
| `ch_addralign` | `crate::endian::U64Bytes<E>` | Uncompressed data alignment. |

##### Implementations

###### Trait Implementations

- **Send**
- **Pod**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Default**
  - ```rust
    fn default() -> CompressionHeader64<E> { /* ... */ }
    ```

- **Unpin**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **RefUnwindSafe**
- **Sync**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **CompressionHeader**
  - ```rust
    fn ch_type(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn ch_size(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn ch_addralign(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Freeze**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Copy**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> CompressionHeader64<E> { /* ... */ }
    ```

- **UnwindSafe**
#### Struct `Sym32`

**Attributes:**

- `#[repr(C)]`

Symbol table entry.

```rust
pub struct Sym32<E: Endian> {
    pub st_name: crate::endian::U32<E>,
    pub st_value: crate::endian::U32<E>,
    pub st_size: crate::endian::U32<E>,
    pub st_info: u8,
    pub st_other: u8,
    pub st_shndx: crate::endian::U16<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `st_name` | `crate::endian::U32<E>` | Symbol name.<br><br>This is an offset into the symbol string table. |
| `st_value` | `crate::endian::U32<E>` | Symbol value. |
| `st_size` | `crate::endian::U32<E>` | Symbol size. |
| `st_info` | `u8` | Symbol type and binding.<br><br>Use the `st_type` and `st_bind` methods to access this value. |
| `st_other` | `u8` | Symbol visibility.<br><br>Use the `st_visibility` method to access this value. |
| `st_shndx` | `crate::endian::U16<E>` | Section index or one of the `SHN_*` values. |

##### Implementations

###### Methods

- ```rust
  pub fn st_bind(self: &Self) -> u8 { /* ... */ }
  ```
  Get the `st_bind` component of the `st_info` field.

- ```rust
  pub fn st_type(self: &Self) -> u8 { /* ... */ }
  ```
  Get the `st_type` component of the `st_info` field.

- ```rust
  pub fn set_st_info(self: &mut Self, st_bind: u8, st_type: u8) { /* ... */ }
  ```
  Set the `st_info` field given the `st_bind` and `st_type` components.

- ```rust
  pub fn st_visibility(self: &Self) -> u8 { /* ... */ }
  ```
  Get the `st_visibility` component of the `st_info` field.

###### Trait Implementations

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **RefUnwindSafe**
- **Sync**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Unpin**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Freeze**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **UnwindSafe**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Sym**
  - ```rust
    fn st_name(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn st_info(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn st_bind(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn st_type(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn st_other(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn st_visibility(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn st_shndx(self: &Self, endian: <Self as >::Endian) -> u16 { /* ... */ }
    ```

  - ```rust
    fn st_value(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn st_size(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Default**
  - ```rust
    fn default() -> Sym32<E> { /* ... */ }
    ```

- **Send**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> Sym32<E> { /* ... */ }
    ```

- **Copy**
- **Pod**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

#### Struct `Sym64`

**Attributes:**

- `#[repr(C)]`

Symbol table entry.

```rust
pub struct Sym64<E: Endian> {
    pub st_name: crate::endian::U32<E>,
    pub st_info: u8,
    pub st_other: u8,
    pub st_shndx: crate::endian::U16<E>,
    pub st_value: crate::endian::U64<E>,
    pub st_size: crate::endian::U64<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `st_name` | `crate::endian::U32<E>` | Symbol name.<br><br>This is an offset into the symbol string table. |
| `st_info` | `u8` | Symbol type and binding.<br><br>Use the `st_bind` and `st_type` methods to access this value. |
| `st_other` | `u8` | Symbol visibility.<br><br>Use the `st_visibility` method to access this value. |
| `st_shndx` | `crate::endian::U16<E>` | Section index or one of the `SHN_*` values. |
| `st_value` | `crate::endian::U64<E>` | Symbol value. |
| `st_size` | `crate::endian::U64<E>` | Symbol size. |

##### Implementations

###### Methods

- ```rust
  pub fn st_bind(self: &Self) -> u8 { /* ... */ }
  ```
  Get the `st_bind` component of the `st_info` field.

- ```rust
  pub fn st_type(self: &Self) -> u8 { /* ... */ }
  ```
  Get the `st_type` component of the `st_info` field.

- ```rust
  pub fn set_st_info(self: &mut Self, st_bind: u8, st_type: u8) { /* ... */ }
  ```
  Set the `st_info` field given the `st_bind` and `st_type` components.

- ```rust
  pub fn st_visibility(self: &Self) -> u8 { /* ... */ }
  ```
  Get the `st_visibility` component of the `st_info` field.

###### Trait Implementations

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Sym**
  - ```rust
    fn st_name(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn st_info(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn st_bind(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn st_type(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn st_other(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn st_visibility(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn st_shndx(self: &Self, endian: <Self as >::Endian) -> u16 { /* ... */ }
    ```

  - ```rust
    fn st_value(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn st_size(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

- **Copy**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Send**
- **Sync**
- **Freeze**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **UnwindSafe**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Unpin**
- **Pod**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> Sym64<E> { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **RefUnwindSafe**
- **Default**
  - ```rust
    fn default() -> Sym64<E> { /* ... */ }
    ```

#### Struct `Syminfo32`

**Attributes:**

- `#[repr(C)]`

Additional information about a `Sym32`.

```rust
pub struct Syminfo32<E: Endian> {
    pub si_boundto: crate::endian::U16<E>,
    pub si_flags: crate::endian::U16<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `si_boundto` | `crate::endian::U16<E>` | Direct bindings, symbol bound to. |
| `si_flags` | `crate::endian::U16<E>` | Per symbol flags. |

##### Implementations

###### Trait Implementations

- **Unpin**
- **Send**
- **Freeze**
- **Sync**
- **UnwindSafe**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Pod**
- **Copy**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **RefUnwindSafe**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Clone**
  - ```rust
    fn clone(self: &Self) -> Syminfo32<E> { /* ... */ }
    ```

#### Struct `Syminfo64`

**Attributes:**

- `#[repr(C)]`

Additional information about a `Sym64`.

```rust
pub struct Syminfo64<E: Endian> {
    pub si_boundto: crate::endian::U16<E>,
    pub si_flags: crate::endian::U16<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `si_boundto` | `crate::endian::U16<E>` | Direct bindings, symbol bound to. |
| `si_flags` | `crate::endian::U16<E>` | Per symbol flags. |

##### Implementations

###### Trait Implementations

- **Clone**
  - ```rust
    fn clone(self: &Self) -> Syminfo64<E> { /* ... */ }
    ```

- **Sync**
- **UnwindSafe**
- **RefUnwindSafe**
- **Pod**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Copy**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Unpin**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Send**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Freeze**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

#### Struct `Rel32`

**Attributes:**

- `#[repr(C)]`

Relocation table entry without explicit addend.

```rust
pub struct Rel32<E: Endian> {
    pub r_offset: crate::endian::U32<E>,
    pub r_info: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `r_offset` | `crate::endian::U32<E>` | Relocation address. |
| `r_info` | `crate::endian::U32<E>` | Relocation type and symbol index. |

##### Implementations

###### Methods

- ```rust
  pub fn r_sym(self: &Self, endian: E) -> u32 { /* ... */ }
  ```
  Get the `r_sym` component of the `r_info` field.

- ```rust
  pub fn r_type(self: &Self, endian: E) -> u32 { /* ... */ }
  ```
  Get the `r_type` component of the `r_info` field.

- ```rust
  pub fn r_info(endian: E, r_sym: u32, r_type: u8) -> U32<E> { /* ... */ }
  ```
  Calculate the `r_info` field given the `r_sym` and `r_type` components.

- ```rust
  pub fn set_r_info(self: &mut Self, endian: E, r_sym: u32, r_type: u8) { /* ... */ }
  ```
  Set the `r_info` field given the `r_sym` and `r_type` components.

###### Trait Implementations

- **Copy**
- **Pod**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

  - ```rust
    fn from(rel: Rel32<E>) -> Self { /* ... */ }
    ```

- **Send**
- **Sync**
- **Freeze**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Rel**
  - ```rust
    fn r_offset(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn r_info(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn r_sym(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn r_type(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Unpin**
- **UnwindSafe**
- **RefUnwindSafe**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> Rel32<E> { /* ... */ }
    ```

#### Struct `Rela32`

**Attributes:**

- `#[repr(C)]`

Relocation table entry with explicit addend.

```rust
pub struct Rela32<E: Endian> {
    pub r_offset: crate::endian::U32<E>,
    pub r_info: crate::endian::U32<E>,
    pub r_addend: crate::endian::I32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `r_offset` | `crate::endian::U32<E>` | Relocation address. |
| `r_info` | `crate::endian::U32<E>` | Relocation type and symbol index. |
| `r_addend` | `crate::endian::I32<E>` | Explicit addend. |

##### Implementations

###### Methods

- ```rust
  pub fn r_sym(self: &Self, endian: E) -> u32 { /* ... */ }
  ```
  Get the `r_sym` component of the `r_info` field.

- ```rust
  pub fn r_type(self: &Self, endian: E) -> u32 { /* ... */ }
  ```
  Get the `r_type` component of the `r_info` field.

- ```rust
  pub fn r_info(endian: E, r_sym: u32, r_type: u8) -> U32<E> { /* ... */ }
  ```
  Calculate the `r_info` field given the `r_sym` and `r_type` components.

- ```rust
  pub fn set_r_info(self: &mut Self, endian: E, r_sym: u32, r_type: u8) { /* ... */ }
  ```
  Set the `r_info` field given the `r_sym` and `r_type` components.

###### Trait Implementations

- **Sync**
- **Rela**
  - ```rust
    fn r_offset(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn r_info(self: &Self, endian: <Self as >::Endian, _is_mips64el: bool) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn r_addend(self: &Self, endian: <Self as >::Endian) -> <Self as >::Sword { /* ... */ }
    ```

  - ```rust
    fn r_sym(self: &Self, endian: <Self as >::Endian, _is_mips64el: bool) -> u32 { /* ... */ }
    ```

  - ```rust
    fn r_type(self: &Self, endian: <Self as >::Endian, _is_mips64el: bool) -> u32 { /* ... */ }
    ```

- **Unpin**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Send**
- **Pod**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

  - ```rust
    fn from(rel: Rel32<E>) -> Self { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Freeze**
- **RefUnwindSafe**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **UnwindSafe**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> Rela32<E> { /* ... */ }
    ```

- **Copy**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

#### Struct `Rel64`

**Attributes:**

- `#[repr(C)]`

Relocation table entry without explicit addend.

```rust
pub struct Rel64<E: Endian> {
    pub r_offset: crate::endian::U64<E>,
    pub r_info: crate::endian::U64<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `r_offset` | `crate::endian::U64<E>` | Relocation address. |
| `r_info` | `crate::endian::U64<E>` | Relocation type and symbol index. |

##### Implementations

###### Methods

- ```rust
  pub fn r_sym(self: &Self, endian: E) -> u32 { /* ... */ }
  ```
  Get the `r_sym` component of the `r_info` field.

- ```rust
  pub fn r_type(self: &Self, endian: E) -> u32 { /* ... */ }
  ```
  Get the `r_type` component of the `r_info` field.

- ```rust
  pub fn r_info(endian: E, r_sym: u32, r_type: u32) -> U64<E> { /* ... */ }
  ```
  Calculate the `r_info` field given the `r_sym` and `r_type` components.

- ```rust
  pub fn set_r_info(self: &mut Self, endian: E, r_sym: u32, r_type: u32) { /* ... */ }
  ```
  Set the `r_info` field given the `r_sym` and `r_type` components.

###### Trait Implementations

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Sync**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Send**
- **Rel**
  - ```rust
    fn r_offset(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn r_info(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn r_sym(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn r_type(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> Rel64<E> { /* ... */ }
    ```

- **Copy**
- **Pod**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Freeze**
- **Unpin**
- **RefUnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

  - ```rust
    fn from(rel: Rel64<E>) -> Self { /* ... */ }
    ```

- **UnwindSafe**
#### Struct `Rela64`

**Attributes:**

- `#[repr(C)]`

Relocation table entry with explicit addend.

```rust
pub struct Rela64<E: Endian> {
    pub r_offset: crate::endian::U64<E>,
    pub r_info: crate::endian::U64<E>,
    pub r_addend: crate::endian::I64<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `r_offset` | `crate::endian::U64<E>` | Relocation address. |
| `r_info` | `crate::endian::U64<E>` | Relocation type and symbol index. |
| `r_addend` | `crate::endian::I64<E>` | Explicit addend. |

##### Implementations

###### Methods

- ```rust
  pub fn r_sym(self: &Self, endian: E, is_mips64el: bool) -> u32 { /* ... */ }
  ```
  Get the `r_sym` component of the `r_info` field.

- ```rust
  pub fn r_type(self: &Self, endian: E, is_mips64el: bool) -> u32 { /* ... */ }
  ```
  Get the `r_type` component of the `r_info` field.

- ```rust
  pub fn r_info(endian: E, is_mips64el: bool, r_sym: u32, r_type: u32) -> U64<E> { /* ... */ }
  ```
  Calculate the `r_info` field given the `r_sym` and `r_type` components.

- ```rust
  pub fn set_r_info(self: &mut Self, endian: E, is_mips64el: bool, r_sym: u32, r_type: u32) { /* ... */ }
  ```
  Set the `r_info` field given the `r_sym` and `r_type` components.

###### Trait Implementations

- **Send**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Freeze**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Copy**
- **Pod**
- **Rela**
  - ```rust
    fn r_offset(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn r_info(self: &Self, endian: <Self as >::Endian, is_mips64el: bool) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn r_addend(self: &Self, endian: <Self as >::Endian) -> <Self as >::Sword { /* ... */ }
    ```

  - ```rust
    fn r_sym(self: &Self, endian: <Self as >::Endian, is_mips64el: bool) -> u32 { /* ... */ }
    ```

  - ```rust
    fn r_type(self: &Self, endian: <Self as >::Endian, is_mips64el: bool) -> u32 { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Sync**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Unpin**
- **UnwindSafe**
- **RefUnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

  - ```rust
    fn from(rel: Rel64<E>) -> Self { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> Rela64<E> { /* ... */ }
    ```

#### Struct `Relr32`

**Attributes:**

- `#[repr(C)]`

32-bit relative relocation table entry.

```rust
pub struct Relr32<E: Endian>(pub crate::endian::U32<E>);
```

##### Fields

| Index | Type | Documentation |
|-------|------|---------------|
| 0 | `crate::endian::U32<E>` |  |

##### Implementations

###### Trait Implementations

- **UnwindSafe**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Copy**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **RefUnwindSafe**
- **Relr**
  - ```rust
    fn get(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn next(offset: &mut <Self as >::Word, bits: &mut <Self as >::Word) -> Option<<Self as >::Word> { /* ... */ }
    ```

- **Freeze**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> Relr32<E> { /* ... */ }
    ```

- **Sync**
- **Send**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Pod**
- **Unpin**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

#### Struct `Relr64`

**Attributes:**

- `#[repr(C)]`

64-bit relative relocation table entry.

```rust
pub struct Relr64<E: Endian>(pub crate::endian::U64<E>);
```

##### Fields

| Index | Type | Documentation |
|-------|------|---------------|
| 0 | `crate::endian::U64<E>` |  |

##### Implementations

###### Trait Implementations

- **Sync**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **RefUnwindSafe**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Unpin**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Copy**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Freeze**
- **Send**
- **Relr**
  - ```rust
    fn get(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn next(offset: &mut <Self as >::Word, bits: &mut <Self as >::Word) -> Option<<Self as >::Word> { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> Relr64<E> { /* ... */ }
    ```

- **UnwindSafe**
- **Pod**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

#### Struct `ProgramHeader32`

**Attributes:**

- `#[repr(C)]`

Program segment header.

```rust
pub struct ProgramHeader32<E: Endian> {
    pub p_type: crate::endian::U32<E>,
    pub p_offset: crate::endian::U32<E>,
    pub p_vaddr: crate::endian::U32<E>,
    pub p_paddr: crate::endian::U32<E>,
    pub p_filesz: crate::endian::U32<E>,
    pub p_memsz: crate::endian::U32<E>,
    pub p_flags: crate::endian::U32<E>,
    pub p_align: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `p_type` | `crate::endian::U32<E>` | Segment type. One of the `PT_*` constants. |
| `p_offset` | `crate::endian::U32<E>` | Segment file offset. |
| `p_vaddr` | `crate::endian::U32<E>` | Segment virtual address. |
| `p_paddr` | `crate::endian::U32<E>` | Segment physical address. |
| `p_filesz` | `crate::endian::U32<E>` | Segment size in the file. |
| `p_memsz` | `crate::endian::U32<E>` | Segment size in memory. |
| `p_flags` | `crate::endian::U32<E>` | Segment flags. A combination of the `PF_*` constants. |
| `p_align` | `crate::endian::U32<E>` | Segment alignment. |

##### Implementations

###### Trait Implementations

- **Pod**
- **Unpin**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> ProgramHeader32<E> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **ProgramHeader**
  - ```rust
    fn p_type(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn p_flags(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn p_offset(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn p_vaddr(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn p_paddr(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn p_filesz(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn p_memsz(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn p_align(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

- **Copy**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **RefUnwindSafe**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Freeze**
- **UnwindSafe**
- **Send**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Sync**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

#### Struct `ProgramHeader64`

**Attributes:**

- `#[repr(C)]`

Program segment header.

```rust
pub struct ProgramHeader64<E: Endian> {
    pub p_type: crate::endian::U32<E>,
    pub p_flags: crate::endian::U32<E>,
    pub p_offset: crate::endian::U64<E>,
    pub p_vaddr: crate::endian::U64<E>,
    pub p_paddr: crate::endian::U64<E>,
    pub p_filesz: crate::endian::U64<E>,
    pub p_memsz: crate::endian::U64<E>,
    pub p_align: crate::endian::U64<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `p_type` | `crate::endian::U32<E>` | Segment type. One of the `PT_*` constants. |
| `p_flags` | `crate::endian::U32<E>` | Segment flags. A combination of the `PF_*` constants. |
| `p_offset` | `crate::endian::U64<E>` | Segment file offset. |
| `p_vaddr` | `crate::endian::U64<E>` | Segment virtual address. |
| `p_paddr` | `crate::endian::U64<E>` | Segment physical address. |
| `p_filesz` | `crate::endian::U64<E>` | Segment size in the file. |
| `p_memsz` | `crate::endian::U64<E>` | Segment size in memory. |
| `p_align` | `crate::endian::U64<E>` | Segment alignment. |

##### Implementations

###### Trait Implementations

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Sync**
- **Unpin**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **RefUnwindSafe**
- **Send**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Copy**
- **Freeze**
- **Pod**
- **UnwindSafe**
- **ProgramHeader**
  - ```rust
    fn p_type(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn p_flags(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn p_offset(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn p_vaddr(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn p_paddr(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn p_filesz(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn p_memsz(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn p_align(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ProgramHeader64<E> { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

#### Struct `Dyn32`

**Attributes:**

- `#[repr(C)]`

Dynamic section entry.

```rust
pub struct Dyn32<E: Endian> {
    pub d_tag: crate::endian::U32<E>,
    pub d_val: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `d_tag` | `crate::endian::U32<E>` | Dynamic entry type. |
| `d_val` | `crate::endian::U32<E>` | Value (integer or address). |

##### Implementations

###### Trait Implementations

- **Send**
- **Freeze**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **UnwindSafe**
- **Pod**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Dyn**
  - ```rust
    fn d_tag(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn d_val(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

- **RefUnwindSafe**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> Dyn32<E> { /* ... */ }
    ```

- **Copy**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Sync**
- **Unpin**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

#### Struct `Dyn64`

**Attributes:**

- `#[repr(C)]`

Dynamic section entry.

```rust
pub struct Dyn64<E: Endian> {
    pub d_tag: crate::endian::U64<E>,
    pub d_val: crate::endian::U64<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `d_tag` | `crate::endian::U64<E>` | Dynamic entry type. |
| `d_val` | `crate::endian::U64<E>` | Value (integer or address). |

##### Implementations

###### Trait Implementations

- **Unpin**
- **Freeze**
- **Pod**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Dyn**
  - ```rust
    fn d_tag(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn d_val(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **RefUnwindSafe**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **UnwindSafe**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> Dyn64<E> { /* ... */ }
    ```

- **Copy**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Send**
- **Sync**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

#### Struct `Versym`

**Attributes:**

- `#[repr(C)]`

Version symbol information

```rust
pub struct Versym<E: Endian>(pub crate::endian::U16<E>);
```

##### Fields

| Index | Type | Documentation |
|-------|------|---------------|
| 0 | `crate::endian::U16<E>` |  |

##### Implementations

###### Trait Implementations

- **UnwindSafe**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> Versym<E> { /* ... */ }
    ```

- **Unpin**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Copy**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Pod**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Freeze**
- **Sync**
- **Send**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **RefUnwindSafe**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

#### Struct `Verdef`

**Attributes:**

- `#[repr(C)]`

Version definition sections

```rust
pub struct Verdef<E: Endian> {
    pub vd_version: crate::endian::U16<E>,
    pub vd_flags: crate::endian::U16<E>,
    pub vd_ndx: crate::endian::U16<E>,
    pub vd_cnt: crate::endian::U16<E>,
    pub vd_hash: crate::endian::U32<E>,
    pub vd_aux: crate::endian::U32<E>,
    pub vd_next: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `vd_version` | `crate::endian::U16<E>` | Version revision |
| `vd_flags` | `crate::endian::U16<E>` | Version information |
| `vd_ndx` | `crate::endian::U16<E>` | Version Index |
| `vd_cnt` | `crate::endian::U16<E>` | Number of associated aux entries |
| `vd_hash` | `crate::endian::U32<E>` | Version name hash value |
| `vd_aux` | `crate::endian::U32<E>` | Offset in bytes to verdaux array |
| `vd_next` | `crate::endian::U32<E>` | Offset in bytes to next verdef entry |

##### Implementations

###### Trait Implementations

- **RefUnwindSafe**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Sync**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> Verdef<E> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Unpin**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Send**
- **UnwindSafe**
- **Copy**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Pod**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Freeze**
#### Struct `Verdaux`

**Attributes:**

- `#[repr(C)]`

Auxiliary version information.

```rust
pub struct Verdaux<E: Endian> {
    pub vda_name: crate::endian::U32<E>,
    pub vda_next: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `vda_name` | `crate::endian::U32<E>` | Version or dependency names |
| `vda_next` | `crate::endian::U32<E>` | Offset in bytes to next verdaux |

##### Implementations

###### Methods

- ```rust
  pub fn name<''data, R: ReadRef<''data>>(self: &Self, endian: Endian, strings: StringTable<''data, R>) -> Result<&''data [u8]> { /* ... */ }
  ```
  Parse the version name from the string table.

###### Trait Implementations

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Freeze**
- **Pod**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Sync**
- **Unpin**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **RefUnwindSafe**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **UnwindSafe**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Send**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Copy**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Clone**
  - ```rust
    fn clone(self: &Self) -> Verdaux<E> { /* ... */ }
    ```

#### Struct `Verneed`

**Attributes:**

- `#[repr(C)]`

Version dependency.

```rust
pub struct Verneed<E: Endian> {
    pub vn_version: crate::endian::U16<E>,
    pub vn_cnt: crate::endian::U16<E>,
    pub vn_file: crate::endian::U32<E>,
    pub vn_aux: crate::endian::U32<E>,
    pub vn_next: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `vn_version` | `crate::endian::U16<E>` | Version of structure |
| `vn_cnt` | `crate::endian::U16<E>` | Number of associated aux entries |
| `vn_file` | `crate::endian::U32<E>` | Offset of filename for this dependency |
| `vn_aux` | `crate::endian::U32<E>` | Offset in bytes to vernaux array |
| `vn_next` | `crate::endian::U32<E>` | Offset in bytes to next verneed entry |

##### Implementations

###### Methods

- ```rust
  pub fn file<''data, R: ReadRef<''data>>(self: &Self, endian: Endian, strings: StringTable<''data, R>) -> Result<&''data [u8]> { /* ... */ }
  ```
  Parse the file from the string table.

###### Trait Implementations

- **Unpin**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Freeze**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Send**
- **Pod**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Copy**
- **RefUnwindSafe**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Sync**
- **UnwindSafe**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> Verneed<E> { /* ... */ }
    ```

#### Struct `Vernaux`

**Attributes:**

- `#[repr(C)]`

Auxiliary needed version information.

```rust
pub struct Vernaux<E: Endian> {
    pub vna_hash: crate::endian::U32<E>,
    pub vna_flags: crate::endian::U16<E>,
    pub vna_other: crate::endian::U16<E>,
    pub vna_name: crate::endian::U32<E>,
    pub vna_next: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `vna_hash` | `crate::endian::U32<E>` | Hash value of dependency name |
| `vna_flags` | `crate::endian::U16<E>` | Dependency specific information |
| `vna_other` | `crate::endian::U16<E>` | Version Index |
| `vna_name` | `crate::endian::U32<E>` | Dependency name string offset |
| `vna_next` | `crate::endian::U32<E>` | Offset in bytes to next vernaux entry |

##### Implementations

###### Methods

- ```rust
  pub fn name<''data, R: ReadRef<''data>>(self: &Self, endian: Endian, strings: StringTable<''data, R>) -> Result<&''data [u8]> { /* ... */ }
  ```
  Parse the version name from the string table.

###### Trait Implementations

- **Freeze**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> Vernaux<E> { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Send**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Unpin**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Copy**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **RefUnwindSafe**
- **Sync**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Pod**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **UnwindSafe**
#### Struct `NoteHeader32`

**Attributes:**

- `#[repr(C)]`

Note section entry header.

A note consists of a header followed by a variable length name and descriptor.

```rust
pub struct NoteHeader32<E: Endian> {
    pub n_namesz: crate::endian::U32<E>,
    pub n_descsz: crate::endian::U32<E>,
    pub n_type: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `n_namesz` | `crate::endian::U32<E>` | Length of the note's name.<br><br>Some known names are defined by the `ELF_NOTE_*` constants. |
| `n_descsz` | `crate::endian::U32<E>` | Length of the note's descriptor.<br><br>The content of the descriptor depends on the note name and type. |
| `n_type` | `crate::endian::U32<E>` | Type of the note.<br><br>One of the `NT_*` constants. The note name determines which<br>`NT_*` constants are valid. |

##### Implementations

###### Trait Implementations

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Freeze**
- **Sync**
- **NoteHeader**
  - ```rust
    fn n_namesz(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn n_descsz(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn n_type(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

- **UnwindSafe**
- **Copy**
- **Pod**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Unpin**
- **Send**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> NoteHeader32<E> { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **RefUnwindSafe**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

#### Struct `NoteHeader64`

**Attributes:**

- `#[repr(C)]`

Note section entry header.

```rust
pub struct NoteHeader64<E: Endian> {
    pub n_namesz: crate::endian::U32<E>,
    pub n_descsz: crate::endian::U32<E>,
    pub n_type: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `n_namesz` | `crate::endian::U32<E>` | Length of the note's name.<br><br>Some known names are defined by the `ELF_NOTE_*` constants. |
| `n_descsz` | `crate::endian::U32<E>` | Length of the note's descriptor.<br><br>The content of the descriptor depends on the note name and type. |
| `n_type` | `crate::endian::U32<E>` | Type of the note.<br><br>One of the `NT_*` constants. The note name determines which<br>`NT_*` constants are valid. |

##### Implementations

###### Trait Implementations

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **NoteHeader**
  - ```rust
    fn n_namesz(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn n_descsz(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn n_type(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

- **Send**
- **Freeze**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Unpin**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> NoteHeader64<E> { /* ... */ }
    ```

- **Pod**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **RefUnwindSafe**
- **UnwindSafe**
- **Sync**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Copy**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

#### Struct `HashHeader`

**Attributes:**

- `#[repr(C)]`

Header of `SHT_HASH` section.

```rust
pub struct HashHeader<E: Endian> {
    pub bucket_count: crate::endian::U32<E>,
    pub chain_count: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `bucket_count` | `crate::endian::U32<E>` | The number of hash buckets. |
| `chain_count` | `crate::endian::U32<E>` | The number of chain values. |

##### Implementations

###### Trait Implementations

- **Clone**
  - ```rust
    fn clone(self: &Self) -> HashHeader<E> { /* ... */ }
    ```

- **Copy**
- **Pod**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Sync**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Send**
- **Unpin**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Freeze**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **UnwindSafe**
- **RefUnwindSafe**
#### Struct `GnuHashHeader`

**Attributes:**

- `#[repr(C)]`

Header of `SHT_GNU_HASH` section.

```rust
pub struct GnuHashHeader<E: Endian> {
    pub bucket_count: crate::endian::U32<E>,
    pub symbol_base: crate::endian::U32<E>,
    pub bloom_count: crate::endian::U32<E>,
    pub bloom_shift: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `bucket_count` | `crate::endian::U32<E>` | The number of hash buckets. |
| `symbol_base` | `crate::endian::U32<E>` | The symbol table index of the first symbol in the hash. |
| `bloom_count` | `crate::endian::U32<E>` | The number of words in the bloom filter.<br><br>Must be a non-zero power of 2. |
| `bloom_shift` | `crate::endian::U32<E>` | The bit shift count for the bloom filter. |

##### Implementations

###### Trait Implementations

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Unpin**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **RefUnwindSafe**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Copy**
- **Pod**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Freeze**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Sync**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Send**
- **UnwindSafe**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Clone**
  - ```rust
    fn clone(self: &Self) -> GnuHashHeader<E> { /* ... */ }
    ```

### Functions

#### Function `hash`

Calculate the SysV hash for a symbol name.

Used for `SHT_HASH`.

```rust
pub fn hash(name: &[u8]) -> u32 { /* ... */ }
```

#### Function `gnu_hash`

Calculate the GNU hash for a symbol name.

Used for `SHT_GNU_HASH`.

```rust
pub fn gnu_hash(name: &[u8]) -> u32 { /* ... */ }
```

#### Function `ef_e2k_mach_to_flag`

Encode `E_E2K_MACH_*` into `FileHeader*::e_flags`.

```rust
pub const fn ef_e2k_mach_to_flag(e_flags: u32, x: u32) -> u32 { /* ... */ }
```

#### Function `ef_e2k_flag_to_mach`

Decode `E_E2K_MACH_*` from `FileHeader*::e_flags`.

```rust
pub const fn ef_e2k_flag_to_mach(e_flags: u32) -> u32 { /* ... */ }
```

### Constants and Statics

#### Constant `ELFMAG`

File identification bytes stored in `Ident::magic`.

```rust
pub const ELFMAG: [u8; 4] = _;
```

#### Constant `ELFCLASSNONE`

Invalid class.

```rust
pub const ELFCLASSNONE: u8 = 0;
```

#### Constant `ELFCLASS32`

32-bit object.

```rust
pub const ELFCLASS32: u8 = 1;
```

#### Constant `ELFCLASS64`

64-bit object.

```rust
pub const ELFCLASS64: u8 = 2;
```

#### Constant `ELFDATANONE`

Invalid data encoding.

```rust
pub const ELFDATANONE: u8 = 0;
```

#### Constant `ELFDATA2LSB`

2's complement, little endian.

```rust
pub const ELFDATA2LSB: u8 = 1;
```

#### Constant `ELFDATA2MSB`

2's complement, big endian.

```rust
pub const ELFDATA2MSB: u8 = 2;
```

#### Constant `ELFOSABI_NONE`

UNIX System V ABI.

```rust
pub const ELFOSABI_NONE: u8 = 0;
```

#### Constant `ELFOSABI_SYSV`

UNIX System V ABI.

Alias.

```rust
pub const ELFOSABI_SYSV: u8 = 0;
```

#### Constant `ELFOSABI_HPUX`

HP-UX.

```rust
pub const ELFOSABI_HPUX: u8 = 1;
```

#### Constant `ELFOSABI_NETBSD`

NetBSD.

```rust
pub const ELFOSABI_NETBSD: u8 = 2;
```

#### Constant `ELFOSABI_GNU`

Object uses GNU ELF extensions.

```rust
pub const ELFOSABI_GNU: u8 = 3;
```

#### Constant `ELFOSABI_LINUX`

Object uses GNU ELF extensions.

Compatibility alias.

```rust
pub const ELFOSABI_LINUX: u8 = ELFOSABI_GNU;
```

#### Constant `ELFOSABI_HURD`

GNU/Hurd.

```rust
pub const ELFOSABI_HURD: u8 = 4;
```

#### Constant `ELFOSABI_SOLARIS`

Sun Solaris.

```rust
pub const ELFOSABI_SOLARIS: u8 = 6;
```

#### Constant `ELFOSABI_AIX`

IBM AIX.

```rust
pub const ELFOSABI_AIX: u8 = 7;
```

#### Constant `ELFOSABI_IRIX`

SGI Irix.

```rust
pub const ELFOSABI_IRIX: u8 = 8;
```

#### Constant `ELFOSABI_FREEBSD`

FreeBSD.

```rust
pub const ELFOSABI_FREEBSD: u8 = 9;
```

#### Constant `ELFOSABI_TRU64`

Compaq TRU64 UNIX.

```rust
pub const ELFOSABI_TRU64: u8 = 10;
```

#### Constant `ELFOSABI_MODESTO`

Novell Modesto.

```rust
pub const ELFOSABI_MODESTO: u8 = 11;
```

#### Constant `ELFOSABI_OPENBSD`

OpenBSD.

```rust
pub const ELFOSABI_OPENBSD: u8 = 12;
```

#### Constant `ELFOSABI_OPENVMS`

OpenVMS.

```rust
pub const ELFOSABI_OPENVMS: u8 = 13;
```

#### Constant `ELFOSABI_NSK`

Hewlett-Packard Non-Stop Kernel.

```rust
pub const ELFOSABI_NSK: u8 = 14;
```

#### Constant `ELFOSABI_AROS`

AROS

```rust
pub const ELFOSABI_AROS: u8 = 15;
```

#### Constant `ELFOSABI_FENIXOS`

FenixOS

```rust
pub const ELFOSABI_FENIXOS: u8 = 16;
```

#### Constant `ELFOSABI_CLOUDABI`

Nuxi CloudABI

```rust
pub const ELFOSABI_CLOUDABI: u8 = 17;
```

#### Constant `ELFOSABI_ARM_AEABI`

ARM EABI.

```rust
pub const ELFOSABI_ARM_AEABI: u8 = 64;
```

#### Constant `ELFOSABI_ARM`

ARM.

```rust
pub const ELFOSABI_ARM: u8 = 97;
```

#### Constant `ELFOSABI_STANDALONE`

Standalone (embedded) application.

```rust
pub const ELFOSABI_STANDALONE: u8 = 255;
```

#### Constant `ET_NONE`

No file type.

```rust
pub const ET_NONE: u16 = 0;
```

#### Constant `ET_REL`

Relocatable file.

```rust
pub const ET_REL: u16 = 1;
```

#### Constant `ET_EXEC`

Executable file.

```rust
pub const ET_EXEC: u16 = 2;
```

#### Constant `ET_DYN`

Shared object file.

```rust
pub const ET_DYN: u16 = 3;
```

#### Constant `ET_CORE`

Core file.

```rust
pub const ET_CORE: u16 = 4;
```

#### Constant `ET_LOOS`

OS-specific range start.

```rust
pub const ET_LOOS: u16 = 0xfe00;
```

#### Constant `ET_HIOS`

OS-specific range end.

```rust
pub const ET_HIOS: u16 = 0xfeff;
```

#### Constant `ET_LOPROC`

Processor-specific range start.

```rust
pub const ET_LOPROC: u16 = 0xff00;
```

#### Constant `ET_HIPROC`

Processor-specific range end.

```rust
pub const ET_HIPROC: u16 = 0xffff;
```

#### Constant `EM_NONE`

No machine

```rust
pub const EM_NONE: u16 = 0;
```

#### Constant `EM_M32`

AT&T WE 32100

```rust
pub const EM_M32: u16 = 1;
```

#### Constant `EM_SPARC`

SUN SPARC

```rust
pub const EM_SPARC: u16 = 2;
```

#### Constant `EM_386`

Intel 80386

```rust
pub const EM_386: u16 = 3;
```

#### Constant `EM_68K`

Motorola m68k family

```rust
pub const EM_68K: u16 = 4;
```

#### Constant `EM_88K`

Motorola m88k family

```rust
pub const EM_88K: u16 = 5;
```

#### Constant `EM_IAMCU`

Intel MCU

```rust
pub const EM_IAMCU: u16 = 6;
```

#### Constant `EM_860`

Intel 80860

```rust
pub const EM_860: u16 = 7;
```

#### Constant `EM_MIPS`

MIPS R3000 big-endian

```rust
pub const EM_MIPS: u16 = 8;
```

#### Constant `EM_S370`

IBM System/370

```rust
pub const EM_S370: u16 = 9;
```

#### Constant `EM_MIPS_RS3_LE`

MIPS R3000 little-endian

```rust
pub const EM_MIPS_RS3_LE: u16 = 10;
```

#### Constant `EM_PARISC`

HPPA

```rust
pub const EM_PARISC: u16 = 15;
```

#### Constant `EM_VPP500`

Fujitsu VPP500

```rust
pub const EM_VPP500: u16 = 17;
```

#### Constant `EM_SPARC32PLUS`

Sun's "v8plus"

```rust
pub const EM_SPARC32PLUS: u16 = 18;
```

#### Constant `EM_960`

Intel 80960

```rust
pub const EM_960: u16 = 19;
```

#### Constant `EM_PPC`

PowerPC

```rust
pub const EM_PPC: u16 = 20;
```

#### Constant `EM_PPC64`

PowerPC 64-bit

```rust
pub const EM_PPC64: u16 = 21;
```

#### Constant `EM_S390`

IBM S390

```rust
pub const EM_S390: u16 = 22;
```

#### Constant `EM_SPU`

IBM SPU/SPC

```rust
pub const EM_SPU: u16 = 23;
```

#### Constant `EM_V800`

NEC V800 series

```rust
pub const EM_V800: u16 = 36;
```

#### Constant `EM_FR20`

Fujitsu FR20

```rust
pub const EM_FR20: u16 = 37;
```

#### Constant `EM_RH32`

TRW RH-32

```rust
pub const EM_RH32: u16 = 38;
```

#### Constant `EM_RCE`

Motorola RCE

```rust
pub const EM_RCE: u16 = 39;
```

#### Constant `EM_ARM`

ARM

```rust
pub const EM_ARM: u16 = 40;
```

#### Constant `EM_FAKE_ALPHA`

Digital Alpha

```rust
pub const EM_FAKE_ALPHA: u16 = 41;
```

#### Constant `EM_SH`

Hitachi SH

```rust
pub const EM_SH: u16 = 42;
```

#### Constant `EM_SPARCV9`

SPARC v9 64-bit

```rust
pub const EM_SPARCV9: u16 = 43;
```

#### Constant `EM_TRICORE`

Siemens Tricore

```rust
pub const EM_TRICORE: u16 = 44;
```

#### Constant `EM_ARC`

Argonaut RISC Core

```rust
pub const EM_ARC: u16 = 45;
```

#### Constant `EM_H8_300`

Hitachi H8/300

```rust
pub const EM_H8_300: u16 = 46;
```

#### Constant `EM_H8_300H`

Hitachi H8/300H

```rust
pub const EM_H8_300H: u16 = 47;
```

#### Constant `EM_H8S`

Hitachi H8S

```rust
pub const EM_H8S: u16 = 48;
```

#### Constant `EM_H8_500`

Hitachi H8/500

```rust
pub const EM_H8_500: u16 = 49;
```

#### Constant `EM_IA_64`

Intel Merced

```rust
pub const EM_IA_64: u16 = 50;
```

#### Constant `EM_MIPS_X`

Stanford MIPS-X

```rust
pub const EM_MIPS_X: u16 = 51;
```

#### Constant `EM_COLDFIRE`

Motorola Coldfire

```rust
pub const EM_COLDFIRE: u16 = 52;
```

#### Constant `EM_68HC12`

Motorola M68HC12

```rust
pub const EM_68HC12: u16 = 53;
```

#### Constant `EM_MMA`

Fujitsu MMA Multimedia Accelerator

```rust
pub const EM_MMA: u16 = 54;
```

#### Constant `EM_PCP`

Siemens PCP

```rust
pub const EM_PCP: u16 = 55;
```

#### Constant `EM_NCPU`

Sony nCPU embeeded RISC

```rust
pub const EM_NCPU: u16 = 56;
```

#### Constant `EM_NDR1`

Denso NDR1 microprocessor

```rust
pub const EM_NDR1: u16 = 57;
```

#### Constant `EM_STARCORE`

Motorola Start*Core processor

```rust
pub const EM_STARCORE: u16 = 58;
```

#### Constant `EM_ME16`

Toyota ME16 processor

```rust
pub const EM_ME16: u16 = 59;
```

#### Constant `EM_ST100`

STMicroelectronic ST100 processor

```rust
pub const EM_ST100: u16 = 60;
```

#### Constant `EM_TINYJ`

Advanced Logic Corp. Tinyj emb.fam

```rust
pub const EM_TINYJ: u16 = 61;
```

#### Constant `EM_X86_64`

AMD x86-64 architecture

```rust
pub const EM_X86_64: u16 = 62;
```

#### Constant `EM_PDSP`

Sony DSP Processor

```rust
pub const EM_PDSP: u16 = 63;
```

#### Constant `EM_PDP10`

Digital PDP-10

```rust
pub const EM_PDP10: u16 = 64;
```

#### Constant `EM_PDP11`

Digital PDP-11

```rust
pub const EM_PDP11: u16 = 65;
```

#### Constant `EM_FX66`

Siemens FX66 microcontroller

```rust
pub const EM_FX66: u16 = 66;
```

#### Constant `EM_ST9PLUS`

STMicroelectronics ST9+ 8/16 mc

```rust
pub const EM_ST9PLUS: u16 = 67;
```

#### Constant `EM_ST7`

STmicroelectronics ST7 8 bit mc

```rust
pub const EM_ST7: u16 = 68;
```

#### Constant `EM_68HC16`

Motorola MC68HC16 microcontroller

```rust
pub const EM_68HC16: u16 = 69;
```

#### Constant `EM_68HC11`

Motorola MC68HC11 microcontroller

```rust
pub const EM_68HC11: u16 = 70;
```

#### Constant `EM_68HC08`

Motorola MC68HC08 microcontroller

```rust
pub const EM_68HC08: u16 = 71;
```

#### Constant `EM_68HC05`

Motorola MC68HC05 microcontroller

```rust
pub const EM_68HC05: u16 = 72;
```

#### Constant `EM_SVX`

Silicon Graphics SVx

```rust
pub const EM_SVX: u16 = 73;
```

#### Constant `EM_ST19`

STMicroelectronics ST19 8 bit mc

```rust
pub const EM_ST19: u16 = 74;
```

#### Constant `EM_VAX`

Digital VAX

```rust
pub const EM_VAX: u16 = 75;
```

#### Constant `EM_CRIS`

Axis Communications 32-bit emb.proc

```rust
pub const EM_CRIS: u16 = 76;
```

#### Constant `EM_JAVELIN`

Infineon Technologies 32-bit emb.proc

```rust
pub const EM_JAVELIN: u16 = 77;
```

#### Constant `EM_FIREPATH`

Element 14 64-bit DSP Processor

```rust
pub const EM_FIREPATH: u16 = 78;
```

#### Constant `EM_ZSP`

LSI Logic 16-bit DSP Processor

```rust
pub const EM_ZSP: u16 = 79;
```

#### Constant `EM_MMIX`

Donald Knuth's educational 64-bit proc

```rust
pub const EM_MMIX: u16 = 80;
```

#### Constant `EM_HUANY`

Harvard University machine-independent object files

```rust
pub const EM_HUANY: u16 = 81;
```

#### Constant `EM_PRISM`

SiTera Prism

```rust
pub const EM_PRISM: u16 = 82;
```

#### Constant `EM_AVR`

Atmel AVR 8-bit microcontroller

```rust
pub const EM_AVR: u16 = 83;
```

#### Constant `EM_FR30`

Fujitsu FR30

```rust
pub const EM_FR30: u16 = 84;
```

#### Constant `EM_D10V`

Mitsubishi D10V

```rust
pub const EM_D10V: u16 = 85;
```

#### Constant `EM_D30V`

Mitsubishi D30V

```rust
pub const EM_D30V: u16 = 86;
```

#### Constant `EM_V850`

NEC v850

```rust
pub const EM_V850: u16 = 87;
```

#### Constant `EM_M32R`

Mitsubishi M32R

```rust
pub const EM_M32R: u16 = 88;
```

#### Constant `EM_MN10300`

Matsushita MN10300

```rust
pub const EM_MN10300: u16 = 89;
```

#### Constant `EM_MN10200`

Matsushita MN10200

```rust
pub const EM_MN10200: u16 = 90;
```

#### Constant `EM_PJ`

picoJava

```rust
pub const EM_PJ: u16 = 91;
```

#### Constant `EM_OPENRISC`

OpenRISC 32-bit embedded processor

```rust
pub const EM_OPENRISC: u16 = 92;
```

#### Constant `EM_ARC_COMPACT`

ARC International ARCompact

```rust
pub const EM_ARC_COMPACT: u16 = 93;
```

#### Constant `EM_XTENSA`

Tensilica Xtensa Architecture

```rust
pub const EM_XTENSA: u16 = 94;
```

#### Constant `EM_VIDEOCORE`

Alphamosaic VideoCore

```rust
pub const EM_VIDEOCORE: u16 = 95;
```

#### Constant `EM_TMM_GPP`

Thompson Multimedia General Purpose Proc

```rust
pub const EM_TMM_GPP: u16 = 96;
```

#### Constant `EM_NS32K`

National Semi. 32000

```rust
pub const EM_NS32K: u16 = 97;
```

#### Constant `EM_TPC`

Tenor Network TPC

```rust
pub const EM_TPC: u16 = 98;
```

#### Constant `EM_SNP1K`

Trebia SNP 1000

```rust
pub const EM_SNP1K: u16 = 99;
```

#### Constant `EM_ST200`

STMicroelectronics ST200

```rust
pub const EM_ST200: u16 = 100;
```

#### Constant `EM_IP2K`

Ubicom IP2xxx

```rust
pub const EM_IP2K: u16 = 101;
```

#### Constant `EM_MAX`

MAX processor

```rust
pub const EM_MAX: u16 = 102;
```

#### Constant `EM_CR`

National Semi. CompactRISC

```rust
pub const EM_CR: u16 = 103;
```

#### Constant `EM_F2MC16`

Fujitsu F2MC16

```rust
pub const EM_F2MC16: u16 = 104;
```

#### Constant `EM_MSP430`

Texas Instruments msp430

```rust
pub const EM_MSP430: u16 = 105;
```

#### Constant `EM_BLACKFIN`

Analog Devices Blackfin DSP

```rust
pub const EM_BLACKFIN: u16 = 106;
```

#### Constant `EM_SE_C33`

Seiko Epson S1C33 family

```rust
pub const EM_SE_C33: u16 = 107;
```

#### Constant `EM_SEP`

Sharp embedded microprocessor

```rust
pub const EM_SEP: u16 = 108;
```

#### Constant `EM_ARCA`

Arca RISC

```rust
pub const EM_ARCA: u16 = 109;
```

#### Constant `EM_UNICORE`

PKU-Unity & MPRC Peking Uni. mc series

```rust
pub const EM_UNICORE: u16 = 110;
```

#### Constant `EM_EXCESS`

eXcess configurable cpu

```rust
pub const EM_EXCESS: u16 = 111;
```

#### Constant `EM_DXP`

Icera Semi. Deep Execution Processor

```rust
pub const EM_DXP: u16 = 112;
```

#### Constant `EM_ALTERA_NIOS2`

Altera Nios II

```rust
pub const EM_ALTERA_NIOS2: u16 = 113;
```

#### Constant `EM_CRX`

National Semi. CompactRISC CRX

```rust
pub const EM_CRX: u16 = 114;
```

#### Constant `EM_XGATE`

Motorola XGATE

```rust
pub const EM_XGATE: u16 = 115;
```

#### Constant `EM_C166`

Infineon C16x/XC16x

```rust
pub const EM_C166: u16 = 116;
```

#### Constant `EM_M16C`

Renesas M16C

```rust
pub const EM_M16C: u16 = 117;
```

#### Constant `EM_DSPIC30F`

Microchip Technology dsPIC30F

```rust
pub const EM_DSPIC30F: u16 = 118;
```

#### Constant `EM_CE`

Freescale Communication Engine RISC

```rust
pub const EM_CE: u16 = 119;
```

#### Constant `EM_M32C`

Renesas M32C

```rust
pub const EM_M32C: u16 = 120;
```

#### Constant `EM_TSK3000`

Altium TSK3000

```rust
pub const EM_TSK3000: u16 = 131;
```

#### Constant `EM_RS08`

Freescale RS08

```rust
pub const EM_RS08: u16 = 132;
```

#### Constant `EM_SHARC`

Analog Devices SHARC family

```rust
pub const EM_SHARC: u16 = 133;
```

#### Constant `EM_ECOG2`

Cyan Technology eCOG2

```rust
pub const EM_ECOG2: u16 = 134;
```

#### Constant `EM_SCORE7`

Sunplus S+core7 RISC

```rust
pub const EM_SCORE7: u16 = 135;
```

#### Constant `EM_DSP24`

New Japan Radio (NJR) 24-bit DSP

```rust
pub const EM_DSP24: u16 = 136;
```

#### Constant `EM_VIDEOCORE3`

Broadcom VideoCore III

```rust
pub const EM_VIDEOCORE3: u16 = 137;
```

#### Constant `EM_LATTICEMICO32`

RISC for Lattice FPGA

```rust
pub const EM_LATTICEMICO32: u16 = 138;
```

#### Constant `EM_SE_C17`

Seiko Epson C17

```rust
pub const EM_SE_C17: u16 = 139;
```

#### Constant `EM_TI_C6000`

Texas Instruments TMS320C6000 DSP

```rust
pub const EM_TI_C6000: u16 = 140;
```

#### Constant `EM_TI_C2000`

Texas Instruments TMS320C2000 DSP

```rust
pub const EM_TI_C2000: u16 = 141;
```

#### Constant `EM_TI_C5500`

Texas Instruments TMS320C55x DSP

```rust
pub const EM_TI_C5500: u16 = 142;
```

#### Constant `EM_TI_ARP32`

Texas Instruments App. Specific RISC

```rust
pub const EM_TI_ARP32: u16 = 143;
```

#### Constant `EM_TI_PRU`

Texas Instruments Prog. Realtime Unit

```rust
pub const EM_TI_PRU: u16 = 144;
```

#### Constant `EM_MMDSP_PLUS`

STMicroelectronics 64bit VLIW DSP

```rust
pub const EM_MMDSP_PLUS: u16 = 160;
```

#### Constant `EM_CYPRESS_M8C`

Cypress M8C

```rust
pub const EM_CYPRESS_M8C: u16 = 161;
```

#### Constant `EM_R32C`

Renesas R32C

```rust
pub const EM_R32C: u16 = 162;
```

#### Constant `EM_TRIMEDIA`

NXP Semi. TriMedia

```rust
pub const EM_TRIMEDIA: u16 = 163;
```

#### Constant `EM_HEXAGON`

QUALCOMM Hexagon

```rust
pub const EM_HEXAGON: u16 = 164;
```

#### Constant `EM_8051`

Intel 8051 and variants

```rust
pub const EM_8051: u16 = 165;
```

#### Constant `EM_STXP7X`

STMicroelectronics STxP7x

```rust
pub const EM_STXP7X: u16 = 166;
```

#### Constant `EM_NDS32`

Andes Tech. compact code emb. RISC

```rust
pub const EM_NDS32: u16 = 167;
```

#### Constant `EM_ECOG1X`

Cyan Technology eCOG1X

```rust
pub const EM_ECOG1X: u16 = 168;
```

#### Constant `EM_MAXQ30`

Dallas Semi. MAXQ30 mc

```rust
pub const EM_MAXQ30: u16 = 169;
```

#### Constant `EM_XIMO16`

New Japan Radio (NJR) 16-bit DSP

```rust
pub const EM_XIMO16: u16 = 170;
```

#### Constant `EM_MANIK`

M2000 Reconfigurable RISC

```rust
pub const EM_MANIK: u16 = 171;
```

#### Constant `EM_CRAYNV2`

Cray NV2 vector architecture

```rust
pub const EM_CRAYNV2: u16 = 172;
```

#### Constant `EM_RX`

Renesas RX

```rust
pub const EM_RX: u16 = 173;
```

#### Constant `EM_METAG`

Imagination Tech. META

```rust
pub const EM_METAG: u16 = 174;
```

#### Constant `EM_MCST_ELBRUS`

MCST Elbrus

```rust
pub const EM_MCST_ELBRUS: u16 = 175;
```

#### Constant `EM_ECOG16`

Cyan Technology eCOG16

```rust
pub const EM_ECOG16: u16 = 176;
```

#### Constant `EM_CR16`

National Semi. CompactRISC CR16

```rust
pub const EM_CR16: u16 = 177;
```

#### Constant `EM_ETPU`

Freescale Extended Time Processing Unit

```rust
pub const EM_ETPU: u16 = 178;
```

#### Constant `EM_SLE9X`

Infineon Tech. SLE9X

```rust
pub const EM_SLE9X: u16 = 179;
```

#### Constant `EM_L10M`

Intel L10M

```rust
pub const EM_L10M: u16 = 180;
```

#### Constant `EM_K10M`

Intel K10M

```rust
pub const EM_K10M: u16 = 181;
```

#### Constant `EM_AARCH64`

ARM AARCH64

```rust
pub const EM_AARCH64: u16 = 183;
```

#### Constant `EM_AVR32`

Amtel 32-bit microprocessor

```rust
pub const EM_AVR32: u16 = 185;
```

#### Constant `EM_STM8`

STMicroelectronics STM8

```rust
pub const EM_STM8: u16 = 186;
```

#### Constant `EM_TILE64`

Tileta TILE64

```rust
pub const EM_TILE64: u16 = 187;
```

#### Constant `EM_TILEPRO`

Tilera TILEPro

```rust
pub const EM_TILEPRO: u16 = 188;
```

#### Constant `EM_MICROBLAZE`

Xilinx MicroBlaze

```rust
pub const EM_MICROBLAZE: u16 = 189;
```

#### Constant `EM_CUDA`

NVIDIA CUDA

```rust
pub const EM_CUDA: u16 = 190;
```

#### Constant `EM_TILEGX`

Tilera TILE-Gx

```rust
pub const EM_TILEGX: u16 = 191;
```

#### Constant `EM_CLOUDSHIELD`

CloudShield

```rust
pub const EM_CLOUDSHIELD: u16 = 192;
```

#### Constant `EM_COREA_1ST`

KIPO-KAIST Core-A 1st gen.

```rust
pub const EM_COREA_1ST: u16 = 193;
```

#### Constant `EM_COREA_2ND`

KIPO-KAIST Core-A 2nd gen.

```rust
pub const EM_COREA_2ND: u16 = 194;
```

#### Constant `EM_ARC_COMPACT2`

Synopsys ARCompact V2

```rust
pub const EM_ARC_COMPACT2: u16 = 195;
```

#### Constant `EM_OPEN8`

Open8 RISC

```rust
pub const EM_OPEN8: u16 = 196;
```

#### Constant `EM_RL78`

Renesas RL78

```rust
pub const EM_RL78: u16 = 197;
```

#### Constant `EM_VIDEOCORE5`

Broadcom VideoCore V

```rust
pub const EM_VIDEOCORE5: u16 = 198;
```

#### Constant `EM_78KOR`

Renesas 78KOR

```rust
pub const EM_78KOR: u16 = 199;
```

#### Constant `EM_56800EX`

Freescale 56800EX DSC

```rust
pub const EM_56800EX: u16 = 200;
```

#### Constant `EM_BA1`

Beyond BA1

```rust
pub const EM_BA1: u16 = 201;
```

#### Constant `EM_BA2`

Beyond BA2

```rust
pub const EM_BA2: u16 = 202;
```

#### Constant `EM_XCORE`

XMOS xCORE

```rust
pub const EM_XCORE: u16 = 203;
```

#### Constant `EM_MCHP_PIC`

Microchip 8-bit PIC(r)

```rust
pub const EM_MCHP_PIC: u16 = 204;
```

#### Constant `EM_KM32`

KM211 KM32

```rust
pub const EM_KM32: u16 = 210;
```

#### Constant `EM_KMX32`

KM211 KMX32

```rust
pub const EM_KMX32: u16 = 211;
```

#### Constant `EM_EMX16`

KM211 KMX16

```rust
pub const EM_EMX16: u16 = 212;
```

#### Constant `EM_EMX8`

KM211 KMX8

```rust
pub const EM_EMX8: u16 = 213;
```

#### Constant `EM_KVARC`

KM211 KVARC

```rust
pub const EM_KVARC: u16 = 214;
```

#### Constant `EM_CDP`

Paneve CDP

```rust
pub const EM_CDP: u16 = 215;
```

#### Constant `EM_COGE`

Cognitive Smart Memory Processor

```rust
pub const EM_COGE: u16 = 216;
```

#### Constant `EM_COOL`

Bluechip CoolEngine

```rust
pub const EM_COOL: u16 = 217;
```

#### Constant `EM_NORC`

Nanoradio Optimized RISC

```rust
pub const EM_NORC: u16 = 218;
```

#### Constant `EM_CSR_KALIMBA`

CSR Kalimba

```rust
pub const EM_CSR_KALIMBA: u16 = 219;
```

#### Constant `EM_Z80`

Zilog Z80

```rust
pub const EM_Z80: u16 = 220;
```

#### Constant `EM_VISIUM`

Controls and Data Services VISIUMcore

```rust
pub const EM_VISIUM: u16 = 221;
```

#### Constant `EM_FT32`

FTDI Chip FT32

```rust
pub const EM_FT32: u16 = 222;
```

#### Constant `EM_MOXIE`

Moxie processor

```rust
pub const EM_MOXIE: u16 = 223;
```

#### Constant `EM_AMDGPU`

AMD GPU

```rust
pub const EM_AMDGPU: u16 = 224;
```

#### Constant `EM_RISCV`

RISC-V

```rust
pub const EM_RISCV: u16 = 243;
```

#### Constant `EM_BPF`

Linux BPF -- in-kernel virtual machine

```rust
pub const EM_BPF: u16 = 247;
```

#### Constant `EM_CSKY`

C-SKY

```rust
pub const EM_CSKY: u16 = 252;
```

#### Constant `EM_LOONGARCH`

Loongson LoongArch

```rust
pub const EM_LOONGARCH: u16 = 258;
```

#### Constant `EM_SBF`

Solana Binary Format

```rust
pub const EM_SBF: u16 = 263;
```

#### Constant `EM_ALPHA`

Digital Alpha

```rust
pub const EM_ALPHA: u16 = 0x9026;
```

#### Constant `EV_NONE`

Invalid ELF version.

```rust
pub const EV_NONE: u8 = 0;
```

#### Constant `EV_CURRENT`

Current ELF version.

```rust
pub const EV_CURRENT: u8 = 1;
```

#### Constant `SHN_UNDEF`

Undefined section.

```rust
pub const SHN_UNDEF: u16 = 0;
```

#### Constant `SHN_LORESERVE`

OS-specific range start.
Start of reserved section indices.

```rust
pub const SHN_LORESERVE: u16 = 0xff00;
```

#### Constant `SHN_LOPROC`

Start of processor-specific section indices.

```rust
pub const SHN_LOPROC: u16 = 0xff00;
```

#### Constant `SHN_HIPROC`

End of processor-specific section indices.

```rust
pub const SHN_HIPROC: u16 = 0xff1f;
```

#### Constant `SHN_LOOS`

Start of OS-specific section indices.

```rust
pub const SHN_LOOS: u16 = 0xff20;
```

#### Constant `SHN_HIOS`

End of OS-specific section indices.

```rust
pub const SHN_HIOS: u16 = 0xff3f;
```

#### Constant `SHN_ABS`

Associated symbol is absolute.

```rust
pub const SHN_ABS: u16 = 0xfff1;
```

#### Constant `SHN_COMMON`

Associated symbol is common.

```rust
pub const SHN_COMMON: u16 = 0xfff2;
```

#### Constant `SHN_XINDEX`

Section index is in the `SHT_SYMTAB_SHNDX` section.

```rust
pub const SHN_XINDEX: u16 = 0xffff;
```

#### Constant `SHN_HIRESERVE`

End of reserved section indices.

```rust
pub const SHN_HIRESERVE: u16 = 0xffff;
```

#### Constant `SHT_NULL`

Section header table entry is unused.

```rust
pub const SHT_NULL: u32 = 0;
```

#### Constant `SHT_PROGBITS`

Program data.

```rust
pub const SHT_PROGBITS: u32 = 1;
```

#### Constant `SHT_SYMTAB`

Symbol table.

```rust
pub const SHT_SYMTAB: u32 = 2;
```

#### Constant `SHT_STRTAB`

String table.

```rust
pub const SHT_STRTAB: u32 = 3;
```

#### Constant `SHT_RELA`

Relocation entries with explicit addends.

```rust
pub const SHT_RELA: u32 = 4;
```

#### Constant `SHT_HASH`

Symbol hash table.

```rust
pub const SHT_HASH: u32 = 5;
```

#### Constant `SHT_DYNAMIC`

Dynamic linking information.

```rust
pub const SHT_DYNAMIC: u32 = 6;
```

#### Constant `SHT_NOTE`

Notes.

```rust
pub const SHT_NOTE: u32 = 7;
```

#### Constant `SHT_NOBITS`

Program space with no data (bss).

```rust
pub const SHT_NOBITS: u32 = 8;
```

#### Constant `SHT_REL`

Relocation entries without explicit addends.

```rust
pub const SHT_REL: u32 = 9;
```

#### Constant `SHT_SHLIB`

Reserved section type.

```rust
pub const SHT_SHLIB: u32 = 10;
```

#### Constant `SHT_DYNSYM`

Dynamic linker symbol table.

```rust
pub const SHT_DYNSYM: u32 = 11;
```

#### Constant `SHT_INIT_ARRAY`

Array of constructors.

```rust
pub const SHT_INIT_ARRAY: u32 = 14;
```

#### Constant `SHT_FINI_ARRAY`

Array of destructors.

```rust
pub const SHT_FINI_ARRAY: u32 = 15;
```

#### Constant `SHT_PREINIT_ARRAY`

Array of pre-constructors.

```rust
pub const SHT_PREINIT_ARRAY: u32 = 16;
```

#### Constant `SHT_GROUP`

Section group.

```rust
pub const SHT_GROUP: u32 = 17;
```

#### Constant `SHT_SYMTAB_SHNDX`

Extended section indices for a symbol table.

```rust
pub const SHT_SYMTAB_SHNDX: u32 = 18;
```

#### Constant `SHT_RELR`

Relocation entries; only offsets.

```rust
pub const SHT_RELR: u32 = 19;
```

#### Constant `SHT_LOOS`

Start of OS-specific section types.

```rust
pub const SHT_LOOS: u32 = 0x6000_0000;
```

#### Constant `SHT_LLVM_DEPENDENT_LIBRARIES`

LLVM-style dependent libraries.

```rust
pub const SHT_LLVM_DEPENDENT_LIBRARIES: u32 = 0x6fff4c04;
```

#### Constant `SHT_GNU_ATTRIBUTES`

Object attributes.

```rust
pub const SHT_GNU_ATTRIBUTES: u32 = 0x6fff_fff5;
```

#### Constant `SHT_GNU_HASH`

GNU-style hash table.

```rust
pub const SHT_GNU_HASH: u32 = 0x6fff_fff6;
```

#### Constant `SHT_GNU_LIBLIST`

Prelink library list

```rust
pub const SHT_GNU_LIBLIST: u32 = 0x6fff_fff7;
```

#### Constant `SHT_CHECKSUM`

Checksum for DSO content.

```rust
pub const SHT_CHECKSUM: u32 = 0x6fff_fff8;
```

#### Constant `SHT_LOSUNW`

Sun-specific low bound.

```rust
pub const SHT_LOSUNW: u32 = 0x6fff_fffa;
```

#### Constant `SHT_SUNW_move`

**Attributes:**

- `#[allow(non_upper_case_globals)]`

```rust
pub const SHT_SUNW_move: u32 = 0x6fff_fffa;
```

#### Constant `SHT_SUNW_COMDAT`

```rust
pub const SHT_SUNW_COMDAT: u32 = 0x6fff_fffb;
```

#### Constant `SHT_SUNW_syminfo`

**Attributes:**

- `#[allow(non_upper_case_globals)]`

```rust
pub const SHT_SUNW_syminfo: u32 = 0x6fff_fffc;
```

#### Constant `SHT_GNU_VERDEF`

**Attributes:**

- `#[allow(non_upper_case_globals)]`

Version definition section.

```rust
pub const SHT_GNU_VERDEF: u32 = 0x6fff_fffd;
```

#### Constant `SHT_GNU_VERNEED`

**Attributes:**

- `#[allow(non_upper_case_globals)]`

Version needs section.

```rust
pub const SHT_GNU_VERNEED: u32 = 0x6fff_fffe;
```

#### Constant `SHT_GNU_VERSYM`

**Attributes:**

- `#[allow(non_upper_case_globals)]`

Version symbol table.

```rust
pub const SHT_GNU_VERSYM: u32 = 0x6fff_ffff;
```

#### Constant `SHT_HISUNW`

Sun-specific high bound.

```rust
pub const SHT_HISUNW: u32 = 0x6fff_ffff;
```

#### Constant `SHT_HIOS`

End of OS-specific section types.

```rust
pub const SHT_HIOS: u32 = 0x6fff_ffff;
```

#### Constant `SHT_LOPROC`

Start of processor-specific section types.

```rust
pub const SHT_LOPROC: u32 = 0x7000_0000;
```

#### Constant `SHT_HIPROC`

End of processor-specific section types.

```rust
pub const SHT_HIPROC: u32 = 0x7fff_ffff;
```

#### Constant `SHT_LOUSER`

Start of application-specific section types.

```rust
pub const SHT_LOUSER: u32 = 0x8000_0000;
```

#### Constant `SHT_HIUSER`

End of application-specific section types.

```rust
pub const SHT_HIUSER: u32 = 0x8fff_ffff;
```

#### Constant `SHF_WRITE`

Section is writable.

```rust
pub const SHF_WRITE: u32 = _;
```

#### Constant `SHF_ALLOC`

Section occupies memory during execution.

```rust
pub const SHF_ALLOC: u32 = _;
```

#### Constant `SHF_EXECINSTR`

Section is executable.

```rust
pub const SHF_EXECINSTR: u32 = _;
```

#### Constant `SHF_MERGE`

Section may be be merged to eliminate duplication.

```rust
pub const SHF_MERGE: u32 = _;
```

#### Constant `SHF_STRINGS`

Section contains nul-terminated strings.

```rust
pub const SHF_STRINGS: u32 = _;
```

#### Constant `SHF_INFO_LINK`

The `sh_info` field contains a section header table index.

```rust
pub const SHF_INFO_LINK: u32 = _;
```

#### Constant `SHF_LINK_ORDER`

Section has special ordering requirements when combining sections.

```rust
pub const SHF_LINK_ORDER: u32 = _;
```

#### Constant `SHF_OS_NONCONFORMING`

Section requires special OS-specific handling.

```rust
pub const SHF_OS_NONCONFORMING: u32 = _;
```

#### Constant `SHF_GROUP`

Section is a member of a group.

```rust
pub const SHF_GROUP: u32 = _;
```

#### Constant `SHF_TLS`

Section holds thread-local storage.

```rust
pub const SHF_TLS: u32 = _;
```

#### Constant `SHF_COMPRESSED`

Section is compressed.

Compressed sections begin with one of the `CompressionHeader*` headers.

```rust
pub const SHF_COMPRESSED: u32 = _;
```

#### Constant `SHF_MASKOS`

OS-specific section flags.

```rust
pub const SHF_MASKOS: u32 = 0x0ff0_0000;
```

#### Constant `SHF_GNU_RETAIN`

Section should not be garbage collected by the linker.

```rust
pub const SHF_GNU_RETAIN: u32 = _;
```

#### Constant `SHF_GNU_MBIND`

Mbind section.

```rust
pub const SHF_GNU_MBIND: u32 = _;
```

#### Constant `SHF_MASKPROC`

Processor-specific section flags.

```rust
pub const SHF_MASKPROC: u32 = 0xf000_0000;
```

#### Constant `SHF_EXCLUDE`

This section is excluded from the final executable or shared library.

```rust
pub const SHF_EXCLUDE: u32 = 0x8000_0000;
```

#### Constant `ELFCOMPRESS_ZLIB`

ZLIB/DEFLATE algorithm.

```rust
pub const ELFCOMPRESS_ZLIB: u32 = 1;
```

#### Constant `ELFCOMPRESS_ZSTD`

Zstandard algorithm.

```rust
pub const ELFCOMPRESS_ZSTD: u32 = 2;
```

#### Constant `ELFCOMPRESS_LOOS`

Start of OS-specific compression types.

```rust
pub const ELFCOMPRESS_LOOS: u32 = 0x6000_0000;
```

#### Constant `ELFCOMPRESS_HIOS`

End of OS-specific compression types.

```rust
pub const ELFCOMPRESS_HIOS: u32 = 0x6fff_ffff;
```

#### Constant `ELFCOMPRESS_LOPROC`

Start of processor-specific compression types.

```rust
pub const ELFCOMPRESS_LOPROC: u32 = 0x7000_0000;
```

#### Constant `ELFCOMPRESS_HIPROC`

End of processor-specific compression types.

```rust
pub const ELFCOMPRESS_HIPROC: u32 = 0x7fff_ffff;
```

#### Constant `GRP_COMDAT`

Mark group as COMDAT.

```rust
pub const GRP_COMDAT: u32 = 1;
```

#### Constant `SYMINFO_BT_SELF`

Symbol bound to self

```rust
pub const SYMINFO_BT_SELF: u16 = 0xffff;
```

#### Constant `SYMINFO_BT_PARENT`

Symbol bound to parent

```rust
pub const SYMINFO_BT_PARENT: u16 = 0xfffe;
```

#### Constant `SYMINFO_BT_LOWRESERVE`

Beginning of reserved entries

```rust
pub const SYMINFO_BT_LOWRESERVE: u16 = 0xff00;
```

#### Constant `SYMINFO_FLG_DIRECT`

Direct bound symbol

```rust
pub const SYMINFO_FLG_DIRECT: u16 = 0x0001;
```

#### Constant `SYMINFO_FLG_PASSTHRU`

Pass-thru symbol for translator

```rust
pub const SYMINFO_FLG_PASSTHRU: u16 = 0x0002;
```

#### Constant `SYMINFO_FLG_COPY`

Symbol is a copy-reloc

```rust
pub const SYMINFO_FLG_COPY: u16 = 0x0004;
```

#### Constant `SYMINFO_FLG_LAZYLOAD`

Symbol bound to object to be lazy loaded

```rust
pub const SYMINFO_FLG_LAZYLOAD: u16 = 0x0008;
```

#### Constant `SYMINFO_NONE`

```rust
pub const SYMINFO_NONE: u16 = 0;
```

#### Constant `SYMINFO_CURRENT`

```rust
pub const SYMINFO_CURRENT: u16 = 1;
```

#### Constant `SYMINFO_NUM`

```rust
pub const SYMINFO_NUM: u16 = 2;
```

#### Constant `STB_LOCAL`

Local symbol.

```rust
pub const STB_LOCAL: u8 = 0;
```

#### Constant `STB_GLOBAL`

Global symbol.

```rust
pub const STB_GLOBAL: u8 = 1;
```

#### Constant `STB_WEAK`

Weak symbol.

```rust
pub const STB_WEAK: u8 = 2;
```

#### Constant `STB_LOOS`

Start of OS-specific symbol binding.

```rust
pub const STB_LOOS: u8 = 10;
```

#### Constant `STB_GNU_UNIQUE`

Unique symbol.

```rust
pub const STB_GNU_UNIQUE: u8 = 10;
```

#### Constant `STB_HIOS`

End of OS-specific symbol binding.

```rust
pub const STB_HIOS: u8 = 12;
```

#### Constant `STB_LOPROC`

Start of processor-specific symbol binding.

```rust
pub const STB_LOPROC: u8 = 13;
```

#### Constant `STB_HIPROC`

End of processor-specific symbol binding.

```rust
pub const STB_HIPROC: u8 = 15;
```

#### Constant `STT_NOTYPE`

Symbol type is unspecified.

```rust
pub const STT_NOTYPE: u8 = 0;
```

#### Constant `STT_OBJECT`

Symbol is a data object.

```rust
pub const STT_OBJECT: u8 = 1;
```

#### Constant `STT_FUNC`

Symbol is a code object.

```rust
pub const STT_FUNC: u8 = 2;
```

#### Constant `STT_SECTION`

Symbol is associated with a section.

```rust
pub const STT_SECTION: u8 = 3;
```

#### Constant `STT_FILE`

Symbol's name is a file name.

```rust
pub const STT_FILE: u8 = 4;
```

#### Constant `STT_COMMON`

Symbol is a common data object.

```rust
pub const STT_COMMON: u8 = 5;
```

#### Constant `STT_TLS`

Symbol is a thread-local storage object.

```rust
pub const STT_TLS: u8 = 6;
```

#### Constant `STT_LOOS`

Start of OS-specific symbol types.

```rust
pub const STT_LOOS: u8 = 10;
```

#### Constant `STT_GNU_IFUNC`

Symbol is an indirect code object.

```rust
pub const STT_GNU_IFUNC: u8 = 10;
```

#### Constant `STT_HIOS`

End of OS-specific symbol types.

```rust
pub const STT_HIOS: u8 = 12;
```

#### Constant `STT_LOPROC`

Start of processor-specific symbol types.

```rust
pub const STT_LOPROC: u8 = 13;
```

#### Constant `STT_HIPROC`

End of processor-specific symbol types.

```rust
pub const STT_HIPROC: u8 = 15;
```

#### Constant `STV_DEFAULT`

Default symbol visibility rules.

```rust
pub const STV_DEFAULT: u8 = 0;
```

#### Constant `STV_INTERNAL`

Processor specific hidden class.

```rust
pub const STV_INTERNAL: u8 = 1;
```

#### Constant `STV_HIDDEN`

Symbol is not visible to other components.

```rust
pub const STV_HIDDEN: u8 = 2;
```

#### Constant `STV_PROTECTED`

Symbol is visible to other components, but is not preemptible.

```rust
pub const STV_PROTECTED: u8 = 3;
```

#### Constant `PN_XNUM`

Special value for `FileHeader*::e_phnum`.

This indicates that the real number of program headers is too large to fit into e_phnum.
Instead the real value is in the field `sh_info` of section 0.

```rust
pub const PN_XNUM: u16 = 0xffff;
```

#### Constant `PT_NULL`

Program header table entry is unused.

```rust
pub const PT_NULL: u32 = 0;
```

#### Constant `PT_LOAD`

Loadable program segment.

```rust
pub const PT_LOAD: u32 = 1;
```

#### Constant `PT_DYNAMIC`

Dynamic linking information.

```rust
pub const PT_DYNAMIC: u32 = 2;
```

#### Constant `PT_INTERP`

Program interpreter.

```rust
pub const PT_INTERP: u32 = 3;
```

#### Constant `PT_NOTE`

Auxiliary information.

```rust
pub const PT_NOTE: u32 = 4;
```

#### Constant `PT_SHLIB`

Reserved.

```rust
pub const PT_SHLIB: u32 = 5;
```

#### Constant `PT_PHDR`

Segment contains the program header table.

```rust
pub const PT_PHDR: u32 = 6;
```

#### Constant `PT_TLS`

Thread-local storage segment.

```rust
pub const PT_TLS: u32 = 7;
```

#### Constant `PT_LOOS`

Start of OS-specific segment types.

```rust
pub const PT_LOOS: u32 = 0x6000_0000;
```

#### Constant `PT_GNU_EH_FRAME`

GCC `.eh_frame_hdr` segment.

```rust
pub const PT_GNU_EH_FRAME: u32 = 0x6474_e550;
```

#### Constant `PT_GNU_STACK`

Indicates stack executability.

```rust
pub const PT_GNU_STACK: u32 = 0x6474_e551;
```

#### Constant `PT_GNU_RELRO`

Read-only after relocation.

```rust
pub const PT_GNU_RELRO: u32 = 0x6474_e552;
```

#### Constant `PT_GNU_PROPERTY`

Segment containing `.note.gnu.property` section.

```rust
pub const PT_GNU_PROPERTY: u32 = 0x6474_e553;
```

#### Constant `PT_HIOS`

End of OS-specific segment types.

```rust
pub const PT_HIOS: u32 = 0x6fff_ffff;
```

#### Constant `PT_LOPROC`

Start of processor-specific segment types.

```rust
pub const PT_LOPROC: u32 = 0x7000_0000;
```

#### Constant `PT_HIPROC`

End of processor-specific segment types.

```rust
pub const PT_HIPROC: u32 = 0x7fff_ffff;
```

#### Constant `PF_X`

Segment is executable.

```rust
pub const PF_X: u32 = _;
```

#### Constant `PF_W`

Segment is writable.

```rust
pub const PF_W: u32 = _;
```

#### Constant `PF_R`

Segment is readable.

```rust
pub const PF_R: u32 = _;
```

#### Constant `PF_MASKOS`

OS-specific segment flags.

```rust
pub const PF_MASKOS: u32 = 0x0ff0_0000;
```

#### Constant `PF_MASKPROC`

Processor-specific segment flags.

```rust
pub const PF_MASKPROC: u32 = 0xf000_0000;
```

#### Constant `ELF_NOTE_CORE`

Note name for core files.

```rust
pub const ELF_NOTE_CORE: &[u8] = b"CORE";
```

#### Constant `ELF_NOTE_LINUX`

Note name for linux core files.

Notes in linux core files may also use `ELF_NOTE_CORE`.

```rust
pub const ELF_NOTE_LINUX: &[u8] = b"LINUX";
```

#### Constant `NT_PRSTATUS`

Contains copy of prstatus struct.

```rust
pub const NT_PRSTATUS: u32 = 1;
```

#### Constant `NT_PRFPREG`

Contains copy of fpregset struct.

```rust
pub const NT_PRFPREG: u32 = 2;
```

#### Constant `NT_FPREGSET`

Contains copy of fpregset struct.

```rust
pub const NT_FPREGSET: u32 = 2;
```

#### Constant `NT_PRPSINFO`

Contains copy of prpsinfo struct.

```rust
pub const NT_PRPSINFO: u32 = 3;
```

#### Constant `NT_PRXREG`

Contains copy of prxregset struct.

```rust
pub const NT_PRXREG: u32 = 4;
```

#### Constant `NT_TASKSTRUCT`

Contains copy of task structure.

```rust
pub const NT_TASKSTRUCT: u32 = 4;
```

#### Constant `NT_PLATFORM`

String from sysinfo(SI_PLATFORM).

```rust
pub const NT_PLATFORM: u32 = 5;
```

#### Constant `NT_AUXV`

Contains copy of auxv array.

```rust
pub const NT_AUXV: u32 = 6;
```

#### Constant `NT_GWINDOWS`

Contains copy of gwindows struct.

```rust
pub const NT_GWINDOWS: u32 = 7;
```

#### Constant `NT_ASRS`

Contains copy of asrset struct.

```rust
pub const NT_ASRS: u32 = 8;
```

#### Constant `NT_PSTATUS`

Contains copy of pstatus struct.

```rust
pub const NT_PSTATUS: u32 = 10;
```

#### Constant `NT_PSINFO`

Contains copy of psinfo struct.

```rust
pub const NT_PSINFO: u32 = 13;
```

#### Constant `NT_PRCRED`

Contains copy of prcred struct.

```rust
pub const NT_PRCRED: u32 = 14;
```

#### Constant `NT_UTSNAME`

Contains copy of utsname struct.

```rust
pub const NT_UTSNAME: u32 = 15;
```

#### Constant `NT_LWPSTATUS`

Contains copy of lwpstatus struct.

```rust
pub const NT_LWPSTATUS: u32 = 16;
```

#### Constant `NT_LWPSINFO`

Contains copy of lwpinfo struct.

```rust
pub const NT_LWPSINFO: u32 = 17;
```

#### Constant `NT_PRFPXREG`

Contains copy of fprxregset struct.

```rust
pub const NT_PRFPXREG: u32 = 20;
```

#### Constant `NT_SIGINFO`

Contains copy of siginfo_t, size might increase.

```rust
pub const NT_SIGINFO: u32 = 0x5349_4749;
```

#### Constant `NT_FILE`

Contains information about mapped files.

```rust
pub const NT_FILE: u32 = 0x4649_4c45;
```

#### Constant `NT_PRXFPREG`

Contains copy of user_fxsr_struct.

```rust
pub const NT_PRXFPREG: u32 = 0x46e6_2b7f;
```

#### Constant `NT_PPC_VMX`

PowerPC Altivec/VMX registers.

```rust
pub const NT_PPC_VMX: u32 = 0x100;
```

#### Constant `NT_PPC_SPE`

PowerPC SPE/EVR registers.

```rust
pub const NT_PPC_SPE: u32 = 0x101;
```

#### Constant `NT_PPC_VSX`

PowerPC VSX registers.

```rust
pub const NT_PPC_VSX: u32 = 0x102;
```

#### Constant `NT_PPC_TAR`

Target Address Register.

```rust
pub const NT_PPC_TAR: u32 = 0x103;
```

#### Constant `NT_PPC_PPR`

Program Priority Register.

```rust
pub const NT_PPC_PPR: u32 = 0x104;
```

#### Constant `NT_PPC_DSCR`

Data Stream Control Register.

```rust
pub const NT_PPC_DSCR: u32 = 0x105;
```

#### Constant `NT_PPC_EBB`

Event Based Branch Registers.

```rust
pub const NT_PPC_EBB: u32 = 0x106;
```

#### Constant `NT_PPC_PMU`

Performance Monitor Registers.

```rust
pub const NT_PPC_PMU: u32 = 0x107;
```

#### Constant `NT_PPC_TM_CGPR`

TM checkpointed GPR Registers.

```rust
pub const NT_PPC_TM_CGPR: u32 = 0x108;
```

#### Constant `NT_PPC_TM_CFPR`

TM checkpointed FPR Registers.

```rust
pub const NT_PPC_TM_CFPR: u32 = 0x109;
```

#### Constant `NT_PPC_TM_CVMX`

TM checkpointed VMX Registers.

```rust
pub const NT_PPC_TM_CVMX: u32 = 0x10a;
```

#### Constant `NT_PPC_TM_CVSX`

TM checkpointed VSX Registers.

```rust
pub const NT_PPC_TM_CVSX: u32 = 0x10b;
```

#### Constant `NT_PPC_TM_SPR`

TM Special Purpose Registers.

```rust
pub const NT_PPC_TM_SPR: u32 = 0x10c;
```

#### Constant `NT_PPC_TM_CTAR`

TM checkpointed Target Address Register.

```rust
pub const NT_PPC_TM_CTAR: u32 = 0x10d;
```

#### Constant `NT_PPC_TM_CPPR`

TM checkpointed Program Priority Register.

```rust
pub const NT_PPC_TM_CPPR: u32 = 0x10e;
```

#### Constant `NT_PPC_TM_CDSCR`

TM checkpointed Data Stream Control Register.

```rust
pub const NT_PPC_TM_CDSCR: u32 = 0x10f;
```

#### Constant `NT_PPC_PKEY`

Memory Protection Keys registers.

```rust
pub const NT_PPC_PKEY: u32 = 0x110;
```

#### Constant `NT_386_TLS`

i386 TLS slots (struct user_desc).

```rust
pub const NT_386_TLS: u32 = 0x200;
```

#### Constant `NT_386_IOPERM`

x86 io permission bitmap (1=deny).

```rust
pub const NT_386_IOPERM: u32 = 0x201;
```

#### Constant `NT_X86_XSTATE`

x86 extended state using xsave.

```rust
pub const NT_X86_XSTATE: u32 = 0x202;
```

#### Constant `NT_S390_HIGH_GPRS`

s390 upper register halves.

```rust
pub const NT_S390_HIGH_GPRS: u32 = 0x300;
```

#### Constant `NT_S390_TIMER`

s390 timer register.

```rust
pub const NT_S390_TIMER: u32 = 0x301;
```

#### Constant `NT_S390_TODCMP`

s390 TOD clock comparator register.

```rust
pub const NT_S390_TODCMP: u32 = 0x302;
```

#### Constant `NT_S390_TODPREG`

s390 TOD programmable register.

```rust
pub const NT_S390_TODPREG: u32 = 0x303;
```

#### Constant `NT_S390_CTRS`

s390 control registers.

```rust
pub const NT_S390_CTRS: u32 = 0x304;
```

#### Constant `NT_S390_PREFIX`

s390 prefix register.

```rust
pub const NT_S390_PREFIX: u32 = 0x305;
```

#### Constant `NT_S390_LAST_BREAK`

s390 breaking event address.

```rust
pub const NT_S390_LAST_BREAK: u32 = 0x306;
```

#### Constant `NT_S390_SYSTEM_CALL`

s390 system call restart data.

```rust
pub const NT_S390_SYSTEM_CALL: u32 = 0x307;
```

#### Constant `NT_S390_TDB`

s390 transaction diagnostic block.

```rust
pub const NT_S390_TDB: u32 = 0x308;
```

#### Constant `NT_S390_VXRS_LOW`

s390 vector registers 0-15 upper half.

```rust
pub const NT_S390_VXRS_LOW: u32 = 0x309;
```

#### Constant `NT_S390_VXRS_HIGH`

s390 vector registers 16-31.

```rust
pub const NT_S390_VXRS_HIGH: u32 = 0x30a;
```

#### Constant `NT_S390_GS_CB`

s390 guarded storage registers.

```rust
pub const NT_S390_GS_CB: u32 = 0x30b;
```

#### Constant `NT_S390_GS_BC`

s390 guarded storage broadcast control block.

```rust
pub const NT_S390_GS_BC: u32 = 0x30c;
```

#### Constant `NT_S390_RI_CB`

s390 runtime instrumentation.

```rust
pub const NT_S390_RI_CB: u32 = 0x30d;
```

#### Constant `NT_ARM_VFP`

ARM VFP/NEON registers.

```rust
pub const NT_ARM_VFP: u32 = 0x400;
```

#### Constant `NT_ARM_TLS`

ARM TLS register.

```rust
pub const NT_ARM_TLS: u32 = 0x401;
```

#### Constant `NT_ARM_HW_BREAK`

ARM hardware breakpoint registers.

```rust
pub const NT_ARM_HW_BREAK: u32 = 0x402;
```

#### Constant `NT_ARM_HW_WATCH`

ARM hardware watchpoint registers.

```rust
pub const NT_ARM_HW_WATCH: u32 = 0x403;
```

#### Constant `NT_ARM_SYSTEM_CALL`

ARM system call number.

```rust
pub const NT_ARM_SYSTEM_CALL: u32 = 0x404;
```

#### Constant `NT_ARM_SVE`

ARM Scalable Vector Extension registers.

```rust
pub const NT_ARM_SVE: u32 = 0x405;
```

#### Constant `NT_VMCOREDD`

Vmcore Device Dump Note.

```rust
pub const NT_VMCOREDD: u32 = 0x700;
```

#### Constant `NT_MIPS_DSP`

MIPS DSP ASE registers.

```rust
pub const NT_MIPS_DSP: u32 = 0x800;
```

#### Constant `NT_MIPS_FP_MODE`

MIPS floating-point mode.

```rust
pub const NT_MIPS_FP_MODE: u32 = 0x801;
```

#### Constant `NT_VERSION`

Note type for version string.

This note may appear in object files.

It must be handled as a special case because it has no descriptor, and instead
uses the note name as the version string.

```rust
pub const NT_VERSION: u32 = 1;
```

#### Constant `DT_NULL`

Marks end of dynamic section

```rust
pub const DT_NULL: u32 = 0;
```

#### Constant `DT_NEEDED`

Name of needed library

```rust
pub const DT_NEEDED: u32 = 1;
```

#### Constant `DT_PLTRELSZ`

Size in bytes of PLT relocs

```rust
pub const DT_PLTRELSZ: u32 = 2;
```

#### Constant `DT_PLTGOT`

Processor defined value

```rust
pub const DT_PLTGOT: u32 = 3;
```

#### Constant `DT_HASH`

Address of symbol hash table

```rust
pub const DT_HASH: u32 = 4;
```

#### Constant `DT_STRTAB`

Address of string table

```rust
pub const DT_STRTAB: u32 = 5;
```

#### Constant `DT_SYMTAB`

Address of symbol table

```rust
pub const DT_SYMTAB: u32 = 6;
```

#### Constant `DT_RELA`

Address of Rela relocs

```rust
pub const DT_RELA: u32 = 7;
```

#### Constant `DT_RELASZ`

Total size of Rela relocs

```rust
pub const DT_RELASZ: u32 = 8;
```

#### Constant `DT_RELAENT`

Size of one Rela reloc

```rust
pub const DT_RELAENT: u32 = 9;
```

#### Constant `DT_STRSZ`

Size of string table

```rust
pub const DT_STRSZ: u32 = 10;
```

#### Constant `DT_SYMENT`

Size of one symbol table entry

```rust
pub const DT_SYMENT: u32 = 11;
```

#### Constant `DT_INIT`

Address of init function

```rust
pub const DT_INIT: u32 = 12;
```

#### Constant `DT_FINI`

Address of termination function

```rust
pub const DT_FINI: u32 = 13;
```

#### Constant `DT_SONAME`

Name of shared object

```rust
pub const DT_SONAME: u32 = 14;
```

#### Constant `DT_RPATH`

Library search path (deprecated)

```rust
pub const DT_RPATH: u32 = 15;
```

#### Constant `DT_SYMBOLIC`

Start symbol search here

```rust
pub const DT_SYMBOLIC: u32 = 16;
```

#### Constant `DT_REL`

Address of Rel relocs

```rust
pub const DT_REL: u32 = 17;
```

#### Constant `DT_RELSZ`

Total size of Rel relocs

```rust
pub const DT_RELSZ: u32 = 18;
```

#### Constant `DT_RELENT`

Size of one Rel reloc

```rust
pub const DT_RELENT: u32 = 19;
```

#### Constant `DT_PLTREL`

Type of reloc in PLT

```rust
pub const DT_PLTREL: u32 = 20;
```

#### Constant `DT_DEBUG`

For debugging; unspecified

```rust
pub const DT_DEBUG: u32 = 21;
```

#### Constant `DT_TEXTREL`

Reloc might modify .text

```rust
pub const DT_TEXTREL: u32 = 22;
```

#### Constant `DT_JMPREL`

Address of PLT relocs

```rust
pub const DT_JMPREL: u32 = 23;
```

#### Constant `DT_BIND_NOW`

Process relocations of object

```rust
pub const DT_BIND_NOW: u32 = 24;
```

#### Constant `DT_INIT_ARRAY`

Array with addresses of init fct

```rust
pub const DT_INIT_ARRAY: u32 = 25;
```

#### Constant `DT_FINI_ARRAY`

Array with addresses of fini fct

```rust
pub const DT_FINI_ARRAY: u32 = 26;
```

#### Constant `DT_INIT_ARRAYSZ`

Size in bytes of DT_INIT_ARRAY

```rust
pub const DT_INIT_ARRAYSZ: u32 = 27;
```

#### Constant `DT_FINI_ARRAYSZ`

Size in bytes of DT_FINI_ARRAY

```rust
pub const DT_FINI_ARRAYSZ: u32 = 28;
```

#### Constant `DT_RUNPATH`

Library search path

```rust
pub const DT_RUNPATH: u32 = 29;
```

#### Constant `DT_FLAGS`

Flags for the object being loaded

```rust
pub const DT_FLAGS: u32 = 30;
```

#### Constant `DT_ENCODING`

Start of encoded range

```rust
pub const DT_ENCODING: u32 = 32;
```

#### Constant `DT_PREINIT_ARRAY`

Array with addresses of preinit fct

```rust
pub const DT_PREINIT_ARRAY: u32 = 32;
```

#### Constant `DT_PREINIT_ARRAYSZ`

size in bytes of DT_PREINIT_ARRAY

```rust
pub const DT_PREINIT_ARRAYSZ: u32 = 33;
```

#### Constant `DT_SYMTAB_SHNDX`

Address of SYMTAB_SHNDX section

```rust
pub const DT_SYMTAB_SHNDX: u32 = 34;
```

#### Constant `DT_LOOS`

Start of OS-specific

```rust
pub const DT_LOOS: u32 = 0x6000_000d;
```

#### Constant `DT_HIOS`

End of OS-specific

```rust
pub const DT_HIOS: u32 = 0x6fff_f000;
```

#### Constant `DT_LOPROC`

Start of processor-specific

```rust
pub const DT_LOPROC: u32 = 0x7000_0000;
```

#### Constant `DT_HIPROC`

End of processor-specific

```rust
pub const DT_HIPROC: u32 = 0x7fff_ffff;
```

#### Constant `DT_VALRNGLO`

```rust
pub const DT_VALRNGLO: u32 = 0x6fff_fd00;
```

#### Constant `DT_GNU_PRELINKED`

Prelinking timestamp

```rust
pub const DT_GNU_PRELINKED: u32 = 0x6fff_fdf5;
```

#### Constant `DT_GNU_CONFLICTSZ`

Size of conflict section

```rust
pub const DT_GNU_CONFLICTSZ: u32 = 0x6fff_fdf6;
```

#### Constant `DT_GNU_LIBLISTSZ`

Size of library list

```rust
pub const DT_GNU_LIBLISTSZ: u32 = 0x6fff_fdf7;
```

#### Constant `DT_CHECKSUM`

```rust
pub const DT_CHECKSUM: u32 = 0x6fff_fdf8;
```

#### Constant `DT_PLTPADSZ`

```rust
pub const DT_PLTPADSZ: u32 = 0x6fff_fdf9;
```

#### Constant `DT_MOVEENT`

```rust
pub const DT_MOVEENT: u32 = 0x6fff_fdfa;
```

#### Constant `DT_MOVESZ`

```rust
pub const DT_MOVESZ: u32 = 0x6fff_fdfb;
```

#### Constant `DT_FEATURE_1`

Feature selection (DTF_*).

```rust
pub const DT_FEATURE_1: u32 = 0x6fff_fdfc;
```

#### Constant `DT_POSFLAG_1`

Flags for DT_* entries, affecting the following DT_* entry.

```rust
pub const DT_POSFLAG_1: u32 = 0x6fff_fdfd;
```

#### Constant `DT_SYMINSZ`

Size of syminfo table (in bytes)

```rust
pub const DT_SYMINSZ: u32 = 0x6fff_fdfe;
```

#### Constant `DT_SYMINENT`

Entry size of syminfo

```rust
pub const DT_SYMINENT: u32 = 0x6fff_fdff;
```

#### Constant `DT_VALRNGHI`

```rust
pub const DT_VALRNGHI: u32 = 0x6fff_fdff;
```

#### Constant `DT_ADDRRNGLO`

```rust
pub const DT_ADDRRNGLO: u32 = 0x6fff_fe00;
```

#### Constant `DT_GNU_HASH`

GNU-style hash table.

```rust
pub const DT_GNU_HASH: u32 = 0x6fff_fef5;
```

#### Constant `DT_TLSDESC_PLT`

```rust
pub const DT_TLSDESC_PLT: u32 = 0x6fff_fef6;
```

#### Constant `DT_TLSDESC_GOT`

```rust
pub const DT_TLSDESC_GOT: u32 = 0x6fff_fef7;
```

#### Constant `DT_GNU_CONFLICT`

Start of conflict section

```rust
pub const DT_GNU_CONFLICT: u32 = 0x6fff_fef8;
```

#### Constant `DT_GNU_LIBLIST`

Library list

```rust
pub const DT_GNU_LIBLIST: u32 = 0x6fff_fef9;
```

#### Constant `DT_CONFIG`

Configuration information.

```rust
pub const DT_CONFIG: u32 = 0x6fff_fefa;
```

#### Constant `DT_DEPAUDIT`

Dependency auditing.

```rust
pub const DT_DEPAUDIT: u32 = 0x6fff_fefb;
```

#### Constant `DT_AUDIT`

Object auditing.

```rust
pub const DT_AUDIT: u32 = 0x6fff_fefc;
```

#### Constant `DT_PLTPAD`

PLT padding.

```rust
pub const DT_PLTPAD: u32 = 0x6fff_fefd;
```

#### Constant `DT_MOVETAB`

Move table.

```rust
pub const DT_MOVETAB: u32 = 0x6fff_fefe;
```

#### Constant `DT_SYMINFO`

Syminfo table.

```rust
pub const DT_SYMINFO: u32 = 0x6fff_feff;
```

#### Constant `DT_ADDRRNGHI`

```rust
pub const DT_ADDRRNGHI: u32 = 0x6fff_feff;
```

#### Constant `DT_VERSYM`

```rust
pub const DT_VERSYM: u32 = 0x6fff_fff0;
```

#### Constant `DT_RELACOUNT`

```rust
pub const DT_RELACOUNT: u32 = 0x6fff_fff9;
```

#### Constant `DT_RELCOUNT`

```rust
pub const DT_RELCOUNT: u32 = 0x6fff_fffa;
```

#### Constant `DT_FLAGS_1`

State flags, see DF_1_* below.

```rust
pub const DT_FLAGS_1: u32 = 0x6fff_fffb;
```

#### Constant `DT_VERDEF`

Address of version definition table

```rust
pub const DT_VERDEF: u32 = 0x6fff_fffc;
```

#### Constant `DT_VERDEFNUM`

Number of version definitions

```rust
pub const DT_VERDEFNUM: u32 = 0x6fff_fffd;
```

#### Constant `DT_VERNEED`

Address of table with needed versions

```rust
pub const DT_VERNEED: u32 = 0x6fff_fffe;
```

#### Constant `DT_VERNEEDNUM`

Number of needed versions

```rust
pub const DT_VERNEEDNUM: u32 = 0x6fff_ffff;
```

#### Constant `DT_AUXILIARY`

Shared object to load before self

```rust
pub const DT_AUXILIARY: u32 = 0x7fff_fffd;
```

#### Constant `DT_FILTER`

Shared object to get values from

```rust
pub const DT_FILTER: u32 = 0x7fff_ffff;
```

#### Constant `DF_ORIGIN`

Object may use DF_ORIGIN

```rust
pub const DF_ORIGIN: u32 = 0x0000_0001;
```

#### Constant `DF_SYMBOLIC`

Symbol resolutions starts here

```rust
pub const DF_SYMBOLIC: u32 = 0x0000_0002;
```

#### Constant `DF_TEXTREL`

Object contains text relocations

```rust
pub const DF_TEXTREL: u32 = 0x0000_0004;
```

#### Constant `DF_BIND_NOW`

No lazy binding for this object

```rust
pub const DF_BIND_NOW: u32 = 0x0000_0008;
```

#### Constant `DF_STATIC_TLS`

Module uses the static TLS model

```rust
pub const DF_STATIC_TLS: u32 = 0x0000_0010;
```

#### Constant `DF_1_NOW`

Set RTLD_NOW for this object.

```rust
pub const DF_1_NOW: u32 = 0x0000_0001;
```

#### Constant `DF_1_GLOBAL`

Set RTLD_GLOBAL for this object.

```rust
pub const DF_1_GLOBAL: u32 = 0x0000_0002;
```

#### Constant `DF_1_GROUP`

Set RTLD_GROUP for this object.

```rust
pub const DF_1_GROUP: u32 = 0x0000_0004;
```

#### Constant `DF_1_NODELETE`

Set RTLD_NODELETE for this object.

```rust
pub const DF_1_NODELETE: u32 = 0x0000_0008;
```

#### Constant `DF_1_LOADFLTR`

Trigger filtee loading at runtime.

```rust
pub const DF_1_LOADFLTR: u32 = 0x0000_0010;
```

#### Constant `DF_1_INITFIRST`

Set RTLD_INITFIRST for this object.

```rust
pub const DF_1_INITFIRST: u32 = 0x0000_0020;
```

#### Constant `DF_1_NOOPEN`

Set RTLD_NOOPEN for this object.

```rust
pub const DF_1_NOOPEN: u32 = 0x0000_0040;
```

#### Constant `DF_1_ORIGIN`

$ORIGIN must be handled.

```rust
pub const DF_1_ORIGIN: u32 = 0x0000_0080;
```

#### Constant `DF_1_DIRECT`

Direct binding enabled.

```rust
pub const DF_1_DIRECT: u32 = 0x0000_0100;
```

#### Constant `DF_1_TRANS`

```rust
pub const DF_1_TRANS: u32 = 0x0000_0200;
```

#### Constant `DF_1_INTERPOSE`

Object is used to interpose.

```rust
pub const DF_1_INTERPOSE: u32 = 0x0000_0400;
```

#### Constant `DF_1_NODEFLIB`

Ignore default lib search path.

```rust
pub const DF_1_NODEFLIB: u32 = 0x0000_0800;
```

#### Constant `DF_1_NODUMP`

Object can't be dldump'ed.

```rust
pub const DF_1_NODUMP: u32 = 0x0000_1000;
```

#### Constant `DF_1_CONFALT`

Configuration alternative created.

```rust
pub const DF_1_CONFALT: u32 = 0x0000_2000;
```

#### Constant `DF_1_ENDFILTEE`

Filtee terminates filters search.

```rust
pub const DF_1_ENDFILTEE: u32 = 0x0000_4000;
```

#### Constant `DF_1_DISPRELDNE`

Disp reloc applied at build time.

```rust
pub const DF_1_DISPRELDNE: u32 = 0x0000_8000;
```

#### Constant `DF_1_DISPRELPND`

Disp reloc applied at run-time.

```rust
pub const DF_1_DISPRELPND: u32 = 0x0001_0000;
```

#### Constant `DF_1_NODIRECT`

Object has no-direct binding.

```rust
pub const DF_1_NODIRECT: u32 = 0x0002_0000;
```

#### Constant `DF_1_IGNMULDEF`

```rust
pub const DF_1_IGNMULDEF: u32 = 0x0004_0000;
```

#### Constant `DF_1_NOKSYMS`

```rust
pub const DF_1_NOKSYMS: u32 = 0x0008_0000;
```

#### Constant `DF_1_NOHDR`

```rust
pub const DF_1_NOHDR: u32 = 0x0010_0000;
```

#### Constant `DF_1_EDITED`

Object is modified after built.

```rust
pub const DF_1_EDITED: u32 = 0x0020_0000;
```

#### Constant `DF_1_NORELOC`

```rust
pub const DF_1_NORELOC: u32 = 0x0040_0000;
```

#### Constant `DF_1_SYMINTPOSE`

Object has individual interposers.

```rust
pub const DF_1_SYMINTPOSE: u32 = 0x0080_0000;
```

#### Constant `DF_1_GLOBAUDIT`

Global auditing required.

```rust
pub const DF_1_GLOBAUDIT: u32 = 0x0100_0000;
```

#### Constant `DF_1_SINGLETON`

Singleton symbols are used.

```rust
pub const DF_1_SINGLETON: u32 = 0x0200_0000;
```

#### Constant `DF_1_STUB`

```rust
pub const DF_1_STUB: u32 = 0x0400_0000;
```

#### Constant `DF_1_PIE`

```rust
pub const DF_1_PIE: u32 = 0x0800_0000;
```

#### Constant `VERSYM_HIDDEN`

Symbol is hidden.

```rust
pub const VERSYM_HIDDEN: u16 = 0x8000;
```

#### Constant `VERSYM_VERSION`

Symbol version index.

```rust
pub const VERSYM_VERSION: u16 = 0x7fff;
```

#### Constant `VER_DEF_NONE`

No version

```rust
pub const VER_DEF_NONE: u16 = 0;
```

#### Constant `VER_DEF_CURRENT`

Current version

```rust
pub const VER_DEF_CURRENT: u16 = 1;
```

#### Constant `VER_FLG_BASE`

Version definition of file itself

```rust
pub const VER_FLG_BASE: u16 = 0x1;
```

#### Constant `VER_FLG_WEAK`

Weak version identifier

```rust
pub const VER_FLG_WEAK: u16 = 0x2;
```

#### Constant `VER_NDX_LOCAL`

Symbol is local.

```rust
pub const VER_NDX_LOCAL: u16 = 0;
```

#### Constant `VER_NDX_GLOBAL`

Symbol is global.

```rust
pub const VER_NDX_GLOBAL: u16 = 1;
```

#### Constant `VER_NEED_NONE`

No version

```rust
pub const VER_NEED_NONE: u16 = 0;
```

#### Constant `VER_NEED_CURRENT`

Current version

```rust
pub const VER_NEED_CURRENT: u16 = 1;
```

#### Constant `ELF_NOTE_SOLARIS`

Solaris entries in the note section have this name.

```rust
pub const ELF_NOTE_SOLARIS: &[u8] = b"SUNW Solaris";
```

#### Constant `NT_SOLARIS_PAGESIZE_HINT`

Desired pagesize for the binary.

```rust
pub const NT_SOLARIS_PAGESIZE_HINT: u32 = 1;
```

#### Constant `ELF_NOTE_GNU`

GNU entries in the note section have this name.

```rust
pub const ELF_NOTE_GNU: &[u8] = b"GNU";
```

#### Constant `ELF_NOTE_GO`

Go entries in the note section have this name.

```rust
pub const ELF_NOTE_GO: &[u8] = b"Go";
```

#### Constant `NT_GNU_ABI_TAG`

ABI information.

The descriptor consists of words:
- word 0: OS descriptor
- word 1: major version of the ABI
- word 2: minor version of the ABI
- word 3: subminor version of the ABI

```rust
pub const NT_GNU_ABI_TAG: u32 = 1;
```

#### Constant `ELF_NOTE_OS_LINUX`

OS descriptor for `NT_GNU_ABI_TAG`.

```rust
pub const ELF_NOTE_OS_LINUX: u32 = 0;
```

#### Constant `ELF_NOTE_OS_GNU`

OS descriptor for `NT_GNU_ABI_TAG`.

```rust
pub const ELF_NOTE_OS_GNU: u32 = 1;
```

#### Constant `ELF_NOTE_OS_SOLARIS2`

OS descriptor for `NT_GNU_ABI_TAG`.

```rust
pub const ELF_NOTE_OS_SOLARIS2: u32 = 2;
```

#### Constant `ELF_NOTE_OS_FREEBSD`

OS descriptor for `NT_GNU_ABI_TAG`.

```rust
pub const ELF_NOTE_OS_FREEBSD: u32 = 3;
```

#### Constant `NT_GNU_HWCAP`

Synthetic hwcap information.

The descriptor begins with two words:
- word 0: number of entries
- word 1: bitmask of enabled entries

Then follow variable-length entries, one byte followed by a
'\0'-terminated hwcap name string.  The byte gives the bit
number to test if enabled, (1U << bit) & bitmask.  */

```rust
pub const NT_GNU_HWCAP: u32 = 2;
```

#### Constant `NT_GNU_BUILD_ID`

Build ID bits as generated by `ld --build-id`.

The descriptor consists of any nonzero number of bytes.

```rust
pub const NT_GNU_BUILD_ID: u32 = 3;
```

#### Constant `NT_GO_BUILD_ID`

Build ID bits as generated by Go's gc compiler.

The descriptor consists of any nonzero number of bytes.

```rust
pub const NT_GO_BUILD_ID: u32 = 4;
```

#### Constant `NT_GNU_GOLD_VERSION`

Version note generated by GNU gold containing a version string.

```rust
pub const NT_GNU_GOLD_VERSION: u32 = 4;
```

#### Constant `NT_GNU_PROPERTY_TYPE_0`

Program property.

```rust
pub const NT_GNU_PROPERTY_TYPE_0: u32 = 5;
```

#### Constant `GNU_PROPERTY_STACK_SIZE`

Stack size.

```rust
pub const GNU_PROPERTY_STACK_SIZE: u32 = 1;
```

#### Constant `GNU_PROPERTY_NO_COPY_ON_PROTECTED`

No copy relocation on protected data symbol.

```rust
pub const GNU_PROPERTY_NO_COPY_ON_PROTECTED: u32 = 2;
```

#### Constant `GNU_PROPERTY_UINT32_AND_LO`

```rust
pub const GNU_PROPERTY_UINT32_AND_LO: u32 = 0xb0000000;
```

#### Constant `GNU_PROPERTY_UINT32_AND_HI`

```rust
pub const GNU_PROPERTY_UINT32_AND_HI: u32 = 0xb0007fff;
```

#### Constant `GNU_PROPERTY_UINT32_OR_LO`

```rust
pub const GNU_PROPERTY_UINT32_OR_LO: u32 = 0xb0008000;
```

#### Constant `GNU_PROPERTY_UINT32_OR_HI`

```rust
pub const GNU_PROPERTY_UINT32_OR_HI: u32 = 0xb000ffff;
```

#### Constant `GNU_PROPERTY_1_NEEDED`

The needed properties by the object file.  */

```rust
pub const GNU_PROPERTY_1_NEEDED: u32 = GNU_PROPERTY_UINT32_OR_LO;
```

#### Constant `GNU_PROPERTY_1_NEEDED_INDIRECT_EXTERN_ACCESS`

Set if the object file requires canonical function pointers and
cannot be used with copy relocation.

```rust
pub const GNU_PROPERTY_1_NEEDED_INDIRECT_EXTERN_ACCESS: u32 = _;
```

#### Constant `GNU_PROPERTY_LOPROC`

Processor-specific semantics, lo

```rust
pub const GNU_PROPERTY_LOPROC: u32 = 0xc0000000;
```

#### Constant `GNU_PROPERTY_HIPROC`

Processor-specific semantics, hi

```rust
pub const GNU_PROPERTY_HIPROC: u32 = 0xdfffffff;
```

#### Constant `GNU_PROPERTY_LOUSER`

Application-specific semantics, lo

```rust
pub const GNU_PROPERTY_LOUSER: u32 = 0xe0000000;
```

#### Constant `GNU_PROPERTY_HIUSER`

Application-specific semantics, hi

```rust
pub const GNU_PROPERTY_HIUSER: u32 = 0xffffffff;
```

#### Constant `GNU_PROPERTY_AARCH64_FEATURE_1_AND`

AArch64 specific GNU properties.

```rust
pub const GNU_PROPERTY_AARCH64_FEATURE_1_AND: u32 = 0xc0000000;
```

#### Constant `GNU_PROPERTY_AARCH64_FEATURE_PAUTH`

```rust
pub const GNU_PROPERTY_AARCH64_FEATURE_PAUTH: u32 = 0xc0000001;
```

#### Constant `GNU_PROPERTY_AARCH64_FEATURE_1_BTI`

```rust
pub const GNU_PROPERTY_AARCH64_FEATURE_1_BTI: u32 = _;
```

#### Constant `GNU_PROPERTY_AARCH64_FEATURE_1_PAC`

```rust
pub const GNU_PROPERTY_AARCH64_FEATURE_1_PAC: u32 = _;
```

#### Constant `GNU_PROPERTY_X86_UINT32_AND_LO`

```rust
pub const GNU_PROPERTY_X86_UINT32_AND_LO: u32 = 0xc0000002;
```

#### Constant `GNU_PROPERTY_X86_UINT32_AND_HI`

```rust
pub const GNU_PROPERTY_X86_UINT32_AND_HI: u32 = 0xc0007fff;
```

#### Constant `GNU_PROPERTY_X86_UINT32_OR_LO`

```rust
pub const GNU_PROPERTY_X86_UINT32_OR_LO: u32 = 0xc0008000;
```

#### Constant `GNU_PROPERTY_X86_UINT32_OR_HI`

```rust
pub const GNU_PROPERTY_X86_UINT32_OR_HI: u32 = 0xc000ffff;
```

#### Constant `GNU_PROPERTY_X86_UINT32_OR_AND_LO`

```rust
pub const GNU_PROPERTY_X86_UINT32_OR_AND_LO: u32 = 0xc0010000;
```

#### Constant `GNU_PROPERTY_X86_UINT32_OR_AND_HI`

```rust
pub const GNU_PROPERTY_X86_UINT32_OR_AND_HI: u32 = 0xc0017fff;
```

#### Constant `GNU_PROPERTY_X86_ISA_1_USED`

The x86 instruction sets indicated by the corresponding bits are
used in program.  Their support in the hardware is optional.

```rust
pub const GNU_PROPERTY_X86_ISA_1_USED: u32 = 0xc0010002;
```

#### Constant `GNU_PROPERTY_X86_ISA_1_NEEDED`

The x86 instruction sets indicated by the corresponding bits are
used in program and they must be supported by the hardware.

```rust
pub const GNU_PROPERTY_X86_ISA_1_NEEDED: u32 = 0xc0008002;
```

#### Constant `GNU_PROPERTY_X86_FEATURE_1_AND`

X86 processor-specific features used in program.

```rust
pub const GNU_PROPERTY_X86_FEATURE_1_AND: u32 = 0xc0000002;
```

#### Constant `GNU_PROPERTY_X86_ISA_1_BASELINE`

GNU_PROPERTY_X86_ISA_1_BASELINE: CMOV, CX8 (cmpxchg8b), FPU (fld),
MMX, OSFXSR (fxsave), SCE (syscall), SSE and SSE2.

```rust
pub const GNU_PROPERTY_X86_ISA_1_BASELINE: u32 = _;
```

#### Constant `GNU_PROPERTY_X86_ISA_1_V2`

GNU_PROPERTY_X86_ISA_1_V2: GNU_PROPERTY_X86_ISA_1_BASELINE,
CMPXCHG16B (cmpxchg16b), LAHF-SAHF (lahf), POPCNT (popcnt), SSE3,
SSSE3, SSE4.1 and SSE4.2.

```rust
pub const GNU_PROPERTY_X86_ISA_1_V2: u32 = _;
```

#### Constant `GNU_PROPERTY_X86_ISA_1_V3`

GNU_PROPERTY_X86_ISA_1_V3: GNU_PROPERTY_X86_ISA_1_V2, AVX, AVX2, BMI1,
BMI2, F16C, FMA, LZCNT, MOVBE, XSAVE.

```rust
pub const GNU_PROPERTY_X86_ISA_1_V3: u32 = _;
```

#### Constant `GNU_PROPERTY_X86_ISA_1_V4`

GNU_PROPERTY_X86_ISA_1_V4: GNU_PROPERTY_X86_ISA_1_V3, AVX512F,
AVX512BW, AVX512CD, AVX512DQ and AVX512VL.

```rust
pub const GNU_PROPERTY_X86_ISA_1_V4: u32 = _;
```

#### Constant `GNU_PROPERTY_X86_FEATURE_1_IBT`

This indicates that all executable sections are compatible with IBT.

```rust
pub const GNU_PROPERTY_X86_FEATURE_1_IBT: u32 = _;
```

#### Constant `GNU_PROPERTY_X86_FEATURE_1_SHSTK`

This indicates that all executable sections are compatible with SHSTK.

```rust
pub const GNU_PROPERTY_X86_FEATURE_1_SHSTK: u32 = _;
```

#### Constant `R_68K_NONE`

No reloc

```rust
pub const R_68K_NONE: u32 = 0;
```

#### Constant `R_68K_32`

Direct 32 bit

```rust
pub const R_68K_32: u32 = 1;
```

#### Constant `R_68K_16`

Direct 16 bit

```rust
pub const R_68K_16: u32 = 2;
```

#### Constant `R_68K_8`

Direct 8 bit

```rust
pub const R_68K_8: u32 = 3;
```

#### Constant `R_68K_PC32`

PC relative 32 bit

```rust
pub const R_68K_PC32: u32 = 4;
```

#### Constant `R_68K_PC16`

PC relative 16 bit

```rust
pub const R_68K_PC16: u32 = 5;
```

#### Constant `R_68K_PC8`

PC relative 8 bit

```rust
pub const R_68K_PC8: u32 = 6;
```

#### Constant `R_68K_GOT32`

32 bit PC relative GOT entry

```rust
pub const R_68K_GOT32: u32 = 7;
```

#### Constant `R_68K_GOT16`

16 bit PC relative GOT entry

```rust
pub const R_68K_GOT16: u32 = 8;
```

#### Constant `R_68K_GOT8`

8 bit PC relative GOT entry

```rust
pub const R_68K_GOT8: u32 = 9;
```

#### Constant `R_68K_GOT32O`

32 bit GOT offset

```rust
pub const R_68K_GOT32O: u32 = 10;
```

#### Constant `R_68K_GOT16O`

16 bit GOT offset

```rust
pub const R_68K_GOT16O: u32 = 11;
```

#### Constant `R_68K_GOT8O`

8 bit GOT offset

```rust
pub const R_68K_GOT8O: u32 = 12;
```

#### Constant `R_68K_PLT32`

32 bit PC relative PLT address

```rust
pub const R_68K_PLT32: u32 = 13;
```

#### Constant `R_68K_PLT16`

16 bit PC relative PLT address

```rust
pub const R_68K_PLT16: u32 = 14;
```

#### Constant `R_68K_PLT8`

8 bit PC relative PLT address

```rust
pub const R_68K_PLT8: u32 = 15;
```

#### Constant `R_68K_PLT32O`

32 bit PLT offset

```rust
pub const R_68K_PLT32O: u32 = 16;
```

#### Constant `R_68K_PLT16O`

16 bit PLT offset

```rust
pub const R_68K_PLT16O: u32 = 17;
```

#### Constant `R_68K_PLT8O`

8 bit PLT offset

```rust
pub const R_68K_PLT8O: u32 = 18;
```

#### Constant `R_68K_COPY`

Copy symbol at runtime

```rust
pub const R_68K_COPY: u32 = 19;
```

#### Constant `R_68K_GLOB_DAT`

Create GOT entry

```rust
pub const R_68K_GLOB_DAT: u32 = 20;
```

#### Constant `R_68K_JMP_SLOT`

Create PLT entry

```rust
pub const R_68K_JMP_SLOT: u32 = 21;
```

#### Constant `R_68K_RELATIVE`

Adjust by program base

```rust
pub const R_68K_RELATIVE: u32 = 22;
```

#### Constant `R_68K_TLS_GD32`

32 bit GOT offset for GD

```rust
pub const R_68K_TLS_GD32: u32 = 25;
```

#### Constant `R_68K_TLS_GD16`

16 bit GOT offset for GD

```rust
pub const R_68K_TLS_GD16: u32 = 26;
```

#### Constant `R_68K_TLS_GD8`

8 bit GOT offset for GD

```rust
pub const R_68K_TLS_GD8: u32 = 27;
```

#### Constant `R_68K_TLS_LDM32`

32 bit GOT offset for LDM

```rust
pub const R_68K_TLS_LDM32: u32 = 28;
```

#### Constant `R_68K_TLS_LDM16`

16 bit GOT offset for LDM

```rust
pub const R_68K_TLS_LDM16: u32 = 29;
```

#### Constant `R_68K_TLS_LDM8`

8 bit GOT offset for LDM

```rust
pub const R_68K_TLS_LDM8: u32 = 30;
```

#### Constant `R_68K_TLS_LDO32`

32 bit module-relative offset

```rust
pub const R_68K_TLS_LDO32: u32 = 31;
```

#### Constant `R_68K_TLS_LDO16`

16 bit module-relative offset

```rust
pub const R_68K_TLS_LDO16: u32 = 32;
```

#### Constant `R_68K_TLS_LDO8`

8 bit module-relative offset

```rust
pub const R_68K_TLS_LDO8: u32 = 33;
```

#### Constant `R_68K_TLS_IE32`

32 bit GOT offset for IE

```rust
pub const R_68K_TLS_IE32: u32 = 34;
```

#### Constant `R_68K_TLS_IE16`

16 bit GOT offset for IE

```rust
pub const R_68K_TLS_IE16: u32 = 35;
```

#### Constant `R_68K_TLS_IE8`

8 bit GOT offset for IE

```rust
pub const R_68K_TLS_IE8: u32 = 36;
```

#### Constant `R_68K_TLS_LE32`

32 bit offset relative to static TLS block

```rust
pub const R_68K_TLS_LE32: u32 = 37;
```

#### Constant `R_68K_TLS_LE16`

16 bit offset relative to static TLS block

```rust
pub const R_68K_TLS_LE16: u32 = 38;
```

#### Constant `R_68K_TLS_LE8`

8 bit offset relative to static TLS block

```rust
pub const R_68K_TLS_LE8: u32 = 39;
```

#### Constant `R_68K_TLS_DTPMOD32`

32 bit module number

```rust
pub const R_68K_TLS_DTPMOD32: u32 = 40;
```

#### Constant `R_68K_TLS_DTPREL32`

32 bit module-relative offset

```rust
pub const R_68K_TLS_DTPREL32: u32 = 41;
```

#### Constant `R_68K_TLS_TPREL32`

32 bit TP-relative offset

```rust
pub const R_68K_TLS_TPREL32: u32 = 42;
```

#### Constant `R_386_NONE`

No reloc

```rust
pub const R_386_NONE: u32 = 0;
```

#### Constant `R_386_32`

Direct 32 bit

```rust
pub const R_386_32: u32 = 1;
```

#### Constant `R_386_PC32`

PC relative 32 bit

```rust
pub const R_386_PC32: u32 = 2;
```

#### Constant `R_386_GOT32`

32 bit GOT entry

```rust
pub const R_386_GOT32: u32 = 3;
```

#### Constant `R_386_PLT32`

32 bit PLT address

```rust
pub const R_386_PLT32: u32 = 4;
```

#### Constant `R_386_COPY`

Copy symbol at runtime

```rust
pub const R_386_COPY: u32 = 5;
```

#### Constant `R_386_GLOB_DAT`

Create GOT entry

```rust
pub const R_386_GLOB_DAT: u32 = 6;
```

#### Constant `R_386_JMP_SLOT`

Create PLT entry

```rust
pub const R_386_JMP_SLOT: u32 = 7;
```

#### Constant `R_386_RELATIVE`

Adjust by program base

```rust
pub const R_386_RELATIVE: u32 = 8;
```

#### Constant `R_386_GOTOFF`

32 bit offset to GOT

```rust
pub const R_386_GOTOFF: u32 = 9;
```

#### Constant `R_386_GOTPC`

32 bit PC relative offset to GOT

```rust
pub const R_386_GOTPC: u32 = 10;
```

#### Constant `R_386_32PLT`

Direct 32 bit PLT address

```rust
pub const R_386_32PLT: u32 = 11;
```

#### Constant `R_386_TLS_TPOFF`

Offset in static TLS block

```rust
pub const R_386_TLS_TPOFF: u32 = 14;
```

#### Constant `R_386_TLS_IE`

Address of GOT entry for static TLS block offset

```rust
pub const R_386_TLS_IE: u32 = 15;
```

#### Constant `R_386_TLS_GOTIE`

GOT entry for static TLS block offset

```rust
pub const R_386_TLS_GOTIE: u32 = 16;
```

#### Constant `R_386_TLS_LE`

Offset relative to static TLS block

```rust
pub const R_386_TLS_LE: u32 = 17;
```

#### Constant `R_386_TLS_GD`

Direct 32 bit for GNU version of general dynamic thread local data

```rust
pub const R_386_TLS_GD: u32 = 18;
```

#### Constant `R_386_TLS_LDM`

Direct 32 bit for GNU version of local dynamic thread local data in LE code

```rust
pub const R_386_TLS_LDM: u32 = 19;
```

#### Constant `R_386_16`

Direct 16 bit

```rust
pub const R_386_16: u32 = 20;
```

#### Constant `R_386_PC16`

PC relative 16 bit

```rust
pub const R_386_PC16: u32 = 21;
```

#### Constant `R_386_8`

Direct 8 bit

```rust
pub const R_386_8: u32 = 22;
```

#### Constant `R_386_PC8`

PC relative 8 bit

```rust
pub const R_386_PC8: u32 = 23;
```

#### Constant `R_386_TLS_GD_32`

Direct 32 bit for general dynamic thread local data

```rust
pub const R_386_TLS_GD_32: u32 = 24;
```

#### Constant `R_386_TLS_GD_PUSH`

Tag for pushl in GD TLS code

```rust
pub const R_386_TLS_GD_PUSH: u32 = 25;
```

#### Constant `R_386_TLS_GD_CALL`

Relocation for call to __tls_get_addr()

```rust
pub const R_386_TLS_GD_CALL: u32 = 26;
```

#### Constant `R_386_TLS_GD_POP`

Tag for popl in GD TLS code

```rust
pub const R_386_TLS_GD_POP: u32 = 27;
```

#### Constant `R_386_TLS_LDM_32`

Direct 32 bit for local dynamic thread local data in LE code

```rust
pub const R_386_TLS_LDM_32: u32 = 28;
```

#### Constant `R_386_TLS_LDM_PUSH`

Tag for pushl in LDM TLS code

```rust
pub const R_386_TLS_LDM_PUSH: u32 = 29;
```

#### Constant `R_386_TLS_LDM_CALL`

Relocation for call to __tls_get_addr() in LDM code

```rust
pub const R_386_TLS_LDM_CALL: u32 = 30;
```

#### Constant `R_386_TLS_LDM_POP`

Tag for popl in LDM TLS code

```rust
pub const R_386_TLS_LDM_POP: u32 = 31;
```

#### Constant `R_386_TLS_LDO_32`

Offset relative to TLS block

```rust
pub const R_386_TLS_LDO_32: u32 = 32;
```

#### Constant `R_386_TLS_IE_32`

GOT entry for negated static TLS block offset

```rust
pub const R_386_TLS_IE_32: u32 = 33;
```

#### Constant `R_386_TLS_LE_32`

Negated offset relative to static TLS block

```rust
pub const R_386_TLS_LE_32: u32 = 34;
```

#### Constant `R_386_TLS_DTPMOD32`

ID of module containing symbol

```rust
pub const R_386_TLS_DTPMOD32: u32 = 35;
```

#### Constant `R_386_TLS_DTPOFF32`

Offset in TLS block

```rust
pub const R_386_TLS_DTPOFF32: u32 = 36;
```

#### Constant `R_386_TLS_TPOFF32`

Negated offset in static TLS block

```rust
pub const R_386_TLS_TPOFF32: u32 = 37;
```

#### Constant `R_386_SIZE32`

32-bit symbol size

```rust
pub const R_386_SIZE32: u32 = 38;
```

#### Constant `R_386_TLS_GOTDESC`

GOT offset for TLS descriptor.

```rust
pub const R_386_TLS_GOTDESC: u32 = 39;
```

#### Constant `R_386_TLS_DESC_CALL`

Marker of call through TLS descriptor for relaxation.

```rust
pub const R_386_TLS_DESC_CALL: u32 = 40;
```

#### Constant `R_386_TLS_DESC`

TLS descriptor containing pointer to code and to argument, returning the TLS offset for the symbol.

```rust
pub const R_386_TLS_DESC: u32 = 41;
```

#### Constant `R_386_IRELATIVE`

Adjust indirectly by program base

```rust
pub const R_386_IRELATIVE: u32 = 42;
```

#### Constant `R_386_GOT32X`

Load from 32 bit GOT entry, relaxable.

```rust
pub const R_386_GOT32X: u32 = 43;
```

#### Constant `R_SHARC_ADDR24_V3`

24-bit absolute address in bits 23:0 of a 48-bit instr

Targets:

* Type 25a (PC_DIRECT)

```rust
pub const R_SHARC_ADDR24_V3: u32 = 0x0b;
```

#### Constant `R_SHARC_ADDR32_V3`

32-bit absolute address in bits 31:0 of a 48-bit instr

Targets:

* Type 14a
* Type 14d
* Type 15a
* Type 16a
* Type 17a
* Type 18a
* Type 19a

```rust
pub const R_SHARC_ADDR32_V3: u32 = 0x0c;
```

#### Constant `R_SHARC_ADDR_VAR_V3`

32-bit absolute address in bits 31:0 of a 32-bit data location

Represented with `RelocationEncoding::Generic`

```rust
pub const R_SHARC_ADDR_VAR_V3: u32 = 0x0d;
```

#### Constant `R_SHARC_PCRSHORT_V3`

6-bit PC-relative address in bits 32:27 of a 48-bit instr

Targets:

* Type 9a
* Type 10a

```rust
pub const R_SHARC_PCRSHORT_V3: u32 = 0x0e;
```

#### Constant `R_SHARC_PCRLONG_V3`

24-bit PC-relative address in bits 23:0 of a 48-bit instr

Targets:

* Type 8a
* Type 12a (truncated to 23 bits after relocation)
* Type 13a (truncated to 23 bits after relocation)
* Type 25a (PC Relative)

```rust
pub const R_SHARC_PCRLONG_V3: u32 = 0x0f;
```

#### Constant `R_SHARC_DATA6_V3`

6-bit absolute address in bits 32:27 of a 48-bit instr

Targets:

* Type 4a
* Type 4b
* Type 4d

```rust
pub const R_SHARC_DATA6_V3: u32 = 0x10;
```

#### Constant `R_SHARC_DATA16_V3`

16-bit absolute address in bits 39:24 of a 48-bit instr

Targets:

* Type 12a

```rust
pub const R_SHARC_DATA16_V3: u32 = 0x11;
```

#### Constant `R_SHARC_DATA6_VISA_V3`

6-bit absolute address into bits 16:11 of a 32-bit instr

Targets:

* Type 4b

```rust
pub const R_SHARC_DATA6_VISA_V3: u32 = 0x12;
```

#### Constant `R_SHARC_DATA7_VISA_V3`

7-bit absolute address into bits 6:0 of a 32-bit instr

```rust
pub const R_SHARC_DATA7_VISA_V3: u32 = 0x13;
```

#### Constant `R_SHARC_DATA16_VISA_V3`

16-bit absolute address into bits 15:0 of a 32-bit instr

```rust
pub const R_SHARC_DATA16_VISA_V3: u32 = 0x14;
```

#### Constant `R_SHARC_PCR6_VISA_V3`

6-bit PC-relative address into bits 16:11 of a Type B

Targets:

* Type 9b

```rust
pub const R_SHARC_PCR6_VISA_V3: u32 = 0x17;
```

#### Constant `R_SHARC_ADDR_VAR16_V3`

16-bit absolute address into bits 15:0 of a 16-bit location.

Represented with `RelocationEncoding::Generic`

```rust
pub const R_SHARC_ADDR_VAR16_V3: u32 = 0x19;
```

#### Constant `R_SHARC_CALC_PUSH_ADDR`

```rust
pub const R_SHARC_CALC_PUSH_ADDR: u32 = 0xe0;
```

#### Constant `R_SHARC_CALC_PUSH_ADDEND`

```rust
pub const R_SHARC_CALC_PUSH_ADDEND: u32 = 0xe1;
```

#### Constant `R_SHARC_CALC_ADD`

```rust
pub const R_SHARC_CALC_ADD: u32 = 0xe2;
```

#### Constant `R_SHARC_CALC_SUB`

```rust
pub const R_SHARC_CALC_SUB: u32 = 0xe3;
```

#### Constant `R_SHARC_CALC_MUL`

```rust
pub const R_SHARC_CALC_MUL: u32 = 0xe4;
```

#### Constant `R_SHARC_CALC_DIV`

```rust
pub const R_SHARC_CALC_DIV: u32 = 0xe5;
```

#### Constant `R_SHARC_CALC_MOD`

```rust
pub const R_SHARC_CALC_MOD: u32 = 0xe6;
```

#### Constant `R_SHARC_CALC_LSHIFT`

```rust
pub const R_SHARC_CALC_LSHIFT: u32 = 0xe7;
```

#### Constant `R_SHARC_CALC_RSHIFT`

```rust
pub const R_SHARC_CALC_RSHIFT: u32 = 0xe8;
```

#### Constant `R_SHARC_CALC_AND`

```rust
pub const R_SHARC_CALC_AND: u32 = 0xe9;
```

#### Constant `R_SHARC_CALC_OR`

```rust
pub const R_SHARC_CALC_OR: u32 = 0xea;
```

#### Constant `R_SHARC_CALC_XOR`

```rust
pub const R_SHARC_CALC_XOR: u32 = 0xeb;
```

#### Constant `R_SHARC_CALC_PUSH_LEN`

```rust
pub const R_SHARC_CALC_PUSH_LEN: u32 = 0xec;
```

#### Constant `R_SHARC_CALC_NOT`

```rust
pub const R_SHARC_CALC_NOT: u32 = 0xf6;
```

#### Constant `SHT_SHARC_ADI_ATTRIBUTES`

.adi.attributes

```rust
pub const SHT_SHARC_ADI_ATTRIBUTES: u32 = _;
```

#### Constant `STT_SPARC_REGISTER`

Global register reserved to app.

```rust
pub const STT_SPARC_REGISTER: u8 = 13;
```

#### Constant `EF_SPARCV9_MM`

```rust
pub const EF_SPARCV9_MM: u32 = 3;
```

#### Constant `EF_SPARCV9_TSO`

```rust
pub const EF_SPARCV9_TSO: u32 = 0;
```

#### Constant `EF_SPARCV9_PSO`

```rust
pub const EF_SPARCV9_PSO: u32 = 1;
```

#### Constant `EF_SPARCV9_RMO`

```rust
pub const EF_SPARCV9_RMO: u32 = 2;
```

#### Constant `EF_SPARC_LEDATA`

little endian data

```rust
pub const EF_SPARC_LEDATA: u32 = 0x80_0000;
```

#### Constant `EF_SPARC_EXT_MASK`

```rust
pub const EF_SPARC_EXT_MASK: u32 = 0xFF_FF00;
```

#### Constant `EF_SPARC_32PLUS`

generic V8+ features

```rust
pub const EF_SPARC_32PLUS: u32 = 0x00_0100;
```

#### Constant `EF_SPARC_SUN_US1`

Sun UltraSPARC1 extensions

```rust
pub const EF_SPARC_SUN_US1: u32 = 0x00_0200;
```

#### Constant `EF_SPARC_HAL_R1`

HAL R1 extensions

```rust
pub const EF_SPARC_HAL_R1: u32 = 0x00_0400;
```

#### Constant `EF_SPARC_SUN_US3`

Sun UltraSPARCIII extensions

```rust
pub const EF_SPARC_SUN_US3: u32 = 0x00_0800;
```

#### Constant `R_SPARC_NONE`

No reloc

```rust
pub const R_SPARC_NONE: u32 = 0;
```

#### Constant `R_SPARC_8`

Direct 8 bit

```rust
pub const R_SPARC_8: u32 = 1;
```

#### Constant `R_SPARC_16`

Direct 16 bit

```rust
pub const R_SPARC_16: u32 = 2;
```

#### Constant `R_SPARC_32`

Direct 32 bit

```rust
pub const R_SPARC_32: u32 = 3;
```

#### Constant `R_SPARC_DISP8`

PC relative 8 bit

```rust
pub const R_SPARC_DISP8: u32 = 4;
```

#### Constant `R_SPARC_DISP16`

PC relative 16 bit

```rust
pub const R_SPARC_DISP16: u32 = 5;
```

#### Constant `R_SPARC_DISP32`

PC relative 32 bit

```rust
pub const R_SPARC_DISP32: u32 = 6;
```

#### Constant `R_SPARC_WDISP30`

PC relative 30 bit shifted

```rust
pub const R_SPARC_WDISP30: u32 = 7;
```

#### Constant `R_SPARC_WDISP22`

PC relative 22 bit shifted

```rust
pub const R_SPARC_WDISP22: u32 = 8;
```

#### Constant `R_SPARC_HI22`

High 22 bit

```rust
pub const R_SPARC_HI22: u32 = 9;
```

#### Constant `R_SPARC_22`

Direct 22 bit

```rust
pub const R_SPARC_22: u32 = 10;
```

#### Constant `R_SPARC_13`

Direct 13 bit

```rust
pub const R_SPARC_13: u32 = 11;
```

#### Constant `R_SPARC_LO10`

Truncated 10 bit

```rust
pub const R_SPARC_LO10: u32 = 12;
```

#### Constant `R_SPARC_GOT10`

Truncated 10 bit GOT entry

```rust
pub const R_SPARC_GOT10: u32 = 13;
```

#### Constant `R_SPARC_GOT13`

13 bit GOT entry

```rust
pub const R_SPARC_GOT13: u32 = 14;
```

#### Constant `R_SPARC_GOT22`

22 bit GOT entry shifted

```rust
pub const R_SPARC_GOT22: u32 = 15;
```

#### Constant `R_SPARC_PC10`

PC relative 10 bit truncated

```rust
pub const R_SPARC_PC10: u32 = 16;
```

#### Constant `R_SPARC_PC22`

PC relative 22 bit shifted

```rust
pub const R_SPARC_PC22: u32 = 17;
```

#### Constant `R_SPARC_WPLT30`

30 bit PC relative PLT address

```rust
pub const R_SPARC_WPLT30: u32 = 18;
```

#### Constant `R_SPARC_COPY`

Copy symbol at runtime

```rust
pub const R_SPARC_COPY: u32 = 19;
```

#### Constant `R_SPARC_GLOB_DAT`

Create GOT entry

```rust
pub const R_SPARC_GLOB_DAT: u32 = 20;
```

#### Constant `R_SPARC_JMP_SLOT`

Create PLT entry

```rust
pub const R_SPARC_JMP_SLOT: u32 = 21;
```

#### Constant `R_SPARC_RELATIVE`

Adjust by program base

```rust
pub const R_SPARC_RELATIVE: u32 = 22;
```

#### Constant `R_SPARC_UA32`

Direct 32 bit unaligned

```rust
pub const R_SPARC_UA32: u32 = 23;
```

#### Constant `R_SPARC_PLT32`

Direct 32 bit ref to PLT entry

```rust
pub const R_SPARC_PLT32: u32 = 24;
```

#### Constant `R_SPARC_HIPLT22`

High 22 bit PLT entry

```rust
pub const R_SPARC_HIPLT22: u32 = 25;
```

#### Constant `R_SPARC_LOPLT10`

Truncated 10 bit PLT entry

```rust
pub const R_SPARC_LOPLT10: u32 = 26;
```

#### Constant `R_SPARC_PCPLT32`

PC rel 32 bit ref to PLT entry

```rust
pub const R_SPARC_PCPLT32: u32 = 27;
```

#### Constant `R_SPARC_PCPLT22`

PC rel high 22 bit PLT entry

```rust
pub const R_SPARC_PCPLT22: u32 = 28;
```

#### Constant `R_SPARC_PCPLT10`

PC rel trunc 10 bit PLT entry

```rust
pub const R_SPARC_PCPLT10: u32 = 29;
```

#### Constant `R_SPARC_10`

Direct 10 bit

```rust
pub const R_SPARC_10: u32 = 30;
```

#### Constant `R_SPARC_11`

Direct 11 bit

```rust
pub const R_SPARC_11: u32 = 31;
```

#### Constant `R_SPARC_64`

Direct 64 bit

```rust
pub const R_SPARC_64: u32 = 32;
```

#### Constant `R_SPARC_OLO10`

10bit with secondary 13bit addend

```rust
pub const R_SPARC_OLO10: u32 = 33;
```

#### Constant `R_SPARC_HH22`

Top 22 bits of direct 64 bit

```rust
pub const R_SPARC_HH22: u32 = 34;
```

#### Constant `R_SPARC_HM10`

High middle 10 bits of ...

```rust
pub const R_SPARC_HM10: u32 = 35;
```

#### Constant `R_SPARC_LM22`

Low middle 22 bits of ...

```rust
pub const R_SPARC_LM22: u32 = 36;
```

#### Constant `R_SPARC_PC_HH22`

Top 22 bits of pc rel 64 bit

```rust
pub const R_SPARC_PC_HH22: u32 = 37;
```

#### Constant `R_SPARC_PC_HM10`

High middle 10 bit of ...

```rust
pub const R_SPARC_PC_HM10: u32 = 38;
```

#### Constant `R_SPARC_PC_LM22`

Low miggle 22 bits of ...

```rust
pub const R_SPARC_PC_LM22: u32 = 39;
```

#### Constant `R_SPARC_WDISP16`

PC relative 16 bit shifted

```rust
pub const R_SPARC_WDISP16: u32 = 40;
```

#### Constant `R_SPARC_WDISP19`

PC relative 19 bit shifted

```rust
pub const R_SPARC_WDISP19: u32 = 41;
```

#### Constant `R_SPARC_GLOB_JMP`

was part of v9 ABI but was removed

```rust
pub const R_SPARC_GLOB_JMP: u32 = 42;
```

#### Constant `R_SPARC_7`

Direct 7 bit

```rust
pub const R_SPARC_7: u32 = 43;
```

#### Constant `R_SPARC_5`

Direct 5 bit

```rust
pub const R_SPARC_5: u32 = 44;
```

#### Constant `R_SPARC_6`

Direct 6 bit

```rust
pub const R_SPARC_6: u32 = 45;
```

#### Constant `R_SPARC_DISP64`

PC relative 64 bit

```rust
pub const R_SPARC_DISP64: u32 = 46;
```

#### Constant `R_SPARC_PLT64`

Direct 64 bit ref to PLT entry

```rust
pub const R_SPARC_PLT64: u32 = 47;
```

#### Constant `R_SPARC_HIX22`

High 22 bit complemented

```rust
pub const R_SPARC_HIX22: u32 = 48;
```

#### Constant `R_SPARC_LOX10`

Truncated 11 bit complemented

```rust
pub const R_SPARC_LOX10: u32 = 49;
```

#### Constant `R_SPARC_H44`

Direct high 12 of 44 bit

```rust
pub const R_SPARC_H44: u32 = 50;
```

#### Constant `R_SPARC_M44`

Direct mid 22 of 44 bit

```rust
pub const R_SPARC_M44: u32 = 51;
```

#### Constant `R_SPARC_L44`

Direct low 10 of 44 bit

```rust
pub const R_SPARC_L44: u32 = 52;
```

#### Constant `R_SPARC_REGISTER`

Global register usage

```rust
pub const R_SPARC_REGISTER: u32 = 53;
```

#### Constant `R_SPARC_UA64`

Direct 64 bit unaligned

```rust
pub const R_SPARC_UA64: u32 = 54;
```

#### Constant `R_SPARC_UA16`

Direct 16 bit unaligned

```rust
pub const R_SPARC_UA16: u32 = 55;
```

#### Constant `R_SPARC_TLS_GD_HI22`

```rust
pub const R_SPARC_TLS_GD_HI22: u32 = 56;
```

#### Constant `R_SPARC_TLS_GD_LO10`

```rust
pub const R_SPARC_TLS_GD_LO10: u32 = 57;
```

#### Constant `R_SPARC_TLS_GD_ADD`

```rust
pub const R_SPARC_TLS_GD_ADD: u32 = 58;
```

#### Constant `R_SPARC_TLS_GD_CALL`

```rust
pub const R_SPARC_TLS_GD_CALL: u32 = 59;
```

#### Constant `R_SPARC_TLS_LDM_HI22`

```rust
pub const R_SPARC_TLS_LDM_HI22: u32 = 60;
```

#### Constant `R_SPARC_TLS_LDM_LO10`

```rust
pub const R_SPARC_TLS_LDM_LO10: u32 = 61;
```

#### Constant `R_SPARC_TLS_LDM_ADD`

```rust
pub const R_SPARC_TLS_LDM_ADD: u32 = 62;
```

#### Constant `R_SPARC_TLS_LDM_CALL`

```rust
pub const R_SPARC_TLS_LDM_CALL: u32 = 63;
```

#### Constant `R_SPARC_TLS_LDO_HIX22`

```rust
pub const R_SPARC_TLS_LDO_HIX22: u32 = 64;
```

#### Constant `R_SPARC_TLS_LDO_LOX10`

```rust
pub const R_SPARC_TLS_LDO_LOX10: u32 = 65;
```

#### Constant `R_SPARC_TLS_LDO_ADD`

```rust
pub const R_SPARC_TLS_LDO_ADD: u32 = 66;
```

#### Constant `R_SPARC_TLS_IE_HI22`

```rust
pub const R_SPARC_TLS_IE_HI22: u32 = 67;
```

#### Constant `R_SPARC_TLS_IE_LO10`

```rust
pub const R_SPARC_TLS_IE_LO10: u32 = 68;
```

#### Constant `R_SPARC_TLS_IE_LD`

```rust
pub const R_SPARC_TLS_IE_LD: u32 = 69;
```

#### Constant `R_SPARC_TLS_IE_LDX`

```rust
pub const R_SPARC_TLS_IE_LDX: u32 = 70;
```

#### Constant `R_SPARC_TLS_IE_ADD`

```rust
pub const R_SPARC_TLS_IE_ADD: u32 = 71;
```

#### Constant `R_SPARC_TLS_LE_HIX22`

```rust
pub const R_SPARC_TLS_LE_HIX22: u32 = 72;
```

#### Constant `R_SPARC_TLS_LE_LOX10`

```rust
pub const R_SPARC_TLS_LE_LOX10: u32 = 73;
```

#### Constant `R_SPARC_TLS_DTPMOD32`

```rust
pub const R_SPARC_TLS_DTPMOD32: u32 = 74;
```

#### Constant `R_SPARC_TLS_DTPMOD64`

```rust
pub const R_SPARC_TLS_DTPMOD64: u32 = 75;
```

#### Constant `R_SPARC_TLS_DTPOFF32`

```rust
pub const R_SPARC_TLS_DTPOFF32: u32 = 76;
```

#### Constant `R_SPARC_TLS_DTPOFF64`

```rust
pub const R_SPARC_TLS_DTPOFF64: u32 = 77;
```

#### Constant `R_SPARC_TLS_TPOFF32`

```rust
pub const R_SPARC_TLS_TPOFF32: u32 = 78;
```

#### Constant `R_SPARC_TLS_TPOFF64`

```rust
pub const R_SPARC_TLS_TPOFF64: u32 = 79;
```

#### Constant `R_SPARC_GOTDATA_HIX22`

```rust
pub const R_SPARC_GOTDATA_HIX22: u32 = 80;
```

#### Constant `R_SPARC_GOTDATA_LOX10`

```rust
pub const R_SPARC_GOTDATA_LOX10: u32 = 81;
```

#### Constant `R_SPARC_GOTDATA_OP_HIX22`

```rust
pub const R_SPARC_GOTDATA_OP_HIX22: u32 = 82;
```

#### Constant `R_SPARC_GOTDATA_OP_LOX10`

```rust
pub const R_SPARC_GOTDATA_OP_LOX10: u32 = 83;
```

#### Constant `R_SPARC_GOTDATA_OP`

```rust
pub const R_SPARC_GOTDATA_OP: u32 = 84;
```

#### Constant `R_SPARC_H34`

```rust
pub const R_SPARC_H34: u32 = 85;
```

#### Constant `R_SPARC_SIZE32`

```rust
pub const R_SPARC_SIZE32: u32 = 86;
```

#### Constant `R_SPARC_SIZE64`

```rust
pub const R_SPARC_SIZE64: u32 = 87;
```

#### Constant `R_SPARC_WDISP10`

```rust
pub const R_SPARC_WDISP10: u32 = 88;
```

#### Constant `R_SPARC_JMP_IREL`

```rust
pub const R_SPARC_JMP_IREL: u32 = 248;
```

#### Constant `R_SPARC_IRELATIVE`

```rust
pub const R_SPARC_IRELATIVE: u32 = 249;
```

#### Constant `R_SPARC_GNU_VTINHERIT`

```rust
pub const R_SPARC_GNU_VTINHERIT: u32 = 250;
```

#### Constant `R_SPARC_GNU_VTENTRY`

```rust
pub const R_SPARC_GNU_VTENTRY: u32 = 251;
```

#### Constant `R_SPARC_REV32`

```rust
pub const R_SPARC_REV32: u32 = 252;
```

#### Constant `DT_SPARC_REGISTER`

```rust
pub const DT_SPARC_REGISTER: u32 = 0x7000_0001;
```

#### Constant `EF_MIPS_NOREORDER`

A .noreorder directive was used.

```rust
pub const EF_MIPS_NOREORDER: u32 = 1;
```

#### Constant `EF_MIPS_PIC`

Contains PIC code.

```rust
pub const EF_MIPS_PIC: u32 = 2;
```

#### Constant `EF_MIPS_CPIC`

Uses PIC calling sequence.

```rust
pub const EF_MIPS_CPIC: u32 = 4;
```

#### Constant `EF_MIPS_XGOT`

```rust
pub const EF_MIPS_XGOT: u32 = 8;
```

#### Constant `EF_MIPS_64BIT_WHIRL`

```rust
pub const EF_MIPS_64BIT_WHIRL: u32 = 16;
```

#### Constant `EF_MIPS_ABI2`

```rust
pub const EF_MIPS_ABI2: u32 = 32;
```

#### Constant `EF_MIPS_ABI_ON32`

```rust
pub const EF_MIPS_ABI_ON32: u32 = 64;
```

#### Constant `EF_MIPS_FP64`

Uses FP64 (12 callee-saved).

```rust
pub const EF_MIPS_FP64: u32 = 512;
```

#### Constant `EF_MIPS_NAN2008`

Uses IEEE 754-2008 NaN encoding.

```rust
pub const EF_MIPS_NAN2008: u32 = 1024;
```

#### Constant `EF_MIPS_ARCH`

MIPS architecture level.

```rust
pub const EF_MIPS_ARCH: u32 = 0xf000_0000;
```

#### Constant `EF_MIPS_ABI_O32`

The first MIPS 32 bit ABI

```rust
pub const EF_MIPS_ABI_O32: u32 = 0x0000_1000;
```

#### Constant `EF_MIPS_ABI_O64`

O32 ABI extended for 64-bit architectures

```rust
pub const EF_MIPS_ABI_O64: u32 = 0x0000_2000;
```

#### Constant `EF_MIPS_ABI_EABI32`

EABI in 32-bit mode

```rust
pub const EF_MIPS_ABI_EABI32: u32 = 0x0000_3000;
```

#### Constant `EF_MIPS_ABI_EABI64`

EABI in 64-bit mode

```rust
pub const EF_MIPS_ABI_EABI64: u32 = 0x0000_4000;
```

#### Constant `EF_MIPS_ABI`

Mask for selecting EF_MIPS_ABI_ variant

```rust
pub const EF_MIPS_ABI: u32 = 0x0000_f000;
```

#### Constant `EF_MIPS_ARCH_1`

-mips1 code.

```rust
pub const EF_MIPS_ARCH_1: u32 = 0x0000_0000;
```

#### Constant `EF_MIPS_ARCH_2`

-mips2 code.

```rust
pub const EF_MIPS_ARCH_2: u32 = 0x1000_0000;
```

#### Constant `EF_MIPS_ARCH_3`

-mips3 code.

```rust
pub const EF_MIPS_ARCH_3: u32 = 0x2000_0000;
```

#### Constant `EF_MIPS_ARCH_4`

-mips4 code.

```rust
pub const EF_MIPS_ARCH_4: u32 = 0x3000_0000;
```

#### Constant `EF_MIPS_ARCH_5`

-mips5 code.

```rust
pub const EF_MIPS_ARCH_5: u32 = 0x4000_0000;
```

#### Constant `EF_MIPS_ARCH_32`

MIPS32 code.

```rust
pub const EF_MIPS_ARCH_32: u32 = 0x5000_0000;
```

#### Constant `EF_MIPS_ARCH_64`

MIPS64 code.

```rust
pub const EF_MIPS_ARCH_64: u32 = 0x6000_0000;
```

#### Constant `EF_MIPS_ARCH_32R2`

MIPS32r2 code.

```rust
pub const EF_MIPS_ARCH_32R2: u32 = 0x7000_0000;
```

#### Constant `EF_MIPS_ARCH_64R2`

MIPS64r2 code.

```rust
pub const EF_MIPS_ARCH_64R2: u32 = 0x8000_0000;
```

#### Constant `EF_MIPS_ARCH_32R6`

MIPS32r6 code

```rust
pub const EF_MIPS_ARCH_32R6: u32 = 0x9000_0000;
```

#### Constant `EF_MIPS_ARCH_64R6`

MIPS64r6 code

```rust
pub const EF_MIPS_ARCH_64R6: u32 = 0xa000_0000;
```

#### Constant `SHN_MIPS_ACOMMON`

Allocated common symbols.

```rust
pub const SHN_MIPS_ACOMMON: u16 = 0xff00;
```

#### Constant `SHN_MIPS_TEXT`

Allocated test symbols.

```rust
pub const SHN_MIPS_TEXT: u16 = 0xff01;
```

#### Constant `SHN_MIPS_DATA`

Allocated data symbols.

```rust
pub const SHN_MIPS_DATA: u16 = 0xff02;
```

#### Constant `SHN_MIPS_SCOMMON`

Small common symbols.

```rust
pub const SHN_MIPS_SCOMMON: u16 = 0xff03;
```

#### Constant `SHN_MIPS_SUNDEFINED`

Small undefined symbols.

```rust
pub const SHN_MIPS_SUNDEFINED: u16 = 0xff04;
```

#### Constant `SHT_MIPS_LIBLIST`

Shared objects used in link.

```rust
pub const SHT_MIPS_LIBLIST: u32 = 0x7000_0000;
```

#### Constant `SHT_MIPS_MSYM`

```rust
pub const SHT_MIPS_MSYM: u32 = 0x7000_0001;
```

#### Constant `SHT_MIPS_CONFLICT`

Conflicting symbols.

```rust
pub const SHT_MIPS_CONFLICT: u32 = 0x7000_0002;
```

#### Constant `SHT_MIPS_GPTAB`

Global data area sizes.

```rust
pub const SHT_MIPS_GPTAB: u32 = 0x7000_0003;
```

#### Constant `SHT_MIPS_UCODE`

Reserved for SGI/MIPS compilers

```rust
pub const SHT_MIPS_UCODE: u32 = 0x7000_0004;
```

#### Constant `SHT_MIPS_DEBUG`

MIPS ECOFF debugging info.

```rust
pub const SHT_MIPS_DEBUG: u32 = 0x7000_0005;
```

#### Constant `SHT_MIPS_REGINFO`

Register usage information.

```rust
pub const SHT_MIPS_REGINFO: u32 = 0x7000_0006;
```

#### Constant `SHT_MIPS_PACKAGE`

```rust
pub const SHT_MIPS_PACKAGE: u32 = 0x7000_0007;
```

#### Constant `SHT_MIPS_PACKSYM`

```rust
pub const SHT_MIPS_PACKSYM: u32 = 0x7000_0008;
```

#### Constant `SHT_MIPS_RELD`

```rust
pub const SHT_MIPS_RELD: u32 = 0x7000_0009;
```

#### Constant `SHT_MIPS_IFACE`

```rust
pub const SHT_MIPS_IFACE: u32 = 0x7000_000b;
```

#### Constant `SHT_MIPS_CONTENT`

```rust
pub const SHT_MIPS_CONTENT: u32 = 0x7000_000c;
```

#### Constant `SHT_MIPS_OPTIONS`

Miscellaneous options.

```rust
pub const SHT_MIPS_OPTIONS: u32 = 0x7000_000d;
```

#### Constant `SHT_MIPS_SHDR`

```rust
pub const SHT_MIPS_SHDR: u32 = 0x7000_0010;
```

#### Constant `SHT_MIPS_FDESC`

```rust
pub const SHT_MIPS_FDESC: u32 = 0x7000_0011;
```

#### Constant `SHT_MIPS_EXTSYM`

```rust
pub const SHT_MIPS_EXTSYM: u32 = 0x7000_0012;
```

#### Constant `SHT_MIPS_DENSE`

```rust
pub const SHT_MIPS_DENSE: u32 = 0x7000_0013;
```

#### Constant `SHT_MIPS_PDESC`

```rust
pub const SHT_MIPS_PDESC: u32 = 0x7000_0014;
```

#### Constant `SHT_MIPS_LOCSYM`

```rust
pub const SHT_MIPS_LOCSYM: u32 = 0x7000_0015;
```

#### Constant `SHT_MIPS_AUXSYM`

```rust
pub const SHT_MIPS_AUXSYM: u32 = 0x7000_0016;
```

#### Constant `SHT_MIPS_OPTSYM`

```rust
pub const SHT_MIPS_OPTSYM: u32 = 0x7000_0017;
```

#### Constant `SHT_MIPS_LOCSTR`

```rust
pub const SHT_MIPS_LOCSTR: u32 = 0x7000_0018;
```

#### Constant `SHT_MIPS_LINE`

```rust
pub const SHT_MIPS_LINE: u32 = 0x7000_0019;
```

#### Constant `SHT_MIPS_RFDESC`

```rust
pub const SHT_MIPS_RFDESC: u32 = 0x7000_001a;
```

#### Constant `SHT_MIPS_DELTASYM`

```rust
pub const SHT_MIPS_DELTASYM: u32 = 0x7000_001b;
```

#### Constant `SHT_MIPS_DELTAINST`

```rust
pub const SHT_MIPS_DELTAINST: u32 = 0x7000_001c;
```

#### Constant `SHT_MIPS_DELTACLASS`

```rust
pub const SHT_MIPS_DELTACLASS: u32 = 0x7000_001d;
```

#### Constant `SHT_MIPS_DWARF`

DWARF debugging information.

```rust
pub const SHT_MIPS_DWARF: u32 = 0x7000_001e;
```

#### Constant `SHT_MIPS_DELTADECL`

```rust
pub const SHT_MIPS_DELTADECL: u32 = 0x7000_001f;
```

#### Constant `SHT_MIPS_SYMBOL_LIB`

```rust
pub const SHT_MIPS_SYMBOL_LIB: u32 = 0x7000_0020;
```

#### Constant `SHT_MIPS_EVENTS`

Event section.

```rust
pub const SHT_MIPS_EVENTS: u32 = 0x7000_0021;
```

#### Constant `SHT_MIPS_TRANSLATE`

```rust
pub const SHT_MIPS_TRANSLATE: u32 = 0x7000_0022;
```

#### Constant `SHT_MIPS_PIXIE`

```rust
pub const SHT_MIPS_PIXIE: u32 = 0x7000_0023;
```

#### Constant `SHT_MIPS_XLATE`

```rust
pub const SHT_MIPS_XLATE: u32 = 0x7000_0024;
```

#### Constant `SHT_MIPS_XLATE_DEBUG`

```rust
pub const SHT_MIPS_XLATE_DEBUG: u32 = 0x7000_0025;
```

#### Constant `SHT_MIPS_WHIRL`

```rust
pub const SHT_MIPS_WHIRL: u32 = 0x7000_0026;
```

#### Constant `SHT_MIPS_EH_REGION`

```rust
pub const SHT_MIPS_EH_REGION: u32 = 0x7000_0027;
```

#### Constant `SHT_MIPS_XLATE_OLD`

```rust
pub const SHT_MIPS_XLATE_OLD: u32 = 0x7000_0028;
```

#### Constant `SHT_MIPS_PDR_EXCEPTION`

```rust
pub const SHT_MIPS_PDR_EXCEPTION: u32 = 0x7000_0029;
```

#### Constant `SHF_MIPS_GPREL`

Must be in global data area.

```rust
pub const SHF_MIPS_GPREL: u32 = 0x1000_0000;
```

#### Constant `SHF_MIPS_MERGE`

```rust
pub const SHF_MIPS_MERGE: u32 = 0x2000_0000;
```

#### Constant `SHF_MIPS_ADDR`

```rust
pub const SHF_MIPS_ADDR: u32 = 0x4000_0000;
```

#### Constant `SHF_MIPS_STRINGS`

```rust
pub const SHF_MIPS_STRINGS: u32 = 0x8000_0000;
```

#### Constant `SHF_MIPS_NOSTRIP`

```rust
pub const SHF_MIPS_NOSTRIP: u32 = 0x0800_0000;
```

#### Constant `SHF_MIPS_LOCAL`

```rust
pub const SHF_MIPS_LOCAL: u32 = 0x0400_0000;
```

#### Constant `SHF_MIPS_NAMES`

```rust
pub const SHF_MIPS_NAMES: u32 = 0x0200_0000;
```

#### Constant `SHF_MIPS_NODUPE`

```rust
pub const SHF_MIPS_NODUPE: u32 = 0x0100_0000;
```

#### Constant `STO_MIPS_PLT`

```rust
pub const STO_MIPS_PLT: u8 = 0x8;
```

#### Constant `STO_MIPS_SC_ALIGN_UNUSED`

Only valid for `STB_MIPS_SPLIT_COMMON`.

```rust
pub const STO_MIPS_SC_ALIGN_UNUSED: u8 = 0xff;
```

#### Constant `STB_MIPS_SPLIT_COMMON`

```rust
pub const STB_MIPS_SPLIT_COMMON: u8 = 13;
```

#### Constant `ODK_NULL`

Undefined.

```rust
pub const ODK_NULL: u32 = 0;
```

#### Constant `ODK_REGINFO`

Register usage information.

```rust
pub const ODK_REGINFO: u32 = 1;
```

#### Constant `ODK_EXCEPTIONS`

Exception processing options.

```rust
pub const ODK_EXCEPTIONS: u32 = 2;
```

#### Constant `ODK_PAD`

Section padding options.

```rust
pub const ODK_PAD: u32 = 3;
```

#### Constant `ODK_HWPATCH`

Hardware workarounds performed

```rust
pub const ODK_HWPATCH: u32 = 4;
```

#### Constant `ODK_FILL`

record the fill value used by the linker.

```rust
pub const ODK_FILL: u32 = 5;
```

#### Constant `ODK_TAGS`

reserve space for desktop tools to write.

```rust
pub const ODK_TAGS: u32 = 6;
```

#### Constant `ODK_HWAND`

HW workarounds.  'AND' bits when merging.

```rust
pub const ODK_HWAND: u32 = 7;
```

#### Constant `ODK_HWOR`

HW workarounds.  'OR' bits when merging.

```rust
pub const ODK_HWOR: u32 = 8;
```

#### Constant `OEX_FPU_MIN`

FPE's which MUST be enabled.

```rust
pub const OEX_FPU_MIN: u32 = 0x1f;
```

#### Constant `OEX_FPU_MAX`

FPE's which MAY be enabled.

```rust
pub const OEX_FPU_MAX: u32 = 0x1f00;
```

#### Constant `OEX_PAGE0`

page zero must be mapped.

```rust
pub const OEX_PAGE0: u32 = 0x10000;
```

#### Constant `OEX_SMM`

Force sequential memory mode?

```rust
pub const OEX_SMM: u32 = 0x20000;
```

#### Constant `OEX_FPDBUG`

Force floating point debug mode?

```rust
pub const OEX_FPDBUG: u32 = 0x40000;
```

#### Constant `OEX_PRECISEFP`

```rust
pub const OEX_PRECISEFP: u32 = OEX_FPDBUG;
```

#### Constant `OEX_DISMISS`

Dismiss invalid address faults?

```rust
pub const OEX_DISMISS: u32 = 0x80000;
```

#### Constant `OEX_FPU_INVAL`

```rust
pub const OEX_FPU_INVAL: u32 = 0x10;
```

#### Constant `OEX_FPU_DIV0`

```rust
pub const OEX_FPU_DIV0: u32 = 0x08;
```

#### Constant `OEX_FPU_OFLO`

```rust
pub const OEX_FPU_OFLO: u32 = 0x04;
```

#### Constant `OEX_FPU_UFLO`

```rust
pub const OEX_FPU_UFLO: u32 = 0x02;
```

#### Constant `OEX_FPU_INEX`

```rust
pub const OEX_FPU_INEX: u32 = 0x01;
```

#### Constant `OHW_R4KEOP`

R4000 end-of-page patch.

```rust
pub const OHW_R4KEOP: u32 = 0x1;
```

#### Constant `OHW_R8KPFETCH`

may need R8000 prefetch patch.

```rust
pub const OHW_R8KPFETCH: u32 = 0x2;
```

#### Constant `OHW_R5KEOP`

R5000 end-of-page patch.

```rust
pub const OHW_R5KEOP: u32 = 0x4;
```

#### Constant `OHW_R5KCVTL`

R5000 cvt.\[ds\].l bug.  clean=1.

```rust
pub const OHW_R5KCVTL: u32 = 0x8;
```

#### Constant `OPAD_PREFIX`

```rust
pub const OPAD_PREFIX: u32 = 0x1;
```

#### Constant `OPAD_POSTFIX`

```rust
pub const OPAD_POSTFIX: u32 = 0x2;
```

#### Constant `OPAD_SYMBOL`

```rust
pub const OPAD_SYMBOL: u32 = 0x4;
```

#### Constant `OHWA0_R4KEOP_CHECKED`

```rust
pub const OHWA0_R4KEOP_CHECKED: u32 = 0x0000_0001;
```

#### Constant `OHWA1_R4KEOP_CLEAN`

```rust
pub const OHWA1_R4KEOP_CLEAN: u32 = 0x0000_0002;
```

#### Constant `R_MIPS_NONE`

No reloc

```rust
pub const R_MIPS_NONE: u32 = 0;
```

#### Constant `R_MIPS_16`

Direct 16 bit

```rust
pub const R_MIPS_16: u32 = 1;
```

#### Constant `R_MIPS_32`

Direct 32 bit

```rust
pub const R_MIPS_32: u32 = 2;
```

#### Constant `R_MIPS_REL32`

PC relative 32 bit

```rust
pub const R_MIPS_REL32: u32 = 3;
```

#### Constant `R_MIPS_26`

Direct 26 bit shifted

```rust
pub const R_MIPS_26: u32 = 4;
```

#### Constant `R_MIPS_HI16`

High 16 bit

```rust
pub const R_MIPS_HI16: u32 = 5;
```

#### Constant `R_MIPS_LO16`

Low 16 bit

```rust
pub const R_MIPS_LO16: u32 = 6;
```

#### Constant `R_MIPS_GPREL16`

GP relative 16 bit

```rust
pub const R_MIPS_GPREL16: u32 = 7;
```

#### Constant `R_MIPS_LITERAL`

16 bit literal entry

```rust
pub const R_MIPS_LITERAL: u32 = 8;
```

#### Constant `R_MIPS_GOT16`

16 bit GOT entry

```rust
pub const R_MIPS_GOT16: u32 = 9;
```

#### Constant `R_MIPS_PC16`

PC relative 16 bit

```rust
pub const R_MIPS_PC16: u32 = 10;
```

#### Constant `R_MIPS_CALL16`

16 bit GOT entry for function

```rust
pub const R_MIPS_CALL16: u32 = 11;
```

#### Constant `R_MIPS_GPREL32`

GP relative 32 bit

```rust
pub const R_MIPS_GPREL32: u32 = 12;
```

#### Constant `R_MIPS_SHIFT5`

```rust
pub const R_MIPS_SHIFT5: u32 = 16;
```

#### Constant `R_MIPS_SHIFT6`

```rust
pub const R_MIPS_SHIFT6: u32 = 17;
```

#### Constant `R_MIPS_64`

```rust
pub const R_MIPS_64: u32 = 18;
```

#### Constant `R_MIPS_GOT_DISP`

```rust
pub const R_MIPS_GOT_DISP: u32 = 19;
```

#### Constant `R_MIPS_GOT_PAGE`

```rust
pub const R_MIPS_GOT_PAGE: u32 = 20;
```

#### Constant `R_MIPS_GOT_OFST`

```rust
pub const R_MIPS_GOT_OFST: u32 = 21;
```

#### Constant `R_MIPS_GOT_HI16`

```rust
pub const R_MIPS_GOT_HI16: u32 = 22;
```

#### Constant `R_MIPS_GOT_LO16`

```rust
pub const R_MIPS_GOT_LO16: u32 = 23;
```

#### Constant `R_MIPS_SUB`

```rust
pub const R_MIPS_SUB: u32 = 24;
```

#### Constant `R_MIPS_INSERT_A`

```rust
pub const R_MIPS_INSERT_A: u32 = 25;
```

#### Constant `R_MIPS_INSERT_B`

```rust
pub const R_MIPS_INSERT_B: u32 = 26;
```

#### Constant `R_MIPS_DELETE`

```rust
pub const R_MIPS_DELETE: u32 = 27;
```

#### Constant `R_MIPS_HIGHER`

```rust
pub const R_MIPS_HIGHER: u32 = 28;
```

#### Constant `R_MIPS_HIGHEST`

```rust
pub const R_MIPS_HIGHEST: u32 = 29;
```

#### Constant `R_MIPS_CALL_HI16`

```rust
pub const R_MIPS_CALL_HI16: u32 = 30;
```

#### Constant `R_MIPS_CALL_LO16`

```rust
pub const R_MIPS_CALL_LO16: u32 = 31;
```

#### Constant `R_MIPS_SCN_DISP`

```rust
pub const R_MIPS_SCN_DISP: u32 = 32;
```

#### Constant `R_MIPS_REL16`

```rust
pub const R_MIPS_REL16: u32 = 33;
```

#### Constant `R_MIPS_ADD_IMMEDIATE`

```rust
pub const R_MIPS_ADD_IMMEDIATE: u32 = 34;
```

#### Constant `R_MIPS_PJUMP`

```rust
pub const R_MIPS_PJUMP: u32 = 35;
```

#### Constant `R_MIPS_RELGOT`

```rust
pub const R_MIPS_RELGOT: u32 = 36;
```

#### Constant `R_MIPS_JALR`

```rust
pub const R_MIPS_JALR: u32 = 37;
```

#### Constant `R_MIPS_TLS_DTPMOD32`

Module number 32 bit

```rust
pub const R_MIPS_TLS_DTPMOD32: u32 = 38;
```

#### Constant `R_MIPS_TLS_DTPREL32`

Module-relative offset 32 bit

```rust
pub const R_MIPS_TLS_DTPREL32: u32 = 39;
```

#### Constant `R_MIPS_TLS_DTPMOD64`

Module number 64 bit

```rust
pub const R_MIPS_TLS_DTPMOD64: u32 = 40;
```

#### Constant `R_MIPS_TLS_DTPREL64`

Module-relative offset 64 bit

```rust
pub const R_MIPS_TLS_DTPREL64: u32 = 41;
```

#### Constant `R_MIPS_TLS_GD`

16 bit GOT offset for GD

```rust
pub const R_MIPS_TLS_GD: u32 = 42;
```

#### Constant `R_MIPS_TLS_LDM`

16 bit GOT offset for LDM

```rust
pub const R_MIPS_TLS_LDM: u32 = 43;
```

#### Constant `R_MIPS_TLS_DTPREL_HI16`

Module-relative offset, high 16 bits

```rust
pub const R_MIPS_TLS_DTPREL_HI16: u32 = 44;
```

#### Constant `R_MIPS_TLS_DTPREL_LO16`

Module-relative offset, low 16 bits

```rust
pub const R_MIPS_TLS_DTPREL_LO16: u32 = 45;
```

#### Constant `R_MIPS_TLS_GOTTPREL`

16 bit GOT offset for IE

```rust
pub const R_MIPS_TLS_GOTTPREL: u32 = 46;
```

#### Constant `R_MIPS_TLS_TPREL32`

TP-relative offset, 32 bit

```rust
pub const R_MIPS_TLS_TPREL32: u32 = 47;
```

#### Constant `R_MIPS_TLS_TPREL64`

TP-relative offset, 64 bit

```rust
pub const R_MIPS_TLS_TPREL64: u32 = 48;
```

#### Constant `R_MIPS_TLS_TPREL_HI16`

TP-relative offset, high 16 bits

```rust
pub const R_MIPS_TLS_TPREL_HI16: u32 = 49;
```

#### Constant `R_MIPS_TLS_TPREL_LO16`

TP-relative offset, low 16 bits

```rust
pub const R_MIPS_TLS_TPREL_LO16: u32 = 50;
```

#### Constant `R_MIPS_GLOB_DAT`

```rust
pub const R_MIPS_GLOB_DAT: u32 = 51;
```

#### Constant `R_MIPS_COPY`

```rust
pub const R_MIPS_COPY: u32 = 126;
```

#### Constant `R_MIPS_JUMP_SLOT`

```rust
pub const R_MIPS_JUMP_SLOT: u32 = 127;
```

#### Constant `PT_MIPS_REGINFO`

Register usage information.

```rust
pub const PT_MIPS_REGINFO: u32 = 0x7000_0000;
```

#### Constant `PT_MIPS_RTPROC`

Runtime procedure table.

```rust
pub const PT_MIPS_RTPROC: u32 = 0x7000_0001;
```

#### Constant `PT_MIPS_OPTIONS`

```rust
pub const PT_MIPS_OPTIONS: u32 = 0x7000_0002;
```

#### Constant `PT_MIPS_ABIFLAGS`

FP mode requirement.

```rust
pub const PT_MIPS_ABIFLAGS: u32 = 0x7000_0003;
```

#### Constant `PF_MIPS_LOCAL`

```rust
pub const PF_MIPS_LOCAL: u32 = 0x1000_0000;
```

#### Constant `DT_MIPS_RLD_VERSION`

Runtime linker interface version

```rust
pub const DT_MIPS_RLD_VERSION: u32 = 0x7000_0001;
```

#### Constant `DT_MIPS_TIME_STAMP`

Timestamp

```rust
pub const DT_MIPS_TIME_STAMP: u32 = 0x7000_0002;
```

#### Constant `DT_MIPS_ICHECKSUM`

Checksum

```rust
pub const DT_MIPS_ICHECKSUM: u32 = 0x7000_0003;
```

#### Constant `DT_MIPS_IVERSION`

Version string (string tbl index)

```rust
pub const DT_MIPS_IVERSION: u32 = 0x7000_0004;
```

#### Constant `DT_MIPS_FLAGS`

Flags

```rust
pub const DT_MIPS_FLAGS: u32 = 0x7000_0005;
```

#### Constant `DT_MIPS_BASE_ADDRESS`

Base address

```rust
pub const DT_MIPS_BASE_ADDRESS: u32 = 0x7000_0006;
```

#### Constant `DT_MIPS_MSYM`

```rust
pub const DT_MIPS_MSYM: u32 = 0x7000_0007;
```

#### Constant `DT_MIPS_CONFLICT`

Address of CONFLICT section

```rust
pub const DT_MIPS_CONFLICT: u32 = 0x7000_0008;
```

#### Constant `DT_MIPS_LIBLIST`

Address of LIBLIST section

```rust
pub const DT_MIPS_LIBLIST: u32 = 0x7000_0009;
```

#### Constant `DT_MIPS_LOCAL_GOTNO`

Number of local GOT entries

```rust
pub const DT_MIPS_LOCAL_GOTNO: u32 = 0x7000_000a;
```

#### Constant `DT_MIPS_CONFLICTNO`

Number of CONFLICT entries

```rust
pub const DT_MIPS_CONFLICTNO: u32 = 0x7000_000b;
```

#### Constant `DT_MIPS_LIBLISTNO`

Number of LIBLIST entries

```rust
pub const DT_MIPS_LIBLISTNO: u32 = 0x7000_0010;
```

#### Constant `DT_MIPS_SYMTABNO`

Number of DYNSYM entries

```rust
pub const DT_MIPS_SYMTABNO: u32 = 0x7000_0011;
```

#### Constant `DT_MIPS_UNREFEXTNO`

First external DYNSYM

```rust
pub const DT_MIPS_UNREFEXTNO: u32 = 0x7000_0012;
```

#### Constant `DT_MIPS_GOTSYM`

First GOT entry in DYNSYM

```rust
pub const DT_MIPS_GOTSYM: u32 = 0x7000_0013;
```

#### Constant `DT_MIPS_HIPAGENO`

Number of GOT page table entries

```rust
pub const DT_MIPS_HIPAGENO: u32 = 0x7000_0014;
```

#### Constant `DT_MIPS_RLD_MAP`

Address of run time loader map.

```rust
pub const DT_MIPS_RLD_MAP: u32 = 0x7000_0016;
```

#### Constant `DT_MIPS_DELTA_CLASS`

Delta C++ class definition.

```rust
pub const DT_MIPS_DELTA_CLASS: u32 = 0x7000_0017;
```

#### Constant `DT_MIPS_DELTA_CLASS_NO`

Number of entries in DT_MIPS_DELTA_CLASS.

```rust
pub const DT_MIPS_DELTA_CLASS_NO: u32 = 0x7000_0018;
```

#### Constant `DT_MIPS_DELTA_INSTANCE`

Delta C++ class instances.

```rust
pub const DT_MIPS_DELTA_INSTANCE: u32 = 0x7000_0019;
```

#### Constant `DT_MIPS_DELTA_INSTANCE_NO`

Number of entries in DT_MIPS_DELTA_INSTANCE.

```rust
pub const DT_MIPS_DELTA_INSTANCE_NO: u32 = 0x7000_001a;
```

#### Constant `DT_MIPS_DELTA_RELOC`

Delta relocations.

```rust
pub const DT_MIPS_DELTA_RELOC: u32 = 0x7000_001b;
```

#### Constant `DT_MIPS_DELTA_RELOC_NO`

Number of entries in DT_MIPS_DELTA_RELOC.

```rust
pub const DT_MIPS_DELTA_RELOC_NO: u32 = 0x7000_001c;
```

#### Constant `DT_MIPS_DELTA_SYM`

Delta symbols that Delta relocations refer to.

```rust
pub const DT_MIPS_DELTA_SYM: u32 = 0x7000_001d;
```

#### Constant `DT_MIPS_DELTA_SYM_NO`

Number of entries in DT_MIPS_DELTA_SYM.

```rust
pub const DT_MIPS_DELTA_SYM_NO: u32 = 0x7000_001e;
```

#### Constant `DT_MIPS_DELTA_CLASSSYM`

Delta symbols that hold the class declaration.

```rust
pub const DT_MIPS_DELTA_CLASSSYM: u32 = 0x7000_0020;
```

#### Constant `DT_MIPS_DELTA_CLASSSYM_NO`

Number of entries in DT_MIPS_DELTA_CLASSSYM.

```rust
pub const DT_MIPS_DELTA_CLASSSYM_NO: u32 = 0x7000_0021;
```

#### Constant `DT_MIPS_CXX_FLAGS`

Flags indicating for C++ flavor.

```rust
pub const DT_MIPS_CXX_FLAGS: u32 = 0x7000_0022;
```

#### Constant `DT_MIPS_PIXIE_INIT`

```rust
pub const DT_MIPS_PIXIE_INIT: u32 = 0x7000_0023;
```

#### Constant `DT_MIPS_SYMBOL_LIB`

```rust
pub const DT_MIPS_SYMBOL_LIB: u32 = 0x7000_0024;
```

#### Constant `DT_MIPS_LOCALPAGE_GOTIDX`

```rust
pub const DT_MIPS_LOCALPAGE_GOTIDX: u32 = 0x7000_0025;
```

#### Constant `DT_MIPS_LOCAL_GOTIDX`

```rust
pub const DT_MIPS_LOCAL_GOTIDX: u32 = 0x7000_0026;
```

#### Constant `DT_MIPS_HIDDEN_GOTIDX`

```rust
pub const DT_MIPS_HIDDEN_GOTIDX: u32 = 0x7000_0027;
```

#### Constant `DT_MIPS_PROTECTED_GOTIDX`

```rust
pub const DT_MIPS_PROTECTED_GOTIDX: u32 = 0x7000_0028;
```

#### Constant `DT_MIPS_OPTIONS`

Address of .options.

```rust
pub const DT_MIPS_OPTIONS: u32 = 0x7000_0029;
```

#### Constant `DT_MIPS_INTERFACE`

Address of .interface.

```rust
pub const DT_MIPS_INTERFACE: u32 = 0x7000_002a;
```

#### Constant `DT_MIPS_DYNSTR_ALIGN`

```rust
pub const DT_MIPS_DYNSTR_ALIGN: u32 = 0x7000_002b;
```

#### Constant `DT_MIPS_INTERFACE_SIZE`

Size of the .interface section.

```rust
pub const DT_MIPS_INTERFACE_SIZE: u32 = 0x7000_002c;
```

#### Constant `DT_MIPS_RLD_TEXT_RESOLVE_ADDR`

Address of rld_text_rsolve function stored in GOT.

```rust
pub const DT_MIPS_RLD_TEXT_RESOLVE_ADDR: u32 = 0x7000_002d;
```

#### Constant `DT_MIPS_PERF_SUFFIX`

Default suffix of dso to be added by rld on dlopen() calls.

```rust
pub const DT_MIPS_PERF_SUFFIX: u32 = 0x7000_002e;
```

#### Constant `DT_MIPS_COMPACT_SIZE`

(O32)Size of compact rel section.

```rust
pub const DT_MIPS_COMPACT_SIZE: u32 = 0x7000_002f;
```

#### Constant `DT_MIPS_GP_VALUE`

GP value for aux GOTs.

```rust
pub const DT_MIPS_GP_VALUE: u32 = 0x7000_0030;
```

#### Constant `DT_MIPS_AUX_DYNAMIC`

Address of aux .dynamic.

```rust
pub const DT_MIPS_AUX_DYNAMIC: u32 = 0x7000_0031;
```

#### Constant `DT_MIPS_PLTGOT`

The address of .got.plt in an executable using the new non-PIC ABI.

```rust
pub const DT_MIPS_PLTGOT: u32 = 0x7000_0032;
```

#### Constant `DT_MIPS_RWPLT`

The base of the PLT in an executable using the new non-PIC ABI if that PLT is writable.  For a non-writable PLT, this is omitted or has a zero value.

```rust
pub const DT_MIPS_RWPLT: u32 = 0x7000_0034;
```

#### Constant `DT_MIPS_RLD_MAP_REL`

An alternative description of the classic MIPS RLD_MAP that is usable in a PIE as it stores a relative offset from the address of the tag rather than an absolute address.

```rust
pub const DT_MIPS_RLD_MAP_REL: u32 = 0x7000_0035;
```

#### Constant `RHF_NONE`

No flags

```rust
pub const RHF_NONE: u32 = 0;
```

#### Constant `RHF_QUICKSTART`

Use quickstart

```rust
pub const RHF_QUICKSTART: u32 = _;
```

#### Constant `RHF_NOTPOT`

Hash size not power of 2

```rust
pub const RHF_NOTPOT: u32 = _;
```

#### Constant `RHF_NO_LIBRARY_REPLACEMENT`

Ignore LD_LIBRARY_PATH

```rust
pub const RHF_NO_LIBRARY_REPLACEMENT: u32 = _;
```

#### Constant `RHF_NO_MOVE`

```rust
pub const RHF_NO_MOVE: u32 = _;
```

#### Constant `RHF_SGI_ONLY`

```rust
pub const RHF_SGI_ONLY: u32 = _;
```

#### Constant `RHF_GUARANTEE_INIT`

```rust
pub const RHF_GUARANTEE_INIT: u32 = _;
```

#### Constant `RHF_DELTA_C_PLUS_PLUS`

```rust
pub const RHF_DELTA_C_PLUS_PLUS: u32 = _;
```

#### Constant `RHF_GUARANTEE_START_INIT`

```rust
pub const RHF_GUARANTEE_START_INIT: u32 = _;
```

#### Constant `RHF_PIXIE`

```rust
pub const RHF_PIXIE: u32 = _;
```

#### Constant `RHF_DEFAULT_DELAY_LOAD`

```rust
pub const RHF_DEFAULT_DELAY_LOAD: u32 = _;
```

#### Constant `RHF_REQUICKSTART`

```rust
pub const RHF_REQUICKSTART: u32 = _;
```

#### Constant `RHF_REQUICKSTARTED`

```rust
pub const RHF_REQUICKSTARTED: u32 = _;
```

#### Constant `RHF_CORD`

```rust
pub const RHF_CORD: u32 = _;
```

#### Constant `RHF_NO_UNRES_UNDEF`

```rust
pub const RHF_NO_UNRES_UNDEF: u32 = _;
```

#### Constant `RHF_RLD_ORDER_SAFE`

```rust
pub const RHF_RLD_ORDER_SAFE: u32 = _;
```

#### Constant `LL_NONE`

```rust
pub const LL_NONE: u32 = 0;
```

#### Constant `LL_EXACT_MATCH`

Require exact match

```rust
pub const LL_EXACT_MATCH: u32 = _;
```

#### Constant `LL_IGNORE_INT_VER`

Ignore interface version

```rust
pub const LL_IGNORE_INT_VER: u32 = _;
```

#### Constant `LL_REQUIRE_MINOR`

```rust
pub const LL_REQUIRE_MINOR: u32 = _;
```

#### Constant `LL_EXPORTS`

```rust
pub const LL_EXPORTS: u32 = _;
```

#### Constant `LL_DELAY_LOAD`

```rust
pub const LL_DELAY_LOAD: u32 = _;
```

#### Constant `LL_DELTA`

```rust
pub const LL_DELTA: u32 = _;
```

#### Constant `EF_PARISC_TRAPNIL`

Trap nil pointer dereference.

```rust
pub const EF_PARISC_TRAPNIL: u32 = 0x0001_0000;
```

#### Constant `EF_PARISC_EXT`

Program uses arch. extensions.

```rust
pub const EF_PARISC_EXT: u32 = 0x0002_0000;
```

#### Constant `EF_PARISC_LSB`

Program expects little endian.

```rust
pub const EF_PARISC_LSB: u32 = 0x0004_0000;
```

#### Constant `EF_PARISC_WIDE`

Program expects wide mode.

```rust
pub const EF_PARISC_WIDE: u32 = 0x0008_0000;
```

#### Constant `EF_PARISC_NO_KABP`

No kernel assisted branch prediction.

```rust
pub const EF_PARISC_NO_KABP: u32 = 0x0010_0000;
```

#### Constant `EF_PARISC_LAZYSWAP`

Allow lazy swapping.

```rust
pub const EF_PARISC_LAZYSWAP: u32 = 0x0040_0000;
```

#### Constant `EF_PARISC_ARCH`

Architecture version.

```rust
pub const EF_PARISC_ARCH: u32 = 0x0000_ffff;
```

#### Constant `EFA_PARISC_1_0`

PA-RISC 1.0 big-endian.

```rust
pub const EFA_PARISC_1_0: u32 = 0x020b;
```

#### Constant `EFA_PARISC_1_1`

PA-RISC 1.1 big-endian.

```rust
pub const EFA_PARISC_1_1: u32 = 0x0210;
```

#### Constant `EFA_PARISC_2_0`

PA-RISC 2.0 big-endian.

```rust
pub const EFA_PARISC_2_0: u32 = 0x0214;
```

#### Constant `SHN_PARISC_ANSI_COMMON`

Section for tentatively declared symbols in ANSI C.

```rust
pub const SHN_PARISC_ANSI_COMMON: u16 = 0xff00;
```

#### Constant `SHN_PARISC_HUGE_COMMON`

Common blocks in huge model.

```rust
pub const SHN_PARISC_HUGE_COMMON: u16 = 0xff01;
```

#### Constant `SHT_PARISC_EXT`

Contains product specific ext.

```rust
pub const SHT_PARISC_EXT: u32 = 0x7000_0000;
```

#### Constant `SHT_PARISC_UNWIND`

Unwind information.

```rust
pub const SHT_PARISC_UNWIND: u32 = 0x7000_0001;
```

#### Constant `SHT_PARISC_DOC`

Debug info for optimized code.

```rust
pub const SHT_PARISC_DOC: u32 = 0x7000_0002;
```

#### Constant `SHF_PARISC_SHORT`

Section with short addressing.

```rust
pub const SHF_PARISC_SHORT: u32 = 0x2000_0000;
```

#### Constant `SHF_PARISC_HUGE`

Section far from gp.

```rust
pub const SHF_PARISC_HUGE: u32 = 0x4000_0000;
```

#### Constant `SHF_PARISC_SBP`

Static branch prediction code.

```rust
pub const SHF_PARISC_SBP: u32 = 0x8000_0000;
```

#### Constant `STT_PARISC_MILLICODE`

Millicode function entry point.

```rust
pub const STT_PARISC_MILLICODE: u8 = 13;
```

#### Constant `STT_HP_OPAQUE`

```rust
pub const STT_HP_OPAQUE: u8 = _;
```

#### Constant `STT_HP_STUB`

```rust
pub const STT_HP_STUB: u8 = _;
```

#### Constant `R_PARISC_NONE`

No reloc.

```rust
pub const R_PARISC_NONE: u32 = 0;
```

#### Constant `R_PARISC_DIR32`

Direct 32-bit reference.

```rust
pub const R_PARISC_DIR32: u32 = 1;
```

#### Constant `R_PARISC_DIR21L`

Left 21 bits of eff. address.

```rust
pub const R_PARISC_DIR21L: u32 = 2;
```

#### Constant `R_PARISC_DIR17R`

Right 17 bits of eff. address.

```rust
pub const R_PARISC_DIR17R: u32 = 3;
```

#### Constant `R_PARISC_DIR17F`

17 bits of eff. address.

```rust
pub const R_PARISC_DIR17F: u32 = 4;
```

#### Constant `R_PARISC_DIR14R`

Right 14 bits of eff. address.

```rust
pub const R_PARISC_DIR14R: u32 = 6;
```

#### Constant `R_PARISC_PCREL32`

32-bit rel. address.

```rust
pub const R_PARISC_PCREL32: u32 = 9;
```

#### Constant `R_PARISC_PCREL21L`

Left 21 bits of rel. address.

```rust
pub const R_PARISC_PCREL21L: u32 = 10;
```

#### Constant `R_PARISC_PCREL17R`

Right 17 bits of rel. address.

```rust
pub const R_PARISC_PCREL17R: u32 = 11;
```

#### Constant `R_PARISC_PCREL17F`

17 bits of rel. address.

```rust
pub const R_PARISC_PCREL17F: u32 = 12;
```

#### Constant `R_PARISC_PCREL14R`

Right 14 bits of rel. address.

```rust
pub const R_PARISC_PCREL14R: u32 = 14;
```

#### Constant `R_PARISC_DPREL21L`

Left 21 bits of rel. address.

```rust
pub const R_PARISC_DPREL21L: u32 = 18;
```

#### Constant `R_PARISC_DPREL14R`

Right 14 bits of rel. address.

```rust
pub const R_PARISC_DPREL14R: u32 = 22;
```

#### Constant `R_PARISC_GPREL21L`

GP-relative, left 21 bits.

```rust
pub const R_PARISC_GPREL21L: u32 = 26;
```

#### Constant `R_PARISC_GPREL14R`

GP-relative, right 14 bits.

```rust
pub const R_PARISC_GPREL14R: u32 = 30;
```

#### Constant `R_PARISC_LTOFF21L`

LT-relative, left 21 bits.

```rust
pub const R_PARISC_LTOFF21L: u32 = 34;
```

#### Constant `R_PARISC_LTOFF14R`

LT-relative, right 14 bits.

```rust
pub const R_PARISC_LTOFF14R: u32 = 38;
```

#### Constant `R_PARISC_SECREL32`

32 bits section rel. address.

```rust
pub const R_PARISC_SECREL32: u32 = 41;
```

#### Constant `R_PARISC_SEGBASE`

No relocation, set segment base.

```rust
pub const R_PARISC_SEGBASE: u32 = 48;
```

#### Constant `R_PARISC_SEGREL32`

32 bits segment rel. address.

```rust
pub const R_PARISC_SEGREL32: u32 = 49;
```

#### Constant `R_PARISC_PLTOFF21L`

PLT rel. address, left 21 bits.

```rust
pub const R_PARISC_PLTOFF21L: u32 = 50;
```

#### Constant `R_PARISC_PLTOFF14R`

PLT rel. address, right 14 bits.

```rust
pub const R_PARISC_PLTOFF14R: u32 = 54;
```

#### Constant `R_PARISC_LTOFF_FPTR32`

32 bits LT-rel. function pointer.

```rust
pub const R_PARISC_LTOFF_FPTR32: u32 = 57;
```

#### Constant `R_PARISC_LTOFF_FPTR21L`

LT-rel. fct ptr, left 21 bits.

```rust
pub const R_PARISC_LTOFF_FPTR21L: u32 = 58;
```

#### Constant `R_PARISC_LTOFF_FPTR14R`

LT-rel. fct ptr, right 14 bits.

```rust
pub const R_PARISC_LTOFF_FPTR14R: u32 = 62;
```

#### Constant `R_PARISC_FPTR64`

64 bits function address.

```rust
pub const R_PARISC_FPTR64: u32 = 64;
```

#### Constant `R_PARISC_PLABEL32`

32 bits function address.

```rust
pub const R_PARISC_PLABEL32: u32 = 65;
```

#### Constant `R_PARISC_PLABEL21L`

Left 21 bits of fdesc address.

```rust
pub const R_PARISC_PLABEL21L: u32 = 66;
```

#### Constant `R_PARISC_PLABEL14R`

Right 14 bits of fdesc address.

```rust
pub const R_PARISC_PLABEL14R: u32 = 70;
```

#### Constant `R_PARISC_PCREL64`

64 bits PC-rel. address.

```rust
pub const R_PARISC_PCREL64: u32 = 72;
```

#### Constant `R_PARISC_PCREL22F`

22 bits PC-rel. address.

```rust
pub const R_PARISC_PCREL22F: u32 = 74;
```

#### Constant `R_PARISC_PCREL14WR`

PC-rel. address, right 14 bits.

```rust
pub const R_PARISC_PCREL14WR: u32 = 75;
```

#### Constant `R_PARISC_PCREL14DR`

PC rel. address, right 14 bits.

```rust
pub const R_PARISC_PCREL14DR: u32 = 76;
```

#### Constant `R_PARISC_PCREL16F`

16 bits PC-rel. address.

```rust
pub const R_PARISC_PCREL16F: u32 = 77;
```

#### Constant `R_PARISC_PCREL16WF`

16 bits PC-rel. address.

```rust
pub const R_PARISC_PCREL16WF: u32 = 78;
```

#### Constant `R_PARISC_PCREL16DF`

16 bits PC-rel. address.

```rust
pub const R_PARISC_PCREL16DF: u32 = 79;
```

#### Constant `R_PARISC_DIR64`

64 bits of eff. address.

```rust
pub const R_PARISC_DIR64: u32 = 80;
```

#### Constant `R_PARISC_DIR14WR`

14 bits of eff. address.

```rust
pub const R_PARISC_DIR14WR: u32 = 83;
```

#### Constant `R_PARISC_DIR14DR`

14 bits of eff. address.

```rust
pub const R_PARISC_DIR14DR: u32 = 84;
```

#### Constant `R_PARISC_DIR16F`

16 bits of eff. address.

```rust
pub const R_PARISC_DIR16F: u32 = 85;
```

#### Constant `R_PARISC_DIR16WF`

16 bits of eff. address.

```rust
pub const R_PARISC_DIR16WF: u32 = 86;
```

#### Constant `R_PARISC_DIR16DF`

16 bits of eff. address.

```rust
pub const R_PARISC_DIR16DF: u32 = 87;
```

#### Constant `R_PARISC_GPREL64`

64 bits of GP-rel. address.

```rust
pub const R_PARISC_GPREL64: u32 = 88;
```

#### Constant `R_PARISC_GPREL14WR`

GP-rel. address, right 14 bits.

```rust
pub const R_PARISC_GPREL14WR: u32 = 91;
```

#### Constant `R_PARISC_GPREL14DR`

GP-rel. address, right 14 bits.

```rust
pub const R_PARISC_GPREL14DR: u32 = 92;
```

#### Constant `R_PARISC_GPREL16F`

16 bits GP-rel. address.

```rust
pub const R_PARISC_GPREL16F: u32 = 93;
```

#### Constant `R_PARISC_GPREL16WF`

16 bits GP-rel. address.

```rust
pub const R_PARISC_GPREL16WF: u32 = 94;
```

#### Constant `R_PARISC_GPREL16DF`

16 bits GP-rel. address.

```rust
pub const R_PARISC_GPREL16DF: u32 = 95;
```

#### Constant `R_PARISC_LTOFF64`

64 bits LT-rel. address.

```rust
pub const R_PARISC_LTOFF64: u32 = 96;
```

#### Constant `R_PARISC_LTOFF14WR`

LT-rel. address, right 14 bits.

```rust
pub const R_PARISC_LTOFF14WR: u32 = 99;
```

#### Constant `R_PARISC_LTOFF14DR`

LT-rel. address, right 14 bits.

```rust
pub const R_PARISC_LTOFF14DR: u32 = 100;
```

#### Constant `R_PARISC_LTOFF16F`

16 bits LT-rel. address.

```rust
pub const R_PARISC_LTOFF16F: u32 = 101;
```

#### Constant `R_PARISC_LTOFF16WF`

16 bits LT-rel. address.

```rust
pub const R_PARISC_LTOFF16WF: u32 = 102;
```

#### Constant `R_PARISC_LTOFF16DF`

16 bits LT-rel. address.

```rust
pub const R_PARISC_LTOFF16DF: u32 = 103;
```

#### Constant `R_PARISC_SECREL64`

64 bits section rel. address.

```rust
pub const R_PARISC_SECREL64: u32 = 104;
```

#### Constant `R_PARISC_SEGREL64`

64 bits segment rel. address.

```rust
pub const R_PARISC_SEGREL64: u32 = 112;
```

#### Constant `R_PARISC_PLTOFF14WR`

PLT-rel. address, right 14 bits.

```rust
pub const R_PARISC_PLTOFF14WR: u32 = 115;
```

#### Constant `R_PARISC_PLTOFF14DR`

PLT-rel. address, right 14 bits.

```rust
pub const R_PARISC_PLTOFF14DR: u32 = 116;
```

#### Constant `R_PARISC_PLTOFF16F`

16 bits LT-rel. address.

```rust
pub const R_PARISC_PLTOFF16F: u32 = 117;
```

#### Constant `R_PARISC_PLTOFF16WF`

16 bits PLT-rel. address.

```rust
pub const R_PARISC_PLTOFF16WF: u32 = 118;
```

#### Constant `R_PARISC_PLTOFF16DF`

16 bits PLT-rel. address.

```rust
pub const R_PARISC_PLTOFF16DF: u32 = 119;
```

#### Constant `R_PARISC_LTOFF_FPTR64`

64 bits LT-rel. function ptr.

```rust
pub const R_PARISC_LTOFF_FPTR64: u32 = 120;
```

#### Constant `R_PARISC_LTOFF_FPTR14WR`

LT-rel. fct. ptr., right 14 bits.

```rust
pub const R_PARISC_LTOFF_FPTR14WR: u32 = 123;
```

#### Constant `R_PARISC_LTOFF_FPTR14DR`

LT-rel. fct. ptr., right 14 bits.

```rust
pub const R_PARISC_LTOFF_FPTR14DR: u32 = 124;
```

#### Constant `R_PARISC_LTOFF_FPTR16F`

16 bits LT-rel. function ptr.

```rust
pub const R_PARISC_LTOFF_FPTR16F: u32 = 125;
```

#### Constant `R_PARISC_LTOFF_FPTR16WF`

16 bits LT-rel. function ptr.

```rust
pub const R_PARISC_LTOFF_FPTR16WF: u32 = 126;
```

#### Constant `R_PARISC_LTOFF_FPTR16DF`

16 bits LT-rel. function ptr.

```rust
pub const R_PARISC_LTOFF_FPTR16DF: u32 = 127;
```

#### Constant `R_PARISC_LORESERVE`

```rust
pub const R_PARISC_LORESERVE: u32 = 128;
```

#### Constant `R_PARISC_COPY`

Copy relocation.

```rust
pub const R_PARISC_COPY: u32 = 128;
```

#### Constant `R_PARISC_IPLT`

Dynamic reloc, imported PLT

```rust
pub const R_PARISC_IPLT: u32 = 129;
```

#### Constant `R_PARISC_EPLT`

Dynamic reloc, exported PLT

```rust
pub const R_PARISC_EPLT: u32 = 130;
```

#### Constant `R_PARISC_TPREL32`

32 bits TP-rel. address.

```rust
pub const R_PARISC_TPREL32: u32 = 153;
```

#### Constant `R_PARISC_TPREL21L`

TP-rel. address, left 21 bits.

```rust
pub const R_PARISC_TPREL21L: u32 = 154;
```

#### Constant `R_PARISC_TPREL14R`

TP-rel. address, right 14 bits.

```rust
pub const R_PARISC_TPREL14R: u32 = 158;
```

#### Constant `R_PARISC_LTOFF_TP21L`

LT-TP-rel. address, left 21 bits.

```rust
pub const R_PARISC_LTOFF_TP21L: u32 = 162;
```

#### Constant `R_PARISC_LTOFF_TP14R`

LT-TP-rel. address, right 14 bits.

```rust
pub const R_PARISC_LTOFF_TP14R: u32 = 166;
```

#### Constant `R_PARISC_LTOFF_TP14F`

14 bits LT-TP-rel. address.

```rust
pub const R_PARISC_LTOFF_TP14F: u32 = 167;
```

#### Constant `R_PARISC_TPREL64`

64 bits TP-rel. address.

```rust
pub const R_PARISC_TPREL64: u32 = 216;
```

#### Constant `R_PARISC_TPREL14WR`

TP-rel. address, right 14 bits.

```rust
pub const R_PARISC_TPREL14WR: u32 = 219;
```

#### Constant `R_PARISC_TPREL14DR`

TP-rel. address, right 14 bits.

```rust
pub const R_PARISC_TPREL14DR: u32 = 220;
```

#### Constant `R_PARISC_TPREL16F`

16 bits TP-rel. address.

```rust
pub const R_PARISC_TPREL16F: u32 = 221;
```

#### Constant `R_PARISC_TPREL16WF`

16 bits TP-rel. address.

```rust
pub const R_PARISC_TPREL16WF: u32 = 222;
```

#### Constant `R_PARISC_TPREL16DF`

16 bits TP-rel. address.

```rust
pub const R_PARISC_TPREL16DF: u32 = 223;
```

#### Constant `R_PARISC_LTOFF_TP64`

64 bits LT-TP-rel. address.

```rust
pub const R_PARISC_LTOFF_TP64: u32 = 224;
```

#### Constant `R_PARISC_LTOFF_TP14WR`

LT-TP-rel. address, right 14 bits.

```rust
pub const R_PARISC_LTOFF_TP14WR: u32 = 227;
```

#### Constant `R_PARISC_LTOFF_TP14DR`

LT-TP-rel. address, right 14 bits.

```rust
pub const R_PARISC_LTOFF_TP14DR: u32 = 228;
```

#### Constant `R_PARISC_LTOFF_TP16F`

16 bits LT-TP-rel. address.

```rust
pub const R_PARISC_LTOFF_TP16F: u32 = 229;
```

#### Constant `R_PARISC_LTOFF_TP16WF`

16 bits LT-TP-rel. address.

```rust
pub const R_PARISC_LTOFF_TP16WF: u32 = 230;
```

#### Constant `R_PARISC_LTOFF_TP16DF`

16 bits LT-TP-rel. address.

```rust
pub const R_PARISC_LTOFF_TP16DF: u32 = 231;
```

#### Constant `R_PARISC_GNU_VTENTRY`

```rust
pub const R_PARISC_GNU_VTENTRY: u32 = 232;
```

#### Constant `R_PARISC_GNU_VTINHERIT`

```rust
pub const R_PARISC_GNU_VTINHERIT: u32 = 233;
```

#### Constant `R_PARISC_TLS_GD21L`

GD 21-bit left.

```rust
pub const R_PARISC_TLS_GD21L: u32 = 234;
```

#### Constant `R_PARISC_TLS_GD14R`

GD 14-bit right.

```rust
pub const R_PARISC_TLS_GD14R: u32 = 235;
```

#### Constant `R_PARISC_TLS_GDCALL`

GD call to __t_g_a.

```rust
pub const R_PARISC_TLS_GDCALL: u32 = 236;
```

#### Constant `R_PARISC_TLS_LDM21L`

LD module 21-bit left.

```rust
pub const R_PARISC_TLS_LDM21L: u32 = 237;
```

#### Constant `R_PARISC_TLS_LDM14R`

LD module 14-bit right.

```rust
pub const R_PARISC_TLS_LDM14R: u32 = 238;
```

#### Constant `R_PARISC_TLS_LDMCALL`

LD module call to __t_g_a.

```rust
pub const R_PARISC_TLS_LDMCALL: u32 = 239;
```

#### Constant `R_PARISC_TLS_LDO21L`

LD offset 21-bit left.

```rust
pub const R_PARISC_TLS_LDO21L: u32 = 240;
```

#### Constant `R_PARISC_TLS_LDO14R`

LD offset 14-bit right.

```rust
pub const R_PARISC_TLS_LDO14R: u32 = 241;
```

#### Constant `R_PARISC_TLS_DTPMOD32`

DTP module 32-bit.

```rust
pub const R_PARISC_TLS_DTPMOD32: u32 = 242;
```

#### Constant `R_PARISC_TLS_DTPMOD64`

DTP module 64-bit.

```rust
pub const R_PARISC_TLS_DTPMOD64: u32 = 243;
```

#### Constant `R_PARISC_TLS_DTPOFF32`

DTP offset 32-bit.

```rust
pub const R_PARISC_TLS_DTPOFF32: u32 = 244;
```

#### Constant `R_PARISC_TLS_DTPOFF64`

DTP offset 32-bit.

```rust
pub const R_PARISC_TLS_DTPOFF64: u32 = 245;
```

#### Constant `R_PARISC_TLS_LE21L`

```rust
pub const R_PARISC_TLS_LE21L: u32 = R_PARISC_TPREL21L;
```

#### Constant `R_PARISC_TLS_LE14R`

```rust
pub const R_PARISC_TLS_LE14R: u32 = R_PARISC_TPREL14R;
```

#### Constant `R_PARISC_TLS_IE21L`

```rust
pub const R_PARISC_TLS_IE21L: u32 = R_PARISC_LTOFF_TP21L;
```

#### Constant `R_PARISC_TLS_IE14R`

```rust
pub const R_PARISC_TLS_IE14R: u32 = R_PARISC_LTOFF_TP14R;
```

#### Constant `R_PARISC_TLS_TPREL32`

```rust
pub const R_PARISC_TLS_TPREL32: u32 = R_PARISC_TPREL32;
```

#### Constant `R_PARISC_TLS_TPREL64`

```rust
pub const R_PARISC_TLS_TPREL64: u32 = R_PARISC_TPREL64;
```

#### Constant `R_PARISC_HIRESERVE`

```rust
pub const R_PARISC_HIRESERVE: u32 = 255;
```

#### Constant `PT_HP_TLS`

```rust
pub const PT_HP_TLS: u32 = _;
```

#### Constant `PT_HP_CORE_NONE`

```rust
pub const PT_HP_CORE_NONE: u32 = _;
```

#### Constant `PT_HP_CORE_VERSION`

```rust
pub const PT_HP_CORE_VERSION: u32 = _;
```

#### Constant `PT_HP_CORE_KERNEL`

```rust
pub const PT_HP_CORE_KERNEL: u32 = _;
```

#### Constant `PT_HP_CORE_COMM`

```rust
pub const PT_HP_CORE_COMM: u32 = _;
```

#### Constant `PT_HP_CORE_PROC`

```rust
pub const PT_HP_CORE_PROC: u32 = _;
```

#### Constant `PT_HP_CORE_LOADABLE`

```rust
pub const PT_HP_CORE_LOADABLE: u32 = _;
```

#### Constant `PT_HP_CORE_STACK`

```rust
pub const PT_HP_CORE_STACK: u32 = _;
```

#### Constant `PT_HP_CORE_SHM`

```rust
pub const PT_HP_CORE_SHM: u32 = _;
```

#### Constant `PT_HP_CORE_MMF`

```rust
pub const PT_HP_CORE_MMF: u32 = _;
```

#### Constant `PT_HP_PARALLEL`

```rust
pub const PT_HP_PARALLEL: u32 = _;
```

#### Constant `PT_HP_FASTBIND`

```rust
pub const PT_HP_FASTBIND: u32 = _;
```

#### Constant `PT_HP_OPT_ANNOT`

```rust
pub const PT_HP_OPT_ANNOT: u32 = _;
```

#### Constant `PT_HP_HSL_ANNOT`

```rust
pub const PT_HP_HSL_ANNOT: u32 = _;
```

#### Constant `PT_HP_STACK`

```rust
pub const PT_HP_STACK: u32 = _;
```

#### Constant `PT_PARISC_ARCHEXT`

```rust
pub const PT_PARISC_ARCHEXT: u32 = 0x7000_0000;
```

#### Constant `PT_PARISC_UNWIND`

```rust
pub const PT_PARISC_UNWIND: u32 = 0x7000_0001;
```

#### Constant `PF_PARISC_SBP`

```rust
pub const PF_PARISC_SBP: u32 = 0x0800_0000;
```

#### Constant `PF_HP_PAGE_SIZE`

```rust
pub const PF_HP_PAGE_SIZE: u32 = 0x0010_0000;
```

#### Constant `PF_HP_FAR_SHARED`

```rust
pub const PF_HP_FAR_SHARED: u32 = 0x0020_0000;
```

#### Constant `PF_HP_NEAR_SHARED`

```rust
pub const PF_HP_NEAR_SHARED: u32 = 0x0040_0000;
```

#### Constant `PF_HP_CODE`

```rust
pub const PF_HP_CODE: u32 = 0x0100_0000;
```

#### Constant `PF_HP_MODIFY`

```rust
pub const PF_HP_MODIFY: u32 = 0x0200_0000;
```

#### Constant `PF_HP_LAZYSWAP`

```rust
pub const PF_HP_LAZYSWAP: u32 = 0x0400_0000;
```

#### Constant `PF_HP_SBP`

```rust
pub const PF_HP_SBP: u32 = 0x0800_0000;
```

#### Constant `EF_ALPHA_32BIT`

All addresses must be < 2GB.

```rust
pub const EF_ALPHA_32BIT: u32 = 1;
```

#### Constant `EF_ALPHA_CANRELAX`

Relocations for relaxing exist.

```rust
pub const EF_ALPHA_CANRELAX: u32 = 2;
```

#### Constant `SHT_ALPHA_DEBUG`

```rust
pub const SHT_ALPHA_DEBUG: u32 = 0x7000_0001;
```

#### Constant `SHT_ALPHA_REGINFO`

```rust
pub const SHT_ALPHA_REGINFO: u32 = 0x7000_0002;
```

#### Constant `SHF_ALPHA_GPREL`

```rust
pub const SHF_ALPHA_GPREL: u32 = 0x1000_0000;
```

#### Constant `STO_ALPHA_NOPV`

No PV required.

```rust
pub const STO_ALPHA_NOPV: u8 = 0x80;
```

#### Constant `STO_ALPHA_STD_GPLOAD`

PV only used for initial ldgp.

```rust
pub const STO_ALPHA_STD_GPLOAD: u8 = 0x88;
```

#### Constant `R_ALPHA_NONE`

No reloc

```rust
pub const R_ALPHA_NONE: u32 = 0;
```

#### Constant `R_ALPHA_REFLONG`

Direct 32 bit

```rust
pub const R_ALPHA_REFLONG: u32 = 1;
```

#### Constant `R_ALPHA_REFQUAD`

Direct 64 bit

```rust
pub const R_ALPHA_REFQUAD: u32 = 2;
```

#### Constant `R_ALPHA_GPREL32`

GP relative 32 bit

```rust
pub const R_ALPHA_GPREL32: u32 = 3;
```

#### Constant `R_ALPHA_LITERAL`

GP relative 16 bit w/optimization

```rust
pub const R_ALPHA_LITERAL: u32 = 4;
```

#### Constant `R_ALPHA_LITUSE`

Optimization hint for LITERAL

```rust
pub const R_ALPHA_LITUSE: u32 = 5;
```

#### Constant `R_ALPHA_GPDISP`

Add displacement to GP

```rust
pub const R_ALPHA_GPDISP: u32 = 6;
```

#### Constant `R_ALPHA_BRADDR`

PC+4 relative 23 bit shifted

```rust
pub const R_ALPHA_BRADDR: u32 = 7;
```

#### Constant `R_ALPHA_HINT`

PC+4 relative 16 bit shifted

```rust
pub const R_ALPHA_HINT: u32 = 8;
```

#### Constant `R_ALPHA_SREL16`

PC relative 16 bit

```rust
pub const R_ALPHA_SREL16: u32 = 9;
```

#### Constant `R_ALPHA_SREL32`

PC relative 32 bit

```rust
pub const R_ALPHA_SREL32: u32 = 10;
```

#### Constant `R_ALPHA_SREL64`

PC relative 64 bit

```rust
pub const R_ALPHA_SREL64: u32 = 11;
```

#### Constant `R_ALPHA_GPRELHIGH`

GP relative 32 bit, high 16 bits

```rust
pub const R_ALPHA_GPRELHIGH: u32 = 17;
```

#### Constant `R_ALPHA_GPRELLOW`

GP relative 32 bit, low 16 bits

```rust
pub const R_ALPHA_GPRELLOW: u32 = 18;
```

#### Constant `R_ALPHA_GPREL16`

GP relative 16 bit

```rust
pub const R_ALPHA_GPREL16: u32 = 19;
```

#### Constant `R_ALPHA_COPY`

Copy symbol at runtime

```rust
pub const R_ALPHA_COPY: u32 = 24;
```

#### Constant `R_ALPHA_GLOB_DAT`

Create GOT entry

```rust
pub const R_ALPHA_GLOB_DAT: u32 = 25;
```

#### Constant `R_ALPHA_JMP_SLOT`

Create PLT entry

```rust
pub const R_ALPHA_JMP_SLOT: u32 = 26;
```

#### Constant `R_ALPHA_RELATIVE`

Adjust by program base

```rust
pub const R_ALPHA_RELATIVE: u32 = 27;
```

#### Constant `R_ALPHA_TLS_GD_HI`

```rust
pub const R_ALPHA_TLS_GD_HI: u32 = 28;
```

#### Constant `R_ALPHA_TLSGD`

```rust
pub const R_ALPHA_TLSGD: u32 = 29;
```

#### Constant `R_ALPHA_TLS_LDM`

```rust
pub const R_ALPHA_TLS_LDM: u32 = 30;
```

#### Constant `R_ALPHA_DTPMOD64`

```rust
pub const R_ALPHA_DTPMOD64: u32 = 31;
```

#### Constant `R_ALPHA_GOTDTPREL`

```rust
pub const R_ALPHA_GOTDTPREL: u32 = 32;
```

#### Constant `R_ALPHA_DTPREL64`

```rust
pub const R_ALPHA_DTPREL64: u32 = 33;
```

#### Constant `R_ALPHA_DTPRELHI`

```rust
pub const R_ALPHA_DTPRELHI: u32 = 34;
```

#### Constant `R_ALPHA_DTPRELLO`

```rust
pub const R_ALPHA_DTPRELLO: u32 = 35;
```

#### Constant `R_ALPHA_DTPREL16`

```rust
pub const R_ALPHA_DTPREL16: u32 = 36;
```

#### Constant `R_ALPHA_GOTTPREL`

```rust
pub const R_ALPHA_GOTTPREL: u32 = 37;
```

#### Constant `R_ALPHA_TPREL64`

```rust
pub const R_ALPHA_TPREL64: u32 = 38;
```

#### Constant `R_ALPHA_TPRELHI`

```rust
pub const R_ALPHA_TPRELHI: u32 = 39;
```

#### Constant `R_ALPHA_TPRELLO`

```rust
pub const R_ALPHA_TPRELLO: u32 = 40;
```

#### Constant `R_ALPHA_TPREL16`

```rust
pub const R_ALPHA_TPREL16: u32 = 41;
```

#### Constant `LITUSE_ALPHA_ADDR`

```rust
pub const LITUSE_ALPHA_ADDR: u32 = 0;
```

#### Constant `LITUSE_ALPHA_BASE`

```rust
pub const LITUSE_ALPHA_BASE: u32 = 1;
```

#### Constant `LITUSE_ALPHA_BYTOFF`

```rust
pub const LITUSE_ALPHA_BYTOFF: u32 = 2;
```

#### Constant `LITUSE_ALPHA_JSR`

```rust
pub const LITUSE_ALPHA_JSR: u32 = 3;
```

#### Constant `LITUSE_ALPHA_TLS_GD`

```rust
pub const LITUSE_ALPHA_TLS_GD: u32 = 4;
```

#### Constant `LITUSE_ALPHA_TLS_LDM`

```rust
pub const LITUSE_ALPHA_TLS_LDM: u32 = 5;
```

#### Constant `DT_ALPHA_PLTRO`

```rust
pub const DT_ALPHA_PLTRO: u32 = _;
```

#### Constant `EF_PPC_EMB`

PowerPC embedded flag

```rust
pub const EF_PPC_EMB: u32 = 0x8000_0000;
```

#### Constant `EF_PPC_RELOCATABLE`

PowerPC -mrelocatable flag

```rust
pub const EF_PPC_RELOCATABLE: u32 = 0x0001_0000;
```

#### Constant `EF_PPC_RELOCATABLE_LIB`

PowerPC -mrelocatable-lib flag

```rust
pub const EF_PPC_RELOCATABLE_LIB: u32 = 0x0000_8000;
```

#### Constant `R_PPC_NONE`

```rust
pub const R_PPC_NONE: u32 = 0;
```

#### Constant `R_PPC_ADDR32`

32bit absolute address

```rust
pub const R_PPC_ADDR32: u32 = 1;
```

#### Constant `R_PPC_ADDR24`

26bit address, 2 bits ignored.

```rust
pub const R_PPC_ADDR24: u32 = 2;
```

#### Constant `R_PPC_ADDR16`

16bit absolute address

```rust
pub const R_PPC_ADDR16: u32 = 3;
```

#### Constant `R_PPC_ADDR16_LO`

lower 16bit of absolute address

```rust
pub const R_PPC_ADDR16_LO: u32 = 4;
```

#### Constant `R_PPC_ADDR16_HI`

high 16bit of absolute address

```rust
pub const R_PPC_ADDR16_HI: u32 = 5;
```

#### Constant `R_PPC_ADDR16_HA`

adjusted high 16bit

```rust
pub const R_PPC_ADDR16_HA: u32 = 6;
```

#### Constant `R_PPC_ADDR14`

16bit address, 2 bits ignored

```rust
pub const R_PPC_ADDR14: u32 = 7;
```

#### Constant `R_PPC_ADDR14_BRTAKEN`

```rust
pub const R_PPC_ADDR14_BRTAKEN: u32 = 8;
```

#### Constant `R_PPC_ADDR14_BRNTAKEN`

```rust
pub const R_PPC_ADDR14_BRNTAKEN: u32 = 9;
```

#### Constant `R_PPC_REL24`

PC relative 26 bit

```rust
pub const R_PPC_REL24: u32 = 10;
```

#### Constant `R_PPC_REL14`

PC relative 16 bit

```rust
pub const R_PPC_REL14: u32 = 11;
```

#### Constant `R_PPC_REL14_BRTAKEN`

```rust
pub const R_PPC_REL14_BRTAKEN: u32 = 12;
```

#### Constant `R_PPC_REL14_BRNTAKEN`

```rust
pub const R_PPC_REL14_BRNTAKEN: u32 = 13;
```

#### Constant `R_PPC_GOT16`

```rust
pub const R_PPC_GOT16: u32 = 14;
```

#### Constant `R_PPC_GOT16_LO`

```rust
pub const R_PPC_GOT16_LO: u32 = 15;
```

#### Constant `R_PPC_GOT16_HI`

```rust
pub const R_PPC_GOT16_HI: u32 = 16;
```

#### Constant `R_PPC_GOT16_HA`

```rust
pub const R_PPC_GOT16_HA: u32 = 17;
```

#### Constant `R_PPC_PLTREL24`

```rust
pub const R_PPC_PLTREL24: u32 = 18;
```

#### Constant `R_PPC_COPY`

```rust
pub const R_PPC_COPY: u32 = 19;
```

#### Constant `R_PPC_GLOB_DAT`

```rust
pub const R_PPC_GLOB_DAT: u32 = 20;
```

#### Constant `R_PPC_JMP_SLOT`

```rust
pub const R_PPC_JMP_SLOT: u32 = 21;
```

#### Constant `R_PPC_RELATIVE`

```rust
pub const R_PPC_RELATIVE: u32 = 22;
```

#### Constant `R_PPC_LOCAL24PC`

```rust
pub const R_PPC_LOCAL24PC: u32 = 23;
```

#### Constant `R_PPC_UADDR32`

```rust
pub const R_PPC_UADDR32: u32 = 24;
```

#### Constant `R_PPC_UADDR16`

```rust
pub const R_PPC_UADDR16: u32 = 25;
```

#### Constant `R_PPC_REL32`

```rust
pub const R_PPC_REL32: u32 = 26;
```

#### Constant `R_PPC_PLT32`

```rust
pub const R_PPC_PLT32: u32 = 27;
```

#### Constant `R_PPC_PLTREL32`

```rust
pub const R_PPC_PLTREL32: u32 = 28;
```

#### Constant `R_PPC_PLT16_LO`

```rust
pub const R_PPC_PLT16_LO: u32 = 29;
```

#### Constant `R_PPC_PLT16_HI`

```rust
pub const R_PPC_PLT16_HI: u32 = 30;
```

#### Constant `R_PPC_PLT16_HA`

```rust
pub const R_PPC_PLT16_HA: u32 = 31;
```

#### Constant `R_PPC_SDAREL16`

```rust
pub const R_PPC_SDAREL16: u32 = 32;
```

#### Constant `R_PPC_SECTOFF`

```rust
pub const R_PPC_SECTOFF: u32 = 33;
```

#### Constant `R_PPC_SECTOFF_LO`

```rust
pub const R_PPC_SECTOFF_LO: u32 = 34;
```

#### Constant `R_PPC_SECTOFF_HI`

```rust
pub const R_PPC_SECTOFF_HI: u32 = 35;
```

#### Constant `R_PPC_SECTOFF_HA`

```rust
pub const R_PPC_SECTOFF_HA: u32 = 36;
```

#### Constant `R_PPC_TLS`

none    (sym+add)@tls

```rust
pub const R_PPC_TLS: u32 = 67;
```

#### Constant `R_PPC_DTPMOD32`

word32  (sym+add)@dtpmod

```rust
pub const R_PPC_DTPMOD32: u32 = 68;
```

#### Constant `R_PPC_TPREL16`

half16* (sym+add)@tprel

```rust
pub const R_PPC_TPREL16: u32 = 69;
```

#### Constant `R_PPC_TPREL16_LO`

half16  (sym+add)@tprel@l

```rust
pub const R_PPC_TPREL16_LO: u32 = 70;
```

#### Constant `R_PPC_TPREL16_HI`

half16  (sym+add)@tprel@h

```rust
pub const R_PPC_TPREL16_HI: u32 = 71;
```

#### Constant `R_PPC_TPREL16_HA`

half16  (sym+add)@tprel@ha

```rust
pub const R_PPC_TPREL16_HA: u32 = 72;
```

#### Constant `R_PPC_TPREL32`

word32  (sym+add)@tprel

```rust
pub const R_PPC_TPREL32: u32 = 73;
```

#### Constant `R_PPC_DTPREL16`

half16*(sym+add)@dtprel

```rust
pub const R_PPC_DTPREL16: u32 = 74;
```

#### Constant `R_PPC_DTPREL16_LO`

half16  (sym+add)@dtprel@l

```rust
pub const R_PPC_DTPREL16_LO: u32 = 75;
```

#### Constant `R_PPC_DTPREL16_HI`

half16  (sym+add)@dtprel@h

```rust
pub const R_PPC_DTPREL16_HI: u32 = 76;
```

#### Constant `R_PPC_DTPREL16_HA`

half16  (sym+add)@dtprel@ha

```rust
pub const R_PPC_DTPREL16_HA: u32 = 77;
```

#### Constant `R_PPC_DTPREL32`

word32  (sym+add)@dtprel

```rust
pub const R_PPC_DTPREL32: u32 = 78;
```

#### Constant `R_PPC_GOT_TLSGD16`

half16* (sym+add)@got@tlsgd

```rust
pub const R_PPC_GOT_TLSGD16: u32 = 79;
```

#### Constant `R_PPC_GOT_TLSGD16_LO`

half16  (sym+add)@got@tlsgd@l

```rust
pub const R_PPC_GOT_TLSGD16_LO: u32 = 80;
```

#### Constant `R_PPC_GOT_TLSGD16_HI`

half16  (sym+add)@got@tlsgd@h

```rust
pub const R_PPC_GOT_TLSGD16_HI: u32 = 81;
```

#### Constant `R_PPC_GOT_TLSGD16_HA`

half16  (sym+add)@got@tlsgd@ha

```rust
pub const R_PPC_GOT_TLSGD16_HA: u32 = 82;
```

#### Constant `R_PPC_GOT_TLSLD16`

half16* (sym+add)@got@tlsld

```rust
pub const R_PPC_GOT_TLSLD16: u32 = 83;
```

#### Constant `R_PPC_GOT_TLSLD16_LO`

half16  (sym+add)@got@tlsld@l

```rust
pub const R_PPC_GOT_TLSLD16_LO: u32 = 84;
```

#### Constant `R_PPC_GOT_TLSLD16_HI`

half16  (sym+add)@got@tlsld@h

```rust
pub const R_PPC_GOT_TLSLD16_HI: u32 = 85;
```

#### Constant `R_PPC_GOT_TLSLD16_HA`

half16  (sym+add)@got@tlsld@ha

```rust
pub const R_PPC_GOT_TLSLD16_HA: u32 = 86;
```

#### Constant `R_PPC_GOT_TPREL16`

half16* (sym+add)@got@tprel

```rust
pub const R_PPC_GOT_TPREL16: u32 = 87;
```

#### Constant `R_PPC_GOT_TPREL16_LO`

half16  (sym+add)@got@tprel@l

```rust
pub const R_PPC_GOT_TPREL16_LO: u32 = 88;
```

#### Constant `R_PPC_GOT_TPREL16_HI`

half16  (sym+add)@got@tprel@h

```rust
pub const R_PPC_GOT_TPREL16_HI: u32 = 89;
```

#### Constant `R_PPC_GOT_TPREL16_HA`

half16  (sym+add)@got@tprel@ha

```rust
pub const R_PPC_GOT_TPREL16_HA: u32 = 90;
```

#### Constant `R_PPC_GOT_DTPREL16`

half16* (sym+add)@got@dtprel

```rust
pub const R_PPC_GOT_DTPREL16: u32 = 91;
```

#### Constant `R_PPC_GOT_DTPREL16_LO`

half16* (sym+add)@got@dtprel@l

```rust
pub const R_PPC_GOT_DTPREL16_LO: u32 = 92;
```

#### Constant `R_PPC_GOT_DTPREL16_HI`

half16* (sym+add)@got@dtprel@h

```rust
pub const R_PPC_GOT_DTPREL16_HI: u32 = 93;
```

#### Constant `R_PPC_GOT_DTPREL16_HA`

half16* (sym+add)@got@dtprel@ha

```rust
pub const R_PPC_GOT_DTPREL16_HA: u32 = 94;
```

#### Constant `R_PPC_TLSGD`

none    (sym+add)@tlsgd

```rust
pub const R_PPC_TLSGD: u32 = 95;
```

#### Constant `R_PPC_TLSLD`

none    (sym+add)@tlsld

```rust
pub const R_PPC_TLSLD: u32 = 96;
```

#### Constant `R_PPC_EMB_NADDR32`

```rust
pub const R_PPC_EMB_NADDR32: u32 = 101;
```

#### Constant `R_PPC_EMB_NADDR16`

```rust
pub const R_PPC_EMB_NADDR16: u32 = 102;
```

#### Constant `R_PPC_EMB_NADDR16_LO`

```rust
pub const R_PPC_EMB_NADDR16_LO: u32 = 103;
```

#### Constant `R_PPC_EMB_NADDR16_HI`

```rust
pub const R_PPC_EMB_NADDR16_HI: u32 = 104;
```

#### Constant `R_PPC_EMB_NADDR16_HA`

```rust
pub const R_PPC_EMB_NADDR16_HA: u32 = 105;
```

#### Constant `R_PPC_EMB_SDAI16`

```rust
pub const R_PPC_EMB_SDAI16: u32 = 106;
```

#### Constant `R_PPC_EMB_SDA2I16`

```rust
pub const R_PPC_EMB_SDA2I16: u32 = 107;
```

#### Constant `R_PPC_EMB_SDA2REL`

```rust
pub const R_PPC_EMB_SDA2REL: u32 = 108;
```

#### Constant `R_PPC_EMB_SDA21`

16 bit offset in SDA

```rust
pub const R_PPC_EMB_SDA21: u32 = 109;
```

#### Constant `R_PPC_EMB_MRKREF`

```rust
pub const R_PPC_EMB_MRKREF: u32 = 110;
```

#### Constant `R_PPC_EMB_RELSEC16`

```rust
pub const R_PPC_EMB_RELSEC16: u32 = 111;
```

#### Constant `R_PPC_EMB_RELST_LO`

```rust
pub const R_PPC_EMB_RELST_LO: u32 = 112;
```

#### Constant `R_PPC_EMB_RELST_HI`

```rust
pub const R_PPC_EMB_RELST_HI: u32 = 113;
```

#### Constant `R_PPC_EMB_RELST_HA`

```rust
pub const R_PPC_EMB_RELST_HA: u32 = 114;
```

#### Constant `R_PPC_EMB_BIT_FLD`

```rust
pub const R_PPC_EMB_BIT_FLD: u32 = 115;
```

#### Constant `R_PPC_EMB_RELSDA`

16 bit relative offset in SDA

```rust
pub const R_PPC_EMB_RELSDA: u32 = 116;
```

#### Constant `R_PPC_DIAB_SDA21_LO`

like EMB_SDA21, but lower 16 bit

```rust
pub const R_PPC_DIAB_SDA21_LO: u32 = 180;
```

#### Constant `R_PPC_DIAB_SDA21_HI`

like EMB_SDA21, but high 16 bit

```rust
pub const R_PPC_DIAB_SDA21_HI: u32 = 181;
```

#### Constant `R_PPC_DIAB_SDA21_HA`

like EMB_SDA21, adjusted high 16

```rust
pub const R_PPC_DIAB_SDA21_HA: u32 = 182;
```

#### Constant `R_PPC_DIAB_RELSDA_LO`

like EMB_RELSDA, but lower 16 bit

```rust
pub const R_PPC_DIAB_RELSDA_LO: u32 = 183;
```

#### Constant `R_PPC_DIAB_RELSDA_HI`

like EMB_RELSDA, but high 16 bit

```rust
pub const R_PPC_DIAB_RELSDA_HI: u32 = 184;
```

#### Constant `R_PPC_DIAB_RELSDA_HA`

like EMB_RELSDA, adjusted high 16

```rust
pub const R_PPC_DIAB_RELSDA_HA: u32 = 185;
```

#### Constant `R_PPC_IRELATIVE`

GNU extension to support local ifunc.

```rust
pub const R_PPC_IRELATIVE: u32 = 248;
```

#### Constant `R_PPC_REL16`

half16   (sym+add-.)

```rust
pub const R_PPC_REL16: u32 = 249;
```

#### Constant `R_PPC_REL16_LO`

half16   (sym+add-.)@l

```rust
pub const R_PPC_REL16_LO: u32 = 250;
```

#### Constant `R_PPC_REL16_HI`

half16   (sym+add-.)@h

```rust
pub const R_PPC_REL16_HI: u32 = 251;
```

#### Constant `R_PPC_REL16_HA`

half16   (sym+add-.)@ha

```rust
pub const R_PPC_REL16_HA: u32 = 252;
```

#### Constant `R_PPC_TOC16`

This is a phony reloc to handle any old fashioned TOC16 references that may
still be in object files.

```rust
pub const R_PPC_TOC16: u32 = 255;
```

#### Constant `DT_PPC_GOT`

```rust
pub const DT_PPC_GOT: u32 = _;
```

#### Constant `DT_PPC_OPT`

```rust
pub const DT_PPC_OPT: u32 = _;
```

#### Constant `PPC_OPT_TLS`

```rust
pub const PPC_OPT_TLS: u32 = 1;
```

#### Constant `R_PPC64_NONE`

```rust
pub const R_PPC64_NONE: u32 = R_PPC_NONE;
```

#### Constant `R_PPC64_ADDR32`

32bit absolute address

```rust
pub const R_PPC64_ADDR32: u32 = R_PPC_ADDR32;
```

#### Constant `R_PPC64_ADDR24`

26bit address, word aligned

```rust
pub const R_PPC64_ADDR24: u32 = R_PPC_ADDR24;
```

#### Constant `R_PPC64_ADDR16`

16bit absolute address

```rust
pub const R_PPC64_ADDR16: u32 = R_PPC_ADDR16;
```

#### Constant `R_PPC64_ADDR16_LO`

lower 16bits of address

```rust
pub const R_PPC64_ADDR16_LO: u32 = R_PPC_ADDR16_LO;
```

#### Constant `R_PPC64_ADDR16_HI`

high 16bits of address.

```rust
pub const R_PPC64_ADDR16_HI: u32 = R_PPC_ADDR16_HI;
```

#### Constant `R_PPC64_ADDR16_HA`

adjusted high 16bits.

```rust
pub const R_PPC64_ADDR16_HA: u32 = R_PPC_ADDR16_HA;
```

#### Constant `R_PPC64_ADDR14`

16bit address, word aligned

```rust
pub const R_PPC64_ADDR14: u32 = R_PPC_ADDR14;
```

#### Constant `R_PPC64_ADDR14_BRTAKEN`

```rust
pub const R_PPC64_ADDR14_BRTAKEN: u32 = R_PPC_ADDR14_BRTAKEN;
```

#### Constant `R_PPC64_ADDR14_BRNTAKEN`

```rust
pub const R_PPC64_ADDR14_BRNTAKEN: u32 = R_PPC_ADDR14_BRNTAKEN;
```

#### Constant `R_PPC64_REL24`

PC-rel. 26 bit, word aligned

```rust
pub const R_PPC64_REL24: u32 = R_PPC_REL24;
```

#### Constant `R_PPC64_REL14`

PC relative 16 bit

```rust
pub const R_PPC64_REL14: u32 = R_PPC_REL14;
```

#### Constant `R_PPC64_REL14_BRTAKEN`

```rust
pub const R_PPC64_REL14_BRTAKEN: u32 = R_PPC_REL14_BRTAKEN;
```

#### Constant `R_PPC64_REL14_BRNTAKEN`

```rust
pub const R_PPC64_REL14_BRNTAKEN: u32 = R_PPC_REL14_BRNTAKEN;
```

#### Constant `R_PPC64_GOT16`

```rust
pub const R_PPC64_GOT16: u32 = R_PPC_GOT16;
```

#### Constant `R_PPC64_GOT16_LO`

```rust
pub const R_PPC64_GOT16_LO: u32 = R_PPC_GOT16_LO;
```

#### Constant `R_PPC64_GOT16_HI`

```rust
pub const R_PPC64_GOT16_HI: u32 = R_PPC_GOT16_HI;
```

#### Constant `R_PPC64_GOT16_HA`

```rust
pub const R_PPC64_GOT16_HA: u32 = R_PPC_GOT16_HA;
```

#### Constant `R_PPC64_COPY`

```rust
pub const R_PPC64_COPY: u32 = R_PPC_COPY;
```

#### Constant `R_PPC64_GLOB_DAT`

```rust
pub const R_PPC64_GLOB_DAT: u32 = R_PPC_GLOB_DAT;
```

#### Constant `R_PPC64_JMP_SLOT`

```rust
pub const R_PPC64_JMP_SLOT: u32 = R_PPC_JMP_SLOT;
```

#### Constant `R_PPC64_RELATIVE`

```rust
pub const R_PPC64_RELATIVE: u32 = R_PPC_RELATIVE;
```

#### Constant `R_PPC64_UADDR32`

```rust
pub const R_PPC64_UADDR32: u32 = R_PPC_UADDR32;
```

#### Constant `R_PPC64_UADDR16`

```rust
pub const R_PPC64_UADDR16: u32 = R_PPC_UADDR16;
```

#### Constant `R_PPC64_REL32`

```rust
pub const R_PPC64_REL32: u32 = R_PPC_REL32;
```

#### Constant `R_PPC64_PLT32`

```rust
pub const R_PPC64_PLT32: u32 = R_PPC_PLT32;
```

#### Constant `R_PPC64_PLTREL32`

```rust
pub const R_PPC64_PLTREL32: u32 = R_PPC_PLTREL32;
```

#### Constant `R_PPC64_PLT16_LO`

```rust
pub const R_PPC64_PLT16_LO: u32 = R_PPC_PLT16_LO;
```

#### Constant `R_PPC64_PLT16_HI`

```rust
pub const R_PPC64_PLT16_HI: u32 = R_PPC_PLT16_HI;
```

#### Constant `R_PPC64_PLT16_HA`

```rust
pub const R_PPC64_PLT16_HA: u32 = R_PPC_PLT16_HA;
```

#### Constant `R_PPC64_SECTOFF`

```rust
pub const R_PPC64_SECTOFF: u32 = R_PPC_SECTOFF;
```

#### Constant `R_PPC64_SECTOFF_LO`

```rust
pub const R_PPC64_SECTOFF_LO: u32 = R_PPC_SECTOFF_LO;
```

#### Constant `R_PPC64_SECTOFF_HI`

```rust
pub const R_PPC64_SECTOFF_HI: u32 = R_PPC_SECTOFF_HI;
```

#### Constant `R_PPC64_SECTOFF_HA`

```rust
pub const R_PPC64_SECTOFF_HA: u32 = R_PPC_SECTOFF_HA;
```

#### Constant `R_PPC64_ADDR30`

word30 (S + A - P) >> 2

```rust
pub const R_PPC64_ADDR30: u32 = 37;
```

#### Constant `R_PPC64_ADDR64`

doubleword64 S + A

```rust
pub const R_PPC64_ADDR64: u32 = 38;
```

#### Constant `R_PPC64_ADDR16_HIGHER`

half16 #higher(S + A)

```rust
pub const R_PPC64_ADDR16_HIGHER: u32 = 39;
```

#### Constant `R_PPC64_ADDR16_HIGHERA`

half16 #highera(S + A)

```rust
pub const R_PPC64_ADDR16_HIGHERA: u32 = 40;
```

#### Constant `R_PPC64_ADDR16_HIGHEST`

half16 #highest(S + A)

```rust
pub const R_PPC64_ADDR16_HIGHEST: u32 = 41;
```

#### Constant `R_PPC64_ADDR16_HIGHESTA`

half16 #highesta(S + A)

```rust
pub const R_PPC64_ADDR16_HIGHESTA: u32 = 42;
```

#### Constant `R_PPC64_UADDR64`

doubleword64 S + A

```rust
pub const R_PPC64_UADDR64: u32 = 43;
```

#### Constant `R_PPC64_REL64`

doubleword64 S + A - P

```rust
pub const R_PPC64_REL64: u32 = 44;
```

#### Constant `R_PPC64_PLT64`

doubleword64 L + A

```rust
pub const R_PPC64_PLT64: u32 = 45;
```

#### Constant `R_PPC64_PLTREL64`

doubleword64 L + A - P

```rust
pub const R_PPC64_PLTREL64: u32 = 46;
```

#### Constant `R_PPC64_TOC16`

half16* S + A - .TOC

```rust
pub const R_PPC64_TOC16: u32 = 47;
```

#### Constant `R_PPC64_TOC16_LO`

half16 #lo(S + A - .TOC.)

```rust
pub const R_PPC64_TOC16_LO: u32 = 48;
```

#### Constant `R_PPC64_TOC16_HI`

half16 #hi(S + A - .TOC.)

```rust
pub const R_PPC64_TOC16_HI: u32 = 49;
```

#### Constant `R_PPC64_TOC16_HA`

half16 #ha(S + A - .TOC.)

```rust
pub const R_PPC64_TOC16_HA: u32 = 50;
```

#### Constant `R_PPC64_TOC`

doubleword64 .TOC

```rust
pub const R_PPC64_TOC: u32 = 51;
```

#### Constant `R_PPC64_PLTGOT16`

half16* M + A

```rust
pub const R_PPC64_PLTGOT16: u32 = 52;
```

#### Constant `R_PPC64_PLTGOT16_LO`

half16 #lo(M + A)

```rust
pub const R_PPC64_PLTGOT16_LO: u32 = 53;
```

#### Constant `R_PPC64_PLTGOT16_HI`

half16 #hi(M + A)

```rust
pub const R_PPC64_PLTGOT16_HI: u32 = 54;
```

#### Constant `R_PPC64_PLTGOT16_HA`

half16 #ha(M + A)

```rust
pub const R_PPC64_PLTGOT16_HA: u32 = 55;
```

#### Constant `R_PPC64_ADDR16_DS`

half16ds* (S + A) >> 2

```rust
pub const R_PPC64_ADDR16_DS: u32 = 56;
```

#### Constant `R_PPC64_ADDR16_LO_DS`

half16ds  #lo(S + A) >> 2

```rust
pub const R_PPC64_ADDR16_LO_DS: u32 = 57;
```

#### Constant `R_PPC64_GOT16_DS`

half16ds* (G + A) >> 2

```rust
pub const R_PPC64_GOT16_DS: u32 = 58;
```

#### Constant `R_PPC64_GOT16_LO_DS`

half16ds  #lo(G + A) >> 2

```rust
pub const R_PPC64_GOT16_LO_DS: u32 = 59;
```

#### Constant `R_PPC64_PLT16_LO_DS`

half16ds  #lo(L + A) >> 2

```rust
pub const R_PPC64_PLT16_LO_DS: u32 = 60;
```

#### Constant `R_PPC64_SECTOFF_DS`

half16ds* (R + A) >> 2

```rust
pub const R_PPC64_SECTOFF_DS: u32 = 61;
```

#### Constant `R_PPC64_SECTOFF_LO_DS`

half16ds  #lo(R + A) >> 2

```rust
pub const R_PPC64_SECTOFF_LO_DS: u32 = 62;
```

#### Constant `R_PPC64_TOC16_DS`

half16ds* (S + A - .TOC.) >> 2

```rust
pub const R_PPC64_TOC16_DS: u32 = 63;
```

#### Constant `R_PPC64_TOC16_LO_DS`

half16ds  #lo(S + A - .TOC.) >> 2

```rust
pub const R_PPC64_TOC16_LO_DS: u32 = 64;
```

#### Constant `R_PPC64_PLTGOT16_DS`

half16ds* (M + A) >> 2

```rust
pub const R_PPC64_PLTGOT16_DS: u32 = 65;
```

#### Constant `R_PPC64_PLTGOT16_LO_DS`

half16ds  #lo(M + A) >> 2

```rust
pub const R_PPC64_PLTGOT16_LO_DS: u32 = 66;
```

#### Constant `R_PPC64_TLS`

none    (sym+add)@tls

```rust
pub const R_PPC64_TLS: u32 = 67;
```

#### Constant `R_PPC64_DTPMOD64`

doubleword64 (sym+add)@dtpmod

```rust
pub const R_PPC64_DTPMOD64: u32 = 68;
```

#### Constant `R_PPC64_TPREL16`

half16* (sym+add)@tprel

```rust
pub const R_PPC64_TPREL16: u32 = 69;
```

#### Constant `R_PPC64_TPREL16_LO`

half16  (sym+add)@tprel@l

```rust
pub const R_PPC64_TPREL16_LO: u32 = 70;
```

#### Constant `R_PPC64_TPREL16_HI`

half16  (sym+add)@tprel@h

```rust
pub const R_PPC64_TPREL16_HI: u32 = 71;
```

#### Constant `R_PPC64_TPREL16_HA`

half16  (sym+add)@tprel@ha

```rust
pub const R_PPC64_TPREL16_HA: u32 = 72;
```

#### Constant `R_PPC64_TPREL64`

doubleword64 (sym+add)@tprel

```rust
pub const R_PPC64_TPREL64: u32 = 73;
```

#### Constant `R_PPC64_DTPREL16`

half16* (sym+add)@dtprel

```rust
pub const R_PPC64_DTPREL16: u32 = 74;
```

#### Constant `R_PPC64_DTPREL16_LO`

half16  (sym+add)@dtprel@l

```rust
pub const R_PPC64_DTPREL16_LO: u32 = 75;
```

#### Constant `R_PPC64_DTPREL16_HI`

half16  (sym+add)@dtprel@h

```rust
pub const R_PPC64_DTPREL16_HI: u32 = 76;
```

#### Constant `R_PPC64_DTPREL16_HA`

half16  (sym+add)@dtprel@ha

```rust
pub const R_PPC64_DTPREL16_HA: u32 = 77;
```

#### Constant `R_PPC64_DTPREL64`

doubleword64 (sym+add)@dtprel

```rust
pub const R_PPC64_DTPREL64: u32 = 78;
```

#### Constant `R_PPC64_GOT_TLSGD16`

half16* (sym+add)@got@tlsgd

```rust
pub const R_PPC64_GOT_TLSGD16: u32 = 79;
```

#### Constant `R_PPC64_GOT_TLSGD16_LO`

half16  (sym+add)@got@tlsgd@l

```rust
pub const R_PPC64_GOT_TLSGD16_LO: u32 = 80;
```

#### Constant `R_PPC64_GOT_TLSGD16_HI`

half16  (sym+add)@got@tlsgd@h

```rust
pub const R_PPC64_GOT_TLSGD16_HI: u32 = 81;
```

#### Constant `R_PPC64_GOT_TLSGD16_HA`

half16  (sym+add)@got@tlsgd@ha

```rust
pub const R_PPC64_GOT_TLSGD16_HA: u32 = 82;
```

#### Constant `R_PPC64_GOT_TLSLD16`

half16* (sym+add)@got@tlsld

```rust
pub const R_PPC64_GOT_TLSLD16: u32 = 83;
```

#### Constant `R_PPC64_GOT_TLSLD16_LO`

half16  (sym+add)@got@tlsld@l

```rust
pub const R_PPC64_GOT_TLSLD16_LO: u32 = 84;
```

#### Constant `R_PPC64_GOT_TLSLD16_HI`

half16  (sym+add)@got@tlsld@h

```rust
pub const R_PPC64_GOT_TLSLD16_HI: u32 = 85;
```

#### Constant `R_PPC64_GOT_TLSLD16_HA`

half16  (sym+add)@got@tlsld@ha

```rust
pub const R_PPC64_GOT_TLSLD16_HA: u32 = 86;
```

#### Constant `R_PPC64_GOT_TPREL16_DS`

half16ds* (sym+add)@got@tprel

```rust
pub const R_PPC64_GOT_TPREL16_DS: u32 = 87;
```

#### Constant `R_PPC64_GOT_TPREL16_LO_DS`

half16ds (sym+add)@got@tprel@l

```rust
pub const R_PPC64_GOT_TPREL16_LO_DS: u32 = 88;
```

#### Constant `R_PPC64_GOT_TPREL16_HI`

half16  (sym+add)@got@tprel@h

```rust
pub const R_PPC64_GOT_TPREL16_HI: u32 = 89;
```

#### Constant `R_PPC64_GOT_TPREL16_HA`

half16  (sym+add)@got@tprel@ha

```rust
pub const R_PPC64_GOT_TPREL16_HA: u32 = 90;
```

#### Constant `R_PPC64_GOT_DTPREL16_DS`

half16ds* (sym+add)@got@dtprel

```rust
pub const R_PPC64_GOT_DTPREL16_DS: u32 = 91;
```

#### Constant `R_PPC64_GOT_DTPREL16_LO_DS`

half16ds (sym+add)@got@dtprel@l

```rust
pub const R_PPC64_GOT_DTPREL16_LO_DS: u32 = 92;
```

#### Constant `R_PPC64_GOT_DTPREL16_HI`

half16  (sym+add)@got@dtprel@h

```rust
pub const R_PPC64_GOT_DTPREL16_HI: u32 = 93;
```

#### Constant `R_PPC64_GOT_DTPREL16_HA`

half16  (sym+add)@got@dtprel@ha

```rust
pub const R_PPC64_GOT_DTPREL16_HA: u32 = 94;
```

#### Constant `R_PPC64_TPREL16_DS`

half16ds* (sym+add)@tprel

```rust
pub const R_PPC64_TPREL16_DS: u32 = 95;
```

#### Constant `R_PPC64_TPREL16_LO_DS`

half16ds (sym+add)@tprel@l

```rust
pub const R_PPC64_TPREL16_LO_DS: u32 = 96;
```

#### Constant `R_PPC64_TPREL16_HIGHER`

half16  (sym+add)@tprel@higher

```rust
pub const R_PPC64_TPREL16_HIGHER: u32 = 97;
```

#### Constant `R_PPC64_TPREL16_HIGHERA`

half16  (sym+add)@tprel@highera

```rust
pub const R_PPC64_TPREL16_HIGHERA: u32 = 98;
```

#### Constant `R_PPC64_TPREL16_HIGHEST`

half16  (sym+add)@tprel@highest

```rust
pub const R_PPC64_TPREL16_HIGHEST: u32 = 99;
```

#### Constant `R_PPC64_TPREL16_HIGHESTA`

half16  (sym+add)@tprel@highesta

```rust
pub const R_PPC64_TPREL16_HIGHESTA: u32 = 100;
```

#### Constant `R_PPC64_DTPREL16_DS`

half16ds* (sym+add)@dtprel

```rust
pub const R_PPC64_DTPREL16_DS: u32 = 101;
```

#### Constant `R_PPC64_DTPREL16_LO_DS`

half16ds (sym+add)@dtprel@l

```rust
pub const R_PPC64_DTPREL16_LO_DS: u32 = 102;
```

#### Constant `R_PPC64_DTPREL16_HIGHER`

half16  (sym+add)@dtprel@higher

```rust
pub const R_PPC64_DTPREL16_HIGHER: u32 = 103;
```

#### Constant `R_PPC64_DTPREL16_HIGHERA`

half16  (sym+add)@dtprel@highera

```rust
pub const R_PPC64_DTPREL16_HIGHERA: u32 = 104;
```

#### Constant `R_PPC64_DTPREL16_HIGHEST`

half16  (sym+add)@dtprel@highest

```rust
pub const R_PPC64_DTPREL16_HIGHEST: u32 = 105;
```

#### Constant `R_PPC64_DTPREL16_HIGHESTA`

half16  (sym+add)@dtprel@highesta

```rust
pub const R_PPC64_DTPREL16_HIGHESTA: u32 = 106;
```

#### Constant `R_PPC64_TLSGD`

none    (sym+add)@tlsgd

```rust
pub const R_PPC64_TLSGD: u32 = 107;
```

#### Constant `R_PPC64_TLSLD`

none    (sym+add)@tlsld

```rust
pub const R_PPC64_TLSLD: u32 = 108;
```

#### Constant `R_PPC64_TOCSAVE`

none

```rust
pub const R_PPC64_TOCSAVE: u32 = 109;
```

#### Constant `R_PPC64_ADDR16_HIGH`

```rust
pub const R_PPC64_ADDR16_HIGH: u32 = 110;
```

#### Constant `R_PPC64_ADDR16_HIGHA`

```rust
pub const R_PPC64_ADDR16_HIGHA: u32 = 111;
```

#### Constant `R_PPC64_TPREL16_HIGH`

```rust
pub const R_PPC64_TPREL16_HIGH: u32 = 112;
```

#### Constant `R_PPC64_TPREL16_HIGHA`

```rust
pub const R_PPC64_TPREL16_HIGHA: u32 = 113;
```

#### Constant `R_PPC64_DTPREL16_HIGH`

```rust
pub const R_PPC64_DTPREL16_HIGH: u32 = 114;
```

#### Constant `R_PPC64_DTPREL16_HIGHA`

```rust
pub const R_PPC64_DTPREL16_HIGHA: u32 = 115;
```

#### Constant `R_PPC64_JMP_IREL`

GNU extension to support local ifunc.

```rust
pub const R_PPC64_JMP_IREL: u32 = 247;
```

#### Constant `R_PPC64_IRELATIVE`

GNU extension to support local ifunc.

```rust
pub const R_PPC64_IRELATIVE: u32 = 248;
```

#### Constant `R_PPC64_REL16`

half16   (sym+add-.)

```rust
pub const R_PPC64_REL16: u32 = 249;
```

#### Constant `R_PPC64_REL16_LO`

half16   (sym+add-.)@l

```rust
pub const R_PPC64_REL16_LO: u32 = 250;
```

#### Constant `R_PPC64_REL16_HI`

half16   (sym+add-.)@h

```rust
pub const R_PPC64_REL16_HI: u32 = 251;
```

#### Constant `R_PPC64_REL16_HA`

half16   (sym+add-.)@ha

```rust
pub const R_PPC64_REL16_HA: u32 = 252;
```

#### Constant `EF_PPC64_ABI`

PowerPC64 bits specifying ABI.

1 for original function descriptor using ABI,
2 for revised ABI without function descriptors,
0 for unspecified or not using any features affected by the differences.

```rust
pub const EF_PPC64_ABI: u32 = 3;
```

#### Constant `DT_PPC64_GLINK`

```rust
pub const DT_PPC64_GLINK: u32 = _;
```

#### Constant `DT_PPC64_OPD`

```rust
pub const DT_PPC64_OPD: u32 = _;
```

#### Constant `DT_PPC64_OPDSZ`

```rust
pub const DT_PPC64_OPDSZ: u32 = _;
```

#### Constant `DT_PPC64_OPT`

```rust
pub const DT_PPC64_OPT: u32 = _;
```

#### Constant `PPC64_OPT_TLS`

```rust
pub const PPC64_OPT_TLS: u32 = 1;
```

#### Constant `PPC64_OPT_MULTI_TOC`

```rust
pub const PPC64_OPT_MULTI_TOC: u32 = 2;
```

#### Constant `PPC64_OPT_LOCALENTRY`

```rust
pub const PPC64_OPT_LOCALENTRY: u32 = 4;
```

#### Constant `STO_PPC64_LOCAL_BIT`

```rust
pub const STO_PPC64_LOCAL_BIT: u8 = 5;
```

#### Constant `STO_PPC64_LOCAL_MASK`

```rust
pub const STO_PPC64_LOCAL_MASK: u8 = _;
```

#### Constant `EF_ARM_RELEXEC`

```rust
pub const EF_ARM_RELEXEC: u32 = 0x01;
```

#### Constant `EF_ARM_HASENTRY`

```rust
pub const EF_ARM_HASENTRY: u32 = 0x02;
```

#### Constant `EF_ARM_INTERWORK`

```rust
pub const EF_ARM_INTERWORK: u32 = 0x04;
```

#### Constant `EF_ARM_APCS_26`

```rust
pub const EF_ARM_APCS_26: u32 = 0x08;
```

#### Constant `EF_ARM_APCS_FLOAT`

```rust
pub const EF_ARM_APCS_FLOAT: u32 = 0x10;
```

#### Constant `EF_ARM_PIC`

```rust
pub const EF_ARM_PIC: u32 = 0x20;
```

#### Constant `EF_ARM_ALIGN8`

8-bit structure alignment is in use

```rust
pub const EF_ARM_ALIGN8: u32 = 0x40;
```

#### Constant `EF_ARM_NEW_ABI`

```rust
pub const EF_ARM_NEW_ABI: u32 = 0x80;
```

#### Constant `EF_ARM_OLD_ABI`

```rust
pub const EF_ARM_OLD_ABI: u32 = 0x100;
```

#### Constant `EF_ARM_SOFT_FLOAT`

```rust
pub const EF_ARM_SOFT_FLOAT: u32 = 0x200;
```

#### Constant `EF_ARM_VFP_FLOAT`

```rust
pub const EF_ARM_VFP_FLOAT: u32 = 0x400;
```

#### Constant `EF_ARM_MAVERICK_FLOAT`

```rust
pub const EF_ARM_MAVERICK_FLOAT: u32 = 0x800;
```

#### Constant `EF_ARM_ABI_FLOAT_SOFT`

NB conflicts with EF_ARM_SOFT_FLOAT

```rust
pub const EF_ARM_ABI_FLOAT_SOFT: u32 = 0x200;
```

#### Constant `EF_ARM_ABI_FLOAT_HARD`

NB conflicts with EF_ARM_VFP_FLOAT

```rust
pub const EF_ARM_ABI_FLOAT_HARD: u32 = 0x400;
```

#### Constant `EF_ARM_SYMSARESORTED`

```rust
pub const EF_ARM_SYMSARESORTED: u32 = 0x04;
```

#### Constant `EF_ARM_DYNSYMSUSESEGIDX`

```rust
pub const EF_ARM_DYNSYMSUSESEGIDX: u32 = 0x08;
```

#### Constant `EF_ARM_MAPSYMSFIRST`

```rust
pub const EF_ARM_MAPSYMSFIRST: u32 = 0x10;
```

#### Constant `EF_ARM_BE8`

```rust
pub const EF_ARM_BE8: u32 = 0x0080_0000;
```

#### Constant `EF_ARM_LE8`

```rust
pub const EF_ARM_LE8: u32 = 0x0040_0000;
```

#### Constant `EF_ARM_EABIMASK`

```rust
pub const EF_ARM_EABIMASK: u32 = 0xff00_0000;
```

#### Constant `EF_ARM_EABI_UNKNOWN`

```rust
pub const EF_ARM_EABI_UNKNOWN: u32 = 0x0000_0000;
```

#### Constant `EF_ARM_EABI_VER1`

```rust
pub const EF_ARM_EABI_VER1: u32 = 0x0100_0000;
```

#### Constant `EF_ARM_EABI_VER2`

```rust
pub const EF_ARM_EABI_VER2: u32 = 0x0200_0000;
```

#### Constant `EF_ARM_EABI_VER3`

```rust
pub const EF_ARM_EABI_VER3: u32 = 0x0300_0000;
```

#### Constant `EF_ARM_EABI_VER4`

```rust
pub const EF_ARM_EABI_VER4: u32 = 0x0400_0000;
```

#### Constant `EF_ARM_EABI_VER5`

```rust
pub const EF_ARM_EABI_VER5: u32 = 0x0500_0000;
```

#### Constant `STT_ARM_TFUNC`

A Thumb function.

```rust
pub const STT_ARM_TFUNC: u8 = STT_LOPROC;
```

#### Constant `STT_ARM_16BIT`

A Thumb label.

```rust
pub const STT_ARM_16BIT: u8 = STT_HIPROC;
```

#### Constant `SHF_ARM_ENTRYSECT`

Section contains an entry point

```rust
pub const SHF_ARM_ENTRYSECT: u32 = 0x1000_0000;
```

#### Constant `SHF_ARM_COMDEF`

Section may be multiply defined in the input to a link step.

```rust
pub const SHF_ARM_COMDEF: u32 = 0x8000_0000;
```

#### Constant `PF_ARM_SB`

Segment contains the location addressed by the static base.

```rust
pub const PF_ARM_SB: u32 = 0x1000_0000;
```

#### Constant `PF_ARM_PI`

Position-independent segment.

```rust
pub const PF_ARM_PI: u32 = 0x2000_0000;
```

#### Constant `PF_ARM_ABS`

Absolute segment.

```rust
pub const PF_ARM_ABS: u32 = 0x4000_0000;
```

#### Constant `PT_ARM_EXIDX`

ARM unwind segment.

```rust
pub const PT_ARM_EXIDX: u32 = _;
```

#### Constant `SHT_ARM_EXIDX`

ARM unwind section.

```rust
pub const SHT_ARM_EXIDX: u32 = _;
```

#### Constant `SHT_ARM_PREEMPTMAP`

Preemption details.

```rust
pub const SHT_ARM_PREEMPTMAP: u32 = _;
```

#### Constant `SHT_ARM_ATTRIBUTES`

ARM attributes section.

```rust
pub const SHT_ARM_ATTRIBUTES: u32 = _;
```

#### Constant `SHT_AARCH64_ATTRIBUTES`

AArch64 attributes section.

```rust
pub const SHT_AARCH64_ATTRIBUTES: u32 = _;
```

#### Constant `R_AARCH64_NONE`

No relocation.

```rust
pub const R_AARCH64_NONE: u32 = 0;
```

#### Constant `R_AARCH64_P32_ABS32`

Direct 32 bit.

```rust
pub const R_AARCH64_P32_ABS32: u32 = 1;
```

#### Constant `R_AARCH64_P32_COPY`

Copy symbol at runtime.

```rust
pub const R_AARCH64_P32_COPY: u32 = 180;
```

#### Constant `R_AARCH64_P32_GLOB_DAT`

Create GOT entry.

```rust
pub const R_AARCH64_P32_GLOB_DAT: u32 = 181;
```

#### Constant `R_AARCH64_P32_JUMP_SLOT`

Create PLT entry.

```rust
pub const R_AARCH64_P32_JUMP_SLOT: u32 = 182;
```

#### Constant `R_AARCH64_P32_RELATIVE`

Adjust by program base.

```rust
pub const R_AARCH64_P32_RELATIVE: u32 = 183;
```

#### Constant `R_AARCH64_P32_TLS_DTPMOD`

Module number, 32 bit.

```rust
pub const R_AARCH64_P32_TLS_DTPMOD: u32 = 184;
```

#### Constant `R_AARCH64_P32_TLS_DTPREL`

Module-relative offset, 32 bit.

```rust
pub const R_AARCH64_P32_TLS_DTPREL: u32 = 185;
```

#### Constant `R_AARCH64_P32_TLS_TPREL`

TP-relative offset, 32 bit.

```rust
pub const R_AARCH64_P32_TLS_TPREL: u32 = 186;
```

#### Constant `R_AARCH64_P32_TLSDESC`

TLS Descriptor.

```rust
pub const R_AARCH64_P32_TLSDESC: u32 = 187;
```

#### Constant `R_AARCH64_P32_IRELATIVE`

STT_GNU_IFUNC relocation.

```rust
pub const R_AARCH64_P32_IRELATIVE: u32 = 188;
```

#### Constant `R_AARCH64_ABS64`

Direct 64 bit.

```rust
pub const R_AARCH64_ABS64: u32 = 257;
```

#### Constant `R_AARCH64_ABS32`

Direct 32 bit.

```rust
pub const R_AARCH64_ABS32: u32 = 258;
```

#### Constant `R_AARCH64_ABS16`

Direct 16-bit.

```rust
pub const R_AARCH64_ABS16: u32 = 259;
```

#### Constant `R_AARCH64_PREL64`

PC-relative 64-bit.

```rust
pub const R_AARCH64_PREL64: u32 = 260;
```

#### Constant `R_AARCH64_PREL32`

PC-relative 32-bit.

```rust
pub const R_AARCH64_PREL32: u32 = 261;
```

#### Constant `R_AARCH64_PREL16`

PC-relative 16-bit.

```rust
pub const R_AARCH64_PREL16: u32 = 262;
```

#### Constant `R_AARCH64_MOVW_UABS_G0`

Dir. MOVZ imm. from bits 15:0.

```rust
pub const R_AARCH64_MOVW_UABS_G0: u32 = 263;
```

#### Constant `R_AARCH64_MOVW_UABS_G0_NC`

Likewise for MOVK; no check.

```rust
pub const R_AARCH64_MOVW_UABS_G0_NC: u32 = 264;
```

#### Constant `R_AARCH64_MOVW_UABS_G1`

Dir. MOVZ imm. from bits 31:16.

```rust
pub const R_AARCH64_MOVW_UABS_G1: u32 = 265;
```

#### Constant `R_AARCH64_MOVW_UABS_G1_NC`

Likewise for MOVK; no check.

```rust
pub const R_AARCH64_MOVW_UABS_G1_NC: u32 = 266;
```

#### Constant `R_AARCH64_MOVW_UABS_G2`

Dir. MOVZ imm. from bits 47:32.

```rust
pub const R_AARCH64_MOVW_UABS_G2: u32 = 267;
```

#### Constant `R_AARCH64_MOVW_UABS_G2_NC`

Likewise for MOVK; no check.

```rust
pub const R_AARCH64_MOVW_UABS_G2_NC: u32 = 268;
```

#### Constant `R_AARCH64_MOVW_UABS_G3`

Dir. MOV{K,Z} imm. from 63:48.

```rust
pub const R_AARCH64_MOVW_UABS_G3: u32 = 269;
```

#### Constant `R_AARCH64_MOVW_SABS_G0`

Dir. MOV{N,Z} imm. from 15:0.

```rust
pub const R_AARCH64_MOVW_SABS_G0: u32 = 270;
```

#### Constant `R_AARCH64_MOVW_SABS_G1`

Dir. MOV{N,Z} imm. from 31:16.

```rust
pub const R_AARCH64_MOVW_SABS_G1: u32 = 271;
```

#### Constant `R_AARCH64_MOVW_SABS_G2`

Dir. MOV{N,Z} imm. from 47:32.

```rust
pub const R_AARCH64_MOVW_SABS_G2: u32 = 272;
```

#### Constant `R_AARCH64_LD_PREL_LO19`

PC-rel. LD imm. from bits 20:2.

```rust
pub const R_AARCH64_LD_PREL_LO19: u32 = 273;
```

#### Constant `R_AARCH64_ADR_PREL_LO21`

PC-rel. ADR imm. from bits 20:0.

```rust
pub const R_AARCH64_ADR_PREL_LO21: u32 = 274;
```

#### Constant `R_AARCH64_ADR_PREL_PG_HI21`

Page-rel. ADRP imm. from 32:12.

```rust
pub const R_AARCH64_ADR_PREL_PG_HI21: u32 = 275;
```

#### Constant `R_AARCH64_ADR_PREL_PG_HI21_NC`

Likewise; no overflow check.

```rust
pub const R_AARCH64_ADR_PREL_PG_HI21_NC: u32 = 276;
```

#### Constant `R_AARCH64_ADD_ABS_LO12_NC`

Dir. ADD imm. from bits 11:0.

```rust
pub const R_AARCH64_ADD_ABS_LO12_NC: u32 = 277;
```

#### Constant `R_AARCH64_LDST8_ABS_LO12_NC`

Likewise for LD/ST; no check.

```rust
pub const R_AARCH64_LDST8_ABS_LO12_NC: u32 = 278;
```

#### Constant `R_AARCH64_TSTBR14`

PC-rel. TBZ/TBNZ imm. from 15:2.

```rust
pub const R_AARCH64_TSTBR14: u32 = 279;
```

#### Constant `R_AARCH64_CONDBR19`

PC-rel. cond. br. imm. from 20:2.

```rust
pub const R_AARCH64_CONDBR19: u32 = 280;
```

#### Constant `R_AARCH64_JUMP26`

PC-rel. B imm. from bits 27:2.

```rust
pub const R_AARCH64_JUMP26: u32 = 282;
```

#### Constant `R_AARCH64_CALL26`

Likewise for CALL.

```rust
pub const R_AARCH64_CALL26: u32 = 283;
```

#### Constant `R_AARCH64_LDST16_ABS_LO12_NC`

Dir. ADD imm. from bits 11:1.

```rust
pub const R_AARCH64_LDST16_ABS_LO12_NC: u32 = 284;
```

#### Constant `R_AARCH64_LDST32_ABS_LO12_NC`

Likewise for bits 11:2.

```rust
pub const R_AARCH64_LDST32_ABS_LO12_NC: u32 = 285;
```

#### Constant `R_AARCH64_LDST64_ABS_LO12_NC`

Likewise for bits 11:3.

```rust
pub const R_AARCH64_LDST64_ABS_LO12_NC: u32 = 286;
```

#### Constant `R_AARCH64_MOVW_PREL_G0`

PC-rel. MOV{N,Z} imm. from 15:0.

```rust
pub const R_AARCH64_MOVW_PREL_G0: u32 = 287;
```

#### Constant `R_AARCH64_MOVW_PREL_G0_NC`

Likewise for MOVK; no check.

```rust
pub const R_AARCH64_MOVW_PREL_G0_NC: u32 = 288;
```

#### Constant `R_AARCH64_MOVW_PREL_G1`

PC-rel. MOV{N,Z} imm. from 31:16.

```rust
pub const R_AARCH64_MOVW_PREL_G1: u32 = 289;
```

#### Constant `R_AARCH64_MOVW_PREL_G1_NC`

Likewise for MOVK; no check.

```rust
pub const R_AARCH64_MOVW_PREL_G1_NC: u32 = 290;
```

#### Constant `R_AARCH64_MOVW_PREL_G2`

PC-rel. MOV{N,Z} imm. from 47:32.

```rust
pub const R_AARCH64_MOVW_PREL_G2: u32 = 291;
```

#### Constant `R_AARCH64_MOVW_PREL_G2_NC`

Likewise for MOVK; no check.

```rust
pub const R_AARCH64_MOVW_PREL_G2_NC: u32 = 292;
```

#### Constant `R_AARCH64_MOVW_PREL_G3`

PC-rel. MOV{N,Z} imm. from 63:48.

```rust
pub const R_AARCH64_MOVW_PREL_G3: u32 = 293;
```

#### Constant `R_AARCH64_LDST128_ABS_LO12_NC`

Dir. ADD imm. from bits 11:4.

```rust
pub const R_AARCH64_LDST128_ABS_LO12_NC: u32 = 299;
```

#### Constant `R_AARCH64_MOVW_GOTOFF_G0`

GOT-rel. off. MOV{N,Z} imm. 15:0.

```rust
pub const R_AARCH64_MOVW_GOTOFF_G0: u32 = 300;
```

#### Constant `R_AARCH64_MOVW_GOTOFF_G0_NC`

Likewise for MOVK; no check.

```rust
pub const R_AARCH64_MOVW_GOTOFF_G0_NC: u32 = 301;
```

#### Constant `R_AARCH64_MOVW_GOTOFF_G1`

GOT-rel. o. MOV{N,Z} imm. 31:16.

```rust
pub const R_AARCH64_MOVW_GOTOFF_G1: u32 = 302;
```

#### Constant `R_AARCH64_MOVW_GOTOFF_G1_NC`

Likewise for MOVK; no check.

```rust
pub const R_AARCH64_MOVW_GOTOFF_G1_NC: u32 = 303;
```

#### Constant `R_AARCH64_MOVW_GOTOFF_G2`

GOT-rel. o. MOV{N,Z} imm. 47:32.

```rust
pub const R_AARCH64_MOVW_GOTOFF_G2: u32 = 304;
```

#### Constant `R_AARCH64_MOVW_GOTOFF_G2_NC`

Likewise for MOVK; no check.

```rust
pub const R_AARCH64_MOVW_GOTOFF_G2_NC: u32 = 305;
```

#### Constant `R_AARCH64_MOVW_GOTOFF_G3`

GOT-rel. o. MOV{N,Z} imm. 63:48.

```rust
pub const R_AARCH64_MOVW_GOTOFF_G3: u32 = 306;
```

#### Constant `R_AARCH64_GOTREL64`

GOT-relative 64-bit.

```rust
pub const R_AARCH64_GOTREL64: u32 = 307;
```

#### Constant `R_AARCH64_GOTREL32`

GOT-relative 32-bit.

```rust
pub const R_AARCH64_GOTREL32: u32 = 308;
```

#### Constant `R_AARCH64_GOT_LD_PREL19`

PC-rel. GOT off. load imm. 20:2.

```rust
pub const R_AARCH64_GOT_LD_PREL19: u32 = 309;
```

#### Constant `R_AARCH64_LD64_GOTOFF_LO15`

GOT-rel. off. LD/ST imm. 14:3.

```rust
pub const R_AARCH64_LD64_GOTOFF_LO15: u32 = 310;
```

#### Constant `R_AARCH64_ADR_GOT_PAGE`

P-page-rel. GOT off. ADRP 32:12.

```rust
pub const R_AARCH64_ADR_GOT_PAGE: u32 = 311;
```

#### Constant `R_AARCH64_LD64_GOT_LO12_NC`

Dir. GOT off. LD/ST imm. 11:3.

```rust
pub const R_AARCH64_LD64_GOT_LO12_NC: u32 = 312;
```

#### Constant `R_AARCH64_LD64_GOTPAGE_LO15`

GOT-page-rel. GOT off. LD/ST 14:3

```rust
pub const R_AARCH64_LD64_GOTPAGE_LO15: u32 = 313;
```

#### Constant `R_AARCH64_TLSGD_ADR_PREL21`

PC-relative ADR imm. 20:0.

```rust
pub const R_AARCH64_TLSGD_ADR_PREL21: u32 = 512;
```

#### Constant `R_AARCH64_TLSGD_ADR_PAGE21`

page-rel. ADRP imm. 32:12.

```rust
pub const R_AARCH64_TLSGD_ADR_PAGE21: u32 = 513;
```

#### Constant `R_AARCH64_TLSGD_ADD_LO12_NC`

direct ADD imm. from 11:0.

```rust
pub const R_AARCH64_TLSGD_ADD_LO12_NC: u32 = 514;
```

#### Constant `R_AARCH64_TLSGD_MOVW_G1`

GOT-rel. MOV{N,Z} 31:16.

```rust
pub const R_AARCH64_TLSGD_MOVW_G1: u32 = 515;
```

#### Constant `R_AARCH64_TLSGD_MOVW_G0_NC`

GOT-rel. MOVK imm. 15:0.

```rust
pub const R_AARCH64_TLSGD_MOVW_G0_NC: u32 = 516;
```

#### Constant `R_AARCH64_TLSLD_ADR_PREL21`

Like 512; local dynamic model.

```rust
pub const R_AARCH64_TLSLD_ADR_PREL21: u32 = 517;
```

#### Constant `R_AARCH64_TLSLD_ADR_PAGE21`

Like 513; local dynamic model.

```rust
pub const R_AARCH64_TLSLD_ADR_PAGE21: u32 = 518;
```

#### Constant `R_AARCH64_TLSLD_ADD_LO12_NC`

Like 514; local dynamic model.

```rust
pub const R_AARCH64_TLSLD_ADD_LO12_NC: u32 = 519;
```

#### Constant `R_AARCH64_TLSLD_MOVW_G1`

Like 515; local dynamic model.

```rust
pub const R_AARCH64_TLSLD_MOVW_G1: u32 = 520;
```

#### Constant `R_AARCH64_TLSLD_MOVW_G0_NC`

Like 516; local dynamic model.

```rust
pub const R_AARCH64_TLSLD_MOVW_G0_NC: u32 = 521;
```

#### Constant `R_AARCH64_TLSLD_LD_PREL19`

TLS PC-rel. load imm. 20:2.

```rust
pub const R_AARCH64_TLSLD_LD_PREL19: u32 = 522;
```

#### Constant `R_AARCH64_TLSLD_MOVW_DTPREL_G2`

TLS DTP-rel. MOV{N,Z} 47:32.

```rust
pub const R_AARCH64_TLSLD_MOVW_DTPREL_G2: u32 = 523;
```

#### Constant `R_AARCH64_TLSLD_MOVW_DTPREL_G1`

TLS DTP-rel. MOV{N,Z} 31:16.

```rust
pub const R_AARCH64_TLSLD_MOVW_DTPREL_G1: u32 = 524;
```

#### Constant `R_AARCH64_TLSLD_MOVW_DTPREL_G1_NC`

Likewise; MOVK; no check.

```rust
pub const R_AARCH64_TLSLD_MOVW_DTPREL_G1_NC: u32 = 525;
```

#### Constant `R_AARCH64_TLSLD_MOVW_DTPREL_G0`

TLS DTP-rel. MOV{N,Z} 15:0.

```rust
pub const R_AARCH64_TLSLD_MOVW_DTPREL_G0: u32 = 526;
```

#### Constant `R_AARCH64_TLSLD_MOVW_DTPREL_G0_NC`

Likewise; MOVK; no check.

```rust
pub const R_AARCH64_TLSLD_MOVW_DTPREL_G0_NC: u32 = 527;
```

#### Constant `R_AARCH64_TLSLD_ADD_DTPREL_HI12`

DTP-rel. ADD imm. from 23:12.

```rust
pub const R_AARCH64_TLSLD_ADD_DTPREL_HI12: u32 = 528;
```

#### Constant `R_AARCH64_TLSLD_ADD_DTPREL_LO12`

DTP-rel. ADD imm. from 11:0.

```rust
pub const R_AARCH64_TLSLD_ADD_DTPREL_LO12: u32 = 529;
```

#### Constant `R_AARCH64_TLSLD_ADD_DTPREL_LO12_NC`

Likewise; no ovfl. check.

```rust
pub const R_AARCH64_TLSLD_ADD_DTPREL_LO12_NC: u32 = 530;
```

#### Constant `R_AARCH64_TLSLD_LDST8_DTPREL_LO12`

DTP-rel. LD/ST imm. 11:0.

```rust
pub const R_AARCH64_TLSLD_LDST8_DTPREL_LO12: u32 = 531;
```

#### Constant `R_AARCH64_TLSLD_LDST8_DTPREL_LO12_NC`

Likewise; no check.

```rust
pub const R_AARCH64_TLSLD_LDST8_DTPREL_LO12_NC: u32 = 532;
```

#### Constant `R_AARCH64_TLSLD_LDST16_DTPREL_LO12`

DTP-rel. LD/ST imm. 11:1.

```rust
pub const R_AARCH64_TLSLD_LDST16_DTPREL_LO12: u32 = 533;
```

#### Constant `R_AARCH64_TLSLD_LDST16_DTPREL_LO12_NC`

Likewise; no check.

```rust
pub const R_AARCH64_TLSLD_LDST16_DTPREL_LO12_NC: u32 = 534;
```

#### Constant `R_AARCH64_TLSLD_LDST32_DTPREL_LO12`

DTP-rel. LD/ST imm. 11:2.

```rust
pub const R_AARCH64_TLSLD_LDST32_DTPREL_LO12: u32 = 535;
```

#### Constant `R_AARCH64_TLSLD_LDST32_DTPREL_LO12_NC`

Likewise; no check.

```rust
pub const R_AARCH64_TLSLD_LDST32_DTPREL_LO12_NC: u32 = 536;
```

#### Constant `R_AARCH64_TLSLD_LDST64_DTPREL_LO12`

DTP-rel. LD/ST imm. 11:3.

```rust
pub const R_AARCH64_TLSLD_LDST64_DTPREL_LO12: u32 = 537;
```

#### Constant `R_AARCH64_TLSLD_LDST64_DTPREL_LO12_NC`

Likewise; no check.

```rust
pub const R_AARCH64_TLSLD_LDST64_DTPREL_LO12_NC: u32 = 538;
```

#### Constant `R_AARCH64_TLSIE_MOVW_GOTTPREL_G1`

GOT-rel. MOV{N,Z} 31:16.

```rust
pub const R_AARCH64_TLSIE_MOVW_GOTTPREL_G1: u32 = 539;
```

#### Constant `R_AARCH64_TLSIE_MOVW_GOTTPREL_G0_NC`

GOT-rel. MOVK 15:0.

```rust
pub const R_AARCH64_TLSIE_MOVW_GOTTPREL_G0_NC: u32 = 540;
```

#### Constant `R_AARCH64_TLSIE_ADR_GOTTPREL_PAGE21`

Page-rel. ADRP 32:12.

```rust
pub const R_AARCH64_TLSIE_ADR_GOTTPREL_PAGE21: u32 = 541;
```

#### Constant `R_AARCH64_TLSIE_LD64_GOTTPREL_LO12_NC`

Direct LD off. 11:3.

```rust
pub const R_AARCH64_TLSIE_LD64_GOTTPREL_LO12_NC: u32 = 542;
```

#### Constant `R_AARCH64_TLSIE_LD_GOTTPREL_PREL19`

PC-rel. load imm. 20:2.

```rust
pub const R_AARCH64_TLSIE_LD_GOTTPREL_PREL19: u32 = 543;
```

#### Constant `R_AARCH64_TLSLE_MOVW_TPREL_G2`

TLS TP-rel. MOV{N,Z} 47:32.

```rust
pub const R_AARCH64_TLSLE_MOVW_TPREL_G2: u32 = 544;
```

#### Constant `R_AARCH64_TLSLE_MOVW_TPREL_G1`

TLS TP-rel. MOV{N,Z} 31:16.

```rust
pub const R_AARCH64_TLSLE_MOVW_TPREL_G1: u32 = 545;
```

#### Constant `R_AARCH64_TLSLE_MOVW_TPREL_G1_NC`

Likewise; MOVK; no check.

```rust
pub const R_AARCH64_TLSLE_MOVW_TPREL_G1_NC: u32 = 546;
```

#### Constant `R_AARCH64_TLSLE_MOVW_TPREL_G0`

TLS TP-rel. MOV{N,Z} 15:0.

```rust
pub const R_AARCH64_TLSLE_MOVW_TPREL_G0: u32 = 547;
```

#### Constant `R_AARCH64_TLSLE_MOVW_TPREL_G0_NC`

Likewise; MOVK; no check.

```rust
pub const R_AARCH64_TLSLE_MOVW_TPREL_G0_NC: u32 = 548;
```

#### Constant `R_AARCH64_TLSLE_ADD_TPREL_HI12`

TP-rel. ADD imm. 23:12.

```rust
pub const R_AARCH64_TLSLE_ADD_TPREL_HI12: u32 = 549;
```

#### Constant `R_AARCH64_TLSLE_ADD_TPREL_LO12`

TP-rel. ADD imm. 11:0.

```rust
pub const R_AARCH64_TLSLE_ADD_TPREL_LO12: u32 = 550;
```

#### Constant `R_AARCH64_TLSLE_ADD_TPREL_LO12_NC`

Likewise; no ovfl. check.

```rust
pub const R_AARCH64_TLSLE_ADD_TPREL_LO12_NC: u32 = 551;
```

#### Constant `R_AARCH64_TLSLE_LDST8_TPREL_LO12`

TP-rel. LD/ST off. 11:0.

```rust
pub const R_AARCH64_TLSLE_LDST8_TPREL_LO12: u32 = 552;
```

#### Constant `R_AARCH64_TLSLE_LDST8_TPREL_LO12_NC`

Likewise; no ovfl. check.

```rust
pub const R_AARCH64_TLSLE_LDST8_TPREL_LO12_NC: u32 = 553;
```

#### Constant `R_AARCH64_TLSLE_LDST16_TPREL_LO12`

TP-rel. LD/ST off. 11:1.

```rust
pub const R_AARCH64_TLSLE_LDST16_TPREL_LO12: u32 = 554;
```

#### Constant `R_AARCH64_TLSLE_LDST16_TPREL_LO12_NC`

Likewise; no check.

```rust
pub const R_AARCH64_TLSLE_LDST16_TPREL_LO12_NC: u32 = 555;
```

#### Constant `R_AARCH64_TLSLE_LDST32_TPREL_LO12`

TP-rel. LD/ST off. 11:2.

```rust
pub const R_AARCH64_TLSLE_LDST32_TPREL_LO12: u32 = 556;
```

#### Constant `R_AARCH64_TLSLE_LDST32_TPREL_LO12_NC`

Likewise; no check.

```rust
pub const R_AARCH64_TLSLE_LDST32_TPREL_LO12_NC: u32 = 557;
```

#### Constant `R_AARCH64_TLSLE_LDST64_TPREL_LO12`

TP-rel. LD/ST off. 11:3.

```rust
pub const R_AARCH64_TLSLE_LDST64_TPREL_LO12: u32 = 558;
```

#### Constant `R_AARCH64_TLSLE_LDST64_TPREL_LO12_NC`

Likewise; no check.

```rust
pub const R_AARCH64_TLSLE_LDST64_TPREL_LO12_NC: u32 = 559;
```

#### Constant `R_AARCH64_TLSDESC_LD_PREL19`

PC-rel. load immediate 20:2.

```rust
pub const R_AARCH64_TLSDESC_LD_PREL19: u32 = 560;
```

#### Constant `R_AARCH64_TLSDESC_ADR_PREL21`

PC-rel. ADR immediate 20:0.

```rust
pub const R_AARCH64_TLSDESC_ADR_PREL21: u32 = 561;
```

#### Constant `R_AARCH64_TLSDESC_ADR_PAGE21`

Page-rel. ADRP imm. 32:12.

```rust
pub const R_AARCH64_TLSDESC_ADR_PAGE21: u32 = 562;
```

#### Constant `R_AARCH64_TLSDESC_LD64_LO12`

Direct LD off. from 11:3.

```rust
pub const R_AARCH64_TLSDESC_LD64_LO12: u32 = 563;
```

#### Constant `R_AARCH64_TLSDESC_ADD_LO12`

Direct ADD imm. from 11:0.

```rust
pub const R_AARCH64_TLSDESC_ADD_LO12: u32 = 564;
```

#### Constant `R_AARCH64_TLSDESC_OFF_G1`

GOT-rel. MOV{N,Z} imm. 31:16.

```rust
pub const R_AARCH64_TLSDESC_OFF_G1: u32 = 565;
```

#### Constant `R_AARCH64_TLSDESC_OFF_G0_NC`

GOT-rel. MOVK imm. 15:0; no ck.

```rust
pub const R_AARCH64_TLSDESC_OFF_G0_NC: u32 = 566;
```

#### Constant `R_AARCH64_TLSDESC_LDR`

Relax LDR.

```rust
pub const R_AARCH64_TLSDESC_LDR: u32 = 567;
```

#### Constant `R_AARCH64_TLSDESC_ADD`

Relax ADD.

```rust
pub const R_AARCH64_TLSDESC_ADD: u32 = 568;
```

#### Constant `R_AARCH64_TLSDESC_CALL`

Relax BLR.

```rust
pub const R_AARCH64_TLSDESC_CALL: u32 = 569;
```

#### Constant `R_AARCH64_TLSLE_LDST128_TPREL_LO12`

TP-rel. LD/ST off. 11:4.

```rust
pub const R_AARCH64_TLSLE_LDST128_TPREL_LO12: u32 = 570;
```

#### Constant `R_AARCH64_TLSLE_LDST128_TPREL_LO12_NC`

Likewise; no check.

```rust
pub const R_AARCH64_TLSLE_LDST128_TPREL_LO12_NC: u32 = 571;
```

#### Constant `R_AARCH64_TLSLD_LDST128_DTPREL_LO12`

DTP-rel. LD/ST imm. 11:4.

```rust
pub const R_AARCH64_TLSLD_LDST128_DTPREL_LO12: u32 = 572;
```

#### Constant `R_AARCH64_TLSLD_LDST128_DTPREL_LO12_NC`

Likewise; no check.

```rust
pub const R_AARCH64_TLSLD_LDST128_DTPREL_LO12_NC: u32 = 573;
```

#### Constant `R_AARCH64_COPY`

Copy symbol at runtime.

```rust
pub const R_AARCH64_COPY: u32 = 1024;
```

#### Constant `R_AARCH64_GLOB_DAT`

Create GOT entry.

```rust
pub const R_AARCH64_GLOB_DAT: u32 = 1025;
```

#### Constant `R_AARCH64_JUMP_SLOT`

Create PLT entry.

```rust
pub const R_AARCH64_JUMP_SLOT: u32 = 1026;
```

#### Constant `R_AARCH64_RELATIVE`

Adjust by program base.

```rust
pub const R_AARCH64_RELATIVE: u32 = 1027;
```

#### Constant `R_AARCH64_TLS_DTPMOD`

Module number, 64 bit.

```rust
pub const R_AARCH64_TLS_DTPMOD: u32 = 1028;
```

#### Constant `R_AARCH64_TLS_DTPREL`

Module-relative offset, 64 bit.

```rust
pub const R_AARCH64_TLS_DTPREL: u32 = 1029;
```

#### Constant `R_AARCH64_TLS_TPREL`

TP-relative offset, 64 bit.

```rust
pub const R_AARCH64_TLS_TPREL: u32 = 1030;
```

#### Constant `R_AARCH64_TLSDESC`

TLS Descriptor.

```rust
pub const R_AARCH64_TLSDESC: u32 = 1031;
```

#### Constant `R_AARCH64_IRELATIVE`

STT_GNU_IFUNC relocation.

```rust
pub const R_AARCH64_IRELATIVE: u32 = 1032;
```

#### Constant `EF_AVR_ARCH`

Bitmask for `EF_AVR_ARCH_*`.

```rust
pub const EF_AVR_ARCH: u32 = 0x7F;
```

#### Constant `EF_AVR_LINKRELAX_PREPARED`

If set, it is assumed that the elf file uses local symbols as reference
for the relocations so that linker relaxation is possible.

```rust
pub const EF_AVR_LINKRELAX_PREPARED: u32 = 0x80;
```

#### Constant `EF_AVR_ARCH_AVR1`

```rust
pub const EF_AVR_ARCH_AVR1: u32 = 1;
```

#### Constant `EF_AVR_ARCH_AVR2`

```rust
pub const EF_AVR_ARCH_AVR2: u32 = 2;
```

#### Constant `EF_AVR_ARCH_AVR25`

```rust
pub const EF_AVR_ARCH_AVR25: u32 = 25;
```

#### Constant `EF_AVR_ARCH_AVR3`

```rust
pub const EF_AVR_ARCH_AVR3: u32 = 3;
```

#### Constant `EF_AVR_ARCH_AVR31`

```rust
pub const EF_AVR_ARCH_AVR31: u32 = 31;
```

#### Constant `EF_AVR_ARCH_AVR35`

```rust
pub const EF_AVR_ARCH_AVR35: u32 = 35;
```

#### Constant `EF_AVR_ARCH_AVR4`

```rust
pub const EF_AVR_ARCH_AVR4: u32 = 4;
```

#### Constant `EF_AVR_ARCH_AVR5`

```rust
pub const EF_AVR_ARCH_AVR5: u32 = 5;
```

#### Constant `EF_AVR_ARCH_AVR51`

```rust
pub const EF_AVR_ARCH_AVR51: u32 = 51;
```

#### Constant `EF_AVR_ARCH_AVR6`

```rust
pub const EF_AVR_ARCH_AVR6: u32 = 6;
```

#### Constant `EF_AVR_ARCH_AVRTINY`

```rust
pub const EF_AVR_ARCH_AVRTINY: u32 = 100;
```

#### Constant `EF_AVR_ARCH_XMEGA1`

```rust
pub const EF_AVR_ARCH_XMEGA1: u32 = 101;
```

#### Constant `EF_AVR_ARCH_XMEGA2`

```rust
pub const EF_AVR_ARCH_XMEGA2: u32 = 102;
```

#### Constant `EF_AVR_ARCH_XMEGA3`

```rust
pub const EF_AVR_ARCH_XMEGA3: u32 = 103;
```

#### Constant `EF_AVR_ARCH_XMEGA4`

```rust
pub const EF_AVR_ARCH_XMEGA4: u32 = 104;
```

#### Constant `EF_AVR_ARCH_XMEGA5`

```rust
pub const EF_AVR_ARCH_XMEGA5: u32 = 105;
```

#### Constant `EF_AVR_ARCH_XMEGA6`

```rust
pub const EF_AVR_ARCH_XMEGA6: u32 = 106;
```

#### Constant `EF_AVR_ARCH_XMEGA7`

```rust
pub const EF_AVR_ARCH_XMEGA7: u32 = 107;
```

#### Constant `R_AVR_NONE`

```rust
pub const R_AVR_NONE: u32 = 0;
```

#### Constant `R_AVR_32`

Direct 32 bit

```rust
pub const R_AVR_32: u32 = 1;
```

#### Constant `R_AVR_7_PCREL`

```rust
pub const R_AVR_7_PCREL: u32 = 2;
```

#### Constant `R_AVR_13_PCREL`

```rust
pub const R_AVR_13_PCREL: u32 = 3;
```

#### Constant `R_AVR_16`

Direct 16 bit

```rust
pub const R_AVR_16: u32 = 4;
```

#### Constant `R_AVR_16_PM`

```rust
pub const R_AVR_16_PM: u32 = 5;
```

#### Constant `R_AVR_LO8_LDI`

```rust
pub const R_AVR_LO8_LDI: u32 = 6;
```

#### Constant `R_AVR_HI8_LDI`

```rust
pub const R_AVR_HI8_LDI: u32 = 7;
```

#### Constant `R_AVR_HH8_LDI`

```rust
pub const R_AVR_HH8_LDI: u32 = 8;
```

#### Constant `R_AVR_LO8_LDI_NEG`

```rust
pub const R_AVR_LO8_LDI_NEG: u32 = 9;
```

#### Constant `R_AVR_HI8_LDI_NEG`

```rust
pub const R_AVR_HI8_LDI_NEG: u32 = 10;
```

#### Constant `R_AVR_HH8_LDI_NEG`

```rust
pub const R_AVR_HH8_LDI_NEG: u32 = 11;
```

#### Constant `R_AVR_LO8_LDI_PM`

```rust
pub const R_AVR_LO8_LDI_PM: u32 = 12;
```

#### Constant `R_AVR_HI8_LDI_PM`

```rust
pub const R_AVR_HI8_LDI_PM: u32 = 13;
```

#### Constant `R_AVR_HH8_LDI_PM`

```rust
pub const R_AVR_HH8_LDI_PM: u32 = 14;
```

#### Constant `R_AVR_LO8_LDI_PM_NEG`

```rust
pub const R_AVR_LO8_LDI_PM_NEG: u32 = 15;
```

#### Constant `R_AVR_HI8_LDI_PM_NEG`

```rust
pub const R_AVR_HI8_LDI_PM_NEG: u32 = 16;
```

#### Constant `R_AVR_HH8_LDI_PM_NEG`

```rust
pub const R_AVR_HH8_LDI_PM_NEG: u32 = 17;
```

#### Constant `R_AVR_CALL`

```rust
pub const R_AVR_CALL: u32 = 18;
```

#### Constant `R_AVR_LDI`

```rust
pub const R_AVR_LDI: u32 = 19;
```

#### Constant `R_AVR_6`

```rust
pub const R_AVR_6: u32 = 20;
```

#### Constant `R_AVR_6_ADIW`

```rust
pub const R_AVR_6_ADIW: u32 = 21;
```

#### Constant `R_AVR_MS8_LDI`

```rust
pub const R_AVR_MS8_LDI: u32 = 22;
```

#### Constant `R_AVR_MS8_LDI_NEG`

```rust
pub const R_AVR_MS8_LDI_NEG: u32 = 23;
```

#### Constant `R_AVR_LO8_LDI_GS`

```rust
pub const R_AVR_LO8_LDI_GS: u32 = 24;
```

#### Constant `R_AVR_HI8_LDI_GS`

```rust
pub const R_AVR_HI8_LDI_GS: u32 = 25;
```

#### Constant `R_AVR_8`

```rust
pub const R_AVR_8: u32 = 26;
```

#### Constant `R_AVR_8_LO8`

```rust
pub const R_AVR_8_LO8: u32 = 27;
```

#### Constant `R_AVR_8_HI8`

```rust
pub const R_AVR_8_HI8: u32 = 28;
```

#### Constant `R_AVR_8_HLO8`

```rust
pub const R_AVR_8_HLO8: u32 = 29;
```

#### Constant `R_AVR_DIFF8`

```rust
pub const R_AVR_DIFF8: u32 = 30;
```

#### Constant `R_AVR_DIFF16`

```rust
pub const R_AVR_DIFF16: u32 = 31;
```

#### Constant `R_AVR_DIFF32`

```rust
pub const R_AVR_DIFF32: u32 = 32;
```

#### Constant `R_AVR_LDS_STS_16`

```rust
pub const R_AVR_LDS_STS_16: u32 = 33;
```

#### Constant `R_AVR_PORT6`

```rust
pub const R_AVR_PORT6: u32 = 34;
```

#### Constant `R_AVR_PORT5`

```rust
pub const R_AVR_PORT5: u32 = 35;
```

#### Constant `R_AVR_32_PCREL`

```rust
pub const R_AVR_32_PCREL: u32 = 36;
```

#### Constant `R_MSP430_32`

Direct 32 bit

```rust
pub const R_MSP430_32: u32 = 1;
```

#### Constant `R_MSP430_16_BYTE`

Direct 16 bit

```rust
pub const R_MSP430_16_BYTE: u32 = 5;
```

#### Constant `R_HEX_32`

Direct 32 bit

```rust
pub const R_HEX_32: u32 = 6;
```

#### Constant `R_ARM_NONE`

No reloc

```rust
pub const R_ARM_NONE: u32 = 0;
```

#### Constant `R_ARM_PC24`

Deprecated PC relative 26 bit branch.

```rust
pub const R_ARM_PC24: u32 = 1;
```

#### Constant `R_ARM_ABS32`

Direct 32 bit

```rust
pub const R_ARM_ABS32: u32 = 2;
```

#### Constant `R_ARM_REL32`

PC relative 32 bit

```rust
pub const R_ARM_REL32: u32 = 3;
```

#### Constant `R_ARM_PC13`

```rust
pub const R_ARM_PC13: u32 = 4;
```

#### Constant `R_ARM_ABS16`

Direct 16 bit

```rust
pub const R_ARM_ABS16: u32 = 5;
```

#### Constant `R_ARM_ABS12`

Direct 12 bit

```rust
pub const R_ARM_ABS12: u32 = 6;
```

#### Constant `R_ARM_THM_ABS5`

Direct & 0x7C (`LDR`, `STR`).

```rust
pub const R_ARM_THM_ABS5: u32 = 7;
```

#### Constant `R_ARM_ABS8`

Direct 8 bit

```rust
pub const R_ARM_ABS8: u32 = 8;
```

#### Constant `R_ARM_SBREL32`

```rust
pub const R_ARM_SBREL32: u32 = 9;
```

#### Constant `R_ARM_THM_PC22`

PC relative 24 bit (Thumb32 `BL`).

```rust
pub const R_ARM_THM_PC22: u32 = 10;
```

#### Constant `R_ARM_THM_PC8`

PC relative & 0x3FC (Thumb16 `LDR`, `ADD`, `ADR`).

```rust
pub const R_ARM_THM_PC8: u32 = 11;
```

#### Constant `R_ARM_AMP_VCALL9`

```rust
pub const R_ARM_AMP_VCALL9: u32 = 12;
```

#### Constant `R_ARM_SWI24`

Obsolete static relocation.

```rust
pub const R_ARM_SWI24: u32 = 13;
```

#### Constant `R_ARM_TLS_DESC`

Dynamic relocation.

```rust
pub const R_ARM_TLS_DESC: u32 = 13;
```

#### Constant `R_ARM_THM_SWI8`

Reserved.

```rust
pub const R_ARM_THM_SWI8: u32 = 14;
```

#### Constant `R_ARM_XPC25`

Reserved.

```rust
pub const R_ARM_XPC25: u32 = 15;
```

#### Constant `R_ARM_THM_XPC22`

Reserved.

```rust
pub const R_ARM_THM_XPC22: u32 = 16;
```

#### Constant `R_ARM_TLS_DTPMOD32`

ID of module containing symbol

```rust
pub const R_ARM_TLS_DTPMOD32: u32 = 17;
```

#### Constant `R_ARM_TLS_DTPOFF32`

Offset in TLS block

```rust
pub const R_ARM_TLS_DTPOFF32: u32 = 18;
```

#### Constant `R_ARM_TLS_TPOFF32`

Offset in static TLS block

```rust
pub const R_ARM_TLS_TPOFF32: u32 = 19;
```

#### Constant `R_ARM_COPY`

Copy symbol at runtime

```rust
pub const R_ARM_COPY: u32 = 20;
```

#### Constant `R_ARM_GLOB_DAT`

Create GOT entry

```rust
pub const R_ARM_GLOB_DAT: u32 = 21;
```

#### Constant `R_ARM_JUMP_SLOT`

Create PLT entry

```rust
pub const R_ARM_JUMP_SLOT: u32 = 22;
```

#### Constant `R_ARM_RELATIVE`

Adjust by program base

```rust
pub const R_ARM_RELATIVE: u32 = 23;
```

#### Constant `R_ARM_GOTOFF`

32 bit offset to GOT

```rust
pub const R_ARM_GOTOFF: u32 = 24;
```

#### Constant `R_ARM_GOTPC`

32 bit PC relative offset to GOT

```rust
pub const R_ARM_GOTPC: u32 = 25;
```

#### Constant `R_ARM_GOT32`

32 bit GOT entry

```rust
pub const R_ARM_GOT32: u32 = 26;
```

#### Constant `R_ARM_PLT32`

Deprecated, 32 bit PLT address.

```rust
pub const R_ARM_PLT32: u32 = 27;
```

#### Constant `R_ARM_CALL`

PC relative 24 bit (`BL`, `BLX`).

```rust
pub const R_ARM_CALL: u32 = 28;
```

#### Constant `R_ARM_JUMP24`

PC relative 24 bit (`B`, `BL<cond>`).

```rust
pub const R_ARM_JUMP24: u32 = 29;
```

#### Constant `R_ARM_THM_JUMP24`

PC relative 24 bit (Thumb32 `B.W`).

```rust
pub const R_ARM_THM_JUMP24: u32 = 30;
```

#### Constant `R_ARM_BASE_ABS`

Adjust by program base.

```rust
pub const R_ARM_BASE_ABS: u32 = 31;
```

#### Constant `R_ARM_ALU_PCREL_7_0`

Obsolete.

```rust
pub const R_ARM_ALU_PCREL_7_0: u32 = 32;
```

#### Constant `R_ARM_ALU_PCREL_15_8`

Obsolete.

```rust
pub const R_ARM_ALU_PCREL_15_8: u32 = 33;
```

#### Constant `R_ARM_ALU_PCREL_23_15`

Obsolete.

```rust
pub const R_ARM_ALU_PCREL_23_15: u32 = 34;
```

#### Constant `R_ARM_LDR_SBREL_11_0`

Deprecated, prog. base relative.

```rust
pub const R_ARM_LDR_SBREL_11_0: u32 = 35;
```

#### Constant `R_ARM_ALU_SBREL_19_12`

Deprecated, prog. base relative.

```rust
pub const R_ARM_ALU_SBREL_19_12: u32 = 36;
```

#### Constant `R_ARM_ALU_SBREL_27_20`

Deprecated, prog. base relative.

```rust
pub const R_ARM_ALU_SBREL_27_20: u32 = 37;
```

#### Constant `R_ARM_TARGET1`

```rust
pub const R_ARM_TARGET1: u32 = 38;
```

#### Constant `R_ARM_SBREL31`

Program base relative.

```rust
pub const R_ARM_SBREL31: u32 = 39;
```

#### Constant `R_ARM_V4BX`

```rust
pub const R_ARM_V4BX: u32 = 40;
```

#### Constant `R_ARM_TARGET2`

```rust
pub const R_ARM_TARGET2: u32 = 41;
```

#### Constant `R_ARM_PREL31`

32 bit PC relative.

```rust
pub const R_ARM_PREL31: u32 = 42;
```

#### Constant `R_ARM_MOVW_ABS_NC`

Direct 16-bit (`MOVW`).

```rust
pub const R_ARM_MOVW_ABS_NC: u32 = 43;
```

#### Constant `R_ARM_MOVT_ABS`

Direct high 16-bit (`MOVT`).

```rust
pub const R_ARM_MOVT_ABS: u32 = 44;
```

#### Constant `R_ARM_MOVW_PREL_NC`

PC relative 16-bit (`MOVW`).

```rust
pub const R_ARM_MOVW_PREL_NC: u32 = 45;
```

#### Constant `R_ARM_MOVT_PREL`

PC relative (MOVT).

```rust
pub const R_ARM_MOVT_PREL: u32 = 46;
```

#### Constant `R_ARM_THM_MOVW_ABS_NC`

Direct 16 bit (Thumb32 `MOVW`).

```rust
pub const R_ARM_THM_MOVW_ABS_NC: u32 = 47;
```

#### Constant `R_ARM_THM_MOVT_ABS`

Direct high 16 bit (Thumb32 `MOVT`).

```rust
pub const R_ARM_THM_MOVT_ABS: u32 = 48;
```

#### Constant `R_ARM_THM_MOVW_PREL_NC`

PC relative 16 bit (Thumb32 `MOVW`).

```rust
pub const R_ARM_THM_MOVW_PREL_NC: u32 = 49;
```

#### Constant `R_ARM_THM_MOVT_PREL`

PC relative high 16 bit (Thumb32 `MOVT`).

```rust
pub const R_ARM_THM_MOVT_PREL: u32 = 50;
```

#### Constant `R_ARM_THM_JUMP19`

PC relative 20 bit (Thumb32 `B<cond>.W`).

```rust
pub const R_ARM_THM_JUMP19: u32 = 51;
```

#### Constant `R_ARM_THM_JUMP6`

PC relative X & 0x7E (Thumb16 `CBZ`, `CBNZ`).

```rust
pub const R_ARM_THM_JUMP6: u32 = 52;
```

#### Constant `R_ARM_THM_ALU_PREL_11_0`

PC relative 12 bit (Thumb32 `ADR.W`).

```rust
pub const R_ARM_THM_ALU_PREL_11_0: u32 = 53;
```

#### Constant `R_ARM_THM_PC12`

PC relative 12 bit (Thumb32 `LDR{D,SB,H,SH}`).

```rust
pub const R_ARM_THM_PC12: u32 = 54;
```

#### Constant `R_ARM_ABS32_NOI`

Direct 32-bit.

```rust
pub const R_ARM_ABS32_NOI: u32 = 55;
```

#### Constant `R_ARM_REL32_NOI`

PC relative 32-bit.

```rust
pub const R_ARM_REL32_NOI: u32 = 56;
```

#### Constant `R_ARM_ALU_PC_G0_NC`

PC relative (`ADD`, `SUB`).

```rust
pub const R_ARM_ALU_PC_G0_NC: u32 = 57;
```

#### Constant `R_ARM_ALU_PC_G0`

PC relative (`ADD`, `SUB`).

```rust
pub const R_ARM_ALU_PC_G0: u32 = 58;
```

#### Constant `R_ARM_ALU_PC_G1_NC`

PC relative (`ADD`, `SUB`).

```rust
pub const R_ARM_ALU_PC_G1_NC: u32 = 59;
```

#### Constant `R_ARM_ALU_PC_G1`

PC relative (`ADD`, `SUB`).

```rust
pub const R_ARM_ALU_PC_G1: u32 = 60;
```

#### Constant `R_ARM_ALU_PC_G2`

PC relative (`ADD`, `SUB`).

```rust
pub const R_ARM_ALU_PC_G2: u32 = 61;
```

#### Constant `R_ARM_LDR_PC_G1`

PC relative (`LDR`,`STR`,`LDRB`,`STRB`).

```rust
pub const R_ARM_LDR_PC_G1: u32 = 62;
```

#### Constant `R_ARM_LDR_PC_G2`

PC relative (`LDR`,`STR`,`LDRB`,`STRB`).

```rust
pub const R_ARM_LDR_PC_G2: u32 = 63;
```

#### Constant `R_ARM_LDRS_PC_G0`

PC relative (`STR{D,H}`, `LDR{D,SB,H,SH}`).

```rust
pub const R_ARM_LDRS_PC_G0: u32 = 64;
```

#### Constant `R_ARM_LDRS_PC_G1`

PC relative (`STR{D,H}`, `LDR{D,SB,H,SH}`).

```rust
pub const R_ARM_LDRS_PC_G1: u32 = 65;
```

#### Constant `R_ARM_LDRS_PC_G2`

PC relative (`STR{D,H}`, `LDR{D,SB,H,SH}`).

```rust
pub const R_ARM_LDRS_PC_G2: u32 = 66;
```

#### Constant `R_ARM_LDC_PC_G0`

PC relative (`LDC`, `STC`).

```rust
pub const R_ARM_LDC_PC_G0: u32 = 67;
```

#### Constant `R_ARM_LDC_PC_G1`

PC relative (`LDC`, `STC`).

```rust
pub const R_ARM_LDC_PC_G1: u32 = 68;
```

#### Constant `R_ARM_LDC_PC_G2`

PC relative (`LDC`, `STC`).

```rust
pub const R_ARM_LDC_PC_G2: u32 = 69;
```

#### Constant `R_ARM_ALU_SB_G0_NC`

Program base relative (`ADD`,`SUB`).

```rust
pub const R_ARM_ALU_SB_G0_NC: u32 = 70;
```

#### Constant `R_ARM_ALU_SB_G0`

Program base relative (`ADD`,`SUB`).

```rust
pub const R_ARM_ALU_SB_G0: u32 = 71;
```

#### Constant `R_ARM_ALU_SB_G1_NC`

Program base relative (`ADD`,`SUB`).

```rust
pub const R_ARM_ALU_SB_G1_NC: u32 = 72;
```

#### Constant `R_ARM_ALU_SB_G1`

Program base relative (`ADD`,`SUB`).

```rust
pub const R_ARM_ALU_SB_G1: u32 = 73;
```

#### Constant `R_ARM_ALU_SB_G2`

Program base relative (`ADD`,`SUB`).

```rust
pub const R_ARM_ALU_SB_G2: u32 = 74;
```

#### Constant `R_ARM_LDR_SB_G0`

Program base relative (`LDR`, `STR`, `LDRB`, `STRB`).

```rust
pub const R_ARM_LDR_SB_G0: u32 = 75;
```

#### Constant `R_ARM_LDR_SB_G1`

Program base relative (`LDR`, `STR`, `LDRB`, `STRB`).

```rust
pub const R_ARM_LDR_SB_G1: u32 = 76;
```

#### Constant `R_ARM_LDR_SB_G2`

Program base relative (`LDR`, `STR`, `LDRB`, `STRB`).

```rust
pub const R_ARM_LDR_SB_G2: u32 = 77;
```

#### Constant `R_ARM_LDRS_SB_G0`

Program base relative (`LDR`, `STR`, `LDRB`, `STRB`).

```rust
pub const R_ARM_LDRS_SB_G0: u32 = 78;
```

#### Constant `R_ARM_LDRS_SB_G1`

Program base relative (`LDR`, `STR`, `LDRB`, `STRB`).

```rust
pub const R_ARM_LDRS_SB_G1: u32 = 79;
```

#### Constant `R_ARM_LDRS_SB_G2`

Program base relative (`LDR`, `STR`, `LDRB`, `STRB`).

```rust
pub const R_ARM_LDRS_SB_G2: u32 = 80;
```

#### Constant `R_ARM_LDC_SB_G0`

Program base relative (`LDC`,`STC`).

```rust
pub const R_ARM_LDC_SB_G0: u32 = 81;
```

#### Constant `R_ARM_LDC_SB_G1`

Program base relative (`LDC`,`STC`).

```rust
pub const R_ARM_LDC_SB_G1: u32 = 82;
```

#### Constant `R_ARM_LDC_SB_G2`

Program base relative (`LDC`,`STC`).

```rust
pub const R_ARM_LDC_SB_G2: u32 = 83;
```

#### Constant `R_ARM_MOVW_BREL_NC`

Program base relative 16 bit (`MOVW`).

```rust
pub const R_ARM_MOVW_BREL_NC: u32 = 84;
```

#### Constant `R_ARM_MOVT_BREL`

Program base relative high 16 bit (`MOVT`).

```rust
pub const R_ARM_MOVT_BREL: u32 = 85;
```

#### Constant `R_ARM_MOVW_BREL`

Program base relative 16 bit (`MOVW`).

```rust
pub const R_ARM_MOVW_BREL: u32 = 86;
```

#### Constant `R_ARM_THM_MOVW_BREL_NC`

Program base relative 16 bit (Thumb32 `MOVW`).

```rust
pub const R_ARM_THM_MOVW_BREL_NC: u32 = 87;
```

#### Constant `R_ARM_THM_MOVT_BREL`

Program base relative high 16 bit (Thumb32 `MOVT`).

```rust
pub const R_ARM_THM_MOVT_BREL: u32 = 88;
```

#### Constant `R_ARM_THM_MOVW_BREL`

Program base relative 16 bit (Thumb32 `MOVW`).

```rust
pub const R_ARM_THM_MOVW_BREL: u32 = 89;
```

#### Constant `R_ARM_TLS_GOTDESC`

```rust
pub const R_ARM_TLS_GOTDESC: u32 = 90;
```

#### Constant `R_ARM_TLS_CALL`

```rust
pub const R_ARM_TLS_CALL: u32 = 91;
```

#### Constant `R_ARM_TLS_DESCSEQ`

TLS relaxation.

```rust
pub const R_ARM_TLS_DESCSEQ: u32 = 92;
```

#### Constant `R_ARM_THM_TLS_CALL`

```rust
pub const R_ARM_THM_TLS_CALL: u32 = 93;
```

#### Constant `R_ARM_PLT32_ABS`

```rust
pub const R_ARM_PLT32_ABS: u32 = 94;
```

#### Constant `R_ARM_GOT_ABS`

GOT entry.

```rust
pub const R_ARM_GOT_ABS: u32 = 95;
```

#### Constant `R_ARM_GOT_PREL`

PC relative GOT entry.

```rust
pub const R_ARM_GOT_PREL: u32 = 96;
```

#### Constant `R_ARM_GOT_BREL12`

GOT entry relative to GOT origin (`LDR`).

```rust
pub const R_ARM_GOT_BREL12: u32 = 97;
```

#### Constant `R_ARM_GOTOFF12`

12 bit, GOT entry relative to GOT origin (`LDR`, `STR`).

```rust
pub const R_ARM_GOTOFF12: u32 = 98;
```

#### Constant `R_ARM_GOTRELAX`

```rust
pub const R_ARM_GOTRELAX: u32 = 99;
```

#### Constant `R_ARM_GNU_VTENTRY`

```rust
pub const R_ARM_GNU_VTENTRY: u32 = 100;
```

#### Constant `R_ARM_GNU_VTINHERIT`

```rust
pub const R_ARM_GNU_VTINHERIT: u32 = 101;
```

#### Constant `R_ARM_THM_PC11`

PC relative & 0xFFE (Thumb16 `B`).

```rust
pub const R_ARM_THM_PC11: u32 = 102;
```

#### Constant `R_ARM_THM_PC9`

PC relative & 0x1FE (Thumb16 `B`/`B<cond>`).

```rust
pub const R_ARM_THM_PC9: u32 = 103;
```

#### Constant `R_ARM_TLS_GD32`

PC-rel 32 bit for global dynamic thread local data

```rust
pub const R_ARM_TLS_GD32: u32 = 104;
```

#### Constant `R_ARM_TLS_LDM32`

PC-rel 32 bit for local dynamic thread local data

```rust
pub const R_ARM_TLS_LDM32: u32 = 105;
```

#### Constant `R_ARM_TLS_LDO32`

32 bit offset relative to TLS block

```rust
pub const R_ARM_TLS_LDO32: u32 = 106;
```

#### Constant `R_ARM_TLS_IE32`

PC-rel 32 bit for GOT entry of static TLS block offset

```rust
pub const R_ARM_TLS_IE32: u32 = 107;
```

#### Constant `R_ARM_TLS_LE32`

32 bit offset relative to static TLS block

```rust
pub const R_ARM_TLS_LE32: u32 = 108;
```

#### Constant `R_ARM_TLS_LDO12`

12 bit relative to TLS block (`LDR`, `STR`).

```rust
pub const R_ARM_TLS_LDO12: u32 = 109;
```

#### Constant `R_ARM_TLS_LE12`

12 bit relative to static TLS block (`LDR`, `STR`).

```rust
pub const R_ARM_TLS_LE12: u32 = 110;
```

#### Constant `R_ARM_TLS_IE12GP`

12 bit GOT entry relative to GOT origin (`LDR`).

```rust
pub const R_ARM_TLS_IE12GP: u32 = 111;
```

#### Constant `R_ARM_ME_TOO`

Obsolete.

```rust
pub const R_ARM_ME_TOO: u32 = 128;
```

#### Constant `R_ARM_THM_TLS_DESCSEQ`

```rust
pub const R_ARM_THM_TLS_DESCSEQ: u32 = 129;
```

#### Constant `R_ARM_THM_TLS_DESCSEQ16`

```rust
pub const R_ARM_THM_TLS_DESCSEQ16: u32 = 129;
```

#### Constant `R_ARM_THM_TLS_DESCSEQ32`

```rust
pub const R_ARM_THM_TLS_DESCSEQ32: u32 = 130;
```

#### Constant `R_ARM_THM_GOT_BREL12`

GOT entry relative to GOT origin, 12 bit (Thumb32 `LDR`).

```rust
pub const R_ARM_THM_GOT_BREL12: u32 = 131;
```

#### Constant `R_ARM_IRELATIVE`

```rust
pub const R_ARM_IRELATIVE: u32 = 160;
```

#### Constant `R_ARM_RXPC25`

```rust
pub const R_ARM_RXPC25: u32 = 249;
```

#### Constant `R_ARM_RSBREL32`

```rust
pub const R_ARM_RSBREL32: u32 = 250;
```

#### Constant `R_ARM_THM_RPC22`

```rust
pub const R_ARM_THM_RPC22: u32 = 251;
```

#### Constant `R_ARM_RREL32`

```rust
pub const R_ARM_RREL32: u32 = 252;
```

#### Constant `R_ARM_RABS22`

```rust
pub const R_ARM_RABS22: u32 = 253;
```

#### Constant `R_ARM_RPC24`

```rust
pub const R_ARM_RPC24: u32 = 254;
```

#### Constant `R_ARM_RBASE`

```rust
pub const R_ARM_RBASE: u32 = 255;
```

#### Constant `R_CKCORE_NONE`

no reloc

```rust
pub const R_CKCORE_NONE: u32 = 0;
```

#### Constant `R_CKCORE_ADDR32`

direct 32 bit (S + A)

```rust
pub const R_CKCORE_ADDR32: u32 = 1;
```

#### Constant `R_CKCORE_PCRELIMM8BY4`

disp ((S + A - P) >> 2) & 0xff

```rust
pub const R_CKCORE_PCRELIMM8BY4: u32 = 2;
```

#### Constant `R_CKCORE_PCRELIMM11BY2`

disp ((S + A - P) >> 1) & 0x7ff

```rust
pub const R_CKCORE_PCRELIMM11BY2: u32 = 3;
```

#### Constant `R_CKCORE_PCREL32`

32-bit rel (S + A - P)

```rust
pub const R_CKCORE_PCREL32: u32 = 5;
```

#### Constant `R_CKCORE_PCRELJSR_IMM11BY2`

disp ((S + A - P) >>1) & 0x7ff

```rust
pub const R_CKCORE_PCRELJSR_IMM11BY2: u32 = 6;
```

#### Constant `R_CKCORE_RELATIVE`

32 bit adjust program base(B + A)

```rust
pub const R_CKCORE_RELATIVE: u32 = 9;
```

#### Constant `R_CKCORE_COPY`

32 bit adjust by program base

```rust
pub const R_CKCORE_COPY: u32 = 10;
```

#### Constant `R_CKCORE_GLOB_DAT`

off between got and sym (S)

```rust
pub const R_CKCORE_GLOB_DAT: u32 = 11;
```

#### Constant `R_CKCORE_JUMP_SLOT`

PLT entry (S)

```rust
pub const R_CKCORE_JUMP_SLOT: u32 = 12;
```

#### Constant `R_CKCORE_GOTOFF`

offset to GOT (S + A - GOT)

```rust
pub const R_CKCORE_GOTOFF: u32 = 13;
```

#### Constant `R_CKCORE_GOTPC`

PC offset to GOT (GOT + A - P)

```rust
pub const R_CKCORE_GOTPC: u32 = 14;
```

#### Constant `R_CKCORE_GOT32`

32 bit GOT entry (G)

```rust
pub const R_CKCORE_GOT32: u32 = 15;
```

#### Constant `R_CKCORE_PLT32`

32 bit PLT entry (G)

```rust
pub const R_CKCORE_PLT32: u32 = 16;
```

#### Constant `R_CKCORE_ADDRGOT`

GOT entry in GLOB_DAT (GOT + G)

```rust
pub const R_CKCORE_ADDRGOT: u32 = 17;
```

#### Constant `R_CKCORE_ADDRPLT`

PLT entry in GLOB_DAT (GOT + G)

```rust
pub const R_CKCORE_ADDRPLT: u32 = 18;
```

#### Constant `R_CKCORE_PCREL_IMM26BY2`

((S + A - P) >> 1) & 0x3ff_ffff

```rust
pub const R_CKCORE_PCREL_IMM26BY2: u32 = 19;
```

#### Constant `R_CKCORE_PCREL_IMM16BY2`

disp ((S + A - P) >> 1) & 0xffff

```rust
pub const R_CKCORE_PCREL_IMM16BY2: u32 = 20;
```

#### Constant `R_CKCORE_PCREL_IMM16BY4`

disp ((S + A - P) >> 2) & 0xffff

```rust
pub const R_CKCORE_PCREL_IMM16BY4: u32 = 21;
```

#### Constant `R_CKCORE_PCREL_IMM10BY2`

disp ((S + A - P) >> 1) & 0x3ff

```rust
pub const R_CKCORE_PCREL_IMM10BY2: u32 = 22;
```

#### Constant `R_CKCORE_PCREL_IMM10BY4`

disp ((S + A - P) >> 2) & 0x3ff

```rust
pub const R_CKCORE_PCREL_IMM10BY4: u32 = 23;
```

#### Constant `R_CKCORE_ADDR_HI16`

high & low 16 bit ADDR, ((S + A) >> 16) & 0xffff

```rust
pub const R_CKCORE_ADDR_HI16: u32 = 24;
```

#### Constant `R_CKCORE_ADDR_LO16`

(S + A) & 0xffff

```rust
pub const R_CKCORE_ADDR_LO16: u32 = 25;
```

#### Constant `R_CKCORE_GOTPC_HI16`

high & low 16 bit GOTPC, ((GOT + A - P) >> 16) & 0xffff

```rust
pub const R_CKCORE_GOTPC_HI16: u32 = 26;
```

#### Constant `R_CKCORE_GOTPC_LO16`

(GOT + A - P) & 0xffff

```rust
pub const R_CKCORE_GOTPC_LO16: u32 = 27;
```

#### Constant `R_CKCORE_GOTOFF_HI16`

high & low 16 bit GOTOFF, ((S + A - GOT) >> 16) & 0xffff

```rust
pub const R_CKCORE_GOTOFF_HI16: u32 = 28;
```

#### Constant `R_CKCORE_GOTOFF_LO16`

(S + A - GOT) & 0xffff

```rust
pub const R_CKCORE_GOTOFF_LO16: u32 = 29;
```

#### Constant `R_CKCORE_GOT12`

12 bit disp GOT entry (G)

```rust
pub const R_CKCORE_GOT12: u32 = 30;
```

#### Constant `R_CKCORE_GOT_HI16`

high & low 16 bit GOT, (G >> 16) & 0xffff

```rust
pub const R_CKCORE_GOT_HI16: u32 = 31;
```

#### Constant `R_CKCORE_GOT_LO16`

(G & 0xffff)

```rust
pub const R_CKCORE_GOT_LO16: u32 = 32;
```

#### Constant `R_CKCORE_PLT12`

12 bit disp PLT entry (G)

```rust
pub const R_CKCORE_PLT12: u32 = 33;
```

#### Constant `R_CKCORE_PLT_HI16`

high & low 16 bit PLT, (G >> 16) & 0xffff

```rust
pub const R_CKCORE_PLT_HI16: u32 = 34;
```

#### Constant `R_CKCORE_PLT_LO16`

G & 0xffff

```rust
pub const R_CKCORE_PLT_LO16: u32 = 35;
```

#### Constant `R_CKCORE_ADDRGOT_HI16`

high & low 16 bit ADDRGOT, (GOT + G * 4) & 0xffff

```rust
pub const R_CKCORE_ADDRGOT_HI16: u32 = 36;
```

#### Constant `R_CKCORE_ADDRGOT_LO16`

(GOT + G * 4) & 0xffff

```rust
pub const R_CKCORE_ADDRGOT_LO16: u32 = 37;
```

#### Constant `R_CKCORE_ADDRPLT_HI16`

high & low 16 bit ADDRPLT, ((GOT + G * 4) >> 16) & 0xFFFF

```rust
pub const R_CKCORE_ADDRPLT_HI16: u32 = 38;
```

#### Constant `R_CKCORE_ADDRPLT_LO16`

(GOT+G*4) & 0xffff

```rust
pub const R_CKCORE_ADDRPLT_LO16: u32 = 39;
```

#### Constant `R_CKCORE_PCREL_JSR_IMM26BY2`

disp ((S+A-P) >>1) & x3ff_ffff

```rust
pub const R_CKCORE_PCREL_JSR_IMM26BY2: u32 = 40;
```

#### Constant `R_CKCORE_TOFFSET_LO16`

(S+A-BTEXT) & 0xffff

```rust
pub const R_CKCORE_TOFFSET_LO16: u32 = 41;
```

#### Constant `R_CKCORE_DOFFSET_LO16`

(S+A-BTEXT) & 0xffff

```rust
pub const R_CKCORE_DOFFSET_LO16: u32 = 42;
```

#### Constant `R_CKCORE_PCREL_IMM18BY2`

disp ((S+A-P) >>1) & 0x3ffff

```rust
pub const R_CKCORE_PCREL_IMM18BY2: u32 = 43;
```

#### Constant `R_CKCORE_DOFFSET_IMM18`

disp (S+A-BDATA) & 0x3ffff

```rust
pub const R_CKCORE_DOFFSET_IMM18: u32 = 44;
```

#### Constant `R_CKCORE_DOFFSET_IMM18BY2`

disp ((S+A-BDATA)>>1) & 0x3ffff

```rust
pub const R_CKCORE_DOFFSET_IMM18BY2: u32 = 45;
```

#### Constant `R_CKCORE_DOFFSET_IMM18BY4`

disp ((S+A-BDATA)>>2) & 0x3ffff

```rust
pub const R_CKCORE_DOFFSET_IMM18BY4: u32 = 46;
```

#### Constant `R_CKCORE_GOT_IMM18BY4`

disp (G >> 2)

```rust
pub const R_CKCORE_GOT_IMM18BY4: u32 = 48;
```

#### Constant `R_CKCORE_PLT_IMM18BY4`

disp (G >> 2)

```rust
pub const R_CKCORE_PLT_IMM18BY4: u32 = 49;
```

#### Constant `R_CKCORE_PCREL_IMM7BY4`

disp ((S+A-P) >>2) & 0x7f

```rust
pub const R_CKCORE_PCREL_IMM7BY4: u32 = 50;
```

#### Constant `R_CKCORE_TLS_LE32`

32 bit offset to TLS block

```rust
pub const R_CKCORE_TLS_LE32: u32 = 51;
```

#### Constant `R_CKCORE_TLS_IE32`

```rust
pub const R_CKCORE_TLS_IE32: u32 = 52;
```

#### Constant `R_CKCORE_TLS_GD32`

```rust
pub const R_CKCORE_TLS_GD32: u32 = 53;
```

#### Constant `R_CKCORE_TLS_LDM32`

```rust
pub const R_CKCORE_TLS_LDM32: u32 = 54;
```

#### Constant `R_CKCORE_TLS_LDO32`

```rust
pub const R_CKCORE_TLS_LDO32: u32 = 55;
```

#### Constant `R_CKCORE_TLS_DTPMOD32`

```rust
pub const R_CKCORE_TLS_DTPMOD32: u32 = 56;
```

#### Constant `R_CKCORE_TLS_DTPOFF32`

```rust
pub const R_CKCORE_TLS_DTPOFF32: u32 = 57;
```

#### Constant `R_CKCORE_TLS_TPOFF32`

```rust
pub const R_CKCORE_TLS_TPOFF32: u32 = 58;
```

#### Constant `EF_CSKY_ABIMASK`

```rust
pub const EF_CSKY_ABIMASK: u32 = 0xF000_0000;
```

#### Constant `EF_CSKY_OTHER`

```rust
pub const EF_CSKY_OTHER: u32 = 0x0FFF_0000;
```

#### Constant `EF_CSKY_PROCESSOR`

```rust
pub const EF_CSKY_PROCESSOR: u32 = 0x0000_FFFF;
```

#### Constant `EF_CSKY_ABIV1`

```rust
pub const EF_CSKY_ABIV1: u32 = 0x1000_0000;
```

#### Constant `EF_CSKY_ABIV2`

```rust
pub const EF_CSKY_ABIV2: u32 = 0x2000_0000;
```

#### Constant `SHT_CSKY_ATTRIBUTES`

C-SKY attributes section.

```rust
pub const SHT_CSKY_ATTRIBUTES: u32 = _;
```

#### Constant `EF_IA_64_MASKOS`

os-specific flags

```rust
pub const EF_IA_64_MASKOS: u32 = 0x0000_000f;
```

#### Constant `EF_IA_64_ABI64`

64-bit ABI

```rust
pub const EF_IA_64_ABI64: u32 = 0x0000_0010;
```

#### Constant `EF_IA_64_ARCH`

arch. version mask

```rust
pub const EF_IA_64_ARCH: u32 = 0xff00_0000;
```

#### Constant `PT_IA_64_ARCHEXT`

arch extension bits

```rust
pub const PT_IA_64_ARCHEXT: u32 = _;
```

#### Constant `PT_IA_64_UNWIND`

ia64 unwind bits

```rust
pub const PT_IA_64_UNWIND: u32 = _;
```

#### Constant `PT_IA_64_HP_OPT_ANOT`

```rust
pub const PT_IA_64_HP_OPT_ANOT: u32 = _;
```

#### Constant `PT_IA_64_HP_HSL_ANOT`

```rust
pub const PT_IA_64_HP_HSL_ANOT: u32 = _;
```

#### Constant `PT_IA_64_HP_STACK`

```rust
pub const PT_IA_64_HP_STACK: u32 = _;
```

#### Constant `PF_IA_64_NORECOV`

spec insns w/o recovery

```rust
pub const PF_IA_64_NORECOV: u32 = 0x8000_0000;
```

#### Constant `SHT_IA_64_EXT`

extension bits

```rust
pub const SHT_IA_64_EXT: u32 = _;
```

#### Constant `SHT_IA_64_UNWIND`

unwind bits

```rust
pub const SHT_IA_64_UNWIND: u32 = _;
```

#### Constant `SHF_IA_64_SHORT`

section near gp

```rust
pub const SHF_IA_64_SHORT: u32 = 0x1000_0000;
```

#### Constant `SHF_IA_64_NORECOV`

spec insns w/o recovery

```rust
pub const SHF_IA_64_NORECOV: u32 = 0x2000_0000;
```

#### Constant `DT_IA_64_PLT_RESERVE`

```rust
pub const DT_IA_64_PLT_RESERVE: u32 = _;
```

#### Constant `R_IA64_NONE`

none

```rust
pub const R_IA64_NONE: u32 = 0x00;
```

#### Constant `R_IA64_IMM14`

symbol + addend, add imm14

```rust
pub const R_IA64_IMM14: u32 = 0x21;
```

#### Constant `R_IA64_IMM22`

symbol + addend, add imm22

```rust
pub const R_IA64_IMM22: u32 = 0x22;
```

#### Constant `R_IA64_IMM64`

symbol + addend, mov imm64

```rust
pub const R_IA64_IMM64: u32 = 0x23;
```

#### Constant `R_IA64_DIR32MSB`

symbol + addend, data4 MSB

```rust
pub const R_IA64_DIR32MSB: u32 = 0x24;
```

#### Constant `R_IA64_DIR32LSB`

symbol + addend, data4 LSB

```rust
pub const R_IA64_DIR32LSB: u32 = 0x25;
```

#### Constant `R_IA64_DIR64MSB`

symbol + addend, data8 MSB

```rust
pub const R_IA64_DIR64MSB: u32 = 0x26;
```

#### Constant `R_IA64_DIR64LSB`

symbol + addend, data8 LSB

```rust
pub const R_IA64_DIR64LSB: u32 = 0x27;
```

#### Constant `R_IA64_GPREL22`

@gprel(sym + add), add imm22

```rust
pub const R_IA64_GPREL22: u32 = 0x2a;
```

#### Constant `R_IA64_GPREL64I`

@gprel(sym + add), mov imm64

```rust
pub const R_IA64_GPREL64I: u32 = 0x2b;
```

#### Constant `R_IA64_GPREL32MSB`

@gprel(sym + add), data4 MSB

```rust
pub const R_IA64_GPREL32MSB: u32 = 0x2c;
```

#### Constant `R_IA64_GPREL32LSB`

@gprel(sym + add), data4 LSB

```rust
pub const R_IA64_GPREL32LSB: u32 = 0x2d;
```

#### Constant `R_IA64_GPREL64MSB`

@gprel(sym + add), data8 MSB

```rust
pub const R_IA64_GPREL64MSB: u32 = 0x2e;
```

#### Constant `R_IA64_GPREL64LSB`

@gprel(sym + add), data8 LSB

```rust
pub const R_IA64_GPREL64LSB: u32 = 0x2f;
```

#### Constant `R_IA64_LTOFF22`

@ltoff(sym + add), add imm22

```rust
pub const R_IA64_LTOFF22: u32 = 0x32;
```

#### Constant `R_IA64_LTOFF64I`

@ltoff(sym + add), mov imm64

```rust
pub const R_IA64_LTOFF64I: u32 = 0x33;
```

#### Constant `R_IA64_PLTOFF22`

@pltoff(sym + add), add imm22

```rust
pub const R_IA64_PLTOFF22: u32 = 0x3a;
```

#### Constant `R_IA64_PLTOFF64I`

@pltoff(sym + add), mov imm64

```rust
pub const R_IA64_PLTOFF64I: u32 = 0x3b;
```

#### Constant `R_IA64_PLTOFF64MSB`

@pltoff(sym + add), data8 MSB

```rust
pub const R_IA64_PLTOFF64MSB: u32 = 0x3e;
```

#### Constant `R_IA64_PLTOFF64LSB`

@pltoff(sym + add), data8 LSB

```rust
pub const R_IA64_PLTOFF64LSB: u32 = 0x3f;
```

#### Constant `R_IA64_FPTR64I`

@fptr(sym + add), mov imm64

```rust
pub const R_IA64_FPTR64I: u32 = 0x43;
```

#### Constant `R_IA64_FPTR32MSB`

@fptr(sym + add), data4 MSB

```rust
pub const R_IA64_FPTR32MSB: u32 = 0x44;
```

#### Constant `R_IA64_FPTR32LSB`

@fptr(sym + add), data4 LSB

```rust
pub const R_IA64_FPTR32LSB: u32 = 0x45;
```

#### Constant `R_IA64_FPTR64MSB`

@fptr(sym + add), data8 MSB

```rust
pub const R_IA64_FPTR64MSB: u32 = 0x46;
```

#### Constant `R_IA64_FPTR64LSB`

@fptr(sym + add), data8 LSB

```rust
pub const R_IA64_FPTR64LSB: u32 = 0x47;
```

#### Constant `R_IA64_PCREL60B`

@pcrel(sym + add), brl

```rust
pub const R_IA64_PCREL60B: u32 = 0x48;
```

#### Constant `R_IA64_PCREL21B`

@pcrel(sym + add), ptb, call

```rust
pub const R_IA64_PCREL21B: u32 = 0x49;
```

#### Constant `R_IA64_PCREL21M`

@pcrel(sym + add), chk.s

```rust
pub const R_IA64_PCREL21M: u32 = 0x4a;
```

#### Constant `R_IA64_PCREL21F`

@pcrel(sym + add), fchkf

```rust
pub const R_IA64_PCREL21F: u32 = 0x4b;
```

#### Constant `R_IA64_PCREL32MSB`

@pcrel(sym + add), data4 MSB

```rust
pub const R_IA64_PCREL32MSB: u32 = 0x4c;
```

#### Constant `R_IA64_PCREL32LSB`

@pcrel(sym + add), data4 LSB

```rust
pub const R_IA64_PCREL32LSB: u32 = 0x4d;
```

#### Constant `R_IA64_PCREL64MSB`

@pcrel(sym + add), data8 MSB

```rust
pub const R_IA64_PCREL64MSB: u32 = 0x4e;
```

#### Constant `R_IA64_PCREL64LSB`

@pcrel(sym + add), data8 LSB

```rust
pub const R_IA64_PCREL64LSB: u32 = 0x4f;
```

#### Constant `R_IA64_LTOFF_FPTR22`

@ltoff(@fptr(s+a)), imm22

```rust
pub const R_IA64_LTOFF_FPTR22: u32 = 0x52;
```

#### Constant `R_IA64_LTOFF_FPTR64I`

@ltoff(@fptr(s+a)), imm64

```rust
pub const R_IA64_LTOFF_FPTR64I: u32 = 0x53;
```

#### Constant `R_IA64_LTOFF_FPTR32MSB`

@ltoff(@fptr(s+a)), data4 MSB

```rust
pub const R_IA64_LTOFF_FPTR32MSB: u32 = 0x54;
```

#### Constant `R_IA64_LTOFF_FPTR32LSB`

@ltoff(@fptr(s+a)), data4 LSB

```rust
pub const R_IA64_LTOFF_FPTR32LSB: u32 = 0x55;
```

#### Constant `R_IA64_LTOFF_FPTR64MSB`

@ltoff(@fptr(s+a)), data8 MSB

```rust
pub const R_IA64_LTOFF_FPTR64MSB: u32 = 0x56;
```

#### Constant `R_IA64_LTOFF_FPTR64LSB`

@ltoff(@fptr(s+a)), data8 LSB

```rust
pub const R_IA64_LTOFF_FPTR64LSB: u32 = 0x57;
```

#### Constant `R_IA64_SEGREL32MSB`

@segrel(sym + add), data4 MSB

```rust
pub const R_IA64_SEGREL32MSB: u32 = 0x5c;
```

#### Constant `R_IA64_SEGREL32LSB`

@segrel(sym + add), data4 LSB

```rust
pub const R_IA64_SEGREL32LSB: u32 = 0x5d;
```

#### Constant `R_IA64_SEGREL64MSB`

@segrel(sym + add), data8 MSB

```rust
pub const R_IA64_SEGREL64MSB: u32 = 0x5e;
```

#### Constant `R_IA64_SEGREL64LSB`

@segrel(sym + add), data8 LSB

```rust
pub const R_IA64_SEGREL64LSB: u32 = 0x5f;
```

#### Constant `R_IA64_SECREL32MSB`

@secrel(sym + add), data4 MSB

```rust
pub const R_IA64_SECREL32MSB: u32 = 0x64;
```

#### Constant `R_IA64_SECREL32LSB`

@secrel(sym + add), data4 LSB

```rust
pub const R_IA64_SECREL32LSB: u32 = 0x65;
```

#### Constant `R_IA64_SECREL64MSB`

@secrel(sym + add), data8 MSB

```rust
pub const R_IA64_SECREL64MSB: u32 = 0x66;
```

#### Constant `R_IA64_SECREL64LSB`

@secrel(sym + add), data8 LSB

```rust
pub const R_IA64_SECREL64LSB: u32 = 0x67;
```

#### Constant `R_IA64_REL32MSB`

data 4 + REL

```rust
pub const R_IA64_REL32MSB: u32 = 0x6c;
```

#### Constant `R_IA64_REL32LSB`

data 4 + REL

```rust
pub const R_IA64_REL32LSB: u32 = 0x6d;
```

#### Constant `R_IA64_REL64MSB`

data 8 + REL

```rust
pub const R_IA64_REL64MSB: u32 = 0x6e;
```

#### Constant `R_IA64_REL64LSB`

data 8 + REL

```rust
pub const R_IA64_REL64LSB: u32 = 0x6f;
```

#### Constant `R_IA64_LTV32MSB`

symbol + addend, data4 MSB

```rust
pub const R_IA64_LTV32MSB: u32 = 0x74;
```

#### Constant `R_IA64_LTV32LSB`

symbol + addend, data4 LSB

```rust
pub const R_IA64_LTV32LSB: u32 = 0x75;
```

#### Constant `R_IA64_LTV64MSB`

symbol + addend, data8 MSB

```rust
pub const R_IA64_LTV64MSB: u32 = 0x76;
```

#### Constant `R_IA64_LTV64LSB`

symbol + addend, data8 LSB

```rust
pub const R_IA64_LTV64LSB: u32 = 0x77;
```

#### Constant `R_IA64_PCREL21BI`

@pcrel(sym + add), 21bit inst

```rust
pub const R_IA64_PCREL21BI: u32 = 0x79;
```

#### Constant `R_IA64_PCREL22`

@pcrel(sym + add), 22bit inst

```rust
pub const R_IA64_PCREL22: u32 = 0x7a;
```

#### Constant `R_IA64_PCREL64I`

@pcrel(sym + add), 64bit inst

```rust
pub const R_IA64_PCREL64I: u32 = 0x7b;
```

#### Constant `R_IA64_IPLTMSB`

dynamic reloc, imported PLT, MSB

```rust
pub const R_IA64_IPLTMSB: u32 = 0x80;
```

#### Constant `R_IA64_IPLTLSB`

dynamic reloc, imported PLT, LSB

```rust
pub const R_IA64_IPLTLSB: u32 = 0x81;
```

#### Constant `R_IA64_COPY`

copy relocation

```rust
pub const R_IA64_COPY: u32 = 0x84;
```

#### Constant `R_IA64_SUB`

Addend and symbol difference

```rust
pub const R_IA64_SUB: u32 = 0x85;
```

#### Constant `R_IA64_LTOFF22X`

LTOFF22, relaxable.

```rust
pub const R_IA64_LTOFF22X: u32 = 0x86;
```

#### Constant `R_IA64_LDXMOV`

Use of LTOFF22X.

```rust
pub const R_IA64_LDXMOV: u32 = 0x87;
```

#### Constant `R_IA64_TPREL14`

@tprel(sym + add), imm14

```rust
pub const R_IA64_TPREL14: u32 = 0x91;
```

#### Constant `R_IA64_TPREL22`

@tprel(sym + add), imm22

```rust
pub const R_IA64_TPREL22: u32 = 0x92;
```

#### Constant `R_IA64_TPREL64I`

@tprel(sym + add), imm64

```rust
pub const R_IA64_TPREL64I: u32 = 0x93;
```

#### Constant `R_IA64_TPREL64MSB`

@tprel(sym + add), data8 MSB

```rust
pub const R_IA64_TPREL64MSB: u32 = 0x96;
```

#### Constant `R_IA64_TPREL64LSB`

@tprel(sym + add), data8 LSB

```rust
pub const R_IA64_TPREL64LSB: u32 = 0x97;
```

#### Constant `R_IA64_LTOFF_TPREL22`

@ltoff(@tprel(s+a)), imm2

```rust
pub const R_IA64_LTOFF_TPREL22: u32 = 0x9a;
```

#### Constant `R_IA64_DTPMOD64MSB`

@dtpmod(sym + add), data8 MSB

```rust
pub const R_IA64_DTPMOD64MSB: u32 = 0xa6;
```

#### Constant `R_IA64_DTPMOD64LSB`

@dtpmod(sym + add), data8 LSB

```rust
pub const R_IA64_DTPMOD64LSB: u32 = 0xa7;
```

#### Constant `R_IA64_LTOFF_DTPMOD22`

@ltoff(@dtpmod(sym + add)), imm22

```rust
pub const R_IA64_LTOFF_DTPMOD22: u32 = 0xaa;
```

#### Constant `R_IA64_DTPREL14`

@dtprel(sym + add), imm14

```rust
pub const R_IA64_DTPREL14: u32 = 0xb1;
```

#### Constant `R_IA64_DTPREL22`

@dtprel(sym + add), imm22

```rust
pub const R_IA64_DTPREL22: u32 = 0xb2;
```

#### Constant `R_IA64_DTPREL64I`

@dtprel(sym + add), imm64

```rust
pub const R_IA64_DTPREL64I: u32 = 0xb3;
```

#### Constant `R_IA64_DTPREL32MSB`

@dtprel(sym + add), data4 MSB

```rust
pub const R_IA64_DTPREL32MSB: u32 = 0xb4;
```

#### Constant `R_IA64_DTPREL32LSB`

@dtprel(sym + add), data4 LSB

```rust
pub const R_IA64_DTPREL32LSB: u32 = 0xb5;
```

#### Constant `R_IA64_DTPREL64MSB`

@dtprel(sym + add), data8 MSB

```rust
pub const R_IA64_DTPREL64MSB: u32 = 0xb6;
```

#### Constant `R_IA64_DTPREL64LSB`

@dtprel(sym + add), data8 LSB

```rust
pub const R_IA64_DTPREL64LSB: u32 = 0xb7;
```

#### Constant `R_IA64_LTOFF_DTPREL22`

@ltoff(@dtprel(s+a)), imm22

```rust
pub const R_IA64_LTOFF_DTPREL22: u32 = 0xba;
```

#### Constant `EF_SH_MACH_MASK`

```rust
pub const EF_SH_MACH_MASK: u32 = 0x1f;
```

#### Constant `EF_SH_UNKNOWN`

```rust
pub const EF_SH_UNKNOWN: u32 = 0x0;
```

#### Constant `EF_SH1`

```rust
pub const EF_SH1: u32 = 0x1;
```

#### Constant `EF_SH2`

```rust
pub const EF_SH2: u32 = 0x2;
```

#### Constant `EF_SH3`

```rust
pub const EF_SH3: u32 = 0x3;
```

#### Constant `EF_SH_DSP`

```rust
pub const EF_SH_DSP: u32 = 0x4;
```

#### Constant `EF_SH3_DSP`

```rust
pub const EF_SH3_DSP: u32 = 0x5;
```

#### Constant `EF_SH4AL_DSP`

```rust
pub const EF_SH4AL_DSP: u32 = 0x6;
```

#### Constant `EF_SH3E`

```rust
pub const EF_SH3E: u32 = 0x8;
```

#### Constant `EF_SH4`

```rust
pub const EF_SH4: u32 = 0x9;
```

#### Constant `EF_SH2E`

```rust
pub const EF_SH2E: u32 = 0xb;
```

#### Constant `EF_SH4A`

```rust
pub const EF_SH4A: u32 = 0xc;
```

#### Constant `EF_SH2A`

```rust
pub const EF_SH2A: u32 = 0xd;
```

#### Constant `EF_SH4_NOFPU`

```rust
pub const EF_SH4_NOFPU: u32 = 0x10;
```

#### Constant `EF_SH4A_NOFPU`

```rust
pub const EF_SH4A_NOFPU: u32 = 0x11;
```

#### Constant `EF_SH4_NOMMU_NOFPU`

```rust
pub const EF_SH4_NOMMU_NOFPU: u32 = 0x12;
```

#### Constant `EF_SH2A_NOFPU`

```rust
pub const EF_SH2A_NOFPU: u32 = 0x13;
```

#### Constant `EF_SH3_NOMMU`

```rust
pub const EF_SH3_NOMMU: u32 = 0x14;
```

#### Constant `EF_SH2A_SH4_NOFPU`

```rust
pub const EF_SH2A_SH4_NOFPU: u32 = 0x15;
```

#### Constant `EF_SH2A_SH3_NOFPU`

```rust
pub const EF_SH2A_SH3_NOFPU: u32 = 0x16;
```

#### Constant `EF_SH2A_SH4`

```rust
pub const EF_SH2A_SH4: u32 = 0x17;
```

#### Constant `EF_SH2A_SH3E`

```rust
pub const EF_SH2A_SH3E: u32 = 0x18;
```

#### Constant `R_SH_NONE`

```rust
pub const R_SH_NONE: u32 = 0;
```

#### Constant `R_SH_DIR32`

```rust
pub const R_SH_DIR32: u32 = 1;
```

#### Constant `R_SH_REL32`

```rust
pub const R_SH_REL32: u32 = 2;
```

#### Constant `R_SH_DIR8WPN`

```rust
pub const R_SH_DIR8WPN: u32 = 3;
```

#### Constant `R_SH_IND12W`

```rust
pub const R_SH_IND12W: u32 = 4;
```

#### Constant `R_SH_DIR8WPL`

```rust
pub const R_SH_DIR8WPL: u32 = 5;
```

#### Constant `R_SH_DIR8WPZ`

```rust
pub const R_SH_DIR8WPZ: u32 = 6;
```

#### Constant `R_SH_DIR8BP`

```rust
pub const R_SH_DIR8BP: u32 = 7;
```

#### Constant `R_SH_DIR8W`

```rust
pub const R_SH_DIR8W: u32 = 8;
```

#### Constant `R_SH_DIR8L`

```rust
pub const R_SH_DIR8L: u32 = 9;
```

#### Constant `R_SH_SWITCH16`

```rust
pub const R_SH_SWITCH16: u32 = 25;
```

#### Constant `R_SH_SWITCH32`

```rust
pub const R_SH_SWITCH32: u32 = 26;
```

#### Constant `R_SH_USES`

```rust
pub const R_SH_USES: u32 = 27;
```

#### Constant `R_SH_COUNT`

```rust
pub const R_SH_COUNT: u32 = 28;
```

#### Constant `R_SH_ALIGN`

```rust
pub const R_SH_ALIGN: u32 = 29;
```

#### Constant `R_SH_CODE`

```rust
pub const R_SH_CODE: u32 = 30;
```

#### Constant `R_SH_DATA`

```rust
pub const R_SH_DATA: u32 = 31;
```

#### Constant `R_SH_LABEL`

```rust
pub const R_SH_LABEL: u32 = 32;
```

#### Constant `R_SH_SWITCH8`

```rust
pub const R_SH_SWITCH8: u32 = 33;
```

#### Constant `R_SH_GNU_VTINHERIT`

```rust
pub const R_SH_GNU_VTINHERIT: u32 = 34;
```

#### Constant `R_SH_GNU_VTENTRY`

```rust
pub const R_SH_GNU_VTENTRY: u32 = 35;
```

#### Constant `R_SH_TLS_GD_32`

```rust
pub const R_SH_TLS_GD_32: u32 = 144;
```

#### Constant `R_SH_TLS_LD_32`

```rust
pub const R_SH_TLS_LD_32: u32 = 145;
```

#### Constant `R_SH_TLS_LDO_32`

```rust
pub const R_SH_TLS_LDO_32: u32 = 146;
```

#### Constant `R_SH_TLS_IE_32`

```rust
pub const R_SH_TLS_IE_32: u32 = 147;
```

#### Constant `R_SH_TLS_LE_32`

```rust
pub const R_SH_TLS_LE_32: u32 = 148;
```

#### Constant `R_SH_TLS_DTPMOD32`

```rust
pub const R_SH_TLS_DTPMOD32: u32 = 149;
```

#### Constant `R_SH_TLS_DTPOFF32`

```rust
pub const R_SH_TLS_DTPOFF32: u32 = 150;
```

#### Constant `R_SH_TLS_TPOFF32`

```rust
pub const R_SH_TLS_TPOFF32: u32 = 151;
```

#### Constant `R_SH_GOT32`

```rust
pub const R_SH_GOT32: u32 = 160;
```

#### Constant `R_SH_PLT32`

```rust
pub const R_SH_PLT32: u32 = 161;
```

#### Constant `R_SH_COPY`

```rust
pub const R_SH_COPY: u32 = 162;
```

#### Constant `R_SH_GLOB_DAT`

```rust
pub const R_SH_GLOB_DAT: u32 = 163;
```

#### Constant `R_SH_JMP_SLOT`

```rust
pub const R_SH_JMP_SLOT: u32 = 164;
```

#### Constant `R_SH_RELATIVE`

```rust
pub const R_SH_RELATIVE: u32 = 165;
```

#### Constant `R_SH_GOTOFF`

```rust
pub const R_SH_GOTOFF: u32 = 166;
```

#### Constant `R_SH_GOTPC`

```rust
pub const R_SH_GOTPC: u32 = 167;
```

#### Constant `EF_S390_HIGH_GPRS`

High GPRs kernel facility needed.

```rust
pub const EF_S390_HIGH_GPRS: u32 = 0x0000_0001;
```

#### Constant `R_390_NONE`

No reloc.

```rust
pub const R_390_NONE: u32 = 0;
```

#### Constant `R_390_8`

Direct 8 bit.

```rust
pub const R_390_8: u32 = 1;
```

#### Constant `R_390_12`

Direct 12 bit.

```rust
pub const R_390_12: u32 = 2;
```

#### Constant `R_390_16`

Direct 16 bit.

```rust
pub const R_390_16: u32 = 3;
```

#### Constant `R_390_32`

Direct 32 bit.

```rust
pub const R_390_32: u32 = 4;
```

#### Constant `R_390_PC32`

PC relative 32 bit.

```rust
pub const R_390_PC32: u32 = 5;
```

#### Constant `R_390_GOT12`

12 bit GOT offset.

```rust
pub const R_390_GOT12: u32 = 6;
```

#### Constant `R_390_GOT32`

32 bit GOT offset.

```rust
pub const R_390_GOT32: u32 = 7;
```

#### Constant `R_390_PLT32`

32 bit PC relative PLT address.

```rust
pub const R_390_PLT32: u32 = 8;
```

#### Constant `R_390_COPY`

Copy symbol at runtime.

```rust
pub const R_390_COPY: u32 = 9;
```

#### Constant `R_390_GLOB_DAT`

Create GOT entry.

```rust
pub const R_390_GLOB_DAT: u32 = 10;
```

#### Constant `R_390_JMP_SLOT`

Create PLT entry.

```rust
pub const R_390_JMP_SLOT: u32 = 11;
```

#### Constant `R_390_RELATIVE`

Adjust by program base.

```rust
pub const R_390_RELATIVE: u32 = 12;
```

#### Constant `R_390_GOTOFF32`

32 bit offset to GOT.

```rust
pub const R_390_GOTOFF32: u32 = 13;
```

#### Constant `R_390_GOTPC`

32 bit PC relative offset to GOT.

```rust
pub const R_390_GOTPC: u32 = 14;
```

#### Constant `R_390_GOT16`

16 bit GOT offset.

```rust
pub const R_390_GOT16: u32 = 15;
```

#### Constant `R_390_PC16`

PC relative 16 bit.

```rust
pub const R_390_PC16: u32 = 16;
```

#### Constant `R_390_PC16DBL`

PC relative 16 bit shifted by 1.

```rust
pub const R_390_PC16DBL: u32 = 17;
```

#### Constant `R_390_PLT16DBL`

16 bit PC rel. PLT shifted by 1.

```rust
pub const R_390_PLT16DBL: u32 = 18;
```

#### Constant `R_390_PC32DBL`

PC relative 32 bit shifted by 1.

```rust
pub const R_390_PC32DBL: u32 = 19;
```

#### Constant `R_390_PLT32DBL`

32 bit PC rel. PLT shifted by 1.

```rust
pub const R_390_PLT32DBL: u32 = 20;
```

#### Constant `R_390_GOTPCDBL`

32 bit PC rel. GOT shifted by 1.

```rust
pub const R_390_GOTPCDBL: u32 = 21;
```

#### Constant `R_390_64`

Direct 64 bit.

```rust
pub const R_390_64: u32 = 22;
```

#### Constant `R_390_PC64`

PC relative 64 bit.

```rust
pub const R_390_PC64: u32 = 23;
```

#### Constant `R_390_GOT64`

64 bit GOT offset.

```rust
pub const R_390_GOT64: u32 = 24;
```

#### Constant `R_390_PLT64`

64 bit PC relative PLT address.

```rust
pub const R_390_PLT64: u32 = 25;
```

#### Constant `R_390_GOTENT`

32 bit PC rel. to GOT entry >> 1.

```rust
pub const R_390_GOTENT: u32 = 26;
```

#### Constant `R_390_GOTOFF16`

16 bit offset to GOT.

```rust
pub const R_390_GOTOFF16: u32 = 27;
```

#### Constant `R_390_GOTOFF64`

64 bit offset to GOT.

```rust
pub const R_390_GOTOFF64: u32 = 28;
```

#### Constant `R_390_GOTPLT12`

12 bit offset to jump slot.

```rust
pub const R_390_GOTPLT12: u32 = 29;
```

#### Constant `R_390_GOTPLT16`

16 bit offset to jump slot.

```rust
pub const R_390_GOTPLT16: u32 = 30;
```

#### Constant `R_390_GOTPLT32`

32 bit offset to jump slot.

```rust
pub const R_390_GOTPLT32: u32 = 31;
```

#### Constant `R_390_GOTPLT64`

64 bit offset to jump slot.

```rust
pub const R_390_GOTPLT64: u32 = 32;
```

#### Constant `R_390_GOTPLTENT`

32 bit rel. offset to jump slot.

```rust
pub const R_390_GOTPLTENT: u32 = 33;
```

#### Constant `R_390_PLTOFF16`

16 bit offset from GOT to PLT.

```rust
pub const R_390_PLTOFF16: u32 = 34;
```

#### Constant `R_390_PLTOFF32`

32 bit offset from GOT to PLT.

```rust
pub const R_390_PLTOFF32: u32 = 35;
```

#### Constant `R_390_PLTOFF64`

16 bit offset from GOT to PLT.

```rust
pub const R_390_PLTOFF64: u32 = 36;
```

#### Constant `R_390_TLS_LOAD`

Tag for load insn in TLS code.

```rust
pub const R_390_TLS_LOAD: u32 = 37;
```

#### Constant `R_390_TLS_GDCALL`

Tag for function call in general dynamic TLS code.

```rust
pub const R_390_TLS_GDCALL: u32 = 38;
```

#### Constant `R_390_TLS_LDCALL`

Tag for function call in local dynamic TLS code.

```rust
pub const R_390_TLS_LDCALL: u32 = 39;
```

#### Constant `R_390_TLS_GD32`

Direct 32 bit for general dynamic thread local data.

```rust
pub const R_390_TLS_GD32: u32 = 40;
```

#### Constant `R_390_TLS_GD64`

Direct 64 bit for general dynamic thread local data.

```rust
pub const R_390_TLS_GD64: u32 = 41;
```

#### Constant `R_390_TLS_GOTIE12`

12 bit GOT offset for static TLS block offset.

```rust
pub const R_390_TLS_GOTIE12: u32 = 42;
```

#### Constant `R_390_TLS_GOTIE32`

32 bit GOT offset for static TLS block offset.

```rust
pub const R_390_TLS_GOTIE32: u32 = 43;
```

#### Constant `R_390_TLS_GOTIE64`

64 bit GOT offset for static TLS block offset.

```rust
pub const R_390_TLS_GOTIE64: u32 = 44;
```

#### Constant `R_390_TLS_LDM32`

Direct 32 bit for local dynamic thread local data in LE code.

```rust
pub const R_390_TLS_LDM32: u32 = 45;
```

#### Constant `R_390_TLS_LDM64`

Direct 64 bit for local dynamic thread local data in LE code.

```rust
pub const R_390_TLS_LDM64: u32 = 46;
```

#### Constant `R_390_TLS_IE32`

32 bit address of GOT entry for negated static TLS block offset.

```rust
pub const R_390_TLS_IE32: u32 = 47;
```

#### Constant `R_390_TLS_IE64`

64 bit address of GOT entry for negated static TLS block offset.

```rust
pub const R_390_TLS_IE64: u32 = 48;
```

#### Constant `R_390_TLS_IEENT`

32 bit rel. offset to GOT entry for negated static TLS block offset.

```rust
pub const R_390_TLS_IEENT: u32 = 49;
```

#### Constant `R_390_TLS_LE32`

32 bit negated offset relative to static TLS block.

```rust
pub const R_390_TLS_LE32: u32 = 50;
```

#### Constant `R_390_TLS_LE64`

64 bit negated offset relative to static TLS block.

```rust
pub const R_390_TLS_LE64: u32 = 51;
```

#### Constant `R_390_TLS_LDO32`

32 bit offset relative to TLS block.

```rust
pub const R_390_TLS_LDO32: u32 = 52;
```

#### Constant `R_390_TLS_LDO64`

64 bit offset relative to TLS block.

```rust
pub const R_390_TLS_LDO64: u32 = 53;
```

#### Constant `R_390_TLS_DTPMOD`

ID of module containing symbol.

```rust
pub const R_390_TLS_DTPMOD: u32 = 54;
```

#### Constant `R_390_TLS_DTPOFF`

Offset in TLS block.

```rust
pub const R_390_TLS_DTPOFF: u32 = 55;
```

#### Constant `R_390_TLS_TPOFF`

Negated offset in static TLS block.

```rust
pub const R_390_TLS_TPOFF: u32 = 56;
```

#### Constant `R_390_20`

Direct 20 bit.

```rust
pub const R_390_20: u32 = 57;
```

#### Constant `R_390_GOT20`

20 bit GOT offset.

```rust
pub const R_390_GOT20: u32 = 58;
```

#### Constant `R_390_GOTPLT20`

20 bit offset to jump slot.

```rust
pub const R_390_GOTPLT20: u32 = 59;
```

#### Constant `R_390_TLS_GOTIE20`

20 bit GOT offset for static TLS block offset.

```rust
pub const R_390_TLS_GOTIE20: u32 = 60;
```

#### Constant `R_390_IRELATIVE`

STT_GNU_IFUNC relocation.

```rust
pub const R_390_IRELATIVE: u32 = 61;
```

#### Constant `R_CRIS_NONE`

```rust
pub const R_CRIS_NONE: u32 = 0;
```

#### Constant `R_CRIS_8`

```rust
pub const R_CRIS_8: u32 = 1;
```

#### Constant `R_CRIS_16`

```rust
pub const R_CRIS_16: u32 = 2;
```

#### Constant `R_CRIS_32`

```rust
pub const R_CRIS_32: u32 = 3;
```

#### Constant `R_CRIS_8_PCREL`

```rust
pub const R_CRIS_8_PCREL: u32 = 4;
```

#### Constant `R_CRIS_16_PCREL`

```rust
pub const R_CRIS_16_PCREL: u32 = 5;
```

#### Constant `R_CRIS_32_PCREL`

```rust
pub const R_CRIS_32_PCREL: u32 = 6;
```

#### Constant `R_CRIS_GNU_VTINHERIT`

```rust
pub const R_CRIS_GNU_VTINHERIT: u32 = 7;
```

#### Constant `R_CRIS_GNU_VTENTRY`

```rust
pub const R_CRIS_GNU_VTENTRY: u32 = 8;
```

#### Constant `R_CRIS_COPY`

```rust
pub const R_CRIS_COPY: u32 = 9;
```

#### Constant `R_CRIS_GLOB_DAT`

```rust
pub const R_CRIS_GLOB_DAT: u32 = 10;
```

#### Constant `R_CRIS_JUMP_SLOT`

```rust
pub const R_CRIS_JUMP_SLOT: u32 = 11;
```

#### Constant `R_CRIS_RELATIVE`

```rust
pub const R_CRIS_RELATIVE: u32 = 12;
```

#### Constant `R_CRIS_16_GOT`

```rust
pub const R_CRIS_16_GOT: u32 = 13;
```

#### Constant `R_CRIS_32_GOT`

```rust
pub const R_CRIS_32_GOT: u32 = 14;
```

#### Constant `R_CRIS_16_GOTPLT`

```rust
pub const R_CRIS_16_GOTPLT: u32 = 15;
```

#### Constant `R_CRIS_32_GOTPLT`

```rust
pub const R_CRIS_32_GOTPLT: u32 = 16;
```

#### Constant `R_CRIS_32_GOTREL`

```rust
pub const R_CRIS_32_GOTREL: u32 = 17;
```

#### Constant `R_CRIS_32_PLT_GOTREL`

```rust
pub const R_CRIS_32_PLT_GOTREL: u32 = 18;
```

#### Constant `R_CRIS_32_PLT_PCREL`

```rust
pub const R_CRIS_32_PLT_PCREL: u32 = 19;
```

#### Constant `R_X86_64_NONE`

No reloc

```rust
pub const R_X86_64_NONE: u32 = 0;
```

#### Constant `R_X86_64_64`

Direct 64 bit

```rust
pub const R_X86_64_64: u32 = 1;
```

#### Constant `R_X86_64_PC32`

PC relative 32 bit signed

```rust
pub const R_X86_64_PC32: u32 = 2;
```

#### Constant `R_X86_64_GOT32`

32 bit GOT entry

```rust
pub const R_X86_64_GOT32: u32 = 3;
```

#### Constant `R_X86_64_PLT32`

32 bit PLT address

```rust
pub const R_X86_64_PLT32: u32 = 4;
```

#### Constant `R_X86_64_COPY`

Copy symbol at runtime

```rust
pub const R_X86_64_COPY: u32 = 5;
```

#### Constant `R_X86_64_GLOB_DAT`

Create GOT entry

```rust
pub const R_X86_64_GLOB_DAT: u32 = 6;
```

#### Constant `R_X86_64_JUMP_SLOT`

Create PLT entry

```rust
pub const R_X86_64_JUMP_SLOT: u32 = 7;
```

#### Constant `R_X86_64_RELATIVE`

Adjust by program base

```rust
pub const R_X86_64_RELATIVE: u32 = 8;
```

#### Constant `R_X86_64_GOTPCREL`

32 bit signed PC relative offset to GOT

```rust
pub const R_X86_64_GOTPCREL: u32 = 9;
```

#### Constant `R_X86_64_32`

Direct 32 bit zero extended

```rust
pub const R_X86_64_32: u32 = 10;
```

#### Constant `R_X86_64_32S`

Direct 32 bit sign extended

```rust
pub const R_X86_64_32S: u32 = 11;
```

#### Constant `R_X86_64_16`

Direct 16 bit zero extended

```rust
pub const R_X86_64_16: u32 = 12;
```

#### Constant `R_X86_64_PC16`

16 bit sign extended pc relative

```rust
pub const R_X86_64_PC16: u32 = 13;
```

#### Constant `R_X86_64_8`

Direct 8 bit sign extended

```rust
pub const R_X86_64_8: u32 = 14;
```

#### Constant `R_X86_64_PC8`

8 bit sign extended pc relative

```rust
pub const R_X86_64_PC8: u32 = 15;
```

#### Constant `R_X86_64_DTPMOD64`

ID of module containing symbol

```rust
pub const R_X86_64_DTPMOD64: u32 = 16;
```

#### Constant `R_X86_64_DTPOFF64`

Offset in module's TLS block

```rust
pub const R_X86_64_DTPOFF64: u32 = 17;
```

#### Constant `R_X86_64_TPOFF64`

Offset in initial TLS block

```rust
pub const R_X86_64_TPOFF64: u32 = 18;
```

#### Constant `R_X86_64_TLSGD`

32 bit signed PC relative offset to two GOT entries for GD symbol

```rust
pub const R_X86_64_TLSGD: u32 = 19;
```

#### Constant `R_X86_64_TLSLD`

32 bit signed PC relative offset to two GOT entries for LD symbol

```rust
pub const R_X86_64_TLSLD: u32 = 20;
```

#### Constant `R_X86_64_DTPOFF32`

Offset in TLS block

```rust
pub const R_X86_64_DTPOFF32: u32 = 21;
```

#### Constant `R_X86_64_GOTTPOFF`

32 bit signed PC relative offset to GOT entry for IE symbol

```rust
pub const R_X86_64_GOTTPOFF: u32 = 22;
```

#### Constant `R_X86_64_TPOFF32`

Offset in initial TLS block

```rust
pub const R_X86_64_TPOFF32: u32 = 23;
```

#### Constant `R_X86_64_PC64`

PC relative 64 bit

```rust
pub const R_X86_64_PC64: u32 = 24;
```

#### Constant `R_X86_64_GOTOFF64`

64 bit offset to GOT

```rust
pub const R_X86_64_GOTOFF64: u32 = 25;
```

#### Constant `R_X86_64_GOTPC32`

32 bit signed pc relative offset to GOT

```rust
pub const R_X86_64_GOTPC32: u32 = 26;
```

#### Constant `R_X86_64_GOT64`

64-bit GOT entry offset

```rust
pub const R_X86_64_GOT64: u32 = 27;
```

#### Constant `R_X86_64_GOTPCREL64`

64-bit PC relative offset to GOT entry

```rust
pub const R_X86_64_GOTPCREL64: u32 = 28;
```

#### Constant `R_X86_64_GOTPC64`

64-bit PC relative offset to GOT

```rust
pub const R_X86_64_GOTPC64: u32 = 29;
```

#### Constant `R_X86_64_GOTPLT64`

like GOT64, says PLT entry needed

```rust
pub const R_X86_64_GOTPLT64: u32 = 30;
```

#### Constant `R_X86_64_PLTOFF64`

64-bit GOT relative offset to PLT entry

```rust
pub const R_X86_64_PLTOFF64: u32 = 31;
```

#### Constant `R_X86_64_SIZE32`

Size of symbol plus 32-bit addend

```rust
pub const R_X86_64_SIZE32: u32 = 32;
```

#### Constant `R_X86_64_SIZE64`

Size of symbol plus 64-bit addend

```rust
pub const R_X86_64_SIZE64: u32 = 33;
```

#### Constant `R_X86_64_GOTPC32_TLSDESC`

GOT offset for TLS descriptor.

```rust
pub const R_X86_64_GOTPC32_TLSDESC: u32 = 34;
```

#### Constant `R_X86_64_TLSDESC_CALL`

Marker for call through TLS descriptor.

```rust
pub const R_X86_64_TLSDESC_CALL: u32 = 35;
```

#### Constant `R_X86_64_TLSDESC`

TLS descriptor.

```rust
pub const R_X86_64_TLSDESC: u32 = 36;
```

#### Constant `R_X86_64_IRELATIVE`

Adjust indirectly by program base

```rust
pub const R_X86_64_IRELATIVE: u32 = 37;
```

#### Constant `R_X86_64_RELATIVE64`

64-bit adjust by program base

```rust
pub const R_X86_64_RELATIVE64: u32 = 38;
```

#### Constant `R_X86_64_GOTPCRELX`

Load from 32 bit signed pc relative offset to GOT entry without REX prefix, relaxable.

```rust
pub const R_X86_64_GOTPCRELX: u32 = 41;
```

#### Constant `R_X86_64_REX_GOTPCRELX`

Load from 32 bit signed pc relative offset to GOT entry with REX prefix, relaxable.

```rust
pub const R_X86_64_REX_GOTPCRELX: u32 = 42;
```

#### Constant `SHT_X86_64_UNWIND`

Unwind information.

```rust
pub const SHT_X86_64_UNWIND: u32 = 0x7000_0001;
```

#### Constant `R_MN10300_NONE`

No reloc.

```rust
pub const R_MN10300_NONE: u32 = 0;
```

#### Constant `R_MN10300_32`

Direct 32 bit.

```rust
pub const R_MN10300_32: u32 = 1;
```

#### Constant `R_MN10300_16`

Direct 16 bit.

```rust
pub const R_MN10300_16: u32 = 2;
```

#### Constant `R_MN10300_8`

Direct 8 bit.

```rust
pub const R_MN10300_8: u32 = 3;
```

#### Constant `R_MN10300_PCREL32`

PC-relative 32-bit.

```rust
pub const R_MN10300_PCREL32: u32 = 4;
```

#### Constant `R_MN10300_PCREL16`

PC-relative 16-bit signed.

```rust
pub const R_MN10300_PCREL16: u32 = 5;
```

#### Constant `R_MN10300_PCREL8`

PC-relative 8-bit signed.

```rust
pub const R_MN10300_PCREL8: u32 = 6;
```

#### Constant `R_MN10300_GNU_VTINHERIT`

Ancient C++ vtable garbage...

```rust
pub const R_MN10300_GNU_VTINHERIT: u32 = 7;
```

#### Constant `R_MN10300_GNU_VTENTRY`

... collection annotation.

```rust
pub const R_MN10300_GNU_VTENTRY: u32 = 8;
```

#### Constant `R_MN10300_24`

Direct 24 bit.

```rust
pub const R_MN10300_24: u32 = 9;
```

#### Constant `R_MN10300_GOTPC32`

32-bit PCrel offset to GOT.

```rust
pub const R_MN10300_GOTPC32: u32 = 10;
```

#### Constant `R_MN10300_GOTPC16`

16-bit PCrel offset to GOT.

```rust
pub const R_MN10300_GOTPC16: u32 = 11;
```

#### Constant `R_MN10300_GOTOFF32`

32-bit offset from GOT.

```rust
pub const R_MN10300_GOTOFF32: u32 = 12;
```

#### Constant `R_MN10300_GOTOFF24`

24-bit offset from GOT.

```rust
pub const R_MN10300_GOTOFF24: u32 = 13;
```

#### Constant `R_MN10300_GOTOFF16`

16-bit offset from GOT.

```rust
pub const R_MN10300_GOTOFF16: u32 = 14;
```

#### Constant `R_MN10300_PLT32`

32-bit PCrel to PLT entry.

```rust
pub const R_MN10300_PLT32: u32 = 15;
```

#### Constant `R_MN10300_PLT16`

16-bit PCrel to PLT entry.

```rust
pub const R_MN10300_PLT16: u32 = 16;
```

#### Constant `R_MN10300_GOT32`

32-bit offset to GOT entry.

```rust
pub const R_MN10300_GOT32: u32 = 17;
```

#### Constant `R_MN10300_GOT24`

24-bit offset to GOT entry.

```rust
pub const R_MN10300_GOT24: u32 = 18;
```

#### Constant `R_MN10300_GOT16`

16-bit offset to GOT entry.

```rust
pub const R_MN10300_GOT16: u32 = 19;
```

#### Constant `R_MN10300_COPY`

Copy symbol at runtime.

```rust
pub const R_MN10300_COPY: u32 = 20;
```

#### Constant `R_MN10300_GLOB_DAT`

Create GOT entry.

```rust
pub const R_MN10300_GLOB_DAT: u32 = 21;
```

#### Constant `R_MN10300_JMP_SLOT`

Create PLT entry.

```rust
pub const R_MN10300_JMP_SLOT: u32 = 22;
```

#### Constant `R_MN10300_RELATIVE`

Adjust by program base.

```rust
pub const R_MN10300_RELATIVE: u32 = 23;
```

#### Constant `R_MN10300_TLS_GD`

32-bit offset for global dynamic.

```rust
pub const R_MN10300_TLS_GD: u32 = 24;
```

#### Constant `R_MN10300_TLS_LD`

32-bit offset for local dynamic.

```rust
pub const R_MN10300_TLS_LD: u32 = 25;
```

#### Constant `R_MN10300_TLS_LDO`

Module-relative offset.

```rust
pub const R_MN10300_TLS_LDO: u32 = 26;
```

#### Constant `R_MN10300_TLS_GOTIE`

GOT offset for static TLS block offset.

```rust
pub const R_MN10300_TLS_GOTIE: u32 = 27;
```

#### Constant `R_MN10300_TLS_IE`

GOT address for static TLS block offset.

```rust
pub const R_MN10300_TLS_IE: u32 = 28;
```

#### Constant `R_MN10300_TLS_LE`

Offset relative to static TLS block.

```rust
pub const R_MN10300_TLS_LE: u32 = 29;
```

#### Constant `R_MN10300_TLS_DTPMOD`

ID of module containing symbol.

```rust
pub const R_MN10300_TLS_DTPMOD: u32 = 30;
```

#### Constant `R_MN10300_TLS_DTPOFF`

Offset in module TLS block.

```rust
pub const R_MN10300_TLS_DTPOFF: u32 = 31;
```

#### Constant `R_MN10300_TLS_TPOFF`

Offset in static TLS block.

```rust
pub const R_MN10300_TLS_TPOFF: u32 = 32;
```

#### Constant `R_MN10300_SYM_DIFF`

Adjustment for next reloc as needed by linker relaxation.

```rust
pub const R_MN10300_SYM_DIFF: u32 = 33;
```

#### Constant `R_MN10300_ALIGN`

Alignment requirement for linker relaxation.

```rust
pub const R_MN10300_ALIGN: u32 = 34;
```

#### Constant `R_M32R_NONE`

No reloc.

```rust
pub const R_M32R_NONE: u32 = 0;
```

#### Constant `R_M32R_16`

Direct 16 bit.

```rust
pub const R_M32R_16: u32 = 1;
```

#### Constant `R_M32R_32`

Direct 32 bit.

```rust
pub const R_M32R_32: u32 = 2;
```

#### Constant `R_M32R_24`

Direct 24 bit.

```rust
pub const R_M32R_24: u32 = 3;
```

#### Constant `R_M32R_10_PCREL`

PC relative 10 bit shifted.

```rust
pub const R_M32R_10_PCREL: u32 = 4;
```

#### Constant `R_M32R_18_PCREL`

PC relative 18 bit shifted.

```rust
pub const R_M32R_18_PCREL: u32 = 5;
```

#### Constant `R_M32R_26_PCREL`

PC relative 26 bit shifted.

```rust
pub const R_M32R_26_PCREL: u32 = 6;
```

#### Constant `R_M32R_HI16_ULO`

High 16 bit with unsigned low.

```rust
pub const R_M32R_HI16_ULO: u32 = 7;
```

#### Constant `R_M32R_HI16_SLO`

High 16 bit with signed low.

```rust
pub const R_M32R_HI16_SLO: u32 = 8;
```

#### Constant `R_M32R_LO16`

Low 16 bit.

```rust
pub const R_M32R_LO16: u32 = 9;
```

#### Constant `R_M32R_SDA16`

16 bit offset in SDA.

```rust
pub const R_M32R_SDA16: u32 = 10;
```

#### Constant `R_M32R_GNU_VTINHERIT`

```rust
pub const R_M32R_GNU_VTINHERIT: u32 = 11;
```

#### Constant `R_M32R_GNU_VTENTRY`

```rust
pub const R_M32R_GNU_VTENTRY: u32 = 12;
```

#### Constant `R_M32R_16_RELA`

Direct 16 bit.

```rust
pub const R_M32R_16_RELA: u32 = 33;
```

#### Constant `R_M32R_32_RELA`

Direct 32 bit.

```rust
pub const R_M32R_32_RELA: u32 = 34;
```

#### Constant `R_M32R_24_RELA`

Direct 24 bit.

```rust
pub const R_M32R_24_RELA: u32 = 35;
```

#### Constant `R_M32R_10_PCREL_RELA`

PC relative 10 bit shifted.

```rust
pub const R_M32R_10_PCREL_RELA: u32 = 36;
```

#### Constant `R_M32R_18_PCREL_RELA`

PC relative 18 bit shifted.

```rust
pub const R_M32R_18_PCREL_RELA: u32 = 37;
```

#### Constant `R_M32R_26_PCREL_RELA`

PC relative 26 bit shifted.

```rust
pub const R_M32R_26_PCREL_RELA: u32 = 38;
```

#### Constant `R_M32R_HI16_ULO_RELA`

High 16 bit with unsigned low

```rust
pub const R_M32R_HI16_ULO_RELA: u32 = 39;
```

#### Constant `R_M32R_HI16_SLO_RELA`

High 16 bit with signed low

```rust
pub const R_M32R_HI16_SLO_RELA: u32 = 40;
```

#### Constant `R_M32R_LO16_RELA`

Low 16 bit

```rust
pub const R_M32R_LO16_RELA: u32 = 41;
```

#### Constant `R_M32R_SDA16_RELA`

16 bit offset in SDA

```rust
pub const R_M32R_SDA16_RELA: u32 = 42;
```

#### Constant `R_M32R_RELA_GNU_VTINHERIT`

```rust
pub const R_M32R_RELA_GNU_VTINHERIT: u32 = 43;
```

#### Constant `R_M32R_RELA_GNU_VTENTRY`

```rust
pub const R_M32R_RELA_GNU_VTENTRY: u32 = 44;
```

#### Constant `R_M32R_REL32`

PC relative 32 bit.

```rust
pub const R_M32R_REL32: u32 = 45;
```

#### Constant `R_M32R_GOT24`

24 bit GOT entry

```rust
pub const R_M32R_GOT24: u32 = 48;
```

#### Constant `R_M32R_26_PLTREL`

26 bit PC relative to PLT shifted

```rust
pub const R_M32R_26_PLTREL: u32 = 49;
```

#### Constant `R_M32R_COPY`

Copy symbol at runtime

```rust
pub const R_M32R_COPY: u32 = 50;
```

#### Constant `R_M32R_GLOB_DAT`

Create GOT entry

```rust
pub const R_M32R_GLOB_DAT: u32 = 51;
```

#### Constant `R_M32R_JMP_SLOT`

Create PLT entry

```rust
pub const R_M32R_JMP_SLOT: u32 = 52;
```

#### Constant `R_M32R_RELATIVE`

Adjust by program base

```rust
pub const R_M32R_RELATIVE: u32 = 53;
```

#### Constant `R_M32R_GOTOFF`

24 bit offset to GOT

```rust
pub const R_M32R_GOTOFF: u32 = 54;
```

#### Constant `R_M32R_GOTPC24`

24 bit PC relative offset to GOT

```rust
pub const R_M32R_GOTPC24: u32 = 55;
```

#### Constant `R_M32R_GOT16_HI_ULO`

High 16 bit GOT entry with unsigned low

```rust
pub const R_M32R_GOT16_HI_ULO: u32 = 56;
```

#### Constant `R_M32R_GOT16_HI_SLO`

High 16 bit GOT entry with signed low

```rust
pub const R_M32R_GOT16_HI_SLO: u32 = 57;
```

#### Constant `R_M32R_GOT16_LO`

Low 16 bit GOT entry

```rust
pub const R_M32R_GOT16_LO: u32 = 58;
```

#### Constant `R_M32R_GOTPC_HI_ULO`

High 16 bit PC relative offset to GOT with unsigned low

```rust
pub const R_M32R_GOTPC_HI_ULO: u32 = 59;
```

#### Constant `R_M32R_GOTPC_HI_SLO`

High 16 bit PC relative offset to GOT with signed low

```rust
pub const R_M32R_GOTPC_HI_SLO: u32 = 60;
```

#### Constant `R_M32R_GOTPC_LO`

Low 16 bit PC relative offset to GOT

```rust
pub const R_M32R_GOTPC_LO: u32 = 61;
```

#### Constant `R_M32R_GOTOFF_HI_ULO`

High 16 bit offset to GOT with unsigned low

```rust
pub const R_M32R_GOTOFF_HI_ULO: u32 = 62;
```

#### Constant `R_M32R_GOTOFF_HI_SLO`

High 16 bit offset to GOT with signed low

```rust
pub const R_M32R_GOTOFF_HI_SLO: u32 = 63;
```

#### Constant `R_M32R_GOTOFF_LO`

Low 16 bit offset to GOT

```rust
pub const R_M32R_GOTOFF_LO: u32 = 64;
```

#### Constant `R_M32R_NUM`

Keep this the last entry.

```rust
pub const R_M32R_NUM: u32 = 256;
```

#### Constant `R_MICROBLAZE_NONE`

No reloc.

```rust
pub const R_MICROBLAZE_NONE: u32 = 0;
```

#### Constant `R_MICROBLAZE_32`

Direct 32 bit.

```rust
pub const R_MICROBLAZE_32: u32 = 1;
```

#### Constant `R_MICROBLAZE_32_PCREL`

PC relative 32 bit.

```rust
pub const R_MICROBLAZE_32_PCREL: u32 = 2;
```

#### Constant `R_MICROBLAZE_64_PCREL`

PC relative 64 bit.

```rust
pub const R_MICROBLAZE_64_PCREL: u32 = 3;
```

#### Constant `R_MICROBLAZE_32_PCREL_LO`

Low 16 bits of PCREL32.

```rust
pub const R_MICROBLAZE_32_PCREL_LO: u32 = 4;
```

#### Constant `R_MICROBLAZE_64`

Direct 64 bit.

```rust
pub const R_MICROBLAZE_64: u32 = 5;
```

#### Constant `R_MICROBLAZE_32_LO`

Low 16 bit.

```rust
pub const R_MICROBLAZE_32_LO: u32 = 6;
```

#### Constant `R_MICROBLAZE_SRO32`

Read-only small data area.

```rust
pub const R_MICROBLAZE_SRO32: u32 = 7;
```

#### Constant `R_MICROBLAZE_SRW32`

Read-write small data area.

```rust
pub const R_MICROBLAZE_SRW32: u32 = 8;
```

#### Constant `R_MICROBLAZE_64_NONE`

No reloc.

```rust
pub const R_MICROBLAZE_64_NONE: u32 = 9;
```

#### Constant `R_MICROBLAZE_32_SYM_OP_SYM`

Symbol Op Symbol relocation.

```rust
pub const R_MICROBLAZE_32_SYM_OP_SYM: u32 = 10;
```

#### Constant `R_MICROBLAZE_GNU_VTINHERIT`

GNU C++ vtable hierarchy.

```rust
pub const R_MICROBLAZE_GNU_VTINHERIT: u32 = 11;
```

#### Constant `R_MICROBLAZE_GNU_VTENTRY`

GNU C++ vtable member usage.

```rust
pub const R_MICROBLAZE_GNU_VTENTRY: u32 = 12;
```

#### Constant `R_MICROBLAZE_GOTPC_64`

PC-relative GOT offset.

```rust
pub const R_MICROBLAZE_GOTPC_64: u32 = 13;
```

#### Constant `R_MICROBLAZE_GOT_64`

GOT entry offset.

```rust
pub const R_MICROBLAZE_GOT_64: u32 = 14;
```

#### Constant `R_MICROBLAZE_PLT_64`

PLT offset (PC-relative).

```rust
pub const R_MICROBLAZE_PLT_64: u32 = 15;
```

#### Constant `R_MICROBLAZE_REL`

Adjust by program base.

```rust
pub const R_MICROBLAZE_REL: u32 = 16;
```

#### Constant `R_MICROBLAZE_JUMP_SLOT`

Create PLT entry.

```rust
pub const R_MICROBLAZE_JUMP_SLOT: u32 = 17;
```

#### Constant `R_MICROBLAZE_GLOB_DAT`

Create GOT entry.

```rust
pub const R_MICROBLAZE_GLOB_DAT: u32 = 18;
```

#### Constant `R_MICROBLAZE_GOTOFF_64`

64 bit offset to GOT.

```rust
pub const R_MICROBLAZE_GOTOFF_64: u32 = 19;
```

#### Constant `R_MICROBLAZE_GOTOFF_32`

32 bit offset to GOT.

```rust
pub const R_MICROBLAZE_GOTOFF_32: u32 = 20;
```

#### Constant `R_MICROBLAZE_COPY`

Runtime copy.

```rust
pub const R_MICROBLAZE_COPY: u32 = 21;
```

#### Constant `R_MICROBLAZE_TLS`

TLS Reloc.

```rust
pub const R_MICROBLAZE_TLS: u32 = 22;
```

#### Constant `R_MICROBLAZE_TLSGD`

TLS General Dynamic.

```rust
pub const R_MICROBLAZE_TLSGD: u32 = 23;
```

#### Constant `R_MICROBLAZE_TLSLD`

TLS Local Dynamic.

```rust
pub const R_MICROBLAZE_TLSLD: u32 = 24;
```

#### Constant `R_MICROBLAZE_TLSDTPMOD32`

TLS Module ID.

```rust
pub const R_MICROBLAZE_TLSDTPMOD32: u32 = 25;
```

#### Constant `R_MICROBLAZE_TLSDTPREL32`

TLS Offset Within TLS Block.

```rust
pub const R_MICROBLAZE_TLSDTPREL32: u32 = 26;
```

#### Constant `R_MICROBLAZE_TLSDTPREL64`

TLS Offset Within TLS Block.

```rust
pub const R_MICROBLAZE_TLSDTPREL64: u32 = 27;
```

#### Constant `R_MICROBLAZE_TLSGOTTPREL32`

TLS Offset From Thread Pointer.

```rust
pub const R_MICROBLAZE_TLSGOTTPREL32: u32 = 28;
```

#### Constant `R_MICROBLAZE_TLSTPREL32`

TLS Offset From Thread Pointer.

```rust
pub const R_MICROBLAZE_TLSTPREL32: u32 = 29;
```

#### Constant `DT_NIOS2_GP`

Address of _gp.

```rust
pub const DT_NIOS2_GP: u32 = 0x7000_0002;
```

#### Constant `R_NIOS2_NONE`

No reloc.

```rust
pub const R_NIOS2_NONE: u32 = 0;
```

#### Constant `R_NIOS2_S16`

Direct signed 16 bit.

```rust
pub const R_NIOS2_S16: u32 = 1;
```

#### Constant `R_NIOS2_U16`

Direct unsigned 16 bit.

```rust
pub const R_NIOS2_U16: u32 = 2;
```

#### Constant `R_NIOS2_PCREL16`

PC relative 16 bit.

```rust
pub const R_NIOS2_PCREL16: u32 = 3;
```

#### Constant `R_NIOS2_CALL26`

Direct call.

```rust
pub const R_NIOS2_CALL26: u32 = 4;
```

#### Constant `R_NIOS2_IMM5`

5 bit constant expression.

```rust
pub const R_NIOS2_IMM5: u32 = 5;
```

#### Constant `R_NIOS2_CACHE_OPX`

5 bit expression, shift 22.

```rust
pub const R_NIOS2_CACHE_OPX: u32 = 6;
```

#### Constant `R_NIOS2_IMM6`

6 bit constant expression.

```rust
pub const R_NIOS2_IMM6: u32 = 7;
```

#### Constant `R_NIOS2_IMM8`

8 bit constant expression.

```rust
pub const R_NIOS2_IMM8: u32 = 8;
```

#### Constant `R_NIOS2_HI16`

High 16 bit.

```rust
pub const R_NIOS2_HI16: u32 = 9;
```

#### Constant `R_NIOS2_LO16`

Low 16 bit.

```rust
pub const R_NIOS2_LO16: u32 = 10;
```

#### Constant `R_NIOS2_HIADJ16`

High 16 bit, adjusted.

```rust
pub const R_NIOS2_HIADJ16: u32 = 11;
```

#### Constant `R_NIOS2_BFD_RELOC_32`

32 bit symbol value + addend.

```rust
pub const R_NIOS2_BFD_RELOC_32: u32 = 12;
```

#### Constant `R_NIOS2_BFD_RELOC_16`

16 bit symbol value + addend.

```rust
pub const R_NIOS2_BFD_RELOC_16: u32 = 13;
```

#### Constant `R_NIOS2_BFD_RELOC_8`

8 bit symbol value + addend.

```rust
pub const R_NIOS2_BFD_RELOC_8: u32 = 14;
```

#### Constant `R_NIOS2_GPREL`

16 bit GP pointer offset.

```rust
pub const R_NIOS2_GPREL: u32 = 15;
```

#### Constant `R_NIOS2_GNU_VTINHERIT`

GNU C++ vtable hierarchy.

```rust
pub const R_NIOS2_GNU_VTINHERIT: u32 = 16;
```

#### Constant `R_NIOS2_GNU_VTENTRY`

GNU C++ vtable member usage.

```rust
pub const R_NIOS2_GNU_VTENTRY: u32 = 17;
```

#### Constant `R_NIOS2_UJMP`

Unconditional branch.

```rust
pub const R_NIOS2_UJMP: u32 = 18;
```

#### Constant `R_NIOS2_CJMP`

Conditional branch.

```rust
pub const R_NIOS2_CJMP: u32 = 19;
```

#### Constant `R_NIOS2_CALLR`

Indirect call through register.

```rust
pub const R_NIOS2_CALLR: u32 = 20;
```

#### Constant `R_NIOS2_ALIGN`

Alignment requirement for linker relaxation.

```rust
pub const R_NIOS2_ALIGN: u32 = 21;
```

#### Constant `R_NIOS2_GOT16`

16 bit GOT entry.

```rust
pub const R_NIOS2_GOT16: u32 = 22;
```

#### Constant `R_NIOS2_CALL16`

16 bit GOT entry for function.

```rust
pub const R_NIOS2_CALL16: u32 = 23;
```

#### Constant `R_NIOS2_GOTOFF_LO`

%lo of offset to GOT pointer.

```rust
pub const R_NIOS2_GOTOFF_LO: u32 = 24;
```

#### Constant `R_NIOS2_GOTOFF_HA`

%hiadj of offset to GOT pointer.

```rust
pub const R_NIOS2_GOTOFF_HA: u32 = 25;
```

#### Constant `R_NIOS2_PCREL_LO`

%lo of PC relative offset.

```rust
pub const R_NIOS2_PCREL_LO: u32 = 26;
```

#### Constant `R_NIOS2_PCREL_HA`

%hiadj of PC relative offset.

```rust
pub const R_NIOS2_PCREL_HA: u32 = 27;
```

#### Constant `R_NIOS2_TLS_GD16`

16 bit GOT offset for TLS GD.

```rust
pub const R_NIOS2_TLS_GD16: u32 = 28;
```

#### Constant `R_NIOS2_TLS_LDM16`

16 bit GOT offset for TLS LDM.

```rust
pub const R_NIOS2_TLS_LDM16: u32 = 29;
```

#### Constant `R_NIOS2_TLS_LDO16`

16 bit module relative offset.

```rust
pub const R_NIOS2_TLS_LDO16: u32 = 30;
```

#### Constant `R_NIOS2_TLS_IE16`

16 bit GOT offset for TLS IE.

```rust
pub const R_NIOS2_TLS_IE16: u32 = 31;
```

#### Constant `R_NIOS2_TLS_LE16`

16 bit LE TP-relative offset.

```rust
pub const R_NIOS2_TLS_LE16: u32 = 32;
```

#### Constant `R_NIOS2_TLS_DTPMOD`

Module number.

```rust
pub const R_NIOS2_TLS_DTPMOD: u32 = 33;
```

#### Constant `R_NIOS2_TLS_DTPREL`

Module-relative offset.

```rust
pub const R_NIOS2_TLS_DTPREL: u32 = 34;
```

#### Constant `R_NIOS2_TLS_TPREL`

TP-relative offset.

```rust
pub const R_NIOS2_TLS_TPREL: u32 = 35;
```

#### Constant `R_NIOS2_COPY`

Copy symbol at runtime.

```rust
pub const R_NIOS2_COPY: u32 = 36;
```

#### Constant `R_NIOS2_GLOB_DAT`

Create GOT entry.

```rust
pub const R_NIOS2_GLOB_DAT: u32 = 37;
```

#### Constant `R_NIOS2_JUMP_SLOT`

Create PLT entry.

```rust
pub const R_NIOS2_JUMP_SLOT: u32 = 38;
```

#### Constant `R_NIOS2_RELATIVE`

Adjust by program base.

```rust
pub const R_NIOS2_RELATIVE: u32 = 39;
```

#### Constant `R_NIOS2_GOTOFF`

16 bit offset to GOT pointer.

```rust
pub const R_NIOS2_GOTOFF: u32 = 40;
```

#### Constant `R_NIOS2_CALL26_NOAT`

Direct call in .noat section.

```rust
pub const R_NIOS2_CALL26_NOAT: u32 = 41;
```

#### Constant `R_NIOS2_GOT_LO`

%lo() of GOT entry.

```rust
pub const R_NIOS2_GOT_LO: u32 = 42;
```

#### Constant `R_NIOS2_GOT_HA`

%hiadj() of GOT entry.

```rust
pub const R_NIOS2_GOT_HA: u32 = 43;
```

#### Constant `R_NIOS2_CALL_LO`

%lo() of function GOT entry.

```rust
pub const R_NIOS2_CALL_LO: u32 = 44;
```

#### Constant `R_NIOS2_CALL_HA`

%hiadj() of function GOT entry.

```rust
pub const R_NIOS2_CALL_HA: u32 = 45;
```

#### Constant `R_TILEPRO_NONE`

No reloc

```rust
pub const R_TILEPRO_NONE: u32 = 0;
```

#### Constant `R_TILEPRO_32`

Direct 32 bit

```rust
pub const R_TILEPRO_32: u32 = 1;
```

#### Constant `R_TILEPRO_16`

Direct 16 bit

```rust
pub const R_TILEPRO_16: u32 = 2;
```

#### Constant `R_TILEPRO_8`

Direct 8 bit

```rust
pub const R_TILEPRO_8: u32 = 3;
```

#### Constant `R_TILEPRO_32_PCREL`

PC relative 32 bit

```rust
pub const R_TILEPRO_32_PCREL: u32 = 4;
```

#### Constant `R_TILEPRO_16_PCREL`

PC relative 16 bit

```rust
pub const R_TILEPRO_16_PCREL: u32 = 5;
```

#### Constant `R_TILEPRO_8_PCREL`

PC relative 8 bit

```rust
pub const R_TILEPRO_8_PCREL: u32 = 6;
```

#### Constant `R_TILEPRO_LO16`

Low 16 bit

```rust
pub const R_TILEPRO_LO16: u32 = 7;
```

#### Constant `R_TILEPRO_HI16`

High 16 bit

```rust
pub const R_TILEPRO_HI16: u32 = 8;
```

#### Constant `R_TILEPRO_HA16`

High 16 bit, adjusted

```rust
pub const R_TILEPRO_HA16: u32 = 9;
```

#### Constant `R_TILEPRO_COPY`

Copy relocation

```rust
pub const R_TILEPRO_COPY: u32 = 10;
```

#### Constant `R_TILEPRO_GLOB_DAT`

Create GOT entry

```rust
pub const R_TILEPRO_GLOB_DAT: u32 = 11;
```

#### Constant `R_TILEPRO_JMP_SLOT`

Create PLT entry

```rust
pub const R_TILEPRO_JMP_SLOT: u32 = 12;
```

#### Constant `R_TILEPRO_RELATIVE`

Adjust by program base

```rust
pub const R_TILEPRO_RELATIVE: u32 = 13;
```

#### Constant `R_TILEPRO_BROFF_X1`

X1 pipe branch offset

```rust
pub const R_TILEPRO_BROFF_X1: u32 = 14;
```

#### Constant `R_TILEPRO_JOFFLONG_X1`

X1 pipe jump offset

```rust
pub const R_TILEPRO_JOFFLONG_X1: u32 = 15;
```

#### Constant `R_TILEPRO_JOFFLONG_X1_PLT`

X1 pipe jump offset to PLT

```rust
pub const R_TILEPRO_JOFFLONG_X1_PLT: u32 = 16;
```

#### Constant `R_TILEPRO_IMM8_X0`

X0 pipe 8-bit

```rust
pub const R_TILEPRO_IMM8_X0: u32 = 17;
```

#### Constant `R_TILEPRO_IMM8_Y0`

Y0 pipe 8-bit

```rust
pub const R_TILEPRO_IMM8_Y0: u32 = 18;
```

#### Constant `R_TILEPRO_IMM8_X1`

X1 pipe 8-bit

```rust
pub const R_TILEPRO_IMM8_X1: u32 = 19;
```

#### Constant `R_TILEPRO_IMM8_Y1`

Y1 pipe 8-bit

```rust
pub const R_TILEPRO_IMM8_Y1: u32 = 20;
```

#### Constant `R_TILEPRO_MT_IMM15_X1`

X1 pipe mtspr

```rust
pub const R_TILEPRO_MT_IMM15_X1: u32 = 21;
```

#### Constant `R_TILEPRO_MF_IMM15_X1`

X1 pipe mfspr

```rust
pub const R_TILEPRO_MF_IMM15_X1: u32 = 22;
```

#### Constant `R_TILEPRO_IMM16_X0`

X0 pipe 16-bit

```rust
pub const R_TILEPRO_IMM16_X0: u32 = 23;
```

#### Constant `R_TILEPRO_IMM16_X1`

X1 pipe 16-bit

```rust
pub const R_TILEPRO_IMM16_X1: u32 = 24;
```

#### Constant `R_TILEPRO_IMM16_X0_LO`

X0 pipe low 16-bit

```rust
pub const R_TILEPRO_IMM16_X0_LO: u32 = 25;
```

#### Constant `R_TILEPRO_IMM16_X1_LO`

X1 pipe low 16-bit

```rust
pub const R_TILEPRO_IMM16_X1_LO: u32 = 26;
```

#### Constant `R_TILEPRO_IMM16_X0_HI`

X0 pipe high 16-bit

```rust
pub const R_TILEPRO_IMM16_X0_HI: u32 = 27;
```

#### Constant `R_TILEPRO_IMM16_X1_HI`

X1 pipe high 16-bit

```rust
pub const R_TILEPRO_IMM16_X1_HI: u32 = 28;
```

#### Constant `R_TILEPRO_IMM16_X0_HA`

X0 pipe high 16-bit, adjusted

```rust
pub const R_TILEPRO_IMM16_X0_HA: u32 = 29;
```

#### Constant `R_TILEPRO_IMM16_X1_HA`

X1 pipe high 16-bit, adjusted

```rust
pub const R_TILEPRO_IMM16_X1_HA: u32 = 30;
```

#### Constant `R_TILEPRO_IMM16_X0_PCREL`

X0 pipe PC relative 16 bit

```rust
pub const R_TILEPRO_IMM16_X0_PCREL: u32 = 31;
```

#### Constant `R_TILEPRO_IMM16_X1_PCREL`

X1 pipe PC relative 16 bit

```rust
pub const R_TILEPRO_IMM16_X1_PCREL: u32 = 32;
```

#### Constant `R_TILEPRO_IMM16_X0_LO_PCREL`

X0 pipe PC relative low 16 bit

```rust
pub const R_TILEPRO_IMM16_X0_LO_PCREL: u32 = 33;
```

#### Constant `R_TILEPRO_IMM16_X1_LO_PCREL`

X1 pipe PC relative low 16 bit

```rust
pub const R_TILEPRO_IMM16_X1_LO_PCREL: u32 = 34;
```

#### Constant `R_TILEPRO_IMM16_X0_HI_PCREL`

X0 pipe PC relative high 16 bit

```rust
pub const R_TILEPRO_IMM16_X0_HI_PCREL: u32 = 35;
```

#### Constant `R_TILEPRO_IMM16_X1_HI_PCREL`

X1 pipe PC relative high 16 bit

```rust
pub const R_TILEPRO_IMM16_X1_HI_PCREL: u32 = 36;
```

#### Constant `R_TILEPRO_IMM16_X0_HA_PCREL`

X0 pipe PC relative ha() 16 bit

```rust
pub const R_TILEPRO_IMM16_X0_HA_PCREL: u32 = 37;
```

#### Constant `R_TILEPRO_IMM16_X1_HA_PCREL`

X1 pipe PC relative ha() 16 bit

```rust
pub const R_TILEPRO_IMM16_X1_HA_PCREL: u32 = 38;
```

#### Constant `R_TILEPRO_IMM16_X0_GOT`

X0 pipe 16-bit GOT offset

```rust
pub const R_TILEPRO_IMM16_X0_GOT: u32 = 39;
```

#### Constant `R_TILEPRO_IMM16_X1_GOT`

X1 pipe 16-bit GOT offset

```rust
pub const R_TILEPRO_IMM16_X1_GOT: u32 = 40;
```

#### Constant `R_TILEPRO_IMM16_X0_GOT_LO`

X0 pipe low 16-bit GOT offset

```rust
pub const R_TILEPRO_IMM16_X0_GOT_LO: u32 = 41;
```

#### Constant `R_TILEPRO_IMM16_X1_GOT_LO`

X1 pipe low 16-bit GOT offset

```rust
pub const R_TILEPRO_IMM16_X1_GOT_LO: u32 = 42;
```

#### Constant `R_TILEPRO_IMM16_X0_GOT_HI`

X0 pipe high 16-bit GOT offset

```rust
pub const R_TILEPRO_IMM16_X0_GOT_HI: u32 = 43;
```

#### Constant `R_TILEPRO_IMM16_X1_GOT_HI`

X1 pipe high 16-bit GOT offset

```rust
pub const R_TILEPRO_IMM16_X1_GOT_HI: u32 = 44;
```

#### Constant `R_TILEPRO_IMM16_X0_GOT_HA`

X0 pipe ha() 16-bit GOT offset

```rust
pub const R_TILEPRO_IMM16_X0_GOT_HA: u32 = 45;
```

#### Constant `R_TILEPRO_IMM16_X1_GOT_HA`

X1 pipe ha() 16-bit GOT offset

```rust
pub const R_TILEPRO_IMM16_X1_GOT_HA: u32 = 46;
```

#### Constant `R_TILEPRO_MMSTART_X0`

X0 pipe mm "start"

```rust
pub const R_TILEPRO_MMSTART_X0: u32 = 47;
```

#### Constant `R_TILEPRO_MMEND_X0`

X0 pipe mm "end"

```rust
pub const R_TILEPRO_MMEND_X0: u32 = 48;
```

#### Constant `R_TILEPRO_MMSTART_X1`

X1 pipe mm "start"

```rust
pub const R_TILEPRO_MMSTART_X1: u32 = 49;
```

#### Constant `R_TILEPRO_MMEND_X1`

X1 pipe mm "end"

```rust
pub const R_TILEPRO_MMEND_X1: u32 = 50;
```

#### Constant `R_TILEPRO_SHAMT_X0`

X0 pipe shift amount

```rust
pub const R_TILEPRO_SHAMT_X0: u32 = 51;
```

#### Constant `R_TILEPRO_SHAMT_X1`

X1 pipe shift amount

```rust
pub const R_TILEPRO_SHAMT_X1: u32 = 52;
```

#### Constant `R_TILEPRO_SHAMT_Y0`

Y0 pipe shift amount

```rust
pub const R_TILEPRO_SHAMT_Y0: u32 = 53;
```

#### Constant `R_TILEPRO_SHAMT_Y1`

Y1 pipe shift amount

```rust
pub const R_TILEPRO_SHAMT_Y1: u32 = 54;
```

#### Constant `R_TILEPRO_DEST_IMM8_X1`

X1 pipe destination 8-bit

```rust
pub const R_TILEPRO_DEST_IMM8_X1: u32 = 55;
```

#### Constant `R_TILEPRO_TLS_GD_CALL`

"jal" for TLS GD

```rust
pub const R_TILEPRO_TLS_GD_CALL: u32 = 60;
```

#### Constant `R_TILEPRO_IMM8_X0_TLS_GD_ADD`

X0 pipe "addi" for TLS GD

```rust
pub const R_TILEPRO_IMM8_X0_TLS_GD_ADD: u32 = 61;
```

#### Constant `R_TILEPRO_IMM8_X1_TLS_GD_ADD`

X1 pipe "addi" for TLS GD

```rust
pub const R_TILEPRO_IMM8_X1_TLS_GD_ADD: u32 = 62;
```

#### Constant `R_TILEPRO_IMM8_Y0_TLS_GD_ADD`

Y0 pipe "addi" for TLS GD

```rust
pub const R_TILEPRO_IMM8_Y0_TLS_GD_ADD: u32 = 63;
```

#### Constant `R_TILEPRO_IMM8_Y1_TLS_GD_ADD`

Y1 pipe "addi" for TLS GD

```rust
pub const R_TILEPRO_IMM8_Y1_TLS_GD_ADD: u32 = 64;
```

#### Constant `R_TILEPRO_TLS_IE_LOAD`

"lw_tls" for TLS IE

```rust
pub const R_TILEPRO_TLS_IE_LOAD: u32 = 65;
```

#### Constant `R_TILEPRO_IMM16_X0_TLS_GD`

X0 pipe 16-bit TLS GD offset

```rust
pub const R_TILEPRO_IMM16_X0_TLS_GD: u32 = 66;
```

#### Constant `R_TILEPRO_IMM16_X1_TLS_GD`

X1 pipe 16-bit TLS GD offset

```rust
pub const R_TILEPRO_IMM16_X1_TLS_GD: u32 = 67;
```

#### Constant `R_TILEPRO_IMM16_X0_TLS_GD_LO`

X0 pipe low 16-bit TLS GD offset

```rust
pub const R_TILEPRO_IMM16_X0_TLS_GD_LO: u32 = 68;
```

#### Constant `R_TILEPRO_IMM16_X1_TLS_GD_LO`

X1 pipe low 16-bit TLS GD offset

```rust
pub const R_TILEPRO_IMM16_X1_TLS_GD_LO: u32 = 69;
```

#### Constant `R_TILEPRO_IMM16_X0_TLS_GD_HI`

X0 pipe high 16-bit TLS GD offset

```rust
pub const R_TILEPRO_IMM16_X0_TLS_GD_HI: u32 = 70;
```

#### Constant `R_TILEPRO_IMM16_X1_TLS_GD_HI`

X1 pipe high 16-bit TLS GD offset

```rust
pub const R_TILEPRO_IMM16_X1_TLS_GD_HI: u32 = 71;
```

#### Constant `R_TILEPRO_IMM16_X0_TLS_GD_HA`

X0 pipe ha() 16-bit TLS GD offset

```rust
pub const R_TILEPRO_IMM16_X0_TLS_GD_HA: u32 = 72;
```

#### Constant `R_TILEPRO_IMM16_X1_TLS_GD_HA`

X1 pipe ha() 16-bit TLS GD offset

```rust
pub const R_TILEPRO_IMM16_X1_TLS_GD_HA: u32 = 73;
```

#### Constant `R_TILEPRO_IMM16_X0_TLS_IE`

X0 pipe 16-bit TLS IE offset

```rust
pub const R_TILEPRO_IMM16_X0_TLS_IE: u32 = 74;
```

#### Constant `R_TILEPRO_IMM16_X1_TLS_IE`

X1 pipe 16-bit TLS IE offset

```rust
pub const R_TILEPRO_IMM16_X1_TLS_IE: u32 = 75;
```

#### Constant `R_TILEPRO_IMM16_X0_TLS_IE_LO`

X0 pipe low 16-bit TLS IE offset

```rust
pub const R_TILEPRO_IMM16_X0_TLS_IE_LO: u32 = 76;
```

#### Constant `R_TILEPRO_IMM16_X1_TLS_IE_LO`

X1 pipe low 16-bit TLS IE offset

```rust
pub const R_TILEPRO_IMM16_X1_TLS_IE_LO: u32 = 77;
```

#### Constant `R_TILEPRO_IMM16_X0_TLS_IE_HI`

X0 pipe high 16-bit TLS IE offset

```rust
pub const R_TILEPRO_IMM16_X0_TLS_IE_HI: u32 = 78;
```

#### Constant `R_TILEPRO_IMM16_X1_TLS_IE_HI`

X1 pipe high 16-bit TLS IE offset

```rust
pub const R_TILEPRO_IMM16_X1_TLS_IE_HI: u32 = 79;
```

#### Constant `R_TILEPRO_IMM16_X0_TLS_IE_HA`

X0 pipe ha() 16-bit TLS IE offset

```rust
pub const R_TILEPRO_IMM16_X0_TLS_IE_HA: u32 = 80;
```

#### Constant `R_TILEPRO_IMM16_X1_TLS_IE_HA`

X1 pipe ha() 16-bit TLS IE offset

```rust
pub const R_TILEPRO_IMM16_X1_TLS_IE_HA: u32 = 81;
```

#### Constant `R_TILEPRO_TLS_DTPMOD32`

ID of module containing symbol

```rust
pub const R_TILEPRO_TLS_DTPMOD32: u32 = 82;
```

#### Constant `R_TILEPRO_TLS_DTPOFF32`

Offset in TLS block

```rust
pub const R_TILEPRO_TLS_DTPOFF32: u32 = 83;
```

#### Constant `R_TILEPRO_TLS_TPOFF32`

Offset in static TLS block

```rust
pub const R_TILEPRO_TLS_TPOFF32: u32 = 84;
```

#### Constant `R_TILEPRO_IMM16_X0_TLS_LE`

X0 pipe 16-bit TLS LE offset

```rust
pub const R_TILEPRO_IMM16_X0_TLS_LE: u32 = 85;
```

#### Constant `R_TILEPRO_IMM16_X1_TLS_LE`

X1 pipe 16-bit TLS LE offset

```rust
pub const R_TILEPRO_IMM16_X1_TLS_LE: u32 = 86;
```

#### Constant `R_TILEPRO_IMM16_X0_TLS_LE_LO`

X0 pipe low 16-bit TLS LE offset

```rust
pub const R_TILEPRO_IMM16_X0_TLS_LE_LO: u32 = 87;
```

#### Constant `R_TILEPRO_IMM16_X1_TLS_LE_LO`

X1 pipe low 16-bit TLS LE offset

```rust
pub const R_TILEPRO_IMM16_X1_TLS_LE_LO: u32 = 88;
```

#### Constant `R_TILEPRO_IMM16_X0_TLS_LE_HI`

X0 pipe high 16-bit TLS LE offset

```rust
pub const R_TILEPRO_IMM16_X0_TLS_LE_HI: u32 = 89;
```

#### Constant `R_TILEPRO_IMM16_X1_TLS_LE_HI`

X1 pipe high 16-bit TLS LE offset

```rust
pub const R_TILEPRO_IMM16_X1_TLS_LE_HI: u32 = 90;
```

#### Constant `R_TILEPRO_IMM16_X0_TLS_LE_HA`

X0 pipe ha() 16-bit TLS LE offset

```rust
pub const R_TILEPRO_IMM16_X0_TLS_LE_HA: u32 = 91;
```

#### Constant `R_TILEPRO_IMM16_X1_TLS_LE_HA`

X1 pipe ha() 16-bit TLS LE offset

```rust
pub const R_TILEPRO_IMM16_X1_TLS_LE_HA: u32 = 92;
```

#### Constant `R_TILEPRO_GNU_VTINHERIT`

GNU C++ vtable hierarchy

```rust
pub const R_TILEPRO_GNU_VTINHERIT: u32 = 128;
```

#### Constant `R_TILEPRO_GNU_VTENTRY`

GNU C++ vtable member usage

```rust
pub const R_TILEPRO_GNU_VTENTRY: u32 = 129;
```

#### Constant `R_TILEGX_NONE`

No reloc

```rust
pub const R_TILEGX_NONE: u32 = 0;
```

#### Constant `R_TILEGX_64`

Direct 64 bit

```rust
pub const R_TILEGX_64: u32 = 1;
```

#### Constant `R_TILEGX_32`

Direct 32 bit

```rust
pub const R_TILEGX_32: u32 = 2;
```

#### Constant `R_TILEGX_16`

Direct 16 bit

```rust
pub const R_TILEGX_16: u32 = 3;
```

#### Constant `R_TILEGX_8`

Direct 8 bit

```rust
pub const R_TILEGX_8: u32 = 4;
```

#### Constant `R_TILEGX_64_PCREL`

PC relative 64 bit

```rust
pub const R_TILEGX_64_PCREL: u32 = 5;
```

#### Constant `R_TILEGX_32_PCREL`

PC relative 32 bit

```rust
pub const R_TILEGX_32_PCREL: u32 = 6;
```

#### Constant `R_TILEGX_16_PCREL`

PC relative 16 bit

```rust
pub const R_TILEGX_16_PCREL: u32 = 7;
```

#### Constant `R_TILEGX_8_PCREL`

PC relative 8 bit

```rust
pub const R_TILEGX_8_PCREL: u32 = 8;
```

#### Constant `R_TILEGX_HW0`

hword 0 16-bit

```rust
pub const R_TILEGX_HW0: u32 = 9;
```

#### Constant `R_TILEGX_HW1`

hword 1 16-bit

```rust
pub const R_TILEGX_HW1: u32 = 10;
```

#### Constant `R_TILEGX_HW2`

hword 2 16-bit

```rust
pub const R_TILEGX_HW2: u32 = 11;
```

#### Constant `R_TILEGX_HW3`

hword 3 16-bit

```rust
pub const R_TILEGX_HW3: u32 = 12;
```

#### Constant `R_TILEGX_HW0_LAST`

last hword 0 16-bit

```rust
pub const R_TILEGX_HW0_LAST: u32 = 13;
```

#### Constant `R_TILEGX_HW1_LAST`

last hword 1 16-bit

```rust
pub const R_TILEGX_HW1_LAST: u32 = 14;
```

#### Constant `R_TILEGX_HW2_LAST`

last hword 2 16-bit

```rust
pub const R_TILEGX_HW2_LAST: u32 = 15;
```

#### Constant `R_TILEGX_COPY`

Copy relocation

```rust
pub const R_TILEGX_COPY: u32 = 16;
```

#### Constant `R_TILEGX_GLOB_DAT`

Create GOT entry

```rust
pub const R_TILEGX_GLOB_DAT: u32 = 17;
```

#### Constant `R_TILEGX_JMP_SLOT`

Create PLT entry

```rust
pub const R_TILEGX_JMP_SLOT: u32 = 18;
```

#### Constant `R_TILEGX_RELATIVE`

Adjust by program base

```rust
pub const R_TILEGX_RELATIVE: u32 = 19;
```

#### Constant `R_TILEGX_BROFF_X1`

X1 pipe branch offset

```rust
pub const R_TILEGX_BROFF_X1: u32 = 20;
```

#### Constant `R_TILEGX_JUMPOFF_X1`

X1 pipe jump offset

```rust
pub const R_TILEGX_JUMPOFF_X1: u32 = 21;
```

#### Constant `R_TILEGX_JUMPOFF_X1_PLT`

X1 pipe jump offset to PLT

```rust
pub const R_TILEGX_JUMPOFF_X1_PLT: u32 = 22;
```

#### Constant `R_TILEGX_IMM8_X0`

X0 pipe 8-bit

```rust
pub const R_TILEGX_IMM8_X0: u32 = 23;
```

#### Constant `R_TILEGX_IMM8_Y0`

Y0 pipe 8-bit

```rust
pub const R_TILEGX_IMM8_Y0: u32 = 24;
```

#### Constant `R_TILEGX_IMM8_X1`

X1 pipe 8-bit

```rust
pub const R_TILEGX_IMM8_X1: u32 = 25;
```

#### Constant `R_TILEGX_IMM8_Y1`

Y1 pipe 8-bit

```rust
pub const R_TILEGX_IMM8_Y1: u32 = 26;
```

#### Constant `R_TILEGX_DEST_IMM8_X1`

X1 pipe destination 8-bit

```rust
pub const R_TILEGX_DEST_IMM8_X1: u32 = 27;
```

#### Constant `R_TILEGX_MT_IMM14_X1`

X1 pipe mtspr

```rust
pub const R_TILEGX_MT_IMM14_X1: u32 = 28;
```

#### Constant `R_TILEGX_MF_IMM14_X1`

X1 pipe mfspr

```rust
pub const R_TILEGX_MF_IMM14_X1: u32 = 29;
```

#### Constant `R_TILEGX_MMSTART_X0`

X0 pipe mm "start"

```rust
pub const R_TILEGX_MMSTART_X0: u32 = 30;
```

#### Constant `R_TILEGX_MMEND_X0`

X0 pipe mm "end"

```rust
pub const R_TILEGX_MMEND_X0: u32 = 31;
```

#### Constant `R_TILEGX_SHAMT_X0`

X0 pipe shift amount

```rust
pub const R_TILEGX_SHAMT_X0: u32 = 32;
```

#### Constant `R_TILEGX_SHAMT_X1`

X1 pipe shift amount

```rust
pub const R_TILEGX_SHAMT_X1: u32 = 33;
```

#### Constant `R_TILEGX_SHAMT_Y0`

Y0 pipe shift amount

```rust
pub const R_TILEGX_SHAMT_Y0: u32 = 34;
```

#### Constant `R_TILEGX_SHAMT_Y1`

Y1 pipe shift amount

```rust
pub const R_TILEGX_SHAMT_Y1: u32 = 35;
```

#### Constant `R_TILEGX_IMM16_X0_HW0`

X0 pipe hword 0

```rust
pub const R_TILEGX_IMM16_X0_HW0: u32 = 36;
```

#### Constant `R_TILEGX_IMM16_X1_HW0`

X1 pipe hword 0

```rust
pub const R_TILEGX_IMM16_X1_HW0: u32 = 37;
```

#### Constant `R_TILEGX_IMM16_X0_HW1`

X0 pipe hword 1

```rust
pub const R_TILEGX_IMM16_X0_HW1: u32 = 38;
```

#### Constant `R_TILEGX_IMM16_X1_HW1`

X1 pipe hword 1

```rust
pub const R_TILEGX_IMM16_X1_HW1: u32 = 39;
```

#### Constant `R_TILEGX_IMM16_X0_HW2`

X0 pipe hword 2

```rust
pub const R_TILEGX_IMM16_X0_HW2: u32 = 40;
```

#### Constant `R_TILEGX_IMM16_X1_HW2`

X1 pipe hword 2

```rust
pub const R_TILEGX_IMM16_X1_HW2: u32 = 41;
```

#### Constant `R_TILEGX_IMM16_X0_HW3`

X0 pipe hword 3

```rust
pub const R_TILEGX_IMM16_X0_HW3: u32 = 42;
```

#### Constant `R_TILEGX_IMM16_X1_HW3`

X1 pipe hword 3

```rust
pub const R_TILEGX_IMM16_X1_HW3: u32 = 43;
```

#### Constant `R_TILEGX_IMM16_X0_HW0_LAST`

X0 pipe last hword 0

```rust
pub const R_TILEGX_IMM16_X0_HW0_LAST: u32 = 44;
```

#### Constant `R_TILEGX_IMM16_X1_HW0_LAST`

X1 pipe last hword 0

```rust
pub const R_TILEGX_IMM16_X1_HW0_LAST: u32 = 45;
```

#### Constant `R_TILEGX_IMM16_X0_HW1_LAST`

X0 pipe last hword 1

```rust
pub const R_TILEGX_IMM16_X0_HW1_LAST: u32 = 46;
```

#### Constant `R_TILEGX_IMM16_X1_HW1_LAST`

X1 pipe last hword 1

```rust
pub const R_TILEGX_IMM16_X1_HW1_LAST: u32 = 47;
```

#### Constant `R_TILEGX_IMM16_X0_HW2_LAST`

X0 pipe last hword 2

```rust
pub const R_TILEGX_IMM16_X0_HW2_LAST: u32 = 48;
```

#### Constant `R_TILEGX_IMM16_X1_HW2_LAST`

X1 pipe last hword 2

```rust
pub const R_TILEGX_IMM16_X1_HW2_LAST: u32 = 49;
```

#### Constant `R_TILEGX_IMM16_X0_HW0_PCREL`

X0 pipe PC relative hword 0

```rust
pub const R_TILEGX_IMM16_X0_HW0_PCREL: u32 = 50;
```

#### Constant `R_TILEGX_IMM16_X1_HW0_PCREL`

X1 pipe PC relative hword 0

```rust
pub const R_TILEGX_IMM16_X1_HW0_PCREL: u32 = 51;
```

#### Constant `R_TILEGX_IMM16_X0_HW1_PCREL`

X0 pipe PC relative hword 1

```rust
pub const R_TILEGX_IMM16_X0_HW1_PCREL: u32 = 52;
```

#### Constant `R_TILEGX_IMM16_X1_HW1_PCREL`

X1 pipe PC relative hword 1

```rust
pub const R_TILEGX_IMM16_X1_HW1_PCREL: u32 = 53;
```

#### Constant `R_TILEGX_IMM16_X0_HW2_PCREL`

X0 pipe PC relative hword 2

```rust
pub const R_TILEGX_IMM16_X0_HW2_PCREL: u32 = 54;
```

#### Constant `R_TILEGX_IMM16_X1_HW2_PCREL`

X1 pipe PC relative hword 2

```rust
pub const R_TILEGX_IMM16_X1_HW2_PCREL: u32 = 55;
```

#### Constant `R_TILEGX_IMM16_X0_HW3_PCREL`

X0 pipe PC relative hword 3

```rust
pub const R_TILEGX_IMM16_X0_HW3_PCREL: u32 = 56;
```

#### Constant `R_TILEGX_IMM16_X1_HW3_PCREL`

X1 pipe PC relative hword 3

```rust
pub const R_TILEGX_IMM16_X1_HW3_PCREL: u32 = 57;
```

#### Constant `R_TILEGX_IMM16_X0_HW0_LAST_PCREL`

X0 pipe PC-rel last hword 0

```rust
pub const R_TILEGX_IMM16_X0_HW0_LAST_PCREL: u32 = 58;
```

#### Constant `R_TILEGX_IMM16_X1_HW0_LAST_PCREL`

X1 pipe PC-rel last hword 0

```rust
pub const R_TILEGX_IMM16_X1_HW0_LAST_PCREL: u32 = 59;
```

#### Constant `R_TILEGX_IMM16_X0_HW1_LAST_PCREL`

X0 pipe PC-rel last hword 1

```rust
pub const R_TILEGX_IMM16_X0_HW1_LAST_PCREL: u32 = 60;
```

#### Constant `R_TILEGX_IMM16_X1_HW1_LAST_PCREL`

X1 pipe PC-rel last hword 1

```rust
pub const R_TILEGX_IMM16_X1_HW1_LAST_PCREL: u32 = 61;
```

#### Constant `R_TILEGX_IMM16_X0_HW2_LAST_PCREL`

X0 pipe PC-rel last hword 2

```rust
pub const R_TILEGX_IMM16_X0_HW2_LAST_PCREL: u32 = 62;
```

#### Constant `R_TILEGX_IMM16_X1_HW2_LAST_PCREL`

X1 pipe PC-rel last hword 2

```rust
pub const R_TILEGX_IMM16_X1_HW2_LAST_PCREL: u32 = 63;
```

#### Constant `R_TILEGX_IMM16_X0_HW0_GOT`

X0 pipe hword 0 GOT offset

```rust
pub const R_TILEGX_IMM16_X0_HW0_GOT: u32 = 64;
```

#### Constant `R_TILEGX_IMM16_X1_HW0_GOT`

X1 pipe hword 0 GOT offset

```rust
pub const R_TILEGX_IMM16_X1_HW0_GOT: u32 = 65;
```

#### Constant `R_TILEGX_IMM16_X0_HW0_PLT_PCREL`

X0 pipe PC-rel PLT hword 0

```rust
pub const R_TILEGX_IMM16_X0_HW0_PLT_PCREL: u32 = 66;
```

#### Constant `R_TILEGX_IMM16_X1_HW0_PLT_PCREL`

X1 pipe PC-rel PLT hword 0

```rust
pub const R_TILEGX_IMM16_X1_HW0_PLT_PCREL: u32 = 67;
```

#### Constant `R_TILEGX_IMM16_X0_HW1_PLT_PCREL`

X0 pipe PC-rel PLT hword 1

```rust
pub const R_TILEGX_IMM16_X0_HW1_PLT_PCREL: u32 = 68;
```

#### Constant `R_TILEGX_IMM16_X1_HW1_PLT_PCREL`

X1 pipe PC-rel PLT hword 1

```rust
pub const R_TILEGX_IMM16_X1_HW1_PLT_PCREL: u32 = 69;
```

#### Constant `R_TILEGX_IMM16_X0_HW2_PLT_PCREL`

X0 pipe PC-rel PLT hword 2

```rust
pub const R_TILEGX_IMM16_X0_HW2_PLT_PCREL: u32 = 70;
```

#### Constant `R_TILEGX_IMM16_X1_HW2_PLT_PCREL`

X1 pipe PC-rel PLT hword 2

```rust
pub const R_TILEGX_IMM16_X1_HW2_PLT_PCREL: u32 = 71;
```

#### Constant `R_TILEGX_IMM16_X0_HW0_LAST_GOT`

X0 pipe last hword 0 GOT offset

```rust
pub const R_TILEGX_IMM16_X0_HW0_LAST_GOT: u32 = 72;
```

#### Constant `R_TILEGX_IMM16_X1_HW0_LAST_GOT`

X1 pipe last hword 0 GOT offset

```rust
pub const R_TILEGX_IMM16_X1_HW0_LAST_GOT: u32 = 73;
```

#### Constant `R_TILEGX_IMM16_X0_HW1_LAST_GOT`

X0 pipe last hword 1 GOT offset

```rust
pub const R_TILEGX_IMM16_X0_HW1_LAST_GOT: u32 = 74;
```

#### Constant `R_TILEGX_IMM16_X1_HW1_LAST_GOT`

X1 pipe last hword 1 GOT offset

```rust
pub const R_TILEGX_IMM16_X1_HW1_LAST_GOT: u32 = 75;
```

#### Constant `R_TILEGX_IMM16_X0_HW3_PLT_PCREL`

X0 pipe PC-rel PLT hword 3

```rust
pub const R_TILEGX_IMM16_X0_HW3_PLT_PCREL: u32 = 76;
```

#### Constant `R_TILEGX_IMM16_X1_HW3_PLT_PCREL`

X1 pipe PC-rel PLT hword 3

```rust
pub const R_TILEGX_IMM16_X1_HW3_PLT_PCREL: u32 = 77;
```

#### Constant `R_TILEGX_IMM16_X0_HW0_TLS_GD`

X0 pipe hword 0 TLS GD offset

```rust
pub const R_TILEGX_IMM16_X0_HW0_TLS_GD: u32 = 78;
```

#### Constant `R_TILEGX_IMM16_X1_HW0_TLS_GD`

X1 pipe hword 0 TLS GD offset

```rust
pub const R_TILEGX_IMM16_X1_HW0_TLS_GD: u32 = 79;
```

#### Constant `R_TILEGX_IMM16_X0_HW0_TLS_LE`

X0 pipe hword 0 TLS LE offset

```rust
pub const R_TILEGX_IMM16_X0_HW0_TLS_LE: u32 = 80;
```

#### Constant `R_TILEGX_IMM16_X1_HW0_TLS_LE`

X1 pipe hword 0 TLS LE offset

```rust
pub const R_TILEGX_IMM16_X1_HW0_TLS_LE: u32 = 81;
```

#### Constant `R_TILEGX_IMM16_X0_HW0_LAST_TLS_LE`

X0 pipe last hword 0 LE off

```rust
pub const R_TILEGX_IMM16_X0_HW0_LAST_TLS_LE: u32 = 82;
```

#### Constant `R_TILEGX_IMM16_X1_HW0_LAST_TLS_LE`

X1 pipe last hword 0 LE off

```rust
pub const R_TILEGX_IMM16_X1_HW0_LAST_TLS_LE: u32 = 83;
```

#### Constant `R_TILEGX_IMM16_X0_HW1_LAST_TLS_LE`

X0 pipe last hword 1 LE off

```rust
pub const R_TILEGX_IMM16_X0_HW1_LAST_TLS_LE: u32 = 84;
```

#### Constant `R_TILEGX_IMM16_X1_HW1_LAST_TLS_LE`

X1 pipe last hword 1 LE off

```rust
pub const R_TILEGX_IMM16_X1_HW1_LAST_TLS_LE: u32 = 85;
```

#### Constant `R_TILEGX_IMM16_X0_HW0_LAST_TLS_GD`

X0 pipe last hword 0 GD off

```rust
pub const R_TILEGX_IMM16_X0_HW0_LAST_TLS_GD: u32 = 86;
```

#### Constant `R_TILEGX_IMM16_X1_HW0_LAST_TLS_GD`

X1 pipe last hword 0 GD off

```rust
pub const R_TILEGX_IMM16_X1_HW0_LAST_TLS_GD: u32 = 87;
```

#### Constant `R_TILEGX_IMM16_X0_HW1_LAST_TLS_GD`

X0 pipe last hword 1 GD off

```rust
pub const R_TILEGX_IMM16_X0_HW1_LAST_TLS_GD: u32 = 88;
```

#### Constant `R_TILEGX_IMM16_X1_HW1_LAST_TLS_GD`

X1 pipe last hword 1 GD off

```rust
pub const R_TILEGX_IMM16_X1_HW1_LAST_TLS_GD: u32 = 89;
```

#### Constant `R_TILEGX_IMM16_X0_HW0_TLS_IE`

X0 pipe hword 0 TLS IE offset

```rust
pub const R_TILEGX_IMM16_X0_HW0_TLS_IE: u32 = 92;
```

#### Constant `R_TILEGX_IMM16_X1_HW0_TLS_IE`

X1 pipe hword 0 TLS IE offset

```rust
pub const R_TILEGX_IMM16_X1_HW0_TLS_IE: u32 = 93;
```

#### Constant `R_TILEGX_IMM16_X0_HW0_LAST_PLT_PCREL`

X0 pipe PC-rel PLT last hword 0

```rust
pub const R_TILEGX_IMM16_X0_HW0_LAST_PLT_PCREL: u32 = 94;
```

#### Constant `R_TILEGX_IMM16_X1_HW0_LAST_PLT_PCREL`

X1 pipe PC-rel PLT last hword 0

```rust
pub const R_TILEGX_IMM16_X1_HW0_LAST_PLT_PCREL: u32 = 95;
```

#### Constant `R_TILEGX_IMM16_X0_HW1_LAST_PLT_PCREL`

X0 pipe PC-rel PLT last hword 1

```rust
pub const R_TILEGX_IMM16_X0_HW1_LAST_PLT_PCREL: u32 = 96;
```

#### Constant `R_TILEGX_IMM16_X1_HW1_LAST_PLT_PCREL`

X1 pipe PC-rel PLT last hword 1

```rust
pub const R_TILEGX_IMM16_X1_HW1_LAST_PLT_PCREL: u32 = 97;
```

#### Constant `R_TILEGX_IMM16_X0_HW2_LAST_PLT_PCREL`

X0 pipe PC-rel PLT last hword 2

```rust
pub const R_TILEGX_IMM16_X0_HW2_LAST_PLT_PCREL: u32 = 98;
```

#### Constant `R_TILEGX_IMM16_X1_HW2_LAST_PLT_PCREL`

X1 pipe PC-rel PLT last hword 2

```rust
pub const R_TILEGX_IMM16_X1_HW2_LAST_PLT_PCREL: u32 = 99;
```

#### Constant `R_TILEGX_IMM16_X0_HW0_LAST_TLS_IE`

X0 pipe last hword 0 IE off

```rust
pub const R_TILEGX_IMM16_X0_HW0_LAST_TLS_IE: u32 = 100;
```

#### Constant `R_TILEGX_IMM16_X1_HW0_LAST_TLS_IE`

X1 pipe last hword 0 IE off

```rust
pub const R_TILEGX_IMM16_X1_HW0_LAST_TLS_IE: u32 = 101;
```

#### Constant `R_TILEGX_IMM16_X0_HW1_LAST_TLS_IE`

X0 pipe last hword 1 IE off

```rust
pub const R_TILEGX_IMM16_X0_HW1_LAST_TLS_IE: u32 = 102;
```

#### Constant `R_TILEGX_IMM16_X1_HW1_LAST_TLS_IE`

X1 pipe last hword 1 IE off

```rust
pub const R_TILEGX_IMM16_X1_HW1_LAST_TLS_IE: u32 = 103;
```

#### Constant `R_TILEGX_TLS_DTPMOD64`

64-bit ID of symbol's module

```rust
pub const R_TILEGX_TLS_DTPMOD64: u32 = 106;
```

#### Constant `R_TILEGX_TLS_DTPOFF64`

64-bit offset in TLS block

```rust
pub const R_TILEGX_TLS_DTPOFF64: u32 = 107;
```

#### Constant `R_TILEGX_TLS_TPOFF64`

64-bit offset in static TLS block

```rust
pub const R_TILEGX_TLS_TPOFF64: u32 = 108;
```

#### Constant `R_TILEGX_TLS_DTPMOD32`

32-bit ID of symbol's module

```rust
pub const R_TILEGX_TLS_DTPMOD32: u32 = 109;
```

#### Constant `R_TILEGX_TLS_DTPOFF32`

32-bit offset in TLS block

```rust
pub const R_TILEGX_TLS_DTPOFF32: u32 = 110;
```

#### Constant `R_TILEGX_TLS_TPOFF32`

32-bit offset in static TLS block

```rust
pub const R_TILEGX_TLS_TPOFF32: u32 = 111;
```

#### Constant `R_TILEGX_TLS_GD_CALL`

"jal" for TLS GD

```rust
pub const R_TILEGX_TLS_GD_CALL: u32 = 112;
```

#### Constant `R_TILEGX_IMM8_X0_TLS_GD_ADD`

X0 pipe "addi" for TLS GD

```rust
pub const R_TILEGX_IMM8_X0_TLS_GD_ADD: u32 = 113;
```

#### Constant `R_TILEGX_IMM8_X1_TLS_GD_ADD`

X1 pipe "addi" for TLS GD

```rust
pub const R_TILEGX_IMM8_X1_TLS_GD_ADD: u32 = 114;
```

#### Constant `R_TILEGX_IMM8_Y0_TLS_GD_ADD`

Y0 pipe "addi" for TLS GD

```rust
pub const R_TILEGX_IMM8_Y0_TLS_GD_ADD: u32 = 115;
```

#### Constant `R_TILEGX_IMM8_Y1_TLS_GD_ADD`

Y1 pipe "addi" for TLS GD

```rust
pub const R_TILEGX_IMM8_Y1_TLS_GD_ADD: u32 = 116;
```

#### Constant `R_TILEGX_TLS_IE_LOAD`

"ld_tls" for TLS IE

```rust
pub const R_TILEGX_TLS_IE_LOAD: u32 = 117;
```

#### Constant `R_TILEGX_IMM8_X0_TLS_ADD`

X0 pipe "addi" for TLS GD/IE

```rust
pub const R_TILEGX_IMM8_X0_TLS_ADD: u32 = 118;
```

#### Constant `R_TILEGX_IMM8_X1_TLS_ADD`

X1 pipe "addi" for TLS GD/IE

```rust
pub const R_TILEGX_IMM8_X1_TLS_ADD: u32 = 119;
```

#### Constant `R_TILEGX_IMM8_Y0_TLS_ADD`

Y0 pipe "addi" for TLS GD/IE

```rust
pub const R_TILEGX_IMM8_Y0_TLS_ADD: u32 = 120;
```

#### Constant `R_TILEGX_IMM8_Y1_TLS_ADD`

Y1 pipe "addi" for TLS GD/IE

```rust
pub const R_TILEGX_IMM8_Y1_TLS_ADD: u32 = 121;
```

#### Constant `R_TILEGX_GNU_VTINHERIT`

GNU C++ vtable hierarchy

```rust
pub const R_TILEGX_GNU_VTINHERIT: u32 = 128;
```

#### Constant `R_TILEGX_GNU_VTENTRY`

GNU C++ vtable member usage

```rust
pub const R_TILEGX_GNU_VTENTRY: u32 = 129;
```

#### Constant `EF_RISCV_RVC`

```rust
pub const EF_RISCV_RVC: u32 = 0x0001;
```

#### Constant `EF_RISCV_FLOAT_ABI`

```rust
pub const EF_RISCV_FLOAT_ABI: u32 = 0x0006;
```

#### Constant `EF_RISCV_FLOAT_ABI_SOFT`

```rust
pub const EF_RISCV_FLOAT_ABI_SOFT: u32 = 0x0000;
```

#### Constant `EF_RISCV_FLOAT_ABI_SINGLE`

```rust
pub const EF_RISCV_FLOAT_ABI_SINGLE: u32 = 0x0002;
```

#### Constant `EF_RISCV_FLOAT_ABI_DOUBLE`

```rust
pub const EF_RISCV_FLOAT_ABI_DOUBLE: u32 = 0x0004;
```

#### Constant `EF_RISCV_FLOAT_ABI_QUAD`

```rust
pub const EF_RISCV_FLOAT_ABI_QUAD: u32 = 0x0006;
```

#### Constant `EF_RISCV_RVE`

```rust
pub const EF_RISCV_RVE: u32 = 0x0008;
```

#### Constant `EF_RISCV_TSO`

```rust
pub const EF_RISCV_TSO: u32 = 0x0010;
```

#### Constant `SHT_RISCV_ATTRIBUTES`

RISC-V attributes section.

```rust
pub const SHT_RISCV_ATTRIBUTES: u32 = _;
```

#### Constant `R_RISCV_NONE`

```rust
pub const R_RISCV_NONE: u32 = 0;
```

#### Constant `R_RISCV_32`

```rust
pub const R_RISCV_32: u32 = 1;
```

#### Constant `R_RISCV_64`

```rust
pub const R_RISCV_64: u32 = 2;
```

#### Constant `R_RISCV_RELATIVE`

```rust
pub const R_RISCV_RELATIVE: u32 = 3;
```

#### Constant `R_RISCV_COPY`

```rust
pub const R_RISCV_COPY: u32 = 4;
```

#### Constant `R_RISCV_JUMP_SLOT`

```rust
pub const R_RISCV_JUMP_SLOT: u32 = 5;
```

#### Constant `R_RISCV_TLS_DTPMOD32`

```rust
pub const R_RISCV_TLS_DTPMOD32: u32 = 6;
```

#### Constant `R_RISCV_TLS_DTPMOD64`

```rust
pub const R_RISCV_TLS_DTPMOD64: u32 = 7;
```

#### Constant `R_RISCV_TLS_DTPREL32`

```rust
pub const R_RISCV_TLS_DTPREL32: u32 = 8;
```

#### Constant `R_RISCV_TLS_DTPREL64`

```rust
pub const R_RISCV_TLS_DTPREL64: u32 = 9;
```

#### Constant `R_RISCV_TLS_TPREL32`

```rust
pub const R_RISCV_TLS_TPREL32: u32 = 10;
```

#### Constant `R_RISCV_TLS_TPREL64`

```rust
pub const R_RISCV_TLS_TPREL64: u32 = 11;
```

#### Constant `R_RISCV_BRANCH`

```rust
pub const R_RISCV_BRANCH: u32 = 16;
```

#### Constant `R_RISCV_JAL`

```rust
pub const R_RISCV_JAL: u32 = 17;
```

#### Constant `R_RISCV_CALL`

```rust
pub const R_RISCV_CALL: u32 = 18;
```

#### Constant `R_RISCV_CALL_PLT`

```rust
pub const R_RISCV_CALL_PLT: u32 = 19;
```

#### Constant `R_RISCV_GOT_HI20`

```rust
pub const R_RISCV_GOT_HI20: u32 = 20;
```

#### Constant `R_RISCV_TLS_GOT_HI20`

```rust
pub const R_RISCV_TLS_GOT_HI20: u32 = 21;
```

#### Constant `R_RISCV_TLS_GD_HI20`

```rust
pub const R_RISCV_TLS_GD_HI20: u32 = 22;
```

#### Constant `R_RISCV_PCREL_HI20`

```rust
pub const R_RISCV_PCREL_HI20: u32 = 23;
```

#### Constant `R_RISCV_PCREL_LO12_I`

```rust
pub const R_RISCV_PCREL_LO12_I: u32 = 24;
```

#### Constant `R_RISCV_PCREL_LO12_S`

```rust
pub const R_RISCV_PCREL_LO12_S: u32 = 25;
```

#### Constant `R_RISCV_HI20`

```rust
pub const R_RISCV_HI20: u32 = 26;
```

#### Constant `R_RISCV_LO12_I`

```rust
pub const R_RISCV_LO12_I: u32 = 27;
```

#### Constant `R_RISCV_LO12_S`

```rust
pub const R_RISCV_LO12_S: u32 = 28;
```

#### Constant `R_RISCV_TPREL_HI20`

```rust
pub const R_RISCV_TPREL_HI20: u32 = 29;
```

#### Constant `R_RISCV_TPREL_LO12_I`

```rust
pub const R_RISCV_TPREL_LO12_I: u32 = 30;
```

#### Constant `R_RISCV_TPREL_LO12_S`

```rust
pub const R_RISCV_TPREL_LO12_S: u32 = 31;
```

#### Constant `R_RISCV_TPREL_ADD`

```rust
pub const R_RISCV_TPREL_ADD: u32 = 32;
```

#### Constant `R_RISCV_ADD8`

```rust
pub const R_RISCV_ADD8: u32 = 33;
```

#### Constant `R_RISCV_ADD16`

```rust
pub const R_RISCV_ADD16: u32 = 34;
```

#### Constant `R_RISCV_ADD32`

```rust
pub const R_RISCV_ADD32: u32 = 35;
```

#### Constant `R_RISCV_ADD64`

```rust
pub const R_RISCV_ADD64: u32 = 36;
```

#### Constant `R_RISCV_SUB8`

```rust
pub const R_RISCV_SUB8: u32 = 37;
```

#### Constant `R_RISCV_SUB16`

```rust
pub const R_RISCV_SUB16: u32 = 38;
```

#### Constant `R_RISCV_SUB32`

```rust
pub const R_RISCV_SUB32: u32 = 39;
```

#### Constant `R_RISCV_SUB64`

```rust
pub const R_RISCV_SUB64: u32 = 40;
```

#### Constant `R_RISCV_GNU_VTINHERIT`

```rust
pub const R_RISCV_GNU_VTINHERIT: u32 = 41;
```

#### Constant `R_RISCV_GNU_VTENTRY`

```rust
pub const R_RISCV_GNU_VTENTRY: u32 = 42;
```

#### Constant `R_RISCV_ALIGN`

```rust
pub const R_RISCV_ALIGN: u32 = 43;
```

#### Constant `R_RISCV_RVC_BRANCH`

```rust
pub const R_RISCV_RVC_BRANCH: u32 = 44;
```

#### Constant `R_RISCV_RVC_JUMP`

```rust
pub const R_RISCV_RVC_JUMP: u32 = 45;
```

#### Constant `R_RISCV_RVC_LUI`

```rust
pub const R_RISCV_RVC_LUI: u32 = 46;
```

#### Constant `R_RISCV_GPREL_I`

```rust
pub const R_RISCV_GPREL_I: u32 = 47;
```

#### Constant `R_RISCV_GPREL_S`

```rust
pub const R_RISCV_GPREL_S: u32 = 48;
```

#### Constant `R_RISCV_TPREL_I`

```rust
pub const R_RISCV_TPREL_I: u32 = 49;
```

#### Constant `R_RISCV_TPREL_S`

```rust
pub const R_RISCV_TPREL_S: u32 = 50;
```

#### Constant `R_RISCV_RELAX`

```rust
pub const R_RISCV_RELAX: u32 = 51;
```

#### Constant `R_RISCV_SUB6`

```rust
pub const R_RISCV_SUB6: u32 = 52;
```

#### Constant `R_RISCV_SET6`

```rust
pub const R_RISCV_SET6: u32 = 53;
```

#### Constant `R_RISCV_SET8`

```rust
pub const R_RISCV_SET8: u32 = 54;
```

#### Constant `R_RISCV_SET16`

```rust
pub const R_RISCV_SET16: u32 = 55;
```

#### Constant `R_RISCV_SET32`

```rust
pub const R_RISCV_SET32: u32 = 56;
```

#### Constant `R_RISCV_32_PCREL`

```rust
pub const R_RISCV_32_PCREL: u32 = 57;
```

#### Constant `R_RISCV_IRELATIVE`

```rust
pub const R_RISCV_IRELATIVE: u32 = 58;
```

#### Constant `R_RISCV_PLT32`

```rust
pub const R_RISCV_PLT32: u32 = 59;
```

#### Constant `R_RISCV_SET_ULEB128`

```rust
pub const R_RISCV_SET_ULEB128: u32 = 60;
```

#### Constant `R_RISCV_SUB_ULEB128`

```rust
pub const R_RISCV_SUB_ULEB128: u32 = 61;
```

#### Constant `R_RISCV_TLSDESC_HI20`

```rust
pub const R_RISCV_TLSDESC_HI20: u32 = 62;
```

#### Constant `R_RISCV_TLSDESC_LOAD_LO12`

```rust
pub const R_RISCV_TLSDESC_LOAD_LO12: u32 = 63;
```

#### Constant `R_RISCV_TLSDESC_ADD_LO12`

```rust
pub const R_RISCV_TLSDESC_ADD_LO12: u32 = 64;
```

#### Constant `R_RISCV_TLSDESC_CALL`

```rust
pub const R_RISCV_TLSDESC_CALL: u32 = 65;
```

#### Constant `R_BPF_NONE`

No reloc

```rust
pub const R_BPF_NONE: u32 = 0;
```

#### Constant `R_BPF_64_64`

```rust
pub const R_BPF_64_64: u32 = 1;
```

#### Constant `R_BPF_64_32`

```rust
pub const R_BPF_64_32: u32 = 10;
```

#### Constant `R_SBF_NONE`

No reloc

```rust
pub const R_SBF_NONE: u32 = 0;
```

#### Constant `R_SBF_64_64`

```rust
pub const R_SBF_64_64: u32 = 1;
```

#### Constant `R_SBF_64_32`

```rust
pub const R_SBF_64_32: u32 = 10;
```

#### Constant `R_METAG_HIADDR16`

```rust
pub const R_METAG_HIADDR16: u32 = 0;
```

#### Constant `R_METAG_LOADDR16`

```rust
pub const R_METAG_LOADDR16: u32 = 1;
```

#### Constant `R_METAG_ADDR32`

32bit absolute address

```rust
pub const R_METAG_ADDR32: u32 = 2;
```

#### Constant `R_METAG_NONE`

No reloc

```rust
pub const R_METAG_NONE: u32 = 3;
```

#### Constant `R_METAG_RELBRANCH`

```rust
pub const R_METAG_RELBRANCH: u32 = 4;
```

#### Constant `R_METAG_GETSETOFF`

```rust
pub const R_METAG_GETSETOFF: u32 = 5;
```

#### Constant `R_METAG_REG32OP1`

```rust
pub const R_METAG_REG32OP1: u32 = 6;
```

#### Constant `R_METAG_REG32OP2`

```rust
pub const R_METAG_REG32OP2: u32 = 7;
```

#### Constant `R_METAG_REG32OP3`

```rust
pub const R_METAG_REG32OP3: u32 = 8;
```

#### Constant `R_METAG_REG16OP1`

```rust
pub const R_METAG_REG16OP1: u32 = 9;
```

#### Constant `R_METAG_REG16OP2`

```rust
pub const R_METAG_REG16OP2: u32 = 10;
```

#### Constant `R_METAG_REG16OP3`

```rust
pub const R_METAG_REG16OP3: u32 = 11;
```

#### Constant `R_METAG_REG32OP4`

```rust
pub const R_METAG_REG32OP4: u32 = 12;
```

#### Constant `R_METAG_HIOG`

```rust
pub const R_METAG_HIOG: u32 = 13;
```

#### Constant `R_METAG_LOOG`

```rust
pub const R_METAG_LOOG: u32 = 14;
```

#### Constant `R_METAG_REL8`

```rust
pub const R_METAG_REL8: u32 = 15;
```

#### Constant `R_METAG_REL16`

```rust
pub const R_METAG_REL16: u32 = 16;
```

#### Constant `R_METAG_GNU_VTINHERIT`

```rust
pub const R_METAG_GNU_VTINHERIT: u32 = 30;
```

#### Constant `R_METAG_GNU_VTENTRY`

```rust
pub const R_METAG_GNU_VTENTRY: u32 = 31;
```

#### Constant `R_METAG_HI16_GOTOFF`

```rust
pub const R_METAG_HI16_GOTOFF: u32 = 32;
```

#### Constant `R_METAG_LO16_GOTOFF`

```rust
pub const R_METAG_LO16_GOTOFF: u32 = 33;
```

#### Constant `R_METAG_GETSET_GOTOFF`

```rust
pub const R_METAG_GETSET_GOTOFF: u32 = 34;
```

#### Constant `R_METAG_GETSET_GOT`

```rust
pub const R_METAG_GETSET_GOT: u32 = 35;
```

#### Constant `R_METAG_HI16_GOTPC`

```rust
pub const R_METAG_HI16_GOTPC: u32 = 36;
```

#### Constant `R_METAG_LO16_GOTPC`

```rust
pub const R_METAG_LO16_GOTPC: u32 = 37;
```

#### Constant `R_METAG_HI16_PLT`

```rust
pub const R_METAG_HI16_PLT: u32 = 38;
```

#### Constant `R_METAG_LO16_PLT`

```rust
pub const R_METAG_LO16_PLT: u32 = 39;
```

#### Constant `R_METAG_RELBRANCH_PLT`

```rust
pub const R_METAG_RELBRANCH_PLT: u32 = 40;
```

#### Constant `R_METAG_GOTOFF`

```rust
pub const R_METAG_GOTOFF: u32 = 41;
```

#### Constant `R_METAG_PLT`

```rust
pub const R_METAG_PLT: u32 = 42;
```

#### Constant `R_METAG_COPY`

```rust
pub const R_METAG_COPY: u32 = 43;
```

#### Constant `R_METAG_JMP_SLOT`

```rust
pub const R_METAG_JMP_SLOT: u32 = 44;
```

#### Constant `R_METAG_RELATIVE`

```rust
pub const R_METAG_RELATIVE: u32 = 45;
```

#### Constant `R_METAG_GLOB_DAT`

```rust
pub const R_METAG_GLOB_DAT: u32 = 46;
```

#### Constant `R_METAG_TLS_GD`

```rust
pub const R_METAG_TLS_GD: u32 = 47;
```

#### Constant `R_METAG_TLS_LDM`

```rust
pub const R_METAG_TLS_LDM: u32 = 48;
```

#### Constant `R_METAG_TLS_LDO_HI16`

```rust
pub const R_METAG_TLS_LDO_HI16: u32 = 49;
```

#### Constant `R_METAG_TLS_LDO_LO16`

```rust
pub const R_METAG_TLS_LDO_LO16: u32 = 50;
```

#### Constant `R_METAG_TLS_LDO`

```rust
pub const R_METAG_TLS_LDO: u32 = 51;
```

#### Constant `R_METAG_TLS_IE`

```rust
pub const R_METAG_TLS_IE: u32 = 52;
```

#### Constant `R_METAG_TLS_IENONPIC`

```rust
pub const R_METAG_TLS_IENONPIC: u32 = 53;
```

#### Constant `R_METAG_TLS_IENONPIC_HI16`

```rust
pub const R_METAG_TLS_IENONPIC_HI16: u32 = 54;
```

#### Constant `R_METAG_TLS_IENONPIC_LO16`

```rust
pub const R_METAG_TLS_IENONPIC_LO16: u32 = 55;
```

#### Constant `R_METAG_TLS_TPOFF`

```rust
pub const R_METAG_TLS_TPOFF: u32 = 56;
```

#### Constant `R_METAG_TLS_DTPMOD`

```rust
pub const R_METAG_TLS_DTPMOD: u32 = 57;
```

#### Constant `R_METAG_TLS_DTPOFF`

```rust
pub const R_METAG_TLS_DTPOFF: u32 = 58;
```

#### Constant `R_METAG_TLS_LE`

```rust
pub const R_METAG_TLS_LE: u32 = 59;
```

#### Constant `R_METAG_TLS_LE_HI16`

```rust
pub const R_METAG_TLS_LE_HI16: u32 = 60;
```

#### Constant `R_METAG_TLS_LE_LO16`

```rust
pub const R_METAG_TLS_LE_LO16: u32 = 61;
```

#### Constant `R_NDS32_NONE`

```rust
pub const R_NDS32_NONE: u32 = 0;
```

#### Constant `R_NDS32_32_RELA`

```rust
pub const R_NDS32_32_RELA: u32 = 20;
```

#### Constant `R_NDS32_COPY`

```rust
pub const R_NDS32_COPY: u32 = 39;
```

#### Constant `R_NDS32_GLOB_DAT`

```rust
pub const R_NDS32_GLOB_DAT: u32 = 40;
```

#### Constant `R_NDS32_JMP_SLOT`

```rust
pub const R_NDS32_JMP_SLOT: u32 = 41;
```

#### Constant `R_NDS32_RELATIVE`

```rust
pub const R_NDS32_RELATIVE: u32 = 42;
```

#### Constant `R_NDS32_TLS_TPOFF`

```rust
pub const R_NDS32_TLS_TPOFF: u32 = 102;
```

#### Constant `R_NDS32_TLS_DESC`

```rust
pub const R_NDS32_TLS_DESC: u32 = 119;
```

#### Constant `EF_LARCH_ABI_MODIFIER_MASK`

Additional properties of the base ABI type, including the FP calling
convention.

```rust
pub const EF_LARCH_ABI_MODIFIER_MASK: u32 = 0x7;
```

#### Constant `EF_LARCH_ABI_SOFT_FLOAT`

Uses GPRs and the stack for parameter passing

```rust
pub const EF_LARCH_ABI_SOFT_FLOAT: u32 = 0x1;
```

#### Constant `EF_LARCH_ABI_SINGLE_FLOAT`

Uses GPRs, 32-bit FPRs and the stack for parameter passing

```rust
pub const EF_LARCH_ABI_SINGLE_FLOAT: u32 = 0x2;
```

#### Constant `EF_LARCH_ABI_DOUBLE_FLOAT`

Uses GPRs, 64-bit FPRs and the stack for parameter passing

```rust
pub const EF_LARCH_ABI_DOUBLE_FLOAT: u32 = 0x3;
```

#### Constant `EF_LARCH_OBJABI_V1`

Uses relocation types directly writing to immediate slots

```rust
pub const EF_LARCH_OBJABI_V1: u32 = 0x40;
```

#### Constant `R_LARCH_NONE`

No reloc

```rust
pub const R_LARCH_NONE: u32 = 0;
```

#### Constant `R_LARCH_32`

Runtime address resolving

```rust
pub const R_LARCH_32: u32 = 1;
```

#### Constant `R_LARCH_64`

Runtime address resolving

```rust
pub const R_LARCH_64: u32 = 2;
```

#### Constant `R_LARCH_RELATIVE`

Runtime fixup for load-address

```rust
pub const R_LARCH_RELATIVE: u32 = 3;
```

#### Constant `R_LARCH_COPY`

Runtime memory copy in executable

```rust
pub const R_LARCH_COPY: u32 = 4;
```

#### Constant `R_LARCH_JUMP_SLOT`

Runtime PLT supporting

```rust
pub const R_LARCH_JUMP_SLOT: u32 = 5;
```

#### Constant `R_LARCH_TLS_DTPMOD32`

Runtime relocation for TLS-GD

```rust
pub const R_LARCH_TLS_DTPMOD32: u32 = 6;
```

#### Constant `R_LARCH_TLS_DTPMOD64`

Runtime relocation for TLS-GD

```rust
pub const R_LARCH_TLS_DTPMOD64: u32 = 7;
```

#### Constant `R_LARCH_TLS_DTPREL32`

Runtime relocation for TLS-GD

```rust
pub const R_LARCH_TLS_DTPREL32: u32 = 8;
```

#### Constant `R_LARCH_TLS_DTPREL64`

Runtime relocation for TLS-GD

```rust
pub const R_LARCH_TLS_DTPREL64: u32 = 9;
```

#### Constant `R_LARCH_TLS_TPREL32`

Runtime relocation for TLE-IE

```rust
pub const R_LARCH_TLS_TPREL32: u32 = 10;
```

#### Constant `R_LARCH_TLS_TPREL64`

Runtime relocation for TLE-IE

```rust
pub const R_LARCH_TLS_TPREL64: u32 = 11;
```

#### Constant `R_LARCH_IRELATIVE`

Runtime local indirect function resolving

```rust
pub const R_LARCH_IRELATIVE: u32 = 12;
```

#### Constant `R_LARCH_MARK_LA`

Mark la.abs: load absolute address for static link.

```rust
pub const R_LARCH_MARK_LA: u32 = 20;
```

#### Constant `R_LARCH_MARK_PCREL`

Mark external label branch: access PC relative address for static link.

```rust
pub const R_LARCH_MARK_PCREL: u32 = 21;
```

#### Constant `R_LARCH_SOP_PUSH_PCREL`

Push PC-relative offset

```rust
pub const R_LARCH_SOP_PUSH_PCREL: u32 = 22;
```

#### Constant `R_LARCH_SOP_PUSH_ABSOLUTE`

Push constant or absolute address

```rust
pub const R_LARCH_SOP_PUSH_ABSOLUTE: u32 = 23;
```

#### Constant `R_LARCH_SOP_PUSH_DUP`

Duplicate stack top

```rust
pub const R_LARCH_SOP_PUSH_DUP: u32 = 24;
```

#### Constant `R_LARCH_SOP_PUSH_GPREL`

Push for access GOT entry

```rust
pub const R_LARCH_SOP_PUSH_GPREL: u32 = 25;
```

#### Constant `R_LARCH_SOP_PUSH_TLS_TPREL`

Push for TLS-LE

```rust
pub const R_LARCH_SOP_PUSH_TLS_TPREL: u32 = 26;
```

#### Constant `R_LARCH_SOP_PUSH_TLS_GOT`

Push for TLS-IE

```rust
pub const R_LARCH_SOP_PUSH_TLS_GOT: u32 = 27;
```

#### Constant `R_LARCH_SOP_PUSH_TLS_GD`

Push for TLS-GD

```rust
pub const R_LARCH_SOP_PUSH_TLS_GD: u32 = 28;
```

#### Constant `R_LARCH_SOP_PUSH_PLT_PCREL`

Push for external function calling

```rust
pub const R_LARCH_SOP_PUSH_PLT_PCREL: u32 = 29;
```

#### Constant `R_LARCH_SOP_ASSERT`

Assert stack top

```rust
pub const R_LARCH_SOP_ASSERT: u32 = 30;
```

#### Constant `R_LARCH_SOP_NOT`

Stack top logical not (unary)

```rust
pub const R_LARCH_SOP_NOT: u32 = 31;
```

#### Constant `R_LARCH_SOP_SUB`

Stack top subtraction (binary)

```rust
pub const R_LARCH_SOP_SUB: u32 = 32;
```

#### Constant `R_LARCH_SOP_SL`

Stack top left shift (binary)

```rust
pub const R_LARCH_SOP_SL: u32 = 33;
```

#### Constant `R_LARCH_SOP_SR`

Stack top right shift (binary)

```rust
pub const R_LARCH_SOP_SR: u32 = 34;
```

#### Constant `R_LARCH_SOP_ADD`

Stack top addition (binary)

```rust
pub const R_LARCH_SOP_ADD: u32 = 35;
```

#### Constant `R_LARCH_SOP_AND`

Stack top bitwise and (binary)

```rust
pub const R_LARCH_SOP_AND: u32 = 36;
```

#### Constant `R_LARCH_SOP_IF_ELSE`

Stack top selection (tertiary)

```rust
pub const R_LARCH_SOP_IF_ELSE: u32 = 37;
```

#### Constant `R_LARCH_SOP_POP_32_S_10_5`

Pop stack top to fill 5-bit signed immediate operand

```rust
pub const R_LARCH_SOP_POP_32_S_10_5: u32 = 38;
```

#### Constant `R_LARCH_SOP_POP_32_U_10_12`

Pop stack top to fill 12-bit unsigned immediate operand

```rust
pub const R_LARCH_SOP_POP_32_U_10_12: u32 = 39;
```

#### Constant `R_LARCH_SOP_POP_32_S_10_12`

Pop stack top to fill 12-bit signed immediate operand

```rust
pub const R_LARCH_SOP_POP_32_S_10_12: u32 = 40;
```

#### Constant `R_LARCH_SOP_POP_32_S_10_16`

Pop stack top to fill 16-bit signed immediate operand

```rust
pub const R_LARCH_SOP_POP_32_S_10_16: u32 = 41;
```

#### Constant `R_LARCH_SOP_POP_32_S_10_16_S2`

Pop stack top to fill 18-bit signed immediate operand with two trailing
zeros implied

```rust
pub const R_LARCH_SOP_POP_32_S_10_16_S2: u32 = 42;
```

#### Constant `R_LARCH_SOP_POP_32_S_5_20`

Pop stack top to fill 20-bit signed immediate operand

```rust
pub const R_LARCH_SOP_POP_32_S_5_20: u32 = 43;
```

#### Constant `R_LARCH_SOP_POP_32_S_0_5_10_16_S2`

Pop stack top to fill 23-bit signed immediate operand with two trailing
zeros implied

```rust
pub const R_LARCH_SOP_POP_32_S_0_5_10_16_S2: u32 = 44;
```

#### Constant `R_LARCH_SOP_POP_32_S_0_10_10_16_S2`

Pop stack top to fill 28-bit signed immediate operand with two trailing
zeros implied

```rust
pub const R_LARCH_SOP_POP_32_S_0_10_10_16_S2: u32 = 45;
```

#### Constant `R_LARCH_SOP_POP_32_U`

Pop stack top to fill an instruction

```rust
pub const R_LARCH_SOP_POP_32_U: u32 = 46;
```

#### Constant `R_LARCH_ADD8`

8-bit in-place addition

```rust
pub const R_LARCH_ADD8: u32 = 47;
```

#### Constant `R_LARCH_ADD16`

16-bit in-place addition

```rust
pub const R_LARCH_ADD16: u32 = 48;
```

#### Constant `R_LARCH_ADD24`

24-bit in-place addition

```rust
pub const R_LARCH_ADD24: u32 = 49;
```

#### Constant `R_LARCH_ADD32`

32-bit in-place addition

```rust
pub const R_LARCH_ADD32: u32 = 50;
```

#### Constant `R_LARCH_ADD64`

64-bit in-place addition

```rust
pub const R_LARCH_ADD64: u32 = 51;
```

#### Constant `R_LARCH_SUB8`

8-bit in-place subtraction

```rust
pub const R_LARCH_SUB8: u32 = 52;
```

#### Constant `R_LARCH_SUB16`

16-bit in-place subtraction

```rust
pub const R_LARCH_SUB16: u32 = 53;
```

#### Constant `R_LARCH_SUB24`

24-bit in-place subtraction

```rust
pub const R_LARCH_SUB24: u32 = 54;
```

#### Constant `R_LARCH_SUB32`

32-bit in-place subtraction

```rust
pub const R_LARCH_SUB32: u32 = 55;
```

#### Constant `R_LARCH_SUB64`

64-bit in-place subtraction

```rust
pub const R_LARCH_SUB64: u32 = 56;
```

#### Constant `R_LARCH_GNU_VTINHERIT`

GNU C++ vtable hierarchy

```rust
pub const R_LARCH_GNU_VTINHERIT: u32 = 57;
```

#### Constant `R_LARCH_GNU_VTENTRY`

GNU C++ vtable member usage

```rust
pub const R_LARCH_GNU_VTENTRY: u32 = 58;
```

#### Constant `R_LARCH_B16`

18-bit PC-relative jump offset with two trailing zeros

```rust
pub const R_LARCH_B16: u32 = 64;
```

#### Constant `R_LARCH_B21`

23-bit PC-relative jump offset with two trailing zeros

```rust
pub const R_LARCH_B21: u32 = 65;
```

#### Constant `R_LARCH_B26`

28-bit PC-relative jump offset with two trailing zeros

```rust
pub const R_LARCH_B26: u32 = 66;
```

#### Constant `R_LARCH_ABS_HI20`

12..=31 bits of 32/64-bit absolute address

```rust
pub const R_LARCH_ABS_HI20: u32 = 67;
```

#### Constant `R_LARCH_ABS_LO12`

0..=11 bits of 32/64-bit absolute address

```rust
pub const R_LARCH_ABS_LO12: u32 = 68;
```

#### Constant `R_LARCH_ABS64_LO20`

32..=51 bits of 64-bit absolute address

```rust
pub const R_LARCH_ABS64_LO20: u32 = 69;
```

#### Constant `R_LARCH_ABS64_HI12`

52..=63 bits of 64-bit absolute address

```rust
pub const R_LARCH_ABS64_HI12: u32 = 70;
```

#### Constant `R_LARCH_PCALA_HI20`

The signed 32-bit offset `offs` from `PC & 0xfffff000` to
`(S + A + 0x800) & 0xfffff000`, with 12 trailing zeros removed.

We define the *PC relative anchor* for `S + A` as `PC + offs` (`offs`
is sign-extended to VA bits).

```rust
pub const R_LARCH_PCALA_HI20: u32 = 71;
```

#### Constant `R_LARCH_PCALA_LO12`

Same as R_LARCH_ABS_LO12.  0..=11 bits of the 32/64-bit offset from the
[PC relative anchor][R_LARCH_PCALA_HI20].

```rust
pub const R_LARCH_PCALA_LO12: u32 = 72;
```

#### Constant `R_LARCH_PCALA64_LO20`

32..=51 bits of the 64-bit offset from the
[PC relative anchor][R_LARCH_PCALA_HI20].

```rust
pub const R_LARCH_PCALA64_LO20: u32 = 73;
```

#### Constant `R_LARCH_PCALA64_HI12`

52..=63 bits of the 64-bit offset from the
[PC relative anchor][R_LARCH_PCALA_HI20].

```rust
pub const R_LARCH_PCALA64_HI12: u32 = 74;
```

#### Constant `R_LARCH_GOT_PC_HI20`

The signed 32-bit offset `offs` from `PC & 0xfffff000` to
`(GP + G + 0x800) & 0xfffff000`, with 12 trailing zeros removed.

We define the *PC relative anchor* for the GOT entry at `GP + G` as
`PC + offs` (`offs` is sign-extended to VA bits).

```rust
pub const R_LARCH_GOT_PC_HI20: u32 = 75;
```

#### Constant `R_LARCH_GOT_PC_LO12`

0..=11 bits of the 32/64-bit offset from the
[PC relative anchor][R_LARCH_GOT_PC_HI20] to the GOT entry.

```rust
pub const R_LARCH_GOT_PC_LO12: u32 = 76;
```

#### Constant `R_LARCH_GOT64_PC_LO20`

32..=51 bits of the 64-bit offset from the
[PC relative anchor][R_LARCH_GOT_PC_HI20] to the GOT entry.

```rust
pub const R_LARCH_GOT64_PC_LO20: u32 = 77;
```

#### Constant `R_LARCH_GOT64_PC_HI12`

52..=63 bits of the 64-bit offset from the
[PC relative anchor][R_LARCH_GOT_PC_HI20] to the GOT entry.

```rust
pub const R_LARCH_GOT64_PC_HI12: u32 = 78;
```

#### Constant `R_LARCH_GOT_HI20`

12..=31 bits of 32/64-bit GOT entry absolute address

```rust
pub const R_LARCH_GOT_HI20: u32 = 79;
```

#### Constant `R_LARCH_GOT_LO12`

0..=11 bits of 32/64-bit GOT entry absolute address

```rust
pub const R_LARCH_GOT_LO12: u32 = 80;
```

#### Constant `R_LARCH_GOT64_LO20`

32..=51 bits of 64-bit GOT entry absolute address

```rust
pub const R_LARCH_GOT64_LO20: u32 = 81;
```

#### Constant `R_LARCH_GOT64_HI12`

52..=63 bits of 64-bit GOT entry absolute address

```rust
pub const R_LARCH_GOT64_HI12: u32 = 82;
```

#### Constant `R_LARCH_TLS_LE_HI20`

12..=31 bits of TLS LE 32/64-bit offset from thread pointer

```rust
pub const R_LARCH_TLS_LE_HI20: u32 = 83;
```

#### Constant `R_LARCH_TLS_LE_LO12`

0..=11 bits of TLS LE 32/64-bit offset from thread pointer

```rust
pub const R_LARCH_TLS_LE_LO12: u32 = 84;
```

#### Constant `R_LARCH_TLS_LE64_LO20`

32..=51 bits of TLS LE 64-bit offset from thread pointer

```rust
pub const R_LARCH_TLS_LE64_LO20: u32 = 85;
```

#### Constant `R_LARCH_TLS_LE64_HI12`

52..=63 bits of TLS LE 64-bit offset from thread pointer

```rust
pub const R_LARCH_TLS_LE64_HI12: u32 = 86;
```

#### Constant `R_LARCH_TLS_IE_PC_HI20`

The signed 32-bit offset `offs` from `PC & 0xfffff000` to
`(GP + IE + 0x800) & 0xfffff000`, with 12 trailing zeros removed.

We define the *PC relative anchor* for the TLS IE GOT entry at
`GP + IE` as `PC + offs` (`offs` is sign-extended to VA bits).

```rust
pub const R_LARCH_TLS_IE_PC_HI20: u32 = 87;
```

#### Constant `R_LARCH_TLS_IE_PC_LO12`

0..=12 bits of the 32/64-bit offset from the
[PC-relative anchor][R_LARCH_TLS_IE_PC_HI20] to the TLS IE GOT entry.

```rust
pub const R_LARCH_TLS_IE_PC_LO12: u32 = 88;
```

#### Constant `R_LARCH_TLS_IE64_PC_LO20`

32..=51 bits of the 64-bit offset from the
[PC-relative anchor][R_LARCH_TLS_IE_PC_HI20] to the TLS IE GOT entry.

```rust
pub const R_LARCH_TLS_IE64_PC_LO20: u32 = 89;
```

#### Constant `R_LARCH_TLS_IE64_PC_HI12`

52..=63 bits of the 64-bit offset from the
[PC-relative anchor][R_LARCH_TLS_IE_PC_HI20] to the TLS IE GOT entry.

```rust
pub const R_LARCH_TLS_IE64_PC_HI12: u32 = 90;
```

#### Constant `R_LARCH_TLS_IE_HI20`

12..=31 bits of TLS IE GOT entry 32/64-bit absolute address

```rust
pub const R_LARCH_TLS_IE_HI20: u32 = 91;
```

#### Constant `R_LARCH_TLS_IE_LO12`

0..=11 bits of TLS IE GOT entry 32/64-bit absolute address

```rust
pub const R_LARCH_TLS_IE_LO12: u32 = 92;
```

#### Constant `R_LARCH_TLS_IE64_LO20`

32..=51 bits of TLS IE GOT entry 64-bit absolute address

```rust
pub const R_LARCH_TLS_IE64_LO20: u32 = 93;
```

#### Constant `R_LARCH_TLS_IE64_HI12`

51..=63 bits of TLS IE GOT entry 64-bit absolute address

```rust
pub const R_LARCH_TLS_IE64_HI12: u32 = 94;
```

#### Constant `R_LARCH_TLS_LD_PC_HI20`

12..=31 bits of the offset from `PC` to `GP + GD + 0x800`, where
`GP + GD` is a TLS LD GOT entry

```rust
pub const R_LARCH_TLS_LD_PC_HI20: u32 = 95;
```

#### Constant `R_LARCH_TLS_LD_HI20`

12..=31 bits of TLS LD GOT entry 32/64-bit absolute address

```rust
pub const R_LARCH_TLS_LD_HI20: u32 = 96;
```

#### Constant `R_LARCH_TLS_GD_PC_HI20`

12..=31 bits of the 32/64-bit PC-relative offset to the PC-relative
anchor for the TLE GD GOT entry.

```rust
pub const R_LARCH_TLS_GD_PC_HI20: u32 = 97;
```

#### Constant `R_LARCH_TLS_GD_HI20`

12..=31 bits of TLS GD GOT entry 32/64-bit absolute address

```rust
pub const R_LARCH_TLS_GD_HI20: u32 = 98;
```

#### Constant `R_LARCH_32_PCREL`

32-bit PC relative

```rust
pub const R_LARCH_32_PCREL: u32 = 99;
```

#### Constant `R_LARCH_RELAX`

Paired with a normal relocation at the same address to indicate the
instruction can be relaxed

```rust
pub const R_LARCH_RELAX: u32 = 100;
```

#### Constant `R_LARCH_DELETE`

Reserved

```rust
pub const R_LARCH_DELETE: u32 = 101;
```

#### Constant `R_LARCH_ALIGN`

Delete some bytes to ensure the instruction at PC + A aligned to
`A.next_power_of_two()`-byte boundary

```rust
pub const R_LARCH_ALIGN: u32 = 102;
```

#### Constant `R_LARCH_PCREL20_S2`

22-bit PC-relative offset with two trailing zeros

```rust
pub const R_LARCH_PCREL20_S2: u32 = 103;
```

#### Constant `R_LARCH_CFA`

Reserved

```rust
pub const R_LARCH_CFA: u32 = 104;
```

#### Constant `R_LARCH_ADD6`

6-bit in-place addition

```rust
pub const R_LARCH_ADD6: u32 = 105;
```

#### Constant `R_LARCH_SUB6`

6-bit in-place subtraction

```rust
pub const R_LARCH_SUB6: u32 = 106;
```

#### Constant `R_LARCH_ADD_ULEB128`

LEB128 in-place addition

```rust
pub const R_LARCH_ADD_ULEB128: u32 = 107;
```

#### Constant `R_LARCH_SUB_ULEB128`

LEB128 in-place subtraction

```rust
pub const R_LARCH_SUB_ULEB128: u32 = 108;
```

#### Constant `R_LARCH_64_PCREL`

64-bit PC relative

```rust
pub const R_LARCH_64_PCREL: u32 = 109;
```

#### Constant `R_LARCH_CALL36`

18..=37 bits of `S + A - PC` into the `pcaddu18i` instruction at `PC`,
and 2..=17 bits of `S + A - PC` into the `jirl` instruction at `PC + 4`

```rust
pub const R_LARCH_CALL36: u32 = 110;
```

#### Constant `R_XTENSA_NONE`

```rust
pub const R_XTENSA_NONE: u32 = 0;
```

#### Constant `R_XTENSA_32`

```rust
pub const R_XTENSA_32: u32 = 1;
```

#### Constant `R_XTENSA_RTLD`

```rust
pub const R_XTENSA_RTLD: u32 = 2;
```

#### Constant `R_XTENSA_GLOB_DAT`

```rust
pub const R_XTENSA_GLOB_DAT: u32 = 3;
```

#### Constant `R_XTENSA_JMP_SLOT`

```rust
pub const R_XTENSA_JMP_SLOT: u32 = 4;
```

#### Constant `R_XTENSA_RELATIVE`

```rust
pub const R_XTENSA_RELATIVE: u32 = 5;
```

#### Constant `R_XTENSA_PLT`

```rust
pub const R_XTENSA_PLT: u32 = 6;
```

#### Constant `R_XTENSA_OP0`

```rust
pub const R_XTENSA_OP0: u32 = 8;
```

#### Constant `R_XTENSA_OP1`

```rust
pub const R_XTENSA_OP1: u32 = 9;
```

#### Constant `R_XTENSA_OP2`

```rust
pub const R_XTENSA_OP2: u32 = 10;
```

#### Constant `R_XTENSA_ASM_EXPAND`

```rust
pub const R_XTENSA_ASM_EXPAND: u32 = 11;
```

#### Constant `R_XTENSA_ASM_SIMPLIFY`

```rust
pub const R_XTENSA_ASM_SIMPLIFY: u32 = 12;
```

#### Constant `R_XTENSA_32_PCREL`

```rust
pub const R_XTENSA_32_PCREL: u32 = 14;
```

#### Constant `R_XTENSA_GNU_VTINHERIT`

```rust
pub const R_XTENSA_GNU_VTINHERIT: u32 = 15;
```

#### Constant `R_XTENSA_GNU_VTENTRY`

```rust
pub const R_XTENSA_GNU_VTENTRY: u32 = 16;
```

#### Constant `R_XTENSA_DIFF8`

```rust
pub const R_XTENSA_DIFF8: u32 = 17;
```

#### Constant `R_XTENSA_DIFF16`

```rust
pub const R_XTENSA_DIFF16: u32 = 18;
```

#### Constant `R_XTENSA_DIFF32`

```rust
pub const R_XTENSA_DIFF32: u32 = 19;
```

#### Constant `R_XTENSA_SLOT0_OP`

```rust
pub const R_XTENSA_SLOT0_OP: u32 = 20;
```

#### Constant `R_XTENSA_SLOT1_OP`

```rust
pub const R_XTENSA_SLOT1_OP: u32 = 21;
```

#### Constant `R_XTENSA_SLOT2_OP`

```rust
pub const R_XTENSA_SLOT2_OP: u32 = 22;
```

#### Constant `R_XTENSA_SLOT3_OP`

```rust
pub const R_XTENSA_SLOT3_OP: u32 = 23;
```

#### Constant `R_XTENSA_SLOT4_OP`

```rust
pub const R_XTENSA_SLOT4_OP: u32 = 24;
```

#### Constant `R_XTENSA_SLOT5_OP`

```rust
pub const R_XTENSA_SLOT5_OP: u32 = 25;
```

#### Constant `R_XTENSA_SLOT6_OP`

```rust
pub const R_XTENSA_SLOT6_OP: u32 = 26;
```

#### Constant `R_XTENSA_SLOT7_OP`

```rust
pub const R_XTENSA_SLOT7_OP: u32 = 27;
```

#### Constant `R_XTENSA_SLOT8_OP`

```rust
pub const R_XTENSA_SLOT8_OP: u32 = 28;
```

#### Constant `R_XTENSA_SLOT9_OP`

```rust
pub const R_XTENSA_SLOT9_OP: u32 = 29;
```

#### Constant `R_XTENSA_SLOT10_OP`

```rust
pub const R_XTENSA_SLOT10_OP: u32 = 30;
```

#### Constant `R_XTENSA_SLOT11_OP`

```rust
pub const R_XTENSA_SLOT11_OP: u32 = 31;
```

#### Constant `R_XTENSA_SLOT12_OP`

```rust
pub const R_XTENSA_SLOT12_OP: u32 = 32;
```

#### Constant `R_XTENSA_SLOT13_OP`

```rust
pub const R_XTENSA_SLOT13_OP: u32 = 33;
```

#### Constant `R_XTENSA_SLOT14_OP`

```rust
pub const R_XTENSA_SLOT14_OP: u32 = 34;
```

#### Constant `R_XTENSA_SLOT0_ALT`

```rust
pub const R_XTENSA_SLOT0_ALT: u32 = 35;
```

#### Constant `R_XTENSA_SLOT1_ALT`

```rust
pub const R_XTENSA_SLOT1_ALT: u32 = 36;
```

#### Constant `R_XTENSA_SLOT2_ALT`

```rust
pub const R_XTENSA_SLOT2_ALT: u32 = 37;
```

#### Constant `R_XTENSA_SLOT3_ALT`

```rust
pub const R_XTENSA_SLOT3_ALT: u32 = 38;
```

#### Constant `R_XTENSA_SLOT4_ALT`

```rust
pub const R_XTENSA_SLOT4_ALT: u32 = 39;
```

#### Constant `R_XTENSA_SLOT5_ALT`

```rust
pub const R_XTENSA_SLOT5_ALT: u32 = 40;
```

#### Constant `R_XTENSA_SLOT6_ALT`

```rust
pub const R_XTENSA_SLOT6_ALT: u32 = 41;
```

#### Constant `R_XTENSA_SLOT7_ALT`

```rust
pub const R_XTENSA_SLOT7_ALT: u32 = 42;
```

#### Constant `R_XTENSA_SLOT8_ALT`

```rust
pub const R_XTENSA_SLOT8_ALT: u32 = 43;
```

#### Constant `R_XTENSA_SLOT9_ALT`

```rust
pub const R_XTENSA_SLOT9_ALT: u32 = 44;
```

#### Constant `R_XTENSA_SLOT10_ALT`

```rust
pub const R_XTENSA_SLOT10_ALT: u32 = 45;
```

#### Constant `R_XTENSA_SLOT11_ALT`

```rust
pub const R_XTENSA_SLOT11_ALT: u32 = 46;
```

#### Constant `R_XTENSA_SLOT12_ALT`

```rust
pub const R_XTENSA_SLOT12_ALT: u32 = 47;
```

#### Constant `R_XTENSA_SLOT13_ALT`

```rust
pub const R_XTENSA_SLOT13_ALT: u32 = 48;
```

#### Constant `R_XTENSA_SLOT14_ALT`

```rust
pub const R_XTENSA_SLOT14_ALT: u32 = 49;
```

#### Constant `R_XTENSA_TLSDESC_FN`

```rust
pub const R_XTENSA_TLSDESC_FN: u32 = 50;
```

#### Constant `R_XTENSA_TLSDESC_ARG`

```rust
pub const R_XTENSA_TLSDESC_ARG: u32 = 51;
```

#### Constant `R_XTENSA_TLS_DTPOFF`

```rust
pub const R_XTENSA_TLS_DTPOFF: u32 = 52;
```

#### Constant `R_XTENSA_TLS_TPOFF`

```rust
pub const R_XTENSA_TLS_TPOFF: u32 = 53;
```

#### Constant `R_XTENSA_TLS_FUNC`

```rust
pub const R_XTENSA_TLS_FUNC: u32 = 54;
```

#### Constant `R_XTENSA_TLS_ARG`

```rust
pub const R_XTENSA_TLS_ARG: u32 = 55;
```

#### Constant `R_XTENSA_TLS_CALL`

```rust
pub const R_XTENSA_TLS_CALL: u32 = 56;
```

#### Constant `R_XTENSA_PDIFF8`

```rust
pub const R_XTENSA_PDIFF8: u32 = 57;
```

#### Constant `R_XTENSA_PDIFF16`

```rust
pub const R_XTENSA_PDIFF16: u32 = 58;
```

#### Constant `R_XTENSA_PDIFF32`

```rust
pub const R_XTENSA_PDIFF32: u32 = 59;
```

#### Constant `R_XTENSA_NDIFF8`

```rust
pub const R_XTENSA_NDIFF8: u32 = 60;
```

#### Constant `R_XTENSA_NDIFF16`

```rust
pub const R_XTENSA_NDIFF16: u32 = 61;
```

#### Constant `R_XTENSA_NDIFF32`

```rust
pub const R_XTENSA_NDIFF32: u32 = 62;
```

#### Constant `EF_E2K_IPD`

```rust
pub const EF_E2K_IPD: u32 = 3;
```

#### Constant `EF_E2K_X86APP`

```rust
pub const EF_E2K_X86APP: u32 = 4;
```

#### Constant `EF_E2K_4MB_PAGES`

```rust
pub const EF_E2K_4MB_PAGES: u32 = 8;
```

#### Constant `EF_E2K_INCOMPAT`

```rust
pub const EF_E2K_INCOMPAT: u32 = 16;
```

#### Constant `EF_E2K_PM`

```rust
pub const EF_E2K_PM: u32 = 32;
```

#### Constant `EF_E2K_PACK_SEGMENTS`

```rust
pub const EF_E2K_PACK_SEGMENTS: u32 = 64;
```

#### Constant `E_E2K_MACH_BASE`

-march=generic code.

Legacy. Shouldn't be created nowadays.

```rust
pub const E_E2K_MACH_BASE: u32 = 0;
```

#### Constant `E_E2K_MACH_EV1`

-march=elbrus-v1 code.

Legacy. Shouldn't be created nowadays.

```rust
pub const E_E2K_MACH_EV1: u32 = 1;
```

#### Constant `E_E2K_MACH_EV2`

-march=elbrus-v2 code.

```rust
pub const E_E2K_MACH_EV2: u32 = 2;
```

#### Constant `E_E2K_MACH_EV3`

-march=elbrus-v3 code.

```rust
pub const E_E2K_MACH_EV3: u32 = 3;
```

#### Constant `E_E2K_MACH_EV4`

-march=elbrus-v4 code.

```rust
pub const E_E2K_MACH_EV4: u32 = 4;
```

#### Constant `E_E2K_MACH_EV5`

-march=elbrus-v5 code.

```rust
pub const E_E2K_MACH_EV5: u32 = 5;
```

#### Constant `E_E2K_MACH_EV6`

-march=elbrus-v6 code.

```rust
pub const E_E2K_MACH_EV6: u32 = 6;
```

#### Constant `E_E2K_MACH_EV7`

-march=elbrus-v7 code.

```rust
pub const E_E2K_MACH_EV7: u32 = 7;
```

#### Constant `E_E2K_MACH_8C`

-mtune=elbrus-8c code.

```rust
pub const E_E2K_MACH_8C: u32 = 19;
```

#### Constant `E_E2K_MACH_1CPLUS`

-mtune=elbrus-1c+ code.

```rust
pub const E_E2K_MACH_1CPLUS: u32 = 20;
```

#### Constant `E_E2K_MACH_12C`

-mtune=elbrus-12c code.

```rust
pub const E_E2K_MACH_12C: u32 = 21;
```

#### Constant `E_E2K_MACH_16C`

-mtune=elbrus-16c code.

```rust
pub const E_E2K_MACH_16C: u32 = 22;
```

#### Constant `E_E2K_MACH_2C3`

-mtune=elbrus-2c3 code.

```rust
pub const E_E2K_MACH_2C3: u32 = 23;
```

#### Constant `E_E2K_MACH_48C`

-mtune=elbrus-48c code.

```rust
pub const E_E2K_MACH_48C: u32 = 24;
```

#### Constant `E_E2K_MACH_8V7`

-mtune=elbrus-8v7 code.

```rust
pub const E_E2K_MACH_8V7: u32 = 25;
```

#### Constant `R_E2K_32_ABS`

Direct 32 bit.

```rust
pub const R_E2K_32_ABS: u32 = 0;
```

#### Constant `R_E2K_32_PC`

PC relative 32 bit.

```rust
pub const R_E2K_32_PC: u32 = 2;
```

#### Constant `R_E2K_AP_GOT`

32-bit offset of AP GOT entry.

```rust
pub const R_E2K_AP_GOT: u32 = 3;
```

#### Constant `R_E2K_PL_GOT`

32-bit offset of PL GOT entry.

```rust
pub const R_E2K_PL_GOT: u32 = 4;
```

#### Constant `R_E2K_32_JMP_SLOT`

Create PLT entry.

```rust
pub const R_E2K_32_JMP_SLOT: u32 = 8;
```

#### Constant `R_E2K_32_COPY`

Copy relocation, 32-bit case.

```rust
pub const R_E2K_32_COPY: u32 = 9;
```

#### Constant `R_E2K_32_RELATIVE`

Adjust by program base, 32-bit case.

```rust
pub const R_E2K_32_RELATIVE: u32 = 10;
```

#### Constant `R_E2K_32_IRELATIVE`

Adjust indirectly by program base, 32-bit case.

```rust
pub const R_E2K_32_IRELATIVE: u32 = 11;
```

#### Constant `R_E2K_32_SIZE`

Size of symbol plus 32-bit addend.

```rust
pub const R_E2K_32_SIZE: u32 = 12;
```

#### Constant `R_E2K_32_DYNOPT`

Symbol value if resolved by the definition in the same
compilation unit or NULL otherwise, 32-bit case.

```rust
pub const R_E2K_32_DYNOPT: u32 = 13;
```

#### Constant `R_E2K_64_ABS`

Direct 64 bit.

```rust
pub const R_E2K_64_ABS: u32 = 50;
```

#### Constant `R_E2K_64_ABS_LIT`

Direct 64 bit for literal.

```rust
pub const R_E2K_64_ABS_LIT: u32 = 51;
```

#### Constant `R_E2K_64_PC_LIT`

PC relative 64 bit for literal.

```rust
pub const R_E2K_64_PC_LIT: u32 = 54;
```

#### Constant `R_E2K_64_JMP_SLOT`

Create PLT entry, 64-bit case.

```rust
pub const R_E2K_64_JMP_SLOT: u32 = 63;
```

#### Constant `R_E2K_64_COPY`

Copy relocation, 64-bit case.

```rust
pub const R_E2K_64_COPY: u32 = 64;
```

#### Constant `R_E2K_64_RELATIVE`

Adjust by program base, 64-bit case.

```rust
pub const R_E2K_64_RELATIVE: u32 = 65;
```

#### Constant `R_E2K_64_RELATIVE_LIT`

Adjust by program base for literal, 64-bit case.

```rust
pub const R_E2K_64_RELATIVE_LIT: u32 = 66;
```

#### Constant `R_E2K_64_IRELATIVE`

Adjust indirectly by program base, 64-bit case.

```rust
pub const R_E2K_64_IRELATIVE: u32 = 67;
```

#### Constant `R_E2K_64_SIZE`

Size of symbol plus 64-bit addend.

```rust
pub const R_E2K_64_SIZE: u32 = 68;
```

#### Constant `R_E2K_64_GOTOFF`

64-bit offset of the symbol from GOT.

```rust
pub const R_E2K_64_GOTOFF: u32 = 69;
```

#### Constant `R_E2K_TLS_GDMOD`

GOT entry for ID of module containing symbol.

```rust
pub const R_E2K_TLS_GDMOD: u32 = 70;
```

#### Constant `R_E2K_TLS_GDREL`

GOT entry for offset in module TLS block.

```rust
pub const R_E2K_TLS_GDREL: u32 = 71;
```

#### Constant `R_E2K_TLS_IE`

Static TLS block offset GOT entry.

```rust
pub const R_E2K_TLS_IE: u32 = 74;
```

#### Constant `R_E2K_32_TLS_LE`

Offset relative to static TLS block, 32-bit case.

```rust
pub const R_E2K_32_TLS_LE: u32 = 75;
```

#### Constant `R_E2K_64_TLS_LE`

Offset relative to static TLS block, 64-bit case.

```rust
pub const R_E2K_64_TLS_LE: u32 = 76;
```

#### Constant `R_E2K_TLS_32_DTPMOD`

ID of module containing symbol, 32-bit case.

```rust
pub const R_E2K_TLS_32_DTPMOD: u32 = 80;
```

#### Constant `R_E2K_TLS_32_DTPREL`

Offset in module TLS block, 32-bit case.

```rust
pub const R_E2K_TLS_32_DTPREL: u32 = 81;
```

#### Constant `R_E2K_TLS_64_DTPMOD`

ID of module containing symbol, 64-bit case.

```rust
pub const R_E2K_TLS_64_DTPMOD: u32 = 82;
```

#### Constant `R_E2K_TLS_64_DTPREL`

Offset in module TLS block, 64-bit case.

```rust
pub const R_E2K_TLS_64_DTPREL: u32 = 83;
```

#### Constant `R_E2K_TLS_32_TPREL`

Offset in static TLS block, 32-bit case.

```rust
pub const R_E2K_TLS_32_TPREL: u32 = 84;
```

#### Constant `R_E2K_TLS_64_TPREL`

Offset in static TLS block, 64-bit case.

```rust
pub const R_E2K_TLS_64_TPREL: u32 = 85;
```

#### Constant `R_E2K_AP`

Direct AP.

```rust
pub const R_E2K_AP: u32 = 100;
```

#### Constant `R_E2K_PL`

Direct PL.

```rust
pub const R_E2K_PL: u32 = 101;
```

#### Constant `R_E2K_GOT`

32-bit offset of the symbol's entry in GOT.

```rust
pub const R_E2K_GOT: u32 = 108;
```

#### Constant `R_E2K_GOTOFF`

32-bit offset of the symbol from GOT.

```rust
pub const R_E2K_GOTOFF: u32 = 109;
```

#### Constant `R_E2K_DISP`

PC relative 28 bit for DISP.

```rust
pub const R_E2K_DISP: u32 = 110;
```

#### Constant `R_E2K_PREF`

Prefetch insn line containing the label (symbol).

```rust
pub const R_E2K_PREF: u32 = 111;
```

#### Constant `R_E2K_NONE`

No reloc.

```rust
pub const R_E2K_NONE: u32 = 112;
```

#### Constant `R_E2K_GOTPLT`

32-bit offset of the symbol's entry in .got.plt.

```rust
pub const R_E2K_GOTPLT: u32 = 114;
```

#### Constant `R_E2K_ISLOCAL`

Is symbol resolved locally during the link.
The result is encoded in 5-bit ALS.src1.

```rust
pub const R_E2K_ISLOCAL: u32 = 115;
```

#### Constant `R_E2K_ISLOCAL32`

Is symbol resloved locally during the link.
The result is encoded in a long 32-bit LTS.

```rust
pub const R_E2K_ISLOCAL32: u32 = 118;
```

#### Constant `R_E2K_64_GOTOFF_LIT`

The symbol's offset from GOT encoded within a 64-bit literal.

```rust
pub const R_E2K_64_GOTOFF_LIT: u32 = 256;
```

#### Constant `R_E2K_64_DYNOPT`

Symbol value if resolved by the definition in the same
compilation unit or NULL otherwise, 64-bit case.

```rust
pub const R_E2K_64_DYNOPT: u32 = 257;
```

#### Constant `R_E2K_64_PC`

PC relative 64 bit in data.

```rust
pub const R_E2K_64_PC: u32 = 258;
```

#### Constant `DT_E2K_LAZY`

```rust
pub const DT_E2K_LAZY: u32 = _;
```

#### Constant `DT_E2K_LAZY_GOT`

```rust
pub const DT_E2K_LAZY_GOT: u32 = _;
```

#### Constant `DT_E2K_INIT_GOT`

```rust
pub const DT_E2K_INIT_GOT: u32 = _;
```

#### Constant `DT_E2K_EXPORT_PL`

```rust
pub const DT_E2K_EXPORT_PL: u32 = _;
```

#### Constant `DT_E2K_EXPORT_PLSZ`

```rust
pub const DT_E2K_EXPORT_PLSZ: u32 = _;
```

#### Constant `DT_E2K_REAL_PLTGOT`

```rust
pub const DT_E2K_REAL_PLTGOT: u32 = _;
```

#### Constant `DT_E2K_NO_SELFINIT`

```rust
pub const DT_E2K_NO_SELFINIT: u32 = _;
```

#### Constant `DT_E2K_NUM`

```rust
pub const DT_E2K_NUM: u32 = 0x1021;
```

#### Constant `Tag_File`

**Attributes:**

- `#[allow(non_upper_case_globals)]`

```rust
pub const Tag_File: u8 = 1;
```

#### Constant `Tag_Section`

**Attributes:**

- `#[allow(non_upper_case_globals)]`

```rust
pub const Tag_Section: u8 = 2;
```

#### Constant `Tag_Symbol`

**Attributes:**

- `#[allow(non_upper_case_globals)]`

```rust
pub const Tag_Symbol: u8 = 3;
```

## Module `macho`

**Attributes:**

- `#[<cfg>(feature = "macho")]`
- `#[allow(missing_docs)]`

Mach-O definitions.

These definitions are independent of read/write support, although we do implement
some traits useful for those.

This module is based heavily on header files from MacOSX11.1.sdk.

```rust
pub mod macho { /* ... */ }
```

### Types

#### Struct `DyldCacheHeader`

**Attributes:**

- `#[repr(C)]`

The dyld cache header.
Corresponds to struct dyld_cache_header from dyld_cache_format.h.
This header has grown over time. Only the fields up to and including dyld_base_address
are guaranteed to be present. For all other fields, check the header size before
accessing the field. The header size is stored in mapping_offset; the mappings start
right after the theader.

```rust
pub struct DyldCacheHeader<E: Endian> {
    pub magic: [u8; 16],
    pub mapping_offset: crate::endian::U32<E>,
    pub mapping_count: crate::endian::U32<E>,
    pub images_offset: crate::endian::U32<E>,
    pub images_count: crate::endian::U32<E>,
    pub dyld_base_address: crate::endian::U64<E>,
    pub local_symbols_offset: crate::endian::U64<E>,
    pub local_symbols_size: crate::endian::U64<E>,
    pub uuid: [u8; 16],
    pub subcaches_offset: crate::endian::U32<E>,
    pub subcaches_count: crate::endian::U32<E>,
    pub symbols_subcache_uuid: [u8; 16],
    pub images_across_all_subcaches_offset: crate::endian::U32<E>,
    pub images_across_all_subcaches_count: crate::endian::U32<E>,
    // Some fields omitted
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `magic` | `[u8; 16]` | e.g. "dyld_v0    i386" |
| `mapping_offset` | `crate::endian::U32<E>` | file offset to first dyld_cache_mapping_info |
| `mapping_count` | `crate::endian::U32<E>` | number of dyld_cache_mapping_info entries |
| `images_offset` | `crate::endian::U32<E>` | file offset to first dyld_cache_image_info |
| `images_count` | `crate::endian::U32<E>` | number of dyld_cache_image_info entries |
| `dyld_base_address` | `crate::endian::U64<E>` | base address of dyld when cache was built |
| `local_symbols_offset` | `crate::endian::U64<E>` | file offset of where local symbols are stored |
| `local_symbols_size` | `crate::endian::U64<E>` | size of local symbols information |
| `uuid` | `[u8; 16]` | unique value for each shared cache file |
| `subcaches_offset` | `crate::endian::U32<E>` | file offset to first dyld_subcache_info |
| `subcaches_count` | `crate::endian::U32<E>` | number of dyld_subcache_info entries |
| `symbols_subcache_uuid` | `[u8; 16]` | the UUID of the .symbols subcache |
| `images_across_all_subcaches_offset` | `crate::endian::U32<E>` | file offset to first dyld_cache_image_info<br>Use this  instead of images_offset if mapping_offset is at least 0x1c4. |
| `images_across_all_subcaches_count` | `crate::endian::U32<E>` | number of dyld_cache_image_info entries<br>Use this  instead of images_count if mapping_offset is at least 0x1c4. |
| *private fields* | ... | *Some fields have been omitted* |

##### Implementations

###### Methods

- ```rust
  pub fn parse<''data, R: ReadRef<''data>>(data: R) -> Result<&''data Self> { /* ... */ }
  ```
  Read the dyld cache header.

- ```rust
  pub fn parse_magic(self: &Self) -> Result<(Architecture, E)> { /* ... */ }
  ```
  Returns (arch, endian) based on the magic string.

- ```rust
  pub fn mappings<''data, R: ReadRef<''data>>(self: &Self, endian: E, data: R) -> Result<&''data [macho::DyldCacheMappingInfo<E>]> { /* ... */ }
  ```
  Return the mapping information table.

- ```rust
  pub fn subcaches<''data, R: ReadRef<''data>>(self: &Self, endian: E, data: R) -> Result<Option<DyldSubCacheSlice<''data, E>>> { /* ... */ }
  ```
  Return the information about subcaches, if present.

- ```rust
  pub fn symbols_subcache_uuid(self: &Self, endian: E) -> Option<[u8; 16]> { /* ... */ }
  ```
  Return the UUID for the .symbols subcache, if present.

- ```rust
  pub fn images<''data, R: ReadRef<''data>>(self: &Self, endian: E, data: R) -> Result<&''data [macho::DyldCacheImageInfo<E>]> { /* ... */ }
  ```
  Return the image information table.

###### Trait Implementations

- **Freeze**
- **UnwindSafe**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> DyldCacheHeader<E> { /* ... */ }
    ```

- **RefUnwindSafe**
- **Sync**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Copy**
- **Unpin**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Send**
- **Pod**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

#### Struct `DyldCacheMappingInfo`

**Attributes:**

- `#[repr(C)]`

Corresponds to struct dyld_cache_mapping_info from dyld_cache_format.h.

```rust
pub struct DyldCacheMappingInfo<E: Endian> {
    pub address: crate::endian::U64<E>,
    pub size: crate::endian::U64<E>,
    pub file_offset: crate::endian::U64<E>,
    pub max_prot: crate::endian::U32<E>,
    pub init_prot: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `address` | `crate::endian::U64<E>` |  |
| `size` | `crate::endian::U64<E>` |  |
| `file_offset` | `crate::endian::U64<E>` |  |
| `max_prot` | `crate::endian::U32<E>` |  |
| `init_prot` | `crate::endian::U32<E>` |  |

##### Implementations

###### Trait Implementations

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Sync**
- **Freeze**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> DyldCacheMappingInfo<E> { /* ... */ }
    ```

- **Copy**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **UnwindSafe**
- **Pod**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Unpin**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Send**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **RefUnwindSafe**
#### Struct `DyldCacheImageInfo`

**Attributes:**

- `#[repr(C)]`

Corresponds to struct dyld_cache_image_info from dyld_cache_format.h.

```rust
pub struct DyldCacheImageInfo<E: Endian> {
    pub address: crate::endian::U64<E>,
    pub mod_time: crate::endian::U64<E>,
    pub inode: crate::endian::U64<E>,
    pub path_file_offset: crate::endian::U32<E>,
    pub pad: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `address` | `crate::endian::U64<E>` |  |
| `mod_time` | `crate::endian::U64<E>` |  |
| `inode` | `crate::endian::U64<E>` |  |
| `path_file_offset` | `crate::endian::U32<E>` |  |
| `pad` | `crate::endian::U32<E>` |  |

##### Implementations

###### Methods

- ```rust
  pub fn path<''data, R: ReadRef<''data>>(self: &Self, endian: E, data: R) -> Result<&''data [u8]> { /* ... */ }
  ```
  The file system path of this image.

- ```rust
  pub fn file_offset(self: &Self, endian: E, mappings: &[macho::DyldCacheMappingInfo<E>]) -> Result<u64> { /* ... */ }
  ```
  Find the file offset of the image by looking up its address in the mappings.

###### Trait Implementations

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Sync**
- **RefUnwindSafe**
- **Copy**
- **Unpin**
- **Pod**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Send**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Clone**
  - ```rust
    fn clone(self: &Self) -> DyldCacheImageInfo<E> { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Freeze**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **UnwindSafe**
#### Struct `DyldSubCacheEntryV1`

**Attributes:**

- `#[repr(C)]`

Added in dyld-940, which shipped with macOS 12 / iOS 15.
Originally called `dyld_subcache_entry`, renamed to `dyld_subcache_entry_v1`
in dyld-1042.1.

```rust
pub struct DyldSubCacheEntryV1<E: Endian> {
    pub uuid: [u8; 16],
    pub cache_vm_offset: crate::endian::U64<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `uuid` | `[u8; 16]` | The UUID of this subcache. |
| `cache_vm_offset` | `crate::endian::U64<E>` | The offset of this subcache from the main cache base address. |

##### Implementations

###### Trait Implementations

- **Send**
- **Freeze**
- **Pod**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> DyldSubCacheEntryV1<E> { /* ... */ }
    ```

- **Sync**
- **UnwindSafe**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Unpin**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Copy**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **RefUnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

#### Struct `DyldSubCacheEntryV2`

**Attributes:**

- `#[repr(C)]`

Added in dyld-1042.1, which shipped with macOS 13 / iOS 16.
Called `dyld_subcache_entry` as of dyld-1042.1.

```rust
pub struct DyldSubCacheEntryV2<E: Endian> {
    pub uuid: [u8; 16],
    pub cache_vm_offset: crate::endian::U64<E>,
    pub file_suffix: [u8; 32],
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `uuid` | `[u8; 16]` | The UUID of this subcache. |
| `cache_vm_offset` | `crate::endian::U64<E>` | The offset of this subcache from the main cache base address. |
| `file_suffix` | `[u8; 32]` | The file name suffix of the subCache file, e.g. ".25.data" or ".03.development". |

##### Implementations

###### Trait Implementations

- **Pod**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **UnwindSafe**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Send**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Sync**
- **Freeze**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> DyldSubCacheEntryV2<E> { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Copy**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **RefUnwindSafe**
- **Unpin**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

#### Struct `FatHeader`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct FatHeader {
    pub magic: crate::endian::U32<crate::endian::BigEndian>,
    pub nfat_arch: crate::endian::U32<crate::endian::BigEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `magic` | `crate::endian::U32<crate::endian::BigEndian>` | FAT_MAGIC or FAT_MAGIC_64 |
| `nfat_arch` | `crate::endian::U32<crate::endian::BigEndian>` | number of structs that follow |

##### Implementations

###### Trait Implementations

- **Pod**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Send**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Unpin**
- **UnwindSafe**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **RefUnwindSafe**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Sync**
- **Copy**
- **Freeze**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> FatHeader { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

#### Struct `FatArch32`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct FatArch32 {
    pub cputype: crate::endian::U32<crate::endian::BigEndian>,
    pub cpusubtype: crate::endian::U32<crate::endian::BigEndian>,
    pub offset: crate::endian::U32<crate::endian::BigEndian>,
    pub size: crate::endian::U32<crate::endian::BigEndian>,
    pub align: crate::endian::U32<crate::endian::BigEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cputype` | `crate::endian::U32<crate::endian::BigEndian>` | cpu specifier (int) |
| `cpusubtype` | `crate::endian::U32<crate::endian::BigEndian>` | machine specifier (int) |
| `offset` | `crate::endian::U32<crate::endian::BigEndian>` | file offset to this object file |
| `size` | `crate::endian::U32<crate::endian::BigEndian>` | size of this object file |
| `align` | `crate::endian::U32<crate::endian::BigEndian>` | alignment as a power of 2 |

##### Implementations

###### Trait Implementations

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Pod**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Freeze**
- **Send**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **UnwindSafe**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **RefUnwindSafe**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Sync**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Unpin**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> FatArch32 { /* ... */ }
    ```

- **FatArch**
  - ```rust
    fn cputype(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn cpusubtype(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn offset(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn size(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn align(self: &Self) -> u32 { /* ... */ }
    ```

- **Copy**
#### Struct `FatArch64`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct FatArch64 {
    pub cputype: crate::endian::U32<crate::endian::BigEndian>,
    pub cpusubtype: crate::endian::U32<crate::endian::BigEndian>,
    pub offset: crate::endian::U64<crate::endian::BigEndian>,
    pub size: crate::endian::U64<crate::endian::BigEndian>,
    pub align: crate::endian::U32<crate::endian::BigEndian>,
    pub reserved: crate::endian::U32<crate::endian::BigEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cputype` | `crate::endian::U32<crate::endian::BigEndian>` | cpu specifier (int) |
| `cpusubtype` | `crate::endian::U32<crate::endian::BigEndian>` | machine specifier (int) |
| `offset` | `crate::endian::U64<crate::endian::BigEndian>` | file offset to this object file |
| `size` | `crate::endian::U64<crate::endian::BigEndian>` | size of this object file |
| `align` | `crate::endian::U32<crate::endian::BigEndian>` | alignment as a power of 2 |
| `reserved` | `crate::endian::U32<crate::endian::BigEndian>` | reserved |

##### Implementations

###### Trait Implementations

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **FatArch**
  - ```rust
    fn cputype(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn cpusubtype(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn offset(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn size(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn align(self: &Self) -> u32 { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Freeze**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Sync**
- **UnwindSafe**
- **Send**
- **RefUnwindSafe**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Copy**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Unpin**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> FatArch64 { /* ... */ }
    ```

- **Pod**
#### Struct `MachHeader32`

**Attributes:**

- `#[repr(C)]`

The 32-bit mach header.

Appears at the very beginning of the object file for 32-bit architectures.

```rust
pub struct MachHeader32<E: Endian> {
    pub magic: crate::endian::U32<crate::endian::BigEndian>,
    pub cputype: crate::endian::U32<E>,
    pub cpusubtype: crate::endian::U32<E>,
    pub filetype: crate::endian::U32<E>,
    pub ncmds: crate::endian::U32<E>,
    pub sizeofcmds: crate::endian::U32<E>,
    pub flags: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `magic` | `crate::endian::U32<crate::endian::BigEndian>` | mach magic number identifier |
| `cputype` | `crate::endian::U32<E>` | cpu specifier |
| `cpusubtype` | `crate::endian::U32<E>` | machine specifier |
| `filetype` | `crate::endian::U32<E>` | type of file |
| `ncmds` | `crate::endian::U32<E>` | number of load commands |
| `sizeofcmds` | `crate::endian::U32<E>` | the size of all the load commands |
| `flags` | `crate::endian::U32<E>` | flags |

##### Implementations

###### Trait Implementations

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> MachHeader32<E> { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **RefUnwindSafe**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Sync**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Pod**
- **UnwindSafe**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **MachHeader**
  - ```rust
    fn is_type_64(self: &Self) -> bool { /* ... */ }
    ```

  - ```rust
    fn is_big_endian(self: &Self) -> bool { /* ... */ }
    ```

  - ```rust
    fn is_little_endian(self: &Self) -> bool { /* ... */ }
    ```

  - ```rust
    fn magic(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn cputype(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn cpusubtype(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn filetype(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn ncmds(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn sizeofcmds(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn flags(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Copy**
- **Freeze**
- **Send**
- **Unpin**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

#### Struct `MachHeader64`

**Attributes:**

- `#[repr(C)]`

The 64-bit mach header.

Appears at the very beginning of object files for 64-bit architectures.

```rust
pub struct MachHeader64<E: Endian> {
    pub magic: crate::endian::U32<crate::endian::BigEndian>,
    pub cputype: crate::endian::U32<E>,
    pub cpusubtype: crate::endian::U32<E>,
    pub filetype: crate::endian::U32<E>,
    pub ncmds: crate::endian::U32<E>,
    pub sizeofcmds: crate::endian::U32<E>,
    pub flags: crate::endian::U32<E>,
    pub reserved: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `magic` | `crate::endian::U32<crate::endian::BigEndian>` | mach magic number identifier |
| `cputype` | `crate::endian::U32<E>` | cpu specifier |
| `cpusubtype` | `crate::endian::U32<E>` | machine specifier |
| `filetype` | `crate::endian::U32<E>` | type of file |
| `ncmds` | `crate::endian::U32<E>` | number of load commands |
| `sizeofcmds` | `crate::endian::U32<E>` | the size of all the load commands |
| `flags` | `crate::endian::U32<E>` | flags |
| `reserved` | `crate::endian::U32<E>` | reserved |

##### Implementations

###### Trait Implementations

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Sync**
- **Unpin**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **UnwindSafe**
- **RefUnwindSafe**
- **MachHeader**
  - ```rust
    fn is_type_64(self: &Self) -> bool { /* ... */ }
    ```

  - ```rust
    fn is_big_endian(self: &Self) -> bool { /* ... */ }
    ```

  - ```rust
    fn is_little_endian(self: &Self) -> bool { /* ... */ }
    ```

  - ```rust
    fn magic(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn cputype(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn cpusubtype(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn filetype(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn ncmds(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn sizeofcmds(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn flags(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Send**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Copy**
- **Freeze**
- **Pod**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> MachHeader64<E> { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

#### Struct `LoadCommand`

**Attributes:**

- `#[repr(C)]`

Common fields at the start of every load command.

The load commands directly follow the mach_header.  The total size of all
of the commands is given by the sizeofcmds field in the mach_header.  All
load commands must have as their first two fields `cmd` and `cmdsize`.  The `cmd`
field is filled in with a constant for that command type.  Each command type
has a structure specifically for it.  The `cmdsize` field is the size in bytes
of the particular load command structure plus anything that follows it that
is a part of the load command (i.e. section structures, strings, etc.).  To
advance to the next load command the `cmdsize` can be added to the offset or
pointer of the current load command.  The `cmdsize` for 32-bit architectures
MUST be a multiple of 4 bytes and for 64-bit architectures MUST be a multiple
of 8 bytes (these are forever the maximum alignment of any load commands).
The padded bytes must be zero.  All tables in the object file must also
follow these rules so the file can be memory mapped.  Otherwise the pointers
to these tables will not work well or at all on some machines.  With all
padding zeroed like objects will compare byte for byte.

```rust
pub struct LoadCommand<E: Endian> {
    pub cmd: crate::endian::U32<E>,
    pub cmdsize: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cmd` | `crate::endian::U32<E>` | Type of load command.<br><br>One of the `LC_*` constants. |
| `cmdsize` | `crate::endian::U32<E>` | Total size of command in bytes. |

##### Implementations

###### Trait Implementations

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Unpin**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Pod**
- **UnwindSafe**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **RefUnwindSafe**
- **Send**
- **Copy**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Sync**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> LoadCommand<E> { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Freeze**
#### Struct `LcStr`

**Attributes:**

- `#[repr(C)]`

A variable length string in a load command.

The strings are stored just after the load command structure and
the offset is from the start of the load command structure.  The size
of the string is reflected in the `cmdsize` field of the load command.
Once again any padded bytes to bring the `cmdsize` field to a multiple
of 4 bytes must be zero.

```rust
pub struct LcStr<E: Endian> {
    pub offset: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `offset` | `crate::endian::U32<E>` | offset to the string |

##### Implementations

###### Trait Implementations

- **Clone**
  - ```rust
    fn clone(self: &Self) -> LcStr<E> { /* ... */ }
    ```

- **Pod**
- **UnwindSafe**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Copy**
- **Unpin**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Send**
- **Freeze**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Sync**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **RefUnwindSafe**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

#### Struct `SegmentCommand32`

**Attributes:**

- `#[repr(C)]`

32-bit segment load command.

The segment load command indicates that a part of this file is to be
mapped into the task's address space.  The size of this segment in memory,
vmsize, maybe equal to or larger than the amount to map from this file,
filesize.  The file is mapped starting at fileoff to the beginning of
the segment in memory, vmaddr.  The rest of the memory of the segment,
if any, is allocated zero fill on demand.  The segment's maximum virtual
memory protection and initial virtual memory protection are specified
by the maxprot and initprot fields.  If the segment has sections then the
`Section32` structures directly follow the segment command and their size is
reflected in `cmdsize`.

```rust
pub struct SegmentCommand32<E: Endian> {
    pub cmd: crate::endian::U32<E>,
    pub cmdsize: crate::endian::U32<E>,
    pub segname: [u8; 16],
    pub vmaddr: crate::endian::U32<E>,
    pub vmsize: crate::endian::U32<E>,
    pub fileoff: crate::endian::U32<E>,
    pub filesize: crate::endian::U32<E>,
    pub maxprot: crate::endian::U32<E>,
    pub initprot: crate::endian::U32<E>,
    pub nsects: crate::endian::U32<E>,
    pub flags: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cmd` | `crate::endian::U32<E>` | LC_SEGMENT |
| `cmdsize` | `crate::endian::U32<E>` | includes sizeof section structs |
| `segname` | `[u8; 16]` | segment name |
| `vmaddr` | `crate::endian::U32<E>` | memory address of this segment |
| `vmsize` | `crate::endian::U32<E>` | memory size of this segment |
| `fileoff` | `crate::endian::U32<E>` | file offset of this segment |
| `filesize` | `crate::endian::U32<E>` | amount to map from the file |
| `maxprot` | `crate::endian::U32<E>` | maximum VM protection |
| `initprot` | `crate::endian::U32<E>` | initial VM protection |
| `nsects` | `crate::endian::U32<E>` | number of sections in segment |
| `flags` | `crate::endian::U32<E>` | flags |

##### Implementations

###### Trait Implementations

- **Segment**
  - ```rust
    fn from_command(command: LoadCommandData<''_, <Self as >::Endian>) -> Result<Option<(&Self, &[u8])>> { /* ... */ }
    ```

  - ```rust
    fn cmd(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn cmdsize(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn segname(self: &Self) -> &[u8; 16] { /* ... */ }
    ```

  - ```rust
    fn vmaddr(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn vmsize(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn fileoff(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn filesize(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn maxprot(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn initprot(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn nsects(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn flags(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

- **Unpin**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Freeze**
- **Send**
- **RefUnwindSafe**
- **UnwindSafe**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> SegmentCommand32<E> { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Pod**
- **Copy**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Sync**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

#### Struct `SegmentCommand64`

**Attributes:**

- `#[repr(C)]`

64-bit segment load command.

The 64-bit segment load command indicates that a part of this file is to be
mapped into a 64-bit task's address space.  If the 64-bit segment has
sections then `Section64` structures directly follow the 64-bit segment
command and their size is reflected in `cmdsize`.

```rust
pub struct SegmentCommand64<E: Endian> {
    pub cmd: crate::endian::U32<E>,
    pub cmdsize: crate::endian::U32<E>,
    pub segname: [u8; 16],
    pub vmaddr: crate::endian::U64<E>,
    pub vmsize: crate::endian::U64<E>,
    pub fileoff: crate::endian::U64<E>,
    pub filesize: crate::endian::U64<E>,
    pub maxprot: crate::endian::U32<E>,
    pub initprot: crate::endian::U32<E>,
    pub nsects: crate::endian::U32<E>,
    pub flags: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cmd` | `crate::endian::U32<E>` | LC_SEGMENT_64 |
| `cmdsize` | `crate::endian::U32<E>` | includes sizeof section_64 structs |
| `segname` | `[u8; 16]` | segment name |
| `vmaddr` | `crate::endian::U64<E>` | memory address of this segment |
| `vmsize` | `crate::endian::U64<E>` | memory size of this segment |
| `fileoff` | `crate::endian::U64<E>` | file offset of this segment |
| `filesize` | `crate::endian::U64<E>` | amount to map from the file |
| `maxprot` | `crate::endian::U32<E>` | maximum VM protection |
| `initprot` | `crate::endian::U32<E>` | initial VM protection |
| `nsects` | `crate::endian::U32<E>` | number of sections in segment |
| `flags` | `crate::endian::U32<E>` | flags |

##### Implementations

###### Trait Implementations

- **Pod**
- **Segment**
  - ```rust
    fn from_command(command: LoadCommandData<''_, <Self as >::Endian>) -> Result<Option<(&Self, &[u8])>> { /* ... */ }
    ```

  - ```rust
    fn cmd(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn cmdsize(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn segname(self: &Self) -> &[u8; 16] { /* ... */ }
    ```

  - ```rust
    fn vmaddr(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn vmsize(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn fileoff(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn filesize(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn maxprot(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn initprot(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn nsects(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn flags(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Unpin**
- **Freeze**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **RefUnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Sync**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Copy**
- **UnwindSafe**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> SegmentCommand64<E> { /* ... */ }
    ```

- **Send**
#### Struct `Section32`

**Attributes:**

- `#[repr(C)]`

32-bit section.

```rust
pub struct Section32<E: Endian> {
    pub sectname: [u8; 16],
    pub segname: [u8; 16],
    pub addr: crate::endian::U32<E>,
    pub size: crate::endian::U32<E>,
    pub offset: crate::endian::U32<E>,
    pub align: crate::endian::U32<E>,
    pub reloff: crate::endian::U32<E>,
    pub nreloc: crate::endian::U32<E>,
    pub flags: crate::endian::U32<E>,
    pub reserved1: crate::endian::U32<E>,
    pub reserved2: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `sectname` | `[u8; 16]` | name of this section |
| `segname` | `[u8; 16]` | segment this section goes in |
| `addr` | `crate::endian::U32<E>` | memory address of this section |
| `size` | `crate::endian::U32<E>` | size in bytes of this section |
| `offset` | `crate::endian::U32<E>` | file offset of this section |
| `align` | `crate::endian::U32<E>` | section alignment (power of 2) |
| `reloff` | `crate::endian::U32<E>` | file offset of relocation entries |
| `nreloc` | `crate::endian::U32<E>` | number of relocation entries |
| `flags` | `crate::endian::U32<E>` | flags (section type and attributes) |
| `reserved1` | `crate::endian::U32<E>` | reserved (for offset or index) |
| `reserved2` | `crate::endian::U32<E>` | reserved (for count or sizeof) |

##### Implementations

###### Trait Implementations

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Section**
  - ```rust
    fn sectname(self: &Self) -> &[u8; 16] { /* ... */ }
    ```

  - ```rust
    fn segname(self: &Self) -> &[u8; 16] { /* ... */ }
    ```

  - ```rust
    fn addr(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn size(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn offset(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn align(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn reloff(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn nreloc(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn flags(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Copy**
- **Pod**
- **UnwindSafe**
- **Sync**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Freeze**
- **Send**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Unpin**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **RefUnwindSafe**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> Section32<E> { /* ... */ }
    ```

#### Struct `Section64`

**Attributes:**

- `#[repr(C)]`

64-bit section.

```rust
pub struct Section64<E: Endian> {
    pub sectname: [u8; 16],
    pub segname: [u8; 16],
    pub addr: crate::endian::U64<E>,
    pub size: crate::endian::U64<E>,
    pub offset: crate::endian::U32<E>,
    pub align: crate::endian::U32<E>,
    pub reloff: crate::endian::U32<E>,
    pub nreloc: crate::endian::U32<E>,
    pub flags: crate::endian::U32<E>,
    pub reserved1: crate::endian::U32<E>,
    pub reserved2: crate::endian::U32<E>,
    pub reserved3: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `sectname` | `[u8; 16]` | name of this section |
| `segname` | `[u8; 16]` | segment this section goes in |
| `addr` | `crate::endian::U64<E>` | memory address of this section |
| `size` | `crate::endian::U64<E>` | size in bytes of this section |
| `offset` | `crate::endian::U32<E>` | file offset of this section |
| `align` | `crate::endian::U32<E>` | section alignment (power of 2) |
| `reloff` | `crate::endian::U32<E>` | file offset of relocation entries |
| `nreloc` | `crate::endian::U32<E>` | number of relocation entries |
| `flags` | `crate::endian::U32<E>` | flags (section type and attributes) |
| `reserved1` | `crate::endian::U32<E>` | reserved (for offset or index) |
| `reserved2` | `crate::endian::U32<E>` | reserved (for count or sizeof) |
| `reserved3` | `crate::endian::U32<E>` | reserved |

##### Implementations

###### Trait Implementations

- **Send**
- **Unpin**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **UnwindSafe**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Copy**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Pod**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Sync**
- **RefUnwindSafe**
- **Section**
  - ```rust
    fn sectname(self: &Self) -> &[u8; 16] { /* ... */ }
    ```

  - ```rust
    fn segname(self: &Self) -> &[u8; 16] { /* ... */ }
    ```

  - ```rust
    fn addr(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn size(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn offset(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn align(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn reloff(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn nreloc(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn flags(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

- **Freeze**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> Section64<E> { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

#### Struct `Fvmlib`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct Fvmlib<E: Endian> {
    pub name: LcStr<E>,
    pub minor_version: crate::endian::U32<E>,
    pub header_addr: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `name` | `LcStr<E>` | library's target pathname |
| `minor_version` | `crate::endian::U32<E>` | library's minor version number |
| `header_addr` | `crate::endian::U32<E>` | library's header address |

##### Implementations

###### Trait Implementations

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Send**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> Fvmlib<E> { /* ... */ }
    ```

- **RefUnwindSafe**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Sync**
- **Freeze**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Pod**
- **Unpin**
- **UnwindSafe**
- **Copy**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

#### Struct `FvmlibCommand`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct FvmlibCommand<E: Endian> {
    pub cmd: crate::endian::U32<E>,
    pub cmdsize: crate::endian::U32<E>,
    pub fvmlib: Fvmlib<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cmd` | `crate::endian::U32<E>` | LC_IDFVMLIB or LC_LOADFVMLIB |
| `cmdsize` | `crate::endian::U32<E>` | includes pathname string |
| `fvmlib` | `Fvmlib<E>` | the library identification |

##### Implementations

###### Trait Implementations

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Freeze**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Pod**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **RefUnwindSafe**
- **UnwindSafe**
- **Copy**
- **Sync**
- **Send**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> FvmlibCommand<E> { /* ... */ }
    ```

- **Unpin**
#### Struct `Dylib`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct Dylib<E: Endian> {
    pub name: LcStr<E>,
    pub timestamp: crate::endian::U32<E>,
    pub current_version: crate::endian::U32<E>,
    pub compatibility_version: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `name` | `LcStr<E>` | library's path name |
| `timestamp` | `crate::endian::U32<E>` | library's build time stamp |
| `current_version` | `crate::endian::U32<E>` | library's current version number |
| `compatibility_version` | `crate::endian::U32<E>` | library's compatibility vers number |

##### Implementations

###### Trait Implementations

- **Sync**
- **Copy**
- **Send**
- **RefUnwindSafe**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Pod**
- **Unpin**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **UnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Clone**
  - ```rust
    fn clone(self: &Self) -> Dylib<E> { /* ... */ }
    ```

- **Freeze**
#### Struct `DylibCommand`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct DylibCommand<E: Endian> {
    pub cmd: crate::endian::U32<E>,
    pub cmdsize: crate::endian::U32<E>,
    pub dylib: Dylib<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cmd` | `crate::endian::U32<E>` | LC_ID_DYLIB, LC_LOAD_{,WEAK_}DYLIB, LC_REEXPORT_DYLIB |
| `cmdsize` | `crate::endian::U32<E>` | includes pathname string |
| `dylib` | `Dylib<E>` | the library identification |

##### Implementations

###### Trait Implementations

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Send**
- **Sync**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Freeze**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Copy**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> DylibCommand<E> { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Unpin**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **RefUnwindSafe**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Pod**
- **UnwindSafe**
#### Struct `SubFrameworkCommand`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct SubFrameworkCommand<E: Endian> {
    pub cmd: crate::endian::U32<E>,
    pub cmdsize: crate::endian::U32<E>,
    pub umbrella: LcStr<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cmd` | `crate::endian::U32<E>` | LC_SUB_FRAMEWORK |
| `cmdsize` | `crate::endian::U32<E>` | includes umbrella string |
| `umbrella` | `LcStr<E>` | the umbrella framework name |

##### Implementations

###### Trait Implementations

- **Pod**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **RefUnwindSafe**
- **Freeze**
- **Sync**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Copy**
- **Send**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> SubFrameworkCommand<E> { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Unpin**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **UnwindSafe**
#### Struct `SubClientCommand`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct SubClientCommand<E: Endian> {
    pub cmd: crate::endian::U32<E>,
    pub cmdsize: crate::endian::U32<E>,
    pub client: LcStr<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cmd` | `crate::endian::U32<E>` | LC_SUB_CLIENT |
| `cmdsize` | `crate::endian::U32<E>` | includes client string |
| `client` | `LcStr<E>` | the client name |

##### Implementations

###### Trait Implementations

- **Freeze**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **UnwindSafe**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Sync**
- **RefUnwindSafe**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Pod**
- **Send**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Unpin**
- **Copy**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> SubClientCommand<E> { /* ... */ }
    ```

#### Struct `SubUmbrellaCommand`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct SubUmbrellaCommand<E: Endian> {
    pub cmd: crate::endian::U32<E>,
    pub cmdsize: crate::endian::U32<E>,
    pub sub_umbrella: LcStr<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cmd` | `crate::endian::U32<E>` | LC_SUB_UMBRELLA |
| `cmdsize` | `crate::endian::U32<E>` | includes sub_umbrella string |
| `sub_umbrella` | `LcStr<E>` | the sub_umbrella framework name |

##### Implementations

###### Trait Implementations

- **Send**
- **Freeze**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Sync**
- **RefUnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Unpin**
- **Pod**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Copy**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> SubUmbrellaCommand<E> { /* ... */ }
    ```

- **UnwindSafe**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

#### Struct `SubLibraryCommand`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct SubLibraryCommand<E: Endian> {
    pub cmd: crate::endian::U32<E>,
    pub cmdsize: crate::endian::U32<E>,
    pub sub_library: LcStr<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cmd` | `crate::endian::U32<E>` | LC_SUB_LIBRARY |
| `cmdsize` | `crate::endian::U32<E>` | includes sub_library string |
| `sub_library` | `LcStr<E>` | the sub_library name |

##### Implementations

###### Trait Implementations

- **Sync**
- **Send**
- **Unpin**
- **UnwindSafe**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> SubLibraryCommand<E> { /* ... */ }
    ```

- **RefUnwindSafe**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Copy**
- **Freeze**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Pod**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

#### Struct `PreboundDylibCommand`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct PreboundDylibCommand<E: Endian> {
    pub cmd: crate::endian::U32<E>,
    pub cmdsize: crate::endian::U32<E>,
    pub name: LcStr<E>,
    pub nmodules: crate::endian::U32<E>,
    pub linked_modules: LcStr<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cmd` | `crate::endian::U32<E>` | LC_PREBOUND_DYLIB |
| `cmdsize` | `crate::endian::U32<E>` | includes strings |
| `name` | `LcStr<E>` | library's path name |
| `nmodules` | `crate::endian::U32<E>` | number of modules in library |
| `linked_modules` | `LcStr<E>` | bit vector of linked modules |

##### Implementations

###### Trait Implementations

- **Unpin**
- **UnwindSafe**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Sync**
- **Freeze**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Copy**
- **Pod**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **RefUnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Send**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> PreboundDylibCommand<E> { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

#### Struct `DylinkerCommand`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct DylinkerCommand<E: Endian> {
    pub cmd: crate::endian::U32<E>,
    pub cmdsize: crate::endian::U32<E>,
    pub name: LcStr<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cmd` | `crate::endian::U32<E>` | LC_ID_DYLINKER, LC_LOAD_DYLINKER or LC_DYLD_ENVIRONMENT |
| `cmdsize` | `crate::endian::U32<E>` | includes pathname string |
| `name` | `LcStr<E>` | dynamic linker's path name |

##### Implementations

###### Trait Implementations

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Pod**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **RefUnwindSafe**
- **UnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Send**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> DylinkerCommand<E> { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Freeze**
- **Sync**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Copy**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Unpin**
#### Struct `ThreadCommand`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ThreadCommand<E: Endian> {
    pub cmd: crate::endian::U32<E>,
    pub cmdsize: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cmd` | `crate::endian::U32<E>` | LC_THREAD or  LC_UNIXTHREAD |
| `cmdsize` | `crate::endian::U32<E>` | total size of this command |

##### Implementations

###### Trait Implementations

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Send**
- **Unpin**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Freeze**
- **Sync**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Pod**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **RefUnwindSafe**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ThreadCommand<E> { /* ... */ }
    ```

- **UnwindSafe**
- **Copy**
#### Struct `RoutinesCommand32`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct RoutinesCommand32<E: Endian> {
    pub cmd: crate::endian::U32<E>,
    pub cmdsize: crate::endian::U32<E>,
    pub init_address: crate::endian::U32<E>,
    pub init_module: crate::endian::U32<E>,
    pub reserved1: crate::endian::U32<E>,
    pub reserved2: crate::endian::U32<E>,
    pub reserved3: crate::endian::U32<E>,
    pub reserved4: crate::endian::U32<E>,
    pub reserved5: crate::endian::U32<E>,
    pub reserved6: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cmd` | `crate::endian::U32<E>` | LC_ROUTINES |
| `cmdsize` | `crate::endian::U32<E>` | total size of this command |
| `init_address` | `crate::endian::U32<E>` | address of initialization routine |
| `init_module` | `crate::endian::U32<E>` | index into the module table that the init routine is defined in |
| `reserved1` | `crate::endian::U32<E>` |  |
| `reserved2` | `crate::endian::U32<E>` |  |
| `reserved3` | `crate::endian::U32<E>` |  |
| `reserved4` | `crate::endian::U32<E>` |  |
| `reserved5` | `crate::endian::U32<E>` |  |
| `reserved6` | `crate::endian::U32<E>` |  |

##### Implementations

###### Trait Implementations

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> RoutinesCommand32<E> { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Freeze**
- **Unpin**
- **RefUnwindSafe**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Send**
- **Sync**
- **Copy**
- **Pod**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **UnwindSafe**
#### Struct `RoutinesCommand64`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct RoutinesCommand64<E: Endian> {
    pub cmd: crate::endian::U32<E>,
    pub cmdsize: crate::endian::U32<E>,
    pub init_address: crate::endian::U64<E>,
    pub init_module: crate::endian::U64<E>,
    pub reserved1: crate::endian::U64<E>,
    pub reserved2: crate::endian::U64<E>,
    pub reserved3: crate::endian::U64<E>,
    pub reserved4: crate::endian::U64<E>,
    pub reserved5: crate::endian::U64<E>,
    pub reserved6: crate::endian::U64<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cmd` | `crate::endian::U32<E>` | LC_ROUTINES_64 |
| `cmdsize` | `crate::endian::U32<E>` | total size of this command |
| `init_address` | `crate::endian::U64<E>` | address of initialization routine |
| `init_module` | `crate::endian::U64<E>` | index into the module table that the init routine is defined in |
| `reserved1` | `crate::endian::U64<E>` |  |
| `reserved2` | `crate::endian::U64<E>` |  |
| `reserved3` | `crate::endian::U64<E>` |  |
| `reserved4` | `crate::endian::U64<E>` |  |
| `reserved5` | `crate::endian::U64<E>` |  |
| `reserved6` | `crate::endian::U64<E>` |  |

##### Implementations

###### Trait Implementations

- **Send**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Unpin**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Sync**
- **Freeze**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Clone**
  - ```rust
    fn clone(self: &Self) -> RoutinesCommand64<E> { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Pod**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **UnwindSafe**
- **RefUnwindSafe**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Copy**
#### Struct `SymtabCommand`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct SymtabCommand<E: Endian> {
    pub cmd: crate::endian::U32<E>,
    pub cmdsize: crate::endian::U32<E>,
    pub symoff: crate::endian::U32<E>,
    pub nsyms: crate::endian::U32<E>,
    pub stroff: crate::endian::U32<E>,
    pub strsize: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cmd` | `crate::endian::U32<E>` | LC_SYMTAB |
| `cmdsize` | `crate::endian::U32<E>` | sizeof(struct SymtabCommand) |
| `symoff` | `crate::endian::U32<E>` | symbol table offset |
| `nsyms` | `crate::endian::U32<E>` | number of symbol table entries |
| `stroff` | `crate::endian::U32<E>` | string table offset |
| `strsize` | `crate::endian::U32<E>` | string table size in bytes |

##### Implementations

###### Methods

- ```rust
  pub fn symbols<''data, Mach: MachHeader<Endian = E>, R: ReadRef<''data>>(self: &Self, endian: E, data: R) -> Result<SymbolTable<''data, Mach, R>> { /* ... */ }
  ```
  Return the symbol table that this command references.

###### Trait Implementations

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> SymtabCommand<E> { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **UnwindSafe**
- **Freeze**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Pod**
- **Sync**
- **Unpin**
- **Send**
- **RefUnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Copy**
#### Struct `DysymtabCommand`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct DysymtabCommand<E: Endian> {
    pub cmd: crate::endian::U32<E>,
    pub cmdsize: crate::endian::U32<E>,
    pub ilocalsym: crate::endian::U32<E>,
    pub nlocalsym: crate::endian::U32<E>,
    pub iextdefsym: crate::endian::U32<E>,
    pub nextdefsym: crate::endian::U32<E>,
    pub iundefsym: crate::endian::U32<E>,
    pub nundefsym: crate::endian::U32<E>,
    pub tocoff: crate::endian::U32<E>,
    pub ntoc: crate::endian::U32<E>,
    pub modtaboff: crate::endian::U32<E>,
    pub nmodtab: crate::endian::U32<E>,
    pub extrefsymoff: crate::endian::U32<E>,
    pub nextrefsyms: crate::endian::U32<E>,
    pub indirectsymoff: crate::endian::U32<E>,
    pub nindirectsyms: crate::endian::U32<E>,
    pub extreloff: crate::endian::U32<E>,
    pub nextrel: crate::endian::U32<E>,
    pub locreloff: crate::endian::U32<E>,
    pub nlocrel: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cmd` | `crate::endian::U32<E>` | LC_DYSYMTAB |
| `cmdsize` | `crate::endian::U32<E>` | sizeof(struct DysymtabCommand) |
| `ilocalsym` | `crate::endian::U32<E>` | index to local symbols |
| `nlocalsym` | `crate::endian::U32<E>` | number of local symbols |
| `iextdefsym` | `crate::endian::U32<E>` | index to externally defined symbols |
| `nextdefsym` | `crate::endian::U32<E>` | number of externally defined symbols |
| `iundefsym` | `crate::endian::U32<E>` | index to undefined symbols |
| `nundefsym` | `crate::endian::U32<E>` | number of undefined symbols |
| `tocoff` | `crate::endian::U32<E>` | file offset to table of contents |
| `ntoc` | `crate::endian::U32<E>` | number of entries in table of contents |
| `modtaboff` | `crate::endian::U32<E>` | file offset to module table |
| `nmodtab` | `crate::endian::U32<E>` | number of module table entries |
| `extrefsymoff` | `crate::endian::U32<E>` | offset to referenced symbol table |
| `nextrefsyms` | `crate::endian::U32<E>` | number of referenced symbol table entries |
| `indirectsymoff` | `crate::endian::U32<E>` | file offset to the indirect symbol table |
| `nindirectsyms` | `crate::endian::U32<E>` | number of indirect symbol table entries |
| `extreloff` | `crate::endian::U32<E>` | offset to external relocation entries |
| `nextrel` | `crate::endian::U32<E>` | number of external relocation entries |
| `locreloff` | `crate::endian::U32<E>` | offset to local relocation entries |
| `nlocrel` | `crate::endian::U32<E>` | number of local relocation entries |

##### Implementations

###### Trait Implementations

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Pod**
- **Freeze**
- **Copy**
- **Send**
- **UnwindSafe**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> DysymtabCommand<E> { /* ... */ }
    ```

- **Sync**
- **Unpin**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **RefUnwindSafe**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

#### Struct `DylibTableOfContents`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct DylibTableOfContents<E: Endian> {
    pub symbol_index: crate::endian::U32<E>,
    pub module_index: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `symbol_index` | `crate::endian::U32<E>` | the defined external symbol (index into the symbol table) |
| `module_index` | `crate::endian::U32<E>` | index into the module table this symbol is defined in |

##### Implementations

###### Trait Implementations

- **UnwindSafe**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Send**
- **Sync**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Unpin**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **RefUnwindSafe**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> DylibTableOfContents<E> { /* ... */ }
    ```

- **Pod**
- **Freeze**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Copy**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

#### Struct `DylibModule32`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct DylibModule32<E: Endian> {
    pub module_name: crate::endian::U32<E>,
    pub iextdefsym: crate::endian::U32<E>,
    pub nextdefsym: crate::endian::U32<E>,
    pub irefsym: crate::endian::U32<E>,
    pub nrefsym: crate::endian::U32<E>,
    pub ilocalsym: crate::endian::U32<E>,
    pub nlocalsym: crate::endian::U32<E>,
    pub iextrel: crate::endian::U32<E>,
    pub nextrel: crate::endian::U32<E>,
    pub iinit_iterm: crate::endian::U32<E>,
    pub ninit_nterm: crate::endian::U32<E>,
    pub objc_module_info_addr: crate::endian::U32<E>,
    pub objc_module_info_size: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `module_name` | `crate::endian::U32<E>` | the module name (index into string table) |
| `iextdefsym` | `crate::endian::U32<E>` | index into externally defined symbols |
| `nextdefsym` | `crate::endian::U32<E>` | number of externally defined symbols |
| `irefsym` | `crate::endian::U32<E>` | index into reference symbol table |
| `nrefsym` | `crate::endian::U32<E>` | number of reference symbol table entries |
| `ilocalsym` | `crate::endian::U32<E>` | index into symbols for local symbols |
| `nlocalsym` | `crate::endian::U32<E>` | number of local symbols |
| `iextrel` | `crate::endian::U32<E>` | index into external relocation entries |
| `nextrel` | `crate::endian::U32<E>` | number of external relocation entries |
| `iinit_iterm` | `crate::endian::U32<E>` | low 16 bits are the index into the init section, high 16 bits are the index into the term section |
| `ninit_nterm` | `crate::endian::U32<E>` | low 16 bits are the number of init section entries, high 16 bits are the number of term section entries |
| `objc_module_info_addr` | `crate::endian::U32<E>` | for this module address of the start of the (__OBJC,__module_info) section |
| `objc_module_info_size` | `crate::endian::U32<E>` | for this module size of the (__OBJC,__module_info) section |

##### Implementations

###### Trait Implementations

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Send**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **UnwindSafe**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Sync**
- **RefUnwindSafe**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> DylibModule32<E> { /* ... */ }
    ```

- **Copy**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Unpin**
- **Freeze**
- **Pod**
#### Struct `DylibModule64`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct DylibModule64<E: Endian> {
    pub module_name: crate::endian::U32<E>,
    pub iextdefsym: crate::endian::U32<E>,
    pub nextdefsym: crate::endian::U32<E>,
    pub irefsym: crate::endian::U32<E>,
    pub nrefsym: crate::endian::U32<E>,
    pub ilocalsym: crate::endian::U32<E>,
    pub nlocalsym: crate::endian::U32<E>,
    pub iextrel: crate::endian::U32<E>,
    pub nextrel: crate::endian::U32<E>,
    pub iinit_iterm: crate::endian::U32<E>,
    pub ninit_nterm: crate::endian::U32<E>,
    pub objc_module_info_size: crate::endian::U32<E>,
    pub objc_module_info_addr: crate::endian::U64<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `module_name` | `crate::endian::U32<E>` | the module name (index into string table) |
| `iextdefsym` | `crate::endian::U32<E>` | index into externally defined symbols |
| `nextdefsym` | `crate::endian::U32<E>` | number of externally defined symbols |
| `irefsym` | `crate::endian::U32<E>` | index into reference symbol table |
| `nrefsym` | `crate::endian::U32<E>` | number of reference symbol table entries |
| `ilocalsym` | `crate::endian::U32<E>` | index into symbols for local symbols |
| `nlocalsym` | `crate::endian::U32<E>` | number of local symbols |
| `iextrel` | `crate::endian::U32<E>` | index into external relocation entries |
| `nextrel` | `crate::endian::U32<E>` | number of external relocation entries |
| `iinit_iterm` | `crate::endian::U32<E>` | low 16 bits are the index into the init section, high 16 bits are the index into the term section |
| `ninit_nterm` | `crate::endian::U32<E>` | low 16 bits are the number of init section entries, high 16 bits are the number of term section entries |
| `objc_module_info_size` | `crate::endian::U32<E>` | for this module size of the (__OBJC,__module_info) section |
| `objc_module_info_addr` | `crate::endian::U64<E>` | for this module address of the start of the (__OBJC,__module_info) section |

##### Implementations

###### Trait Implementations

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Send**
- **Sync**
- **Copy**
- **Freeze**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **UnwindSafe**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Unpin**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Clone**
  - ```rust
    fn clone(self: &Self) -> DylibModule64<E> { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Pod**
- **RefUnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

#### Struct `DylibReference`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct DylibReference<E: Endian> {
    pub bitfield: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `bitfield` | `crate::endian::U32<E>` |  |

##### Implementations

###### Trait Implementations

- **Sync**
- **Freeze**
- **UnwindSafe**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Copy**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Pod**
- **Unpin**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> DylibReference<E> { /* ... */ }
    ```

- **RefUnwindSafe**
- **Send**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

#### Struct `TwolevelHintsCommand`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct TwolevelHintsCommand<E: Endian> {
    pub cmd: crate::endian::U32<E>,
    pub cmdsize: crate::endian::U32<E>,
    pub offset: crate::endian::U32<E>,
    pub nhints: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cmd` | `crate::endian::U32<E>` | LC_TWOLEVEL_HINTS |
| `cmdsize` | `crate::endian::U32<E>` | sizeof(struct TwolevelHintsCommand) |
| `offset` | `crate::endian::U32<E>` | offset to the hint table |
| `nhints` | `crate::endian::U32<E>` | number of hints in the hint table |

##### Implementations

###### Trait Implementations

- **UnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Send**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Freeze**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> TwolevelHintsCommand<E> { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Unpin**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Pod**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Sync**
- **RefUnwindSafe**
- **Copy**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

#### Struct `TwolevelHint`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct TwolevelHint<E: Endian> {
    pub bitfield: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `bitfield` | `crate::endian::U32<E>` |  |

##### Implementations

###### Trait Implementations

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> TwolevelHint<E> { /* ... */ }
    ```

- **RefUnwindSafe**
- **Unpin**
- **Copy**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Freeze**
- **Sync**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **UnwindSafe**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Pod**
- **Send**
#### Struct `PrebindCksumCommand`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct PrebindCksumCommand<E: Endian> {
    pub cmd: crate::endian::U32<E>,
    pub cmdsize: crate::endian::U32<E>,
    pub cksum: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cmd` | `crate::endian::U32<E>` | LC_PREBIND_CKSUM |
| `cmdsize` | `crate::endian::U32<E>` | sizeof(struct PrebindCksumCommand) |
| `cksum` | `crate::endian::U32<E>` | the check sum or zero |

##### Implementations

###### Trait Implementations

- **Send**
- **Copy**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **RefUnwindSafe**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> PrebindCksumCommand<E> { /* ... */ }
    ```

- **Sync**
- **Freeze**
- **Pod**
- **Unpin**
- **UnwindSafe**
#### Struct `UuidCommand`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct UuidCommand<E: Endian> {
    pub cmd: crate::endian::U32<E>,
    pub cmdsize: crate::endian::U32<E>,
    pub uuid: [u8; 16],
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cmd` | `crate::endian::U32<E>` | LC_UUID |
| `cmdsize` | `crate::endian::U32<E>` | sizeof(struct UuidCommand) |
| `uuid` | `[u8; 16]` | the 128-bit uuid |

##### Implementations

###### Trait Implementations

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Send**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Sync**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **RefUnwindSafe**
- **Freeze**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Unpin**
- **Pod**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **UnwindSafe**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> UuidCommand<E> { /* ... */ }
    ```

- **Copy**
#### Struct `RpathCommand`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct RpathCommand<E: Endian> {
    pub cmd: crate::endian::U32<E>,
    pub cmdsize: crate::endian::U32<E>,
    pub path: LcStr<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cmd` | `crate::endian::U32<E>` | LC_RPATH |
| `cmdsize` | `crate::endian::U32<E>` | includes string |
| `path` | `LcStr<E>` | path to add to run path |

##### Implementations

###### Trait Implementations

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **RefUnwindSafe**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **UnwindSafe**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Pod**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Sync**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> RpathCommand<E> { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Unpin**
- **Send**
- **Freeze**
- **Copy**
#### Struct `LinkeditDataCommand`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct LinkeditDataCommand<E: Endian> {
    pub cmd: crate::endian::U32<E>,
    pub cmdsize: crate::endian::U32<E>,
    pub dataoff: crate::endian::U32<E>,
    pub datasize: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cmd` | `crate::endian::U32<E>` | `LC_CODE_SIGNATURE`, `LC_SEGMENT_SPLIT_INFO`, `LC_FUNCTION_STARTS`,<br>`LC_DATA_IN_CODE`, `LC_DYLIB_CODE_SIGN_DRS`, `LC_LINKER_OPTIMIZATION_HINT`,<br>`LC_DYLD_EXPORTS_TRIE`, or `LC_DYLD_CHAINED_FIXUPS`. |
| `cmdsize` | `crate::endian::U32<E>` | sizeof(struct LinkeditDataCommand) |
| `dataoff` | `crate::endian::U32<E>` | file offset of data in __LINKEDIT segment |
| `datasize` | `crate::endian::U32<E>` | file size of data in __LINKEDIT segment |

##### Implementations

###### Trait Implementations

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Copy**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Sync**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Freeze**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **RefUnwindSafe**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> LinkeditDataCommand<E> { /* ... */ }
    ```

- **Pod**
- **Send**
- **UnwindSafe**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Unpin**
#### Struct `FilesetEntryCommand`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct FilesetEntryCommand<E: Endian> {
    pub cmd: crate::endian::U32<E>,
    pub cmdsize: crate::endian::U32<E>,
    pub vmaddr: crate::endian::U64<E>,
    pub fileoff: crate::endian::U64<E>,
    pub entry_id: LcStr<E>,
    pub reserved: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cmd` | `crate::endian::U32<E>` |  |
| `cmdsize` | `crate::endian::U32<E>` | includes id string |
| `vmaddr` | `crate::endian::U64<E>` | memory address of the dylib |
| `fileoff` | `crate::endian::U64<E>` | file offset of the dylib |
| `entry_id` | `LcStr<E>` | contained entry id |
| `reserved` | `crate::endian::U32<E>` | entry_id is 32-bits long, so this is the reserved padding |

##### Implementations

###### Trait Implementations

- **UnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Send**
- **Sync**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Pod**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> FilesetEntryCommand<E> { /* ... */ }
    ```

- **Freeze**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **RefUnwindSafe**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Unpin**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Copy**
#### Struct `EncryptionInfoCommand32`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct EncryptionInfoCommand32<E: Endian> {
    pub cmd: crate::endian::U32<E>,
    pub cmdsize: crate::endian::U32<E>,
    pub cryptoff: crate::endian::U32<E>,
    pub cryptsize: crate::endian::U32<E>,
    pub cryptid: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cmd` | `crate::endian::U32<E>` | LC_ENCRYPTION_INFO |
| `cmdsize` | `crate::endian::U32<E>` | sizeof(struct EncryptionInfoCommand32) |
| `cryptoff` | `crate::endian::U32<E>` | file offset of encrypted range |
| `cryptsize` | `crate::endian::U32<E>` | file size of encrypted range |
| `cryptid` | `crate::endian::U32<E>` | which enryption system, 0 means not-encrypted yet |

##### Implementations

###### Trait Implementations

- **Clone**
  - ```rust
    fn clone(self: &Self) -> EncryptionInfoCommand32<E> { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Sync**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Pod**
- **UnwindSafe**
- **RefUnwindSafe**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Freeze**
- **Unpin**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Send**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Copy**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

#### Struct `EncryptionInfoCommand64`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct EncryptionInfoCommand64<E: Endian> {
    pub cmd: crate::endian::U32<E>,
    pub cmdsize: crate::endian::U32<E>,
    pub cryptoff: crate::endian::U32<E>,
    pub cryptsize: crate::endian::U32<E>,
    pub cryptid: crate::endian::U32<E>,
    pub pad: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cmd` | `crate::endian::U32<E>` | LC_ENCRYPTION_INFO_64 |
| `cmdsize` | `crate::endian::U32<E>` | sizeof(struct EncryptionInfoCommand64) |
| `cryptoff` | `crate::endian::U32<E>` | file offset of encrypted range |
| `cryptsize` | `crate::endian::U32<E>` | file size of encrypted range |
| `cryptid` | `crate::endian::U32<E>` | which enryption system, 0 means not-encrypted yet |
| `pad` | `crate::endian::U32<E>` | padding to make this struct's size a multiple of 8 bytes |

##### Implementations

###### Trait Implementations

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Pod**
- **Freeze**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> EncryptionInfoCommand64<E> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Send**
- **RefUnwindSafe**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Unpin**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Sync**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Copy**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **UnwindSafe**
#### Struct `VersionMinCommand`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct VersionMinCommand<E: Endian> {
    pub cmd: crate::endian::U32<E>,
    pub cmdsize: crate::endian::U32<E>,
    pub version: crate::endian::U32<E>,
    pub sdk: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cmd` | `crate::endian::U32<E>` | LC_VERSION_MIN_MACOSX or LC_VERSION_MIN_IPHONEOS or LC_VERSION_MIN_WATCHOS or LC_VERSION_MIN_TVOS |
| `cmdsize` | `crate::endian::U32<E>` | sizeof(struct VersionMinCommand) |
| `version` | `crate::endian::U32<E>` | X.Y.Z is encoded in nibbles xxxx.yy.zz |
| `sdk` | `crate::endian::U32<E>` | X.Y.Z is encoded in nibbles xxxx.yy.zz |

##### Implementations

###### Trait Implementations

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Copy**
- **Unpin**
- **RefUnwindSafe**
- **UnwindSafe**
- **Freeze**
- **Sync**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Clone**
  - ```rust
    fn clone(self: &Self) -> VersionMinCommand<E> { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Send**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Pod**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

#### Struct `BuildVersionCommand`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct BuildVersionCommand<E: Endian> {
    pub cmd: crate::endian::U32<E>,
    pub cmdsize: crate::endian::U32<E>,
    pub platform: crate::endian::U32<E>,
    pub minos: crate::endian::U32<E>,
    pub sdk: crate::endian::U32<E>,
    pub ntools: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cmd` | `crate::endian::U32<E>` | LC_BUILD_VERSION |
| `cmdsize` | `crate::endian::U32<E>` | sizeof(struct BuildVersionCommand) plus ntools * sizeof(struct BuildToolVersion) |
| `platform` | `crate::endian::U32<E>` | platform |
| `minos` | `crate::endian::U32<E>` | X.Y.Z is encoded in nibbles xxxx.yy.zz |
| `sdk` | `crate::endian::U32<E>` | X.Y.Z is encoded in nibbles xxxx.yy.zz |
| `ntools` | `crate::endian::U32<E>` | number of tool entries following this |

##### Implementations

###### Trait Implementations

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **RefUnwindSafe**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Freeze**
- **Unpin**
- **Sync**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Copy**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Pod**
- **UnwindSafe**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> BuildVersionCommand<E> { /* ... */ }
    ```

- **Send**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

#### Struct `BuildToolVersion`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct BuildToolVersion<E: Endian> {
    pub tool: crate::endian::U32<E>,
    pub version: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `tool` | `crate::endian::U32<E>` | enum for the tool |
| `version` | `crate::endian::U32<E>` | version number of the tool |

##### Implementations

###### Trait Implementations

- **Clone**
  - ```rust
    fn clone(self: &Self) -> BuildToolVersion<E> { /* ... */ }
    ```

- **Pod**
- **Unpin**
- **Sync**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Copy**
- **Send**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **UnwindSafe**
- **RefUnwindSafe**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Freeze**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

#### Struct `DyldInfoCommand`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct DyldInfoCommand<E: Endian> {
    pub cmd: crate::endian::U32<E>,
    pub cmdsize: crate::endian::U32<E>,
    pub rebase_off: crate::endian::U32<E>,
    pub rebase_size: crate::endian::U32<E>,
    pub bind_off: crate::endian::U32<E>,
    pub bind_size: crate::endian::U32<E>,
    pub weak_bind_off: crate::endian::U32<E>,
    pub weak_bind_size: crate::endian::U32<E>,
    pub lazy_bind_off: crate::endian::U32<E>,
    pub lazy_bind_size: crate::endian::U32<E>,
    pub export_off: crate::endian::U32<E>,
    pub export_size: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cmd` | `crate::endian::U32<E>` | LC_DYLD_INFO or LC_DYLD_INFO_ONLY |
| `cmdsize` | `crate::endian::U32<E>` | sizeof(struct DyldInfoCommand) |
| `rebase_off` | `crate::endian::U32<E>` | file offset to rebase info |
| `rebase_size` | `crate::endian::U32<E>` | size of rebase info |
| `bind_off` | `crate::endian::U32<E>` | file offset to binding info |
| `bind_size` | `crate::endian::U32<E>` | size of binding info |
| `weak_bind_off` | `crate::endian::U32<E>` | file offset to weak binding info |
| `weak_bind_size` | `crate::endian::U32<E>` | size of weak binding info |
| `lazy_bind_off` | `crate::endian::U32<E>` | file offset to lazy binding info |
| `lazy_bind_size` | `crate::endian::U32<E>` | size of lazy binding infs |
| `export_off` | `crate::endian::U32<E>` | file offset to lazy binding info |
| `export_size` | `crate::endian::U32<E>` | size of lazy binding infs |

##### Implementations

###### Trait Implementations

- **Clone**
  - ```rust
    fn clone(self: &Self) -> DyldInfoCommand<E> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Pod**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Unpin**
- **Freeze**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Copy**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Sync**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **RefUnwindSafe**
- **Send**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **UnwindSafe**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

#### Struct `LinkerOptionCommand`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct LinkerOptionCommand<E: Endian> {
    pub cmd: crate::endian::U32<E>,
    pub cmdsize: crate::endian::U32<E>,
    pub count: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cmd` | `crate::endian::U32<E>` | LC_LINKER_OPTION only used in MH_OBJECT filetypes |
| `cmdsize` | `crate::endian::U32<E>` |  |
| `count` | `crate::endian::U32<E>` | number of strings |

##### Implementations

###### Trait Implementations

- **UnwindSafe**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Send**
- **Freeze**
- **RefUnwindSafe**
- **Sync**
- **Unpin**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Copy**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Pod**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> LinkerOptionCommand<E> { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

#### Struct `SymsegCommand`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct SymsegCommand<E: Endian> {
    pub cmd: crate::endian::U32<E>,
    pub cmdsize: crate::endian::U32<E>,
    pub offset: crate::endian::U32<E>,
    pub size: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cmd` | `crate::endian::U32<E>` | LC_SYMSEG |
| `cmdsize` | `crate::endian::U32<E>` | sizeof(struct SymsegCommand) |
| `offset` | `crate::endian::U32<E>` | symbol segment offset |
| `size` | `crate::endian::U32<E>` | symbol segment size in bytes |

##### Implementations

###### Trait Implementations

- **Sync**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Send**
- **RefUnwindSafe**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> SymsegCommand<E> { /* ... */ }
    ```

- **Pod**
- **UnwindSafe**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Freeze**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Copy**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Unpin**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

#### Struct `IdentCommand`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct IdentCommand<E: Endian> {
    pub cmd: crate::endian::U32<E>,
    pub cmdsize: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cmd` | `crate::endian::U32<E>` | LC_IDENT |
| `cmdsize` | `crate::endian::U32<E>` | strings that follow this command |

##### Implementations

###### Trait Implementations

- **UnwindSafe**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> IdentCommand<E> { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Freeze**
- **Sync**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Send**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Pod**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Unpin**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Copy**
- **RefUnwindSafe**
#### Struct `FvmfileCommand`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct FvmfileCommand<E: Endian> {
    pub cmd: crate::endian::U32<E>,
    pub cmdsize: crate::endian::U32<E>,
    pub name: LcStr<E>,
    pub header_addr: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cmd` | `crate::endian::U32<E>` | LC_FVMFILE |
| `cmdsize` | `crate::endian::U32<E>` | includes pathname string |
| `name` | `LcStr<E>` | files pathname |
| `header_addr` | `crate::endian::U32<E>` | files virtual address |

##### Implementations

###### Trait Implementations

- **Clone**
  - ```rust
    fn clone(self: &Self) -> FvmfileCommand<E> { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Sync**
- **UnwindSafe**
- **Pod**
- **Send**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Freeze**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Copy**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **RefUnwindSafe**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Unpin**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

#### Struct `EntryPointCommand`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct EntryPointCommand<E: Endian> {
    pub cmd: crate::endian::U32<E>,
    pub cmdsize: crate::endian::U32<E>,
    pub entryoff: crate::endian::U64<E>,
    pub stacksize: crate::endian::U64<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cmd` | `crate::endian::U32<E>` | LC_MAIN only used in MH_EXECUTE filetypes |
| `cmdsize` | `crate::endian::U32<E>` | 24 |
| `entryoff` | `crate::endian::U64<E>` | file (__TEXT) offset of main() |
| `stacksize` | `crate::endian::U64<E>` | if not zero, initial stack size |

##### Implementations

###### Trait Implementations

- **UnwindSafe**
- **Send**
- **RefUnwindSafe**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Sync**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Unpin**
- **Copy**
- **Pod**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> EntryPointCommand<E> { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Freeze**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

#### Struct `SourceVersionCommand`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct SourceVersionCommand<E: Endian> {
    pub cmd: crate::endian::U32<E>,
    pub cmdsize: crate::endian::U32<E>,
    pub version: crate::endian::U64<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cmd` | `crate::endian::U32<E>` | LC_SOURCE_VERSION |
| `cmdsize` | `crate::endian::U32<E>` | 16 |
| `version` | `crate::endian::U64<E>` | A.B.C.D.E packed as a24.b10.c10.d10.e10 |

##### Implementations

###### Trait Implementations

- **UnwindSafe**
- **Sync**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Pod**
- **RefUnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Unpin**
- **Send**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> SourceVersionCommand<E> { /* ... */ }
    ```

- **Copy**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Freeze**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

#### Struct `DataInCodeEntry`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct DataInCodeEntry<E: Endian> {
    pub offset: crate::endian::U32<E>,
    pub length: crate::endian::U16<E>,
    pub kind: crate::endian::U16<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `offset` | `crate::endian::U32<E>` | from mach_header to start of data range |
| `length` | `crate::endian::U16<E>` | number of bytes in data range |
| `kind` | `crate::endian::U16<E>` | a DICE_KIND_* value |

##### Implementations

###### Trait Implementations

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Unpin**
- **Freeze**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Copy**
- **RefUnwindSafe**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Sync**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Send**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Clone**
  - ```rust
    fn clone(self: &Self) -> DataInCodeEntry<E> { /* ... */ }
    ```

- **Pod**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **UnwindSafe**
#### Struct `NoteCommand`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct NoteCommand<E: Endian> {
    pub cmd: crate::endian::U32<E>,
    pub cmdsize: crate::endian::U32<E>,
    pub data_owner: [u8; 16],
    pub offset: crate::endian::U64<E>,
    pub size: crate::endian::U64<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cmd` | `crate::endian::U32<E>` | LC_NOTE |
| `cmdsize` | `crate::endian::U32<E>` | sizeof(struct NoteCommand) |
| `data_owner` | `[u8; 16]` | owner name for this LC_NOTE |
| `offset` | `crate::endian::U64<E>` | file offset of this data |
| `size` | `crate::endian::U64<E>` | length of data region |

##### Implementations

###### Trait Implementations

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **UnwindSafe**
- **Copy**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Freeze**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Unpin**
- **Sync**
- **Pod**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Send**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> NoteCommand<E> { /* ... */ }
    ```

- **RefUnwindSafe**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

#### Struct `Nlist32`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct Nlist32<E: Endian> {
    pub n_strx: crate::endian::U32<E>,
    pub n_type: u8,
    pub n_sect: u8,
    pub n_desc: crate::endian::U16<E>,
    pub n_value: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `n_strx` | `crate::endian::U32<E>` | index into the string table |
| `n_type` | `u8` | type flag, see below |
| `n_sect` | `u8` | section number or NO_SECT |
| `n_desc` | `crate::endian::U16<E>` | see <mach-o/stab.h> |
| `n_value` | `crate::endian::U32<E>` | value of this symbol (or stab offset) |

##### Implementations

###### Trait Implementations

- **Freeze**
- **Copy**
- **Pod**
- **RefUnwindSafe**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Unpin**
- **Send**
- **UnwindSafe**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> Nlist32<E> { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Nlist**
  - ```rust
    fn n_strx(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn n_type(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn n_sect(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn n_desc(self: &Self, endian: <Self as >::Endian) -> u16 { /* ... */ }
    ```

  - ```rust
    fn n_value(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

- **Sync**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

#### Struct `Nlist64`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct Nlist64<E: Endian> {
    pub n_strx: crate::endian::U32<E>,
    pub n_type: u8,
    pub n_sect: u8,
    pub n_desc: crate::endian::U16<E>,
    pub n_value: crate::endian::U64Bytes<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `n_strx` | `crate::endian::U32<E>` | index into the string table |
| `n_type` | `u8` | type flag, see below |
| `n_sect` | `u8` | section number or NO_SECT |
| `n_desc` | `crate::endian::U16<E>` | see <mach-o/stab.h> |
| `n_value` | `crate::endian::U64Bytes<E>` | value of this symbol (or stab offset) |

##### Implementations

###### Trait Implementations

- **RefUnwindSafe**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Nlist**
  - ```rust
    fn n_strx(self: &Self, endian: <Self as >::Endian) -> u32 { /* ... */ }
    ```

  - ```rust
    fn n_type(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn n_sect(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn n_desc(self: &Self, endian: <Self as >::Endian) -> u16 { /* ... */ }
    ```

  - ```rust
    fn n_value(self: &Self, endian: <Self as >::Endian) -> <Self as >::Word { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Freeze**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Sync**
- **Copy**
- **Send**
- **Unpin**
- **UnwindSafe**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> Nlist64<E> { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Pod**
#### Struct `Relocation`

**Attributes:**

- `#[repr(C)]`

A relocation entry.

Mach-O relocations have plain and scattered variants, with the
meaning of the fields depending on the variant.

This type provides functions for determining whether the relocation
is scattered, and for accessing the fields of each variant.

```rust
pub struct Relocation<E: Endian> {
    pub r_word0: crate::endian::U32<E>,
    pub r_word1: crate::endian::U32<E>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `r_word0` | `crate::endian::U32<E>` |  |
| `r_word1` | `crate::endian::U32<E>` |  |

##### Implementations

###### Methods

- ```rust
  pub fn r_scattered(self: Self, endian: E, cputype: u32) -> bool { /* ... */ }
  ```
  Determine whether this is a scattered relocation.

- ```rust
  pub fn info(self: Self, endian: E) -> RelocationInfo { /* ... */ }
  ```
  Return the fields of a plain relocation.

- ```rust
  pub fn scattered_info(self: Self, endian: E) -> ScatteredRelocationInfo { /* ... */ }
  ```
  Return the fields of a scattered relocation.

###### Trait Implementations

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Freeze**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Unpin**
- **Send**
- **RefUnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> Relocation<E> { /* ... */ }
    ```

- **Copy**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Sync**
- **UnwindSafe**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Pod**
#### Struct `RelocationInfo`

```rust
pub struct RelocationInfo {
    pub r_address: u32,
    pub r_symbolnum: u32,
    pub r_pcrel: bool,
    pub r_length: u8,
    pub r_extern: bool,
    pub r_type: u8,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `r_address` | `u32` | offset in the section to what is being relocated |
| `r_symbolnum` | `u32` | symbol index if r_extern == 1 or section ordinal if r_extern == 0 |
| `r_pcrel` | `bool` | was relocated pc relative already |
| `r_length` | `u8` | 0=byte, 1=word, 2=long, 3=quad |
| `r_extern` | `bool` | does not include value of sym referenced |
| `r_type` | `u8` | if not 0, machine specific relocation type |

##### Implementations

###### Methods

- ```rust
  pub fn relocation<E: Endian>(self: Self, endian: E) -> Relocation<E> { /* ... */ }
  ```
  Combine the fields into a `Relocation`.

###### Trait Implementations

- **Clone**
  - ```rust
    fn clone(self: &Self) -> RelocationInfo { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **RefUnwindSafe**
- **Freeze**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Copy**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Send**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Unpin**
- **UnwindSafe**
- **Sync**
#### Struct `ScatteredRelocationInfo`

```rust
pub struct ScatteredRelocationInfo {
    pub r_address: u32,
    pub r_type: u8,
    pub r_length: u8,
    pub r_pcrel: bool,
    pub r_value: u32,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `r_address` | `u32` | offset in the section to what is being relocated |
| `r_type` | `u8` | if not 0, machine specific relocation type |
| `r_length` | `u8` | 0=byte, 1=word, 2=long, 3=quad |
| `r_pcrel` | `bool` | was relocated pc relative already |
| `r_value` | `u32` | the value the item to be relocated is referring to (without any offset added) |

##### Implementations

###### Methods

- ```rust
  pub fn relocation<E: Endian>(self: Self, endian: E) -> Relocation<E> { /* ... */ }
  ```
  Combine the fields into a `Relocation`.

###### Trait Implementations

- **Copy**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Freeze**
- **Sync**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **UnwindSafe**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ScatteredRelocationInfo { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **RefUnwindSafe**
- **Send**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Unpin**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

### Functions

#### Function `cpu_subtype_intel`

**Attributes:**

- `#[inline]`

```rust
pub const fn cpu_subtype_intel(f: u32, m: u32) -> u32 { /* ... */ }
```

#### Function `cpu_subtype_intel_family`

**Attributes:**

- `#[inline]`

```rust
pub const fn cpu_subtype_intel_family(x: u32) -> u32 { /* ... */ }
```

#### Function `cpu_subtype_intel_model`

**Attributes:**

- `#[inline]`

```rust
pub const fn cpu_subtype_intel_model(x: u32) -> u32 { /* ... */ }
```

### Constants and Statics

#### Constant `CPU_ARCH_MASK`

mask for architecture bits

```rust
pub const CPU_ARCH_MASK: u32 = 0xff00_0000;
```

#### Constant `CPU_ARCH_ABI64`

64 bit ABI

```rust
pub const CPU_ARCH_ABI64: u32 = 0x0100_0000;
```

#### Constant `CPU_ARCH_ABI64_32`

ABI for 64-bit hardware with 32-bit types; LP32

```rust
pub const CPU_ARCH_ABI64_32: u32 = 0x0200_0000;
```

#### Constant `CPU_TYPE_ANY`

```rust
pub const CPU_TYPE_ANY: u32 = _;
```

#### Constant `CPU_TYPE_VAX`

```rust
pub const CPU_TYPE_VAX: u32 = 1;
```

#### Constant `CPU_TYPE_MC680X0`

```rust
pub const CPU_TYPE_MC680X0: u32 = 6;
```

#### Constant `CPU_TYPE_X86`

```rust
pub const CPU_TYPE_X86: u32 = 7;
```

#### Constant `CPU_TYPE_X86_64`

```rust
pub const CPU_TYPE_X86_64: u32 = _;
```

#### Constant `CPU_TYPE_MIPS`

```rust
pub const CPU_TYPE_MIPS: u32 = 8;
```

#### Constant `CPU_TYPE_MC98000`

```rust
pub const CPU_TYPE_MC98000: u32 = 10;
```

#### Constant `CPU_TYPE_HPPA`

```rust
pub const CPU_TYPE_HPPA: u32 = 11;
```

#### Constant `CPU_TYPE_ARM`

```rust
pub const CPU_TYPE_ARM: u32 = 12;
```

#### Constant `CPU_TYPE_ARM64`

```rust
pub const CPU_TYPE_ARM64: u32 = _;
```

#### Constant `CPU_TYPE_ARM64_32`

```rust
pub const CPU_TYPE_ARM64_32: u32 = _;
```

#### Constant `CPU_TYPE_MC88000`

```rust
pub const CPU_TYPE_MC88000: u32 = 13;
```

#### Constant `CPU_TYPE_SPARC`

```rust
pub const CPU_TYPE_SPARC: u32 = 14;
```

#### Constant `CPU_TYPE_I860`

```rust
pub const CPU_TYPE_I860: u32 = 15;
```

#### Constant `CPU_TYPE_ALPHA`

```rust
pub const CPU_TYPE_ALPHA: u32 = 16;
```

#### Constant `CPU_TYPE_POWERPC`

```rust
pub const CPU_TYPE_POWERPC: u32 = 18;
```

#### Constant `CPU_TYPE_POWERPC64`

```rust
pub const CPU_TYPE_POWERPC64: u32 = _;
```

#### Constant `CPU_SUBTYPE_MASK`

mask for feature flags

```rust
pub const CPU_SUBTYPE_MASK: u32 = 0xff00_0000;
```

#### Constant `CPU_SUBTYPE_LIB64`

64 bit libraries

```rust
pub const CPU_SUBTYPE_LIB64: u32 = 0x8000_0000;
```

#### Constant `CPU_SUBTYPE_PTRAUTH_ABI`

pointer authentication with versioned ABI

```rust
pub const CPU_SUBTYPE_PTRAUTH_ABI: u32 = 0x8000_0000;
```

#### Constant `CPU_SUBTYPE_ANY`

When selecting a slice, ANY will pick the slice with the best
grading for the selected cpu_type_t, unlike the "ALL" subtypes,
which are the slices that can run on any hardware for that cpu type.

```rust
pub const CPU_SUBTYPE_ANY: u32 = _;
```

#### Constant `CPU_SUBTYPE_MULTIPLE`

```rust
pub const CPU_SUBTYPE_MULTIPLE: u32 = _;
```

#### Constant `CPU_SUBTYPE_LITTLE_ENDIAN`

```rust
pub const CPU_SUBTYPE_LITTLE_ENDIAN: u32 = 0;
```

#### Constant `CPU_SUBTYPE_BIG_ENDIAN`

```rust
pub const CPU_SUBTYPE_BIG_ENDIAN: u32 = 1;
```

#### Constant `CPU_SUBTYPE_VAX_ALL`

```rust
pub const CPU_SUBTYPE_VAX_ALL: u32 = 0;
```

#### Constant `CPU_SUBTYPE_VAX780`

```rust
pub const CPU_SUBTYPE_VAX780: u32 = 1;
```

#### Constant `CPU_SUBTYPE_VAX785`

```rust
pub const CPU_SUBTYPE_VAX785: u32 = 2;
```

#### Constant `CPU_SUBTYPE_VAX750`

```rust
pub const CPU_SUBTYPE_VAX750: u32 = 3;
```

#### Constant `CPU_SUBTYPE_VAX730`

```rust
pub const CPU_SUBTYPE_VAX730: u32 = 4;
```

#### Constant `CPU_SUBTYPE_UVAXI`

```rust
pub const CPU_SUBTYPE_UVAXI: u32 = 5;
```

#### Constant `CPU_SUBTYPE_UVAXII`

```rust
pub const CPU_SUBTYPE_UVAXII: u32 = 6;
```

#### Constant `CPU_SUBTYPE_VAX8200`

```rust
pub const CPU_SUBTYPE_VAX8200: u32 = 7;
```

#### Constant `CPU_SUBTYPE_VAX8500`

```rust
pub const CPU_SUBTYPE_VAX8500: u32 = 8;
```

#### Constant `CPU_SUBTYPE_VAX8600`

```rust
pub const CPU_SUBTYPE_VAX8600: u32 = 9;
```

#### Constant `CPU_SUBTYPE_VAX8650`

```rust
pub const CPU_SUBTYPE_VAX8650: u32 = 10;
```

#### Constant `CPU_SUBTYPE_VAX8800`

```rust
pub const CPU_SUBTYPE_VAX8800: u32 = 11;
```

#### Constant `CPU_SUBTYPE_UVAXIII`

```rust
pub const CPU_SUBTYPE_UVAXIII: u32 = 12;
```

#### Constant `CPU_SUBTYPE_MC680X0_ALL`

```rust
pub const CPU_SUBTYPE_MC680X0_ALL: u32 = 1;
```

#### Constant `CPU_SUBTYPE_MC68030`

```rust
pub const CPU_SUBTYPE_MC68030: u32 = 1;
```

#### Constant `CPU_SUBTYPE_MC68040`

```rust
pub const CPU_SUBTYPE_MC68040: u32 = 2;
```

#### Constant `CPU_SUBTYPE_MC68030_ONLY`

```rust
pub const CPU_SUBTYPE_MC68030_ONLY: u32 = 3;
```

#### Constant `CPU_SUBTYPE_I386_ALL`

```rust
pub const CPU_SUBTYPE_I386_ALL: u32 = _;
```

#### Constant `CPU_SUBTYPE_386`

```rust
pub const CPU_SUBTYPE_386: u32 = _;
```

#### Constant `CPU_SUBTYPE_486`

```rust
pub const CPU_SUBTYPE_486: u32 = _;
```

#### Constant `CPU_SUBTYPE_486SX`

```rust
pub const CPU_SUBTYPE_486SX: u32 = _;
```

#### Constant `CPU_SUBTYPE_586`

```rust
pub const CPU_SUBTYPE_586: u32 = _;
```

#### Constant `CPU_SUBTYPE_PENT`

```rust
pub const CPU_SUBTYPE_PENT: u32 = _;
```

#### Constant `CPU_SUBTYPE_PENTPRO`

```rust
pub const CPU_SUBTYPE_PENTPRO: u32 = _;
```

#### Constant `CPU_SUBTYPE_PENTII_M3`

```rust
pub const CPU_SUBTYPE_PENTII_M3: u32 = _;
```

#### Constant `CPU_SUBTYPE_PENTII_M5`

```rust
pub const CPU_SUBTYPE_PENTII_M5: u32 = _;
```

#### Constant `CPU_SUBTYPE_CELERON`

```rust
pub const CPU_SUBTYPE_CELERON: u32 = _;
```

#### Constant `CPU_SUBTYPE_CELERON_MOBILE`

```rust
pub const CPU_SUBTYPE_CELERON_MOBILE: u32 = _;
```

#### Constant `CPU_SUBTYPE_PENTIUM_3`

```rust
pub const CPU_SUBTYPE_PENTIUM_3: u32 = _;
```

#### Constant `CPU_SUBTYPE_PENTIUM_3_M`

```rust
pub const CPU_SUBTYPE_PENTIUM_3_M: u32 = _;
```

#### Constant `CPU_SUBTYPE_PENTIUM_3_XEON`

```rust
pub const CPU_SUBTYPE_PENTIUM_3_XEON: u32 = _;
```

#### Constant `CPU_SUBTYPE_PENTIUM_M`

```rust
pub const CPU_SUBTYPE_PENTIUM_M: u32 = _;
```

#### Constant `CPU_SUBTYPE_PENTIUM_4`

```rust
pub const CPU_SUBTYPE_PENTIUM_4: u32 = _;
```

#### Constant `CPU_SUBTYPE_PENTIUM_4_M`

```rust
pub const CPU_SUBTYPE_PENTIUM_4_M: u32 = _;
```

#### Constant `CPU_SUBTYPE_ITANIUM`

```rust
pub const CPU_SUBTYPE_ITANIUM: u32 = _;
```

#### Constant `CPU_SUBTYPE_ITANIUM_2`

```rust
pub const CPU_SUBTYPE_ITANIUM_2: u32 = _;
```

#### Constant `CPU_SUBTYPE_XEON`

```rust
pub const CPU_SUBTYPE_XEON: u32 = _;
```

#### Constant `CPU_SUBTYPE_XEON_MP`

```rust
pub const CPU_SUBTYPE_XEON_MP: u32 = _;
```

#### Constant `CPU_SUBTYPE_INTEL_FAMILY_MAX`

```rust
pub const CPU_SUBTYPE_INTEL_FAMILY_MAX: u32 = 15;
```

#### Constant `CPU_SUBTYPE_INTEL_MODEL_ALL`

```rust
pub const CPU_SUBTYPE_INTEL_MODEL_ALL: u32 = 0;
```

#### Constant `CPU_SUBTYPE_X86_ALL`

```rust
pub const CPU_SUBTYPE_X86_ALL: u32 = 3;
```

#### Constant `CPU_SUBTYPE_X86_64_ALL`

```rust
pub const CPU_SUBTYPE_X86_64_ALL: u32 = 3;
```

#### Constant `CPU_SUBTYPE_X86_ARCH1`

```rust
pub const CPU_SUBTYPE_X86_ARCH1: u32 = 4;
```

#### Constant `CPU_SUBTYPE_X86_64_H`

Haswell feature subset

```rust
pub const CPU_SUBTYPE_X86_64_H: u32 = 8;
```

#### Constant `CPU_SUBTYPE_MIPS_ALL`

```rust
pub const CPU_SUBTYPE_MIPS_ALL: u32 = 0;
```

#### Constant `CPU_SUBTYPE_MIPS_R2300`

```rust
pub const CPU_SUBTYPE_MIPS_R2300: u32 = 1;
```

#### Constant `CPU_SUBTYPE_MIPS_R2600`

```rust
pub const CPU_SUBTYPE_MIPS_R2600: u32 = 2;
```

#### Constant `CPU_SUBTYPE_MIPS_R2800`

```rust
pub const CPU_SUBTYPE_MIPS_R2800: u32 = 3;
```

#### Constant `CPU_SUBTYPE_MIPS_R2000A`

pmax

```rust
pub const CPU_SUBTYPE_MIPS_R2000A: u32 = 4;
```

#### Constant `CPU_SUBTYPE_MIPS_R2000`

```rust
pub const CPU_SUBTYPE_MIPS_R2000: u32 = 5;
```

#### Constant `CPU_SUBTYPE_MIPS_R3000A`

3max

```rust
pub const CPU_SUBTYPE_MIPS_R3000A: u32 = 6;
```

#### Constant `CPU_SUBTYPE_MIPS_R3000`

```rust
pub const CPU_SUBTYPE_MIPS_R3000: u32 = 7;
```

#### Constant `CPU_SUBTYPE_MC98000_ALL`

```rust
pub const CPU_SUBTYPE_MC98000_ALL: u32 = 0;
```

#### Constant `CPU_SUBTYPE_MC98601`

```rust
pub const CPU_SUBTYPE_MC98601: u32 = 1;
```

#### Constant `CPU_SUBTYPE_HPPA_ALL`

```rust
pub const CPU_SUBTYPE_HPPA_ALL: u32 = 0;
```

#### Constant `CPU_SUBTYPE_HPPA_7100LC`

```rust
pub const CPU_SUBTYPE_HPPA_7100LC: u32 = 1;
```

#### Constant `CPU_SUBTYPE_MC88000_ALL`

```rust
pub const CPU_SUBTYPE_MC88000_ALL: u32 = 0;
```

#### Constant `CPU_SUBTYPE_MC88100`

```rust
pub const CPU_SUBTYPE_MC88100: u32 = 1;
```

#### Constant `CPU_SUBTYPE_MC88110`

```rust
pub const CPU_SUBTYPE_MC88110: u32 = 2;
```

#### Constant `CPU_SUBTYPE_SPARC_ALL`

```rust
pub const CPU_SUBTYPE_SPARC_ALL: u32 = 0;
```

#### Constant `CPU_SUBTYPE_I860_ALL`

```rust
pub const CPU_SUBTYPE_I860_ALL: u32 = 0;
```

#### Constant `CPU_SUBTYPE_I860_860`

```rust
pub const CPU_SUBTYPE_I860_860: u32 = 1;
```

#### Constant `CPU_SUBTYPE_POWERPC_ALL`

```rust
pub const CPU_SUBTYPE_POWERPC_ALL: u32 = 0;
```

#### Constant `CPU_SUBTYPE_POWERPC_601`

```rust
pub const CPU_SUBTYPE_POWERPC_601: u32 = 1;
```

#### Constant `CPU_SUBTYPE_POWERPC_602`

```rust
pub const CPU_SUBTYPE_POWERPC_602: u32 = 2;
```

#### Constant `CPU_SUBTYPE_POWERPC_603`

```rust
pub const CPU_SUBTYPE_POWERPC_603: u32 = 3;
```

#### Constant `CPU_SUBTYPE_POWERPC_603E`

```rust
pub const CPU_SUBTYPE_POWERPC_603E: u32 = 4;
```

#### Constant `CPU_SUBTYPE_POWERPC_603EV`

```rust
pub const CPU_SUBTYPE_POWERPC_603EV: u32 = 5;
```

#### Constant `CPU_SUBTYPE_POWERPC_604`

```rust
pub const CPU_SUBTYPE_POWERPC_604: u32 = 6;
```

#### Constant `CPU_SUBTYPE_POWERPC_604E`

```rust
pub const CPU_SUBTYPE_POWERPC_604E: u32 = 7;
```

#### Constant `CPU_SUBTYPE_POWERPC_620`

```rust
pub const CPU_SUBTYPE_POWERPC_620: u32 = 8;
```

#### Constant `CPU_SUBTYPE_POWERPC_750`

```rust
pub const CPU_SUBTYPE_POWERPC_750: u32 = 9;
```

#### Constant `CPU_SUBTYPE_POWERPC_7400`

```rust
pub const CPU_SUBTYPE_POWERPC_7400: u32 = 10;
```

#### Constant `CPU_SUBTYPE_POWERPC_7450`

```rust
pub const CPU_SUBTYPE_POWERPC_7450: u32 = 11;
```

#### Constant `CPU_SUBTYPE_POWERPC_970`

```rust
pub const CPU_SUBTYPE_POWERPC_970: u32 = 100;
```

#### Constant `CPU_SUBTYPE_ARM_ALL`

```rust
pub const CPU_SUBTYPE_ARM_ALL: u32 = 0;
```

#### Constant `CPU_SUBTYPE_ARM_V4T`

```rust
pub const CPU_SUBTYPE_ARM_V4T: u32 = 5;
```

#### Constant `CPU_SUBTYPE_ARM_V6`

```rust
pub const CPU_SUBTYPE_ARM_V6: u32 = 6;
```

#### Constant `CPU_SUBTYPE_ARM_V5TEJ`

```rust
pub const CPU_SUBTYPE_ARM_V5TEJ: u32 = 7;
```

#### Constant `CPU_SUBTYPE_ARM_XSCALE`

```rust
pub const CPU_SUBTYPE_ARM_XSCALE: u32 = 8;
```

#### Constant `CPU_SUBTYPE_ARM_V7`

ARMv7-A and ARMv7-R

```rust
pub const CPU_SUBTYPE_ARM_V7: u32 = 9;
```

#### Constant `CPU_SUBTYPE_ARM_V7F`

Cortex A9

```rust
pub const CPU_SUBTYPE_ARM_V7F: u32 = 10;
```

#### Constant `CPU_SUBTYPE_ARM_V7S`

Swift

```rust
pub const CPU_SUBTYPE_ARM_V7S: u32 = 11;
```

#### Constant `CPU_SUBTYPE_ARM_V7K`

```rust
pub const CPU_SUBTYPE_ARM_V7K: u32 = 12;
```

#### Constant `CPU_SUBTYPE_ARM_V8`

```rust
pub const CPU_SUBTYPE_ARM_V8: u32 = 13;
```

#### Constant `CPU_SUBTYPE_ARM_V6M`

Not meant to be run under xnu

```rust
pub const CPU_SUBTYPE_ARM_V6M: u32 = 14;
```

#### Constant `CPU_SUBTYPE_ARM_V7M`

Not meant to be run under xnu

```rust
pub const CPU_SUBTYPE_ARM_V7M: u32 = 15;
```

#### Constant `CPU_SUBTYPE_ARM_V7EM`

Not meant to be run under xnu

```rust
pub const CPU_SUBTYPE_ARM_V7EM: u32 = 16;
```

#### Constant `CPU_SUBTYPE_ARM_V8M`

Not meant to be run under xnu

```rust
pub const CPU_SUBTYPE_ARM_V8M: u32 = 17;
```

#### Constant `CPU_SUBTYPE_ARM64_ALL`

```rust
pub const CPU_SUBTYPE_ARM64_ALL: u32 = 0;
```

#### Constant `CPU_SUBTYPE_ARM64_V8`

```rust
pub const CPU_SUBTYPE_ARM64_V8: u32 = 1;
```

#### Constant `CPU_SUBTYPE_ARM64E`

```rust
pub const CPU_SUBTYPE_ARM64E: u32 = 2;
```

#### Constant `CPU_SUBTYPE_ARM64_32_ALL`

```rust
pub const CPU_SUBTYPE_ARM64_32_ALL: u32 = 0;
```

#### Constant `CPU_SUBTYPE_ARM64_32_V8`

```rust
pub const CPU_SUBTYPE_ARM64_32_V8: u32 = 1;
```

#### Constant `VM_PROT_READ`

read permission

```rust
pub const VM_PROT_READ: u32 = 0x01;
```

#### Constant `VM_PROT_WRITE`

write permission

```rust
pub const VM_PROT_WRITE: u32 = 0x02;
```

#### Constant `VM_PROT_EXECUTE`

execute permission

```rust
pub const VM_PROT_EXECUTE: u32 = 0x04;
```

#### Constant `FAT_MAGIC`

```rust
pub const FAT_MAGIC: u32 = 0xcafe_babe;
```

#### Constant `FAT_CIGAM`

NXSwapLong(FAT_MAGIC)

```rust
pub const FAT_CIGAM: u32 = 0xbeba_feca;
```

#### Constant `FAT_MAGIC_64`

```rust
pub const FAT_MAGIC_64: u32 = 0xcafe_babf;
```

#### Constant `FAT_CIGAM_64`

NXSwapLong(FAT_MAGIC_64)

```rust
pub const FAT_CIGAM_64: u32 = 0xbfba_feca;
```

#### Constant `MH_MAGIC`

the mach magic number

```rust
pub const MH_MAGIC: u32 = 0xfeed_face;
```

#### Constant `MH_CIGAM`

NXSwapInt(MH_MAGIC)

```rust
pub const MH_CIGAM: u32 = 0xcefa_edfe;
```

#### Constant `MH_MAGIC_64`

the 64-bit mach magic number

```rust
pub const MH_MAGIC_64: u32 = 0xfeed_facf;
```

#### Constant `MH_CIGAM_64`

NXSwapInt(MH_MAGIC_64)

```rust
pub const MH_CIGAM_64: u32 = 0xcffa_edfe;
```

#### Constant `MH_OBJECT`

relocatable object file

```rust
pub const MH_OBJECT: u32 = 0x1;
```

#### Constant `MH_EXECUTE`

demand paged executable file

```rust
pub const MH_EXECUTE: u32 = 0x2;
```

#### Constant `MH_FVMLIB`

fixed VM shared library file

```rust
pub const MH_FVMLIB: u32 = 0x3;
```

#### Constant `MH_CORE`

core file

```rust
pub const MH_CORE: u32 = 0x4;
```

#### Constant `MH_PRELOAD`

preloaded executable file

```rust
pub const MH_PRELOAD: u32 = 0x5;
```

#### Constant `MH_DYLIB`

dynamically bound shared library

```rust
pub const MH_DYLIB: u32 = 0x6;
```

#### Constant `MH_DYLINKER`

dynamic link editor

```rust
pub const MH_DYLINKER: u32 = 0x7;
```

#### Constant `MH_BUNDLE`

dynamically bound bundle file

```rust
pub const MH_BUNDLE: u32 = 0x8;
```

#### Constant `MH_DYLIB_STUB`

shared library stub for static linking only, no section contents

```rust
pub const MH_DYLIB_STUB: u32 = 0x9;
```

#### Constant `MH_DSYM`

companion file with only debug sections

```rust
pub const MH_DSYM: u32 = 0xa;
```

#### Constant `MH_KEXT_BUNDLE`

x86_64 kexts

```rust
pub const MH_KEXT_BUNDLE: u32 = 0xb;
```

#### Constant `MH_FILESET`

set of mach-o's

```rust
pub const MH_FILESET: u32 = 0xc;
```

#### Constant `MH_NOUNDEFS`

the object file has no undefined references

```rust
pub const MH_NOUNDEFS: u32 = 0x1;
```

#### Constant `MH_INCRLINK`

the object file is the output of an incremental link against a base file and can't be link edited again

```rust
pub const MH_INCRLINK: u32 = 0x2;
```

#### Constant `MH_DYLDLINK`

the object file is input for the dynamic linker and can't be statically link edited again

```rust
pub const MH_DYLDLINK: u32 = 0x4;
```

#### Constant `MH_BINDATLOAD`

the object file's undefined references are bound by the dynamic linker when loaded.

```rust
pub const MH_BINDATLOAD: u32 = 0x8;
```

#### Constant `MH_PREBOUND`

the file has its dynamic undefined references prebound.

```rust
pub const MH_PREBOUND: u32 = 0x10;
```

#### Constant `MH_SPLIT_SEGS`

the file has its read-only and read-write segments split

```rust
pub const MH_SPLIT_SEGS: u32 = 0x20;
```

#### Constant `MH_LAZY_INIT`

the shared library init routine is to be run lazily via catching memory faults to its writeable segments (obsolete)

```rust
pub const MH_LAZY_INIT: u32 = 0x40;
```

#### Constant `MH_TWOLEVEL`

the image is using two-level name space bindings

```rust
pub const MH_TWOLEVEL: u32 = 0x80;
```

#### Constant `MH_FORCE_FLAT`

the executable is forcing all images to use flat name space bindings

```rust
pub const MH_FORCE_FLAT: u32 = 0x100;
```

#### Constant `MH_NOMULTIDEFS`

this umbrella guarantees no multiple definitions of symbols in its sub-images so the two-level namespace hints can always be used.

```rust
pub const MH_NOMULTIDEFS: u32 = 0x200;
```

#### Constant `MH_NOFIXPREBINDING`

do not have dyld notify the prebinding agent about this executable

```rust
pub const MH_NOFIXPREBINDING: u32 = 0x400;
```

#### Constant `MH_PREBINDABLE`

the binary is not prebound but can have its prebinding redone. only used when MH_PREBOUND is not set.

```rust
pub const MH_PREBINDABLE: u32 = 0x800;
```

#### Constant `MH_ALLMODSBOUND`

indicates that this binary binds to all two-level namespace modules of its dependent libraries. only used when MH_PREBINDABLE and MH_TWOLEVEL are both set.

```rust
pub const MH_ALLMODSBOUND: u32 = 0x1000;
```

#### Constant `MH_SUBSECTIONS_VIA_SYMBOLS`

safe to divide up the sections into sub-sections via symbols for dead code stripping

```rust
pub const MH_SUBSECTIONS_VIA_SYMBOLS: u32 = 0x2000;
```

#### Constant `MH_CANONICAL`

the binary has been canonicalized via the unprebind operation

```rust
pub const MH_CANONICAL: u32 = 0x4000;
```

#### Constant `MH_WEAK_DEFINES`

the final linked image contains external weak symbols

```rust
pub const MH_WEAK_DEFINES: u32 = 0x8000;
```

#### Constant `MH_BINDS_TO_WEAK`

the final linked image uses weak symbols

```rust
pub const MH_BINDS_TO_WEAK: u32 = 0x10000;
```

#### Constant `MH_ALLOW_STACK_EXECUTION`

When this bit is set, all stacks in the task will be given stack execution privilege.  Only used in MH_EXECUTE filetypes.

```rust
pub const MH_ALLOW_STACK_EXECUTION: u32 = 0x20000;
```

#### Constant `MH_ROOT_SAFE`

When this bit is set, the binary declares it is safe for use in processes with uid zero

```rust
pub const MH_ROOT_SAFE: u32 = 0x40000;
```

#### Constant `MH_SETUID_SAFE`

When this bit is set, the binary declares it is safe for use in processes when issetugid() is true

```rust
pub const MH_SETUID_SAFE: u32 = 0x80000;
```

#### Constant `MH_NO_REEXPORTED_DYLIBS`

When this bit is set on a dylib, the static linker does not need to examine dependent dylibs to see if any are re-exported

```rust
pub const MH_NO_REEXPORTED_DYLIBS: u32 = 0x10_0000;
```

#### Constant `MH_PIE`

When this bit is set, the OS will load the main executable at a random address.  Only used in MH_EXECUTE filetypes.

```rust
pub const MH_PIE: u32 = 0x20_0000;
```

#### Constant `MH_DEAD_STRIPPABLE_DYLIB`

Only for use on dylibs.  When linking against a dylib that has this bit set, the static linker will automatically not create a LC_LOAD_DYLIB load command to the dylib if no symbols are being referenced from the dylib.

```rust
pub const MH_DEAD_STRIPPABLE_DYLIB: u32 = 0x40_0000;
```

#### Constant `MH_HAS_TLV_DESCRIPTORS`

Contains a section of type S_THREAD_LOCAL_VARIABLES

```rust
pub const MH_HAS_TLV_DESCRIPTORS: u32 = 0x80_0000;
```

#### Constant `MH_NO_HEAP_EXECUTION`

When this bit is set, the OS will run the main executable with a non-executable heap even on platforms (e.g. i386) that don't require it. Only used in MH_EXECUTE filetypes.

```rust
pub const MH_NO_HEAP_EXECUTION: u32 = 0x100_0000;
```

#### Constant `MH_APP_EXTENSION_SAFE`

The code was linked for use in an application extension.

```rust
pub const MH_APP_EXTENSION_SAFE: u32 = 0x0200_0000;
```

#### Constant `MH_NLIST_OUTOFSYNC_WITH_DYLDINFO`

The external symbols listed in the nlist symbol table do not include all the symbols listed in the dyld info.

```rust
pub const MH_NLIST_OUTOFSYNC_WITH_DYLDINFO: u32 = 0x0400_0000;
```

#### Constant `MH_SIM_SUPPORT`

Allow LC_MIN_VERSION_MACOS and LC_BUILD_VERSION load commands with
the platforms macOS, iOSMac, iOSSimulator, tvOSSimulator and watchOSSimulator.

```rust
pub const MH_SIM_SUPPORT: u32 = 0x0800_0000;
```

#### Constant `MH_DYLIB_IN_CACHE`

Only for use on dylibs. When this bit is set, the dylib is part of the dyld
shared cache, rather than loose in the filesystem.

```rust
pub const MH_DYLIB_IN_CACHE: u32 = 0x8000_0000;
```

#### Constant `LC_REQ_DYLD`

```rust
pub const LC_REQ_DYLD: u32 = 0x8000_0000;
```

#### Constant `LC_SEGMENT`

segment of this file to be mapped

```rust
pub const LC_SEGMENT: u32 = 0x1;
```

#### Constant `LC_SYMTAB`

link-edit stab symbol table info

```rust
pub const LC_SYMTAB: u32 = 0x2;
```

#### Constant `LC_SYMSEG`

link-edit gdb symbol table info (obsolete)

```rust
pub const LC_SYMSEG: u32 = 0x3;
```

#### Constant `LC_THREAD`

thread

```rust
pub const LC_THREAD: u32 = 0x4;
```

#### Constant `LC_UNIXTHREAD`

unix thread (includes a stack)

```rust
pub const LC_UNIXTHREAD: u32 = 0x5;
```

#### Constant `LC_LOADFVMLIB`

load a specified fixed VM shared library

```rust
pub const LC_LOADFVMLIB: u32 = 0x6;
```

#### Constant `LC_IDFVMLIB`

fixed VM shared library identification

```rust
pub const LC_IDFVMLIB: u32 = 0x7;
```

#### Constant `LC_IDENT`

object identification info (obsolete)

```rust
pub const LC_IDENT: u32 = 0x8;
```

#### Constant `LC_FVMFILE`

fixed VM file inclusion (internal use)

```rust
pub const LC_FVMFILE: u32 = 0x9;
```

#### Constant `LC_PREPAGE`

prepage command (internal use)

```rust
pub const LC_PREPAGE: u32 = 0xa;
```

#### Constant `LC_DYSYMTAB`

dynamic link-edit symbol table info

```rust
pub const LC_DYSYMTAB: u32 = 0xb;
```

#### Constant `LC_LOAD_DYLIB`

load a dynamically linked shared library

```rust
pub const LC_LOAD_DYLIB: u32 = 0xc;
```

#### Constant `LC_ID_DYLIB`

dynamically linked shared lib ident

```rust
pub const LC_ID_DYLIB: u32 = 0xd;
```

#### Constant `LC_LOAD_DYLINKER`

load a dynamic linker

```rust
pub const LC_LOAD_DYLINKER: u32 = 0xe;
```

#### Constant `LC_ID_DYLINKER`

dynamic linker identification

```rust
pub const LC_ID_DYLINKER: u32 = 0xf;
```

#### Constant `LC_PREBOUND_DYLIB`

modules prebound for a dynamically linked shared library

```rust
pub const LC_PREBOUND_DYLIB: u32 = 0x10;
```

#### Constant `LC_ROUTINES`

image routines

```rust
pub const LC_ROUTINES: u32 = 0x11;
```

#### Constant `LC_SUB_FRAMEWORK`

sub framework

```rust
pub const LC_SUB_FRAMEWORK: u32 = 0x12;
```

#### Constant `LC_SUB_UMBRELLA`

sub umbrella

```rust
pub const LC_SUB_UMBRELLA: u32 = 0x13;
```

#### Constant `LC_SUB_CLIENT`

sub client

```rust
pub const LC_SUB_CLIENT: u32 = 0x14;
```

#### Constant `LC_SUB_LIBRARY`

sub library

```rust
pub const LC_SUB_LIBRARY: u32 = 0x15;
```

#### Constant `LC_TWOLEVEL_HINTS`

two-level namespace lookup hints

```rust
pub const LC_TWOLEVEL_HINTS: u32 = 0x16;
```

#### Constant `LC_PREBIND_CKSUM`

prebind checksum

```rust
pub const LC_PREBIND_CKSUM: u32 = 0x17;
```

#### Constant `LC_LOAD_WEAK_DYLIB`

load a dynamically linked shared library that is allowed to be missing
(all symbols are weak imported).

```rust
pub const LC_LOAD_WEAK_DYLIB: u32 = _;
```

#### Constant `LC_SEGMENT_64`

64-bit segment of this file to be mapped

```rust
pub const LC_SEGMENT_64: u32 = 0x19;
```

#### Constant `LC_ROUTINES_64`

64-bit image routines

```rust
pub const LC_ROUTINES_64: u32 = 0x1a;
```

#### Constant `LC_UUID`

the uuid

```rust
pub const LC_UUID: u32 = 0x1b;
```

#### Constant `LC_RPATH`

runpath additions

```rust
pub const LC_RPATH: u32 = _;
```

#### Constant `LC_CODE_SIGNATURE`

local of code signature

```rust
pub const LC_CODE_SIGNATURE: u32 = 0x1d;
```

#### Constant `LC_SEGMENT_SPLIT_INFO`

local of info to split segments

```rust
pub const LC_SEGMENT_SPLIT_INFO: u32 = 0x1e;
```

#### Constant `LC_REEXPORT_DYLIB`

load and re-export dylib

```rust
pub const LC_REEXPORT_DYLIB: u32 = _;
```

#### Constant `LC_LAZY_LOAD_DYLIB`

delay load of dylib until first use

```rust
pub const LC_LAZY_LOAD_DYLIB: u32 = 0x20;
```

#### Constant `LC_ENCRYPTION_INFO`

encrypted segment information

```rust
pub const LC_ENCRYPTION_INFO: u32 = 0x21;
```

#### Constant `LC_DYLD_INFO`

compressed dyld information

```rust
pub const LC_DYLD_INFO: u32 = 0x22;
```

#### Constant `LC_DYLD_INFO_ONLY`

compressed dyld information only

```rust
pub const LC_DYLD_INFO_ONLY: u32 = _;
```

#### Constant `LC_LOAD_UPWARD_DYLIB`

load upward dylib

```rust
pub const LC_LOAD_UPWARD_DYLIB: u32 = _;
```

#### Constant `LC_VERSION_MIN_MACOSX`

build for MacOSX min OS version

```rust
pub const LC_VERSION_MIN_MACOSX: u32 = 0x24;
```

#### Constant `LC_VERSION_MIN_IPHONEOS`

build for iPhoneOS min OS version

```rust
pub const LC_VERSION_MIN_IPHONEOS: u32 = 0x25;
```

#### Constant `LC_FUNCTION_STARTS`

compressed table of function start addresses

```rust
pub const LC_FUNCTION_STARTS: u32 = 0x26;
```

#### Constant `LC_DYLD_ENVIRONMENT`

string for dyld to treat like environment variable

```rust
pub const LC_DYLD_ENVIRONMENT: u32 = 0x27;
```

#### Constant `LC_MAIN`

replacement for LC_UNIXTHREAD

```rust
pub const LC_MAIN: u32 = _;
```

#### Constant `LC_DATA_IN_CODE`

table of non-instructions in __text

```rust
pub const LC_DATA_IN_CODE: u32 = 0x29;
```

#### Constant `LC_SOURCE_VERSION`

source version used to build binary

```rust
pub const LC_SOURCE_VERSION: u32 = 0x2A;
```

#### Constant `LC_DYLIB_CODE_SIGN_DRS`

Code signing DRs copied from linked dylibs

```rust
pub const LC_DYLIB_CODE_SIGN_DRS: u32 = 0x2B;
```

#### Constant `LC_ENCRYPTION_INFO_64`

64-bit encrypted segment information

```rust
pub const LC_ENCRYPTION_INFO_64: u32 = 0x2C;
```

#### Constant `LC_LINKER_OPTION`

linker options in MH_OBJECT files

```rust
pub const LC_LINKER_OPTION: u32 = 0x2D;
```

#### Constant `LC_LINKER_OPTIMIZATION_HINT`

optimization hints in MH_OBJECT files

```rust
pub const LC_LINKER_OPTIMIZATION_HINT: u32 = 0x2E;
```

#### Constant `LC_VERSION_MIN_TVOS`

build for AppleTV min OS version

```rust
pub const LC_VERSION_MIN_TVOS: u32 = 0x2F;
```

#### Constant `LC_VERSION_MIN_WATCHOS`

build for Watch min OS version

```rust
pub const LC_VERSION_MIN_WATCHOS: u32 = 0x30;
```

#### Constant `LC_NOTE`

arbitrary data included within a Mach-O file

```rust
pub const LC_NOTE: u32 = 0x31;
```

#### Constant `LC_BUILD_VERSION`

build for platform min OS version

```rust
pub const LC_BUILD_VERSION: u32 = 0x32;
```

#### Constant `LC_DYLD_EXPORTS_TRIE`

used with `LinkeditDataCommand`, payload is trie

```rust
pub const LC_DYLD_EXPORTS_TRIE: u32 = _;
```

#### Constant `LC_DYLD_CHAINED_FIXUPS`

used with `LinkeditDataCommand`

```rust
pub const LC_DYLD_CHAINED_FIXUPS: u32 = _;
```

#### Constant `LC_FILESET_ENTRY`

used with `FilesetEntryCommand`

```rust
pub const LC_FILESET_ENTRY: u32 = _;
```

#### Constant `SG_HIGHVM`

the file contents for this segment is for the high part of the VM space, the low part is zero filled (for stacks in core files)

```rust
pub const SG_HIGHVM: u32 = 0x1;
```

#### Constant `SG_FVMLIB`

this segment is the VM that is allocated by a fixed VM library, for overlap checking in the link editor

```rust
pub const SG_FVMLIB: u32 = 0x2;
```

#### Constant `SG_NORELOC`

this segment has nothing that was relocated in it and nothing relocated to it, that is it maybe safely replaced without relocation

```rust
pub const SG_NORELOC: u32 = 0x4;
```

#### Constant `SG_PROTECTED_VERSION_1`

This segment is protected.  If the segment starts at file offset 0, the first page of the segment is not protected.  All other pages of the segment are protected.

```rust
pub const SG_PROTECTED_VERSION_1: u32 = 0x8;
```

#### Constant `SG_READ_ONLY`

This segment is made read-only after fixups

```rust
pub const SG_READ_ONLY: u32 = 0x10;
```

#### Constant `SECTION_TYPE`

256 section types

```rust
pub const SECTION_TYPE: u32 = 0x0000_00ff;
```

#### Constant `SECTION_ATTRIBUTES`

24 section attributes

```rust
pub const SECTION_ATTRIBUTES: u32 = 0xffff_ff00;
```

#### Constant `S_REGULAR`

regular section

```rust
pub const S_REGULAR: u32 = 0x0;
```

#### Constant `S_ZEROFILL`

zero fill on demand section

```rust
pub const S_ZEROFILL: u32 = 0x1;
```

#### Constant `S_CSTRING_LITERALS`

section with only literal C strings

```rust
pub const S_CSTRING_LITERALS: u32 = 0x2;
```

#### Constant `S_4BYTE_LITERALS`

section with only 4 byte literals

```rust
pub const S_4BYTE_LITERALS: u32 = 0x3;
```

#### Constant `S_8BYTE_LITERALS`

section with only 8 byte literals

```rust
pub const S_8BYTE_LITERALS: u32 = 0x4;
```

#### Constant `S_LITERAL_POINTERS`

section with only pointers to literals

```rust
pub const S_LITERAL_POINTERS: u32 = 0x5;
```

#### Constant `S_NON_LAZY_SYMBOL_POINTERS`

section with only non-lazy symbol pointers

```rust
pub const S_NON_LAZY_SYMBOL_POINTERS: u32 = 0x6;
```

#### Constant `S_LAZY_SYMBOL_POINTERS`

section with only lazy symbol pointers

```rust
pub const S_LAZY_SYMBOL_POINTERS: u32 = 0x7;
```

#### Constant `S_SYMBOL_STUBS`

section with only symbol stubs, byte size of stub in the reserved2 field

```rust
pub const S_SYMBOL_STUBS: u32 = 0x8;
```

#### Constant `S_MOD_INIT_FUNC_POINTERS`

section with only function pointers for initialization

```rust
pub const S_MOD_INIT_FUNC_POINTERS: u32 = 0x9;
```

#### Constant `S_MOD_TERM_FUNC_POINTERS`

section with only function pointers for termination

```rust
pub const S_MOD_TERM_FUNC_POINTERS: u32 = 0xa;
```

#### Constant `S_COALESCED`

section contains symbols that are to be coalesced

```rust
pub const S_COALESCED: u32 = 0xb;
```

#### Constant `S_GB_ZEROFILL`

zero fill on demand section (that can be larger than 4 gigabytes)

```rust
pub const S_GB_ZEROFILL: u32 = 0xc;
```

#### Constant `S_INTERPOSING`

section with only pairs of function pointers for interposing

```rust
pub const S_INTERPOSING: u32 = 0xd;
```

#### Constant `S_16BYTE_LITERALS`

section with only 16 byte literals

```rust
pub const S_16BYTE_LITERALS: u32 = 0xe;
```

#### Constant `S_DTRACE_DOF`

section contains DTrace Object Format

```rust
pub const S_DTRACE_DOF: u32 = 0xf;
```

#### Constant `S_LAZY_DYLIB_SYMBOL_POINTERS`

section with only lazy symbol pointers to lazy loaded dylibs

```rust
pub const S_LAZY_DYLIB_SYMBOL_POINTERS: u32 = 0x10;
```

#### Constant `S_THREAD_LOCAL_REGULAR`

template of initial values for TLVs

```rust
pub const S_THREAD_LOCAL_REGULAR: u32 = 0x11;
```

#### Constant `S_THREAD_LOCAL_ZEROFILL`

template of initial values for TLVs

```rust
pub const S_THREAD_LOCAL_ZEROFILL: u32 = 0x12;
```

#### Constant `S_THREAD_LOCAL_VARIABLES`

TLV descriptors

```rust
pub const S_THREAD_LOCAL_VARIABLES: u32 = 0x13;
```

#### Constant `S_THREAD_LOCAL_VARIABLE_POINTERS`

pointers to TLV descriptors

```rust
pub const S_THREAD_LOCAL_VARIABLE_POINTERS: u32 = 0x14;
```

#### Constant `S_THREAD_LOCAL_INIT_FUNCTION_POINTERS`

functions to call to initialize TLV values

```rust
pub const S_THREAD_LOCAL_INIT_FUNCTION_POINTERS: u32 = 0x15;
```

#### Constant `S_INIT_FUNC_OFFSETS`

32-bit offsets to initializers

```rust
pub const S_INIT_FUNC_OFFSETS: u32 = 0x16;
```

#### Constant `SECTION_ATTRIBUTES_USR`

User setable attributes

```rust
pub const SECTION_ATTRIBUTES_USR: u32 = 0xff00_0000;
```

#### Constant `S_ATTR_PURE_INSTRUCTIONS`

section contains only true machine instructions

```rust
pub const S_ATTR_PURE_INSTRUCTIONS: u32 = 0x8000_0000;
```

#### Constant `S_ATTR_NO_TOC`

section contains coalesced symbols that are not to be in a ranlib table of contents

```rust
pub const S_ATTR_NO_TOC: u32 = 0x4000_0000;
```

#### Constant `S_ATTR_STRIP_STATIC_SYMS`

ok to strip static symbols in this section in files with the MH_DYLDLINK flag

```rust
pub const S_ATTR_STRIP_STATIC_SYMS: u32 = 0x2000_0000;
```

#### Constant `S_ATTR_NO_DEAD_STRIP`

no dead stripping

```rust
pub const S_ATTR_NO_DEAD_STRIP: u32 = 0x1000_0000;
```

#### Constant `S_ATTR_LIVE_SUPPORT`

blocks are live if they reference live blocks

```rust
pub const S_ATTR_LIVE_SUPPORT: u32 = 0x0800_0000;
```

#### Constant `S_ATTR_SELF_MODIFYING_CODE`

Used with i386 code stubs written on by dyld

```rust
pub const S_ATTR_SELF_MODIFYING_CODE: u32 = 0x0400_0000;
```

#### Constant `S_ATTR_DEBUG`

a debug section

```rust
pub const S_ATTR_DEBUG: u32 = 0x0200_0000;
```

#### Constant `SECTION_ATTRIBUTES_SYS`

system setable attributes

```rust
pub const SECTION_ATTRIBUTES_SYS: u32 = 0x00ff_ff00;
```

#### Constant `S_ATTR_SOME_INSTRUCTIONS`

section contains some machine instructions

```rust
pub const S_ATTR_SOME_INSTRUCTIONS: u32 = 0x0000_0400;
```

#### Constant `S_ATTR_EXT_RELOC`

section has external relocation entries

```rust
pub const S_ATTR_EXT_RELOC: u32 = 0x0000_0200;
```

#### Constant `S_ATTR_LOC_RELOC`

section has local relocation entries

```rust
pub const S_ATTR_LOC_RELOC: u32 = 0x0000_0100;
```

#### Constant `SEG_PAGEZERO`

the pagezero segment which has no protections and catches NULL references for MH_EXECUTE files

```rust
pub const SEG_PAGEZERO: &str = "__PAGEZERO";
```

#### Constant `SEG_TEXT`

the tradition UNIX text segment

```rust
pub const SEG_TEXT: &str = "__TEXT";
```

#### Constant `SECT_TEXT`

the real text part of the text section no headers, and no padding

```rust
pub const SECT_TEXT: &str = "__text";
```

#### Constant `SECT_FVMLIB_INIT0`

the fvmlib initialization section

```rust
pub const SECT_FVMLIB_INIT0: &str = "__fvmlib_init0";
```

#### Constant `SECT_FVMLIB_INIT1`

the section following the fvmlib initialization section

```rust
pub const SECT_FVMLIB_INIT1: &str = "__fvmlib_init1";
```

#### Constant `SEG_DATA`

the tradition UNIX data segment

```rust
pub const SEG_DATA: &str = "__DATA";
```

#### Constant `SECT_DATA`

the real initialized data section no padding, no bss overlap

```rust
pub const SECT_DATA: &str = "__data";
```

#### Constant `SECT_BSS`

the real uninitialized data section no padding

```rust
pub const SECT_BSS: &str = "__bss";
```

#### Constant `SECT_COMMON`

the section common symbols are allocated in by the link editor

```rust
pub const SECT_COMMON: &str = "__common";
```

#### Constant `SEG_OBJC`

objective-C runtime segment

```rust
pub const SEG_OBJC: &str = "__OBJC";
```

#### Constant `SECT_OBJC_SYMBOLS`

symbol table

```rust
pub const SECT_OBJC_SYMBOLS: &str = "__symbol_table";
```

#### Constant `SECT_OBJC_MODULES`

module information

```rust
pub const SECT_OBJC_MODULES: &str = "__module_info";
```

#### Constant `SECT_OBJC_STRINGS`

string table

```rust
pub const SECT_OBJC_STRINGS: &str = "__selector_strs";
```

#### Constant `SECT_OBJC_REFS`

string table

```rust
pub const SECT_OBJC_REFS: &str = "__selector_refs";
```

#### Constant `SEG_ICON`

the icon segment

```rust
pub const SEG_ICON: &str = "__ICON";
```

#### Constant `SECT_ICON_HEADER`

the icon headers

```rust
pub const SECT_ICON_HEADER: &str = "__header";
```

#### Constant `SECT_ICON_TIFF`

the icons in tiff format

```rust
pub const SECT_ICON_TIFF: &str = "__tiff";
```

#### Constant `SEG_LINKEDIT`

the segment containing all structs created and maintained by the link editor.  Created with -seglinkedit option to ld(1) for MH_EXECUTE and FVMLIB file types only

```rust
pub const SEG_LINKEDIT: &str = "__LINKEDIT";
```

#### Constant `SEG_LINKINFO`

the segment overlapping with linkedit containing linking information

```rust
pub const SEG_LINKINFO: &str = "__LINKINFO";
```

#### Constant `SEG_UNIXSTACK`

the unix stack segment

```rust
pub const SEG_UNIXSTACK: &str = "__UNIXSTACK";
```

#### Constant `SEG_IMPORT`

the segment for the self (dyld) modifying code stubs that has read, write and execute permissions

```rust
pub const SEG_IMPORT: &str = "__IMPORT";
```

#### Constant `INDIRECT_SYMBOL_LOCAL`

```rust
pub const INDIRECT_SYMBOL_LOCAL: u32 = 0x8000_0000;
```

#### Constant `INDIRECT_SYMBOL_ABS`

```rust
pub const INDIRECT_SYMBOL_ABS: u32 = 0x4000_0000;
```

#### Constant `PLATFORM_MACOS`

```rust
pub const PLATFORM_MACOS: u32 = 1;
```

#### Constant `PLATFORM_IOS`

```rust
pub const PLATFORM_IOS: u32 = 2;
```

#### Constant `PLATFORM_TVOS`

```rust
pub const PLATFORM_TVOS: u32 = 3;
```

#### Constant `PLATFORM_WATCHOS`

```rust
pub const PLATFORM_WATCHOS: u32 = 4;
```

#### Constant `PLATFORM_BRIDGEOS`

```rust
pub const PLATFORM_BRIDGEOS: u32 = 5;
```

#### Constant `PLATFORM_MACCATALYST`

```rust
pub const PLATFORM_MACCATALYST: u32 = 6;
```

#### Constant `PLATFORM_IOSSIMULATOR`

```rust
pub const PLATFORM_IOSSIMULATOR: u32 = 7;
```

#### Constant `PLATFORM_TVOSSIMULATOR`

```rust
pub const PLATFORM_TVOSSIMULATOR: u32 = 8;
```

#### Constant `PLATFORM_WATCHOSSIMULATOR`

```rust
pub const PLATFORM_WATCHOSSIMULATOR: u32 = 9;
```

#### Constant `PLATFORM_DRIVERKIT`

```rust
pub const PLATFORM_DRIVERKIT: u32 = 10;
```

#### Constant `PLATFORM_XROS`

```rust
pub const PLATFORM_XROS: u32 = 11;
```

#### Constant `PLATFORM_XROSSIMULATOR`

```rust
pub const PLATFORM_XROSSIMULATOR: u32 = 12;
```

#### Constant `TOOL_CLANG`

```rust
pub const TOOL_CLANG: u32 = 1;
```

#### Constant `TOOL_SWIFT`

```rust
pub const TOOL_SWIFT: u32 = 2;
```

#### Constant `TOOL_LD`

```rust
pub const TOOL_LD: u32 = 3;
```

#### Constant `REBASE_TYPE_POINTER`

```rust
pub const REBASE_TYPE_POINTER: u8 = 1;
```

#### Constant `REBASE_TYPE_TEXT_ABSOLUTE32`

```rust
pub const REBASE_TYPE_TEXT_ABSOLUTE32: u8 = 2;
```

#### Constant `REBASE_TYPE_TEXT_PCREL32`

```rust
pub const REBASE_TYPE_TEXT_PCREL32: u8 = 3;
```

#### Constant `REBASE_OPCODE_MASK`

```rust
pub const REBASE_OPCODE_MASK: u8 = 0xF0;
```

#### Constant `REBASE_IMMEDIATE_MASK`

```rust
pub const REBASE_IMMEDIATE_MASK: u8 = 0x0F;
```

#### Constant `REBASE_OPCODE_DONE`

```rust
pub const REBASE_OPCODE_DONE: u8 = 0x00;
```

#### Constant `REBASE_OPCODE_SET_TYPE_IMM`

```rust
pub const REBASE_OPCODE_SET_TYPE_IMM: u8 = 0x10;
```

#### Constant `REBASE_OPCODE_SET_SEGMENT_AND_OFFSET_ULEB`

```rust
pub const REBASE_OPCODE_SET_SEGMENT_AND_OFFSET_ULEB: u8 = 0x20;
```

#### Constant `REBASE_OPCODE_ADD_ADDR_ULEB`

```rust
pub const REBASE_OPCODE_ADD_ADDR_ULEB: u8 = 0x30;
```

#### Constant `REBASE_OPCODE_ADD_ADDR_IMM_SCALED`

```rust
pub const REBASE_OPCODE_ADD_ADDR_IMM_SCALED: u8 = 0x40;
```

#### Constant `REBASE_OPCODE_DO_REBASE_IMM_TIMES`

```rust
pub const REBASE_OPCODE_DO_REBASE_IMM_TIMES: u8 = 0x50;
```

#### Constant `REBASE_OPCODE_DO_REBASE_ULEB_TIMES`

```rust
pub const REBASE_OPCODE_DO_REBASE_ULEB_TIMES: u8 = 0x60;
```

#### Constant `REBASE_OPCODE_DO_REBASE_ADD_ADDR_ULEB`

```rust
pub const REBASE_OPCODE_DO_REBASE_ADD_ADDR_ULEB: u8 = 0x70;
```

#### Constant `REBASE_OPCODE_DO_REBASE_ULEB_TIMES_SKIPPING_ULEB`

```rust
pub const REBASE_OPCODE_DO_REBASE_ULEB_TIMES_SKIPPING_ULEB: u8 = 0x80;
```

#### Constant `BIND_TYPE_POINTER`

```rust
pub const BIND_TYPE_POINTER: u8 = 1;
```

#### Constant `BIND_TYPE_TEXT_ABSOLUTE32`

```rust
pub const BIND_TYPE_TEXT_ABSOLUTE32: u8 = 2;
```

#### Constant `BIND_TYPE_TEXT_PCREL32`

```rust
pub const BIND_TYPE_TEXT_PCREL32: u8 = 3;
```

#### Constant `BIND_SPECIAL_DYLIB_SELF`

```rust
pub const BIND_SPECIAL_DYLIB_SELF: i8 = 0;
```

#### Constant `BIND_SPECIAL_DYLIB_MAIN_EXECUTABLE`

```rust
pub const BIND_SPECIAL_DYLIB_MAIN_EXECUTABLE: i8 = -1;
```

#### Constant `BIND_SPECIAL_DYLIB_FLAT_LOOKUP`

```rust
pub const BIND_SPECIAL_DYLIB_FLAT_LOOKUP: i8 = -2;
```

#### Constant `BIND_SPECIAL_DYLIB_WEAK_LOOKUP`

```rust
pub const BIND_SPECIAL_DYLIB_WEAK_LOOKUP: i8 = -3;
```

#### Constant `BIND_SYMBOL_FLAGS_WEAK_IMPORT`

```rust
pub const BIND_SYMBOL_FLAGS_WEAK_IMPORT: u8 = 0x1;
```

#### Constant `BIND_SYMBOL_FLAGS_NON_WEAK_DEFINITION`

```rust
pub const BIND_SYMBOL_FLAGS_NON_WEAK_DEFINITION: u8 = 0x8;
```

#### Constant `BIND_OPCODE_MASK`

```rust
pub const BIND_OPCODE_MASK: u8 = 0xF0;
```

#### Constant `BIND_IMMEDIATE_MASK`

```rust
pub const BIND_IMMEDIATE_MASK: u8 = 0x0F;
```

#### Constant `BIND_OPCODE_DONE`

```rust
pub const BIND_OPCODE_DONE: u8 = 0x00;
```

#### Constant `BIND_OPCODE_SET_DYLIB_ORDINAL_IMM`

```rust
pub const BIND_OPCODE_SET_DYLIB_ORDINAL_IMM: u8 = 0x10;
```

#### Constant `BIND_OPCODE_SET_DYLIB_ORDINAL_ULEB`

```rust
pub const BIND_OPCODE_SET_DYLIB_ORDINAL_ULEB: u8 = 0x20;
```

#### Constant `BIND_OPCODE_SET_DYLIB_SPECIAL_IMM`

```rust
pub const BIND_OPCODE_SET_DYLIB_SPECIAL_IMM: u8 = 0x30;
```

#### Constant `BIND_OPCODE_SET_SYMBOL_TRAILING_FLAGS_IMM`

```rust
pub const BIND_OPCODE_SET_SYMBOL_TRAILING_FLAGS_IMM: u8 = 0x40;
```

#### Constant `BIND_OPCODE_SET_TYPE_IMM`

```rust
pub const BIND_OPCODE_SET_TYPE_IMM: u8 = 0x50;
```

#### Constant `BIND_OPCODE_SET_ADDEND_SLEB`

```rust
pub const BIND_OPCODE_SET_ADDEND_SLEB: u8 = 0x60;
```

#### Constant `BIND_OPCODE_SET_SEGMENT_AND_OFFSET_ULEB`

```rust
pub const BIND_OPCODE_SET_SEGMENT_AND_OFFSET_ULEB: u8 = 0x70;
```

#### Constant `BIND_OPCODE_ADD_ADDR_ULEB`

```rust
pub const BIND_OPCODE_ADD_ADDR_ULEB: u8 = 0x80;
```

#### Constant `BIND_OPCODE_DO_BIND`

```rust
pub const BIND_OPCODE_DO_BIND: u8 = 0x90;
```

#### Constant `BIND_OPCODE_DO_BIND_ADD_ADDR_ULEB`

```rust
pub const BIND_OPCODE_DO_BIND_ADD_ADDR_ULEB: u8 = 0xA0;
```

#### Constant `BIND_OPCODE_DO_BIND_ADD_ADDR_IMM_SCALED`

```rust
pub const BIND_OPCODE_DO_BIND_ADD_ADDR_IMM_SCALED: u8 = 0xB0;
```

#### Constant `BIND_OPCODE_DO_BIND_ULEB_TIMES_SKIPPING_ULEB`

```rust
pub const BIND_OPCODE_DO_BIND_ULEB_TIMES_SKIPPING_ULEB: u8 = 0xC0;
```

#### Constant `BIND_OPCODE_THREADED`

```rust
pub const BIND_OPCODE_THREADED: u8 = 0xD0;
```

#### Constant `BIND_SUBOPCODE_THREADED_SET_BIND_ORDINAL_TABLE_SIZE_ULEB`

```rust
pub const BIND_SUBOPCODE_THREADED_SET_BIND_ORDINAL_TABLE_SIZE_ULEB: u8 = 0x00;
```

#### Constant `BIND_SUBOPCODE_THREADED_APPLY`

```rust
pub const BIND_SUBOPCODE_THREADED_APPLY: u8 = 0x01;
```

#### Constant `EXPORT_SYMBOL_FLAGS_KIND_MASK`

```rust
pub const EXPORT_SYMBOL_FLAGS_KIND_MASK: u32 = 0x03;
```

#### Constant `EXPORT_SYMBOL_FLAGS_KIND_REGULAR`

```rust
pub const EXPORT_SYMBOL_FLAGS_KIND_REGULAR: u32 = 0x00;
```

#### Constant `EXPORT_SYMBOL_FLAGS_KIND_THREAD_LOCAL`

```rust
pub const EXPORT_SYMBOL_FLAGS_KIND_THREAD_LOCAL: u32 = 0x01;
```

#### Constant `EXPORT_SYMBOL_FLAGS_KIND_ABSOLUTE`

```rust
pub const EXPORT_SYMBOL_FLAGS_KIND_ABSOLUTE: u32 = 0x02;
```

#### Constant `EXPORT_SYMBOL_FLAGS_WEAK_DEFINITION`

```rust
pub const EXPORT_SYMBOL_FLAGS_WEAK_DEFINITION: u32 = 0x04;
```

#### Constant `EXPORT_SYMBOL_FLAGS_REEXPORT`

```rust
pub const EXPORT_SYMBOL_FLAGS_REEXPORT: u32 = 0x08;
```

#### Constant `EXPORT_SYMBOL_FLAGS_STUB_AND_RESOLVER`

```rust
pub const EXPORT_SYMBOL_FLAGS_STUB_AND_RESOLVER: u32 = 0x10;
```

#### Constant `DICE_KIND_DATA`

```rust
pub const DICE_KIND_DATA: u32 = 0x0001;
```

#### Constant `DICE_KIND_JUMP_TABLE8`

```rust
pub const DICE_KIND_JUMP_TABLE8: u32 = 0x0002;
```

#### Constant `DICE_KIND_JUMP_TABLE16`

```rust
pub const DICE_KIND_JUMP_TABLE16: u32 = 0x0003;
```

#### Constant `DICE_KIND_JUMP_TABLE32`

```rust
pub const DICE_KIND_JUMP_TABLE32: u32 = 0x0004;
```

#### Constant `DICE_KIND_ABS_JUMP_TABLE32`

```rust
pub const DICE_KIND_ABS_JUMP_TABLE32: u32 = 0x0005;
```

#### Constant `N_STAB`

if any of these bits set, a symbolic debugging entry

```rust
pub const N_STAB: u8 = 0xe0;
```

#### Constant `N_PEXT`

private external symbol bit

```rust
pub const N_PEXT: u8 = 0x10;
```

#### Constant `N_TYPE`

mask for the type bits

```rust
pub const N_TYPE: u8 = 0x0e;
```

#### Constant `N_EXT`

external symbol bit, set for external symbols

```rust
pub const N_EXT: u8 = 0x01;
```

#### Constant `N_UNDF`

undefined, n_sect == NO_SECT

```rust
pub const N_UNDF: u8 = 0x0;
```

#### Constant `N_ABS`

absolute, n_sect == NO_SECT

```rust
pub const N_ABS: u8 = 0x2;
```

#### Constant `N_SECT`

defined in section number n_sect

```rust
pub const N_SECT: u8 = 0xe;
```

#### Constant `N_PBUD`

prebound undefined (defined in a dylib)

```rust
pub const N_PBUD: u8 = 0xc;
```

#### Constant `N_INDR`

indirect

```rust
pub const N_INDR: u8 = 0xa;
```

#### Constant `NO_SECT`

symbol is not in any section

```rust
pub const NO_SECT: u8 = 0;
```

#### Constant `MAX_SECT`

1 thru 255 inclusive

```rust
pub const MAX_SECT: u8 = 255;
```

#### Constant `REFERENCE_TYPE`

```rust
pub const REFERENCE_TYPE: u16 = 0x7;
```

#### Constant `REFERENCE_FLAG_UNDEFINED_NON_LAZY`

```rust
pub const REFERENCE_FLAG_UNDEFINED_NON_LAZY: u16 = 0;
```

#### Constant `REFERENCE_FLAG_UNDEFINED_LAZY`

```rust
pub const REFERENCE_FLAG_UNDEFINED_LAZY: u16 = 1;
```

#### Constant `REFERENCE_FLAG_DEFINED`

```rust
pub const REFERENCE_FLAG_DEFINED: u16 = 2;
```

#### Constant `REFERENCE_FLAG_PRIVATE_DEFINED`

```rust
pub const REFERENCE_FLAG_PRIVATE_DEFINED: u16 = 3;
```

#### Constant `REFERENCE_FLAG_PRIVATE_UNDEFINED_NON_LAZY`

```rust
pub const REFERENCE_FLAG_PRIVATE_UNDEFINED_NON_LAZY: u16 = 4;
```

#### Constant `REFERENCE_FLAG_PRIVATE_UNDEFINED_LAZY`

```rust
pub const REFERENCE_FLAG_PRIVATE_UNDEFINED_LAZY: u16 = 5;
```

#### Constant `REFERENCED_DYNAMICALLY`

```rust
pub const REFERENCED_DYNAMICALLY: u16 = 0x0010;
```

#### Constant `SELF_LIBRARY_ORDINAL`

```rust
pub const SELF_LIBRARY_ORDINAL: u8 = 0x0;
```

#### Constant `MAX_LIBRARY_ORDINAL`

```rust
pub const MAX_LIBRARY_ORDINAL: u8 = 0xfd;
```

#### Constant `DYNAMIC_LOOKUP_ORDINAL`

```rust
pub const DYNAMIC_LOOKUP_ORDINAL: u8 = 0xfe;
```

#### Constant `EXECUTABLE_ORDINAL`

```rust
pub const EXECUTABLE_ORDINAL: u8 = 0xff;
```

#### Constant `N_NO_DEAD_STRIP`

symbol is not to be dead stripped

```rust
pub const N_NO_DEAD_STRIP: u16 = 0x0020;
```

#### Constant `N_DESC_DISCARDED`

symbol is discarded

```rust
pub const N_DESC_DISCARDED: u16 = 0x0020;
```

#### Constant `N_WEAK_REF`

symbol is weak referenced

```rust
pub const N_WEAK_REF: u16 = 0x0040;
```

#### Constant `N_WEAK_DEF`

coalesced symbol is a weak definition

```rust
pub const N_WEAK_DEF: u16 = 0x0080;
```

#### Constant `N_REF_TO_WEAK`

reference to a weak symbol

```rust
pub const N_REF_TO_WEAK: u16 = 0x0080;
```

#### Constant `N_ARM_THUMB_DEF`

symbol is a Thumb function (ARM)

```rust
pub const N_ARM_THUMB_DEF: u16 = 0x0008;
```

#### Constant `N_SYMBOL_RESOLVER`

```rust
pub const N_SYMBOL_RESOLVER: u16 = 0x0100;
```

#### Constant `N_ALT_ENTRY`

```rust
pub const N_ALT_ENTRY: u16 = 0x0200;
```

#### Constant `N_GSYM`

global symbol: name,,NO_SECT,type,0

```rust
pub const N_GSYM: u8 = 0x20;
```

#### Constant `N_FNAME`

procedure name (f77 kludge): name,,NO_SECT,0,0

```rust
pub const N_FNAME: u8 = 0x22;
```

#### Constant `N_FUN`

procedure: name,,n_sect,linenumber,address

```rust
pub const N_FUN: u8 = 0x24;
```

#### Constant `N_STSYM`

static symbol: name,,n_sect,type,address

```rust
pub const N_STSYM: u8 = 0x26;
```

#### Constant `N_LCSYM`

.lcomm symbol: name,,n_sect,type,address

```rust
pub const N_LCSYM: u8 = 0x28;
```

#### Constant `N_BNSYM`

begin nsect sym: 0,,n_sect,0,address

```rust
pub const N_BNSYM: u8 = 0x2e;
```

#### Constant `N_AST`

AST file path: name,,NO_SECT,0,0

```rust
pub const N_AST: u8 = 0x32;
```

#### Constant `N_OPT`

emitted with gcc2_compiled and in gcc source

```rust
pub const N_OPT: u8 = 0x3c;
```

#### Constant `N_RSYM`

register sym: name,,NO_SECT,type,register

```rust
pub const N_RSYM: u8 = 0x40;
```

#### Constant `N_SLINE`

src line: 0,,n_sect,linenumber,address

```rust
pub const N_SLINE: u8 = 0x44;
```

#### Constant `N_ENSYM`

end nsect sym: 0,,n_sect,0,address

```rust
pub const N_ENSYM: u8 = 0x4e;
```

#### Constant `N_SSYM`

structure elt: name,,NO_SECT,type,struct_offset

```rust
pub const N_SSYM: u8 = 0x60;
```

#### Constant `N_SO`

source file name: name,,n_sect,0,address

```rust
pub const N_SO: u8 = 0x64;
```

#### Constant `N_OSO`

object file name: name,,0,0,st_mtime

```rust
pub const N_OSO: u8 = 0x66;
```

#### Constant `N_LSYM`

local sym: name,,NO_SECT,type,offset

```rust
pub const N_LSYM: u8 = 0x80;
```

#### Constant `N_BINCL`

include file beginning: name,,NO_SECT,0,sum

```rust
pub const N_BINCL: u8 = 0x82;
```

#### Constant `N_SOL`

#included file name: name,,n_sect,0,address

```rust
pub const N_SOL: u8 = 0x84;
```

#### Constant `N_PARAMS`

compiler parameters: name,,NO_SECT,0,0

```rust
pub const N_PARAMS: u8 = 0x86;
```

#### Constant `N_VERSION`

compiler version: name,,NO_SECT,0,0

```rust
pub const N_VERSION: u8 = 0x88;
```

#### Constant `N_OLEVEL`

compiler -O level: name,,NO_SECT,0,0

```rust
pub const N_OLEVEL: u8 = 0x8A;
```

#### Constant `N_PSYM`

parameter: name,,NO_SECT,type,offset

```rust
pub const N_PSYM: u8 = 0xa0;
```

#### Constant `N_EINCL`

include file end: name,,NO_SECT,0,0

```rust
pub const N_EINCL: u8 = 0xa2;
```

#### Constant `N_ENTRY`

alternate entry: name,,n_sect,linenumber,address

```rust
pub const N_ENTRY: u8 = 0xa4;
```

#### Constant `N_LBRAC`

left bracket: 0,,NO_SECT,nesting level,address

```rust
pub const N_LBRAC: u8 = 0xc0;
```

#### Constant `N_EXCL`

deleted include file: name,,NO_SECT,0,sum

```rust
pub const N_EXCL: u8 = 0xc2;
```

#### Constant `N_RBRAC`

right bracket: 0,,NO_SECT,nesting level,address

```rust
pub const N_RBRAC: u8 = 0xe0;
```

#### Constant `N_BCOMM`

begin common: name,,NO_SECT,0,0

```rust
pub const N_BCOMM: u8 = 0xe2;
```

#### Constant `N_ECOMM`

end common: name,,n_sect,0,0

```rust
pub const N_ECOMM: u8 = 0xe4;
```

#### Constant `N_ECOML`

end common (local name): 0,,n_sect,0,address

```rust
pub const N_ECOML: u8 = 0xe8;
```

#### Constant `N_LENG`

second stab entry with length information

```rust
pub const N_LENG: u8 = 0xfe;
```

#### Constant `N_PC`

global pascal symbol: name,,NO_SECT,subtype,line

```rust
pub const N_PC: u8 = 0x30;
```

#### Constant `R_ABS`

absolute relocation type for Mach-O files

```rust
pub const R_ABS: u8 = 0;
```

#### Constant `R_SCATTERED`

Bit set in `Relocation::r_word0` for scattered relocations.

```rust
pub const R_SCATTERED: u32 = 0x8000_0000;
```

#### Constant `GENERIC_RELOC_VANILLA`

generic relocation as described above

```rust
pub const GENERIC_RELOC_VANILLA: u8 = 0;
```

#### Constant `GENERIC_RELOC_PAIR`

Only follows a GENERIC_RELOC_SECTDIFF

```rust
pub const GENERIC_RELOC_PAIR: u8 = 1;
```

#### Constant `GENERIC_RELOC_SECTDIFF`

```rust
pub const GENERIC_RELOC_SECTDIFF: u8 = 2;
```

#### Constant `GENERIC_RELOC_PB_LA_PTR`

prebound lazy pointer

```rust
pub const GENERIC_RELOC_PB_LA_PTR: u8 = 3;
```

#### Constant `GENERIC_RELOC_LOCAL_SECTDIFF`

```rust
pub const GENERIC_RELOC_LOCAL_SECTDIFF: u8 = 4;
```

#### Constant `GENERIC_RELOC_TLV`

thread local variables

```rust
pub const GENERIC_RELOC_TLV: u8 = 5;
```

#### Constant `ARM_RELOC_VANILLA`

generic relocation as described above

```rust
pub const ARM_RELOC_VANILLA: u8 = 0;
```

#### Constant `ARM_RELOC_PAIR`

the second relocation entry of a pair

```rust
pub const ARM_RELOC_PAIR: u8 = 1;
```

#### Constant `ARM_RELOC_SECTDIFF`

a PAIR follows with subtract symbol value

```rust
pub const ARM_RELOC_SECTDIFF: u8 = 2;
```

#### Constant `ARM_RELOC_LOCAL_SECTDIFF`

like ARM_RELOC_SECTDIFF, but the symbol referenced was local.

```rust
pub const ARM_RELOC_LOCAL_SECTDIFF: u8 = 3;
```

#### Constant `ARM_RELOC_PB_LA_PTR`

prebound lazy pointer

```rust
pub const ARM_RELOC_PB_LA_PTR: u8 = 4;
```

#### Constant `ARM_RELOC_BR24`

24 bit branch displacement (to a word address)

```rust
pub const ARM_RELOC_BR24: u8 = 5;
```

#### Constant `ARM_THUMB_RELOC_BR22`

22 bit branch displacement (to a half-word address)

```rust
pub const ARM_THUMB_RELOC_BR22: u8 = 6;
```

#### Constant `ARM_THUMB_32BIT_BRANCH`

obsolete - a thumb 32-bit branch instruction possibly needing page-spanning branch workaround

```rust
pub const ARM_THUMB_32BIT_BRANCH: u8 = 7;
```

#### Constant `ARM_RELOC_HALF`

```rust
pub const ARM_RELOC_HALF: u8 = 8;
```

#### Constant `ARM_RELOC_HALF_SECTDIFF`

```rust
pub const ARM_RELOC_HALF_SECTDIFF: u8 = 9;
```

#### Constant `ARM64_RELOC_UNSIGNED`

for pointers

```rust
pub const ARM64_RELOC_UNSIGNED: u8 = 0;
```

#### Constant `ARM64_RELOC_SUBTRACTOR`

must be followed by a ARM64_RELOC_UNSIGNED

```rust
pub const ARM64_RELOC_SUBTRACTOR: u8 = 1;
```

#### Constant `ARM64_RELOC_BRANCH26`

a B/BL instruction with 26-bit displacement

```rust
pub const ARM64_RELOC_BRANCH26: u8 = 2;
```

#### Constant `ARM64_RELOC_PAGE21`

pc-rel distance to page of target

```rust
pub const ARM64_RELOC_PAGE21: u8 = 3;
```

#### Constant `ARM64_RELOC_PAGEOFF12`

offset within page, scaled by r_length

```rust
pub const ARM64_RELOC_PAGEOFF12: u8 = 4;
```

#### Constant `ARM64_RELOC_GOT_LOAD_PAGE21`

pc-rel distance to page of GOT slot

```rust
pub const ARM64_RELOC_GOT_LOAD_PAGE21: u8 = 5;
```

#### Constant `ARM64_RELOC_GOT_LOAD_PAGEOFF12`

offset within page of GOT slot, scaled by r_length

```rust
pub const ARM64_RELOC_GOT_LOAD_PAGEOFF12: u8 = 6;
```

#### Constant `ARM64_RELOC_POINTER_TO_GOT`

for pointers to GOT slots

```rust
pub const ARM64_RELOC_POINTER_TO_GOT: u8 = 7;
```

#### Constant `ARM64_RELOC_TLVP_LOAD_PAGE21`

pc-rel distance to page of TLVP slot

```rust
pub const ARM64_RELOC_TLVP_LOAD_PAGE21: u8 = 8;
```

#### Constant `ARM64_RELOC_TLVP_LOAD_PAGEOFF12`

offset within page of TLVP slot, scaled by r_length

```rust
pub const ARM64_RELOC_TLVP_LOAD_PAGEOFF12: u8 = 9;
```

#### Constant `ARM64_RELOC_ADDEND`

must be followed by PAGE21 or PAGEOFF12

```rust
pub const ARM64_RELOC_ADDEND: u8 = 10;
```

#### Constant `ARM64_RELOC_AUTHENTICATED_POINTER`

```rust
pub const ARM64_RELOC_AUTHENTICATED_POINTER: u8 = 11;
```

#### Constant `PPC_RELOC_VANILLA`

generic relocation as described above

```rust
pub const PPC_RELOC_VANILLA: u8 = 0;
```

#### Constant `PPC_RELOC_PAIR`

the second relocation entry of a pair

```rust
pub const PPC_RELOC_PAIR: u8 = 1;
```

#### Constant `PPC_RELOC_BR14`

14 bit branch displacement (to a word address)

```rust
pub const PPC_RELOC_BR14: u8 = 2;
```

#### Constant `PPC_RELOC_BR24`

24 bit branch displacement (to a word address)

```rust
pub const PPC_RELOC_BR24: u8 = 3;
```

#### Constant `PPC_RELOC_HI16`

a PAIR follows with the low half

```rust
pub const PPC_RELOC_HI16: u8 = 4;
```

#### Constant `PPC_RELOC_LO16`

a PAIR follows with the high half

```rust
pub const PPC_RELOC_LO16: u8 = 5;
```

#### Constant `PPC_RELOC_HA16`

Same as the RELOC_HI16 except the low 16 bits and the high 16 bits are added together
with the low 16 bits sign extended first.  This means if bit 15 of the low 16 bits is
set the high 16 bits stored in the instruction will be adjusted.

```rust
pub const PPC_RELOC_HA16: u8 = 6;
```

#### Constant `PPC_RELOC_LO14`

Same as the LO16 except that the low 2 bits are not stored in the instruction and are
always zero.  This is used in double word load/store instructions.

```rust
pub const PPC_RELOC_LO14: u8 = 7;
```

#### Constant `PPC_RELOC_SECTDIFF`

a PAIR follows with subtract symbol value

```rust
pub const PPC_RELOC_SECTDIFF: u8 = 8;
```

#### Constant `PPC_RELOC_PB_LA_PTR`

prebound lazy pointer

```rust
pub const PPC_RELOC_PB_LA_PTR: u8 = 9;
```

#### Constant `PPC_RELOC_HI16_SECTDIFF`

section difference forms of above.  a PAIR

```rust
pub const PPC_RELOC_HI16_SECTDIFF: u8 = 10;
```

#### Constant `PPC_RELOC_LO16_SECTDIFF`

follows these with subtract symbol value

```rust
pub const PPC_RELOC_LO16_SECTDIFF: u8 = 11;
```

#### Constant `PPC_RELOC_HA16_SECTDIFF`

```rust
pub const PPC_RELOC_HA16_SECTDIFF: u8 = 12;
```

#### Constant `PPC_RELOC_JBSR`

```rust
pub const PPC_RELOC_JBSR: u8 = 13;
```

#### Constant `PPC_RELOC_LO14_SECTDIFF`

```rust
pub const PPC_RELOC_LO14_SECTDIFF: u8 = 14;
```

#### Constant `PPC_RELOC_LOCAL_SECTDIFF`

like PPC_RELOC_SECTDIFF, but the symbol referenced was local.

```rust
pub const PPC_RELOC_LOCAL_SECTDIFF: u8 = 15;
```

#### Constant `X86_64_RELOC_UNSIGNED`

for absolute addresses

```rust
pub const X86_64_RELOC_UNSIGNED: u8 = 0;
```

#### Constant `X86_64_RELOC_SIGNED`

for signed 32-bit displacement

```rust
pub const X86_64_RELOC_SIGNED: u8 = 1;
```

#### Constant `X86_64_RELOC_BRANCH`

a CALL/JMP instruction with 32-bit displacement

```rust
pub const X86_64_RELOC_BRANCH: u8 = 2;
```

#### Constant `X86_64_RELOC_GOT_LOAD`

a MOVQ load of a GOT entry

```rust
pub const X86_64_RELOC_GOT_LOAD: u8 = 3;
```

#### Constant `X86_64_RELOC_GOT`

other GOT references

```rust
pub const X86_64_RELOC_GOT: u8 = 4;
```

#### Constant `X86_64_RELOC_SUBTRACTOR`

must be followed by a X86_64_RELOC_UNSIGNED

```rust
pub const X86_64_RELOC_SUBTRACTOR: u8 = 5;
```

#### Constant `X86_64_RELOC_SIGNED_1`

for signed 32-bit displacement with a -1 addend

```rust
pub const X86_64_RELOC_SIGNED_1: u8 = 6;
```

#### Constant `X86_64_RELOC_SIGNED_2`

for signed 32-bit displacement with a -2 addend

```rust
pub const X86_64_RELOC_SIGNED_2: u8 = 7;
```

#### Constant `X86_64_RELOC_SIGNED_4`

for signed 32-bit displacement with a -4 addend

```rust
pub const X86_64_RELOC_SIGNED_4: u8 = 8;
```

#### Constant `X86_64_RELOC_TLV`

for thread local variables

```rust
pub const X86_64_RELOC_TLV: u8 = 9;
```

## Module `pe`

**Attributes:**

- `#[<cfg>(any(feature = "coff", feature = "pe"))]`
- `#[allow(missing_docs)]`

PE/COFF definitions.

These definitions are independent of read/write support, although we do implement
some traits useful for those.

This module is based heavily on "winnt.h" (10.0.17763.0).

```rust
pub mod pe { /* ... */ }
```

### Types

#### Struct `ImageDosHeader`

**Attributes:**

- `#[repr(C)]`

DOS .EXE header

```rust
pub struct ImageDosHeader {
    pub e_magic: crate::endian::U16<crate::endian::LittleEndian>,
    pub e_cblp: crate::endian::U16<crate::endian::LittleEndian>,
    pub e_cp: crate::endian::U16<crate::endian::LittleEndian>,
    pub e_crlc: crate::endian::U16<crate::endian::LittleEndian>,
    pub e_cparhdr: crate::endian::U16<crate::endian::LittleEndian>,
    pub e_minalloc: crate::endian::U16<crate::endian::LittleEndian>,
    pub e_maxalloc: crate::endian::U16<crate::endian::LittleEndian>,
    pub e_ss: crate::endian::U16<crate::endian::LittleEndian>,
    pub e_sp: crate::endian::U16<crate::endian::LittleEndian>,
    pub e_csum: crate::endian::U16<crate::endian::LittleEndian>,
    pub e_ip: crate::endian::U16<crate::endian::LittleEndian>,
    pub e_cs: crate::endian::U16<crate::endian::LittleEndian>,
    pub e_lfarlc: crate::endian::U16<crate::endian::LittleEndian>,
    pub e_ovno: crate::endian::U16<crate::endian::LittleEndian>,
    pub e_res: [crate::endian::U16<crate::endian::LittleEndian>; 4],
    pub e_oemid: crate::endian::U16<crate::endian::LittleEndian>,
    pub e_oeminfo: crate::endian::U16<crate::endian::LittleEndian>,
    pub e_res2: [crate::endian::U16<crate::endian::LittleEndian>; 10],
    pub e_lfanew: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `e_magic` | `crate::endian::U16<crate::endian::LittleEndian>` | Magic number |
| `e_cblp` | `crate::endian::U16<crate::endian::LittleEndian>` | Bytes on last page of file |
| `e_cp` | `crate::endian::U16<crate::endian::LittleEndian>` | Pages in file |
| `e_crlc` | `crate::endian::U16<crate::endian::LittleEndian>` | Relocations |
| `e_cparhdr` | `crate::endian::U16<crate::endian::LittleEndian>` | Size of header in paragraphs |
| `e_minalloc` | `crate::endian::U16<crate::endian::LittleEndian>` | Minimum extra paragraphs needed |
| `e_maxalloc` | `crate::endian::U16<crate::endian::LittleEndian>` | Maximum extra paragraphs needed |
| `e_ss` | `crate::endian::U16<crate::endian::LittleEndian>` | Initial (relative) SS value |
| `e_sp` | `crate::endian::U16<crate::endian::LittleEndian>` | Initial SP value |
| `e_csum` | `crate::endian::U16<crate::endian::LittleEndian>` | Checksum |
| `e_ip` | `crate::endian::U16<crate::endian::LittleEndian>` | Initial IP value |
| `e_cs` | `crate::endian::U16<crate::endian::LittleEndian>` | Initial (relative) CS value |
| `e_lfarlc` | `crate::endian::U16<crate::endian::LittleEndian>` | File address of relocation table |
| `e_ovno` | `crate::endian::U16<crate::endian::LittleEndian>` | Overlay number |
| `e_res` | `[crate::endian::U16<crate::endian::LittleEndian>; 4]` | Reserved words |
| `e_oemid` | `crate::endian::U16<crate::endian::LittleEndian>` | OEM identifier (for e_oeminfo) |
| `e_oeminfo` | `crate::endian::U16<crate::endian::LittleEndian>` | OEM information; e_oemid specific |
| `e_res2` | `[crate::endian::U16<crate::endian::LittleEndian>; 10]` | Reserved words |
| `e_lfanew` | `crate::endian::U32<crate::endian::LittleEndian>` | File address of new exe header |

##### Implementations

###### Methods

- ```rust
  pub fn parse<''data, R: ReadRef<''data>>(data: R) -> read::Result<&''data Self> { /* ... */ }
  ```
  Read the DOS header.

- ```rust
  pub fn nt_headers_offset(self: &Self) -> u32 { /* ... */ }
  ```
  Return the file offset of the nt_headers.

###### Trait Implementations

- **UnwindSafe**
- **Freeze**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Sync**
- **Unpin**
- **RefUnwindSafe**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Pod**
- **Copy**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Send**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageDosHeader { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

#### Struct `ImageOs2Header`

**Attributes:**

- `#[repr(C)]`

OS/2 .EXE header

```rust
pub struct ImageOs2Header {
    pub ne_magic: crate::endian::U16<crate::endian::LittleEndian>,
    pub ne_ver: i8,
    pub ne_rev: i8,
    pub ne_enttab: crate::endian::U16<crate::endian::LittleEndian>,
    pub ne_cbenttab: crate::endian::U16<crate::endian::LittleEndian>,
    pub ne_crc: crate::endian::I32<crate::endian::LittleEndian>,
    pub ne_flags: crate::endian::U16<crate::endian::LittleEndian>,
    pub ne_autodata: crate::endian::U16<crate::endian::LittleEndian>,
    pub ne_heap: crate::endian::U16<crate::endian::LittleEndian>,
    pub ne_stack: crate::endian::U16<crate::endian::LittleEndian>,
    pub ne_csip: crate::endian::I32<crate::endian::LittleEndian>,
    pub ne_sssp: crate::endian::I32<crate::endian::LittleEndian>,
    pub ne_cseg: crate::endian::U16<crate::endian::LittleEndian>,
    pub ne_cmod: crate::endian::U16<crate::endian::LittleEndian>,
    pub ne_cbnrestab: crate::endian::U16<crate::endian::LittleEndian>,
    pub ne_segtab: crate::endian::U16<crate::endian::LittleEndian>,
    pub ne_rsrctab: crate::endian::U16<crate::endian::LittleEndian>,
    pub ne_restab: crate::endian::U16<crate::endian::LittleEndian>,
    pub ne_modtab: crate::endian::U16<crate::endian::LittleEndian>,
    pub ne_imptab: crate::endian::U16<crate::endian::LittleEndian>,
    pub ne_nrestab: crate::endian::I32<crate::endian::LittleEndian>,
    pub ne_cmovent: crate::endian::U16<crate::endian::LittleEndian>,
    pub ne_align: crate::endian::U16<crate::endian::LittleEndian>,
    pub ne_cres: crate::endian::U16<crate::endian::LittleEndian>,
    pub ne_exetyp: u8,
    pub ne_flagsothers: u8,
    pub ne_pretthunks: crate::endian::U16<crate::endian::LittleEndian>,
    pub ne_psegrefbytes: crate::endian::U16<crate::endian::LittleEndian>,
    pub ne_swaparea: crate::endian::U16<crate::endian::LittleEndian>,
    pub ne_expver: crate::endian::U16<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `ne_magic` | `crate::endian::U16<crate::endian::LittleEndian>` | Magic number |
| `ne_ver` | `i8` | Version number |
| `ne_rev` | `i8` | Revision number |
| `ne_enttab` | `crate::endian::U16<crate::endian::LittleEndian>` | Offset of Entry Table |
| `ne_cbenttab` | `crate::endian::U16<crate::endian::LittleEndian>` | Number of bytes in Entry Table |
| `ne_crc` | `crate::endian::I32<crate::endian::LittleEndian>` | Checksum of whole file |
| `ne_flags` | `crate::endian::U16<crate::endian::LittleEndian>` | Flag word |
| `ne_autodata` | `crate::endian::U16<crate::endian::LittleEndian>` | Automatic data segment number |
| `ne_heap` | `crate::endian::U16<crate::endian::LittleEndian>` | Initial heap allocation |
| `ne_stack` | `crate::endian::U16<crate::endian::LittleEndian>` | Initial stack allocation |
| `ne_csip` | `crate::endian::I32<crate::endian::LittleEndian>` | Initial CS:IP setting |
| `ne_sssp` | `crate::endian::I32<crate::endian::LittleEndian>` | Initial SS:SP setting |
| `ne_cseg` | `crate::endian::U16<crate::endian::LittleEndian>` | Count of file segments |
| `ne_cmod` | `crate::endian::U16<crate::endian::LittleEndian>` | Entries in Module Reference Table |
| `ne_cbnrestab` | `crate::endian::U16<crate::endian::LittleEndian>` | Size of non-resident name table |
| `ne_segtab` | `crate::endian::U16<crate::endian::LittleEndian>` | Offset of Segment Table |
| `ne_rsrctab` | `crate::endian::U16<crate::endian::LittleEndian>` | Offset of Resource Table |
| `ne_restab` | `crate::endian::U16<crate::endian::LittleEndian>` | Offset of resident name table |
| `ne_modtab` | `crate::endian::U16<crate::endian::LittleEndian>` | Offset of Module Reference Table |
| `ne_imptab` | `crate::endian::U16<crate::endian::LittleEndian>` | Offset of Imported Names Table |
| `ne_nrestab` | `crate::endian::I32<crate::endian::LittleEndian>` | Offset of Non-resident Names Table |
| `ne_cmovent` | `crate::endian::U16<crate::endian::LittleEndian>` | Count of movable entries |
| `ne_align` | `crate::endian::U16<crate::endian::LittleEndian>` | Segment alignment shift count |
| `ne_cres` | `crate::endian::U16<crate::endian::LittleEndian>` | Count of resource segments |
| `ne_exetyp` | `u8` | Target Operating system |
| `ne_flagsothers` | `u8` | Other .EXE flags |
| `ne_pretthunks` | `crate::endian::U16<crate::endian::LittleEndian>` | offset to return thunks |
| `ne_psegrefbytes` | `crate::endian::U16<crate::endian::LittleEndian>` | offset to segment ref. bytes |
| `ne_swaparea` | `crate::endian::U16<crate::endian::LittleEndian>` | Minimum code swap area size |
| `ne_expver` | `crate::endian::U16<crate::endian::LittleEndian>` | Expected Windows version number |

##### Implementations

###### Trait Implementations

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageOs2Header { /* ... */ }
    ```

- **Copy**
- **Pod**
- **Sync**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Unpin**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Freeze**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Send**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **UnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **RefUnwindSafe**
#### Struct `ImageVxdHeader`

**Attributes:**

- `#[repr(C)]`

Windows VXD header

```rust
pub struct ImageVxdHeader {
    pub e32_magic: crate::endian::U16<crate::endian::LittleEndian>,
    pub e32_border: u8,
    pub e32_worder: u8,
    pub e32_level: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_cpu: crate::endian::U16<crate::endian::LittleEndian>,
    pub e32_os: crate::endian::U16<crate::endian::LittleEndian>,
    pub e32_ver: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_mflags: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_mpages: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_startobj: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_eip: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_stackobj: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_esp: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_pagesize: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_lastpagesize: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_fixupsize: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_fixupsum: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_ldrsize: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_ldrsum: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_objtab: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_objcnt: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_objmap: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_itermap: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_rsrctab: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_rsrccnt: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_restab: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_enttab: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_dirtab: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_dircnt: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_fpagetab: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_frectab: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_impmod: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_impmodcnt: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_impproc: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_pagesum: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_datapage: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_preload: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_nrestab: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_cbnrestab: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_nressum: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_autodata: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_debuginfo: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_debuglen: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_instpreload: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_instdemand: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_heapsize: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_res3: [u8; 12],
    pub e32_winresoff: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_winreslen: crate::endian::U32<crate::endian::LittleEndian>,
    pub e32_devid: crate::endian::U16<crate::endian::LittleEndian>,
    pub e32_ddkver: crate::endian::U16<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `e32_magic` | `crate::endian::U16<crate::endian::LittleEndian>` | Magic number |
| `e32_border` | `u8` | The byte ordering for the VXD |
| `e32_worder` | `u8` | The word ordering for the VXD |
| `e32_level` | `crate::endian::U32<crate::endian::LittleEndian>` | The EXE format level for now = 0 |
| `e32_cpu` | `crate::endian::U16<crate::endian::LittleEndian>` | The CPU type |
| `e32_os` | `crate::endian::U16<crate::endian::LittleEndian>` | The OS type |
| `e32_ver` | `crate::endian::U32<crate::endian::LittleEndian>` | Module version |
| `e32_mflags` | `crate::endian::U32<crate::endian::LittleEndian>` | Module flags |
| `e32_mpages` | `crate::endian::U32<crate::endian::LittleEndian>` | Module # pages |
| `e32_startobj` | `crate::endian::U32<crate::endian::LittleEndian>` | Object # for instruction pointer |
| `e32_eip` | `crate::endian::U32<crate::endian::LittleEndian>` | Extended instruction pointer |
| `e32_stackobj` | `crate::endian::U32<crate::endian::LittleEndian>` | Object # for stack pointer |
| `e32_esp` | `crate::endian::U32<crate::endian::LittleEndian>` | Extended stack pointer |
| `e32_pagesize` | `crate::endian::U32<crate::endian::LittleEndian>` | VXD page size |
| `e32_lastpagesize` | `crate::endian::U32<crate::endian::LittleEndian>` | Last page size in VXD |
| `e32_fixupsize` | `crate::endian::U32<crate::endian::LittleEndian>` | Fixup section size |
| `e32_fixupsum` | `crate::endian::U32<crate::endian::LittleEndian>` | Fixup section checksum |
| `e32_ldrsize` | `crate::endian::U32<crate::endian::LittleEndian>` | Loader section size |
| `e32_ldrsum` | `crate::endian::U32<crate::endian::LittleEndian>` | Loader section checksum |
| `e32_objtab` | `crate::endian::U32<crate::endian::LittleEndian>` | Object table offset |
| `e32_objcnt` | `crate::endian::U32<crate::endian::LittleEndian>` | Number of objects in module |
| `e32_objmap` | `crate::endian::U32<crate::endian::LittleEndian>` | Object page map offset |
| `e32_itermap` | `crate::endian::U32<crate::endian::LittleEndian>` | Object iterated data map offset |
| `e32_rsrctab` | `crate::endian::U32<crate::endian::LittleEndian>` | Offset of Resource Table |
| `e32_rsrccnt` | `crate::endian::U32<crate::endian::LittleEndian>` | Number of resource entries |
| `e32_restab` | `crate::endian::U32<crate::endian::LittleEndian>` | Offset of resident name table |
| `e32_enttab` | `crate::endian::U32<crate::endian::LittleEndian>` | Offset of Entry Table |
| `e32_dirtab` | `crate::endian::U32<crate::endian::LittleEndian>` | Offset of Module Directive Table |
| `e32_dircnt` | `crate::endian::U32<crate::endian::LittleEndian>` | Number of module directives |
| `e32_fpagetab` | `crate::endian::U32<crate::endian::LittleEndian>` | Offset of Fixup Page Table |
| `e32_frectab` | `crate::endian::U32<crate::endian::LittleEndian>` | Offset of Fixup Record Table |
| `e32_impmod` | `crate::endian::U32<crate::endian::LittleEndian>` | Offset of Import Module Name Table |
| `e32_impmodcnt` | `crate::endian::U32<crate::endian::LittleEndian>` | Number of entries in Import Module Name Table |
| `e32_impproc` | `crate::endian::U32<crate::endian::LittleEndian>` | Offset of Import Procedure Name Table |
| `e32_pagesum` | `crate::endian::U32<crate::endian::LittleEndian>` | Offset of Per-Page Checksum Table |
| `e32_datapage` | `crate::endian::U32<crate::endian::LittleEndian>` | Offset of Enumerated Data Pages |
| `e32_preload` | `crate::endian::U32<crate::endian::LittleEndian>` | Number of preload pages |
| `e32_nrestab` | `crate::endian::U32<crate::endian::LittleEndian>` | Offset of Non-resident Names Table |
| `e32_cbnrestab` | `crate::endian::U32<crate::endian::LittleEndian>` | Size of Non-resident Name Table |
| `e32_nressum` | `crate::endian::U32<crate::endian::LittleEndian>` | Non-resident Name Table Checksum |
| `e32_autodata` | `crate::endian::U32<crate::endian::LittleEndian>` | Object # for automatic data object |
| `e32_debuginfo` | `crate::endian::U32<crate::endian::LittleEndian>` | Offset of the debugging information |
| `e32_debuglen` | `crate::endian::U32<crate::endian::LittleEndian>` | The length of the debugging info. in bytes |
| `e32_instpreload` | `crate::endian::U32<crate::endian::LittleEndian>` | Number of instance pages in preload section of VXD file |
| `e32_instdemand` | `crate::endian::U32<crate::endian::LittleEndian>` | Number of instance pages in demand load section of VXD file |
| `e32_heapsize` | `crate::endian::U32<crate::endian::LittleEndian>` | Size of heap - for 16-bit apps |
| `e32_res3` | `[u8; 12]` | Reserved words |
| `e32_winresoff` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `e32_winreslen` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `e32_devid` | `crate::endian::U16<crate::endian::LittleEndian>` | Device ID for VxD |
| `e32_ddkver` | `crate::endian::U16<crate::endian::LittleEndian>` | DDK version for VxD |

##### Implementations

###### Trait Implementations

- **Send**
- **RefUnwindSafe**
- **Freeze**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **UnwindSafe**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Copy**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageVxdHeader { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Pod**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Sync**
- **Unpin**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

#### Struct `MaskedRichHeaderEntry`

**Attributes:**

- `#[repr(C)]`

A PE rich header entry.

Rich headers have no official documentation, but have been heavily
reversed-engineered and documented in the wild, e.g.:
* `http://www.ntcore.com/files/richsign.htm`
* `https://www.researchgate.net/figure/Structure-of-the-Rich-Header_fig1_318145388`

This data is "masked", i.e. XORed with a checksum derived from the file data.

```rust
pub struct MaskedRichHeaderEntry {
    pub masked_comp_id: crate::endian::U32<crate::endian::LittleEndian>,
    pub masked_count: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `masked_comp_id` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `masked_count` | `crate::endian::U32<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Pod**
- **Copy**
- **Freeze**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> MaskedRichHeaderEntry { /* ... */ }
    ```

- **Send**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Sync**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Unpin**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **UnwindSafe**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **RefUnwindSafe**
#### Struct `ImageFileHeader`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageFileHeader {
    pub machine: crate::endian::U16<crate::endian::LittleEndian>,
    pub number_of_sections: crate::endian::U16<crate::endian::LittleEndian>,
    pub time_date_stamp: crate::endian::U32<crate::endian::LittleEndian>,
    pub pointer_to_symbol_table: crate::endian::U32<crate::endian::LittleEndian>,
    pub number_of_symbols: crate::endian::U32<crate::endian::LittleEndian>,
    pub size_of_optional_header: crate::endian::U16<crate::endian::LittleEndian>,
    pub characteristics: crate::endian::U16<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `machine` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `number_of_sections` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `time_date_stamp` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `pointer_to_symbol_table` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `number_of_symbols` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `size_of_optional_header` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `characteristics` | `crate::endian::U16<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **Freeze**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **CoffHeader**
  - ```rust
    fn is_type_bigobj() -> bool { /* ... */ }
    ```

  - ```rust
    fn machine(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn number_of_sections(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn pointer_to_symbol_table(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn number_of_symbols(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn characteristics(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn parse<''data, R: ReadRef<''data>>(data: R, offset: &mut u64) -> read::Result<&''data Self> { /* ... */ }
    ```

- **Unpin**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Send**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageFileHeader { /* ... */ }
    ```

- **Sync**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Copy**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **UnwindSafe**
- **RefUnwindSafe**
- **Pod**
#### Struct `ImageDataDirectory`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageDataDirectory {
    pub virtual_address: crate::endian::U32<crate::endian::LittleEndian>,
    pub size: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `virtual_address` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `size` | `crate::endian::U32<crate::endian::LittleEndian>` |  |

##### Implementations

###### Methods

- ```rust
  pub fn address_range(self: &Self) -> (u32, u32) { /* ... */ }
  ```
  Return the virtual address range of this directory entry.

- ```rust
  pub fn file_range(self: &Self, sections: &SectionTable<''_>) -> Result<(u32, u32)> { /* ... */ }
  ```
  Return the file offset and size of this directory entry.

- ```rust
  pub fn data<''data, R: ReadRef<''data>>(self: &Self, data: R, sections: &SectionTable<''data>) -> Result<&''data [u8]> { /* ... */ }
  ```
  Get the data referenced by this directory entry.

###### Trait Implementations

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Copy**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Send**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageDataDirectory { /* ... */ }
    ```

- **Pod**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Sync**
- **Freeze**
- **UnwindSafe**
- **RefUnwindSafe**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Unpin**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

#### Struct `ImageOptionalHeader32`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageOptionalHeader32 {
    pub magic: crate::endian::U16<crate::endian::LittleEndian>,
    pub major_linker_version: u8,
    pub minor_linker_version: u8,
    pub size_of_code: crate::endian::U32<crate::endian::LittleEndian>,
    pub size_of_initialized_data: crate::endian::U32<crate::endian::LittleEndian>,
    pub size_of_uninitialized_data: crate::endian::U32<crate::endian::LittleEndian>,
    pub address_of_entry_point: crate::endian::U32<crate::endian::LittleEndian>,
    pub base_of_code: crate::endian::U32<crate::endian::LittleEndian>,
    pub base_of_data: crate::endian::U32<crate::endian::LittleEndian>,
    pub image_base: crate::endian::U32<crate::endian::LittleEndian>,
    pub section_alignment: crate::endian::U32<crate::endian::LittleEndian>,
    pub file_alignment: crate::endian::U32<crate::endian::LittleEndian>,
    pub major_operating_system_version: crate::endian::U16<crate::endian::LittleEndian>,
    pub minor_operating_system_version: crate::endian::U16<crate::endian::LittleEndian>,
    pub major_image_version: crate::endian::U16<crate::endian::LittleEndian>,
    pub minor_image_version: crate::endian::U16<crate::endian::LittleEndian>,
    pub major_subsystem_version: crate::endian::U16<crate::endian::LittleEndian>,
    pub minor_subsystem_version: crate::endian::U16<crate::endian::LittleEndian>,
    pub win32_version_value: crate::endian::U32<crate::endian::LittleEndian>,
    pub size_of_image: crate::endian::U32<crate::endian::LittleEndian>,
    pub size_of_headers: crate::endian::U32<crate::endian::LittleEndian>,
    pub check_sum: crate::endian::U32<crate::endian::LittleEndian>,
    pub subsystem: crate::endian::U16<crate::endian::LittleEndian>,
    pub dll_characteristics: crate::endian::U16<crate::endian::LittleEndian>,
    pub size_of_stack_reserve: crate::endian::U32<crate::endian::LittleEndian>,
    pub size_of_stack_commit: crate::endian::U32<crate::endian::LittleEndian>,
    pub size_of_heap_reserve: crate::endian::U32<crate::endian::LittleEndian>,
    pub size_of_heap_commit: crate::endian::U32<crate::endian::LittleEndian>,
    pub loader_flags: crate::endian::U32<crate::endian::LittleEndian>,
    pub number_of_rva_and_sizes: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `magic` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `major_linker_version` | `u8` |  |
| `minor_linker_version` | `u8` |  |
| `size_of_code` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `size_of_initialized_data` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `size_of_uninitialized_data` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `address_of_entry_point` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `base_of_code` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `base_of_data` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `image_base` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `section_alignment` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `file_alignment` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `major_operating_system_version` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `minor_operating_system_version` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `major_image_version` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `minor_image_version` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `major_subsystem_version` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `minor_subsystem_version` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `win32_version_value` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `size_of_image` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `size_of_headers` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `check_sum` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `subsystem` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `dll_characteristics` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `size_of_stack_reserve` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `size_of_stack_commit` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `size_of_heap_reserve` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `size_of_heap_commit` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `loader_flags` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `number_of_rva_and_sizes` | `crate::endian::U32<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Pod**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Unpin**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageOptionalHeader32 { /* ... */ }
    ```

- **ImageOptionalHeader**
  - ```rust
    fn magic(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn major_linker_version(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn minor_linker_version(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn size_of_code(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn size_of_initialized_data(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn size_of_uninitialized_data(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn address_of_entry_point(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn base_of_code(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn base_of_data(self: &Self) -> Option<u32> { /* ... */ }
    ```

  - ```rust
    fn image_base(self: &Self) -> u64 { /* ... */ }
    ```

  - ```rust
    fn section_alignment(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn file_alignment(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn major_operating_system_version(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn minor_operating_system_version(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn major_image_version(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn minor_image_version(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn major_subsystem_version(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn minor_subsystem_version(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn win32_version_value(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn size_of_image(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn size_of_headers(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn check_sum(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn subsystem(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn dll_characteristics(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn size_of_stack_reserve(self: &Self) -> u64 { /* ... */ }
    ```

  - ```rust
    fn size_of_stack_commit(self: &Self) -> u64 { /* ... */ }
    ```

  - ```rust
    fn size_of_heap_reserve(self: &Self) -> u64 { /* ... */ }
    ```

  - ```rust
    fn size_of_heap_commit(self: &Self) -> u64 { /* ... */ }
    ```

  - ```rust
    fn loader_flags(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn number_of_rva_and_sizes(self: &Self) -> u32 { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Freeze**
- **UnwindSafe**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **RefUnwindSafe**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Copy**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Sync**
- **Send**
#### Struct `ImageRomOptionalHeader`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageRomOptionalHeader {
    pub magic: crate::endian::U16<crate::endian::LittleEndian>,
    pub major_linker_version: u8,
    pub minor_linker_version: u8,
    pub size_of_code: crate::endian::U32<crate::endian::LittleEndian>,
    pub size_of_initialized_data: crate::endian::U32<crate::endian::LittleEndian>,
    pub size_of_uninitialized_data: crate::endian::U32<crate::endian::LittleEndian>,
    pub address_of_entry_point: crate::endian::U32<crate::endian::LittleEndian>,
    pub base_of_code: crate::endian::U32<crate::endian::LittleEndian>,
    pub base_of_data: crate::endian::U32<crate::endian::LittleEndian>,
    pub base_of_bss: crate::endian::U32<crate::endian::LittleEndian>,
    pub gpr_mask: crate::endian::U32<crate::endian::LittleEndian>,
    pub cpr_mask: [crate::endian::U32<crate::endian::LittleEndian>; 4],
    pub gp_value: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `magic` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `major_linker_version` | `u8` |  |
| `minor_linker_version` | `u8` |  |
| `size_of_code` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `size_of_initialized_data` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `size_of_uninitialized_data` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `address_of_entry_point` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `base_of_code` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `base_of_data` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `base_of_bss` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `gpr_mask` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `cpr_mask` | `[crate::endian::U32<crate::endian::LittleEndian>; 4]` |  |
| `gp_value` | `crate::endian::U32<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageRomOptionalHeader { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Freeze**
- **Copy**
- **Pod**
- **Unpin**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **RefUnwindSafe**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Sync**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Send**
- **UnwindSafe**
#### Struct `ImageOptionalHeader64`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageOptionalHeader64 {
    pub magic: crate::endian::U16<crate::endian::LittleEndian>,
    pub major_linker_version: u8,
    pub minor_linker_version: u8,
    pub size_of_code: crate::endian::U32<crate::endian::LittleEndian>,
    pub size_of_initialized_data: crate::endian::U32<crate::endian::LittleEndian>,
    pub size_of_uninitialized_data: crate::endian::U32<crate::endian::LittleEndian>,
    pub address_of_entry_point: crate::endian::U32<crate::endian::LittleEndian>,
    pub base_of_code: crate::endian::U32<crate::endian::LittleEndian>,
    pub image_base: crate::endian::U64<crate::endian::LittleEndian>,
    pub section_alignment: crate::endian::U32<crate::endian::LittleEndian>,
    pub file_alignment: crate::endian::U32<crate::endian::LittleEndian>,
    pub major_operating_system_version: crate::endian::U16<crate::endian::LittleEndian>,
    pub minor_operating_system_version: crate::endian::U16<crate::endian::LittleEndian>,
    pub major_image_version: crate::endian::U16<crate::endian::LittleEndian>,
    pub minor_image_version: crate::endian::U16<crate::endian::LittleEndian>,
    pub major_subsystem_version: crate::endian::U16<crate::endian::LittleEndian>,
    pub minor_subsystem_version: crate::endian::U16<crate::endian::LittleEndian>,
    pub win32_version_value: crate::endian::U32<crate::endian::LittleEndian>,
    pub size_of_image: crate::endian::U32<crate::endian::LittleEndian>,
    pub size_of_headers: crate::endian::U32<crate::endian::LittleEndian>,
    pub check_sum: crate::endian::U32<crate::endian::LittleEndian>,
    pub subsystem: crate::endian::U16<crate::endian::LittleEndian>,
    pub dll_characteristics: crate::endian::U16<crate::endian::LittleEndian>,
    pub size_of_stack_reserve: crate::endian::U64<crate::endian::LittleEndian>,
    pub size_of_stack_commit: crate::endian::U64<crate::endian::LittleEndian>,
    pub size_of_heap_reserve: crate::endian::U64<crate::endian::LittleEndian>,
    pub size_of_heap_commit: crate::endian::U64<crate::endian::LittleEndian>,
    pub loader_flags: crate::endian::U32<crate::endian::LittleEndian>,
    pub number_of_rva_and_sizes: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `magic` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `major_linker_version` | `u8` |  |
| `minor_linker_version` | `u8` |  |
| `size_of_code` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `size_of_initialized_data` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `size_of_uninitialized_data` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `address_of_entry_point` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `base_of_code` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `image_base` | `crate::endian::U64<crate::endian::LittleEndian>` |  |
| `section_alignment` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `file_alignment` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `major_operating_system_version` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `minor_operating_system_version` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `major_image_version` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `minor_image_version` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `major_subsystem_version` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `minor_subsystem_version` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `win32_version_value` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `size_of_image` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `size_of_headers` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `check_sum` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `subsystem` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `dll_characteristics` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `size_of_stack_reserve` | `crate::endian::U64<crate::endian::LittleEndian>` |  |
| `size_of_stack_commit` | `crate::endian::U64<crate::endian::LittleEndian>` |  |
| `size_of_heap_reserve` | `crate::endian::U64<crate::endian::LittleEndian>` |  |
| `size_of_heap_commit` | `crate::endian::U64<crate::endian::LittleEndian>` |  |
| `loader_flags` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `number_of_rva_and_sizes` | `crate::endian::U32<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **Copy**
- **Freeze**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Unpin**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **ImageOptionalHeader**
  - ```rust
    fn magic(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn major_linker_version(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn minor_linker_version(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn size_of_code(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn size_of_initialized_data(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn size_of_uninitialized_data(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn address_of_entry_point(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn base_of_code(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn base_of_data(self: &Self) -> Option<u32> { /* ... */ }
    ```

  - ```rust
    fn image_base(self: &Self) -> u64 { /* ... */ }
    ```

  - ```rust
    fn section_alignment(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn file_alignment(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn major_operating_system_version(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn minor_operating_system_version(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn major_image_version(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn minor_image_version(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn major_subsystem_version(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn minor_subsystem_version(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn win32_version_value(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn size_of_image(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn size_of_headers(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn check_sum(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn subsystem(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn dll_characteristics(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn size_of_stack_reserve(self: &Self) -> u64 { /* ... */ }
    ```

  - ```rust
    fn size_of_stack_commit(self: &Self) -> u64 { /* ... */ }
    ```

  - ```rust
    fn size_of_heap_reserve(self: &Self) -> u64 { /* ... */ }
    ```

  - ```rust
    fn size_of_heap_commit(self: &Self) -> u64 { /* ... */ }
    ```

  - ```rust
    fn loader_flags(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn number_of_rva_and_sizes(self: &Self) -> u32 { /* ... */ }
    ```

- **Send**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **UnwindSafe**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **RefUnwindSafe**
- **Sync**
- **Pod**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageOptionalHeader64 { /* ... */ }
    ```

#### Struct `ImageNtHeaders64`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageNtHeaders64 {
    pub signature: crate::endian::U32<crate::endian::LittleEndian>,
    pub file_header: ImageFileHeader,
    pub optional_header: ImageOptionalHeader64,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `signature` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `file_header` | `ImageFileHeader` |  |
| `optional_header` | `ImageOptionalHeader64` |  |

##### Implementations

###### Trait Implementations

- **Send**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageNtHeaders64 { /* ... */ }
    ```

- **UnwindSafe**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **ImageNtHeaders**
  - ```rust
    fn is_type_64(self: &Self) -> bool { /* ... */ }
    ```

  - ```rust
    fn is_valid_optional_magic(self: &Self) -> bool { /* ... */ }
    ```

  - ```rust
    fn signature(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn file_header(self: &Self) -> &pe::ImageFileHeader { /* ... */ }
    ```

  - ```rust
    fn optional_header(self: &Self) -> &<Self as >::ImageOptionalHeader { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **RefUnwindSafe**
- **Sync**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Copy**
- **Unpin**
- **Freeze**
- **Pod**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

#### Struct `ImageNtHeaders32`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageNtHeaders32 {
    pub signature: crate::endian::U32<crate::endian::LittleEndian>,
    pub file_header: ImageFileHeader,
    pub optional_header: ImageOptionalHeader32,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `signature` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `file_header` | `ImageFileHeader` |  |
| `optional_header` | `ImageOptionalHeader32` |  |

##### Implementations

###### Trait Implementations

- **ImageNtHeaders**
  - ```rust
    fn is_type_64(self: &Self) -> bool { /* ... */ }
    ```

  - ```rust
    fn is_valid_optional_magic(self: &Self) -> bool { /* ... */ }
    ```

  - ```rust
    fn signature(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn file_header(self: &Self) -> &pe::ImageFileHeader { /* ... */ }
    ```

  - ```rust
    fn optional_header(self: &Self) -> &<Self as >::ImageOptionalHeader { /* ... */ }
    ```

- **Freeze**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Pod**
- **Copy**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Unpin**
- **Sync**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageNtHeaders32 { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **RefUnwindSafe**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **UnwindSafe**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Send**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

#### Struct `ImageRomHeaders`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageRomHeaders {
    pub file_header: ImageFileHeader,
    pub optional_header: ImageRomOptionalHeader,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `file_header` | `ImageFileHeader` |  |
| `optional_header` | `ImageRomOptionalHeader` |  |

##### Implementations

###### Trait Implementations

- **RefUnwindSafe**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **UnwindSafe**
- **Copy**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Unpin**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Freeze**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageRomHeaders { /* ... */ }
    ```

- **Send**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Pod**
- **Sync**
#### Struct `Guid`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct Guid(pub [u8; 16]);
```

##### Fields

| Index | Type | Documentation |
|-------|------|---------------|
| 0 | `[u8; 16]` |  |

##### Implementations

###### Methods

- ```rust
  pub fn data1(self: Self) -> U32<LE> { /* ... */ }
  ```

- ```rust
  pub fn data2(self: Self) -> U16<LE> { /* ... */ }
  ```

- ```rust
  pub fn data3(self: Self) -> U16<LE> { /* ... */ }
  ```

- ```rust
  pub fn data4(self: Self) -> [u8; 8] { /* ... */ }
  ```

###### Trait Implementations

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **UnwindSafe**
- **Sync**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Send**
- **Freeze**
- **PartialEq**
  - ```rust
    fn eq(self: &Self, other: &Guid) -> bool { /* ... */ }
    ```

- **Unpin**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **StructuralPartialEq**
- **Eq**
- **RefUnwindSafe**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> Guid { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Pod**
- **Copy**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

#### Struct `AnonObjectHeader`

**Attributes:**

- `#[repr(C)]`

Non-COFF Object file header

```rust
pub struct AnonObjectHeader {
    pub sig1: crate::endian::U16<crate::endian::LittleEndian>,
    pub sig2: crate::endian::U16<crate::endian::LittleEndian>,
    pub version: crate::endian::U16<crate::endian::LittleEndian>,
    pub machine: crate::endian::U16<crate::endian::LittleEndian>,
    pub time_date_stamp: crate::endian::U32<crate::endian::LittleEndian>,
    pub class_id: ClsId,
    pub size_of_data: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `sig1` | `crate::endian::U16<crate::endian::LittleEndian>` | Must be IMAGE_FILE_MACHINE_UNKNOWN |
| `sig2` | `crate::endian::U16<crate::endian::LittleEndian>` | Must be 0xffff |
| `version` | `crate::endian::U16<crate::endian::LittleEndian>` | >= 1 (implies the ClsId field is present) |
| `machine` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `time_date_stamp` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `class_id` | `ClsId` | Used to invoke CoCreateInstance |
| `size_of_data` | `crate::endian::U32<crate::endian::LittleEndian>` | Size of data that follows the header |

##### Implementations

###### Trait Implementations

- **Unpin**
- **Sync**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Send**
- **Pod**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **RefUnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **UnwindSafe**
- **Freeze**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> AnonObjectHeader { /* ... */ }
    ```

- **Copy**
#### Struct `AnonObjectHeaderV2`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct AnonObjectHeaderV2 {
    pub sig1: crate::endian::U16<crate::endian::LittleEndian>,
    pub sig2: crate::endian::U16<crate::endian::LittleEndian>,
    pub version: crate::endian::U16<crate::endian::LittleEndian>,
    pub machine: crate::endian::U16<crate::endian::LittleEndian>,
    pub time_date_stamp: crate::endian::U32<crate::endian::LittleEndian>,
    pub class_id: ClsId,
    pub size_of_data: crate::endian::U32<crate::endian::LittleEndian>,
    pub flags: crate::endian::U32<crate::endian::LittleEndian>,
    pub meta_data_size: crate::endian::U32<crate::endian::LittleEndian>,
    pub meta_data_offset: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `sig1` | `crate::endian::U16<crate::endian::LittleEndian>` | Must be IMAGE_FILE_MACHINE_UNKNOWN |
| `sig2` | `crate::endian::U16<crate::endian::LittleEndian>` | Must be 0xffff |
| `version` | `crate::endian::U16<crate::endian::LittleEndian>` | >= 2 (implies the Flags field is present - otherwise V1) |
| `machine` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `time_date_stamp` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `class_id` | `ClsId` | Used to invoke CoCreateInstance |
| `size_of_data` | `crate::endian::U32<crate::endian::LittleEndian>` | Size of data that follows the header |
| `flags` | `crate::endian::U32<crate::endian::LittleEndian>` | 0x1 -> contains metadata |
| `meta_data_size` | `crate::endian::U32<crate::endian::LittleEndian>` | Size of CLR metadata |
| `meta_data_offset` | `crate::endian::U32<crate::endian::LittleEndian>` | Offset of CLR metadata |

##### Implementations

###### Trait Implementations

- **Pod**
- **UnwindSafe**
- **Freeze**
- **RefUnwindSafe**
- **Copy**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Sync**
- **Unpin**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> AnonObjectHeaderV2 { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Send**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

#### Struct `AnonObjectHeaderBigobj`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct AnonObjectHeaderBigobj {
    pub sig1: crate::endian::U16<crate::endian::LittleEndian>,
    pub sig2: crate::endian::U16<crate::endian::LittleEndian>,
    pub version: crate::endian::U16<crate::endian::LittleEndian>,
    pub machine: crate::endian::U16<crate::endian::LittleEndian>,
    pub time_date_stamp: crate::endian::U32<crate::endian::LittleEndian>,
    pub class_id: ClsId,
    pub size_of_data: crate::endian::U32<crate::endian::LittleEndian>,
    pub flags: crate::endian::U32<crate::endian::LittleEndian>,
    pub meta_data_size: crate::endian::U32<crate::endian::LittleEndian>,
    pub meta_data_offset: crate::endian::U32<crate::endian::LittleEndian>,
    pub number_of_sections: crate::endian::U32<crate::endian::LittleEndian>,
    pub pointer_to_symbol_table: crate::endian::U32<crate::endian::LittleEndian>,
    pub number_of_symbols: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `sig1` | `crate::endian::U16<crate::endian::LittleEndian>` | Must be IMAGE_FILE_MACHINE_UNKNOWN |
| `sig2` | `crate::endian::U16<crate::endian::LittleEndian>` | Must be 0xffff |
| `version` | `crate::endian::U16<crate::endian::LittleEndian>` | >= 2 (implies the Flags field is present) |
| `machine` | `crate::endian::U16<crate::endian::LittleEndian>` | Actual machine - IMAGE_FILE_MACHINE_xxx |
| `time_date_stamp` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `class_id` | `ClsId` | Must be `ANON_OBJECT_HEADER_BIGOBJ_CLASS_ID`. |
| `size_of_data` | `crate::endian::U32<crate::endian::LittleEndian>` | Size of data that follows the header |
| `flags` | `crate::endian::U32<crate::endian::LittleEndian>` | 0x1 -> contains metadata |
| `meta_data_size` | `crate::endian::U32<crate::endian::LittleEndian>` | Size of CLR metadata |
| `meta_data_offset` | `crate::endian::U32<crate::endian::LittleEndian>` | Offset of CLR metadata |
| `number_of_sections` | `crate::endian::U32<crate::endian::LittleEndian>` | extended from WORD |
| `pointer_to_symbol_table` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `number_of_symbols` | `crate::endian::U32<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **Send**
- **Unpin**
- **UnwindSafe**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Sync**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **CoffHeader**
  - ```rust
    fn is_type_bigobj() -> bool { /* ... */ }
    ```

  - ```rust
    fn machine(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn number_of_sections(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn pointer_to_symbol_table(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn number_of_symbols(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn characteristics(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn parse<''data, R: ReadRef<''data>>(data: R, offset: &mut u64) -> read::Result<&''data Self> { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Freeze**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **RefUnwindSafe**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Pod**
- **Copy**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> AnonObjectHeaderBigobj { /* ... */ }
    ```

#### Struct `ImageSectionHeader`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageSectionHeader {
    pub name: [u8; 8],
    pub virtual_size: crate::endian::U32<crate::endian::LittleEndian>,
    pub virtual_address: crate::endian::U32<crate::endian::LittleEndian>,
    pub size_of_raw_data: crate::endian::U32<crate::endian::LittleEndian>,
    pub pointer_to_raw_data: crate::endian::U32<crate::endian::LittleEndian>,
    pub pointer_to_relocations: crate::endian::U32<crate::endian::LittleEndian>,
    pub pointer_to_linenumbers: crate::endian::U32<crate::endian::LittleEndian>,
    pub number_of_relocations: crate::endian::U16<crate::endian::LittleEndian>,
    pub number_of_linenumbers: crate::endian::U16<crate::endian::LittleEndian>,
    pub characteristics: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `name` | `[u8; 8]` |  |
| `virtual_size` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `virtual_address` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `size_of_raw_data` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `pointer_to_raw_data` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `pointer_to_relocations` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `pointer_to_linenumbers` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `number_of_relocations` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `number_of_linenumbers` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `characteristics` | `crate::endian::U32<crate::endian::LittleEndian>` |  |

##### Implementations

###### Methods

- ```rust
  pub fn name_offset(self: &Self) -> Result<Option<u32>> { /* ... */ }
  ```
  Return the string table offset of the section name.

- ```rust
  pub fn name<''data, R: ReadRef<''data>>(self: &''data Self, strings: StringTable<''data, R>) -> Result<&''data [u8]> { /* ... */ }
  ```
  Return the section name.

- ```rust
  pub fn raw_name(self: &Self) -> &[u8] { /* ... */ }
  ```
  Return the raw section name.

- ```rust
  pub fn coff_file_range(self: &Self) -> Option<(u32, u32)> { /* ... */ }
  ```
  Return the offset and size of the section in a COFF file.

- ```rust
  pub fn coff_data<''data, R: ReadRef<''data>>(self: &Self, data: R) -> result::Result<&''data [u8], ()> { /* ... */ }
  ```
  Return the section data in a COFF file.

- ```rust
  pub fn coff_alignment(self: &Self) -> u64 { /* ... */ }
  ```
  Return the section alignment in bytes.

- ```rust
  pub fn coff_relocations<''data, R: ReadRef<''data>>(self: &Self, data: R) -> read::Result<&''data [pe::ImageRelocation]> { /* ... */ }
  ```
  Read the relocations in a COFF file.

- ```rust
  pub fn pe_file_range(self: &Self) -> (u32, u32) { /* ... */ }
  ```
  Return the offset and size of the section in a PE file.

- ```rust
  pub fn pe_file_range_at(self: &Self, va: u32) -> Option<(u32, u32)> { /* ... */ }
  ```
  Return the file offset of the given virtual address, and the remaining size up

- ```rust
  pub fn pe_address_range(self: &Self) -> (u32, u32) { /* ... */ }
  ```
  Return the virtual address and size of the section.

- ```rust
  pub fn pe_data<''data, R: ReadRef<''data>>(self: &Self, data: R) -> Result<&''data [u8]> { /* ... */ }
  ```
  Return the section data in a PE file.

- ```rust
  pub fn pe_data_at<''data, R: ReadRef<''data>>(self: &Self, data: R, va: u32) -> Option<&''data [u8]> { /* ... */ }
  ```
  Return the data starting at the given virtual address, up to the end of the

- ```rust
  pub fn contains_rva(self: &Self, va: u32) -> bool { /* ... */ }
  ```
  Tests whether a given RVA is part of this section

- ```rust
  pub fn pe_data_containing<''data, R: ReadRef<''data>>(self: &Self, data: R, va: u32) -> Option<(&''data [u8], u32)> { /* ... */ }
  ```
  Return the section data if it contains the given virtual address.

###### Trait Implementations

- **Sync**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **RefUnwindSafe**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageSectionHeader { /* ... */ }
    ```

- **Send**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Pod**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **UnwindSafe**
- **Default**
  - ```rust
    fn default() -> ImageSectionHeader { /* ... */ }
    ```

- **Freeze**
- **Unpin**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Copy**
#### Struct `ImageSymbol`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageSymbol {
    pub name: [u8; 8],
    pub value: crate::endian::U32Bytes<crate::endian::LittleEndian>,
    pub section_number: crate::endian::U16Bytes<crate::endian::LittleEndian>,
    pub typ: crate::endian::U16Bytes<crate::endian::LittleEndian>,
    pub storage_class: u8,
    pub number_of_aux_symbols: u8,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `name` | `[u8; 8]` | If first 4 bytes are 0, then second 4 bytes are offset into string table. |
| `value` | `crate::endian::U32Bytes<crate::endian::LittleEndian>` |  |
| `section_number` | `crate::endian::U16Bytes<crate::endian::LittleEndian>` |  |
| `typ` | `crate::endian::U16Bytes<crate::endian::LittleEndian>` |  |
| `storage_class` | `u8` |  |
| `number_of_aux_symbols` | `u8` |  |

##### Implementations

###### Trait Implementations

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Send**
- **Unpin**
- **UnwindSafe**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageSymbol { /* ... */ }
    ```

- **Sync**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Copy**
- **Pod**
- **Freeze**
- **RefUnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **ImageSymbol**
  - ```rust
    fn raw_name(self: &Self) -> &[u8; 8] { /* ... */ }
    ```

  - ```rust
    fn value(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn section_number(self: &Self) -> i32 { /* ... */ }
    ```

  - ```rust
    fn typ(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn storage_class(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn number_of_aux_symbols(self: &Self) -> u8 { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

#### Struct `ImageSymbolBytes`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageSymbolBytes(pub [u8; 18]);
```

##### Fields

| Index | Type | Documentation |
|-------|------|---------------|
| 0 | `[u8; 18]` |  |

##### Implementations

###### Trait Implementations

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Pod**
- **RefUnwindSafe**
- **Sync**
- **Send**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **UnwindSafe**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageSymbolBytes { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Freeze**
- **Copy**
- **Unpin**
#### Struct `ImageSymbolEx`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageSymbolEx {
    pub name: [u8; 8],
    pub value: crate::endian::U32Bytes<crate::endian::LittleEndian>,
    pub section_number: crate::endian::I32Bytes<crate::endian::LittleEndian>,
    pub typ: crate::endian::U16Bytes<crate::endian::LittleEndian>,
    pub storage_class: u8,
    pub number_of_aux_symbols: u8,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `name` | `[u8; 8]` | If first 4 bytes are 0, then second 4 bytes are offset into string table. |
| `value` | `crate::endian::U32Bytes<crate::endian::LittleEndian>` |  |
| `section_number` | `crate::endian::I32Bytes<crate::endian::LittleEndian>` |  |
| `typ` | `crate::endian::U16Bytes<crate::endian::LittleEndian>` |  |
| `storage_class` | `u8` |  |
| `number_of_aux_symbols` | `u8` |  |

##### Implementations

###### Trait Implementations

- **UnwindSafe**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **RefUnwindSafe**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageSymbolEx { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Freeze**
- **Send**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Unpin**
- **ImageSymbol**
  - ```rust
    fn raw_name(self: &Self) -> &[u8; 8] { /* ... */ }
    ```

  - ```rust
    fn value(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn section_number(self: &Self) -> i32 { /* ... */ }
    ```

  - ```rust
    fn typ(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn storage_class(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn number_of_aux_symbols(self: &Self) -> u8 { /* ... */ }
    ```

- **Copy**
- **Sync**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Pod**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

#### Struct `ImageSymbolExBytes`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageSymbolExBytes(pub [u8; 20]);
```

##### Fields

| Index | Type | Documentation |
|-------|------|---------------|
| 0 | `[u8; 20]` |  |

##### Implementations

###### Trait Implementations

- **Freeze**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **RefUnwindSafe**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Pod**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Sync**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageSymbolExBytes { /* ... */ }
    ```

- **Copy**
- **Send**
- **Unpin**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **UnwindSafe**
#### Struct `ImageAuxSymbolTokenDef`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageAuxSymbolTokenDef {
    pub aux_type: u8,
    pub reserved1: u8,
    pub symbol_table_index: crate::endian::U32Bytes<crate::endian::LittleEndian>,
    pub reserved2: [u8; 12],
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `aux_type` | `u8` | IMAGE_AUX_SYMBOL_TYPE |
| `reserved1` | `u8` | Must be 0 |
| `symbol_table_index` | `crate::endian::U32Bytes<crate::endian::LittleEndian>` |  |
| `reserved2` | `[u8; 12]` | Must be 0 |

##### Implementations

###### Trait Implementations

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageAuxSymbolTokenDef { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **UnwindSafe**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Pod**
- **Sync**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Freeze**
- **Send**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Unpin**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Copy**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **RefUnwindSafe**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

#### Struct `ImageAuxSymbolFunction`

**Attributes:**

- `#[repr(C)]`

Auxiliary symbol format 1: function definitions.

```rust
pub struct ImageAuxSymbolFunction {
    pub tag_index: crate::endian::U32Bytes<crate::endian::LittleEndian>,
    pub total_size: crate::endian::U32Bytes<crate::endian::LittleEndian>,
    pub pointer_to_linenumber: crate::endian::U32Bytes<crate::endian::LittleEndian>,
    pub pointer_to_next_function: crate::endian::U32Bytes<crate::endian::LittleEndian>,
    pub unused: [u8; 2],
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `tag_index` | `crate::endian::U32Bytes<crate::endian::LittleEndian>` |  |
| `total_size` | `crate::endian::U32Bytes<crate::endian::LittleEndian>` |  |
| `pointer_to_linenumber` | `crate::endian::U32Bytes<crate::endian::LittleEndian>` |  |
| `pointer_to_next_function` | `crate::endian::U32Bytes<crate::endian::LittleEndian>` |  |
| `unused` | `[u8; 2]` |  |

##### Implementations

###### Trait Implementations

- **Send**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Sync**
- **Copy**
- **RefUnwindSafe**
- **Freeze**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Unpin**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Pod**
- **UnwindSafe**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageAuxSymbolFunction { /* ... */ }
    ```

#### Struct `ImageAuxSymbolFunctionBeginEnd`

**Attributes:**

- `#[repr(C)]`

Auxiliary symbol format 2: .bf and .ef symbols.

```rust
pub struct ImageAuxSymbolFunctionBeginEnd {
    pub unused1: [u8; 4],
    pub linenumber: crate::endian::U16Bytes<crate::endian::LittleEndian>,
    pub unused2: [u8; 6],
    pub pointer_to_next_function: crate::endian::U32Bytes<crate::endian::LittleEndian>,
    pub unused3: [u8; 2],
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `unused1` | `[u8; 4]` |  |
| `linenumber` | `crate::endian::U16Bytes<crate::endian::LittleEndian>` | declaration line number |
| `unused2` | `[u8; 6]` |  |
| `pointer_to_next_function` | `crate::endian::U32Bytes<crate::endian::LittleEndian>` |  |
| `unused3` | `[u8; 2]` |  |

##### Implementations

###### Trait Implementations

- **Freeze**
- **Unpin**
- **RefUnwindSafe**
- **Pod**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageAuxSymbolFunctionBeginEnd { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **UnwindSafe**
- **Sync**
- **Send**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Copy**
#### Struct `ImageAuxSymbolWeak`

**Attributes:**

- `#[repr(C)]`

Auxiliary symbol format 3: weak externals.

Used for both `ImageSymbol` and `ImageSymbolEx` (both with padding).

```rust
pub struct ImageAuxSymbolWeak {
    pub weak_default_sym_index: crate::endian::U32Bytes<crate::endian::LittleEndian>,
    pub weak_search_type: crate::endian::U32Bytes<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `weak_default_sym_index` | `crate::endian::U32Bytes<crate::endian::LittleEndian>` | the weak extern default symbol index |
| `weak_search_type` | `crate::endian::U32Bytes<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **RefUnwindSafe**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **UnwindSafe**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Unpin**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Pod**
- **Send**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Sync**
- **Copy**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageAuxSymbolWeak { /* ... */ }
    ```

- **Freeze**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

#### Struct `ImageAuxSymbolSection`

**Attributes:**

- `#[repr(C)]`

Auxiliary symbol format 5: sections.

Used for both `ImageSymbol` and `ImageSymbolEx` (with padding).

```rust
pub struct ImageAuxSymbolSection {
    pub length: crate::endian::U32Bytes<crate::endian::LittleEndian>,
    pub number_of_relocations: crate::endian::U16Bytes<crate::endian::LittleEndian>,
    pub number_of_linenumbers: crate::endian::U16Bytes<crate::endian::LittleEndian>,
    pub check_sum: crate::endian::U32Bytes<crate::endian::LittleEndian>,
    pub number: crate::endian::U16Bytes<crate::endian::LittleEndian>,
    pub selection: u8,
    pub reserved: u8,
    pub high_number: crate::endian::U16Bytes<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `length` | `crate::endian::U32Bytes<crate::endian::LittleEndian>` | section length |
| `number_of_relocations` | `crate::endian::U16Bytes<crate::endian::LittleEndian>` | number of relocation entries |
| `number_of_linenumbers` | `crate::endian::U16Bytes<crate::endian::LittleEndian>` | number of line numbers |
| `check_sum` | `crate::endian::U32Bytes<crate::endian::LittleEndian>` | checksum for communal |
| `number` | `crate::endian::U16Bytes<crate::endian::LittleEndian>` | section number to associate with |
| `selection` | `u8` | communal selection type |
| `reserved` | `u8` |  |
| `high_number` | `crate::endian::U16Bytes<crate::endian::LittleEndian>` | high bits of the section number |

##### Implementations

###### Trait Implementations

- **Unpin**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Send**
- **UnwindSafe**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageAuxSymbolSection { /* ... */ }
    ```

- **RefUnwindSafe**
- **Sync**
- **Copy**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Pod**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Freeze**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

#### Struct `ImageAuxSymbolCrc`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageAuxSymbolCrc {
    pub crc: crate::endian::U32Bytes<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `crc` | `crate::endian::U32Bytes<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageAuxSymbolCrc { /* ... */ }
    ```

- **Copy**
- **Freeze**
- **Pod**
- **Send**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Unpin**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **RefUnwindSafe**
- **Sync**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **UnwindSafe**
#### Struct `ImageRelocation`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageRelocation {
    pub virtual_address: crate::endian::U32Bytes<crate::endian::LittleEndian>,
    pub symbol_table_index: crate::endian::U32Bytes<crate::endian::LittleEndian>,
    pub typ: crate::endian::U16Bytes<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `virtual_address` | `crate::endian::U32Bytes<crate::endian::LittleEndian>` | Also `RelocCount` when IMAGE_SCN_LNK_NRELOC_OVFL is set |
| `symbol_table_index` | `crate::endian::U32Bytes<crate::endian::LittleEndian>` |  |
| `typ` | `crate::endian::U16Bytes<crate::endian::LittleEndian>` |  |

##### Implementations

###### Methods

- ```rust
  pub fn symbol(self: &Self) -> SymbolIndex { /* ... */ }
  ```
  Get the index of the symbol referenced by this relocation.

###### Trait Implementations

- **Sync**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Copy**
- **Pod**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **UnwindSafe**
- **Send**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **RefUnwindSafe**
- **Freeze**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageRelocation { /* ... */ }
    ```

- **Unpin**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

#### Struct `ImageLinenumber`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageLinenumber {
    pub symbol_table_index_or_virtual_address: crate::endian::U32Bytes<crate::endian::LittleEndian>,
    pub linenumber: crate::endian::U16Bytes<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `symbol_table_index_or_virtual_address` | `crate::endian::U32Bytes<crate::endian::LittleEndian>` | Symbol table index of function name if Linenumber is 0.<br>Otherwise virtual address of line number. |
| `linenumber` | `crate::endian::U16Bytes<crate::endian::LittleEndian>` | Line number. |

##### Implementations

###### Trait Implementations

- **Copy**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **UnwindSafe**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Freeze**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Sync**
- **Unpin**
- **Send**
- **RefUnwindSafe**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageLinenumber { /* ... */ }
    ```

- **Pod**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

#### Struct `ImageBaseRelocation`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageBaseRelocation {
    pub virtual_address: crate::endian::U32<crate::endian::LittleEndian>,
    pub size_of_block: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `virtual_address` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `size_of_block` | `crate::endian::U32<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Pod**
- **Unpin**
- **Sync**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **UnwindSafe**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Copy**
- **RefUnwindSafe**
- **Send**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Freeze**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageBaseRelocation { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

#### Struct `ImageArchiveMemberHeader`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageArchiveMemberHeader {
    pub name: [u8; 16],
    pub date: [u8; 12],
    pub user_id: [u8; 6],
    pub group_id: [u8; 6],
    pub mode: [u8; 8],
    pub size: [u8; 10],
    pub end_header: [u8; 2],
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `name` | `[u8; 16]` | File member name - `/' terminated. |
| `date` | `[u8; 12]` | File member date - decimal. |
| `user_id` | `[u8; 6]` | File member user id - decimal. |
| `group_id` | `[u8; 6]` | File member group id - decimal. |
| `mode` | `[u8; 8]` | File member mode - octal. |
| `size` | `[u8; 10]` | File member size - decimal. |
| `end_header` | `[u8; 2]` | String to end header. |

##### Implementations

###### Trait Implementations

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Freeze**
- **Send**
- **Copy**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageArchiveMemberHeader { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Pod**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Unpin**
- **Sync**
- **UnwindSafe**
- **RefUnwindSafe**
#### Struct `ImageExportDirectory`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageExportDirectory {
    pub characteristics: crate::endian::U32<crate::endian::LittleEndian>,
    pub time_date_stamp: crate::endian::U32<crate::endian::LittleEndian>,
    pub major_version: crate::endian::U16<crate::endian::LittleEndian>,
    pub minor_version: crate::endian::U16<crate::endian::LittleEndian>,
    pub name: crate::endian::U32<crate::endian::LittleEndian>,
    pub base: crate::endian::U32<crate::endian::LittleEndian>,
    pub number_of_functions: crate::endian::U32<crate::endian::LittleEndian>,
    pub number_of_names: crate::endian::U32<crate::endian::LittleEndian>,
    pub address_of_functions: crate::endian::U32<crate::endian::LittleEndian>,
    pub address_of_names: crate::endian::U32<crate::endian::LittleEndian>,
    pub address_of_name_ordinals: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `characteristics` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `time_date_stamp` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `major_version` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `minor_version` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `name` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `base` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `number_of_functions` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `number_of_names` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `address_of_functions` | `crate::endian::U32<crate::endian::LittleEndian>` | RVA from base of image |
| `address_of_names` | `crate::endian::U32<crate::endian::LittleEndian>` | RVA from base of image |
| `address_of_name_ordinals` | `crate::endian::U32<crate::endian::LittleEndian>` | RVA from base of image |

##### Implementations

###### Trait Implementations

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **UnwindSafe**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Copy**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Sync**
- **Unpin**
- **RefUnwindSafe**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Send**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageExportDirectory { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Freeze**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Pod**
#### Struct `ImageImportByName`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageImportByName {
    pub hint: crate::endian::U16<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `hint` | `crate::endian::U16<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **Unpin**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Pod**
- **UnwindSafe**
- **RefUnwindSafe**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Freeze**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageImportByName { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Copy**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Sync**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Send**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

#### Struct `ImageThunkData64`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageThunkData64(pub crate::endian::U64<crate::endian::LittleEndian>);
```

##### Fields

| Index | Type | Documentation |
|-------|------|---------------|
| 0 | `crate::endian::U64<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **Unpin**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageThunkData64 { /* ... */ }
    ```

- **RefUnwindSafe**
- **Sync**
- **Copy**
- **Freeze**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Pod**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **UnwindSafe**
- **Send**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **ImageThunkData**
  - ```rust
    fn raw(self: Self) -> u64 { /* ... */ }
    ```

  - ```rust
    fn is_ordinal(self: Self) -> bool { /* ... */ }
    ```

  - ```rust
    fn ordinal(self: Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn address(self: Self) -> u32 { /* ... */ }
    ```

#### Struct `ImageThunkData32`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageThunkData32(pub crate::endian::U32<crate::endian::LittleEndian>);
```

##### Fields

| Index | Type | Documentation |
|-------|------|---------------|
| 0 | `crate::endian::U32<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **UnwindSafe**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageThunkData32 { /* ... */ }
    ```

- **Send**
- **Unpin**
- **Copy**
- **Sync**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **ImageThunkData**
  - ```rust
    fn raw(self: Self) -> u64 { /* ... */ }
    ```

  - ```rust
    fn is_ordinal(self: Self) -> bool { /* ... */ }
    ```

  - ```rust
    fn ordinal(self: Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn address(self: Self) -> u32 { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Pod**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **RefUnwindSafe**
- **Freeze**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

#### Struct `ImageTlsDirectory64`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageTlsDirectory64 {
    pub start_address_of_raw_data: crate::endian::U64<crate::endian::LittleEndian>,
    pub end_address_of_raw_data: crate::endian::U64<crate::endian::LittleEndian>,
    pub address_of_index: crate::endian::U64<crate::endian::LittleEndian>,
    pub address_of_call_backs: crate::endian::U64<crate::endian::LittleEndian>,
    pub size_of_zero_fill: crate::endian::U32<crate::endian::LittleEndian>,
    pub characteristics: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `start_address_of_raw_data` | `crate::endian::U64<crate::endian::LittleEndian>` |  |
| `end_address_of_raw_data` | `crate::endian::U64<crate::endian::LittleEndian>` |  |
| `address_of_index` | `crate::endian::U64<crate::endian::LittleEndian>` | PDWORD |
| `address_of_call_backs` | `crate::endian::U64<crate::endian::LittleEndian>` | PIMAGE_TLS_CALLBACK *; |
| `size_of_zero_fill` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `characteristics` | `crate::endian::U32<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Copy**
- **Pod**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageTlsDirectory64 { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Unpin**
- **Freeze**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **UnwindSafe**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **RefUnwindSafe**
- **Sync**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Send**
#### Struct `ImageTlsDirectory32`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageTlsDirectory32 {
    pub start_address_of_raw_data: crate::endian::U32<crate::endian::LittleEndian>,
    pub end_address_of_raw_data: crate::endian::U32<crate::endian::LittleEndian>,
    pub address_of_index: crate::endian::U32<crate::endian::LittleEndian>,
    pub address_of_call_backs: crate::endian::U32<crate::endian::LittleEndian>,
    pub size_of_zero_fill: crate::endian::U32<crate::endian::LittleEndian>,
    pub characteristics: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `start_address_of_raw_data` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `end_address_of_raw_data` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `address_of_index` | `crate::endian::U32<crate::endian::LittleEndian>` | PDWORD |
| `address_of_call_backs` | `crate::endian::U32<crate::endian::LittleEndian>` | PIMAGE_TLS_CALLBACK * |
| `size_of_zero_fill` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `characteristics` | `crate::endian::U32<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Freeze**
- **Unpin**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageTlsDirectory32 { /* ... */ }
    ```

- **Pod**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Sync**
- **UnwindSafe**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **RefUnwindSafe**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Send**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Copy**
#### Struct `ImageImportDescriptor`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageImportDescriptor {
    pub original_first_thunk: crate::endian::U32Bytes<crate::endian::LittleEndian>,
    pub time_date_stamp: crate::endian::U32Bytes<crate::endian::LittleEndian>,
    pub forwarder_chain: crate::endian::U32Bytes<crate::endian::LittleEndian>,
    pub name: crate::endian::U32Bytes<crate::endian::LittleEndian>,
    pub first_thunk: crate::endian::U32Bytes<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `original_first_thunk` | `crate::endian::U32Bytes<crate::endian::LittleEndian>` | RVA to original unbound IAT (`ImageThunkData32`/`ImageThunkData64`)<br>0 for terminating null import descriptor |
| `time_date_stamp` | `crate::endian::U32Bytes<crate::endian::LittleEndian>` | 0 if not bound,<br>-1 if bound, and real date\time stamp<br>    in IMAGE_DIRECTORY_ENTRY_BOUND_IMPORT (new BIND)<br>O.W. date/time stamp of DLL bound to (Old BIND) |
| `forwarder_chain` | `crate::endian::U32Bytes<crate::endian::LittleEndian>` | -1 if no forwarders |
| `name` | `crate::endian::U32Bytes<crate::endian::LittleEndian>` |  |
| `first_thunk` | `crate::endian::U32Bytes<crate::endian::LittleEndian>` | RVA to IAT (if bound this IAT has actual addresses) |

##### Implementations

###### Methods

- ```rust
  pub fn is_null(self: &Self) -> bool { /* ... */ }
  ```
  Tell whether this import descriptor is the null descriptor

###### Trait Implementations

- **Unpin**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **UnwindSafe**
- **Freeze**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Sync**
- **RefUnwindSafe**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageImportDescriptor { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Copy**
- **Send**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Pod**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

#### Struct `ImageBoundImportDescriptor`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageBoundImportDescriptor {
    pub time_date_stamp: crate::endian::U32<crate::endian::LittleEndian>,
    pub offset_module_name: crate::endian::U16<crate::endian::LittleEndian>,
    pub number_of_module_forwarder_refs: crate::endian::U16<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `time_date_stamp` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `offset_module_name` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `number_of_module_forwarder_refs` | `crate::endian::U16<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **RefUnwindSafe**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Unpin**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Send**
- **Copy**
- **Freeze**
- **Sync**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Pod**
- **UnwindSafe**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageBoundImportDescriptor { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

#### Struct `ImageBoundForwarderRef`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageBoundForwarderRef {
    pub time_date_stamp: crate::endian::U32<crate::endian::LittleEndian>,
    pub offset_module_name: crate::endian::U16<crate::endian::LittleEndian>,
    pub reserved: crate::endian::U16<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `time_date_stamp` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `offset_module_name` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `reserved` | `crate::endian::U16<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **RefUnwindSafe**
- **Send**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Copy**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Unpin**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **UnwindSafe**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Sync**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageBoundForwarderRef { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Freeze**
- **Pod**
#### Struct `ImageDelayloadDescriptor`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageDelayloadDescriptor {
    pub attributes: crate::endian::U32<crate::endian::LittleEndian>,
    pub dll_name_rva: crate::endian::U32<crate::endian::LittleEndian>,
    pub module_handle_rva: crate::endian::U32<crate::endian::LittleEndian>,
    pub import_address_table_rva: crate::endian::U32<crate::endian::LittleEndian>,
    pub import_name_table_rva: crate::endian::U32<crate::endian::LittleEndian>,
    pub bound_import_address_table_rva: crate::endian::U32<crate::endian::LittleEndian>,
    pub unload_information_table_rva: crate::endian::U32<crate::endian::LittleEndian>,
    pub time_date_stamp: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `attributes` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `dll_name_rva` | `crate::endian::U32<crate::endian::LittleEndian>` | RVA to the name of the target library (NULL-terminate ASCII string) |
| `module_handle_rva` | `crate::endian::U32<crate::endian::LittleEndian>` | RVA to the HMODULE caching location (PHMODULE) |
| `import_address_table_rva` | `crate::endian::U32<crate::endian::LittleEndian>` | RVA to the start of the IAT (PIMAGE_THUNK_DATA) |
| `import_name_table_rva` | `crate::endian::U32<crate::endian::LittleEndian>` | RVA to the start of the name table (PIMAGE_THUNK_DATA::AddressOfData) |
| `bound_import_address_table_rva` | `crate::endian::U32<crate::endian::LittleEndian>` | RVA to an optional bound IAT |
| `unload_information_table_rva` | `crate::endian::U32<crate::endian::LittleEndian>` | RVA to an optional unload info table |
| `time_date_stamp` | `crate::endian::U32<crate::endian::LittleEndian>` | 0 if not bound, otherwise, date/time of the target DLL |

##### Implementations

###### Methods

- ```rust
  pub fn is_null(self: &Self) -> bool { /* ... */ }
  ```
  Tell whether this delay-load import descriptor is the null descriptor

###### Trait Implementations

- **Send**
- **RefUnwindSafe**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Pod**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Copy**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **UnwindSafe**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageDelayloadDescriptor { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Freeze**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Unpin**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Sync**
#### Struct `ImageResourceDirectory`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageResourceDirectory {
    pub characteristics: crate::endian::U32<crate::endian::LittleEndian>,
    pub time_date_stamp: crate::endian::U32<crate::endian::LittleEndian>,
    pub major_version: crate::endian::U16<crate::endian::LittleEndian>,
    pub minor_version: crate::endian::U16<crate::endian::LittleEndian>,
    pub number_of_named_entries: crate::endian::U16<crate::endian::LittleEndian>,
    pub number_of_id_entries: crate::endian::U16<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `characteristics` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `time_date_stamp` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `major_version` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `minor_version` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `number_of_named_entries` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `number_of_id_entries` | `crate::endian::U16<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Copy**
- **UnwindSafe**
- **Freeze**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Send**
- **RefUnwindSafe**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Pod**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Sync**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Unpin**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageResourceDirectory { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

#### Struct `ImageResourceDirectoryEntry`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageResourceDirectoryEntry {
    pub name_or_id: crate::endian::U32<crate::endian::LittleEndian>,
    pub offset_to_data_or_directory: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `name_or_id` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `offset_to_data_or_directory` | `crate::endian::U32<crate::endian::LittleEndian>` |  |

##### Implementations

###### Methods

- ```rust
  pub fn has_name(self: &Self) -> bool { /* ... */ }
  ```
  Returns true if the entry has a name, rather than an ID.

- ```rust
  pub fn name_or_id(self: &Self) -> ResourceNameOrId { /* ... */ }
  ```
  Returns the entry name

- ```rust
  pub fn is_table(self: &Self) -> bool { /* ... */ }
  ```
  Returns true if the entry is a subtable.

- ```rust
  pub fn data_offset(self: &Self) -> u32 { /* ... */ }
  ```
  Returns the section offset of the associated table or data.

- ```rust
  pub fn data<''data>(self: &Self, section: ResourceDirectory<''data>) -> Result<ResourceDirectoryEntryData<''data>> { /* ... */ }
  ```
  Returns the data associated to this directory entry.

###### Trait Implementations

- **UnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Freeze**
- **RefUnwindSafe**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Copy**
- **Send**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Unpin**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Pod**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageResourceDirectoryEntry { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Sync**
#### Struct `ImageResourceDirectoryString`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageResourceDirectoryString {
    pub length: crate::endian::U16<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `length` | `crate::endian::U16<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **RefUnwindSafe**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageResourceDirectoryString { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Pod**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Send**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Freeze**
- **Unpin**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **UnwindSafe**
- **Sync**
- **Copy**
#### Struct `ImageResourceDirStringU`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageResourceDirStringU {
    pub length: crate::endian::U16<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `length` | `crate::endian::U16<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **UnwindSafe**
- **Sync**
- **RefUnwindSafe**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageResourceDirStringU { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Unpin**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Send**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Copy**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Freeze**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Pod**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

#### Struct `ImageResourceDataEntry`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageResourceDataEntry {
    pub offset_to_data: crate::endian::U32<crate::endian::LittleEndian>,
    pub size: crate::endian::U32<crate::endian::LittleEndian>,
    pub code_page: crate::endian::U32<crate::endian::LittleEndian>,
    pub reserved: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `offset_to_data` | `crate::endian::U32<crate::endian::LittleEndian>` | RVA of the data. |
| `size` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `code_page` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `reserved` | `crate::endian::U32<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **Pod**
- **UnwindSafe**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Copy**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Send**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageResourceDataEntry { /* ... */ }
    ```

- **Freeze**
- **Unpin**
- **RefUnwindSafe**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Sync**
#### Struct `ImageLoadConfigCodeIntegrity`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageLoadConfigCodeIntegrity {
    pub flags: crate::endian::U16<crate::endian::LittleEndian>,
    pub catalog: crate::endian::U16<crate::endian::LittleEndian>,
    pub catalog_offset: crate::endian::U32<crate::endian::LittleEndian>,
    pub reserved: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `flags` | `crate::endian::U16<crate::endian::LittleEndian>` | Flags to indicate if CI information is available, etc. |
| `catalog` | `crate::endian::U16<crate::endian::LittleEndian>` | 0xFFFF means not available |
| `catalog_offset` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `reserved` | `crate::endian::U32<crate::endian::LittleEndian>` | Additional bitmask to be defined later |

##### Implementations

###### Trait Implementations

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageLoadConfigCodeIntegrity { /* ... */ }
    ```

- **Send**
- **UnwindSafe**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Pod**
- **Copy**
- **Unpin**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Sync**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Freeze**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **RefUnwindSafe**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

#### Struct `ImageDynamicRelocationTable`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageDynamicRelocationTable {
    pub version: crate::endian::U32<crate::endian::LittleEndian>,
    pub size: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `version` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `size` | `crate::endian::U32<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **Pod**
- **RefUnwindSafe**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **UnwindSafe**
- **Send**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageDynamicRelocationTable { /* ... */ }
    ```

- **Freeze**
- **Copy**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Sync**
- **Unpin**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

#### Struct `ImageDynamicRelocation32`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageDynamicRelocation32 {
    pub symbol: crate::endian::U32<crate::endian::LittleEndian>,
    pub base_reloc_size: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `symbol` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `base_reloc_size` | `crate::endian::U32<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **UnwindSafe**
- **Freeze**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Pod**
- **Sync**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Unpin**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageDynamicRelocation32 { /* ... */ }
    ```

- **RefUnwindSafe**
- **Send**
- **Copy**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

#### Struct `ImageDynamicRelocation64`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageDynamicRelocation64 {
    pub symbol: crate::endian::U64<crate::endian::LittleEndian>,
    pub base_reloc_size: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `symbol` | `crate::endian::U64<crate::endian::LittleEndian>` |  |
| `base_reloc_size` | `crate::endian::U32<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Sync**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **UnwindSafe**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Pod**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **RefUnwindSafe**
- **Freeze**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Send**
- **Unpin**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageDynamicRelocation64 { /* ... */ }
    ```

- **Copy**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

#### Struct `ImageDynamicRelocation32V2`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageDynamicRelocation32V2 {
    pub header_size: crate::endian::U32<crate::endian::LittleEndian>,
    pub fixup_info_size: crate::endian::U32<crate::endian::LittleEndian>,
    pub symbol: crate::endian::U32<crate::endian::LittleEndian>,
    pub symbol_group: crate::endian::U32<crate::endian::LittleEndian>,
    pub flags: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `header_size` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `fixup_info_size` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `symbol` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `symbol_group` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `flags` | `crate::endian::U32<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **Copy**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **RefUnwindSafe**
- **UnwindSafe**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Pod**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Sync**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageDynamicRelocation32V2 { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Freeze**
- **Send**
- **Unpin**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

#### Struct `ImageDynamicRelocation64V2`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageDynamicRelocation64V2 {
    pub header_size: crate::endian::U32<crate::endian::LittleEndian>,
    pub fixup_info_size: crate::endian::U32<crate::endian::LittleEndian>,
    pub symbol: crate::endian::U64<crate::endian::LittleEndian>,
    pub symbol_group: crate::endian::U32<crate::endian::LittleEndian>,
    pub flags: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `header_size` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `fixup_info_size` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `symbol` | `crate::endian::U64<crate::endian::LittleEndian>` |  |
| `symbol_group` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `flags` | `crate::endian::U32<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageDynamicRelocation64V2 { /* ... */ }
    ```

- **RefUnwindSafe**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Unpin**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Freeze**
- **Send**
- **Pod**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **UnwindSafe**
- **Copy**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Sync**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

#### Struct `ImagePrologueDynamicRelocationHeader`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImagePrologueDynamicRelocationHeader {
    pub prologue_byte_count: u8,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `prologue_byte_count` | `u8` |  |

##### Implementations

###### Trait Implementations

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Copy**
- **Pod**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **UnwindSafe**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Send**
- **Unpin**
- **RefUnwindSafe**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Freeze**
- **Sync**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImagePrologueDynamicRelocationHeader { /* ... */ }
    ```

#### Struct `ImageEpilogueDynamicRelocationHeader`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageEpilogueDynamicRelocationHeader {
    pub epilogue_count: crate::endian::U32Bytes<crate::endian::LittleEndian>,
    pub epilogue_byte_count: u8,
    pub branch_descriptor_element_size: u8,
    pub branch_descriptor_count: crate::endian::U16Bytes<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `epilogue_count` | `crate::endian::U32Bytes<crate::endian::LittleEndian>` |  |
| `epilogue_byte_count` | `u8` |  |
| `branch_descriptor_element_size` | `u8` |  |
| `branch_descriptor_count` | `crate::endian::U16Bytes<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **RefUnwindSafe**
- **UnwindSafe**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Freeze**
- **Unpin**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageEpilogueDynamicRelocationHeader { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Copy**
- **Send**
- **Sync**
- **Pod**
#### Struct `ImageLoadConfigDirectory32`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageLoadConfigDirectory32 {
    pub size: crate::endian::U32<crate::endian::LittleEndian>,
    pub time_date_stamp: crate::endian::U32<crate::endian::LittleEndian>,
    pub major_version: crate::endian::U16<crate::endian::LittleEndian>,
    pub minor_version: crate::endian::U16<crate::endian::LittleEndian>,
    pub global_flags_clear: crate::endian::U32<crate::endian::LittleEndian>,
    pub global_flags_set: crate::endian::U32<crate::endian::LittleEndian>,
    pub critical_section_default_timeout: crate::endian::U32<crate::endian::LittleEndian>,
    pub de_commit_free_block_threshold: crate::endian::U32<crate::endian::LittleEndian>,
    pub de_commit_total_free_threshold: crate::endian::U32<crate::endian::LittleEndian>,
    pub lock_prefix_table: crate::endian::U32<crate::endian::LittleEndian>,
    pub maximum_allocation_size: crate::endian::U32<crate::endian::LittleEndian>,
    pub virtual_memory_threshold: crate::endian::U32<crate::endian::LittleEndian>,
    pub process_heap_flags: crate::endian::U32<crate::endian::LittleEndian>,
    pub process_affinity_mask: crate::endian::U32<crate::endian::LittleEndian>,
    pub csd_version: crate::endian::U16<crate::endian::LittleEndian>,
    pub dependent_load_flags: crate::endian::U16<crate::endian::LittleEndian>,
    pub edit_list: crate::endian::U32<crate::endian::LittleEndian>,
    pub security_cookie: crate::endian::U32<crate::endian::LittleEndian>,
    pub sehandler_table: crate::endian::U32<crate::endian::LittleEndian>,
    pub sehandler_count: crate::endian::U32<crate::endian::LittleEndian>,
    pub guard_cf_check_function_pointer: crate::endian::U32<crate::endian::LittleEndian>,
    pub guard_cf_dispatch_function_pointer: crate::endian::U32<crate::endian::LittleEndian>,
    pub guard_cf_function_table: crate::endian::U32<crate::endian::LittleEndian>,
    pub guard_cf_function_count: crate::endian::U32<crate::endian::LittleEndian>,
    pub guard_flags: crate::endian::U32<crate::endian::LittleEndian>,
    pub code_integrity: ImageLoadConfigCodeIntegrity,
    pub guard_address_taken_iat_entry_table: crate::endian::U32<crate::endian::LittleEndian>,
    pub guard_address_taken_iat_entry_count: crate::endian::U32<crate::endian::LittleEndian>,
    pub guard_long_jump_target_table: crate::endian::U32<crate::endian::LittleEndian>,
    pub guard_long_jump_target_count: crate::endian::U32<crate::endian::LittleEndian>,
    pub dynamic_value_reloc_table: crate::endian::U32<crate::endian::LittleEndian>,
    pub chpe_metadata_pointer: crate::endian::U32<crate::endian::LittleEndian>,
    pub guard_rf_failure_routine: crate::endian::U32<crate::endian::LittleEndian>,
    pub guard_rf_failure_routine_function_pointer: crate::endian::U32<crate::endian::LittleEndian>,
    pub dynamic_value_reloc_table_offset: crate::endian::U32<crate::endian::LittleEndian>,
    pub dynamic_value_reloc_table_section: crate::endian::U16<crate::endian::LittleEndian>,
    pub reserved2: crate::endian::U16<crate::endian::LittleEndian>,
    pub guard_rf_verify_stack_pointer_function_pointer: crate::endian::U32<crate::endian::LittleEndian>,
    pub hot_patch_table_offset: crate::endian::U32<crate::endian::LittleEndian>,
    pub reserved3: crate::endian::U32<crate::endian::LittleEndian>,
    pub enclave_configuration_pointer: crate::endian::U32<crate::endian::LittleEndian>,
    pub volatile_metadata_pointer: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `size` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `time_date_stamp` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `major_version` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `minor_version` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `global_flags_clear` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `global_flags_set` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `critical_section_default_timeout` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `de_commit_free_block_threshold` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `de_commit_total_free_threshold` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `lock_prefix_table` | `crate::endian::U32<crate::endian::LittleEndian>` | VA |
| `maximum_allocation_size` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `virtual_memory_threshold` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `process_heap_flags` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `process_affinity_mask` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `csd_version` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `dependent_load_flags` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `edit_list` | `crate::endian::U32<crate::endian::LittleEndian>` | VA |
| `security_cookie` | `crate::endian::U32<crate::endian::LittleEndian>` | VA |
| `sehandler_table` | `crate::endian::U32<crate::endian::LittleEndian>` | VA |
| `sehandler_count` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `guard_cf_check_function_pointer` | `crate::endian::U32<crate::endian::LittleEndian>` | VA |
| `guard_cf_dispatch_function_pointer` | `crate::endian::U32<crate::endian::LittleEndian>` | VA |
| `guard_cf_function_table` | `crate::endian::U32<crate::endian::LittleEndian>` | VA |
| `guard_cf_function_count` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `guard_flags` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `code_integrity` | `ImageLoadConfigCodeIntegrity` |  |
| `guard_address_taken_iat_entry_table` | `crate::endian::U32<crate::endian::LittleEndian>` | VA |
| `guard_address_taken_iat_entry_count` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `guard_long_jump_target_table` | `crate::endian::U32<crate::endian::LittleEndian>` | VA |
| `guard_long_jump_target_count` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `dynamic_value_reloc_table` | `crate::endian::U32<crate::endian::LittleEndian>` | VA |
| `chpe_metadata_pointer` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `guard_rf_failure_routine` | `crate::endian::U32<crate::endian::LittleEndian>` | VA |
| `guard_rf_failure_routine_function_pointer` | `crate::endian::U32<crate::endian::LittleEndian>` | VA |
| `dynamic_value_reloc_table_offset` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `dynamic_value_reloc_table_section` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `reserved2` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `guard_rf_verify_stack_pointer_function_pointer` | `crate::endian::U32<crate::endian::LittleEndian>` | VA |
| `hot_patch_table_offset` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `reserved3` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `enclave_configuration_pointer` | `crate::endian::U32<crate::endian::LittleEndian>` | VA |
| `volatile_metadata_pointer` | `crate::endian::U32<crate::endian::LittleEndian>` | VA |

##### Implementations

###### Trait Implementations

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Unpin**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Pod**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Sync**
- **RefUnwindSafe**
- **UnwindSafe**
- **Send**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Copy**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageLoadConfigDirectory32 { /* ... */ }
    ```

- **Freeze**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

#### Struct `ImageLoadConfigDirectory64`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageLoadConfigDirectory64 {
    pub size: crate::endian::U32<crate::endian::LittleEndian>,
    pub time_date_stamp: crate::endian::U32<crate::endian::LittleEndian>,
    pub major_version: crate::endian::U16<crate::endian::LittleEndian>,
    pub minor_version: crate::endian::U16<crate::endian::LittleEndian>,
    pub global_flags_clear: crate::endian::U32<crate::endian::LittleEndian>,
    pub global_flags_set: crate::endian::U32<crate::endian::LittleEndian>,
    pub critical_section_default_timeout: crate::endian::U32<crate::endian::LittleEndian>,
    pub de_commit_free_block_threshold: crate::endian::U64<crate::endian::LittleEndian>,
    pub de_commit_total_free_threshold: crate::endian::U64<crate::endian::LittleEndian>,
    pub lock_prefix_table: crate::endian::U64<crate::endian::LittleEndian>,
    pub maximum_allocation_size: crate::endian::U64<crate::endian::LittleEndian>,
    pub virtual_memory_threshold: crate::endian::U64<crate::endian::LittleEndian>,
    pub process_affinity_mask: crate::endian::U64<crate::endian::LittleEndian>,
    pub process_heap_flags: crate::endian::U32<crate::endian::LittleEndian>,
    pub csd_version: crate::endian::U16<crate::endian::LittleEndian>,
    pub dependent_load_flags: crate::endian::U16<crate::endian::LittleEndian>,
    pub edit_list: crate::endian::U64<crate::endian::LittleEndian>,
    pub security_cookie: crate::endian::U64<crate::endian::LittleEndian>,
    pub sehandler_table: crate::endian::U64<crate::endian::LittleEndian>,
    pub sehandler_count: crate::endian::U64<crate::endian::LittleEndian>,
    pub guard_cf_check_function_pointer: crate::endian::U64<crate::endian::LittleEndian>,
    pub guard_cf_dispatch_function_pointer: crate::endian::U64<crate::endian::LittleEndian>,
    pub guard_cf_function_table: crate::endian::U64<crate::endian::LittleEndian>,
    pub guard_cf_function_count: crate::endian::U64<crate::endian::LittleEndian>,
    pub guard_flags: crate::endian::U32<crate::endian::LittleEndian>,
    pub code_integrity: ImageLoadConfigCodeIntegrity,
    pub guard_address_taken_iat_entry_table: crate::endian::U64<crate::endian::LittleEndian>,
    pub guard_address_taken_iat_entry_count: crate::endian::U64<crate::endian::LittleEndian>,
    pub guard_long_jump_target_table: crate::endian::U64<crate::endian::LittleEndian>,
    pub guard_long_jump_target_count: crate::endian::U64<crate::endian::LittleEndian>,
    pub dynamic_value_reloc_table: crate::endian::U64<crate::endian::LittleEndian>,
    pub chpe_metadata_pointer: crate::endian::U64<crate::endian::LittleEndian>,
    pub guard_rf_failure_routine: crate::endian::U64<crate::endian::LittleEndian>,
    pub guard_rf_failure_routine_function_pointer: crate::endian::U64<crate::endian::LittleEndian>,
    pub dynamic_value_reloc_table_offset: crate::endian::U32<crate::endian::LittleEndian>,
    pub dynamic_value_reloc_table_section: crate::endian::U16<crate::endian::LittleEndian>,
    pub reserved2: crate::endian::U16<crate::endian::LittleEndian>,
    pub guard_rf_verify_stack_pointer_function_pointer: crate::endian::U64<crate::endian::LittleEndian>,
    pub hot_patch_table_offset: crate::endian::U32<crate::endian::LittleEndian>,
    pub reserved3: crate::endian::U32<crate::endian::LittleEndian>,
    pub enclave_configuration_pointer: crate::endian::U64<crate::endian::LittleEndian>,
    pub volatile_metadata_pointer: crate::endian::U64<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `size` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `time_date_stamp` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `major_version` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `minor_version` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `global_flags_clear` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `global_flags_set` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `critical_section_default_timeout` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `de_commit_free_block_threshold` | `crate::endian::U64<crate::endian::LittleEndian>` |  |
| `de_commit_total_free_threshold` | `crate::endian::U64<crate::endian::LittleEndian>` |  |
| `lock_prefix_table` | `crate::endian::U64<crate::endian::LittleEndian>` | VA |
| `maximum_allocation_size` | `crate::endian::U64<crate::endian::LittleEndian>` |  |
| `virtual_memory_threshold` | `crate::endian::U64<crate::endian::LittleEndian>` |  |
| `process_affinity_mask` | `crate::endian::U64<crate::endian::LittleEndian>` |  |
| `process_heap_flags` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `csd_version` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `dependent_load_flags` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `edit_list` | `crate::endian::U64<crate::endian::LittleEndian>` | VA |
| `security_cookie` | `crate::endian::U64<crate::endian::LittleEndian>` | VA |
| `sehandler_table` | `crate::endian::U64<crate::endian::LittleEndian>` | VA |
| `sehandler_count` | `crate::endian::U64<crate::endian::LittleEndian>` |  |
| `guard_cf_check_function_pointer` | `crate::endian::U64<crate::endian::LittleEndian>` | VA |
| `guard_cf_dispatch_function_pointer` | `crate::endian::U64<crate::endian::LittleEndian>` | VA |
| `guard_cf_function_table` | `crate::endian::U64<crate::endian::LittleEndian>` | VA |
| `guard_cf_function_count` | `crate::endian::U64<crate::endian::LittleEndian>` |  |
| `guard_flags` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `code_integrity` | `ImageLoadConfigCodeIntegrity` |  |
| `guard_address_taken_iat_entry_table` | `crate::endian::U64<crate::endian::LittleEndian>` | VA |
| `guard_address_taken_iat_entry_count` | `crate::endian::U64<crate::endian::LittleEndian>` |  |
| `guard_long_jump_target_table` | `crate::endian::U64<crate::endian::LittleEndian>` | VA |
| `guard_long_jump_target_count` | `crate::endian::U64<crate::endian::LittleEndian>` |  |
| `dynamic_value_reloc_table` | `crate::endian::U64<crate::endian::LittleEndian>` | VA |
| `chpe_metadata_pointer` | `crate::endian::U64<crate::endian::LittleEndian>` | VA |
| `guard_rf_failure_routine` | `crate::endian::U64<crate::endian::LittleEndian>` | VA |
| `guard_rf_failure_routine_function_pointer` | `crate::endian::U64<crate::endian::LittleEndian>` | VA |
| `dynamic_value_reloc_table_offset` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `dynamic_value_reloc_table_section` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `reserved2` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `guard_rf_verify_stack_pointer_function_pointer` | `crate::endian::U64<crate::endian::LittleEndian>` | VA |
| `hot_patch_table_offset` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `reserved3` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `enclave_configuration_pointer` | `crate::endian::U64<crate::endian::LittleEndian>` | VA |
| `volatile_metadata_pointer` | `crate::endian::U64<crate::endian::LittleEndian>` | VA |

##### Implementations

###### Trait Implementations

- **Send**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageLoadConfigDirectory64 { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **UnwindSafe**
- **Copy**
- **RefUnwindSafe**
- **Sync**
- **Pod**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Unpin**
- **Freeze**
#### Struct `ImageHotPatchInfo`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageHotPatchInfo {
    pub version: crate::endian::U32<crate::endian::LittleEndian>,
    pub size: crate::endian::U32<crate::endian::LittleEndian>,
    pub sequence_number: crate::endian::U32<crate::endian::LittleEndian>,
    pub base_image_list: crate::endian::U32<crate::endian::LittleEndian>,
    pub base_image_count: crate::endian::U32<crate::endian::LittleEndian>,
    pub buffer_offset: crate::endian::U32<crate::endian::LittleEndian>,
    pub extra_patch_size: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `version` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `size` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `sequence_number` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `base_image_list` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `base_image_count` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `buffer_offset` | `crate::endian::U32<crate::endian::LittleEndian>` | Version 2 and later |
| `extra_patch_size` | `crate::endian::U32<crate::endian::LittleEndian>` | Version 3 and later |

##### Implementations

###### Trait Implementations

- **RefUnwindSafe**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Send**
- **Freeze**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Unpin**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Sync**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Copy**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Pod**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **UnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageHotPatchInfo { /* ... */ }
    ```

#### Struct `ImageHotPatchBase`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageHotPatchBase {
    pub sequence_number: crate::endian::U32<crate::endian::LittleEndian>,
    pub flags: crate::endian::U32<crate::endian::LittleEndian>,
    pub original_time_date_stamp: crate::endian::U32<crate::endian::LittleEndian>,
    pub original_check_sum: crate::endian::U32<crate::endian::LittleEndian>,
    pub code_integrity_info: crate::endian::U32<crate::endian::LittleEndian>,
    pub code_integrity_size: crate::endian::U32<crate::endian::LittleEndian>,
    pub patch_table: crate::endian::U32<crate::endian::LittleEndian>,
    pub buffer_offset: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `sequence_number` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `flags` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `original_time_date_stamp` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `original_check_sum` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `code_integrity_info` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `code_integrity_size` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `patch_table` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `buffer_offset` | `crate::endian::U32<crate::endian::LittleEndian>` | Version 2 and later |

##### Implementations

###### Trait Implementations

- **UnwindSafe**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Pod**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageHotPatchBase { /* ... */ }
    ```

- **Copy**
- **RefUnwindSafe**
- **Unpin**
- **Freeze**
- **Send**
- **Sync**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

#### Struct `ImageHotPatchHashes`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageHotPatchHashes {
    pub sha256: [u8; 32],
    pub sha1: [u8; 20],
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `sha256` | `[u8; 32]` |  |
| `sha1` | `[u8; 20]` |  |

##### Implementations

###### Trait Implementations

- **Freeze**
- **Unpin**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Sync**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Send**
- **RefUnwindSafe**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageHotPatchHashes { /* ... */ }
    ```

- **Copy**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **UnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Pod**
#### Struct `ImageArmRuntimeFunctionEntry`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageArmRuntimeFunctionEntry {
    pub begin_address: crate::endian::U32<crate::endian::LittleEndian>,
    pub unwind_data: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `begin_address` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `unwind_data` | `crate::endian::U32<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **Send**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Freeze**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **UnwindSafe**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Unpin**
- **RefUnwindSafe**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Sync**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageArmRuntimeFunctionEntry { /* ... */ }
    ```

- **Copy**
- **Pod**
#### Struct `ImageArm64RuntimeFunctionEntry`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageArm64RuntimeFunctionEntry {
    pub begin_address: crate::endian::U32<crate::endian::LittleEndian>,
    pub unwind_data: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `begin_address` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `unwind_data` | `crate::endian::U32<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **Freeze**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageArm64RuntimeFunctionEntry { /* ... */ }
    ```

- **Copy**
- **Pod**
- **Unpin**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Sync**
- **RefUnwindSafe**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Send**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **UnwindSafe**
#### Struct `ImageAlpha64RuntimeFunctionEntry`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageAlpha64RuntimeFunctionEntry {
    pub begin_address: crate::endian::U64<crate::endian::LittleEndian>,
    pub end_address: crate::endian::U64<crate::endian::LittleEndian>,
    pub exception_handler: crate::endian::U64<crate::endian::LittleEndian>,
    pub handler_data: crate::endian::U64<crate::endian::LittleEndian>,
    pub prolog_end_address: crate::endian::U64<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `begin_address` | `crate::endian::U64<crate::endian::LittleEndian>` |  |
| `end_address` | `crate::endian::U64<crate::endian::LittleEndian>` |  |
| `exception_handler` | `crate::endian::U64<crate::endian::LittleEndian>` |  |
| `handler_data` | `crate::endian::U64<crate::endian::LittleEndian>` |  |
| `prolog_end_address` | `crate::endian::U64<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **RefUnwindSafe**
- **Send**
- **Copy**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Unpin**
- **Sync**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageAlpha64RuntimeFunctionEntry { /* ... */ }
    ```

- **UnwindSafe**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Freeze**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Pod**
#### Struct `ImageAlphaRuntimeFunctionEntry`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageAlphaRuntimeFunctionEntry {
    pub begin_address: crate::endian::U32<crate::endian::LittleEndian>,
    pub end_address: crate::endian::U32<crate::endian::LittleEndian>,
    pub exception_handler: crate::endian::U32<crate::endian::LittleEndian>,
    pub handler_data: crate::endian::U32<crate::endian::LittleEndian>,
    pub prolog_end_address: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `begin_address` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `end_address` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `exception_handler` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `handler_data` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `prolog_end_address` | `crate::endian::U32<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **UnwindSafe**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Unpin**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageAlphaRuntimeFunctionEntry { /* ... */ }
    ```

- **Freeze**
- **RefUnwindSafe**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Pod**
- **Copy**
- **Send**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Sync**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

#### Struct `ImageRuntimeFunctionEntry`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageRuntimeFunctionEntry {
    pub begin_address: crate::endian::U32<crate::endian::LittleEndian>,
    pub end_address: crate::endian::U32<crate::endian::LittleEndian>,
    pub unwind_info_address_or_data: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `begin_address` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `end_address` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `unwind_info_address_or_data` | `crate::endian::U32<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **Unpin**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Send**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageRuntimeFunctionEntry { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **UnwindSafe**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Sync**
- **Copy**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Freeze**
- **Pod**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **RefUnwindSafe**
#### Struct `ImageEnclaveConfig32`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageEnclaveConfig32 {
    pub size: crate::endian::U32<crate::endian::LittleEndian>,
    pub minimum_required_config_size: crate::endian::U32<crate::endian::LittleEndian>,
    pub policy_flags: crate::endian::U32<crate::endian::LittleEndian>,
    pub number_of_imports: crate::endian::U32<crate::endian::LittleEndian>,
    pub import_list: crate::endian::U32<crate::endian::LittleEndian>,
    pub import_entry_size: crate::endian::U32<crate::endian::LittleEndian>,
    pub family_id: [u8; 16],
    pub image_id: [u8; 16],
    pub image_version: crate::endian::U32<crate::endian::LittleEndian>,
    pub security_version: crate::endian::U32<crate::endian::LittleEndian>,
    pub enclave_size: crate::endian::U32<crate::endian::LittleEndian>,
    pub number_of_threads: crate::endian::U32<crate::endian::LittleEndian>,
    pub enclave_flags: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `size` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `minimum_required_config_size` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `policy_flags` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `number_of_imports` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `import_list` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `import_entry_size` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `family_id` | `[u8; 16]` |  |
| `image_id` | `[u8; 16]` |  |
| `image_version` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `security_version` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `enclave_size` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `number_of_threads` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `enclave_flags` | `crate::endian::U32<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Send**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **RefUnwindSafe**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageEnclaveConfig32 { /* ... */ }
    ```

- **Freeze**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Sync**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **UnwindSafe**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Unpin**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Copy**
- **Pod**
#### Struct `ImageEnclaveConfig64`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageEnclaveConfig64 {
    pub size: crate::endian::U32<crate::endian::LittleEndian>,
    pub minimum_required_config_size: crate::endian::U32<crate::endian::LittleEndian>,
    pub policy_flags: crate::endian::U32<crate::endian::LittleEndian>,
    pub number_of_imports: crate::endian::U32<crate::endian::LittleEndian>,
    pub import_list: crate::endian::U32<crate::endian::LittleEndian>,
    pub import_entry_size: crate::endian::U32<crate::endian::LittleEndian>,
    pub family_id: [u8; 16],
    pub image_id: [u8; 16],
    pub image_version: crate::endian::U32<crate::endian::LittleEndian>,
    pub security_version: crate::endian::U32<crate::endian::LittleEndian>,
    pub enclave_size: crate::endian::U64<crate::endian::LittleEndian>,
    pub number_of_threads: crate::endian::U32<crate::endian::LittleEndian>,
    pub enclave_flags: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `size` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `minimum_required_config_size` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `policy_flags` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `number_of_imports` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `import_list` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `import_entry_size` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `family_id` | `[u8; 16]` |  |
| `image_id` | `[u8; 16]` |  |
| `image_version` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `security_version` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `enclave_size` | `crate::endian::U64<crate::endian::LittleEndian>` |  |
| `number_of_threads` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `enclave_flags` | `crate::endian::U32<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Sync**
- **Freeze**
- **Unpin**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **RefUnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **UnwindSafe**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Send**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Copy**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageEnclaveConfig64 { /* ... */ }
    ```

- **Pod**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

#### Struct `ImageEnclaveImport`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageEnclaveImport {
    pub match_type: crate::endian::U32<crate::endian::LittleEndian>,
    pub minimum_security_version: crate::endian::U32<crate::endian::LittleEndian>,
    pub unique_or_author_id: [u8; 32],
    pub family_id: [u8; 16],
    pub image_id: [u8; 16],
    pub import_name: crate::endian::U32<crate::endian::LittleEndian>,
    pub reserved: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `match_type` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `minimum_security_version` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `unique_or_author_id` | `[u8; 32]` |  |
| `family_id` | `[u8; 16]` |  |
| `image_id` | `[u8; 16]` |  |
| `import_name` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `reserved` | `crate::endian::U32<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **UnwindSafe**
- **Send**
- **Unpin**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Freeze**
- **Copy**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Sync**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageEnclaveImport { /* ... */ }
    ```

- **Pod**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **RefUnwindSafe**
#### Struct `ImageDebugDirectory`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageDebugDirectory {
    pub characteristics: crate::endian::U32<crate::endian::LittleEndian>,
    pub time_date_stamp: crate::endian::U32<crate::endian::LittleEndian>,
    pub major_version: crate::endian::U16<crate::endian::LittleEndian>,
    pub minor_version: crate::endian::U16<crate::endian::LittleEndian>,
    pub typ: crate::endian::U32<crate::endian::LittleEndian>,
    pub size_of_data: crate::endian::U32<crate::endian::LittleEndian>,
    pub address_of_raw_data: crate::endian::U32<crate::endian::LittleEndian>,
    pub pointer_to_raw_data: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `characteristics` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `time_date_stamp` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `major_version` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `minor_version` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `typ` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `size_of_data` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `address_of_raw_data` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `pointer_to_raw_data` | `crate::endian::U32<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageDebugDirectory { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Unpin**
- **Freeze**
- **Pod**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **UnwindSafe**
- **RefUnwindSafe**
- **Sync**
- **Send**
- **Copy**
#### Struct `ImageCoffSymbolsHeader`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageCoffSymbolsHeader {
    pub number_of_symbols: crate::endian::U32<crate::endian::LittleEndian>,
    pub lva_to_first_symbol: crate::endian::U32<crate::endian::LittleEndian>,
    pub number_of_linenumbers: crate::endian::U32<crate::endian::LittleEndian>,
    pub lva_to_first_linenumber: crate::endian::U32<crate::endian::LittleEndian>,
    pub rva_to_first_byte_of_code: crate::endian::U32<crate::endian::LittleEndian>,
    pub rva_to_last_byte_of_code: crate::endian::U32<crate::endian::LittleEndian>,
    pub rva_to_first_byte_of_data: crate::endian::U32<crate::endian::LittleEndian>,
    pub rva_to_last_byte_of_data: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `number_of_symbols` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `lva_to_first_symbol` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `number_of_linenumbers` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `lva_to_first_linenumber` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `rva_to_first_byte_of_code` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `rva_to_last_byte_of_code` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `rva_to_first_byte_of_data` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `rva_to_last_byte_of_data` | `crate::endian::U32<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Unpin**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Sync**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Freeze**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **RefUnwindSafe**
- **Copy**
- **Send**
- **UnwindSafe**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageCoffSymbolsHeader { /* ... */ }
    ```

- **Pod**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

#### Struct `ImageDebugMisc`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageDebugMisc {
    pub data_type: crate::endian::U32<crate::endian::LittleEndian>,
    pub length: crate::endian::U32<crate::endian::LittleEndian>,
    pub unicode: u8,
    pub reserved: [u8; 3],
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `data_type` | `crate::endian::U32<crate::endian::LittleEndian>` | type of misc data, see defines |
| `length` | `crate::endian::U32<crate::endian::LittleEndian>` | total length of record, rounded to four byte multiple. |
| `unicode` | `u8` | TRUE if data is unicode string |
| `reserved` | `[u8; 3]` |  |

##### Implementations

###### Trait Implementations

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Copy**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Send**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Sync**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageDebugMisc { /* ... */ }
    ```

- **Unpin**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **RefUnwindSafe**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **UnwindSafe**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Freeze**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Pod**
#### Struct `ImageFunctionEntry`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageFunctionEntry {
    pub starting_address: crate::endian::U32<crate::endian::LittleEndian>,
    pub ending_address: crate::endian::U32<crate::endian::LittleEndian>,
    pub end_of_prologue: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `starting_address` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `ending_address` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `end_of_prologue` | `crate::endian::U32<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **UnwindSafe**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageFunctionEntry { /* ... */ }
    ```

- **Pod**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Copy**
- **Unpin**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Sync**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **RefUnwindSafe**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Freeze**
- **Send**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

#### Struct `ImageFunctionEntry64`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageFunctionEntry64 {
    pub starting_address: crate::endian::U64<crate::endian::LittleEndian>,
    pub ending_address: crate::endian::U64<crate::endian::LittleEndian>,
    pub end_of_prologue_or_unwind_info_address: crate::endian::U64<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `starting_address` | `crate::endian::U64<crate::endian::LittleEndian>` |  |
| `ending_address` | `crate::endian::U64<crate::endian::LittleEndian>` |  |
| `end_of_prologue_or_unwind_info_address` | `crate::endian::U64<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **UnwindSafe**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **RefUnwindSafe**
- **Sync**
- **Pod**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Copy**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageFunctionEntry64 { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Unpin**
- **Freeze**
- **Send**
#### Struct `ImageSeparateDebugHeader`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageSeparateDebugHeader {
    pub signature: crate::endian::U16<crate::endian::LittleEndian>,
    pub flags: crate::endian::U16<crate::endian::LittleEndian>,
    pub machine: crate::endian::U16<crate::endian::LittleEndian>,
    pub characteristics: crate::endian::U16<crate::endian::LittleEndian>,
    pub time_date_stamp: crate::endian::U32<crate::endian::LittleEndian>,
    pub check_sum: crate::endian::U32<crate::endian::LittleEndian>,
    pub image_base: crate::endian::U32<crate::endian::LittleEndian>,
    pub size_of_image: crate::endian::U32<crate::endian::LittleEndian>,
    pub number_of_sections: crate::endian::U32<crate::endian::LittleEndian>,
    pub exported_names_size: crate::endian::U32<crate::endian::LittleEndian>,
    pub debug_directory_size: crate::endian::U32<crate::endian::LittleEndian>,
    pub section_alignment: crate::endian::U32<crate::endian::LittleEndian>,
    pub reserved: [crate::endian::U32<crate::endian::LittleEndian>; 2],
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `signature` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `flags` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `machine` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `characteristics` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `time_date_stamp` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `check_sum` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `image_base` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `size_of_image` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `number_of_sections` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `exported_names_size` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `debug_directory_size` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `section_alignment` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `reserved` | `[crate::endian::U32<crate::endian::LittleEndian>; 2]` |  |

##### Implementations

###### Trait Implementations

- **Pod**
- **Copy**
- **Sync**
- **Freeze**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Send**
- **RefUnwindSafe**
- **UnwindSafe**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageSeparateDebugHeader { /* ... */ }
    ```

- **Unpin**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

#### Struct `NonPagedDebugInfo`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct NonPagedDebugInfo {
    pub signature: crate::endian::U16<crate::endian::LittleEndian>,
    pub flags: crate::endian::U16<crate::endian::LittleEndian>,
    pub size: crate::endian::U32<crate::endian::LittleEndian>,
    pub machine: crate::endian::U16<crate::endian::LittleEndian>,
    pub characteristics: crate::endian::U16<crate::endian::LittleEndian>,
    pub time_date_stamp: crate::endian::U32<crate::endian::LittleEndian>,
    pub check_sum: crate::endian::U32<crate::endian::LittleEndian>,
    pub size_of_image: crate::endian::U32<crate::endian::LittleEndian>,
    pub image_base: crate::endian::U64<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `signature` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `flags` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `size` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `machine` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `characteristics` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `time_date_stamp` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `check_sum` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `size_of_image` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `image_base` | `crate::endian::U64<crate::endian::LittleEndian>` |  |

##### Implementations

###### Trait Implementations

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> NonPagedDebugInfo { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Unpin**
- **Sync**
- **RefUnwindSafe**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Copy**
- **Pod**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Freeze**
- **Send**
- **UnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

#### Struct `ImageArchitectureEntry`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageArchitectureEntry {
    pub fixup_inst_rva: crate::endian::U32<crate::endian::LittleEndian>,
    pub new_inst: crate::endian::U32<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `fixup_inst_rva` | `crate::endian::U32<crate::endian::LittleEndian>` | RVA of instruction to fixup |
| `new_inst` | `crate::endian::U32<crate::endian::LittleEndian>` | fixup instruction (see alphaops.h) |

##### Implementations

###### Trait Implementations

- **RefUnwindSafe**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Copy**
- **UnwindSafe**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Pod**
- **Freeze**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Sync**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Send**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageArchitectureEntry { /* ... */ }
    ```

- **Unpin**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

#### Struct `ImportObjectHeader`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImportObjectHeader {
    pub sig1: crate::endian::U16<crate::endian::LittleEndian>,
    pub sig2: crate::endian::U16<crate::endian::LittleEndian>,
    pub version: crate::endian::U16<crate::endian::LittleEndian>,
    pub machine: crate::endian::U16<crate::endian::LittleEndian>,
    pub time_date_stamp: crate::endian::U32<crate::endian::LittleEndian>,
    pub size_of_data: crate::endian::U32<crate::endian::LittleEndian>,
    pub ordinal_or_hint: crate::endian::U16<crate::endian::LittleEndian>,
    pub name_type: crate::endian::U16<crate::endian::LittleEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `sig1` | `crate::endian::U16<crate::endian::LittleEndian>` | Must be IMAGE_FILE_MACHINE_UNKNOWN |
| `sig2` | `crate::endian::U16<crate::endian::LittleEndian>` | Must be IMPORT_OBJECT_HDR_SIG2. |
| `version` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `machine` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `time_date_stamp` | `crate::endian::U32<crate::endian::LittleEndian>` | Time/date stamp |
| `size_of_data` | `crate::endian::U32<crate::endian::LittleEndian>` | particularly useful for incremental links |
| `ordinal_or_hint` | `crate::endian::U16<crate::endian::LittleEndian>` | if grf & IMPORT_OBJECT_ORDINAL |
| `name_type` | `crate::endian::U16<crate::endian::LittleEndian>` |  |

##### Implementations

###### Methods

- ```rust
  pub fn parse<''data, R: ReadRef<''data>>(data: R, offset: &mut u64) -> Result<&''data Self> { /* ... */ }
  ```
  Read the short import header.

- ```rust
  pub fn parse_data<''data, R: ReadRef<''data>>(self: &Self, data: R, offset: &mut u64) -> Result<ImportObjectData<''data>> { /* ... */ }
  ```
  Parse the data following the header.

- ```rust
  pub fn import_type(self: &Self) -> u16 { /* ... */ }
  ```
  The type of import.

- ```rust
  pub fn name_type(self: &Self) -> u16 { /* ... */ }
  ```
  The type of import name.

###### Trait Implementations

- **Freeze**
- **UnwindSafe**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImportObjectHeader { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Unpin**
- **Copy**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Send**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Sync**
- **RefUnwindSafe**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Pod**
#### Struct `ImageCor20Header`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct ImageCor20Header {
    pub cb: crate::endian::U32<crate::endian::LittleEndian>,
    pub major_runtime_version: crate::endian::U16<crate::endian::LittleEndian>,
    pub minor_runtime_version: crate::endian::U16<crate::endian::LittleEndian>,
    pub meta_data: ImageDataDirectory,
    pub flags: crate::endian::U32<crate::endian::LittleEndian>,
    pub entry_point_token_or_rva: crate::endian::U32<crate::endian::LittleEndian>,
    pub resources: ImageDataDirectory,
    pub strong_name_signature: ImageDataDirectory,
    pub code_manager_table: ImageDataDirectory,
    pub vtable_fixups: ImageDataDirectory,
    pub export_address_table_jumps: ImageDataDirectory,
    pub managed_native_header: ImageDataDirectory,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `cb` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `major_runtime_version` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `minor_runtime_version` | `crate::endian::U16<crate::endian::LittleEndian>` |  |
| `meta_data` | `ImageDataDirectory` |  |
| `flags` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `entry_point_token_or_rva` | `crate::endian::U32<crate::endian::LittleEndian>` |  |
| `resources` | `ImageDataDirectory` |  |
| `strong_name_signature` | `ImageDataDirectory` |  |
| `code_manager_table` | `ImageDataDirectory` |  |
| `vtable_fixups` | `ImageDataDirectory` |  |
| `export_address_table_jumps` | `ImageDataDirectory` |  |
| `managed_native_header` | `ImageDataDirectory` |  |

##### Implementations

###### Trait Implementations

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ImageCor20Header { /* ... */ }
    ```

- **UnwindSafe**
- **Freeze**
- **Unpin**
- **RefUnwindSafe**
- **Sync**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Send**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Copy**
- **Pod**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

### Constants and Statics

#### Constant `IMAGE_DOS_SIGNATURE`

MZ

```rust
pub const IMAGE_DOS_SIGNATURE: u16 = 0x5A4D;
```

#### Constant `IMAGE_OS2_SIGNATURE`

NE

```rust
pub const IMAGE_OS2_SIGNATURE: u16 = 0x454E;
```

#### Constant `IMAGE_OS2_SIGNATURE_LE`

LE

```rust
pub const IMAGE_OS2_SIGNATURE_LE: u16 = 0x454C;
```

#### Constant `IMAGE_VXD_SIGNATURE`

LE

```rust
pub const IMAGE_VXD_SIGNATURE: u16 = 0x454C;
```

#### Constant `IMAGE_NT_SIGNATURE`

PE00

```rust
pub const IMAGE_NT_SIGNATURE: u32 = 0x0000_4550;
```

#### Constant `IMAGE_SIZEOF_FILE_HEADER`

```rust
pub const IMAGE_SIZEOF_FILE_HEADER: usize = 20;
```

#### Constant `IMAGE_FILE_RELOCS_STRIPPED`

Relocation info stripped from file.

```rust
pub const IMAGE_FILE_RELOCS_STRIPPED: u16 = 0x0001;
```

#### Constant `IMAGE_FILE_EXECUTABLE_IMAGE`

File is executable  (i.e. no unresolved external references).

```rust
pub const IMAGE_FILE_EXECUTABLE_IMAGE: u16 = 0x0002;
```

#### Constant `IMAGE_FILE_LINE_NUMS_STRIPPED`

Line numbers stripped from file.

```rust
pub const IMAGE_FILE_LINE_NUMS_STRIPPED: u16 = 0x0004;
```

#### Constant `IMAGE_FILE_LOCAL_SYMS_STRIPPED`

Local symbols stripped from file.

```rust
pub const IMAGE_FILE_LOCAL_SYMS_STRIPPED: u16 = 0x0008;
```

#### Constant `IMAGE_FILE_AGGRESIVE_WS_TRIM`

Aggressively trim working set

```rust
pub const IMAGE_FILE_AGGRESIVE_WS_TRIM: u16 = 0x0010;
```

#### Constant `IMAGE_FILE_LARGE_ADDRESS_AWARE`

App can handle >2gb addresses

```rust
pub const IMAGE_FILE_LARGE_ADDRESS_AWARE: u16 = 0x0020;
```

#### Constant `IMAGE_FILE_BYTES_REVERSED_LO`

Bytes of machine word are reversed.

```rust
pub const IMAGE_FILE_BYTES_REVERSED_LO: u16 = 0x0080;
```

#### Constant `IMAGE_FILE_32BIT_MACHINE`

32 bit word machine.

```rust
pub const IMAGE_FILE_32BIT_MACHINE: u16 = 0x0100;
```

#### Constant `IMAGE_FILE_DEBUG_STRIPPED`

Debugging info stripped from file in .DBG file

```rust
pub const IMAGE_FILE_DEBUG_STRIPPED: u16 = 0x0200;
```

#### Constant `IMAGE_FILE_REMOVABLE_RUN_FROM_SWAP`

If Image is on removable media, copy and run from the swap file.

```rust
pub const IMAGE_FILE_REMOVABLE_RUN_FROM_SWAP: u16 = 0x0400;
```

#### Constant `IMAGE_FILE_NET_RUN_FROM_SWAP`

If Image is on Net, copy and run from the swap file.

```rust
pub const IMAGE_FILE_NET_RUN_FROM_SWAP: u16 = 0x0800;
```

#### Constant `IMAGE_FILE_SYSTEM`

System File.

```rust
pub const IMAGE_FILE_SYSTEM: u16 = 0x1000;
```

#### Constant `IMAGE_FILE_DLL`

File is a DLL.

```rust
pub const IMAGE_FILE_DLL: u16 = 0x2000;
```

#### Constant `IMAGE_FILE_UP_SYSTEM_ONLY`

File should only be run on a UP machine

```rust
pub const IMAGE_FILE_UP_SYSTEM_ONLY: u16 = 0x4000;
```

#### Constant `IMAGE_FILE_BYTES_REVERSED_HI`

Bytes of machine word are reversed.

```rust
pub const IMAGE_FILE_BYTES_REVERSED_HI: u16 = 0x8000;
```

#### Constant `IMAGE_FILE_MACHINE_UNKNOWN`

```rust
pub const IMAGE_FILE_MACHINE_UNKNOWN: u16 = 0;
```

#### Constant `IMAGE_FILE_MACHINE_TARGET_HOST`

Useful for indicating we want to interact with the host and not a WoW guest.

```rust
pub const IMAGE_FILE_MACHINE_TARGET_HOST: u16 = 0x0001;
```

#### Constant `IMAGE_FILE_MACHINE_I386`

Intel 386.

```rust
pub const IMAGE_FILE_MACHINE_I386: u16 = 0x014c;
```

#### Constant `IMAGE_FILE_MACHINE_R3000`

MIPS little-endian, 0x160 big-endian

```rust
pub const IMAGE_FILE_MACHINE_R3000: u16 = 0x0162;
```

#### Constant `IMAGE_FILE_MACHINE_R4000`

MIPS little-endian

```rust
pub const IMAGE_FILE_MACHINE_R4000: u16 = 0x0166;
```

#### Constant `IMAGE_FILE_MACHINE_R10000`

MIPS little-endian

```rust
pub const IMAGE_FILE_MACHINE_R10000: u16 = 0x0168;
```

#### Constant `IMAGE_FILE_MACHINE_WCEMIPSV2`

MIPS little-endian WCE v2

```rust
pub const IMAGE_FILE_MACHINE_WCEMIPSV2: u16 = 0x0169;
```

#### Constant `IMAGE_FILE_MACHINE_ALPHA`

Alpha_AXP

```rust
pub const IMAGE_FILE_MACHINE_ALPHA: u16 = 0x0184;
```

#### Constant `IMAGE_FILE_MACHINE_SH3`

SH3 little-endian

```rust
pub const IMAGE_FILE_MACHINE_SH3: u16 = 0x01a2;
```

#### Constant `IMAGE_FILE_MACHINE_SH3DSP`

```rust
pub const IMAGE_FILE_MACHINE_SH3DSP: u16 = 0x01a3;
```

#### Constant `IMAGE_FILE_MACHINE_SH3E`

SH3E little-endian

```rust
pub const IMAGE_FILE_MACHINE_SH3E: u16 = 0x01a4;
```

#### Constant `IMAGE_FILE_MACHINE_SH4`

SH4 little-endian

```rust
pub const IMAGE_FILE_MACHINE_SH4: u16 = 0x01a6;
```

#### Constant `IMAGE_FILE_MACHINE_SH5`

SH5

```rust
pub const IMAGE_FILE_MACHINE_SH5: u16 = 0x01a8;
```

#### Constant `IMAGE_FILE_MACHINE_ARM`

ARM Little-Endian

```rust
pub const IMAGE_FILE_MACHINE_ARM: u16 = 0x01c0;
```

#### Constant `IMAGE_FILE_MACHINE_THUMB`

ARM Thumb/Thumb-2 Little-Endian

```rust
pub const IMAGE_FILE_MACHINE_THUMB: u16 = 0x01c2;
```

#### Constant `IMAGE_FILE_MACHINE_ARMNT`

ARM Thumb-2 Little-Endian

```rust
pub const IMAGE_FILE_MACHINE_ARMNT: u16 = 0x01c4;
```

#### Constant `IMAGE_FILE_MACHINE_AM33`

```rust
pub const IMAGE_FILE_MACHINE_AM33: u16 = 0x01d3;
```

#### Constant `IMAGE_FILE_MACHINE_POWERPC`

IBM PowerPC Little-Endian

```rust
pub const IMAGE_FILE_MACHINE_POWERPC: u16 = 0x01F0;
```

#### Constant `IMAGE_FILE_MACHINE_POWERPCFP`

```rust
pub const IMAGE_FILE_MACHINE_POWERPCFP: u16 = 0x01f1;
```

#### Constant `IMAGE_FILE_MACHINE_IA64`

Intel 64

```rust
pub const IMAGE_FILE_MACHINE_IA64: u16 = 0x0200;
```

#### Constant `IMAGE_FILE_MACHINE_MIPS16`

MIPS

```rust
pub const IMAGE_FILE_MACHINE_MIPS16: u16 = 0x0266;
```

#### Constant `IMAGE_FILE_MACHINE_ALPHA64`

ALPHA64

```rust
pub const IMAGE_FILE_MACHINE_ALPHA64: u16 = 0x0284;
```

#### Constant `IMAGE_FILE_MACHINE_MIPSFPU`

MIPS

```rust
pub const IMAGE_FILE_MACHINE_MIPSFPU: u16 = 0x0366;
```

#### Constant `IMAGE_FILE_MACHINE_MIPSFPU16`

MIPS

```rust
pub const IMAGE_FILE_MACHINE_MIPSFPU16: u16 = 0x0466;
```

#### Constant `IMAGE_FILE_MACHINE_AXP64`

```rust
pub const IMAGE_FILE_MACHINE_AXP64: u16 = IMAGE_FILE_MACHINE_ALPHA64;
```

#### Constant `IMAGE_FILE_MACHINE_TRICORE`

Infineon

```rust
pub const IMAGE_FILE_MACHINE_TRICORE: u16 = 0x0520;
```

#### Constant `IMAGE_FILE_MACHINE_CEF`

```rust
pub const IMAGE_FILE_MACHINE_CEF: u16 = 0x0CEF;
```

#### Constant `IMAGE_FILE_MACHINE_EBC`

EFI Byte Code

```rust
pub const IMAGE_FILE_MACHINE_EBC: u16 = 0x0EBC;
```

#### Constant `IMAGE_FILE_MACHINE_AMD64`

AMD64 (K8)

```rust
pub const IMAGE_FILE_MACHINE_AMD64: u16 = 0x8664;
```

#### Constant `IMAGE_FILE_MACHINE_M32R`

M32R little-endian

```rust
pub const IMAGE_FILE_MACHINE_M32R: u16 = 0x9041;
```

#### Constant `IMAGE_FILE_MACHINE_ARM64`

ARM64 Little-Endian

```rust
pub const IMAGE_FILE_MACHINE_ARM64: u16 = 0xAA64;
```

#### Constant `IMAGE_FILE_MACHINE_ARM64EC`

ARM64EC ("Emulation Compatible")

```rust
pub const IMAGE_FILE_MACHINE_ARM64EC: u16 = 0xA641;
```

#### Constant `IMAGE_FILE_MACHINE_CEE`

```rust
pub const IMAGE_FILE_MACHINE_CEE: u16 = 0xC0EE;
```

#### Constant `IMAGE_FILE_MACHINE_RISCV32`

RISCV32

```rust
pub const IMAGE_FILE_MACHINE_RISCV32: u16 = 0x5032;
```

#### Constant `IMAGE_FILE_MACHINE_RISCV64`

RISCV64

```rust
pub const IMAGE_FILE_MACHINE_RISCV64: u16 = 0x5064;
```

#### Constant `IMAGE_FILE_MACHINE_RISCV128`

RISCV128

```rust
pub const IMAGE_FILE_MACHINE_RISCV128: u16 = 0x5128;
```

#### Constant `IMAGE_FILE_MACHINE_ARM64X`

ARM64X (Mixed ARM64 and ARM64EC)

```rust
pub const IMAGE_FILE_MACHINE_ARM64X: u16 = 0xA64E;
```

#### Constant `IMAGE_FILE_MACHINE_CHPE_X86`

CHPE x86 ("Compiled Hybrid Portable Executable")

```rust
pub const IMAGE_FILE_MACHINE_CHPE_X86: u16 = 0x3A64;
```

#### Constant `IMAGE_NUMBEROF_DIRECTORY_ENTRIES`

```rust
pub const IMAGE_NUMBEROF_DIRECTORY_ENTRIES: usize = 16;
```

#### Constant `IMAGE_NT_OPTIONAL_HDR32_MAGIC`

```rust
pub const IMAGE_NT_OPTIONAL_HDR32_MAGIC: u16 = 0x10b;
```

#### Constant `IMAGE_NT_OPTIONAL_HDR64_MAGIC`

```rust
pub const IMAGE_NT_OPTIONAL_HDR64_MAGIC: u16 = 0x20b;
```

#### Constant `IMAGE_ROM_OPTIONAL_HDR_MAGIC`

```rust
pub const IMAGE_ROM_OPTIONAL_HDR_MAGIC: u16 = 0x107;
```

#### Constant `IMAGE_SUBSYSTEM_UNKNOWN`

Unknown subsystem.

```rust
pub const IMAGE_SUBSYSTEM_UNKNOWN: u16 = 0;
```

#### Constant `IMAGE_SUBSYSTEM_NATIVE`

Image doesn't require a subsystem.

```rust
pub const IMAGE_SUBSYSTEM_NATIVE: u16 = 1;
```

#### Constant `IMAGE_SUBSYSTEM_WINDOWS_GUI`

Image runs in the Windows GUI subsystem.

```rust
pub const IMAGE_SUBSYSTEM_WINDOWS_GUI: u16 = 2;
```

#### Constant `IMAGE_SUBSYSTEM_WINDOWS_CUI`

Image runs in the Windows character subsystem.

```rust
pub const IMAGE_SUBSYSTEM_WINDOWS_CUI: u16 = 3;
```

#### Constant `IMAGE_SUBSYSTEM_OS2_CUI`

image runs in the OS/2 character subsystem.

```rust
pub const IMAGE_SUBSYSTEM_OS2_CUI: u16 = 5;
```

#### Constant `IMAGE_SUBSYSTEM_POSIX_CUI`

image runs in the Posix character subsystem.

```rust
pub const IMAGE_SUBSYSTEM_POSIX_CUI: u16 = 7;
```

#### Constant `IMAGE_SUBSYSTEM_NATIVE_WINDOWS`

image is a native Win9x driver.

```rust
pub const IMAGE_SUBSYSTEM_NATIVE_WINDOWS: u16 = 8;
```

#### Constant `IMAGE_SUBSYSTEM_WINDOWS_CE_GUI`

Image runs in the Windows CE subsystem.

```rust
pub const IMAGE_SUBSYSTEM_WINDOWS_CE_GUI: u16 = 9;
```

#### Constant `IMAGE_SUBSYSTEM_EFI_APPLICATION`

```rust
pub const IMAGE_SUBSYSTEM_EFI_APPLICATION: u16 = 10;
```

#### Constant `IMAGE_SUBSYSTEM_EFI_BOOT_SERVICE_DRIVER`

```rust
pub const IMAGE_SUBSYSTEM_EFI_BOOT_SERVICE_DRIVER: u16 = 11;
```

#### Constant `IMAGE_SUBSYSTEM_EFI_RUNTIME_DRIVER`

```rust
pub const IMAGE_SUBSYSTEM_EFI_RUNTIME_DRIVER: u16 = 12;
```

#### Constant `IMAGE_SUBSYSTEM_EFI_ROM`

```rust
pub const IMAGE_SUBSYSTEM_EFI_ROM: u16 = 13;
```

#### Constant `IMAGE_SUBSYSTEM_XBOX`

```rust
pub const IMAGE_SUBSYSTEM_XBOX: u16 = 14;
```

#### Constant `IMAGE_SUBSYSTEM_WINDOWS_BOOT_APPLICATION`

```rust
pub const IMAGE_SUBSYSTEM_WINDOWS_BOOT_APPLICATION: u16 = 16;
```

#### Constant `IMAGE_SUBSYSTEM_XBOX_CODE_CATALOG`

```rust
pub const IMAGE_SUBSYSTEM_XBOX_CODE_CATALOG: u16 = 17;
```

#### Constant `IMAGE_DLLCHARACTERISTICS_HIGH_ENTROPY_VA`

Image can handle a high entropy 64-bit virtual address space.

```rust
pub const IMAGE_DLLCHARACTERISTICS_HIGH_ENTROPY_VA: u16 = 0x0020;
```

#### Constant `IMAGE_DLLCHARACTERISTICS_DYNAMIC_BASE`

DLL can move.

```rust
pub const IMAGE_DLLCHARACTERISTICS_DYNAMIC_BASE: u16 = 0x0040;
```

#### Constant `IMAGE_DLLCHARACTERISTICS_FORCE_INTEGRITY`

Code Integrity Image

```rust
pub const IMAGE_DLLCHARACTERISTICS_FORCE_INTEGRITY: u16 = 0x0080;
```

#### Constant `IMAGE_DLLCHARACTERISTICS_NX_COMPAT`

Image is NX compatible

```rust
pub const IMAGE_DLLCHARACTERISTICS_NX_COMPAT: u16 = 0x0100;
```

#### Constant `IMAGE_DLLCHARACTERISTICS_NO_ISOLATION`

Image understands isolation and doesn't want it

```rust
pub const IMAGE_DLLCHARACTERISTICS_NO_ISOLATION: u16 = 0x0200;
```

#### Constant `IMAGE_DLLCHARACTERISTICS_NO_SEH`

Image does not use SEH.  No SE handler may reside in this image

```rust
pub const IMAGE_DLLCHARACTERISTICS_NO_SEH: u16 = 0x0400;
```

#### Constant `IMAGE_DLLCHARACTERISTICS_NO_BIND`

Do not bind this image.

```rust
pub const IMAGE_DLLCHARACTERISTICS_NO_BIND: u16 = 0x0800;
```

#### Constant `IMAGE_DLLCHARACTERISTICS_APPCONTAINER`

Image should execute in an AppContainer

```rust
pub const IMAGE_DLLCHARACTERISTICS_APPCONTAINER: u16 = 0x1000;
```

#### Constant `IMAGE_DLLCHARACTERISTICS_WDM_DRIVER`

Driver uses WDM model

```rust
pub const IMAGE_DLLCHARACTERISTICS_WDM_DRIVER: u16 = 0x2000;
```

#### Constant `IMAGE_DLLCHARACTERISTICS_GUARD_CF`

Image supports Control Flow Guard.

```rust
pub const IMAGE_DLLCHARACTERISTICS_GUARD_CF: u16 = 0x4000;
```

#### Constant `IMAGE_DLLCHARACTERISTICS_TERMINAL_SERVER_AWARE`

```rust
pub const IMAGE_DLLCHARACTERISTICS_TERMINAL_SERVER_AWARE: u16 = 0x8000;
```

#### Constant `IMAGE_DIRECTORY_ENTRY_EXPORT`

Export Directory

```rust
pub const IMAGE_DIRECTORY_ENTRY_EXPORT: usize = 0;
```

#### Constant `IMAGE_DIRECTORY_ENTRY_IMPORT`

Import Directory

```rust
pub const IMAGE_DIRECTORY_ENTRY_IMPORT: usize = 1;
```

#### Constant `IMAGE_DIRECTORY_ENTRY_RESOURCE`

Resource Directory

```rust
pub const IMAGE_DIRECTORY_ENTRY_RESOURCE: usize = 2;
```

#### Constant `IMAGE_DIRECTORY_ENTRY_EXCEPTION`

Exception Directory

```rust
pub const IMAGE_DIRECTORY_ENTRY_EXCEPTION: usize = 3;
```

#### Constant `IMAGE_DIRECTORY_ENTRY_SECURITY`

Security Directory

```rust
pub const IMAGE_DIRECTORY_ENTRY_SECURITY: usize = 4;
```

#### Constant `IMAGE_DIRECTORY_ENTRY_BASERELOC`

Base Relocation Table

```rust
pub const IMAGE_DIRECTORY_ENTRY_BASERELOC: usize = 5;
```

#### Constant `IMAGE_DIRECTORY_ENTRY_DEBUG`

Debug Directory

```rust
pub const IMAGE_DIRECTORY_ENTRY_DEBUG: usize = 6;
```

#### Constant `IMAGE_DIRECTORY_ENTRY_ARCHITECTURE`

Architecture Specific Data

```rust
pub const IMAGE_DIRECTORY_ENTRY_ARCHITECTURE: usize = 7;
```

#### Constant `IMAGE_DIRECTORY_ENTRY_GLOBALPTR`

RVA of GP

```rust
pub const IMAGE_DIRECTORY_ENTRY_GLOBALPTR: usize = 8;
```

#### Constant `IMAGE_DIRECTORY_ENTRY_TLS`

TLS Directory

```rust
pub const IMAGE_DIRECTORY_ENTRY_TLS: usize = 9;
```

#### Constant `IMAGE_DIRECTORY_ENTRY_LOAD_CONFIG`

Load Configuration Directory

```rust
pub const IMAGE_DIRECTORY_ENTRY_LOAD_CONFIG: usize = 10;
```

#### Constant `IMAGE_DIRECTORY_ENTRY_BOUND_IMPORT`

Bound Import Directory in headers

```rust
pub const IMAGE_DIRECTORY_ENTRY_BOUND_IMPORT: usize = 11;
```

#### Constant `IMAGE_DIRECTORY_ENTRY_IAT`

Import Address Table

```rust
pub const IMAGE_DIRECTORY_ENTRY_IAT: usize = 12;
```

#### Constant `IMAGE_DIRECTORY_ENTRY_DELAY_IMPORT`

Delay Load Import Descriptors

```rust
pub const IMAGE_DIRECTORY_ENTRY_DELAY_IMPORT: usize = 13;
```

#### Constant `IMAGE_DIRECTORY_ENTRY_COM_DESCRIPTOR`

COM Runtime descriptor

```rust
pub const IMAGE_DIRECTORY_ENTRY_COM_DESCRIPTOR: usize = 14;
```

#### Constant `ANON_OBJECT_HEADER_BIGOBJ_CLASS_ID`

The required value of `AnonObjectHeaderBigobj::class_id`.

```rust
pub const ANON_OBJECT_HEADER_BIGOBJ_CLASS_ID: ClsId = _;
```

#### Constant `IMAGE_SIZEOF_SHORT_NAME`

```rust
pub const IMAGE_SIZEOF_SHORT_NAME: usize = 8;
```

#### Constant `IMAGE_SIZEOF_SECTION_HEADER`

```rust
pub const IMAGE_SIZEOF_SECTION_HEADER: usize = 40;
```

#### Constant `IMAGE_SCN_TYPE_NO_PAD`

Reserved.

```rust
pub const IMAGE_SCN_TYPE_NO_PAD: u32 = 0x0000_0008;
```

#### Constant `IMAGE_SCN_CNT_CODE`

Section contains code.

```rust
pub const IMAGE_SCN_CNT_CODE: u32 = 0x0000_0020;
```

#### Constant `IMAGE_SCN_CNT_INITIALIZED_DATA`

Section contains initialized data.

```rust
pub const IMAGE_SCN_CNT_INITIALIZED_DATA: u32 = 0x0000_0040;
```

#### Constant `IMAGE_SCN_CNT_UNINITIALIZED_DATA`

Section contains uninitialized data.

```rust
pub const IMAGE_SCN_CNT_UNINITIALIZED_DATA: u32 = 0x0000_0080;
```

#### Constant `IMAGE_SCN_LNK_OTHER`

Reserved.

```rust
pub const IMAGE_SCN_LNK_OTHER: u32 = 0x0000_0100;
```

#### Constant `IMAGE_SCN_LNK_INFO`

Section contains comments or some other type of information.

```rust
pub const IMAGE_SCN_LNK_INFO: u32 = 0x0000_0200;
```

#### Constant `IMAGE_SCN_LNK_REMOVE`

Section contents will not become part of image.

```rust
pub const IMAGE_SCN_LNK_REMOVE: u32 = 0x0000_0800;
```

#### Constant `IMAGE_SCN_LNK_COMDAT`

Section contents comdat.

```rust
pub const IMAGE_SCN_LNK_COMDAT: u32 = 0x0000_1000;
```

#### Constant `IMAGE_SCN_NO_DEFER_SPEC_EXC`

Reset speculative exceptions handling bits in the TLB entries for this section.

```rust
pub const IMAGE_SCN_NO_DEFER_SPEC_EXC: u32 = 0x0000_4000;
```

#### Constant `IMAGE_SCN_GPREL`

Section content can be accessed relative to GP

```rust
pub const IMAGE_SCN_GPREL: u32 = 0x0000_8000;
```

#### Constant `IMAGE_SCN_MEM_FARDATA`

```rust
pub const IMAGE_SCN_MEM_FARDATA: u32 = 0x0000_8000;
```

#### Constant `IMAGE_SCN_MEM_PURGEABLE`

```rust
pub const IMAGE_SCN_MEM_PURGEABLE: u32 = 0x0002_0000;
```

#### Constant `IMAGE_SCN_MEM_16BIT`

```rust
pub const IMAGE_SCN_MEM_16BIT: u32 = 0x0002_0000;
```

#### Constant `IMAGE_SCN_MEM_LOCKED`

```rust
pub const IMAGE_SCN_MEM_LOCKED: u32 = 0x0004_0000;
```

#### Constant `IMAGE_SCN_MEM_PRELOAD`

```rust
pub const IMAGE_SCN_MEM_PRELOAD: u32 = 0x0008_0000;
```

#### Constant `IMAGE_SCN_ALIGN_1BYTES`

```rust
pub const IMAGE_SCN_ALIGN_1BYTES: u32 = 0x0010_0000;
```

#### Constant `IMAGE_SCN_ALIGN_2BYTES`

```rust
pub const IMAGE_SCN_ALIGN_2BYTES: u32 = 0x0020_0000;
```

#### Constant `IMAGE_SCN_ALIGN_4BYTES`

```rust
pub const IMAGE_SCN_ALIGN_4BYTES: u32 = 0x0030_0000;
```

#### Constant `IMAGE_SCN_ALIGN_8BYTES`

```rust
pub const IMAGE_SCN_ALIGN_8BYTES: u32 = 0x0040_0000;
```

#### Constant `IMAGE_SCN_ALIGN_16BYTES`

Default alignment if no others are specified.

```rust
pub const IMAGE_SCN_ALIGN_16BYTES: u32 = 0x0050_0000;
```

#### Constant `IMAGE_SCN_ALIGN_32BYTES`

```rust
pub const IMAGE_SCN_ALIGN_32BYTES: u32 = 0x0060_0000;
```

#### Constant `IMAGE_SCN_ALIGN_64BYTES`

```rust
pub const IMAGE_SCN_ALIGN_64BYTES: u32 = 0x0070_0000;
```

#### Constant `IMAGE_SCN_ALIGN_128BYTES`

```rust
pub const IMAGE_SCN_ALIGN_128BYTES: u32 = 0x0080_0000;
```

#### Constant `IMAGE_SCN_ALIGN_256BYTES`

```rust
pub const IMAGE_SCN_ALIGN_256BYTES: u32 = 0x0090_0000;
```

#### Constant `IMAGE_SCN_ALIGN_512BYTES`

```rust
pub const IMAGE_SCN_ALIGN_512BYTES: u32 = 0x00A0_0000;
```

#### Constant `IMAGE_SCN_ALIGN_1024BYTES`

```rust
pub const IMAGE_SCN_ALIGN_1024BYTES: u32 = 0x00B0_0000;
```

#### Constant `IMAGE_SCN_ALIGN_2048BYTES`

```rust
pub const IMAGE_SCN_ALIGN_2048BYTES: u32 = 0x00C0_0000;
```

#### Constant `IMAGE_SCN_ALIGN_4096BYTES`

```rust
pub const IMAGE_SCN_ALIGN_4096BYTES: u32 = 0x00D0_0000;
```

#### Constant `IMAGE_SCN_ALIGN_8192BYTES`

```rust
pub const IMAGE_SCN_ALIGN_8192BYTES: u32 = 0x00E0_0000;
```

#### Constant `IMAGE_SCN_ALIGN_MASK`

```rust
pub const IMAGE_SCN_ALIGN_MASK: u32 = 0x00F0_0000;
```

#### Constant `IMAGE_SCN_LNK_NRELOC_OVFL`

Section contains extended relocations.

```rust
pub const IMAGE_SCN_LNK_NRELOC_OVFL: u32 = 0x0100_0000;
```

#### Constant `IMAGE_SCN_MEM_DISCARDABLE`

Section can be discarded.

```rust
pub const IMAGE_SCN_MEM_DISCARDABLE: u32 = 0x0200_0000;
```

#### Constant `IMAGE_SCN_MEM_NOT_CACHED`

Section is not cacheable.

```rust
pub const IMAGE_SCN_MEM_NOT_CACHED: u32 = 0x0400_0000;
```

#### Constant `IMAGE_SCN_MEM_NOT_PAGED`

Section is not pageable.

```rust
pub const IMAGE_SCN_MEM_NOT_PAGED: u32 = 0x0800_0000;
```

#### Constant `IMAGE_SCN_MEM_SHARED`

Section is shareable.

```rust
pub const IMAGE_SCN_MEM_SHARED: u32 = 0x1000_0000;
```

#### Constant `IMAGE_SCN_MEM_EXECUTE`

Section is executable.

```rust
pub const IMAGE_SCN_MEM_EXECUTE: u32 = 0x2000_0000;
```

#### Constant `IMAGE_SCN_MEM_READ`

Section is readable.

```rust
pub const IMAGE_SCN_MEM_READ: u32 = 0x4000_0000;
```

#### Constant `IMAGE_SCN_MEM_WRITE`

Section is writeable.

```rust
pub const IMAGE_SCN_MEM_WRITE: u32 = 0x8000_0000;
```

#### Constant `IMAGE_SCN_SCALE_INDEX`

Tls index is scaled

```rust
pub const IMAGE_SCN_SCALE_INDEX: u32 = 0x0000_0001;
```

#### Constant `IMAGE_SIZEOF_SYMBOL`

```rust
pub const IMAGE_SIZEOF_SYMBOL: usize = 18;
```

#### Constant `IMAGE_SIZEOF_SYMBOL_EX`

```rust
pub const IMAGE_SIZEOF_SYMBOL_EX: usize = 20;
```

#### Constant `IMAGE_SYM_UNDEFINED`

Symbol is undefined or is common.

```rust
pub const IMAGE_SYM_UNDEFINED: i32 = 0;
```

#### Constant `IMAGE_SYM_ABSOLUTE`

Symbol is an absolute value.

```rust
pub const IMAGE_SYM_ABSOLUTE: i32 = -1;
```

#### Constant `IMAGE_SYM_DEBUG`

Symbol is a special debug item.

```rust
pub const IMAGE_SYM_DEBUG: i32 = -2;
```

#### Constant `IMAGE_SYM_SECTION_MAX`

Values 0xFF00-0xFFFF are special

```rust
pub const IMAGE_SYM_SECTION_MAX: u16 = 0xFEFF;
```

#### Constant `IMAGE_SYM_SECTION_MAX_EX`

```rust
pub const IMAGE_SYM_SECTION_MAX_EX: u32 = 0x7fff_ffff;
```

#### Constant `IMAGE_SYM_TYPE_NULL`

no type.

```rust
pub const IMAGE_SYM_TYPE_NULL: u16 = 0x0000;
```

#### Constant `IMAGE_SYM_TYPE_VOID`

```rust
pub const IMAGE_SYM_TYPE_VOID: u16 = 0x0001;
```

#### Constant `IMAGE_SYM_TYPE_CHAR`

type character.

```rust
pub const IMAGE_SYM_TYPE_CHAR: u16 = 0x0002;
```

#### Constant `IMAGE_SYM_TYPE_SHORT`

type short integer.

```rust
pub const IMAGE_SYM_TYPE_SHORT: u16 = 0x0003;
```

#### Constant `IMAGE_SYM_TYPE_INT`

```rust
pub const IMAGE_SYM_TYPE_INT: u16 = 0x0004;
```

#### Constant `IMAGE_SYM_TYPE_LONG`

```rust
pub const IMAGE_SYM_TYPE_LONG: u16 = 0x0005;
```

#### Constant `IMAGE_SYM_TYPE_FLOAT`

```rust
pub const IMAGE_SYM_TYPE_FLOAT: u16 = 0x0006;
```

#### Constant `IMAGE_SYM_TYPE_DOUBLE`

```rust
pub const IMAGE_SYM_TYPE_DOUBLE: u16 = 0x0007;
```

#### Constant `IMAGE_SYM_TYPE_STRUCT`

```rust
pub const IMAGE_SYM_TYPE_STRUCT: u16 = 0x0008;
```

#### Constant `IMAGE_SYM_TYPE_UNION`

```rust
pub const IMAGE_SYM_TYPE_UNION: u16 = 0x0009;
```

#### Constant `IMAGE_SYM_TYPE_ENUM`

enumeration.

```rust
pub const IMAGE_SYM_TYPE_ENUM: u16 = 0x000A;
```

#### Constant `IMAGE_SYM_TYPE_MOE`

member of enumeration.

```rust
pub const IMAGE_SYM_TYPE_MOE: u16 = 0x000B;
```

#### Constant `IMAGE_SYM_TYPE_BYTE`

```rust
pub const IMAGE_SYM_TYPE_BYTE: u16 = 0x000C;
```

#### Constant `IMAGE_SYM_TYPE_WORD`

```rust
pub const IMAGE_SYM_TYPE_WORD: u16 = 0x000D;
```

#### Constant `IMAGE_SYM_TYPE_UINT`

```rust
pub const IMAGE_SYM_TYPE_UINT: u16 = 0x000E;
```

#### Constant `IMAGE_SYM_TYPE_DWORD`

```rust
pub const IMAGE_SYM_TYPE_DWORD: u16 = 0x000F;
```

#### Constant `IMAGE_SYM_TYPE_PCODE`

```rust
pub const IMAGE_SYM_TYPE_PCODE: u16 = 0x8000;
```

#### Constant `IMAGE_SYM_DTYPE_NULL`

no derived type.

```rust
pub const IMAGE_SYM_DTYPE_NULL: u16 = 0;
```

#### Constant `IMAGE_SYM_DTYPE_POINTER`

pointer.

```rust
pub const IMAGE_SYM_DTYPE_POINTER: u16 = 1;
```

#### Constant `IMAGE_SYM_DTYPE_FUNCTION`

function.

```rust
pub const IMAGE_SYM_DTYPE_FUNCTION: u16 = 2;
```

#### Constant `IMAGE_SYM_DTYPE_ARRAY`

array.

```rust
pub const IMAGE_SYM_DTYPE_ARRAY: u16 = 3;
```

#### Constant `IMAGE_SYM_CLASS_END_OF_FUNCTION`

```rust
pub const IMAGE_SYM_CLASS_END_OF_FUNCTION: u8 = 0xff;
```

#### Constant `IMAGE_SYM_CLASS_NULL`

```rust
pub const IMAGE_SYM_CLASS_NULL: u8 = 0x00;
```

#### Constant `IMAGE_SYM_CLASS_AUTOMATIC`

```rust
pub const IMAGE_SYM_CLASS_AUTOMATIC: u8 = 0x01;
```

#### Constant `IMAGE_SYM_CLASS_EXTERNAL`

```rust
pub const IMAGE_SYM_CLASS_EXTERNAL: u8 = 0x02;
```

#### Constant `IMAGE_SYM_CLASS_STATIC`

```rust
pub const IMAGE_SYM_CLASS_STATIC: u8 = 0x03;
```

#### Constant `IMAGE_SYM_CLASS_REGISTER`

```rust
pub const IMAGE_SYM_CLASS_REGISTER: u8 = 0x04;
```

#### Constant `IMAGE_SYM_CLASS_EXTERNAL_DEF`

```rust
pub const IMAGE_SYM_CLASS_EXTERNAL_DEF: u8 = 0x05;
```

#### Constant `IMAGE_SYM_CLASS_LABEL`

```rust
pub const IMAGE_SYM_CLASS_LABEL: u8 = 0x06;
```

#### Constant `IMAGE_SYM_CLASS_UNDEFINED_LABEL`

```rust
pub const IMAGE_SYM_CLASS_UNDEFINED_LABEL: u8 = 0x07;
```

#### Constant `IMAGE_SYM_CLASS_MEMBER_OF_STRUCT`

```rust
pub const IMAGE_SYM_CLASS_MEMBER_OF_STRUCT: u8 = 0x08;
```

#### Constant `IMAGE_SYM_CLASS_ARGUMENT`

```rust
pub const IMAGE_SYM_CLASS_ARGUMENT: u8 = 0x09;
```

#### Constant `IMAGE_SYM_CLASS_STRUCT_TAG`

```rust
pub const IMAGE_SYM_CLASS_STRUCT_TAG: u8 = 0x0A;
```

#### Constant `IMAGE_SYM_CLASS_MEMBER_OF_UNION`

```rust
pub const IMAGE_SYM_CLASS_MEMBER_OF_UNION: u8 = 0x0B;
```

#### Constant `IMAGE_SYM_CLASS_UNION_TAG`

```rust
pub const IMAGE_SYM_CLASS_UNION_TAG: u8 = 0x0C;
```

#### Constant `IMAGE_SYM_CLASS_TYPE_DEFINITION`

```rust
pub const IMAGE_SYM_CLASS_TYPE_DEFINITION: u8 = 0x0D;
```

#### Constant `IMAGE_SYM_CLASS_UNDEFINED_STATIC`

```rust
pub const IMAGE_SYM_CLASS_UNDEFINED_STATIC: u8 = 0x0E;
```

#### Constant `IMAGE_SYM_CLASS_ENUM_TAG`

```rust
pub const IMAGE_SYM_CLASS_ENUM_TAG: u8 = 0x0F;
```

#### Constant `IMAGE_SYM_CLASS_MEMBER_OF_ENUM`

```rust
pub const IMAGE_SYM_CLASS_MEMBER_OF_ENUM: u8 = 0x10;
```

#### Constant `IMAGE_SYM_CLASS_REGISTER_PARAM`

```rust
pub const IMAGE_SYM_CLASS_REGISTER_PARAM: u8 = 0x11;
```

#### Constant `IMAGE_SYM_CLASS_BIT_FIELD`

```rust
pub const IMAGE_SYM_CLASS_BIT_FIELD: u8 = 0x12;
```

#### Constant `IMAGE_SYM_CLASS_FAR_EXTERNAL`

```rust
pub const IMAGE_SYM_CLASS_FAR_EXTERNAL: u8 = 0x44;
```

#### Constant `IMAGE_SYM_CLASS_BLOCK`

```rust
pub const IMAGE_SYM_CLASS_BLOCK: u8 = 0x64;
```

#### Constant `IMAGE_SYM_CLASS_FUNCTION`

```rust
pub const IMAGE_SYM_CLASS_FUNCTION: u8 = 0x65;
```

#### Constant `IMAGE_SYM_CLASS_END_OF_STRUCT`

```rust
pub const IMAGE_SYM_CLASS_END_OF_STRUCT: u8 = 0x66;
```

#### Constant `IMAGE_SYM_CLASS_FILE`

```rust
pub const IMAGE_SYM_CLASS_FILE: u8 = 0x67;
```

#### Constant `IMAGE_SYM_CLASS_SECTION`

```rust
pub const IMAGE_SYM_CLASS_SECTION: u8 = 0x68;
```

#### Constant `IMAGE_SYM_CLASS_WEAK_EXTERNAL`

```rust
pub const IMAGE_SYM_CLASS_WEAK_EXTERNAL: u8 = 0x69;
```

#### Constant `IMAGE_SYM_CLASS_CLR_TOKEN`

```rust
pub const IMAGE_SYM_CLASS_CLR_TOKEN: u8 = 0x6B;
```

#### Constant `N_BTMASK`

```rust
pub const N_BTMASK: u16 = 0x000F;
```

#### Constant `N_TMASK`

```rust
pub const N_TMASK: u16 = 0x0030;
```

#### Constant `N_TMASK1`

```rust
pub const N_TMASK1: u16 = 0x00C0;
```

#### Constant `N_TMASK2`

```rust
pub const N_TMASK2: u16 = 0x00F0;
```

#### Constant `N_BTSHFT`

```rust
pub const N_BTSHFT: usize = 4;
```

#### Constant `N_TSHIFT`

```rust
pub const N_TSHIFT: usize = 2;
```

#### Constant `IMAGE_SYM_DTYPE_SHIFT`

```rust
pub const IMAGE_SYM_DTYPE_SHIFT: usize = N_BTSHFT;
```

#### Constant `IMAGE_AUX_SYMBOL_TYPE_TOKEN_DEF`

```rust
pub const IMAGE_AUX_SYMBOL_TYPE_TOKEN_DEF: u16 = 1;
```

#### Constant `IMAGE_COMDAT_SELECT_NODUPLICATES`

```rust
pub const IMAGE_COMDAT_SELECT_NODUPLICATES: u8 = 1;
```

#### Constant `IMAGE_COMDAT_SELECT_ANY`

```rust
pub const IMAGE_COMDAT_SELECT_ANY: u8 = 2;
```

#### Constant `IMAGE_COMDAT_SELECT_SAME_SIZE`

```rust
pub const IMAGE_COMDAT_SELECT_SAME_SIZE: u8 = 3;
```

#### Constant `IMAGE_COMDAT_SELECT_EXACT_MATCH`

```rust
pub const IMAGE_COMDAT_SELECT_EXACT_MATCH: u8 = 4;
```

#### Constant `IMAGE_COMDAT_SELECT_ASSOCIATIVE`

```rust
pub const IMAGE_COMDAT_SELECT_ASSOCIATIVE: u8 = 5;
```

#### Constant `IMAGE_COMDAT_SELECT_LARGEST`

```rust
pub const IMAGE_COMDAT_SELECT_LARGEST: u8 = 6;
```

#### Constant `IMAGE_COMDAT_SELECT_NEWEST`

```rust
pub const IMAGE_COMDAT_SELECT_NEWEST: u8 = 7;
```

#### Constant `IMAGE_WEAK_EXTERN_SEARCH_NOLIBRARY`

```rust
pub const IMAGE_WEAK_EXTERN_SEARCH_NOLIBRARY: u16 = 1;
```

#### Constant `IMAGE_WEAK_EXTERN_SEARCH_LIBRARY`

```rust
pub const IMAGE_WEAK_EXTERN_SEARCH_LIBRARY: u16 = 2;
```

#### Constant `IMAGE_WEAK_EXTERN_SEARCH_ALIAS`

```rust
pub const IMAGE_WEAK_EXTERN_SEARCH_ALIAS: u16 = 3;
```

#### Constant `IMAGE_WEAK_EXTERN_ANTI_DEPENDENCY`

```rust
pub const IMAGE_WEAK_EXTERN_ANTI_DEPENDENCY: u16 = 4;
```

#### Constant `IMAGE_REL_I386_ABSOLUTE`

Reference is absolute, no relocation is necessary

```rust
pub const IMAGE_REL_I386_ABSOLUTE: u16 = 0x0000;
```

#### Constant `IMAGE_REL_I386_DIR16`

Direct 16-bit reference to the symbols virtual address

```rust
pub const IMAGE_REL_I386_DIR16: u16 = 0x0001;
```

#### Constant `IMAGE_REL_I386_REL16`

PC-relative 16-bit reference to the symbols virtual address

```rust
pub const IMAGE_REL_I386_REL16: u16 = 0x0002;
```

#### Constant `IMAGE_REL_I386_DIR32`

Direct 32-bit reference to the symbols virtual address

```rust
pub const IMAGE_REL_I386_DIR32: u16 = 0x0006;
```

#### Constant `IMAGE_REL_I386_DIR32NB`

Direct 32-bit reference to the symbols virtual address, base not included

```rust
pub const IMAGE_REL_I386_DIR32NB: u16 = 0x0007;
```

#### Constant `IMAGE_REL_I386_SEG12`

Direct 16-bit reference to the segment-selector bits of a 32-bit virtual address

```rust
pub const IMAGE_REL_I386_SEG12: u16 = 0x0009;
```

#### Constant `IMAGE_REL_I386_SECTION`

```rust
pub const IMAGE_REL_I386_SECTION: u16 = 0x000A;
```

#### Constant `IMAGE_REL_I386_SECREL`

```rust
pub const IMAGE_REL_I386_SECREL: u16 = 0x000B;
```

#### Constant `IMAGE_REL_I386_TOKEN`

clr token

```rust
pub const IMAGE_REL_I386_TOKEN: u16 = 0x000C;
```

#### Constant `IMAGE_REL_I386_SECREL7`

7 bit offset from base of section containing target

```rust
pub const IMAGE_REL_I386_SECREL7: u16 = 0x000D;
```

#### Constant `IMAGE_REL_I386_REL32`

PC-relative 32-bit reference to the symbols virtual address

```rust
pub const IMAGE_REL_I386_REL32: u16 = 0x0014;
```

#### Constant `IMAGE_REL_MIPS_ABSOLUTE`

Reference is absolute, no relocation is necessary

```rust
pub const IMAGE_REL_MIPS_ABSOLUTE: u16 = 0x0000;
```

#### Constant `IMAGE_REL_MIPS_REFHALF`

```rust
pub const IMAGE_REL_MIPS_REFHALF: u16 = 0x0001;
```

#### Constant `IMAGE_REL_MIPS_REFWORD`

```rust
pub const IMAGE_REL_MIPS_REFWORD: u16 = 0x0002;
```

#### Constant `IMAGE_REL_MIPS_JMPADDR`

```rust
pub const IMAGE_REL_MIPS_JMPADDR: u16 = 0x0003;
```

#### Constant `IMAGE_REL_MIPS_REFHI`

```rust
pub const IMAGE_REL_MIPS_REFHI: u16 = 0x0004;
```

#### Constant `IMAGE_REL_MIPS_REFLO`

```rust
pub const IMAGE_REL_MIPS_REFLO: u16 = 0x0005;
```

#### Constant `IMAGE_REL_MIPS_GPREL`

```rust
pub const IMAGE_REL_MIPS_GPREL: u16 = 0x0006;
```

#### Constant `IMAGE_REL_MIPS_LITERAL`

```rust
pub const IMAGE_REL_MIPS_LITERAL: u16 = 0x0007;
```

#### Constant `IMAGE_REL_MIPS_SECTION`

```rust
pub const IMAGE_REL_MIPS_SECTION: u16 = 0x000A;
```

#### Constant `IMAGE_REL_MIPS_SECREL`

```rust
pub const IMAGE_REL_MIPS_SECREL: u16 = 0x000B;
```

#### Constant `IMAGE_REL_MIPS_SECRELLO`

Low 16-bit section relative reference (used for >32k TLS)

```rust
pub const IMAGE_REL_MIPS_SECRELLO: u16 = 0x000C;
```

#### Constant `IMAGE_REL_MIPS_SECRELHI`

High 16-bit section relative reference (used for >32k TLS)

```rust
pub const IMAGE_REL_MIPS_SECRELHI: u16 = 0x000D;
```

#### Constant `IMAGE_REL_MIPS_TOKEN`

clr token

```rust
pub const IMAGE_REL_MIPS_TOKEN: u16 = 0x000E;
```

#### Constant `IMAGE_REL_MIPS_JMPADDR16`

```rust
pub const IMAGE_REL_MIPS_JMPADDR16: u16 = 0x0010;
```

#### Constant `IMAGE_REL_MIPS_REFWORDNB`

```rust
pub const IMAGE_REL_MIPS_REFWORDNB: u16 = 0x0022;
```

#### Constant `IMAGE_REL_MIPS_PAIR`

```rust
pub const IMAGE_REL_MIPS_PAIR: u16 = 0x0025;
```

#### Constant `IMAGE_REL_ALPHA_ABSOLUTE`

```rust
pub const IMAGE_REL_ALPHA_ABSOLUTE: u16 = 0x0000;
```

#### Constant `IMAGE_REL_ALPHA_REFLONG`

```rust
pub const IMAGE_REL_ALPHA_REFLONG: u16 = 0x0001;
```

#### Constant `IMAGE_REL_ALPHA_REFQUAD`

```rust
pub const IMAGE_REL_ALPHA_REFQUAD: u16 = 0x0002;
```

#### Constant `IMAGE_REL_ALPHA_GPREL32`

```rust
pub const IMAGE_REL_ALPHA_GPREL32: u16 = 0x0003;
```

#### Constant `IMAGE_REL_ALPHA_LITERAL`

```rust
pub const IMAGE_REL_ALPHA_LITERAL: u16 = 0x0004;
```

#### Constant `IMAGE_REL_ALPHA_LITUSE`

```rust
pub const IMAGE_REL_ALPHA_LITUSE: u16 = 0x0005;
```

#### Constant `IMAGE_REL_ALPHA_GPDISP`

```rust
pub const IMAGE_REL_ALPHA_GPDISP: u16 = 0x0006;
```

#### Constant `IMAGE_REL_ALPHA_BRADDR`

```rust
pub const IMAGE_REL_ALPHA_BRADDR: u16 = 0x0007;
```

#### Constant `IMAGE_REL_ALPHA_HINT`

```rust
pub const IMAGE_REL_ALPHA_HINT: u16 = 0x0008;
```

#### Constant `IMAGE_REL_ALPHA_INLINE_REFLONG`

```rust
pub const IMAGE_REL_ALPHA_INLINE_REFLONG: u16 = 0x0009;
```

#### Constant `IMAGE_REL_ALPHA_REFHI`

```rust
pub const IMAGE_REL_ALPHA_REFHI: u16 = 0x000A;
```

#### Constant `IMAGE_REL_ALPHA_REFLO`

```rust
pub const IMAGE_REL_ALPHA_REFLO: u16 = 0x000B;
```

#### Constant `IMAGE_REL_ALPHA_PAIR`

```rust
pub const IMAGE_REL_ALPHA_PAIR: u16 = 0x000C;
```

#### Constant `IMAGE_REL_ALPHA_MATCH`

```rust
pub const IMAGE_REL_ALPHA_MATCH: u16 = 0x000D;
```

#### Constant `IMAGE_REL_ALPHA_SECTION`

```rust
pub const IMAGE_REL_ALPHA_SECTION: u16 = 0x000E;
```

#### Constant `IMAGE_REL_ALPHA_SECREL`

```rust
pub const IMAGE_REL_ALPHA_SECREL: u16 = 0x000F;
```

#### Constant `IMAGE_REL_ALPHA_REFLONGNB`

```rust
pub const IMAGE_REL_ALPHA_REFLONGNB: u16 = 0x0010;
```

#### Constant `IMAGE_REL_ALPHA_SECRELLO`

Low 16-bit section relative reference

```rust
pub const IMAGE_REL_ALPHA_SECRELLO: u16 = 0x0011;
```

#### Constant `IMAGE_REL_ALPHA_SECRELHI`

High 16-bit section relative reference

```rust
pub const IMAGE_REL_ALPHA_SECRELHI: u16 = 0x0012;
```

#### Constant `IMAGE_REL_ALPHA_REFQ3`

High 16 bits of 48 bit reference

```rust
pub const IMAGE_REL_ALPHA_REFQ3: u16 = 0x0013;
```

#### Constant `IMAGE_REL_ALPHA_REFQ2`

Middle 16 bits of 48 bit reference

```rust
pub const IMAGE_REL_ALPHA_REFQ2: u16 = 0x0014;
```

#### Constant `IMAGE_REL_ALPHA_REFQ1`

Low 16 bits of 48 bit reference

```rust
pub const IMAGE_REL_ALPHA_REFQ1: u16 = 0x0015;
```

#### Constant `IMAGE_REL_ALPHA_GPRELLO`

Low 16-bit GP relative reference

```rust
pub const IMAGE_REL_ALPHA_GPRELLO: u16 = 0x0016;
```

#### Constant `IMAGE_REL_ALPHA_GPRELHI`

High 16-bit GP relative reference

```rust
pub const IMAGE_REL_ALPHA_GPRELHI: u16 = 0x0017;
```

#### Constant `IMAGE_REL_PPC_ABSOLUTE`

NOP

```rust
pub const IMAGE_REL_PPC_ABSOLUTE: u16 = 0x0000;
```

#### Constant `IMAGE_REL_PPC_ADDR64`

64-bit address

```rust
pub const IMAGE_REL_PPC_ADDR64: u16 = 0x0001;
```

#### Constant `IMAGE_REL_PPC_ADDR32`

32-bit address

```rust
pub const IMAGE_REL_PPC_ADDR32: u16 = 0x0002;
```

#### Constant `IMAGE_REL_PPC_ADDR24`

26-bit address, shifted left 2 (branch absolute)

```rust
pub const IMAGE_REL_PPC_ADDR24: u16 = 0x0003;
```

#### Constant `IMAGE_REL_PPC_ADDR16`

16-bit address

```rust
pub const IMAGE_REL_PPC_ADDR16: u16 = 0x0004;
```

#### Constant `IMAGE_REL_PPC_ADDR14`

16-bit address, shifted left 2 (load doubleword)

```rust
pub const IMAGE_REL_PPC_ADDR14: u16 = 0x0005;
```

#### Constant `IMAGE_REL_PPC_REL24`

26-bit PC-relative offset, shifted left 2 (branch relative)

```rust
pub const IMAGE_REL_PPC_REL24: u16 = 0x0006;
```

#### Constant `IMAGE_REL_PPC_REL14`

16-bit PC-relative offset, shifted left 2 (br cond relative)

```rust
pub const IMAGE_REL_PPC_REL14: u16 = 0x0007;
```

#### Constant `IMAGE_REL_PPC_TOCREL16`

16-bit offset from TOC base

```rust
pub const IMAGE_REL_PPC_TOCREL16: u16 = 0x0008;
```

#### Constant `IMAGE_REL_PPC_TOCREL14`

16-bit offset from TOC base, shifted left 2 (load doubleword)

```rust
pub const IMAGE_REL_PPC_TOCREL14: u16 = 0x0009;
```

#### Constant `IMAGE_REL_PPC_ADDR32NB`

32-bit addr w/o image base

```rust
pub const IMAGE_REL_PPC_ADDR32NB: u16 = 0x000A;
```

#### Constant `IMAGE_REL_PPC_SECREL`

va of containing section (as in an image sectionhdr)

```rust
pub const IMAGE_REL_PPC_SECREL: u16 = 0x000B;
```

#### Constant `IMAGE_REL_PPC_SECTION`

sectionheader number

```rust
pub const IMAGE_REL_PPC_SECTION: u16 = 0x000C;
```

#### Constant `IMAGE_REL_PPC_IFGLUE`

substitute TOC restore instruction iff symbol is glue code

```rust
pub const IMAGE_REL_PPC_IFGLUE: u16 = 0x000D;
```

#### Constant `IMAGE_REL_PPC_IMGLUE`

symbol is glue code; virtual address is TOC restore instruction

```rust
pub const IMAGE_REL_PPC_IMGLUE: u16 = 0x000E;
```

#### Constant `IMAGE_REL_PPC_SECREL16`

va of containing section (limited to 16 bits)

```rust
pub const IMAGE_REL_PPC_SECREL16: u16 = 0x000F;
```

#### Constant `IMAGE_REL_PPC_REFHI`

```rust
pub const IMAGE_REL_PPC_REFHI: u16 = 0x0010;
```

#### Constant `IMAGE_REL_PPC_REFLO`

```rust
pub const IMAGE_REL_PPC_REFLO: u16 = 0x0011;
```

#### Constant `IMAGE_REL_PPC_PAIR`

```rust
pub const IMAGE_REL_PPC_PAIR: u16 = 0x0012;
```

#### Constant `IMAGE_REL_PPC_SECRELLO`

Low 16-bit section relative reference (used for >32k TLS)

```rust
pub const IMAGE_REL_PPC_SECRELLO: u16 = 0x0013;
```

#### Constant `IMAGE_REL_PPC_SECRELHI`

High 16-bit section relative reference (used for >32k TLS)

```rust
pub const IMAGE_REL_PPC_SECRELHI: u16 = 0x0014;
```

#### Constant `IMAGE_REL_PPC_GPREL`

```rust
pub const IMAGE_REL_PPC_GPREL: u16 = 0x0015;
```

#### Constant `IMAGE_REL_PPC_TOKEN`

clr token

```rust
pub const IMAGE_REL_PPC_TOKEN: u16 = 0x0016;
```

#### Constant `IMAGE_REL_PPC_TYPEMASK`

mask to isolate above values in IMAGE_RELOCATION.Type

```rust
pub const IMAGE_REL_PPC_TYPEMASK: u16 = 0x00FF;
```

#### Constant `IMAGE_REL_PPC_NEG`

subtract reloc value rather than adding it

```rust
pub const IMAGE_REL_PPC_NEG: u16 = 0x0100;
```

#### Constant `IMAGE_REL_PPC_BRTAKEN`

fix branch prediction bit to predict branch taken

```rust
pub const IMAGE_REL_PPC_BRTAKEN: u16 = 0x0200;
```

#### Constant `IMAGE_REL_PPC_BRNTAKEN`

fix branch prediction bit to predict branch not taken

```rust
pub const IMAGE_REL_PPC_BRNTAKEN: u16 = 0x0400;
```

#### Constant `IMAGE_REL_PPC_TOCDEFN`

toc slot defined in file (or, data in toc)

```rust
pub const IMAGE_REL_PPC_TOCDEFN: u16 = 0x0800;
```

#### Constant `IMAGE_REL_SH3_ABSOLUTE`

No relocation

```rust
pub const IMAGE_REL_SH3_ABSOLUTE: u16 = 0x0000;
```

#### Constant `IMAGE_REL_SH3_DIRECT16`

16 bit direct

```rust
pub const IMAGE_REL_SH3_DIRECT16: u16 = 0x0001;
```

#### Constant `IMAGE_REL_SH3_DIRECT32`

32 bit direct

```rust
pub const IMAGE_REL_SH3_DIRECT32: u16 = 0x0002;
```

#### Constant `IMAGE_REL_SH3_DIRECT8`

8 bit direct, -128..255

```rust
pub const IMAGE_REL_SH3_DIRECT8: u16 = 0x0003;
```

#### Constant `IMAGE_REL_SH3_DIRECT8_WORD`

8 bit direct .W (0 ext.)

```rust
pub const IMAGE_REL_SH3_DIRECT8_WORD: u16 = 0x0004;
```

#### Constant `IMAGE_REL_SH3_DIRECT8_LONG`

8 bit direct .L (0 ext.)

```rust
pub const IMAGE_REL_SH3_DIRECT8_LONG: u16 = 0x0005;
```

#### Constant `IMAGE_REL_SH3_DIRECT4`

4 bit direct (0 ext.)

```rust
pub const IMAGE_REL_SH3_DIRECT4: u16 = 0x0006;
```

#### Constant `IMAGE_REL_SH3_DIRECT4_WORD`

4 bit direct .W (0 ext.)

```rust
pub const IMAGE_REL_SH3_DIRECT4_WORD: u16 = 0x0007;
```

#### Constant `IMAGE_REL_SH3_DIRECT4_LONG`

4 bit direct .L (0 ext.)

```rust
pub const IMAGE_REL_SH3_DIRECT4_LONG: u16 = 0x0008;
```

#### Constant `IMAGE_REL_SH3_PCREL8_WORD`

8 bit PC relative .W

```rust
pub const IMAGE_REL_SH3_PCREL8_WORD: u16 = 0x0009;
```

#### Constant `IMAGE_REL_SH3_PCREL8_LONG`

8 bit PC relative .L

```rust
pub const IMAGE_REL_SH3_PCREL8_LONG: u16 = 0x000A;
```

#### Constant `IMAGE_REL_SH3_PCREL12_WORD`

12 LSB PC relative .W

```rust
pub const IMAGE_REL_SH3_PCREL12_WORD: u16 = 0x000B;
```

#### Constant `IMAGE_REL_SH3_STARTOF_SECTION`

Start of EXE section

```rust
pub const IMAGE_REL_SH3_STARTOF_SECTION: u16 = 0x000C;
```

#### Constant `IMAGE_REL_SH3_SIZEOF_SECTION`

Size of EXE section

```rust
pub const IMAGE_REL_SH3_SIZEOF_SECTION: u16 = 0x000D;
```

#### Constant `IMAGE_REL_SH3_SECTION`

Section table index

```rust
pub const IMAGE_REL_SH3_SECTION: u16 = 0x000E;
```

#### Constant `IMAGE_REL_SH3_SECREL`

Offset within section

```rust
pub const IMAGE_REL_SH3_SECREL: u16 = 0x000F;
```

#### Constant `IMAGE_REL_SH3_DIRECT32_NB`

32 bit direct not based

```rust
pub const IMAGE_REL_SH3_DIRECT32_NB: u16 = 0x0010;
```

#### Constant `IMAGE_REL_SH3_GPREL4_LONG`

GP-relative addressing

```rust
pub const IMAGE_REL_SH3_GPREL4_LONG: u16 = 0x0011;
```

#### Constant `IMAGE_REL_SH3_TOKEN`

clr token

```rust
pub const IMAGE_REL_SH3_TOKEN: u16 = 0x0012;
```

#### Constant `IMAGE_REL_SHM_PCRELPT`

Offset from current instruction in longwords
if not NOMODE, insert the inverse of the low bit at bit 32 to select PTA/PTB

```rust
pub const IMAGE_REL_SHM_PCRELPT: u16 = 0x0013;
```

#### Constant `IMAGE_REL_SHM_REFLO`

Low bits of 32-bit address

```rust
pub const IMAGE_REL_SHM_REFLO: u16 = 0x0014;
```

#### Constant `IMAGE_REL_SHM_REFHALF`

High bits of 32-bit address

```rust
pub const IMAGE_REL_SHM_REFHALF: u16 = 0x0015;
```

#### Constant `IMAGE_REL_SHM_RELLO`

Low bits of relative reference

```rust
pub const IMAGE_REL_SHM_RELLO: u16 = 0x0016;
```

#### Constant `IMAGE_REL_SHM_RELHALF`

High bits of relative reference

```rust
pub const IMAGE_REL_SHM_RELHALF: u16 = 0x0017;
```

#### Constant `IMAGE_REL_SHM_PAIR`

offset operand for relocation

```rust
pub const IMAGE_REL_SHM_PAIR: u16 = 0x0018;
```

#### Constant `IMAGE_REL_SH_NOMODE`

relocation ignores section mode

```rust
pub const IMAGE_REL_SH_NOMODE: u16 = 0x8000;
```

#### Constant `IMAGE_REL_ARM_ABSOLUTE`

No relocation required

```rust
pub const IMAGE_REL_ARM_ABSOLUTE: u16 = 0x0000;
```

#### Constant `IMAGE_REL_ARM_ADDR32`

32 bit address

```rust
pub const IMAGE_REL_ARM_ADDR32: u16 = 0x0001;
```

#### Constant `IMAGE_REL_ARM_ADDR32NB`

32 bit address w/o image base

```rust
pub const IMAGE_REL_ARM_ADDR32NB: u16 = 0x0002;
```

#### Constant `IMAGE_REL_ARM_BRANCH24`

24 bit offset << 2 & sign ext.

```rust
pub const IMAGE_REL_ARM_BRANCH24: u16 = 0x0003;
```

#### Constant `IMAGE_REL_ARM_BRANCH11`

Thumb: 2 11 bit offsets

```rust
pub const IMAGE_REL_ARM_BRANCH11: u16 = 0x0004;
```

#### Constant `IMAGE_REL_ARM_TOKEN`

clr token

```rust
pub const IMAGE_REL_ARM_TOKEN: u16 = 0x0005;
```

#### Constant `IMAGE_REL_ARM_GPREL12`

GP-relative addressing (ARM)

```rust
pub const IMAGE_REL_ARM_GPREL12: u16 = 0x0006;
```

#### Constant `IMAGE_REL_ARM_GPREL7`

GP-relative addressing (Thumb)

```rust
pub const IMAGE_REL_ARM_GPREL7: u16 = 0x0007;
```

#### Constant `IMAGE_REL_ARM_BLX24`

```rust
pub const IMAGE_REL_ARM_BLX24: u16 = 0x0008;
```

#### Constant `IMAGE_REL_ARM_BLX11`

```rust
pub const IMAGE_REL_ARM_BLX11: u16 = 0x0009;
```

#### Constant `IMAGE_REL_ARM_REL32`

32-bit relative address from byte following reloc

```rust
pub const IMAGE_REL_ARM_REL32: u16 = 0x000A;
```

#### Constant `IMAGE_REL_ARM_SECTION`

Section table index

```rust
pub const IMAGE_REL_ARM_SECTION: u16 = 0x000E;
```

#### Constant `IMAGE_REL_ARM_SECREL`

Offset within section

```rust
pub const IMAGE_REL_ARM_SECREL: u16 = 0x000F;
```

#### Constant `IMAGE_REL_ARM_MOV32A`

ARM: MOVW/MOVT

```rust
pub const IMAGE_REL_ARM_MOV32A: u16 = 0x0010;
```

#### Constant `IMAGE_REL_ARM_MOV32`

ARM: MOVW/MOVT (deprecated)

```rust
pub const IMAGE_REL_ARM_MOV32: u16 = 0x0010;
```

#### Constant `IMAGE_REL_ARM_MOV32T`

Thumb: MOVW/MOVT

```rust
pub const IMAGE_REL_ARM_MOV32T: u16 = 0x0011;
```

#### Constant `IMAGE_REL_THUMB_MOV32`

Thumb: MOVW/MOVT (deprecated)

```rust
pub const IMAGE_REL_THUMB_MOV32: u16 = 0x0011;
```

#### Constant `IMAGE_REL_ARM_BRANCH20T`

Thumb: 32-bit conditional B

```rust
pub const IMAGE_REL_ARM_BRANCH20T: u16 = 0x0012;
```

#### Constant `IMAGE_REL_THUMB_BRANCH20`

Thumb: 32-bit conditional B (deprecated)

```rust
pub const IMAGE_REL_THUMB_BRANCH20: u16 = 0x0012;
```

#### Constant `IMAGE_REL_ARM_BRANCH24T`

Thumb: 32-bit B or BL

```rust
pub const IMAGE_REL_ARM_BRANCH24T: u16 = 0x0014;
```

#### Constant `IMAGE_REL_THUMB_BRANCH24`

Thumb: 32-bit B or BL (deprecated)

```rust
pub const IMAGE_REL_THUMB_BRANCH24: u16 = 0x0014;
```

#### Constant `IMAGE_REL_ARM_BLX23T`

Thumb: BLX immediate

```rust
pub const IMAGE_REL_ARM_BLX23T: u16 = 0x0015;
```

#### Constant `IMAGE_REL_THUMB_BLX23`

Thumb: BLX immediate (deprecated)

```rust
pub const IMAGE_REL_THUMB_BLX23: u16 = 0x0015;
```

#### Constant `IMAGE_REL_AM_ABSOLUTE`

```rust
pub const IMAGE_REL_AM_ABSOLUTE: u16 = 0x0000;
```

#### Constant `IMAGE_REL_AM_ADDR32`

```rust
pub const IMAGE_REL_AM_ADDR32: u16 = 0x0001;
```

#### Constant `IMAGE_REL_AM_ADDR32NB`

```rust
pub const IMAGE_REL_AM_ADDR32NB: u16 = 0x0002;
```

#### Constant `IMAGE_REL_AM_CALL32`

```rust
pub const IMAGE_REL_AM_CALL32: u16 = 0x0003;
```

#### Constant `IMAGE_REL_AM_FUNCINFO`

```rust
pub const IMAGE_REL_AM_FUNCINFO: u16 = 0x0004;
```

#### Constant `IMAGE_REL_AM_REL32_1`

```rust
pub const IMAGE_REL_AM_REL32_1: u16 = 0x0005;
```

#### Constant `IMAGE_REL_AM_REL32_2`

```rust
pub const IMAGE_REL_AM_REL32_2: u16 = 0x0006;
```

#### Constant `IMAGE_REL_AM_SECREL`

```rust
pub const IMAGE_REL_AM_SECREL: u16 = 0x0007;
```

#### Constant `IMAGE_REL_AM_SECTION`

```rust
pub const IMAGE_REL_AM_SECTION: u16 = 0x0008;
```

#### Constant `IMAGE_REL_AM_TOKEN`

```rust
pub const IMAGE_REL_AM_TOKEN: u16 = 0x0009;
```

#### Constant `IMAGE_REL_ARM64_ABSOLUTE`

No relocation required

```rust
pub const IMAGE_REL_ARM64_ABSOLUTE: u16 = 0x0000;
```

#### Constant `IMAGE_REL_ARM64_ADDR32`

32 bit address. Review! do we need it?

```rust
pub const IMAGE_REL_ARM64_ADDR32: u16 = 0x0001;
```

#### Constant `IMAGE_REL_ARM64_ADDR32NB`

32 bit address w/o image base (RVA: for Data/PData/XData)

```rust
pub const IMAGE_REL_ARM64_ADDR32NB: u16 = 0x0002;
```

#### Constant `IMAGE_REL_ARM64_BRANCH26`

26 bit offset << 2 & sign ext. for B & BL

```rust
pub const IMAGE_REL_ARM64_BRANCH26: u16 = 0x0003;
```

#### Constant `IMAGE_REL_ARM64_PAGEBASE_REL21`

ADRP

```rust
pub const IMAGE_REL_ARM64_PAGEBASE_REL21: u16 = 0x0004;
```

#### Constant `IMAGE_REL_ARM64_REL21`

ADR

```rust
pub const IMAGE_REL_ARM64_REL21: u16 = 0x0005;
```

#### Constant `IMAGE_REL_ARM64_PAGEOFFSET_12A`

ADD/ADDS (immediate) with zero shift, for page offset

```rust
pub const IMAGE_REL_ARM64_PAGEOFFSET_12A: u16 = 0x0006;
```

#### Constant `IMAGE_REL_ARM64_PAGEOFFSET_12L`

LDR (indexed, unsigned immediate), for page offset

```rust
pub const IMAGE_REL_ARM64_PAGEOFFSET_12L: u16 = 0x0007;
```

#### Constant `IMAGE_REL_ARM64_SECREL`

Offset within section

```rust
pub const IMAGE_REL_ARM64_SECREL: u16 = 0x0008;
```

#### Constant `IMAGE_REL_ARM64_SECREL_LOW12A`

ADD/ADDS (immediate) with zero shift, for bit 0:11 of section offset

```rust
pub const IMAGE_REL_ARM64_SECREL_LOW12A: u16 = 0x0009;
```

#### Constant `IMAGE_REL_ARM64_SECREL_HIGH12A`

ADD/ADDS (immediate) with zero shift, for bit 12:23 of section offset

```rust
pub const IMAGE_REL_ARM64_SECREL_HIGH12A: u16 = 0x000A;
```

#### Constant `IMAGE_REL_ARM64_SECREL_LOW12L`

LDR (indexed, unsigned immediate), for bit 0:11 of section offset

```rust
pub const IMAGE_REL_ARM64_SECREL_LOW12L: u16 = 0x000B;
```

#### Constant `IMAGE_REL_ARM64_TOKEN`

```rust
pub const IMAGE_REL_ARM64_TOKEN: u16 = 0x000C;
```

#### Constant `IMAGE_REL_ARM64_SECTION`

Section table index

```rust
pub const IMAGE_REL_ARM64_SECTION: u16 = 0x000D;
```

#### Constant `IMAGE_REL_ARM64_ADDR64`

64 bit address

```rust
pub const IMAGE_REL_ARM64_ADDR64: u16 = 0x000E;
```

#### Constant `IMAGE_REL_ARM64_BRANCH19`

19 bit offset << 2 & sign ext. for conditional B

```rust
pub const IMAGE_REL_ARM64_BRANCH19: u16 = 0x000F;
```

#### Constant `IMAGE_REL_ARM64_BRANCH14`

TBZ/TBNZ

```rust
pub const IMAGE_REL_ARM64_BRANCH14: u16 = 0x0010;
```

#### Constant `IMAGE_REL_ARM64_REL32`

32-bit relative address from byte following reloc

```rust
pub const IMAGE_REL_ARM64_REL32: u16 = 0x0011;
```

#### Constant `IMAGE_REL_AMD64_ABSOLUTE`

Reference is absolute, no relocation is necessary

```rust
pub const IMAGE_REL_AMD64_ABSOLUTE: u16 = 0x0000;
```

#### Constant `IMAGE_REL_AMD64_ADDR64`

64-bit address (VA).

```rust
pub const IMAGE_REL_AMD64_ADDR64: u16 = 0x0001;
```

#### Constant `IMAGE_REL_AMD64_ADDR32`

32-bit address (VA).

```rust
pub const IMAGE_REL_AMD64_ADDR32: u16 = 0x0002;
```

#### Constant `IMAGE_REL_AMD64_ADDR32NB`

32-bit address w/o image base (RVA).

```rust
pub const IMAGE_REL_AMD64_ADDR32NB: u16 = 0x0003;
```

#### Constant `IMAGE_REL_AMD64_REL32`

32-bit relative address from byte following reloc

```rust
pub const IMAGE_REL_AMD64_REL32: u16 = 0x0004;
```

#### Constant `IMAGE_REL_AMD64_REL32_1`

32-bit relative address from byte distance 1 from reloc

```rust
pub const IMAGE_REL_AMD64_REL32_1: u16 = 0x0005;
```

#### Constant `IMAGE_REL_AMD64_REL32_2`

32-bit relative address from byte distance 2 from reloc

```rust
pub const IMAGE_REL_AMD64_REL32_2: u16 = 0x0006;
```

#### Constant `IMAGE_REL_AMD64_REL32_3`

32-bit relative address from byte distance 3 from reloc

```rust
pub const IMAGE_REL_AMD64_REL32_3: u16 = 0x0007;
```

#### Constant `IMAGE_REL_AMD64_REL32_4`

32-bit relative address from byte distance 4 from reloc

```rust
pub const IMAGE_REL_AMD64_REL32_4: u16 = 0x0008;
```

#### Constant `IMAGE_REL_AMD64_REL32_5`

32-bit relative address from byte distance 5 from reloc

```rust
pub const IMAGE_REL_AMD64_REL32_5: u16 = 0x0009;
```

#### Constant `IMAGE_REL_AMD64_SECTION`

Section index

```rust
pub const IMAGE_REL_AMD64_SECTION: u16 = 0x000A;
```

#### Constant `IMAGE_REL_AMD64_SECREL`

32 bit offset from base of section containing target

```rust
pub const IMAGE_REL_AMD64_SECREL: u16 = 0x000B;
```

#### Constant `IMAGE_REL_AMD64_SECREL7`

7 bit unsigned offset from base of section containing target

```rust
pub const IMAGE_REL_AMD64_SECREL7: u16 = 0x000C;
```

#### Constant `IMAGE_REL_AMD64_TOKEN`

32 bit metadata token

```rust
pub const IMAGE_REL_AMD64_TOKEN: u16 = 0x000D;
```

#### Constant `IMAGE_REL_AMD64_SREL32`

32 bit signed span-dependent value emitted into object

```rust
pub const IMAGE_REL_AMD64_SREL32: u16 = 0x000E;
```

#### Constant `IMAGE_REL_AMD64_PAIR`

```rust
pub const IMAGE_REL_AMD64_PAIR: u16 = 0x000F;
```

#### Constant `IMAGE_REL_AMD64_SSPAN32`

32 bit signed span-dependent value applied at link time

```rust
pub const IMAGE_REL_AMD64_SSPAN32: u16 = 0x0010;
```

#### Constant `IMAGE_REL_AMD64_EHANDLER`

```rust
pub const IMAGE_REL_AMD64_EHANDLER: u16 = 0x0011;
```

#### Constant `IMAGE_REL_AMD64_IMPORT_BR`

Indirect branch to an import

```rust
pub const IMAGE_REL_AMD64_IMPORT_BR: u16 = 0x0012;
```

#### Constant `IMAGE_REL_AMD64_IMPORT_CALL`

Indirect call to an import

```rust
pub const IMAGE_REL_AMD64_IMPORT_CALL: u16 = 0x0013;
```

#### Constant `IMAGE_REL_AMD64_CFG_BR`

Indirect branch to a CFG check

```rust
pub const IMAGE_REL_AMD64_CFG_BR: u16 = 0x0014;
```

#### Constant `IMAGE_REL_AMD64_CFG_BR_REX`

Indirect branch to a CFG check, with REX.W prefix

```rust
pub const IMAGE_REL_AMD64_CFG_BR_REX: u16 = 0x0015;
```

#### Constant `IMAGE_REL_AMD64_CFG_CALL`

Indirect call to a CFG check

```rust
pub const IMAGE_REL_AMD64_CFG_CALL: u16 = 0x0016;
```

#### Constant `IMAGE_REL_AMD64_INDIR_BR`

Indirect branch to a target in RAX (no CFG)

```rust
pub const IMAGE_REL_AMD64_INDIR_BR: u16 = 0x0017;
```

#### Constant `IMAGE_REL_AMD64_INDIR_BR_REX`

Indirect branch to a target in RAX, with REX.W prefix (no CFG)

```rust
pub const IMAGE_REL_AMD64_INDIR_BR_REX: u16 = 0x0018;
```

#### Constant `IMAGE_REL_AMD64_INDIR_CALL`

Indirect call to a target in RAX (no CFG)

```rust
pub const IMAGE_REL_AMD64_INDIR_CALL: u16 = 0x0019;
```

#### Constant `IMAGE_REL_AMD64_INDIR_BR_SWITCHTABLE_FIRST`

Indirect branch for a switch table using Reg 0 (RAX)

```rust
pub const IMAGE_REL_AMD64_INDIR_BR_SWITCHTABLE_FIRST: u16 = 0x0020;
```

#### Constant `IMAGE_REL_AMD64_INDIR_BR_SWITCHTABLE_LAST`

Indirect branch for a switch table using Reg 15 (R15)

```rust
pub const IMAGE_REL_AMD64_INDIR_BR_SWITCHTABLE_LAST: u16 = 0x002F;
```

#### Constant `IMAGE_REL_IA64_ABSOLUTE`

```rust
pub const IMAGE_REL_IA64_ABSOLUTE: u16 = 0x0000;
```

#### Constant `IMAGE_REL_IA64_IMM14`

```rust
pub const IMAGE_REL_IA64_IMM14: u16 = 0x0001;
```

#### Constant `IMAGE_REL_IA64_IMM22`

```rust
pub const IMAGE_REL_IA64_IMM22: u16 = 0x0002;
```

#### Constant `IMAGE_REL_IA64_IMM64`

```rust
pub const IMAGE_REL_IA64_IMM64: u16 = 0x0003;
```

#### Constant `IMAGE_REL_IA64_DIR32`

```rust
pub const IMAGE_REL_IA64_DIR32: u16 = 0x0004;
```

#### Constant `IMAGE_REL_IA64_DIR64`

```rust
pub const IMAGE_REL_IA64_DIR64: u16 = 0x0005;
```

#### Constant `IMAGE_REL_IA64_PCREL21B`

```rust
pub const IMAGE_REL_IA64_PCREL21B: u16 = 0x0006;
```

#### Constant `IMAGE_REL_IA64_PCREL21M`

```rust
pub const IMAGE_REL_IA64_PCREL21M: u16 = 0x0007;
```

#### Constant `IMAGE_REL_IA64_PCREL21F`

```rust
pub const IMAGE_REL_IA64_PCREL21F: u16 = 0x0008;
```

#### Constant `IMAGE_REL_IA64_GPREL22`

```rust
pub const IMAGE_REL_IA64_GPREL22: u16 = 0x0009;
```

#### Constant `IMAGE_REL_IA64_LTOFF22`

```rust
pub const IMAGE_REL_IA64_LTOFF22: u16 = 0x000A;
```

#### Constant `IMAGE_REL_IA64_SECTION`

```rust
pub const IMAGE_REL_IA64_SECTION: u16 = 0x000B;
```

#### Constant `IMAGE_REL_IA64_SECREL22`

```rust
pub const IMAGE_REL_IA64_SECREL22: u16 = 0x000C;
```

#### Constant `IMAGE_REL_IA64_SECREL64I`

```rust
pub const IMAGE_REL_IA64_SECREL64I: u16 = 0x000D;
```

#### Constant `IMAGE_REL_IA64_SECREL32`

```rust
pub const IMAGE_REL_IA64_SECREL32: u16 = 0x000E;
```

#### Constant `IMAGE_REL_IA64_DIR32NB`

```rust
pub const IMAGE_REL_IA64_DIR32NB: u16 = 0x0010;
```

#### Constant `IMAGE_REL_IA64_SREL14`

```rust
pub const IMAGE_REL_IA64_SREL14: u16 = 0x0011;
```

#### Constant `IMAGE_REL_IA64_SREL22`

```rust
pub const IMAGE_REL_IA64_SREL22: u16 = 0x0012;
```

#### Constant `IMAGE_REL_IA64_SREL32`

```rust
pub const IMAGE_REL_IA64_SREL32: u16 = 0x0013;
```

#### Constant `IMAGE_REL_IA64_UREL32`

```rust
pub const IMAGE_REL_IA64_UREL32: u16 = 0x0014;
```

#### Constant `IMAGE_REL_IA64_PCREL60X`

This is always a BRL and never converted

```rust
pub const IMAGE_REL_IA64_PCREL60X: u16 = 0x0015;
```

#### Constant `IMAGE_REL_IA64_PCREL60B`

If possible, convert to MBB bundle with NOP.B in slot 1

```rust
pub const IMAGE_REL_IA64_PCREL60B: u16 = 0x0016;
```

#### Constant `IMAGE_REL_IA64_PCREL60F`

If possible, convert to MFB bundle with NOP.F in slot 1

```rust
pub const IMAGE_REL_IA64_PCREL60F: u16 = 0x0017;
```

#### Constant `IMAGE_REL_IA64_PCREL60I`

If possible, convert to MIB bundle with NOP.I in slot 1

```rust
pub const IMAGE_REL_IA64_PCREL60I: u16 = 0x0018;
```

#### Constant `IMAGE_REL_IA64_PCREL60M`

If possible, convert to MMB bundle with NOP.M in slot 1

```rust
pub const IMAGE_REL_IA64_PCREL60M: u16 = 0x0019;
```

#### Constant `IMAGE_REL_IA64_IMMGPREL64`

```rust
pub const IMAGE_REL_IA64_IMMGPREL64: u16 = 0x001A;
```

#### Constant `IMAGE_REL_IA64_TOKEN`

clr token

```rust
pub const IMAGE_REL_IA64_TOKEN: u16 = 0x001B;
```

#### Constant `IMAGE_REL_IA64_GPREL32`

```rust
pub const IMAGE_REL_IA64_GPREL32: u16 = 0x001C;
```

#### Constant `IMAGE_REL_IA64_ADDEND`

```rust
pub const IMAGE_REL_IA64_ADDEND: u16 = 0x001F;
```

#### Constant `IMAGE_REL_CEF_ABSOLUTE`

Reference is absolute, no relocation is necessary

```rust
pub const IMAGE_REL_CEF_ABSOLUTE: u16 = 0x0000;
```

#### Constant `IMAGE_REL_CEF_ADDR32`

32-bit address (VA).

```rust
pub const IMAGE_REL_CEF_ADDR32: u16 = 0x0001;
```

#### Constant `IMAGE_REL_CEF_ADDR64`

64-bit address (VA).

```rust
pub const IMAGE_REL_CEF_ADDR64: u16 = 0x0002;
```

#### Constant `IMAGE_REL_CEF_ADDR32NB`

32-bit address w/o image base (RVA).

```rust
pub const IMAGE_REL_CEF_ADDR32NB: u16 = 0x0003;
```

#### Constant `IMAGE_REL_CEF_SECTION`

Section index

```rust
pub const IMAGE_REL_CEF_SECTION: u16 = 0x0004;
```

#### Constant `IMAGE_REL_CEF_SECREL`

32 bit offset from base of section containing target

```rust
pub const IMAGE_REL_CEF_SECREL: u16 = 0x0005;
```

#### Constant `IMAGE_REL_CEF_TOKEN`

32 bit metadata token

```rust
pub const IMAGE_REL_CEF_TOKEN: u16 = 0x0006;
```

#### Constant `IMAGE_REL_CEE_ABSOLUTE`

Reference is absolute, no relocation is necessary

```rust
pub const IMAGE_REL_CEE_ABSOLUTE: u16 = 0x0000;
```

#### Constant `IMAGE_REL_CEE_ADDR32`

32-bit address (VA).

```rust
pub const IMAGE_REL_CEE_ADDR32: u16 = 0x0001;
```

#### Constant `IMAGE_REL_CEE_ADDR64`

64-bit address (VA).

```rust
pub const IMAGE_REL_CEE_ADDR64: u16 = 0x0002;
```

#### Constant `IMAGE_REL_CEE_ADDR32NB`

32-bit address w/o image base (RVA).

```rust
pub const IMAGE_REL_CEE_ADDR32NB: u16 = 0x0003;
```

#### Constant `IMAGE_REL_CEE_SECTION`

Section index

```rust
pub const IMAGE_REL_CEE_SECTION: u16 = 0x0004;
```

#### Constant `IMAGE_REL_CEE_SECREL`

32 bit offset from base of section containing target

```rust
pub const IMAGE_REL_CEE_SECREL: u16 = 0x0005;
```

#### Constant `IMAGE_REL_CEE_TOKEN`

32 bit metadata token

```rust
pub const IMAGE_REL_CEE_TOKEN: u16 = 0x0006;
```

#### Constant `IMAGE_REL_M32R_ABSOLUTE`

No relocation required

```rust
pub const IMAGE_REL_M32R_ABSOLUTE: u16 = 0x0000;
```

#### Constant `IMAGE_REL_M32R_ADDR32`

32 bit address

```rust
pub const IMAGE_REL_M32R_ADDR32: u16 = 0x0001;
```

#### Constant `IMAGE_REL_M32R_ADDR32NB`

32 bit address w/o image base

```rust
pub const IMAGE_REL_M32R_ADDR32NB: u16 = 0x0002;
```

#### Constant `IMAGE_REL_M32R_ADDR24`

24 bit address

```rust
pub const IMAGE_REL_M32R_ADDR24: u16 = 0x0003;
```

#### Constant `IMAGE_REL_M32R_GPREL16`

GP relative addressing

```rust
pub const IMAGE_REL_M32R_GPREL16: u16 = 0x0004;
```

#### Constant `IMAGE_REL_M32R_PCREL24`

24 bit offset << 2 & sign ext.

```rust
pub const IMAGE_REL_M32R_PCREL24: u16 = 0x0005;
```

#### Constant `IMAGE_REL_M32R_PCREL16`

16 bit offset << 2 & sign ext.

```rust
pub const IMAGE_REL_M32R_PCREL16: u16 = 0x0006;
```

#### Constant `IMAGE_REL_M32R_PCREL8`

8 bit offset << 2 & sign ext.

```rust
pub const IMAGE_REL_M32R_PCREL8: u16 = 0x0007;
```

#### Constant `IMAGE_REL_M32R_REFHALF`

16 MSBs

```rust
pub const IMAGE_REL_M32R_REFHALF: u16 = 0x0008;
```

#### Constant `IMAGE_REL_M32R_REFHI`

16 MSBs; adj for LSB sign ext.

```rust
pub const IMAGE_REL_M32R_REFHI: u16 = 0x0009;
```

#### Constant `IMAGE_REL_M32R_REFLO`

16 LSBs

```rust
pub const IMAGE_REL_M32R_REFLO: u16 = 0x000A;
```

#### Constant `IMAGE_REL_M32R_PAIR`

Link HI and LO

```rust
pub const IMAGE_REL_M32R_PAIR: u16 = 0x000B;
```

#### Constant `IMAGE_REL_M32R_SECTION`

Section table index

```rust
pub const IMAGE_REL_M32R_SECTION: u16 = 0x000C;
```

#### Constant `IMAGE_REL_M32R_SECREL32`

32 bit section relative reference

```rust
pub const IMAGE_REL_M32R_SECREL32: u16 = 0x000D;
```

#### Constant `IMAGE_REL_M32R_TOKEN`

clr token

```rust
pub const IMAGE_REL_M32R_TOKEN: u16 = 0x000E;
```

#### Constant `IMAGE_REL_EBC_ABSOLUTE`

No relocation required

```rust
pub const IMAGE_REL_EBC_ABSOLUTE: u16 = 0x0000;
```

#### Constant `IMAGE_REL_EBC_ADDR32NB`

32 bit address w/o image base

```rust
pub const IMAGE_REL_EBC_ADDR32NB: u16 = 0x0001;
```

#### Constant `IMAGE_REL_EBC_REL32`

32-bit relative address from byte following reloc

```rust
pub const IMAGE_REL_EBC_REL32: u16 = 0x0002;
```

#### Constant `IMAGE_REL_EBC_SECTION`

Section table index

```rust
pub const IMAGE_REL_EBC_SECTION: u16 = 0x0003;
```

#### Constant `IMAGE_REL_EBC_SECREL`

Offset within section

```rust
pub const IMAGE_REL_EBC_SECREL: u16 = 0x0004;
```

#### Constant `EMARCH_ENC_I17_IMM7B_INST_WORD_X`

Intel-IA64-Filler

```rust
pub const EMARCH_ENC_I17_IMM7B_INST_WORD_X: u16 = 3;
```

#### Constant `EMARCH_ENC_I17_IMM7B_SIZE_X`

Intel-IA64-Filler

```rust
pub const EMARCH_ENC_I17_IMM7B_SIZE_X: u16 = 7;
```

#### Constant `EMARCH_ENC_I17_IMM7B_INST_WORD_POS_X`

Intel-IA64-Filler

```rust
pub const EMARCH_ENC_I17_IMM7B_INST_WORD_POS_X: u16 = 4;
```

#### Constant `EMARCH_ENC_I17_IMM7B_VAL_POS_X`

Intel-IA64-Filler

```rust
pub const EMARCH_ENC_I17_IMM7B_VAL_POS_X: u16 = 0;
```

#### Constant `EMARCH_ENC_I17_IMM9D_INST_WORD_X`

Intel-IA64-Filler

```rust
pub const EMARCH_ENC_I17_IMM9D_INST_WORD_X: u16 = 3;
```

#### Constant `EMARCH_ENC_I17_IMM9D_SIZE_X`

Intel-IA64-Filler

```rust
pub const EMARCH_ENC_I17_IMM9D_SIZE_X: u16 = 9;
```

#### Constant `EMARCH_ENC_I17_IMM9D_INST_WORD_POS_X`

Intel-IA64-Filler

```rust
pub const EMARCH_ENC_I17_IMM9D_INST_WORD_POS_X: u16 = 18;
```

#### Constant `EMARCH_ENC_I17_IMM9D_VAL_POS_X`

Intel-IA64-Filler

```rust
pub const EMARCH_ENC_I17_IMM9D_VAL_POS_X: u16 = 7;
```

#### Constant `EMARCH_ENC_I17_IMM5C_INST_WORD_X`

Intel-IA64-Filler

```rust
pub const EMARCH_ENC_I17_IMM5C_INST_WORD_X: u16 = 3;
```

#### Constant `EMARCH_ENC_I17_IMM5C_SIZE_X`

Intel-IA64-Filler

```rust
pub const EMARCH_ENC_I17_IMM5C_SIZE_X: u16 = 5;
```

#### Constant `EMARCH_ENC_I17_IMM5C_INST_WORD_POS_X`

Intel-IA64-Filler

```rust
pub const EMARCH_ENC_I17_IMM5C_INST_WORD_POS_X: u16 = 13;
```

#### Constant `EMARCH_ENC_I17_IMM5C_VAL_POS_X`

Intel-IA64-Filler

```rust
pub const EMARCH_ENC_I17_IMM5C_VAL_POS_X: u16 = 16;
```

#### Constant `EMARCH_ENC_I17_IC_INST_WORD_X`

Intel-IA64-Filler

```rust
pub const EMARCH_ENC_I17_IC_INST_WORD_X: u16 = 3;
```

#### Constant `EMARCH_ENC_I17_IC_SIZE_X`

Intel-IA64-Filler

```rust
pub const EMARCH_ENC_I17_IC_SIZE_X: u16 = 1;
```

#### Constant `EMARCH_ENC_I17_IC_INST_WORD_POS_X`

Intel-IA64-Filler

```rust
pub const EMARCH_ENC_I17_IC_INST_WORD_POS_X: u16 = 12;
```

#### Constant `EMARCH_ENC_I17_IC_VAL_POS_X`

Intel-IA64-Filler

```rust
pub const EMARCH_ENC_I17_IC_VAL_POS_X: u16 = 21;
```

#### Constant `EMARCH_ENC_I17_IMM41A_INST_WORD_X`

Intel-IA64-Filler

```rust
pub const EMARCH_ENC_I17_IMM41A_INST_WORD_X: u16 = 1;
```

#### Constant `EMARCH_ENC_I17_IMM41A_SIZE_X`

Intel-IA64-Filler

```rust
pub const EMARCH_ENC_I17_IMM41A_SIZE_X: u16 = 10;
```

#### Constant `EMARCH_ENC_I17_IMM41A_INST_WORD_POS_X`

Intel-IA64-Filler

```rust
pub const EMARCH_ENC_I17_IMM41A_INST_WORD_POS_X: u16 = 14;
```

#### Constant `EMARCH_ENC_I17_IMM41A_VAL_POS_X`

Intel-IA64-Filler

```rust
pub const EMARCH_ENC_I17_IMM41A_VAL_POS_X: u16 = 22;
```

#### Constant `EMARCH_ENC_I17_IMM41B_INST_WORD_X`

Intel-IA64-Filler

```rust
pub const EMARCH_ENC_I17_IMM41B_INST_WORD_X: u16 = 1;
```

#### Constant `EMARCH_ENC_I17_IMM41B_SIZE_X`

Intel-IA64-Filler

```rust
pub const EMARCH_ENC_I17_IMM41B_SIZE_X: u16 = 8;
```

#### Constant `EMARCH_ENC_I17_IMM41B_INST_WORD_POS_X`

Intel-IA64-Filler

```rust
pub const EMARCH_ENC_I17_IMM41B_INST_WORD_POS_X: u16 = 24;
```

#### Constant `EMARCH_ENC_I17_IMM41B_VAL_POS_X`

Intel-IA64-Filler

```rust
pub const EMARCH_ENC_I17_IMM41B_VAL_POS_X: u16 = 32;
```

#### Constant `EMARCH_ENC_I17_IMM41C_INST_WORD_X`

Intel-IA64-Filler

```rust
pub const EMARCH_ENC_I17_IMM41C_INST_WORD_X: u16 = 2;
```

#### Constant `EMARCH_ENC_I17_IMM41C_SIZE_X`

Intel-IA64-Filler

```rust
pub const EMARCH_ENC_I17_IMM41C_SIZE_X: u16 = 23;
```

#### Constant `EMARCH_ENC_I17_IMM41C_INST_WORD_POS_X`

Intel-IA64-Filler

```rust
pub const EMARCH_ENC_I17_IMM41C_INST_WORD_POS_X: u16 = 0;
```

#### Constant `EMARCH_ENC_I17_IMM41C_VAL_POS_X`

Intel-IA64-Filler

```rust
pub const EMARCH_ENC_I17_IMM41C_VAL_POS_X: u16 = 40;
```

#### Constant `EMARCH_ENC_I17_SIGN_INST_WORD_X`

Intel-IA64-Filler

```rust
pub const EMARCH_ENC_I17_SIGN_INST_WORD_X: u16 = 3;
```

#### Constant `EMARCH_ENC_I17_SIGN_SIZE_X`

Intel-IA64-Filler

```rust
pub const EMARCH_ENC_I17_SIGN_SIZE_X: u16 = 1;
```

#### Constant `EMARCH_ENC_I17_SIGN_INST_WORD_POS_X`

Intel-IA64-Filler

```rust
pub const EMARCH_ENC_I17_SIGN_INST_WORD_POS_X: u16 = 27;
```

#### Constant `EMARCH_ENC_I17_SIGN_VAL_POS_X`

Intel-IA64-Filler

```rust
pub const EMARCH_ENC_I17_SIGN_VAL_POS_X: u16 = 63;
```

#### Constant `X3_OPCODE_INST_WORD_X`

Intel-IA64-Filler

```rust
pub const X3_OPCODE_INST_WORD_X: u16 = 3;
```

#### Constant `X3_OPCODE_SIZE_X`

Intel-IA64-Filler

```rust
pub const X3_OPCODE_SIZE_X: u16 = 4;
```

#### Constant `X3_OPCODE_INST_WORD_POS_X`

Intel-IA64-Filler

```rust
pub const X3_OPCODE_INST_WORD_POS_X: u16 = 28;
```

#### Constant `X3_OPCODE_SIGN_VAL_POS_X`

Intel-IA64-Filler

```rust
pub const X3_OPCODE_SIGN_VAL_POS_X: u16 = 0;
```

#### Constant `X3_I_INST_WORD_X`

Intel-IA64-Filler

```rust
pub const X3_I_INST_WORD_X: u16 = 3;
```

#### Constant `X3_I_SIZE_X`

Intel-IA64-Filler

```rust
pub const X3_I_SIZE_X: u16 = 1;
```

#### Constant `X3_I_INST_WORD_POS_X`

Intel-IA64-Filler

```rust
pub const X3_I_INST_WORD_POS_X: u16 = 27;
```

#### Constant `X3_I_SIGN_VAL_POS_X`

Intel-IA64-Filler

```rust
pub const X3_I_SIGN_VAL_POS_X: u16 = 59;
```

#### Constant `X3_D_WH_INST_WORD_X`

Intel-IA64-Filler

```rust
pub const X3_D_WH_INST_WORD_X: u16 = 3;
```

#### Constant `X3_D_WH_SIZE_X`

Intel-IA64-Filler

```rust
pub const X3_D_WH_SIZE_X: u16 = 3;
```

#### Constant `X3_D_WH_INST_WORD_POS_X`

Intel-IA64-Filler

```rust
pub const X3_D_WH_INST_WORD_POS_X: u16 = 24;
```

#### Constant `X3_D_WH_SIGN_VAL_POS_X`

Intel-IA64-Filler

```rust
pub const X3_D_WH_SIGN_VAL_POS_X: u16 = 0;
```

#### Constant `X3_IMM20_INST_WORD_X`

Intel-IA64-Filler

```rust
pub const X3_IMM20_INST_WORD_X: u16 = 3;
```

#### Constant `X3_IMM20_SIZE_X`

Intel-IA64-Filler

```rust
pub const X3_IMM20_SIZE_X: u16 = 20;
```

#### Constant `X3_IMM20_INST_WORD_POS_X`

Intel-IA64-Filler

```rust
pub const X3_IMM20_INST_WORD_POS_X: u16 = 4;
```

#### Constant `X3_IMM20_SIGN_VAL_POS_X`

Intel-IA64-Filler

```rust
pub const X3_IMM20_SIGN_VAL_POS_X: u16 = 0;
```

#### Constant `X3_IMM39_1_INST_WORD_X`

Intel-IA64-Filler

```rust
pub const X3_IMM39_1_INST_WORD_X: u16 = 2;
```

#### Constant `X3_IMM39_1_SIZE_X`

Intel-IA64-Filler

```rust
pub const X3_IMM39_1_SIZE_X: u16 = 23;
```

#### Constant `X3_IMM39_1_INST_WORD_POS_X`

Intel-IA64-Filler

```rust
pub const X3_IMM39_1_INST_WORD_POS_X: u16 = 0;
```

#### Constant `X3_IMM39_1_SIGN_VAL_POS_X`

Intel-IA64-Filler

```rust
pub const X3_IMM39_1_SIGN_VAL_POS_X: u16 = 36;
```

#### Constant `X3_IMM39_2_INST_WORD_X`

Intel-IA64-Filler

```rust
pub const X3_IMM39_2_INST_WORD_X: u16 = 1;
```

#### Constant `X3_IMM39_2_SIZE_X`

Intel-IA64-Filler

```rust
pub const X3_IMM39_2_SIZE_X: u16 = 16;
```

#### Constant `X3_IMM39_2_INST_WORD_POS_X`

Intel-IA64-Filler

```rust
pub const X3_IMM39_2_INST_WORD_POS_X: u16 = 16;
```

#### Constant `X3_IMM39_2_SIGN_VAL_POS_X`

Intel-IA64-Filler

```rust
pub const X3_IMM39_2_SIGN_VAL_POS_X: u16 = 20;
```

#### Constant `X3_P_INST_WORD_X`

Intel-IA64-Filler

```rust
pub const X3_P_INST_WORD_X: u16 = 3;
```

#### Constant `X3_P_SIZE_X`

Intel-IA64-Filler

```rust
pub const X3_P_SIZE_X: u16 = 4;
```

#### Constant `X3_P_INST_WORD_POS_X`

Intel-IA64-Filler

```rust
pub const X3_P_INST_WORD_POS_X: u16 = 0;
```

#### Constant `X3_P_SIGN_VAL_POS_X`

Intel-IA64-Filler

```rust
pub const X3_P_SIGN_VAL_POS_X: u16 = 0;
```

#### Constant `X3_TMPLT_INST_WORD_X`

Intel-IA64-Filler

```rust
pub const X3_TMPLT_INST_WORD_X: u16 = 0;
```

#### Constant `X3_TMPLT_SIZE_X`

Intel-IA64-Filler

```rust
pub const X3_TMPLT_SIZE_X: u16 = 4;
```

#### Constant `X3_TMPLT_INST_WORD_POS_X`

Intel-IA64-Filler

```rust
pub const X3_TMPLT_INST_WORD_POS_X: u16 = 0;
```

#### Constant `X3_TMPLT_SIGN_VAL_POS_X`

Intel-IA64-Filler

```rust
pub const X3_TMPLT_SIGN_VAL_POS_X: u16 = 0;
```

#### Constant `X3_BTYPE_QP_INST_WORD_X`

Intel-IA64-Filler

```rust
pub const X3_BTYPE_QP_INST_WORD_X: u16 = 2;
```

#### Constant `X3_BTYPE_QP_SIZE_X`

Intel-IA64-Filler

```rust
pub const X3_BTYPE_QP_SIZE_X: u16 = 9;
```

#### Constant `X3_BTYPE_QP_INST_WORD_POS_X`

Intel-IA64-Filler

```rust
pub const X3_BTYPE_QP_INST_WORD_POS_X: u16 = 23;
```

#### Constant `X3_BTYPE_QP_INST_VAL_POS_X`

Intel-IA64-Filler

```rust
pub const X3_BTYPE_QP_INST_VAL_POS_X: u16 = 0;
```

#### Constant `X3_EMPTY_INST_WORD_X`

Intel-IA64-Filler

```rust
pub const X3_EMPTY_INST_WORD_X: u16 = 1;
```

#### Constant `X3_EMPTY_SIZE_X`

Intel-IA64-Filler

```rust
pub const X3_EMPTY_SIZE_X: u16 = 2;
```

#### Constant `X3_EMPTY_INST_WORD_POS_X`

Intel-IA64-Filler

```rust
pub const X3_EMPTY_INST_WORD_POS_X: u16 = 14;
```

#### Constant `X3_EMPTY_INST_VAL_POS_X`

Intel-IA64-Filler

```rust
pub const X3_EMPTY_INST_VAL_POS_X: u16 = 0;
```

#### Constant `IMAGE_REL_BASED_ABSOLUTE`

```rust
pub const IMAGE_REL_BASED_ABSOLUTE: u16 = 0;
```

#### Constant `IMAGE_REL_BASED_HIGH`

```rust
pub const IMAGE_REL_BASED_HIGH: u16 = 1;
```

#### Constant `IMAGE_REL_BASED_LOW`

```rust
pub const IMAGE_REL_BASED_LOW: u16 = 2;
```

#### Constant `IMAGE_REL_BASED_HIGHLOW`

```rust
pub const IMAGE_REL_BASED_HIGHLOW: u16 = 3;
```

#### Constant `IMAGE_REL_BASED_HIGHADJ`

```rust
pub const IMAGE_REL_BASED_HIGHADJ: u16 = 4;
```

#### Constant `IMAGE_REL_BASED_MACHINE_SPECIFIC_5`

```rust
pub const IMAGE_REL_BASED_MACHINE_SPECIFIC_5: u16 = 5;
```

#### Constant `IMAGE_REL_BASED_RESERVED`

```rust
pub const IMAGE_REL_BASED_RESERVED: u16 = 6;
```

#### Constant `IMAGE_REL_BASED_MACHINE_SPECIFIC_7`

```rust
pub const IMAGE_REL_BASED_MACHINE_SPECIFIC_7: u16 = 7;
```

#### Constant `IMAGE_REL_BASED_MACHINE_SPECIFIC_8`

```rust
pub const IMAGE_REL_BASED_MACHINE_SPECIFIC_8: u16 = 8;
```

#### Constant `IMAGE_REL_BASED_MACHINE_SPECIFIC_9`

```rust
pub const IMAGE_REL_BASED_MACHINE_SPECIFIC_9: u16 = 9;
```

#### Constant `IMAGE_REL_BASED_DIR64`

```rust
pub const IMAGE_REL_BASED_DIR64: u16 = 10;
```

#### Constant `IMAGE_REL_BASED_IA64_IMM64`

```rust
pub const IMAGE_REL_BASED_IA64_IMM64: u16 = 9;
```

#### Constant `IMAGE_REL_BASED_MIPS_JMPADDR`

```rust
pub const IMAGE_REL_BASED_MIPS_JMPADDR: u16 = 5;
```

#### Constant `IMAGE_REL_BASED_MIPS_JMPADDR16`

```rust
pub const IMAGE_REL_BASED_MIPS_JMPADDR16: u16 = 9;
```

#### Constant `IMAGE_REL_BASED_ARM_MOV32`

```rust
pub const IMAGE_REL_BASED_ARM_MOV32: u16 = 5;
```

#### Constant `IMAGE_REL_BASED_THUMB_MOV32`

```rust
pub const IMAGE_REL_BASED_THUMB_MOV32: u16 = 7;
```

#### Constant `IMAGE_REL_BASED_RISCV_HIGH20`

```rust
pub const IMAGE_REL_BASED_RISCV_HIGH20: u16 = 5;
```

#### Constant `IMAGE_REL_BASED_RISCV_LOW12I`

```rust
pub const IMAGE_REL_BASED_RISCV_LOW12I: u16 = 7;
```

#### Constant `IMAGE_REL_BASED_RISCV_LOW12S`

```rust
pub const IMAGE_REL_BASED_RISCV_LOW12S: u16 = 8;
```

#### Constant `IMAGE_ARCHIVE_START_SIZE`

```rust
pub const IMAGE_ARCHIVE_START_SIZE: usize = 8;
```

#### Constant `IMAGE_ARCHIVE_START`

```rust
pub const IMAGE_ARCHIVE_START: &[u8; 8] = b"!<arch>\n";
```

#### Constant `IMAGE_ARCHIVE_END`

```rust
pub const IMAGE_ARCHIVE_END: &[u8] = b"`\n";
```

#### Constant `IMAGE_ARCHIVE_PAD`

```rust
pub const IMAGE_ARCHIVE_PAD: &[u8] = b"\n";
```

#### Constant `IMAGE_ARCHIVE_LINKER_MEMBER`

```rust
pub const IMAGE_ARCHIVE_LINKER_MEMBER: &[u8; 16] = b"/               ";
```

#### Constant `IMAGE_ARCHIVE_LONGNAMES_MEMBER`

```rust
pub const IMAGE_ARCHIVE_LONGNAMES_MEMBER: &[u8; 16] = b"//              ";
```

#### Constant `IMAGE_ARCHIVE_HYBRIDMAP_MEMBER`

```rust
pub const IMAGE_ARCHIVE_HYBRIDMAP_MEMBER: &[u8; 16] = b"/<HYBRIDMAP>/   ";
```

#### Constant `IMAGE_SIZEOF_ARCHIVE_MEMBER_HDR`

```rust
pub const IMAGE_SIZEOF_ARCHIVE_MEMBER_HDR: u16 = 60;
```

#### Constant `IMAGE_ORDINAL_FLAG64`

```rust
pub const IMAGE_ORDINAL_FLAG64: u64 = 0x8000000000000000;
```

#### Constant `IMAGE_ORDINAL_FLAG32`

```rust
pub const IMAGE_ORDINAL_FLAG32: u32 = 0x80000000;
```

#### Constant `IMAGE_DELAYLOAD_RVA_BASED`

Delay load version 2 flag for `ImageDelayloadDescriptor::attributes`.

```rust
pub const IMAGE_DELAYLOAD_RVA_BASED: u32 = 0x8000_0000;
```

#### Constant `IMAGE_RESOURCE_NAME_IS_STRING`

```rust
pub const IMAGE_RESOURCE_NAME_IS_STRING: u32 = 0x8000_0000;
```

#### Constant `IMAGE_RESOURCE_DATA_IS_DIRECTORY`

```rust
pub const IMAGE_RESOURCE_DATA_IS_DIRECTORY: u32 = 0x8000_0000;
```

#### Constant `RT_CURSOR`

ID for: Hardware-dependent cursor resource.

```rust
pub const RT_CURSOR: u16 = 1;
```

#### Constant `RT_BITMAP`

ID for: Bitmap resource.

```rust
pub const RT_BITMAP: u16 = 2;
```

#### Constant `RT_ICON`

ID for: Hardware-dependent icon resource.

```rust
pub const RT_ICON: u16 = 3;
```

#### Constant `RT_MENU`

ID for: Menu resource.

```rust
pub const RT_MENU: u16 = 4;
```

#### Constant `RT_DIALOG`

ID for: Dialog box.

```rust
pub const RT_DIALOG: u16 = 5;
```

#### Constant `RT_STRING`

ID for: String-table entry.

```rust
pub const RT_STRING: u16 = 6;
```

#### Constant `RT_FONTDIR`

ID for: Font directory resource.

```rust
pub const RT_FONTDIR: u16 = 7;
```

#### Constant `RT_FONT`

ID for: Font resource.

```rust
pub const RT_FONT: u16 = 8;
```

#### Constant `RT_ACCELERATOR`

ID for: Accelerator table.

```rust
pub const RT_ACCELERATOR: u16 = 9;
```

#### Constant `RT_RCDATA`

ID for: Application-defined resource (raw data).

```rust
pub const RT_RCDATA: u16 = 10;
```

#### Constant `RT_MESSAGETABLE`

ID for: Message-table entry.

```rust
pub const RT_MESSAGETABLE: u16 = 11;
```

#### Constant `RT_GROUP_CURSOR`

ID for: Hardware-independent cursor resource.

```rust
pub const RT_GROUP_CURSOR: u16 = 12;
```

#### Constant `RT_GROUP_ICON`

ID for: Hardware-independent icon resource.

```rust
pub const RT_GROUP_ICON: u16 = 14;
```

#### Constant `RT_VERSION`

ID for: Version resource.

```rust
pub const RT_VERSION: u16 = 16;
```

#### Constant `RT_DLGINCLUDE`

ID for: Allows a resource editing tool to associate a string with an .rc file.

```rust
pub const RT_DLGINCLUDE: u16 = 17;
```

#### Constant `RT_PLUGPLAY`

ID for: Plug and Play resource.

```rust
pub const RT_PLUGPLAY: u16 = 19;
```

#### Constant `RT_VXD`

ID for: VXD.

```rust
pub const RT_VXD: u16 = 20;
```

#### Constant `RT_ANICURSOR`

ID for: Animated cursor.

```rust
pub const RT_ANICURSOR: u16 = 21;
```

#### Constant `RT_ANIICON`

ID for: Animated icon.

```rust
pub const RT_ANIICON: u16 = 22;
```

#### Constant `RT_HTML`

ID for: HTML resource.

```rust
pub const RT_HTML: u16 = 23;
```

#### Constant `RT_MANIFEST`

ID for: Side-by-Side Assembly Manifest.

```rust
pub const RT_MANIFEST: u16 = 24;
```

#### Constant `IMAGE_DYNAMIC_RELOCATION_GUARD_RF_PROLOGUE`

```rust
pub const IMAGE_DYNAMIC_RELOCATION_GUARD_RF_PROLOGUE: u32 = 0x0000_0001;
```

#### Constant `IMAGE_DYNAMIC_RELOCATION_GUARD_RF_EPILOGUE`

```rust
pub const IMAGE_DYNAMIC_RELOCATION_GUARD_RF_EPILOGUE: u32 = 0x0000_0002;
```

#### Constant `IMAGE_DYNAMIC_RELOCATION_GUARD_IMPORT_CONTROL_TRANSFER`

```rust
pub const IMAGE_DYNAMIC_RELOCATION_GUARD_IMPORT_CONTROL_TRANSFER: u32 = 0x0000_0003;
```

#### Constant `IMAGE_DYNAMIC_RELOCATION_GUARD_INDIR_CONTROL_TRANSFER`

```rust
pub const IMAGE_DYNAMIC_RELOCATION_GUARD_INDIR_CONTROL_TRANSFER: u32 = 0x0000_0004;
```

#### Constant `IMAGE_DYNAMIC_RELOCATION_GUARD_SWITCHTABLE_BRANCH`

```rust
pub const IMAGE_DYNAMIC_RELOCATION_GUARD_SWITCHTABLE_BRANCH: u32 = 0x0000_0005;
```

#### Constant `IMAGE_HOT_PATCH_BASE_OBLIGATORY`

```rust
pub const IMAGE_HOT_PATCH_BASE_OBLIGATORY: u32 = 0x0000_0001;
```

#### Constant `IMAGE_HOT_PATCH_BASE_CAN_ROLL_BACK`

```rust
pub const IMAGE_HOT_PATCH_BASE_CAN_ROLL_BACK: u32 = 0x0000_0002;
```

#### Constant `IMAGE_HOT_PATCH_CHUNK_INVERSE`

```rust
pub const IMAGE_HOT_PATCH_CHUNK_INVERSE: u32 = 0x8000_0000;
```

#### Constant `IMAGE_HOT_PATCH_CHUNK_OBLIGATORY`

```rust
pub const IMAGE_HOT_PATCH_CHUNK_OBLIGATORY: u32 = 0x4000_0000;
```

#### Constant `IMAGE_HOT_PATCH_CHUNK_RESERVED`

```rust
pub const IMAGE_HOT_PATCH_CHUNK_RESERVED: u32 = 0x3FF0_3000;
```

#### Constant `IMAGE_HOT_PATCH_CHUNK_TYPE`

```rust
pub const IMAGE_HOT_PATCH_CHUNK_TYPE: u32 = 0x000F_C000;
```

#### Constant `IMAGE_HOT_PATCH_CHUNK_SOURCE_RVA`

```rust
pub const IMAGE_HOT_PATCH_CHUNK_SOURCE_RVA: u32 = 0x0000_8000;
```

#### Constant `IMAGE_HOT_PATCH_CHUNK_TARGET_RVA`

```rust
pub const IMAGE_HOT_PATCH_CHUNK_TARGET_RVA: u32 = 0x0000_4000;
```

#### Constant `IMAGE_HOT_PATCH_CHUNK_SIZE`

```rust
pub const IMAGE_HOT_PATCH_CHUNK_SIZE: u32 = 0x0000_0FFF;
```

#### Constant `IMAGE_HOT_PATCH_NONE`

```rust
pub const IMAGE_HOT_PATCH_NONE: u32 = 0x0000_0000;
```

#### Constant `IMAGE_HOT_PATCH_FUNCTION`

```rust
pub const IMAGE_HOT_PATCH_FUNCTION: u32 = 0x0001_C000;
```

#### Constant `IMAGE_HOT_PATCH_ABSOLUTE`

```rust
pub const IMAGE_HOT_PATCH_ABSOLUTE: u32 = 0x0002_C000;
```

#### Constant `IMAGE_HOT_PATCH_REL32`

```rust
pub const IMAGE_HOT_PATCH_REL32: u32 = 0x0003_C000;
```

#### Constant `IMAGE_HOT_PATCH_CALL_TARGET`

```rust
pub const IMAGE_HOT_PATCH_CALL_TARGET: u32 = 0x0004_4000;
```

#### Constant `IMAGE_HOT_PATCH_INDIRECT`

```rust
pub const IMAGE_HOT_PATCH_INDIRECT: u32 = 0x0005_C000;
```

#### Constant `IMAGE_HOT_PATCH_NO_CALL_TARGET`

```rust
pub const IMAGE_HOT_PATCH_NO_CALL_TARGET: u32 = 0x0006_4000;
```

#### Constant `IMAGE_HOT_PATCH_DYNAMIC_VALUE`

```rust
pub const IMAGE_HOT_PATCH_DYNAMIC_VALUE: u32 = 0x0007_8000;
```

#### Constant `IMAGE_GUARD_CF_INSTRUMENTED`

Module performs control flow integrity checks using system-supplied support

```rust
pub const IMAGE_GUARD_CF_INSTRUMENTED: u32 = 0x0000_0100;
```

#### Constant `IMAGE_GUARD_CFW_INSTRUMENTED`

Module performs control flow and write integrity checks

```rust
pub const IMAGE_GUARD_CFW_INSTRUMENTED: u32 = 0x0000_0200;
```

#### Constant `IMAGE_GUARD_CF_FUNCTION_TABLE_PRESENT`

Module contains valid control flow target metadata

```rust
pub const IMAGE_GUARD_CF_FUNCTION_TABLE_PRESENT: u32 = 0x0000_0400;
```

#### Constant `IMAGE_GUARD_SECURITY_COOKIE_UNUSED`

Module does not make use of the /GS security cookie

```rust
pub const IMAGE_GUARD_SECURITY_COOKIE_UNUSED: u32 = 0x0000_0800;
```

#### Constant `IMAGE_GUARD_PROTECT_DELAYLOAD_IAT`

Module supports read only delay load IAT

```rust
pub const IMAGE_GUARD_PROTECT_DELAYLOAD_IAT: u32 = 0x0000_1000;
```

#### Constant `IMAGE_GUARD_DELAYLOAD_IAT_IN_ITS_OWN_SECTION`

Delayload import table in its own .didat section (with nothing else in it) that can be freely reprotected

```rust
pub const IMAGE_GUARD_DELAYLOAD_IAT_IN_ITS_OWN_SECTION: u32 = 0x0000_2000;
```

#### Constant `IMAGE_GUARD_CF_EXPORT_SUPPRESSION_INFO_PRESENT`

Module contains suppressed export information.

This also infers that the address taken taken IAT table is also present in the load config.

```rust
pub const IMAGE_GUARD_CF_EXPORT_SUPPRESSION_INFO_PRESENT: u32 = 0x0000_4000;
```

#### Constant `IMAGE_GUARD_CF_ENABLE_EXPORT_SUPPRESSION`

Module enables suppression of exports

```rust
pub const IMAGE_GUARD_CF_ENABLE_EXPORT_SUPPRESSION: u32 = 0x0000_8000;
```

#### Constant `IMAGE_GUARD_CF_LONGJUMP_TABLE_PRESENT`

Module contains longjmp target information

```rust
pub const IMAGE_GUARD_CF_LONGJUMP_TABLE_PRESENT: u32 = 0x0001_0000;
```

#### Constant `IMAGE_GUARD_RF_INSTRUMENTED`

Module contains return flow instrumentation and metadata

```rust
pub const IMAGE_GUARD_RF_INSTRUMENTED: u32 = 0x0002_0000;
```

#### Constant `IMAGE_GUARD_RF_ENABLE`

Module requests that the OS enable return flow protection

```rust
pub const IMAGE_GUARD_RF_ENABLE: u32 = 0x0004_0000;
```

#### Constant `IMAGE_GUARD_RF_STRICT`

Module requests that the OS enable return flow protection in strict mode

```rust
pub const IMAGE_GUARD_RF_STRICT: u32 = 0x0008_0000;
```

#### Constant `IMAGE_GUARD_RETPOLINE_PRESENT`

Module was built with retpoline support

```rust
pub const IMAGE_GUARD_RETPOLINE_PRESENT: u32 = 0x0010_0000;
```

#### Constant `IMAGE_GUARD_CF_FUNCTION_TABLE_SIZE_MASK`

Stride of Guard CF function table encoded in these bits (additional count of bytes per element)

```rust
pub const IMAGE_GUARD_CF_FUNCTION_TABLE_SIZE_MASK: u32 = 0xF000_0000;
```

#### Constant `IMAGE_GUARD_CF_FUNCTION_TABLE_SIZE_SHIFT`

Shift to right-justify Guard CF function table stride

```rust
pub const IMAGE_GUARD_CF_FUNCTION_TABLE_SIZE_SHIFT: u32 = 28;
```

#### Constant `IMAGE_GUARD_FLAG_FID_SUPPRESSED`

The containing GFID entry is suppressed

```rust
pub const IMAGE_GUARD_FLAG_FID_SUPPRESSED: u16 = 0x01;
```

#### Constant `IMAGE_GUARD_FLAG_EXPORT_SUPPRESSED`

The containing GFID entry is export suppressed

```rust
pub const IMAGE_GUARD_FLAG_EXPORT_SUPPRESSED: u16 = 0x02;
```

#### Constant `IMAGE_ENCLAVE_LONG_ID_LENGTH`

```rust
pub const IMAGE_ENCLAVE_LONG_ID_LENGTH: usize = 32;
```

#### Constant `IMAGE_ENCLAVE_SHORT_ID_LENGTH`

```rust
pub const IMAGE_ENCLAVE_SHORT_ID_LENGTH: usize = 16;
```

#### Constant `IMAGE_ENCLAVE_POLICY_DEBUGGABLE`

```rust
pub const IMAGE_ENCLAVE_POLICY_DEBUGGABLE: u32 = 0x0000_0001;
```

#### Constant `IMAGE_ENCLAVE_FLAG_PRIMARY_IMAGE`

```rust
pub const IMAGE_ENCLAVE_FLAG_PRIMARY_IMAGE: u32 = 0x0000_0001;
```

#### Constant `IMAGE_ENCLAVE_IMPORT_MATCH_NONE`

```rust
pub const IMAGE_ENCLAVE_IMPORT_MATCH_NONE: u32 = 0x0000_0000;
```

#### Constant `IMAGE_ENCLAVE_IMPORT_MATCH_UNIQUE_ID`

```rust
pub const IMAGE_ENCLAVE_IMPORT_MATCH_UNIQUE_ID: u32 = 0x0000_0001;
```

#### Constant `IMAGE_ENCLAVE_IMPORT_MATCH_AUTHOR_ID`

```rust
pub const IMAGE_ENCLAVE_IMPORT_MATCH_AUTHOR_ID: u32 = 0x0000_0002;
```

#### Constant `IMAGE_ENCLAVE_IMPORT_MATCH_FAMILY_ID`

```rust
pub const IMAGE_ENCLAVE_IMPORT_MATCH_FAMILY_ID: u32 = 0x0000_0003;
```

#### Constant `IMAGE_ENCLAVE_IMPORT_MATCH_IMAGE_ID`

```rust
pub const IMAGE_ENCLAVE_IMPORT_MATCH_IMAGE_ID: u32 = 0x0000_0004;
```

#### Constant `IMAGE_DEBUG_TYPE_UNKNOWN`

```rust
pub const IMAGE_DEBUG_TYPE_UNKNOWN: u32 = 0;
```

#### Constant `IMAGE_DEBUG_TYPE_COFF`

```rust
pub const IMAGE_DEBUG_TYPE_COFF: u32 = 1;
```

#### Constant `IMAGE_DEBUG_TYPE_CODEVIEW`

```rust
pub const IMAGE_DEBUG_TYPE_CODEVIEW: u32 = 2;
```

#### Constant `IMAGE_DEBUG_TYPE_FPO`

```rust
pub const IMAGE_DEBUG_TYPE_FPO: u32 = 3;
```

#### Constant `IMAGE_DEBUG_TYPE_MISC`

```rust
pub const IMAGE_DEBUG_TYPE_MISC: u32 = 4;
```

#### Constant `IMAGE_DEBUG_TYPE_EXCEPTION`

```rust
pub const IMAGE_DEBUG_TYPE_EXCEPTION: u32 = 5;
```

#### Constant `IMAGE_DEBUG_TYPE_FIXUP`

```rust
pub const IMAGE_DEBUG_TYPE_FIXUP: u32 = 6;
```

#### Constant `IMAGE_DEBUG_TYPE_OMAP_TO_SRC`

```rust
pub const IMAGE_DEBUG_TYPE_OMAP_TO_SRC: u32 = 7;
```

#### Constant `IMAGE_DEBUG_TYPE_OMAP_FROM_SRC`

```rust
pub const IMAGE_DEBUG_TYPE_OMAP_FROM_SRC: u32 = 8;
```

#### Constant `IMAGE_DEBUG_TYPE_BORLAND`

```rust
pub const IMAGE_DEBUG_TYPE_BORLAND: u32 = 9;
```

#### Constant `IMAGE_DEBUG_TYPE_RESERVED10`

```rust
pub const IMAGE_DEBUG_TYPE_RESERVED10: u32 = 10;
```

#### Constant `IMAGE_DEBUG_TYPE_CLSID`

```rust
pub const IMAGE_DEBUG_TYPE_CLSID: u32 = 11;
```

#### Constant `IMAGE_DEBUG_TYPE_VC_FEATURE`

```rust
pub const IMAGE_DEBUG_TYPE_VC_FEATURE: u32 = 12;
```

#### Constant `IMAGE_DEBUG_TYPE_POGO`

```rust
pub const IMAGE_DEBUG_TYPE_POGO: u32 = 13;
```

#### Constant `IMAGE_DEBUG_TYPE_ILTCG`

```rust
pub const IMAGE_DEBUG_TYPE_ILTCG: u32 = 14;
```

#### Constant `IMAGE_DEBUG_TYPE_MPX`

```rust
pub const IMAGE_DEBUG_TYPE_MPX: u32 = 15;
```

#### Constant `IMAGE_DEBUG_TYPE_REPRO`

```rust
pub const IMAGE_DEBUG_TYPE_REPRO: u32 = 16;
```

#### Constant `FRAME_FPO`

```rust
pub const FRAME_FPO: u16 = 0;
```

#### Constant `FRAME_TRAP`

```rust
pub const FRAME_TRAP: u16 = 1;
```

#### Constant `FRAME_TSS`

```rust
pub const FRAME_TSS: u16 = 2;
```

#### Constant `FRAME_NONFPO`

```rust
pub const FRAME_NONFPO: u16 = 3;
```

#### Constant `IMAGE_DEBUG_MISC_EXENAME`

```rust
pub const IMAGE_DEBUG_MISC_EXENAME: u16 = 1;
```

#### Constant `IMAGE_SEPARATE_DEBUG_SIGNATURE`

```rust
pub const IMAGE_SEPARATE_DEBUG_SIGNATURE: u16 = 0x4944;
```

#### Constant `NON_PAGED_DEBUG_SIGNATURE`

```rust
pub const NON_PAGED_DEBUG_SIGNATURE: u16 = 0x494E;
```

#### Constant `IMAGE_SEPARATE_DEBUG_FLAGS_MASK`

```rust
pub const IMAGE_SEPARATE_DEBUG_FLAGS_MASK: u16 = 0x8000;
```

#### Constant `IMAGE_SEPARATE_DEBUG_MISMATCH`

when DBG was updated, the old checksum didn't match.

```rust
pub const IMAGE_SEPARATE_DEBUG_MISMATCH: u16 = 0x8000;
```

#### Constant `IMPORT_OBJECT_HDR_SIG2`

```rust
pub const IMPORT_OBJECT_HDR_SIG2: u16 = 0xffff;
```

#### Constant `IMPORT_OBJECT_TYPE_MASK`

```rust
pub const IMPORT_OBJECT_TYPE_MASK: u16 = 0b11;
```

#### Constant `IMPORT_OBJECT_TYPE_SHIFT`

```rust
pub const IMPORT_OBJECT_TYPE_SHIFT: u16 = 0;
```

#### Constant `IMPORT_OBJECT_CODE`

```rust
pub const IMPORT_OBJECT_CODE: u16 = 0;
```

#### Constant `IMPORT_OBJECT_DATA`

```rust
pub const IMPORT_OBJECT_DATA: u16 = 1;
```

#### Constant `IMPORT_OBJECT_CONST`

```rust
pub const IMPORT_OBJECT_CONST: u16 = 2;
```

#### Constant `IMPORT_OBJECT_NAME_MASK`

```rust
pub const IMPORT_OBJECT_NAME_MASK: u16 = 0b111;
```

#### Constant `IMPORT_OBJECT_NAME_SHIFT`

```rust
pub const IMPORT_OBJECT_NAME_SHIFT: u16 = 2;
```

#### Constant `IMPORT_OBJECT_ORDINAL`

Import by ordinal

```rust
pub const IMPORT_OBJECT_ORDINAL: u16 = 0;
```

#### Constant `IMPORT_OBJECT_NAME`

Import name == public symbol name.

```rust
pub const IMPORT_OBJECT_NAME: u16 = 1;
```

#### Constant `IMPORT_OBJECT_NAME_NO_PREFIX`

Import name == public symbol name skipping leading ?, @, or optionally _.

```rust
pub const IMPORT_OBJECT_NAME_NO_PREFIX: u16 = 2;
```

#### Constant `IMPORT_OBJECT_NAME_UNDECORATE`

Import name == public symbol name skipping leading ?, @, or optionally _ and truncating at first @.

```rust
pub const IMPORT_OBJECT_NAME_UNDECORATE: u16 = 3;
```

#### Constant `IMPORT_OBJECT_NAME_EXPORTAS`

Import name == a name is explicitly provided after the DLL name.

```rust
pub const IMPORT_OBJECT_NAME_EXPORTAS: u16 = 4;
```

#### Constant `COMIMAGE_FLAGS_ILONLY`

```rust
pub const COMIMAGE_FLAGS_ILONLY: u32 = 0x0000_0001;
```

#### Constant `COMIMAGE_FLAGS_32BITREQUIRED`

```rust
pub const COMIMAGE_FLAGS_32BITREQUIRED: u32 = 0x0000_0002;
```

#### Constant `COMIMAGE_FLAGS_IL_LIBRARY`

```rust
pub const COMIMAGE_FLAGS_IL_LIBRARY: u32 = 0x0000_0004;
```

#### Constant `COMIMAGE_FLAGS_STRONGNAMESIGNED`

```rust
pub const COMIMAGE_FLAGS_STRONGNAMESIGNED: u32 = 0x0000_0008;
```

#### Constant `COMIMAGE_FLAGS_NATIVE_ENTRYPOINT`

```rust
pub const COMIMAGE_FLAGS_NATIVE_ENTRYPOINT: u32 = 0x0000_0010;
```

#### Constant `COMIMAGE_FLAGS_TRACKDEBUGDATA`

```rust
pub const COMIMAGE_FLAGS_TRACKDEBUGDATA: u32 = 0x0001_0000;
```

#### Constant `COMIMAGE_FLAGS_32BITPREFERRED`

```rust
pub const COMIMAGE_FLAGS_32BITPREFERRED: u32 = 0x0002_0000;
```

#### Constant `COR_VERSION_MAJOR_V2`

```rust
pub const COR_VERSION_MAJOR_V2: u16 = 2;
```

#### Constant `COR_VERSION_MAJOR`

```rust
pub const COR_VERSION_MAJOR: u16 = COR_VERSION_MAJOR_V2;
```

#### Constant `COR_VERSION_MINOR`

```rust
pub const COR_VERSION_MINOR: u16 = 5;
```

#### Constant `COR_DELETED_NAME_LENGTH`

```rust
pub const COR_DELETED_NAME_LENGTH: usize = 8;
```

#### Constant `COR_VTABLEGAP_NAME_LENGTH`

```rust
pub const COR_VTABLEGAP_NAME_LENGTH: usize = 8;
```

#### Constant `NATIVE_TYPE_MAX_CB`

```rust
pub const NATIVE_TYPE_MAX_CB: u16 = 1;
```

#### Constant `COR_ILMETHOD_SECT_SMALL_MAX_DATASIZE`

```rust
pub const COR_ILMETHOD_SECT_SMALL_MAX_DATASIZE: u16 = 0xFF;
```

#### Constant `IMAGE_COR_MIH_METHODRVA`

```rust
pub const IMAGE_COR_MIH_METHODRVA: u16 = 0x01;
```

#### Constant `IMAGE_COR_MIH_EHRVA`

```rust
pub const IMAGE_COR_MIH_EHRVA: u16 = 0x02;
```

#### Constant `IMAGE_COR_MIH_BASICBLOCK`

```rust
pub const IMAGE_COR_MIH_BASICBLOCK: u16 = 0x08;
```

#### Constant `COR_VTABLE_32BIT`

V-table slots are 32-bits in size.

```rust
pub const COR_VTABLE_32BIT: u16 = 0x01;
```

#### Constant `COR_VTABLE_64BIT`

V-table slots are 64-bits in size.

```rust
pub const COR_VTABLE_64BIT: u16 = 0x02;
```

#### Constant `COR_VTABLE_FROM_UNMANAGED`

If set, transition from unmanaged.

```rust
pub const COR_VTABLE_FROM_UNMANAGED: u16 = 0x04;
```

#### Constant `COR_VTABLE_FROM_UNMANAGED_RETAIN_APPDOMAIN`

If set, transition from unmanaged with keeping the current appdomain.

```rust
pub const COR_VTABLE_FROM_UNMANAGED_RETAIN_APPDOMAIN: u16 = 0x08;
```

#### Constant `COR_VTABLE_CALL_MOST_DERIVED`

Call most derived method described by

```rust
pub const COR_VTABLE_CALL_MOST_DERIVED: u16 = 0x10;
```

#### Constant `IMAGE_COR_EATJ_THUNK_SIZE`

Size of a jump thunk reserved range.

```rust
pub const IMAGE_COR_EATJ_THUNK_SIZE: usize = 32;
```

#### Constant `MAX_CLASS_NAME`

```rust
pub const MAX_CLASS_NAME: usize = 1024;
```

#### Constant `MAX_PACKAGE_NAME`

```rust
pub const MAX_PACKAGE_NAME: usize = 1024;
```

### Re-exports

#### Re-export `Guid`

```rust
pub use Guid as ClsId;
```

## Module `xcoff`

**Attributes:**

- `#[<cfg>(feature = "xcoff")]`
- `#[allow(missing_docs)]`

XCOFF definitions

These definitions are independent of read/write support, although we do implement
some traits useful for those.

This module is the equivalent of /usr/include/xcoff.h, and is based heavily on it.

```rust
pub mod xcoff { /* ... */ }
```

### Types

#### Struct `FileHeader32`

**Attributes:**

- `#[repr(C)]`

The header at the start of every 32-bit XCOFF file.

```rust
pub struct FileHeader32 {
    pub f_magic: crate::endian::U16<crate::endian::BigEndian>,
    pub f_nscns: crate::endian::U16<crate::endian::BigEndian>,
    pub f_timdat: crate::endian::U32<crate::endian::BigEndian>,
    pub f_symptr: crate::endian::U32<crate::endian::BigEndian>,
    pub f_nsyms: crate::endian::U32<crate::endian::BigEndian>,
    pub f_opthdr: crate::endian::U16<crate::endian::BigEndian>,
    pub f_flags: crate::endian::U16<crate::endian::BigEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `f_magic` | `crate::endian::U16<crate::endian::BigEndian>` | Magic number. Must be 0x01DF. |
| `f_nscns` | `crate::endian::U16<crate::endian::BigEndian>` | Number of sections. |
| `f_timdat` | `crate::endian::U32<crate::endian::BigEndian>` | Time and date of file creation. |
| `f_symptr` | `crate::endian::U32<crate::endian::BigEndian>` | Byte offset to symbol table start. |
| `f_nsyms` | `crate::endian::U32<crate::endian::BigEndian>` | Number of entries in symbol table. |
| `f_opthdr` | `crate::endian::U16<crate::endian::BigEndian>` | Number of bytes in optional header |
| `f_flags` | `crate::endian::U16<crate::endian::BigEndian>` | Extra flags. |

##### Implementations

###### Trait Implementations

- **Clone**
  - ```rust
    fn clone(self: &Self) -> FileHeader32 { /* ... */ }
    ```

- **Send**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Freeze**
- **UnwindSafe**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Pod**
- **Unpin**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **FileHeader**
  - ```rust
    fn is_type_64(self: &Self) -> bool { /* ... */ }
    ```

  - ```rust
    fn f_magic(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn f_nscns(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn f_timdat(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn f_symptr(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn f_nsyms(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn f_opthdr(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn f_flags(self: &Self) -> u16 { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Copy**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **RefUnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Sync**
#### Struct `FileHeader64`

**Attributes:**

- `#[repr(C)]`

The header at the start of every 64-bit XCOFF file.

```rust
pub struct FileHeader64 {
    pub f_magic: crate::endian::U16<crate::endian::BigEndian>,
    pub f_nscns: crate::endian::U16<crate::endian::BigEndian>,
    pub f_timdat: crate::endian::U32<crate::endian::BigEndian>,
    pub f_symptr: crate::endian::U64<crate::endian::BigEndian>,
    pub f_opthdr: crate::endian::U16<crate::endian::BigEndian>,
    pub f_flags: crate::endian::U16<crate::endian::BigEndian>,
    pub f_nsyms: crate::endian::U32<crate::endian::BigEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `f_magic` | `crate::endian::U16<crate::endian::BigEndian>` | Magic number. Must be 0x01F7. |
| `f_nscns` | `crate::endian::U16<crate::endian::BigEndian>` | Number of sections. |
| `f_timdat` | `crate::endian::U32<crate::endian::BigEndian>` | Time and date of file creation |
| `f_symptr` | `crate::endian::U64<crate::endian::BigEndian>` | Byte offset to symbol table start. |
| `f_opthdr` | `crate::endian::U16<crate::endian::BigEndian>` | Number of bytes in optional header |
| `f_flags` | `crate::endian::U16<crate::endian::BigEndian>` | Extra flags. |
| `f_nsyms` | `crate::endian::U32<crate::endian::BigEndian>` | Number of entries in symbol table. |

##### Implementations

###### Trait Implementations

- **UnwindSafe**
- **FileHeader**
  - ```rust
    fn is_type_64(self: &Self) -> bool { /* ... */ }
    ```

  - ```rust
    fn f_magic(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn f_nscns(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn f_timdat(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn f_symptr(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn f_nsyms(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn f_opthdr(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn f_flags(self: &Self) -> u16 { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Copy**
- **Sync**
- **Pod**
- **Unpin**
- **Send**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **RefUnwindSafe**
- **Freeze**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> FileHeader64 { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

#### Struct `AuxHeader32`

**Attributes:**

- `#[repr(C)]`

The auxiliary header immediately following file header. If the value of the
f_opthdr field in the file header is 0, the auxiliary header does not exist.

```rust
pub struct AuxHeader32 {
    pub o_mflag: crate::endian::U16<crate::endian::BigEndian>,
    pub o_vstamp: crate::endian::U16<crate::endian::BigEndian>,
    pub o_tsize: crate::endian::U32<crate::endian::BigEndian>,
    pub o_dsize: crate::endian::U32<crate::endian::BigEndian>,
    pub o_bsize: crate::endian::U32<crate::endian::BigEndian>,
    pub o_entry: crate::endian::U32<crate::endian::BigEndian>,
    pub o_text_start: crate::endian::U32<crate::endian::BigEndian>,
    pub o_data_start: crate::endian::U32<crate::endian::BigEndian>,
    pub o_toc: crate::endian::U32<crate::endian::BigEndian>,
    pub o_snentry: crate::endian::U16<crate::endian::BigEndian>,
    pub o_sntext: crate::endian::U16<crate::endian::BigEndian>,
    pub o_sndata: crate::endian::U16<crate::endian::BigEndian>,
    pub o_sntoc: crate::endian::U16<crate::endian::BigEndian>,
    pub o_snloader: crate::endian::U16<crate::endian::BigEndian>,
    pub o_snbss: crate::endian::U16<crate::endian::BigEndian>,
    pub o_algntext: crate::endian::U16<crate::endian::BigEndian>,
    pub o_algndata: crate::endian::U16<crate::endian::BigEndian>,
    pub o_modtype: crate::endian::U16<crate::endian::BigEndian>,
    pub o_cpuflag: u8,
    pub o_cputype: u8,
    pub o_maxstack: crate::endian::U32<crate::endian::BigEndian>,
    pub o_maxdata: crate::endian::U32<crate::endian::BigEndian>,
    pub o_debugger: crate::endian::U32<crate::endian::BigEndian>,
    pub o_textpsize: u8,
    pub o_datapsize: u8,
    pub o_stackpsize: u8,
    pub o_flags: u8,
    pub o_sntdata: crate::endian::U16<crate::endian::BigEndian>,
    pub o_sntbss: crate::endian::U16<crate::endian::BigEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `o_mflag` | `crate::endian::U16<crate::endian::BigEndian>` | Flags. |
| `o_vstamp` | `crate::endian::U16<crate::endian::BigEndian>` | Version. |
| `o_tsize` | `crate::endian::U32<crate::endian::BigEndian>` | Text size in bytes. |
| `o_dsize` | `crate::endian::U32<crate::endian::BigEndian>` | Initialized data size in bytes. |
| `o_bsize` | `crate::endian::U32<crate::endian::BigEndian>` | Uninitialized data size in bytes. |
| `o_entry` | `crate::endian::U32<crate::endian::BigEndian>` | Entry point descriptor (virtual address). |
| `o_text_start` | `crate::endian::U32<crate::endian::BigEndian>` | Base address of text (virtual address). |
| `o_data_start` | `crate::endian::U32<crate::endian::BigEndian>` | Base address of data (virtual address). |
| `o_toc` | `crate::endian::U32<crate::endian::BigEndian>` | Address of TOC anchor. |
| `o_snentry` | `crate::endian::U16<crate::endian::BigEndian>` | Section number for entry point. |
| `o_sntext` | `crate::endian::U16<crate::endian::BigEndian>` | Section number for .text. |
| `o_sndata` | `crate::endian::U16<crate::endian::BigEndian>` | Section number for .data. |
| `o_sntoc` | `crate::endian::U16<crate::endian::BigEndian>` | Section number for TOC. |
| `o_snloader` | `crate::endian::U16<crate::endian::BigEndian>` | Section number for loader data. |
| `o_snbss` | `crate::endian::U16<crate::endian::BigEndian>` | Section number for .bss. |
| `o_algntext` | `crate::endian::U16<crate::endian::BigEndian>` | Maximum alignment for .text. |
| `o_algndata` | `crate::endian::U16<crate::endian::BigEndian>` | Maximum alignment for .data. |
| `o_modtype` | `crate::endian::U16<crate::endian::BigEndian>` | Module type field. |
| `o_cpuflag` | `u8` | Bit flags - cpu types of objects. |
| `o_cputype` | `u8` | Reserved for CPU type. |
| `o_maxstack` | `crate::endian::U32<crate::endian::BigEndian>` | Maximum stack size allowed (bytes). |
| `o_maxdata` | `crate::endian::U32<crate::endian::BigEndian>` | Maximum data size allowed (bytes). |
| `o_debugger` | `crate::endian::U32<crate::endian::BigEndian>` | Reserved for debuggers. |
| `o_textpsize` | `u8` | Requested text page size. |
| `o_datapsize` | `u8` | Requested data page size. |
| `o_stackpsize` | `u8` | Requested stack page size. |
| `o_flags` | `u8` | Flags and thread-local storage alignment. |
| `o_sntdata` | `crate::endian::U16<crate::endian::BigEndian>` | Section number for .tdata. |
| `o_sntbss` | `crate::endian::U16<crate::endian::BigEndian>` | Section number for .tbss. |

##### Implementations

###### Trait Implementations

- **Unpin**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Send**
- **Pod**
- **RefUnwindSafe**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> AuxHeader32 { /* ... */ }
    ```

- **Copy**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Freeze**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Sync**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **AuxHeader**
  - ```rust
    fn o_mflag(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn o_vstamp(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn o_tsize(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn o_dsize(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn o_bsize(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn o_entry(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn o_text_start(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn o_data_start(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn o_toc(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn o_snentry(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn o_sntext(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn o_sndata(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn o_sntoc(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn o_snloader(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn o_snbss(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn o_algntext(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn o_algndata(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn o_modtype(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn o_cpuflag(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn o_cputype(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn o_maxstack(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn o_maxdata(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn o_debugger(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn o_textpsize(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn o_datapsize(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn o_stackpsize(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn o_flags(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn o_sntdata(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn o_sntbss(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn o_x64flags(self: &Self) -> Option<u16> { /* ... */ }
    ```

- **UnwindSafe**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

#### Struct `AuxHeader64`

**Attributes:**

- `#[repr(C)]`

The auxiliary header immediately following file header. If the value of the
f_opthdr field in the file header is 0, the auxiliary header does not exist.

```rust
pub struct AuxHeader64 {
    pub o_mflag: crate::endian::U16<crate::endian::BigEndian>,
    pub o_vstamp: crate::endian::U16<crate::endian::BigEndian>,
    pub o_debugger: crate::endian::U32<crate::endian::BigEndian>,
    pub o_text_start: crate::endian::U64<crate::endian::BigEndian>,
    pub o_data_start: crate::endian::U64<crate::endian::BigEndian>,
    pub o_toc: crate::endian::U64<crate::endian::BigEndian>,
    pub o_snentry: crate::endian::U16<crate::endian::BigEndian>,
    pub o_sntext: crate::endian::U16<crate::endian::BigEndian>,
    pub o_sndata: crate::endian::U16<crate::endian::BigEndian>,
    pub o_sntoc: crate::endian::U16<crate::endian::BigEndian>,
    pub o_snloader: crate::endian::U16<crate::endian::BigEndian>,
    pub o_snbss: crate::endian::U16<crate::endian::BigEndian>,
    pub o_algntext: crate::endian::U16<crate::endian::BigEndian>,
    pub o_algndata: crate::endian::U16<crate::endian::BigEndian>,
    pub o_modtype: crate::endian::U16<crate::endian::BigEndian>,
    pub o_cpuflag: u8,
    pub o_cputype: u8,
    pub o_textpsize: u8,
    pub o_datapsize: u8,
    pub o_stackpsize: u8,
    pub o_flags: u8,
    pub o_tsize: crate::endian::U64<crate::endian::BigEndian>,
    pub o_dsize: crate::endian::U64<crate::endian::BigEndian>,
    pub o_bsize: crate::endian::U64<crate::endian::BigEndian>,
    pub o_entry: crate::endian::U64<crate::endian::BigEndian>,
    pub o_maxstack: crate::endian::U64<crate::endian::BigEndian>,
    pub o_maxdata: crate::endian::U64<crate::endian::BigEndian>,
    pub o_sntdata: crate::endian::U16<crate::endian::BigEndian>,
    pub o_sntbss: crate::endian::U16<crate::endian::BigEndian>,
    pub o_x64flags: crate::endian::U16<crate::endian::BigEndian>,
    pub o_resv3a: crate::endian::U16<crate::endian::BigEndian>,
    pub o_resv3: [crate::endian::U32<crate::endian::BigEndian>; 2],
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `o_mflag` | `crate::endian::U16<crate::endian::BigEndian>` | Flags. |
| `o_vstamp` | `crate::endian::U16<crate::endian::BigEndian>` | Version. |
| `o_debugger` | `crate::endian::U32<crate::endian::BigEndian>` | Reserved for debuggers. |
| `o_text_start` | `crate::endian::U64<crate::endian::BigEndian>` | Base address of text (virtual address). |
| `o_data_start` | `crate::endian::U64<crate::endian::BigEndian>` | Base address of data (virtual address). |
| `o_toc` | `crate::endian::U64<crate::endian::BigEndian>` | Address of TOC anchor. |
| `o_snentry` | `crate::endian::U16<crate::endian::BigEndian>` | Section number for entry point. |
| `o_sntext` | `crate::endian::U16<crate::endian::BigEndian>` | Section number for .text. |
| `o_sndata` | `crate::endian::U16<crate::endian::BigEndian>` | Section number for .data. |
| `o_sntoc` | `crate::endian::U16<crate::endian::BigEndian>` | Section number for TOC. |
| `o_snloader` | `crate::endian::U16<crate::endian::BigEndian>` | Section number for loader data. |
| `o_snbss` | `crate::endian::U16<crate::endian::BigEndian>` | Section number for .bss. |
| `o_algntext` | `crate::endian::U16<crate::endian::BigEndian>` | Maximum alignment for .text. |
| `o_algndata` | `crate::endian::U16<crate::endian::BigEndian>` | Maximum alignment for .data. |
| `o_modtype` | `crate::endian::U16<crate::endian::BigEndian>` | Module type field. |
| `o_cpuflag` | `u8` | Bit flags - cpu types of objects. |
| `o_cputype` | `u8` | Reserved for CPU type. |
| `o_textpsize` | `u8` | Requested text page size. |
| `o_datapsize` | `u8` | Requested data page size. |
| `o_stackpsize` | `u8` | Requested stack page size. |
| `o_flags` | `u8` | Flags and thread-local storage alignment. |
| `o_tsize` | `crate::endian::U64<crate::endian::BigEndian>` | Text size in bytes. |
| `o_dsize` | `crate::endian::U64<crate::endian::BigEndian>` | Initialized data size in bytes. |
| `o_bsize` | `crate::endian::U64<crate::endian::BigEndian>` | Uninitialized data size in bytes. |
| `o_entry` | `crate::endian::U64<crate::endian::BigEndian>` | Entry point descriptor (virtual address). |
| `o_maxstack` | `crate::endian::U64<crate::endian::BigEndian>` | Maximum stack size allowed (bytes). |
| `o_maxdata` | `crate::endian::U64<crate::endian::BigEndian>` | Maximum data size allowed (bytes). |
| `o_sntdata` | `crate::endian::U16<crate::endian::BigEndian>` | Section number for .tdata. |
| `o_sntbss` | `crate::endian::U16<crate::endian::BigEndian>` | Section number for .tbss. |
| `o_x64flags` | `crate::endian::U16<crate::endian::BigEndian>` | XCOFF64 flags. |
| `o_resv3a` | `crate::endian::U16<crate::endian::BigEndian>` | Reserved. |
| `o_resv3` | `[crate::endian::U32<crate::endian::BigEndian>; 2]` | Reserved. |

##### Implementations

###### Trait Implementations

- **Pod**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **RefUnwindSafe**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Copy**
- **AuxHeader**
  - ```rust
    fn o_mflag(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn o_vstamp(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn o_tsize(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn o_dsize(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn o_bsize(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn o_entry(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn o_text_start(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn o_data_start(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn o_toc(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn o_snentry(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn o_sntext(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn o_sndata(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn o_sntoc(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn o_snloader(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn o_snbss(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn o_algntext(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn o_algndata(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn o_modtype(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn o_cpuflag(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn o_cputype(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn o_maxstack(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn o_maxdata(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn o_debugger(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn o_textpsize(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn o_datapsize(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn o_stackpsize(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn o_flags(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn o_sntdata(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn o_sntbss(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn o_x64flags(self: &Self) -> Option<u16> { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Unpin**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Sync**
- **Freeze**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Send**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> AuxHeader64 { /* ... */ }
    ```

- **UnwindSafe**
#### Struct `SectionHeader32`

**Attributes:**

- `#[repr(C)]`

Section header.

```rust
pub struct SectionHeader32 {
    pub s_name: [u8; 8],
    pub s_paddr: crate::endian::U32<crate::endian::BigEndian>,
    pub s_vaddr: crate::endian::U32<crate::endian::BigEndian>,
    pub s_size: crate::endian::U32<crate::endian::BigEndian>,
    pub s_scnptr: crate::endian::U32<crate::endian::BigEndian>,
    pub s_relptr: crate::endian::U32<crate::endian::BigEndian>,
    pub s_lnnoptr: crate::endian::U32<crate::endian::BigEndian>,
    pub s_nreloc: crate::endian::U16<crate::endian::BigEndian>,
    pub s_nlnno: crate::endian::U16<crate::endian::BigEndian>,
    pub s_flags: crate::endian::U32<crate::endian::BigEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `s_name` | `[u8; 8]` | Section name. |
| `s_paddr` | `crate::endian::U32<crate::endian::BigEndian>` | Physical address. |
| `s_vaddr` | `crate::endian::U32<crate::endian::BigEndian>` | Virtual address (same as physical address). |
| `s_size` | `crate::endian::U32<crate::endian::BigEndian>` | Section size. |
| `s_scnptr` | `crate::endian::U32<crate::endian::BigEndian>` | Offset in file to raw data for section. |
| `s_relptr` | `crate::endian::U32<crate::endian::BigEndian>` | Offset in file to relocation entries for section. |
| `s_lnnoptr` | `crate::endian::U32<crate::endian::BigEndian>` | Offset in file to line number entries for section. |
| `s_nreloc` | `crate::endian::U16<crate::endian::BigEndian>` | Number of relocation entries. |
| `s_nlnno` | `crate::endian::U16<crate::endian::BigEndian>` | Number of line number entries. |
| `s_flags` | `crate::endian::U32<crate::endian::BigEndian>` | Flags to define the section type. |

##### Implementations

###### Trait Implementations

- **UnwindSafe**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> SectionHeader32 { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Sync**
- **Send**
- **SectionHeader**
  - ```rust
    fn s_name(self: &Self) -> &[u8; 8] { /* ... */ }
    ```

  - ```rust
    fn s_paddr(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn s_vaddr(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn s_size(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn s_scnptr(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn s_relptr(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn s_lnnoptr(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn s_nreloc(self: &Self) -> <Self as >::HalfWord { /* ... */ }
    ```

  - ```rust
    fn s_nlnno(self: &Self) -> <Self as >::HalfWord { /* ... */ }
    ```

  - ```rust
    fn s_flags(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn relocations<''data, R: ReadRef<''data>>(self: &Self, data: R) -> read::Result<&''data [<Self as >::Rel]> { /* ... */ }
    ```
    Read the relocations in a XCOFF32 file.

- **Pod**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Freeze**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **RefUnwindSafe**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Copy**
- **Unpin**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

#### Struct `SectionHeader64`

**Attributes:**

- `#[repr(C)]`

Section header.

```rust
pub struct SectionHeader64 {
    pub s_name: [u8; 8],
    pub s_paddr: crate::endian::U64<crate::endian::BigEndian>,
    pub s_vaddr: crate::endian::U64<crate::endian::BigEndian>,
    pub s_size: crate::endian::U64<crate::endian::BigEndian>,
    pub s_scnptr: crate::endian::U64<crate::endian::BigEndian>,
    pub s_relptr: crate::endian::U64<crate::endian::BigEndian>,
    pub s_lnnoptr: crate::endian::U64<crate::endian::BigEndian>,
    pub s_nreloc: crate::endian::U32<crate::endian::BigEndian>,
    pub s_nlnno: crate::endian::U32<crate::endian::BigEndian>,
    pub s_flags: crate::endian::U32<crate::endian::BigEndian>,
    pub s_reserve: crate::endian::U32<crate::endian::BigEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `s_name` | `[u8; 8]` | Section name. |
| `s_paddr` | `crate::endian::U64<crate::endian::BigEndian>` | Physical address. |
| `s_vaddr` | `crate::endian::U64<crate::endian::BigEndian>` | Virtual address (same as physical address). |
| `s_size` | `crate::endian::U64<crate::endian::BigEndian>` | Section size. |
| `s_scnptr` | `crate::endian::U64<crate::endian::BigEndian>` | Offset in file to raw data for section. |
| `s_relptr` | `crate::endian::U64<crate::endian::BigEndian>` | Offset in file to relocation entries for section. |
| `s_lnnoptr` | `crate::endian::U64<crate::endian::BigEndian>` | Offset in file to line number entries for section. |
| `s_nreloc` | `crate::endian::U32<crate::endian::BigEndian>` | Number of relocation entries. |
| `s_nlnno` | `crate::endian::U32<crate::endian::BigEndian>` | Number of line number entries. |
| `s_flags` | `crate::endian::U32<crate::endian::BigEndian>` | Flags to define the section type. |
| `s_reserve` | `crate::endian::U32<crate::endian::BigEndian>` | Reserved. |

##### Implementations

###### Trait Implementations

- **Copy**
- **UnwindSafe**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> SectionHeader64 { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Sync**
- **Freeze**
- **RefUnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **SectionHeader**
  - ```rust
    fn s_name(self: &Self) -> &[u8; 8] { /* ... */ }
    ```

  - ```rust
    fn s_paddr(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn s_vaddr(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn s_size(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn s_scnptr(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn s_relptr(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn s_lnnoptr(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn s_nreloc(self: &Self) -> <Self as >::HalfWord { /* ... */ }
    ```

  - ```rust
    fn s_nlnno(self: &Self) -> <Self as >::HalfWord { /* ... */ }
    ```

  - ```rust
    fn s_flags(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn relocations<''data, R: ReadRef<''data>>(self: &Self, data: R) -> read::Result<&''data [<Self as >::Rel]> { /* ... */ }
    ```
    Read the relocations in a XCOFF64 file.

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Pod**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Unpin**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Send**
#### Struct `SymbolBytes`

**Attributes:**

- `#[repr(C)]`

```rust
pub struct SymbolBytes(pub [u8; 18]);
```

##### Fields

| Index | Type | Documentation |
|-------|------|---------------|
| 0 | `[u8; 18]` |  |

##### Implementations

###### Trait Implementations

- **Send**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Sync**
- **Unpin**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Pod**
- **UnwindSafe**
- **Copy**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **RefUnwindSafe**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Freeze**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> SymbolBytes { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

#### Struct `Symbol32`

**Attributes:**

- `#[repr(C)]`

Symbol table entry.

```rust
pub struct Symbol32 {
    pub n_name: [u8; 8],
    pub n_value: crate::endian::U32<crate::endian::BigEndian>,
    pub n_scnum: crate::endian::I16<crate::endian::BigEndian>,
    pub n_type: crate::endian::U16<crate::endian::BigEndian>,
    pub n_sclass: u8,
    pub n_numaux: u8,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `n_name` | `[u8; 8]` | Symbol name.<br><br>If first 4 bytes are 0, then second 4 bytes are offset into string table. |
| `n_value` | `crate::endian::U32<crate::endian::BigEndian>` | Symbol value; storage class-dependent. |
| `n_scnum` | `crate::endian::I16<crate::endian::BigEndian>` | Section number of symbol. |
| `n_type` | `crate::endian::U16<crate::endian::BigEndian>` | Basic and derived type specification. |
| `n_sclass` | `u8` | Storage class of symbol. |
| `n_numaux` | `u8` | Number of auxiliary entries. |

##### Implementations

###### Trait Implementations

- **Freeze**
- **UnwindSafe**
- **Sync**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Copy**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Symbol**
  - ```rust
    fn n_value(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn n_scnum(self: &Self) -> i16 { /* ... */ }
    ```

  - ```rust
    fn n_type(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn n_sclass(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn n_numaux(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn name_offset(self: &Self) -> Option<u32> { /* ... */ }
    ```

  - ```rust
    fn name<''data, R: ReadRef<''data>>(self: &''data Self, strings: StringTable<''data, R>) -> Result<&''data [u8]> { /* ... */ }
    ```
    Parse the symbol name for XCOFF32.

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Send**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Unpin**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **RefUnwindSafe**
- **Pod**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> Symbol32 { /* ... */ }
    ```

#### Struct `Symbol64`

**Attributes:**

- `#[repr(C)]`

Symbol table entry.

```rust
pub struct Symbol64 {
    pub n_value: crate::endian::U64<crate::endian::BigEndian>,
    pub n_offset: crate::endian::U32<crate::endian::BigEndian>,
    pub n_scnum: crate::endian::I16<crate::endian::BigEndian>,
    pub n_type: crate::endian::U16<crate::endian::BigEndian>,
    pub n_sclass: u8,
    pub n_numaux: u8,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `n_value` | `crate::endian::U64<crate::endian::BigEndian>` | Symbol value; storage class-dependent. |
| `n_offset` | `crate::endian::U32<crate::endian::BigEndian>` | Offset of the name in string table or .debug section. |
| `n_scnum` | `crate::endian::I16<crate::endian::BigEndian>` | Section number of symbol. |
| `n_type` | `crate::endian::U16<crate::endian::BigEndian>` | Basic and derived type specification. |
| `n_sclass` | `u8` | Storage class of symbol. |
| `n_numaux` | `u8` | Number of auxiliary entries. |

##### Implementations

###### Trait Implementations

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Unpin**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Freeze**
- **Send**
- **UnwindSafe**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Symbol**
  - ```rust
    fn n_value(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn n_scnum(self: &Self) -> i16 { /* ... */ }
    ```

  - ```rust
    fn n_type(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn n_sclass(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn n_numaux(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn name_offset(self: &Self) -> Option<u32> { /* ... */ }
    ```

  - ```rust
    fn name<''data, R: ReadRef<''data>>(self: &''data Self, strings: StringTable<''data, R>) -> Result<&''data [u8]> { /* ... */ }
    ```
    Parse the symbol name for XCOFF64.

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Copy**
- **Pod**
- **Sync**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> Symbol64 { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **RefUnwindSafe**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

#### Struct `FileAux32`

**Attributes:**

- `#[repr(C)]`

File Auxiliary Entry for C_FILE Symbols.

```rust
pub struct FileAux32 {
    pub x_fname: [u8; 8],
    pub x_fpad: [u8; 6],
    pub x_ftype: u8,
    pub x_freserve: [u8; 3],
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `x_fname` | `[u8; 8]` | The source file name or compiler-related string.<br><br>If first 4 bytes are 0, then second 4 bytes are offset into string table. |
| `x_fpad` | `[u8; 6]` | Pad size for file name. |
| `x_ftype` | `u8` | The source-file string type. |
| `x_freserve` | `[u8; 3]` | Reserved. |

##### Implementations

###### Trait Implementations

- **Send**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> FileAux32 { /* ... */ }
    ```

- **Unpin**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Pod**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Sync**
- **Copy**
- **RefUnwindSafe**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **UnwindSafe**
- **FileAux**
  - ```rust
    fn x_fname(self: &Self) -> &[u8; 8] { /* ... */ }
    ```

  - ```rust
    fn x_ftype(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn x_auxtype(self: &Self) -> Option<u8> { /* ... */ }
    ```

- **Freeze**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

#### Struct `FileAux64`

**Attributes:**

- `#[repr(C)]`

File Auxiliary Entry for C_FILE Symbols.

```rust
pub struct FileAux64 {
    pub x_fname: [u8; 8],
    pub x_fpad: [u8; 6],
    pub x_ftype: u8,
    pub x_freserve: [u8; 2],
    pub x_auxtype: u8,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `x_fname` | `[u8; 8]` | The source file name or compiler-related string.<br><br>If first 4 bytes are 0, then second 4 bytes are offset into string table. |
| `x_fpad` | `[u8; 6]` | Pad size for file name. |
| `x_ftype` | `u8` | The source-file string type. |
| `x_freserve` | `[u8; 2]` | Reserved. |
| `x_auxtype` | `u8` | Specifies the type of auxiliary entry. Contains _AUX_FILE for this auxiliary entry. |

##### Implementations

###### Trait Implementations

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **FileAux**
  - ```rust
    fn x_fname(self: &Self) -> &[u8; 8] { /* ... */ }
    ```

  - ```rust
    fn x_ftype(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn x_auxtype(self: &Self) -> Option<u8> { /* ... */ }
    ```

- **RefUnwindSafe**
- **Pod**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Sync**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Send**
- **Unpin**
- **Copy**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **UnwindSafe**
- **Freeze**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> FileAux64 { /* ... */ }
    ```

#### Struct `CsectAux32`

**Attributes:**

- `#[repr(C)]`

Csect auxiliary entry for C_EXT, C_WEAKEXT, and C_HIDEXT symbols.

```rust
pub struct CsectAux32 {
    pub x_scnlen: crate::endian::U32<crate::endian::BigEndian>,
    pub x_parmhash: crate::endian::U32<crate::endian::BigEndian>,
    pub x_snhash: crate::endian::U16<crate::endian::BigEndian>,
    pub x_smtyp: u8,
    pub x_smclas: u8,
    pub x_stab: crate::endian::U32<crate::endian::BigEndian>,
    pub x_snstab: crate::endian::U16<crate::endian::BigEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `x_scnlen` | `crate::endian::U32<crate::endian::BigEndian>` | Section length. |
| `x_parmhash` | `crate::endian::U32<crate::endian::BigEndian>` | Offset of parameter type-check hash in .typchk section. |
| `x_snhash` | `crate::endian::U16<crate::endian::BigEndian>` | .typchk section number. |
| `x_smtyp` | `u8` | Symbol alignment and type. |
| `x_smclas` | `u8` | Storage mapping class. |
| `x_stab` | `crate::endian::U32<crate::endian::BigEndian>` | Reserved. |
| `x_snstab` | `crate::endian::U16<crate::endian::BigEndian>` | x_snstab. |

##### Implementations

###### Trait Implementations

- **Freeze**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Copy**
- **Pod**
- **UnwindSafe**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **RefUnwindSafe**
- **Sync**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **CsectAux**
  - ```rust
    fn x_scnlen(self: &Self) -> u64 { /* ... */ }
    ```

  - ```rust
    fn x_parmhash(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn x_snhash(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn x_smtyp(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn x_smclas(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn x_stab(self: &Self) -> Option<u32> { /* ... */ }
    ```

  - ```rust
    fn x_snstab(self: &Self) -> Option<u16> { /* ... */ }
    ```

  - ```rust
    fn x_auxtype(self: &Self) -> Option<u8> { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> CsectAux32 { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Unpin**
- **Send**
#### Struct `CsectAux64`

**Attributes:**

- `#[repr(C)]`

Csect auxiliary entry for C_EXT, C_WEAKEXT, and C_HIDEXT symbols.

```rust
pub struct CsectAux64 {
    pub x_scnlen_lo: crate::endian::U32<crate::endian::BigEndian>,
    pub x_parmhash: crate::endian::U32<crate::endian::BigEndian>,
    pub x_snhash: crate::endian::U16<crate::endian::BigEndian>,
    pub x_smtyp: u8,
    pub x_smclas: u8,
    pub x_scnlen_hi: crate::endian::U32<crate::endian::BigEndian>,
    pub pad: u8,
    pub x_auxtype: u8,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `x_scnlen_lo` | `crate::endian::U32<crate::endian::BigEndian>` | Low 4 bytes of section length. |
| `x_parmhash` | `crate::endian::U32<crate::endian::BigEndian>` | Offset of parameter type-check hash in .typchk section. |
| `x_snhash` | `crate::endian::U16<crate::endian::BigEndian>` | .typchk section number. |
| `x_smtyp` | `u8` | Symbol alignment and type. |
| `x_smclas` | `u8` | Storage mapping class. |
| `x_scnlen_hi` | `crate::endian::U32<crate::endian::BigEndian>` | High 4 bytes of section length. |
| `pad` | `u8` | Reserved. |
| `x_auxtype` | `u8` | Contains _AUX_CSECT; indicates type of auxiliary entry. |

##### Implementations

###### Trait Implementations

- **Unpin**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Send**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **CsectAux**
  - ```rust
    fn x_scnlen(self: &Self) -> u64 { /* ... */ }
    ```

  - ```rust
    fn x_parmhash(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn x_snhash(self: &Self) -> u16 { /* ... */ }
    ```

  - ```rust
    fn x_smtyp(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn x_smclas(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn x_stab(self: &Self) -> Option<u32> { /* ... */ }
    ```

  - ```rust
    fn x_snstab(self: &Self) -> Option<u16> { /* ... */ }
    ```

  - ```rust
    fn x_auxtype(self: &Self) -> Option<u8> { /* ... */ }
    ```

- **RefUnwindSafe**
- **Copy**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **UnwindSafe**
- **Freeze**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> CsectAux64 { /* ... */ }
    ```

- **Pod**
- **Sync**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

#### Struct `FunAux32`

**Attributes:**

- `#[repr(C)]`

Function auxiliary entry.

```rust
pub struct FunAux32 {
    pub x_exptr: crate::endian::U32<crate::endian::BigEndian>,
    pub x_fsize: crate::endian::U32<crate::endian::BigEndian>,
    pub x_lnnoptr: crate::endian::U32<crate::endian::BigEndian>,
    pub x_endndx: crate::endian::U32<crate::endian::BigEndian>,
    pub pad: crate::endian::U16<crate::endian::BigEndian>,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `x_exptr` | `crate::endian::U32<crate::endian::BigEndian>` | File offset to exception table entry. |
| `x_fsize` | `crate::endian::U32<crate::endian::BigEndian>` | Size of function in bytes. |
| `x_lnnoptr` | `crate::endian::U32<crate::endian::BigEndian>` | File pointer to line number |
| `x_endndx` | `crate::endian::U32<crate::endian::BigEndian>` | Symbol table index of next entry beyond this function. |
| `pad` | `crate::endian::U16<crate::endian::BigEndian>` | Pad |

##### Implementations

###### Trait Implementations

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **UnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Unpin**
- **Copy**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> FunAux32 { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Freeze**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Send**
- **Pod**
- **Sync**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **RefUnwindSafe**
#### Struct `FunAux64`

**Attributes:**

- `#[repr(C)]`

Function auxiliary entry.

```rust
pub struct FunAux64 {
    pub x_lnnoptr: crate::endian::U64<crate::endian::BigEndian>,
    pub x_fsize: crate::endian::U32<crate::endian::BigEndian>,
    pub x_endndx: crate::endian::U32<crate::endian::BigEndian>,
    pub pad: u8,
    pub x_auxtype: u8,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `x_lnnoptr` | `crate::endian::U64<crate::endian::BigEndian>` | File pointer to line number |
| `x_fsize` | `crate::endian::U32<crate::endian::BigEndian>` | Size of function in bytes. |
| `x_endndx` | `crate::endian::U32<crate::endian::BigEndian>` | Symbol table index of next entry beyond this function. |
| `pad` | `u8` | Pad |
| `x_auxtype` | `u8` | Contains _AUX_FCN; Type of auxiliary entry. |

##### Implementations

###### Trait Implementations

- **Clone**
  - ```rust
    fn clone(self: &Self) -> FunAux64 { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Freeze**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Unpin**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Copy**
- **RefUnwindSafe**
- **Send**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **UnwindSafe**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Sync**
- **Pod**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

#### Struct `ExpAux`

**Attributes:**

- `#[repr(C)]`

Exception auxiliary entry. (XCOFF64 only)

```rust
pub struct ExpAux {
    pub x_exptr: crate::endian::U64<crate::endian::BigEndian>,
    pub x_fsize: crate::endian::U32<crate::endian::BigEndian>,
    pub x_endndx: crate::endian::U32<crate::endian::BigEndian>,
    pub pad: u8,
    pub x_auxtype: u8,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `x_exptr` | `crate::endian::U64<crate::endian::BigEndian>` | File offset to exception table entry. |
| `x_fsize` | `crate::endian::U32<crate::endian::BigEndian>` | Size of function in bytes. |
| `x_endndx` | `crate::endian::U32<crate::endian::BigEndian>` | Symbol table index of next entry beyond this function. |
| `pad` | `u8` | Pad |
| `x_auxtype` | `u8` | Contains _AUX_EXCEPT; Type of auxiliary entry |

##### Implementations

###### Trait Implementations

- **Pod**
- **Send**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Copy**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **UnwindSafe**
- **RefUnwindSafe**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> ExpAux { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Sync**
- **Freeze**
- **Unpin**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

#### Struct `BlockAux32`

**Attributes:**

- `#[repr(C)]`

Block auxiliary entry for the C_BLOCK and C_FCN Symbols.

```rust
pub struct BlockAux32 {
    pub pad: [u8; 2],
    pub x_lnnohi: crate::endian::U16<crate::endian::BigEndian>,
    pub x_lnnolo: crate::endian::U16<crate::endian::BigEndian>,
    pub pad2: [u8; 12],
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `pad` | `[u8; 2]` | Reserved. |
| `x_lnnohi` | `crate::endian::U16<crate::endian::BigEndian>` | High-order 2 bytes of the source line number. |
| `x_lnnolo` | `crate::endian::U16<crate::endian::BigEndian>` | Low-order 2 bytes of the source line number. |
| `pad2` | `[u8; 12]` | Reserved. |

##### Implementations

###### Trait Implementations

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Sync**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Send**
- **Freeze**
- **RefUnwindSafe**
- **Copy**
- **Pod**
- **Unpin**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **UnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> BlockAux32 { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

#### Struct `BlockAux64`

**Attributes:**

- `#[repr(C)]`

Block auxiliary entry for the C_BLOCK and C_FCN Symbols.

```rust
pub struct BlockAux64 {
    pub x_lnno: crate::endian::U32<crate::endian::BigEndian>,
    pub pad: [u8; 13],
    pub x_auxtype: u8,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `x_lnno` | `crate::endian::U32<crate::endian::BigEndian>` | Source line number. |
| `pad` | `[u8; 13]` | Reserved. |
| `x_auxtype` | `u8` | Contains _AUX_SYM; Type of auxiliary entry. |

##### Implementations

###### Trait Implementations

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> BlockAux64 { /* ... */ }
    ```

- **Send**
- **Freeze**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **RefUnwindSafe**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Sync**
- **Copy**
- **Unpin**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **UnwindSafe**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Pod**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

#### Struct `StatAux`

**Attributes:**

- `#[repr(C)]`

Section auxiliary entry for the C_STAT Symbol. (XCOFF32 Only)

```rust
pub struct StatAux {
    pub x_scnlen: crate::endian::U32<crate::endian::BigEndian>,
    pub x_nreloc: crate::endian::U16<crate::endian::BigEndian>,
    pub x_nlinno: crate::endian::U16<crate::endian::BigEndian>,
    pub pad: [u8; 10],
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `x_scnlen` | `crate::endian::U32<crate::endian::BigEndian>` | Section length. |
| `x_nreloc` | `crate::endian::U16<crate::endian::BigEndian>` | Number of relocation entries. |
| `x_nlinno` | `crate::endian::U16<crate::endian::BigEndian>` | Number of line numbers. |
| `pad` | `[u8; 10]` | Reserved. |

##### Implementations

###### Trait Implementations

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Pod**
- **Freeze**
- **Send**
- **Sync**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Unpin**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> StatAux { /* ... */ }
    ```

- **RefUnwindSafe**
- **Copy**
- **UnwindSafe**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

#### Struct `DwarfAux32`

**Attributes:**

- `#[repr(C)]`

Section auxiliary entry Format for C_DWARF symbols.

```rust
pub struct DwarfAux32 {
    pub x_scnlen: crate::endian::U32<crate::endian::BigEndian>,
    pub pad: [u8; 4],
    pub x_nreloc: crate::endian::U32<crate::endian::BigEndian>,
    pub pad2: [u8; 6],
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `x_scnlen` | `crate::endian::U32<crate::endian::BigEndian>` | Length of portion of section represented by symbol. |
| `pad` | `[u8; 4]` | Reserved. |
| `x_nreloc` | `crate::endian::U32<crate::endian::BigEndian>` | Number of relocation entries in section. |
| `pad2` | `[u8; 6]` | Reserved. |

##### Implementations

###### Trait Implementations

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Sync**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Pod**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> DwarfAux32 { /* ... */ }
    ```

- **RefUnwindSafe**
- **Copy**
- **Freeze**
- **Unpin**
- **UnwindSafe**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Send**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

#### Struct `DwarfAux64`

**Attributes:**

- `#[repr(C)]`

Section auxiliary entry Format for C_DWARF symbols.

```rust
pub struct DwarfAux64 {
    pub x_scnlen: crate::endian::U64<crate::endian::BigEndian>,
    pub x_nreloc: crate::endian::U64<crate::endian::BigEndian>,
    pub pad: u8,
    pub x_auxtype: u8,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `x_scnlen` | `crate::endian::U64<crate::endian::BigEndian>` | Length of portion of section represented by symbol. |
| `x_nreloc` | `crate::endian::U64<crate::endian::BigEndian>` | Number of relocation entries in section. |
| `pad` | `u8` | Reserved. |
| `x_auxtype` | `u8` | Contains _AUX_SECT; Type of Auxiliary entry. |

##### Implementations

###### Trait Implementations

- **Freeze**
- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **Clone**
  - ```rust
    fn clone(self: &Self) -> DwarfAux64 { /* ... */ }
    ```

- **Pod**
- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Unpin**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Send**
- **Copy**
- **UnwindSafe**
- **RefUnwindSafe**
- **Sync**
- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

#### Struct `Rel32`

**Attributes:**

- `#[repr(C)]`

Relocation table entry

```rust
pub struct Rel32 {
    pub r_vaddr: crate::endian::U32<crate::endian::BigEndian>,
    pub r_symndx: crate::endian::U32<crate::endian::BigEndian>,
    pub r_rsize: u8,
    pub r_rtype: u8,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `r_vaddr` | `crate::endian::U32<crate::endian::BigEndian>` | Virtual address (position) in section to be relocated. |
| `r_symndx` | `crate::endian::U32<crate::endian::BigEndian>` | Symbol table index of item that is referenced. |
| `r_rsize` | `u8` | Relocation size and information. |
| `r_rtype` | `u8` | Relocation type. |

##### Implementations

###### Trait Implementations

- **Rel**
  - ```rust
    fn r_vaddr(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn r_symndx(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn r_rsize(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn r_rtype(self: &Self) -> u8 { /* ... */ }
    ```

- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

- **UnwindSafe**
- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **RefUnwindSafe**
- **Sync**
- **Send**
- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **Copy**
- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **Freeze**
- **Clone**
  - ```rust
    fn clone(self: &Self) -> Rel32 { /* ... */ }
    ```

- **Unpin**
- **Pod**
#### Struct `Rel64`

**Attributes:**

- `#[repr(C)]`

Relocation table entry

```rust
pub struct Rel64 {
    pub r_vaddr: crate::endian::U64<crate::endian::BigEndian>,
    pub r_symndx: crate::endian::U32<crate::endian::BigEndian>,
    pub r_rsize: u8,
    pub r_rtype: u8,
}
```

##### Fields

| Name | Type | Documentation |
|------|------|---------------|
| `r_vaddr` | `crate::endian::U64<crate::endian::BigEndian>` | Virtual address (position) in section to be relocated. |
| `r_symndx` | `crate::endian::U32<crate::endian::BigEndian>` | Symbol table index of item that is referenced. |
| `r_rsize` | `u8` | Relocation size and information. |
| `r_rtype` | `u8` | Relocation type. |

##### Implementations

###### Trait Implementations

- **ToOwned**
  - ```rust
    fn to_owned(self: &Self) -> T { /* ... */ }
    ```

  - ```rust
    fn clone_into(self: &Self, target: &mut T) { /* ... */ }
    ```

- **Rel**
  - ```rust
    fn r_vaddr(self: &Self) -> <Self as >::Word { /* ... */ }
    ```

  - ```rust
    fn r_symndx(self: &Self) -> u32 { /* ... */ }
    ```

  - ```rust
    fn r_rsize(self: &Self) -> u8 { /* ... */ }
    ```

  - ```rust
    fn r_rtype(self: &Self) -> u8 { /* ... */ }
    ```

- **Pod**
- **From**
  - ```rust
    fn from(t: T) -> T { /* ... */ }
    ```
    Returns the argument unchanged.

- **UnwindSafe**
- **Unpin**
- **Sync**
- **Send**
- **RefUnwindSafe**
- **CloneToUninit**
  - ```rust
    unsafe fn clone_to_uninit(self: &Self, dest: *mut u8) { /* ... */ }
    ```

- **Freeze**
- **TryInto**
  - ```rust
    fn try_into(self: Self) -> Result<U, <U as TryFrom<T>>::Error> { /* ... */ }
    ```

- **Debug**
  - ```rust
    fn fmt(self: &Self, f: &mut $crate::fmt::Formatter<''_>) -> $crate::fmt::Result { /* ... */ }
    ```

- **Any**
  - ```rust
    fn type_id(self: &Self) -> TypeId { /* ... */ }
    ```

- **Into**
  - ```rust
    fn into(self: Self) -> U { /* ... */ }
    ```
    Calls `U::from(self)`.

- **Clone**
  - ```rust
    fn clone(self: &Self) -> Rel64 { /* ... */ }
    ```

- **Copy**
- **Borrow**
  - ```rust
    fn borrow(self: &Self) -> &T { /* ... */ }
    ```

- **BorrowMut**
  - ```rust
    fn borrow_mut(self: &mut Self) -> &mut T { /* ... */ }
    ```

- **TryFrom**
  - ```rust
    fn try_from(value: U) -> Result<T, <T as TryFrom<U>>::Error> { /* ... */ }
    ```

### Constants and Statics

#### Constant `MAGIC_64`

the 64-bit mach magic number

```rust
pub const MAGIC_64: u16 = 0x01F7;
```

#### Constant `MAGIC_32`

the 32-bit mach magic number

```rust
pub const MAGIC_32: u16 = 0x01DF;
```

#### Constant `F_RELFLG`

Indicates that the relocation information for binding has been removed from
the file.

```rust
pub const F_RELFLG: u16 = 0x0001;
```

#### Constant `F_EXEC`

Indicates that the file is executable. No unresolved external references exist.

```rust
pub const F_EXEC: u16 = 0x0002;
```

#### Constant `F_LNNO`

Indicates that line numbers have been stripped from the file by a utility program.

```rust
pub const F_LNNO: u16 = 0x0004;
```

#### Constant `F_FDPR_PROF`

Indicates that the file was profiled with the fdpr command.

```rust
pub const F_FDPR_PROF: u16 = 0x0010;
```

#### Constant `F_FDPR_OPTI`

Indicates that the file was reordered with the fdpr command.

```rust
pub const F_FDPR_OPTI: u16 = 0x0020;
```

#### Constant `F_DSA`

Indicates that the file uses Very Large Program Support.

```rust
pub const F_DSA: u16 = 0x0040;
```

#### Constant `F_VARPG`

Indicates that one of the members of the auxiliary header specifying the
medium page sizes is non-zero.

```rust
pub const F_VARPG: u16 = 0x0100;
```

#### Constant `F_DYNLOAD`

Indicates the file is dynamically loadable and executable. External references
are resolved by way of imports, and the file might contain exports and loader
relocation.

```rust
pub const F_DYNLOAD: u16 = 0x1000;
```

#### Constant `F_SHROBJ`

Indicates the file is a shared object (shared library). The file is separately
loadable. That is, it is not normally bound with other objects, and its loader
exports symbols are used as automatic import symbols for other object files.

```rust
pub const F_SHROBJ: u16 = 0x2000;
```

#### Constant `F_LOADONLY`

If the object file is a member of an archive, it can be loaded by the system
loader, but the member is ignored by the binder. If the object file is not in
an archive, this flag has no effect.

```rust
pub const F_LOADONLY: u16 = 0x4000;
```

#### Constant `AOUTHSZ_SHORT`

Some AIX programs generate auxiliary headers for 32-bit object files that
end after the data_start field.

```rust
pub const AOUTHSZ_SHORT: u16 = 28;
```

#### Constant `STYP_REG`

"regular" section

```rust
pub const STYP_REG: u16 = 0x00;
```

#### Constant `STYP_PAD`

Specifies a pad section. A section of this type is used to provide alignment
padding between sections within an XCOFF executable object file. This section
header type is obsolete since padding is allowed in an XCOFF file without a
corresponding pad section header.

```rust
pub const STYP_PAD: u16 = 0x08;
```

#### Constant `STYP_DWARF`

Specifies a DWARF debugging section, which provide source file and symbol
information for the symbolic debugger.

```rust
pub const STYP_DWARF: u16 = 0x10;
```

#### Constant `STYP_TEXT`

Specifies an executable text (code) section. A section of this type contains
the executable instructions of a program.

```rust
pub const STYP_TEXT: u16 = 0x20;
```

#### Constant `STYP_DATA`

Specifies an initialized data section. A section of this type contains the
initialized data and the TOC of a program.

```rust
pub const STYP_DATA: u16 = 0x40;
```

#### Constant `STYP_BSS`

Specifies an uninitialized data section. A section header of this type
defines the uninitialized data of a program.

```rust
pub const STYP_BSS: u16 = 0x80;
```

#### Constant `STYP_EXCEPT`

Specifies an exception section. A section of this type provides information
to identify the reason that a trap or exception occurred within an executable
object program.

```rust
pub const STYP_EXCEPT: u16 = 0x0100;
```

#### Constant `STYP_INFO`

Specifies a comment section. A section of this type provides comments or data
to special processing utility programs.

```rust
pub const STYP_INFO: u16 = 0x0200;
```

#### Constant `STYP_TDATA`

Specifies an initialized thread-local data section.

```rust
pub const STYP_TDATA: u16 = 0x0400;
```

#### Constant `STYP_TBSS`

Specifies an uninitialized thread-local data section.

```rust
pub const STYP_TBSS: u16 = 0x0800;
```

#### Constant `STYP_LOADER`

Specifies a loader section. A section of this type contains object file
information for the system loader to load an XCOFF executable. The information
includes imported symbols, exported symbols, relocation data, type-check
information, and shared object names.

```rust
pub const STYP_LOADER: u16 = 0x1000;
```

#### Constant `STYP_DEBUG`

Specifies a debug section. A section of this type contains stabstring
information used by the symbolic debugger.

```rust
pub const STYP_DEBUG: u16 = 0x2000;
```

#### Constant `STYP_TYPCHK`

Specifies a type-check section. A section of this type contains
parameter/argument type-check strings used by the binder.

```rust
pub const STYP_TYPCHK: u16 = 0x4000;
```

#### Constant `STYP_OVRFLO`

Specifies a relocation or line-number field overflow section. A section
header of this type contains the count of relocation entries and line
number entries for some other section. This section header is required
when either of the counts exceeds 65,534.

```rust
pub const STYP_OVRFLO: u16 = 0x8000;
```

#### Constant `SSUBTYP_DWINFO`

```rust
pub const SSUBTYP_DWINFO: u32 = 0x10000;
```

#### Constant `SSUBTYP_DWLINE`

```rust
pub const SSUBTYP_DWLINE: u32 = 0x20000;
```

#### Constant `SSUBTYP_DWPBNMS`

```rust
pub const SSUBTYP_DWPBNMS: u32 = 0x30000;
```

#### Constant `SSUBTYP_DWPBTYP`

```rust
pub const SSUBTYP_DWPBTYP: u32 = 0x40000;
```

#### Constant `SSUBTYP_DWARNGE`

```rust
pub const SSUBTYP_DWARNGE: u32 = 0x50000;
```

#### Constant `SSUBTYP_DWABREV`

```rust
pub const SSUBTYP_DWABREV: u32 = 0x60000;
```

#### Constant `SSUBTYP_DWSTR`

```rust
pub const SSUBTYP_DWSTR: u32 = 0x70000;
```

#### Constant `SSUBTYP_DWRNGES`

```rust
pub const SSUBTYP_DWRNGES: u32 = 0x80000;
```

#### Constant `SSUBTYP_DWLOC`

```rust
pub const SSUBTYP_DWLOC: u32 = 0x90000;
```

#### Constant `SSUBTYP_DWFRAME`

```rust
pub const SSUBTYP_DWFRAME: u32 = 0xA0000;
```

#### Constant `SSUBTYP_DWMAC`

```rust
pub const SSUBTYP_DWMAC: u32 = 0xB0000;
```

#### Constant `SIZEOF_SYMBOL`

```rust
pub const SIZEOF_SYMBOL: usize = 18;
```

#### Constant `N_DEBUG`

A special symbolic debugging symbol.

```rust
pub const N_DEBUG: i16 = -2;
```

#### Constant `N_ABS`

An absolute symbol. The symbol has a value but is not relocatable.

```rust
pub const N_ABS: i16 = -1;
```

#### Constant `N_UNDEF`

An undefined external symbol.

```rust
pub const N_UNDEF: i16 = 0;
```

#### Constant `SYM_V_MASK`

Values for visibility as they would appear when encoded in the high 4 bits
of the 16-bit unsigned n_type field of symbol table entries. Valid for
32-bit XCOFF only when the o_vstamp in the auxiliary header is greater than 1.

```rust
pub const SYM_V_MASK: u16 = 0xF000;
```

#### Constant `SYM_V_INTERNAL`

```rust
pub const SYM_V_INTERNAL: u16 = 0x1000;
```

#### Constant `SYM_V_HIDDEN`

```rust
pub const SYM_V_HIDDEN: u16 = 0x2000;
```

#### Constant `SYM_V_PROTECTED`

```rust
pub const SYM_V_PROTECTED: u16 = 0x3000;
```

#### Constant `SYM_V_EXPORTED`

```rust
pub const SYM_V_EXPORTED: u16 = 0x4000;
```

#### Constant `C_FILE`

Source file name and compiler information.

```rust
pub const C_FILE: u8 = 103;
```

#### Constant `C_BINCL`

Beginning of include file.

```rust
pub const C_BINCL: u8 = 108;
```

#### Constant `C_EINCL`

Ending of include file.

```rust
pub const C_EINCL: u8 = 109;
```

#### Constant `C_GSYM`

Global variable.

```rust
pub const C_GSYM: u8 = 128;
```

#### Constant `C_STSYM`

Statically allocated symbol.

```rust
pub const C_STSYM: u8 = 133;
```

#### Constant `C_BCOMM`

Beginning of common block.

```rust
pub const C_BCOMM: u8 = 135;
```

#### Constant `C_ECOMM`

End of common block.

```rust
pub const C_ECOMM: u8 = 137;
```

#### Constant `C_ENTRY`

Alternate entry.

```rust
pub const C_ENTRY: u8 = 141;
```

#### Constant `C_BSTAT`

Beginning of static block.

```rust
pub const C_BSTAT: u8 = 143;
```

#### Constant `C_ESTAT`

End of static block.

```rust
pub const C_ESTAT: u8 = 144;
```

#### Constant `C_GTLS`

Global thread-local variable.

```rust
pub const C_GTLS: u8 = 145;
```

#### Constant `C_STTLS`

Static thread-local variable.

```rust
pub const C_STTLS: u8 = 146;
```

#### Constant `C_DWARF`

DWARF section symbol.

```rust
pub const C_DWARF: u8 = 112;
```

#### Constant `C_LSYM`

Automatic variable allocated on stack.

```rust
pub const C_LSYM: u8 = 129;
```

#### Constant `C_PSYM`

Argument to subroutine allocated on stack.

```rust
pub const C_PSYM: u8 = 130;
```

#### Constant `C_RSYM`

Register variable.

```rust
pub const C_RSYM: u8 = 131;
```

#### Constant `C_RPSYM`

Argument to function or procedure stored in register.

```rust
pub const C_RPSYM: u8 = 132;
```

#### Constant `C_ECOML`

Local member of common block.

```rust
pub const C_ECOML: u8 = 136;
```

#### Constant `C_FUN`

Function or procedure.

```rust
pub const C_FUN: u8 = 142;
```

#### Constant `C_EXT`

External symbol.

```rust
pub const C_EXT: u8 = 2;
```

#### Constant `C_WEAKEXT`

Weak external symbol.

```rust
pub const C_WEAKEXT: u8 = 111;
```

#### Constant `C_NULL`

Symbol table entry marked for deletion.

```rust
pub const C_NULL: u8 = 0;
```

#### Constant `C_STAT`

Static.

```rust
pub const C_STAT: u8 = 3;
```

#### Constant `C_BLOCK`

Beginning or end of inner block.

```rust
pub const C_BLOCK: u8 = 100;
```

#### Constant `C_FCN`

Beginning or end of function.

```rust
pub const C_FCN: u8 = 101;
```

#### Constant `C_HIDEXT`

Un-named external symbol.

```rust
pub const C_HIDEXT: u8 = 107;
```

#### Constant `C_INFO`

Comment string in .info section.

```rust
pub const C_INFO: u8 = 110;
```

#### Constant `C_DECL`

Declaration of object (type).

```rust
pub const C_DECL: u8 = 140;
```

#### Constant `C_AUTO`

Automatic variable.

```rust
pub const C_AUTO: u8 = 1;
```

#### Constant `C_REG`

Register variable.

```rust
pub const C_REG: u8 = 4;
```

#### Constant `C_EXTDEF`

External definition.

```rust
pub const C_EXTDEF: u8 = 5;
```

#### Constant `C_LABEL`

Label.

```rust
pub const C_LABEL: u8 = 6;
```

#### Constant `C_ULABEL`

Undefined label.

```rust
pub const C_ULABEL: u8 = 7;
```

#### Constant `C_MOS`

Member of structure.

```rust
pub const C_MOS: u8 = 8;
```

#### Constant `C_ARG`

Function argument.

```rust
pub const C_ARG: u8 = 9;
```

#### Constant `C_STRTAG`

Structure tag.

```rust
pub const C_STRTAG: u8 = 10;
```

#### Constant `C_MOU`

Member of union.

```rust
pub const C_MOU: u8 = 11;
```

#### Constant `C_UNTAG`

Union tag.

```rust
pub const C_UNTAG: u8 = 12;
```

#### Constant `C_TPDEF`

Type definition.

```rust
pub const C_TPDEF: u8 = 13;
```

#### Constant `C_USTATIC`

Undefined static.

```rust
pub const C_USTATIC: u8 = 14;
```

#### Constant `C_ENTAG`

Enumeration tag.

```rust
pub const C_ENTAG: u8 = 15;
```

#### Constant `C_MOE`

Member of enumeration.

```rust
pub const C_MOE: u8 = 16;
```

#### Constant `C_REGPARM`

Register parameter.

```rust
pub const C_REGPARM: u8 = 17;
```

#### Constant `C_FIELD`

Bit field.

```rust
pub const C_FIELD: u8 = 18;
```

#### Constant `C_EOS`

End of structure.

```rust
pub const C_EOS: u8 = 102;
```

#### Constant `C_ALIAS`

Duplicate tag.

```rust
pub const C_ALIAS: u8 = 105;
```

#### Constant `C_HIDDEN`

Special storage class for external.

```rust
pub const C_HIDDEN: u8 = 106;
```

#### Constant `C_EFCN`

Physical end of function.

```rust
pub const C_EFCN: u8 = 255;
```

#### Constant `C_TCSYM`

Reserved.

```rust
pub const C_TCSYM: u8 = 134;
```

#### Constant `XFT_FN`

Specifies the source-file name.

```rust
pub const XFT_FN: u8 = 0;
```

#### Constant `XFT_CT`

Specifies the compiler time stamp.

```rust
pub const XFT_CT: u8 = 1;
```

#### Constant `XFT_CV`

Specifies the compiler version number.

```rust
pub const XFT_CV: u8 = 2;
```

#### Constant `XFT_CD`

Specifies compiler-defined information.

```rust
pub const XFT_CD: u8 = 128;
```

#### Constant `XTY_ER`

External reference.

```rust
pub const XTY_ER: u8 = 0;
```

#### Constant `XTY_SD`

Csect definition for initialized storage.

```rust
pub const XTY_SD: u8 = 1;
```

#### Constant `XTY_LD`

Defines an entry point to an initialized csect.

```rust
pub const XTY_LD: u8 = 2;
```

#### Constant `XTY_CM`

Common csect definition. For uninitialized storage.

```rust
pub const XTY_CM: u8 = 3;
```

#### Constant `XMC_PR`

Program Code

```rust
pub const XMC_PR: u8 = 0;
```

#### Constant `XMC_RO`

Read Only Constant

```rust
pub const XMC_RO: u8 = 1;
```

#### Constant `XMC_DB`

Debug Dictionary Table

```rust
pub const XMC_DB: u8 = 2;
```

#### Constant `XMC_GL`

Global Linkage (Interfile Interface Code)

```rust
pub const XMC_GL: u8 = 6;
```

#### Constant `XMC_XO`

Extended Operation (Pseudo Machine Instruction)

```rust
pub const XMC_XO: u8 = 7;
```

#### Constant `XMC_SV`

Supervisor Call (32-bit process only)

```rust
pub const XMC_SV: u8 = 8;
```

#### Constant `XMC_SV64`

Supervisor Call for 64-bit process

```rust
pub const XMC_SV64: u8 = 17;
```

#### Constant `XMC_SV3264`

Supervisor Call for both 32- and 64-bit processes

```rust
pub const XMC_SV3264: u8 = 18;
```

#### Constant `XMC_TI`

Traceback Index csect

```rust
pub const XMC_TI: u8 = 12;
```

#### Constant `XMC_TB`

Traceback Table csect

```rust
pub const XMC_TB: u8 = 13;
```

#### Constant `XMC_RW`

Read Write Data

```rust
pub const XMC_RW: u8 = 5;
```

#### Constant `XMC_TC0`

TOC Anchor for TOC Addressability

```rust
pub const XMC_TC0: u8 = 15;
```

#### Constant `XMC_TC`

General TOC item

```rust
pub const XMC_TC: u8 = 3;
```

#### Constant `XMC_TD`

Scalar data item in the TOC

```rust
pub const XMC_TD: u8 = 16;
```

#### Constant `XMC_DS`

Descriptor csect

```rust
pub const XMC_DS: u8 = 10;
```

#### Constant `XMC_UA`

Unclassified - Treated as Read Write

```rust
pub const XMC_UA: u8 = 4;
```

#### Constant `XMC_BS`

BSS class (uninitialized static internal)

```rust
pub const XMC_BS: u8 = 9;
```

#### Constant `XMC_UC`

Un-named Fortran Common

```rust
pub const XMC_UC: u8 = 11;
```

#### Constant `XMC_TL`

Initialized thread-local variable

```rust
pub const XMC_TL: u8 = 20;
```

#### Constant `XMC_UL`

Uninitialized thread-local variable

```rust
pub const XMC_UL: u8 = 21;
```

#### Constant `XMC_TE`

Symbol mapped at the end of TOC

```rust
pub const XMC_TE: u8 = 22;
```

#### Constant `AUX_EXCEPT`

Identifies an exception auxiliary entry.

```rust
pub const AUX_EXCEPT: u8 = 255;
```

#### Constant `AUX_FCN`

Identifies a function auxiliary entry.

```rust
pub const AUX_FCN: u8 = 254;
```

#### Constant `AUX_SYM`

Identifies a symbol auxiliary entry.

```rust
pub const AUX_SYM: u8 = 253;
```

#### Constant `AUX_FILE`

Identifies a file auxiliary entry.

```rust
pub const AUX_FILE: u8 = 252;
```

#### Constant `AUX_CSECT`

Identifies a csect auxiliary entry.

```rust
pub const AUX_CSECT: u8 = 251;
```

#### Constant `AUX_SECT`

Identifies a SECT auxiliary entry.

```rust
pub const AUX_SECT: u8 = 250;
```

#### Constant `R_POS`

Positive relocation.

```rust
pub const R_POS: u8 = 0x00;
```

#### Constant `R_RL`

Positive indirect load relocation.

```rust
pub const R_RL: u8 = 0x0c;
```

#### Constant `R_RLA`

Positive load address relocation. Modifiable instruction.

```rust
pub const R_RLA: u8 = 0x0d;
```

#### Constant `R_NEG`

Negative relocation.

```rust
pub const R_NEG: u8 = 0x01;
```

#### Constant `R_REL`

Relative to self relocation.

```rust
pub const R_REL: u8 = 0x02;
```

#### Constant `R_TOC`

Relative to the TOC relocation.

```rust
pub const R_TOC: u8 = 0x03;
```

#### Constant `R_TRL`

TOC relative indirect load relocation.

```rust
pub const R_TRL: u8 = 0x12;
```

#### Constant `R_TRLA`

Relative to the TOC or to the thread-local storage base relocation.

```rust
pub const R_TRLA: u8 = 0x13;
```

#### Constant `R_GL`

Global linkage-external TOC address relocation.

```rust
pub const R_GL: u8 = 0x05;
```

#### Constant `R_TCL`

Local object TOC address relocation.

```rust
pub const R_TCL: u8 = 0x06;
```

#### Constant `R_REF`

A non-relocating relocation.

```rust
pub const R_REF: u8 = 0x0f;
```

#### Constant `R_BA`

Branch absolute relocation. References a non-modifiable instruction.

```rust
pub const R_BA: u8 = 0x08;
```

#### Constant `R_BR`

Branch relative to self relocation. References a non-modifiable instruction.

```rust
pub const R_BR: u8 = 0x0a;
```

#### Constant `R_RBA`

Branch absolute relocation. References a modifiable instruction.

```rust
pub const R_RBA: u8 = 0x18;
```

#### Constant `R_RBR`

Branch relative to self relocation. References a modifiable instruction.

```rust
pub const R_RBR: u8 = 0x1a;
```

#### Constant `R_TLS`

General-dynamic reference to TLS symbol.

```rust
pub const R_TLS: u8 = 0x20;
```

#### Constant `R_TLS_IE`

Initial-exec reference to TLS symbol.

```rust
pub const R_TLS_IE: u8 = 0x21;
```

#### Constant `R_TLS_LD`

Local-dynamic reference to TLS symbol.

```rust
pub const R_TLS_LD: u8 = 0x22;
```

#### Constant `R_TLS_LE`

Local-exec reference to TLS symbol.

```rust
pub const R_TLS_LE: u8 = 0x23;
```

#### Constant `R_TLSM`

Module reference to TLS.

```rust
pub const R_TLSM: u8 = 0x24;
```

#### Constant `R_TLSML`

Module reference to the local TLS storage.

```rust
pub const R_TLSML: u8 = 0x25;
```

#### Constant `R_TOCU`

Relative to TOC upper.

```rust
pub const R_TOCU: u8 = 0x30;
```

#### Constant `R_TOCL`

Relative to TOC lower.

```rust
pub const R_TOCL: u8 = 0x31;
```

## Re-exports

### Re-export `common::*`

```rust
pub use common::*;
```

### Re-export `endian::*`

```rust
pub use endian::*;
```

### Re-export `pod::*`

```rust
pub use pod::*;
```

### Re-export `read::*`

**Attributes:**

- `#[<cfg>(feature = "read_core")]`

```rust
pub use read::*;
```

